---
phase: 16-quarterly-schedule-share-link
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CollapsibleSection.vue
  - src/components/__tests__/CollapsibleSection.test.ts
  - .planning/phases/16-quarterly-schedule-share-link/16-SCHEDULE-UX-NOTE.md
autonomous: true
requirements: [R-09, R-10, R-11]
must_haves:
  truths:
    - "A UX research artifact exists in the phase directory recommending a concrete Schedule-page redesign (R-09)"
    - "A shared CollapsibleSection component defaults to expanded and remembers open/closed state per-user via localStorage (R-11, D-17)"
  artifacts:
    - path: ".planning/phases/16-quarterly-schedule-share-link/16-SCHEDULE-UX-NOTE.md"
      provides: "R-09 UX research note + recommended redesign the implementation follows"
      min_lines: 25
    - path: "src/components/CollapsibleSection.vue"
      provides: "Reusable collapsible wrapper with localStorage persistence"
      contains: "localStorage"
  key_links:
    - from: "src/components/CollapsibleSection.vue"
      to: "localStorage"
      via: "storageKey prop read/write of 'open'|'closed'"
      pattern: "localStorage"
---

<objective>
Produce the R-09 UX research artifact (the note whose recommendation the Schedule-page redesign
must follow) and the shared `CollapsibleSection.vue` component that plans 16-06 (Volunteer) and
16-08 (Schedule) consume for R-11 collapsible sections.

Purpose: Decouple the "decide the layout" deliverable (R-09) and the reusable collapse chrome
(R-11) from the two view-redesign plans so those can proceed in Wave 2.
Output: `16-SCHEDULE-UX-NOTE.md` + `CollapsibleSection.vue` (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md
@.planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
CollapsibleSection props (from PATTERNS.md): defineProps<{ title: string; storageKey: string }>()
Default state: expanded (open). Persistence: localStorage[storageKey] = 'open' | 'closed'.
UI-SPEC contract: dark theme only; reuse ArrangementAccordion chevron (h-4 w-4 text-gray-400,
rotate-90 when open) + bg-gray-800 header bar; header title weight 600 semibold; header padding
p-4 (16px) — do NOT carry over ArrangementAccordion's 12px vertical padding or its 700/bold title.
Body slot rendered via `<slot />`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the R-09 Schedule-page UX research note</name>
  <files>.planning/phases/16-quarterly-schedule-share-link/16-SCHEDULE-UX-NOTE.md</files>
  <read_first>
    - .planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md (Pattern 5 "Schedule-Page UX Redesign — Concrete Recommendation" — the evaluated options + recommendation this note formalizes)
    - .planning/phases/16-quarterly-schedule-share-link/16-CONTEXT.md (D-11/D-12/D-13 — layout open, editing-grid format, add-quarter flow)
  </read_first>
  <action>
    Write a concise UX note (the R-09 artifact acceptance requires "a research artifact file
    exists in the phase directory" that the shipped redesign reflects). Evaluate the layout
    options on merit (D-11): (a) evolve the existing dark card-stack with collapsible sections vs
    (b) pivot to a calendar/matrix-centric single view. Recommend option (a) — evolve the
    card-stack — citing the concrete evidence from RESEARCH Pattern 5: QuarterGrid is already a
    roles×dates matrix (D-12 resolves to "keep the current date-grouped grid, no reorientation"),
    R-11 collapsible sections imply "declutter the stack" not "replace it," and no calendar
    dependency exists. Specify the concrete redesign the later plans implement: quarter switcher
    separated from a secondary "+ Add quarter" modal (D-13/R-10), and the three setup cards
    (Volunteer Availability, Service dates, Generate controls) wrapped in CollapsibleSection with
    QuarterGrid staying always-visible. Keep it a decision record, not code.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-quarterly-schedule-share-link/16-SCHEDULE-UX-NOTE.md && grep -qi "card-stack\|collapsible\|add quarter" .planning/phases/16-quarterly-schedule-share-link/16-SCHEDULE-UX-NOTE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/16-quarterly-schedule-share-link/16-SCHEDULE-UX-NOTE.md` exists
    - Note explicitly evaluates ≥2 layout options and states a single recommendation
    - Note names the concrete redesign elements (quarter switcher vs Add-quarter modal, collapsible setup sections, always-visible grid) that plans 16-06/16-08 must follow
  </acceptance_criteria>
  <done>R-09 UX research artifact exists in the phase directory with an evaluated recommendation the redesign plans follow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared CollapsibleSection component with localStorage persistence</name>
  <files>src/components/CollapsibleSection.vue, src/components/__tests__/CollapsibleSection.test.ts</files>
  <read_first>
    - src/components/ArrangementAccordion.vue (chevron affordance + v-if body toggle to extract; note it defaults CLOSED — this component defaults OPEN)
    - src/components/__tests__/AvailabilityDrawer.test.ts (component-interaction test shape to mirror)
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md ("Component-Specific Notes" — CollapsibleSection styling contract)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/components/CollapsibleSection.vue")
  </read_first>
  <action>
    Write `CollapsibleSection.test.ts` FIRST: mounts with a `title` + `storageKey`, asserts the
    body slot is visible by default (default expanded, D-17); clicking the header toggles the body
    hidden and writes `localStorage[storageKey] === 'closed'`; remounting with that localStorage
    value starts collapsed; clicking again writes `'open'`. Then implement `CollapsibleSection.vue`
    (Composition API, `<script setup lang="ts">`) with `defineProps<{ title: string; storageKey:
    string }>()`. Initialize `isOpen` from `localStorage[storageKey]` (default `true` when absent).
    Header: `bg-gray-800` bar, `p-4` padding, title at `text-sm font-semibold text-gray-100`
    (600 — NOT bold/700), chevron svg `h-4 w-4 text-gray-400 transition-transform` with
    `:class="isOpen ? 'rotate-90' : ''"` (copy the ArrangementAccordion path). Body: `<div
    v-if="isOpen"><slot /></div>`. On toggle, persist `localStorage.setItem(storageKey, isOpen ?
    'open' : 'closed')`. Dark theme only — this component is never used on the public share page.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/CollapsibleSection.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/CollapsibleSection.test.ts` passes (default-open, toggle-persists, restore-from-storage)
    - `grep -c "font-bold\|font-semibold" src/components/CollapsibleSection.vue` shows semibold (600) used, not bold (700)
    - `grep -n "localStorage" src/components/CollapsibleSection.vue` shows both a read (init) and a write (toggle)
    - Component uses `defineProps<{ title: string; storageKey: string }>()` and a `<slot />` body
  </acceptance_criteria>
  <done>CollapsibleSection renders a titled, chevron-toggled section defaulting expanded, remembering state per storageKey; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component ↔ localStorage | Per-device UI preference only; no security-sensitive data crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering | localStorage collapse-state key collision across sections | mitigate | Each usage passes a distinct namespaced storageKey (`schedule.section.*` / `roster.section.*`) — documented in UI-SPEC; component is agnostic |
| T-16-03-02 | Information Disclosure | UX note or component leaking data | accept | Note is a design decision record; component stores only 'open'/'closed' — no data exposure |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/CollapsibleSection.test.ts` green.
- UX note file present in phase directory.
</verification>

<success_criteria>
- R-09 UX research artifact exists with an evaluated recommendation.
- CollapsibleSection defaults expanded, remembers per-user state, matches UI-SPEC styling.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-03-SUMMARY.md` when done.
</output>
