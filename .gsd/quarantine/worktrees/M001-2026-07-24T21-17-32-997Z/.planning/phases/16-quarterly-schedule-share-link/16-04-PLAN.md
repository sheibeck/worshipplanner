---
phase: 16-quarterly-schedule-share-link
plan: 04
type: tdd
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/utils/scheduler.ts
  - src/utils/__tests__/scheduler.test.ts
autonomous: true
requirements: [R-12, R-05]
must_haves:
  truths:
    - "The scheduler reads per-role frequency from PersonQuarterData.roleFrequency (not standing Person fields) (D-04)"
    - "A must-serve-with partner is pulled in only when doing so stays within their own per-role cadence budget (D-01/D-02)"
    - "Every date the lower-cadence partner serves also has the higher-cadence anchor serving (containment); the anchor's extra dates do not drag the partner in (D-01)"
    - "Cadence-driven skips are silent — not added to pairingConflicts (D-03)"
    - "Existing hard constraints (blackout, group-compatibility via isGroupCompatible) remain honored"
  artifacts:
    - path: "src/utils/scheduler.ts"
      provides: "propagatePairing cadence-budget gate + roleFrequency-sourced tier/cadence reads"
      contains: "roleFrequency"
    - path: "src/utils/__tests__/scheduler.test.ts"
      provides: "Rewritten factories + Nolan/Tim containment, even-spread, silent-skip tests"
      contains: "roleFrequency"
  key_links:
    - from: "src/utils/scheduler.ts::propagatePairing"
      to: "PersonQuarterData.roleFrequency"
      via: "remaining-cadence-budget filter before assignToRole"
      pattern: "roleFrequency"
---

<objective>
Fix R-12 (`scheduler.ts::propagatePairing`): a must-serve-with pairing must honor each person's
own per-role frequency instead of dragging the partner onto every date the anchor serves. Repoint
the scheduler's tier/cadence reads to the new quarter-scoped `roleFrequency` (D-04) and gate
partner pull-in by a remaining-cadence budget (D-01/D-02), silently (D-03).

Purpose: Nolan (once/month, must-serve-with Tim) serves ~once/month, always paired with a Tim
date; Tim (twice/month) keeps his extra dates alone.
Output: Updated `scheduler.ts` + rewritten/extended `scheduler.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
<!-- Current scheduler read sites (grep-confirmed) that must repoint to roleFrequency: -->
- tierOf (line ~91-92): reads `pqdById.get(id)?.roleTiers?.[roleId] ?? ?.frequencyTier ?? 'regular'`
  → becomes `pqdById.get(id)?.roleFrequency?.[roleId]?.tier ?? 'regular'`
- cadence-N (line ~214): `const n = p.roleFrequencies?.[roleId] ?? p.frequencyTargetN`
  → becomes `const n = pqdById.get(p.id)?.roleFrequency?.[roleId]?.n ?? 4`
- getServedByRole(personId, roleId) (line ~101) — existing running-count helper, reuse as-is
- isGroupCompatible / evaluateGroupCombo (lines 20-53) — shared helper, keep intact, do NOT duplicate
- propagatePairing (lines 143-184): existing filter chain notOutTier → eligibleRoles(isGroupCompatible) → withCapacity → assignToRole → recurse

RoleFrequencyEntry (from 16-01): { tier: FrequencyTier; n: number }; default { tier:'regular', n:4 }.
Budget formula (RESEARCH Pattern 2 / A5): roleBudget = Math.ceil(serviceDates.length / n).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint scheduler reads to roleFrequency; rewrite test factories (keep suite green)</name>
  <files>src/utils/scheduler.ts, src/utils/__tests__/scheduler.test.ts</files>
  <read_first>
    - src/utils/scheduler.ts (tierOf ~91-92, cadence-N ~214, propagatePairing ~143-184, deficit model ~210-233)
    - src/utils/__tests__/scheduler.test.ts (makePerson/makePQD factories ~6-32 — currently model cadence on Person, tier on PQD)
    - .planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md (Pitfall 3 — the factory rewrite is a dedicated task, not a drive-by)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/utils/scheduler.ts")
  </read_first>
  <action>
    This task is a pure repoint + fixture migration with NO behavior change yet. Rewrite the
    `makePerson`/`makePQD` test factories so per-role frequency is expressed on
    `PersonQuarterData.roleFrequency` (`Record<roleId, {tier, n}>`) instead of `Person.frequencyTargetN`/
    `roleFrequencies` and `PersonQuarterData.roleTiers`/`frequencyTier`. Update the ~20 existing
    test cases to construct data via the new factory shape. In `scheduler.ts`, repoint `tierOf`
    to `pqdById.get(id)?.roleFrequency?.[roleId]?.tier ?? 'regular'` and the cadence-N read to
    `pqdById.get(p.id)?.roleFrequency?.[roleId]?.n ?? 4`. Do not yet add the pairing gate. Keep
    `isGroupCompatible`, `getServedByRole`, and the deficit-scoring formula untouched. Goal: the
    full existing suite passes against the new field with identical scheduling behavior.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/scheduler.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/scheduler.test.ts` passes (all pre-existing cases green under new factories)
    - `grep -c "roleTiers\|frequencyTier\|frequencyTargetN\|roleFrequencies" src/utils/scheduler.ts` === 0 (all legacy reads repointed)
    - `grep -n "roleFrequency" src/utils/scheduler.ts` shows tier and n both sourced from roleFrequency
    - `isGroupCompatible` still called (not duplicated) — `grep -c "isGroupCompatible" src/utils/scheduler.ts` unchanged from before (>=2)
  </acceptance_criteria>
  <done>Scheduler + tests read frequency from roleFrequency; existing behavior preserved; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate propagatePairing by remaining cadence budget + containment tests</name>
  <files>src/utils/scheduler.ts, src/utils/__tests__/scheduler.test.ts</files>
  <read_first>
    - src/utils/scheduler.ts (propagatePairing filter chain after Task 1's repoint)
    - .planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md (Pattern 2 exact roleBudget/withinCadence gate; Pitfall 4 — the containment edge case & test guidance; Open Question 1)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/utils/scheduler.ts" — insert gate between eligibleRoles and assignToRole)
  </read_first>
  <action>
    Add failing tests FIRST for the canonical case: Nolan (roleFrequency n=4 ≈ once/month,
    pairedWith Tim) + Tim (n=2 ≈ twice/month), sharing an overlapping role set (per Pitfall 4 —
    construct the test so Nolan's role is one Tim also holds, matching the real "co-vocalist /
    parent-child same role" shape). Assert: (a) containment across the FULL calendar — every date
    Nolan serves also has Tim serving; (b) Nolan's served count ≈ ceil(dates/4), not inflated to
    Tim's cadence; (c) Nolan's occurrences are spread (not all front-loaded); (d) the pairingConflicts
    list contains NO entry for the dates Tim serves alone (silent skip, D-03). Then implement: inside
    `propagatePairing`, after the existing `eligibleRoles` (group-compatible) computation and before
    `assignToRole`, add `roleFrequencyOf`/`roleBudget` (Math.ceil(serviceDates.length / n)) and filter
    `eligibleRoles` to `withinCadence = eligibleRoles.filter(r => getServedByRole(partnerId, r.roleId)
    < roleBudget(partnerId, r.roleId))`. If `withinCadence` is empty, `continue` (silent skip — do NOT
    push to pairingConflicts). Otherwise pick `withCapacity ?? withinCadence[0]`, assignToRole, recurse.
    Keep every existing hard-constraint check (blackout, roleMatchesByName, notOutTier→conflict,
    isGroupCompatible→conflict) exactly as-is. Document the residual edge case from Pitfall 4/Open
    Question 1 (main-loop independent selection) in a code comment as a consciously-accepted scope
    boundary — the propagation-only gate satisfies the canonical acceptance case.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/scheduler.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/scheduler.test.ts` passes including the new Nolan/Tim containment, count (~ceil(dates/4)), even-spread, and silent-skip tests
    - Containment test asserts across the ENTIRE calendar (every Nolan date ⇒ Tim serves that date), not only dates Tim already served
    - `grep -n "roleBudget\|withinCadence" src/utils/scheduler.ts` shows the new gate present between eligibleRoles and assignToRole
    - No new `pairingConflicts.push` was added for cadence-driven skips (the silent-skip path uses `continue`, grep-verify no conflict push in that branch)
    - Full unit suite `npx vitest run` stays green
  </acceptance_criteria>
  <done>propagatePairing honors per-role cadence with containment + even-spread; skips silently; all scheduler tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure function (no I/O) | scheduler.ts takes in-memory quarter data, returns a proposal; no network/DB/user-input boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | pairing gate silently over-constraining (partner never scheduled) | mitigate | Deterministic unit tests assert the partner IS scheduled ~ceil(dates/n) times with containment — regression-guards both over- and under-pulling |
| T-16-04-02 | Denial of Service | infinite recursion on cyclic pairings | accept | Existing `visited` Set already guards recursion cycles (unchanged); gate only shrinks the candidate set, never adds recursion paths |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/scheduler.test.ts` green.
- `npx vitest run` (full suite) green.
</verification>

<success_criteria>
- Scheduler sources frequency from roleFrequency; propagatePairing gates by cadence budget with containment + even-spread; skips silently; existing constraints intact; suite green.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-04-SUMMARY.md` when done.
</output>
