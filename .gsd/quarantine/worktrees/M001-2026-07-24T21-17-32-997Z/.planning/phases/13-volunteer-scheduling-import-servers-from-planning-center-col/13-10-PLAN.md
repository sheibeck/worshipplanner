---
phase: 13-volunteer-scheduling-import-servers-from-planning-center-col
plan: 10
type: execute
wave: 6
depends_on: [13-06, 13-09]
files_modified:
  - src/components/RosterPrintLayout.vue
  - src/views/QuarterShareView.vue
  - src/views/QuarterView.vue
  - src/router/index.ts
autonomous: false
requirements: [D-24, D-25]
must_haves:
  truths:
    - "Covers CONTEXT.md decisions D-24, D-25 (mirrors this plan's requirements)"
    - "A leader can finalize a quarter and get a read-only public share link that renders the schedule without authentication"
    - "A leader can print a clean roster of the finalized schedule (dates × roles × people)"
    - "The share/print surfaces switch to the light print/public theme, not the app dark theme"
    - "The app sends no emails; availability collection stays external"
  artifacts:
    - path: "src/views/QuarterShareView.vue"
      provides: "Public read-only quarter schedule from a share token"
    - path: "src/components/RosterPrintLayout.vue"
      provides: "Print-only roster layout (light theme, break-inside-avoid)"
    - path: "src/router/index.ts"
      provides: "Public /quarter-share/:token route (no auth)"
      contains: "quarter-share"
  key_links:
    - from: "src/views/QuarterShareView.vue"
      to: "shareTokens/{token}"
      via: "getDoc quarterSnapshot"
      pattern: "shareTokens"
    - from: "src/views/QuarterView.vue"
      to: "src/stores/quarters.ts"
      via: "finalizeAndShare"
      pattern: "finalizeAndShare"
---

<objective>
Build the output surfaces (D-24): a printable roster (`RosterPrintLayout.vue`) and a public read-only share view (`QuarterShareView.vue`) reachable via a crypto-random token, plus the "Finalize & Share" + print wiring in `QuarterView.vue` and the public `/quarter-share/:token` route. The app sends no emails (D-25) — availability collection stays external.

Purpose: Let leaders share the finished quarter with volunteers (public link) and print a hard-copy roster, reusing the existing `ShareView.vue` / `ServicePrintLayout.vue` light-theme patterns exactly.

Output: one print component, one public view, QuarterView wiring, one public route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-UI-SPEC.md
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-PATTERNS.md

<interfaces>
```typescript
import { useQuartersStore } from '@/stores/quarters'   // finalizeAndShare(quarterId) → token
import { getDoc, doc } from 'firebase/firestore'
import { db } from '@/firebase'
// quarterSnapshot shape written by finalizeAndShare (Plan 06):
//   { label, serviceDates: string[], roles: {id,name,group}[], calendar: Record<date, Record<roleId, personName[]>> }
```
</interfaces>

Public token-load pattern to copy: @src/views/ShareView.vue lines 136-151 (getDoc shareTokens/{token}, notFound handling, light theme).
Print wrapper to copy (`hidden print:block bg-white text-gray-900`, `break-inside-avoid`): @src/components/ServicePrintLayout.vue lines 1-21.
Public route shape (NO meta.requiresAuth): @src/router/index.ts lines 66-70 (/share/:token).
UI-SPEC: print/share use the LIGHT palette (`bg-white text-gray-900 font-sans`, `text-xs text-gray-500 uppercase tracking-wider` section labels); "Finalize & Share" CTA copy.
D-25: no email-sending code anywhere.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RosterPrintLayout + print wiring</name>
  <files>src/components/RosterPrintLayout.vue, src/views/QuarterView.vue</files>
  <read_first>
    - src/components/ServicePrintLayout.vue (whole file — print-only wrapper, header, break-inside-avoid rows, light theme)
    - src/views/QuarterView.vue (where to add the print button + pass the finalized quarter)
    - 13-UI-SPEC.md (print/share light-palette note)
  </read_first>
  <action>Create `src/components/RosterPrintLayout.vue` mirroring `ServicePrintLayout.vue`: a `hidden print:block bg-white text-gray-900 font-sans text-sm p-8` wrapper with a header (quarter label) and one printable block per service date (weekday + date heading, then that date's roles → assigned people), each date row `break-inside-avoid` so dates don't split across pages. Take the quarter (or quarterSnapshot-equivalent) as a prop and resolve person names via the quarter.calendar + roster. Section labels use `text-xs text-gray-500 uppercase tracking-wider`. In `QuarterView.vue`, add a "Print" button (visible for a generated/finalized quarter) that calls `window.print()`, and mount `<RosterPrintLayout :quarter="selectedQuarter" />` (hidden on screen, shown in print). Do not restyle the on-screen dark grid — the print layout is a separate print-only DOM.</action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | grep -E "RosterPrintLayout|QuarterView" | grep -qi error && echo HAS_ERR || echo TSOK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --build` clean for RosterPrintLayout.vue + QuarterView.vue (prints TSOK)
    - RosterPrintLayout uses `hidden print:block` + `bg-white` (light theme) + `break-inside-avoid` (grep all three)
    - QuarterView has a Print button calling `window.print()` and mounts RosterPrintLayout
    - No dark-palette classes (`bg-gray-9`) inside the print layout wrapper
  </acceptance_criteria>
  <done>A print-only light-theme roster renders dates × roles × people without splitting dates across pages.</done>
</task>

<task type="auto">
  <name>Task 2: QuarterShareView (public) + finalize-and-share wiring + public route</name>
  <files>src/views/QuarterShareView.vue, src/views/QuarterView.vue, src/router/index.ts</files>
  <read_first>
    - src/views/ShareView.vue (whole file — token load via getDoc, notFound/loading states, light read-only render)
    - src/stores/quarters.ts (finalizeAndShare + quarterSnapshot shape)
    - src/router/index.ts (public /share/:token route with no requiresAuth)
    - 13-UI-SPEC.md ("Finalize & Share" copy; light public theme)
  </read_first>
  <action>Create `src/views/QuarterShareView.vue` mirroring `ShareView.vue`: on mount read `route.params.token`, `getDoc(doc(db,'shareTokens',token))`, set `notFound` if missing, else read `snap.data().quarterSnapshot`; render a light-theme (`bg-white text-gray-900`) read-only schedule — quarter label header, then each service date with its roles → assigned person NAMES (from quarterSnapshot.calendar, which already carries names, so NO roster/auth access is needed). Handle loading + notFound like ShareView. In `QuarterView.vue`, add a "Finalize & Share" button that calls `quartersStore.finalizeAndShare(selectedQuarter.id)`, then displays the resulting public URL (`/quarter-share/{token}`) with a copy-to-clipboard affordance (mirror existing share-link UI). Register the public route in router/index.ts: `{ path: '/quarter-share/:token', name: 'quarter-share', component: () => import('../views/QuarterShareView.vue') }` with NO meta (public, like /share/:token). Ensure NO email-sending code is added anywhere (D-25).</action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | grep -E "QuarterShareView|QuarterView|router/index" | grep -qi error && echo HAS_ERR || echo TSOK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --build` clean for QuarterShareView.vue + QuarterView.vue + router/index.ts (prints TSOK)
    - router/index.ts has a '/quarter-share/:token' route with NO `requiresAuth`/`meta` (public)
    - QuarterShareView reads shareTokens/{token} and renders `quarterSnapshot` names without importing roster/auth stores (grep: no useRosterStore/useAuthStore in the share view)
    - QuarterView calls `finalizeAndShare` and shows a copyable /quarter-share/{token} URL; "Finalize & Share" string present
    - No email/SMTP/sendMail code anywhere in the four files (grep -ci "sendmail\|smtp\|nodemailer\|sendEmail" == 0) (D-25)
  </acceptance_criteria>
  <done>Finalizing yields a public, auth-free read-only share link rendering the schedule from a self-contained snapshot; no emails are sent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify print + public share link</name>
  <action>After all automation in this plan is complete, the human verifies the print output and public share link behaviors listed below. This checkpoint runs after the build.</action>
  <what-built>Printable roster + public read-only share link for a finalized quarter, wired into QuarterView with a Finalize & Share and Print action.</what-built>
  <how-to-verify>
    1. `npm run dev`, go to /schedule with a generated quarter. Click "Finalize & Share" and copy the /quarter-share/{token} URL.
    2. Open that URL in a private/incognito window (unauthenticated) — confirm the schedule renders read-only in a light theme with dates → roles → people names, and no editing controls.
    3. Change the token to a random string and confirm a not-found state.
    4. Back in /schedule, click "Print" and confirm the print preview shows a clean light-theme roster (dates × roles × people) with dates not split across page breaks.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → shareTokens/{token} | Anyone with the token reads the quarterSnapshot unauthenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10-01 | Information Disclosure | Public share link enumeration/guessing | mitigate | 144-bit crypto-random token (Plan 06); existing `shareTokens` Firestore rule allows public read of a single token doc only, create restricted to editors, update/delete false (RESEARCH Security) |
| T-13-10-02 | Information Disclosure | Public snapshot leaking emails/phones/blackouts | mitigate | quarterSnapshot carries only names + roles + calendar (Plan 06 T-13-06-02); the share view reads ONLY the snapshot — never the roster/quarter PII collections |
| T-13-10-03 | Elevation of Privilege | Public share route accidentally exposing an authenticated view | mitigate | Route has no meta and the view imports no roster/auth store — it cannot read org-scoped collections even if a snapshot field is missing |
| T-13-10-04 | Repudiation | Assumed in-app email sending (D-25 out of scope) | accept | No email code is added; availability collection is external by design (D-25) — grep-gated absence |
</threat_model>

<verification>
- `npx vue-tsc --build` clean for all four files
- Human checkpoint approved (public link + print)
- Public route auth-free; share view reads snapshot-only; no email code
</verification>

<success_criteria>
Leaders can finalize a quarter into a public read-only share link and print a clean roster (D-24), both using the light print/public theme, with the public view reading a self-contained snapshot and no email sending anywhere (D-25).
</success_criteria>

<output>
Create `.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-10-SUMMARY.md` when done.
</output>
