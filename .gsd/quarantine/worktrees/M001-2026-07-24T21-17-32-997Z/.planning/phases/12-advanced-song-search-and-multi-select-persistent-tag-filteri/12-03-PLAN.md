---
phase: 12-advanced-song-search-and-multi-select-persistent-tag-filteri
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/components/TagFilterChecklist.vue
  - src/components/SongFilters.vue
  - src/views/SongsView.vue
autonomous: true
requirements: [D-06, D-08, D-09, D-10, D-11, D-14]
must_haves:
  truths:
    - "The Songs panel shows a single scrollable checkbox list of user tags in place of the two old Show-only/Hide dropdowns"
    - "Checking a tag filters the song list to songs carrying that tag (OR across checked tags)"
    - "Toggling 'Hide tags' inverts the filter so checked tags are hidden instead"
    - "'Clear tags' unchecks all and turns Hide off, without touching the search box or VW-type/Key dropdowns"
    - "The VW-type, Key, and team-tag dropdowns remain present (D-06)"
    - "When the catalog has no user tags, the checklist shows 'No tags yet'"
  artifacts:
    - path: "src/components/TagFilterChecklist.vue"
      provides: "Shared presentational tag checklist (v-model checkedTags + hide, clear emit)"
      min_lines: 40
      contains: "update:checkedTags"
    - path: "src/components/SongFilters.vue"
      provides: "Songs-panel filter row with the two user-tag selects replaced by TagFilterChecklist"
      contains: "TagFilterChecklist"
  key_links:
    - from: "src/views/SongsView.vue"
      to: "songStore.tagFilterChecked / tagFilterHide / clearTagFilter"
      via: "v-model + @clear through SongFilters to TagFilterChecklist"
      pattern: "tagFilterChecked"
    - from: "src/components/SongFilters.vue"
      to: "src/components/TagFilterChecklist.vue"
      via: "import + mount"
      pattern: "TagFilterChecklist"
---

<objective>
Create the single shared `TagFilterChecklist.vue` control (D-07/D-14) and wire it into the Songs panel: remove the two user-tag `<select>`s from `SongFilters.vue` and mount the checklist in their place, bound through `SongsView.vue` to the store's shared tag state. Keep the VW-type/Key/team-tag dropdowns (D-06).

Purpose: A single, consistent, multi-select tag filter with a Hide toggle and Clear action, driven by the shared store state from Plan 02.
Output: New `TagFilterChecklist.vue`, edited `SongFilters.vue`, edited `SongsView.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UI-SPEC.md

<interfaces>
Shared store state (from Plan 02, src/stores/songs.ts):
- tagFilterChecked: Ref<Set<string>>   (bind v-model:checkedTags)
- tagFilterHide: Ref<boolean>          (bind v-model:hide)
- clearTagFilter(): void               (call on @clear)

SongsView.vue already computes availableUserTags (distinct sorted Song.tags):
```typescript
const availableUserTags = computed(() => {
  const tags = new Set<string>()
  songStore.songs.forEach((song) => { (song.tags ?? []).forEach((tag) => tags.add(tag)) })
  return Array.from(tags).sort()
})
```

TeamTagPill.vue pink user identity (static class map — replicate the static-class discipline so Tailwind v4 does not purge):
```
user: 'bg-pink-900/50 text-pink-300 border-pink-800'
```

SongFilters.vue current props/emits include filterTagInclude, filterTagExclude, update:filterTagInclude, update:filterTagExclude (lines 96-97, 108-109) — REMOVE these. The two user-tag `<select>`s are lines 67-85 (the `focus:ring-pink-500` ones) — REMOVE and replace with the checklist mount. Keep search input + VW-type/Key/team-tag selects.

SongsView.vue current binding block (lines 65-75) passes v-model:filterTagInclude / v-model:filterTagExclude — REMOVE those two lines; add checklist bindings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared TagFilterChecklist.vue (presentational, v-model)</name>
  <files>src/components/TagFilterChecklist.vue</files>
  <read_first>
    - src/components/SongFilters.vue (emit/prop convention to copy — defineProps/defineEmits + `update:X` pattern)
    - src/components/TeamTagPill.vue (pink `user` variant static class map)
    - src/components/SongSlotPicker.vue lines 156-162 (muted empty-state block to copy) and line 19 ("Change song" gray link style)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md (TagFilterChecklist section lines 154-207)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UI-SPEC.md (Tag checklist interaction contract lines 112-117; Typography lines 55-65 — TWO weights only, 400/600, NO font-medium)
  </read_first>
  <action>
    Create src/components/TagFilterChecklist.vue as a `<script setup lang="ts">` SFC. It is PRESENTATIONAL — it must NOT import the store; it binds via v-model at its call sites so it stays reusable in both surfaces (D-07/D-14).

    Props/emits:
    ```typescript
    const props = defineProps<{
      availableUserTags: string[]
      checkedTags: Set<string>
      hide: boolean
    }>()
    const emit = defineEmits<{
      'update:checkedTags': [value: Set<string>]
      'update:hide': [value: boolean]
      clear: []
    }>()
    function toggleTag(tag: string) {
      const next = new Set(props.checkedTags)   // immutable update so Vue reactivity fires
      next.has(tag) ? next.delete(tag) : next.add(tag)
      emit('update:checkedTags', next)
    }
    ```

    Template structure:
    1. A header row containing the "Hide tags" toggle and the "Clear tags" link:
       - "Hide tags" = a checkbox + label. Label text EXACTLY `Hide tags`, `title="Invert: hide checked tags instead of showing only them"`. When ON, indigo-accented (checkbox `text-indigo-600` / `focus:ring-indigo-500`; and show a persistent whole-list cue such as a small `(hiding)` caption `text-xs text-indigo-300` next to the header when `hide` is true, so inverted mode is legible even if the toggle scrolls out of view). Bind via `:checked="hide" @change="$emit('update:hide', ($event.target as HTMLInputElement).checked)"`.
       - "Clear tags" = plain gray text link, class EXACTLY `text-xs text-gray-500 hover:text-gray-300 transition-colors`, label `Clear tags`, `title="Clear tag filter"`, `@click="$emit('clear')"`. Do NOT accent-color it.
    2. The scrollable list container: `max-h-48 overflow-y-auto` bounded scroll. Each tag = a row (`<label>`), `py-1` density, containing a checkbox + the tag text. Checkbox: `:checked="checkedTags.has(tag)" @change="toggleTag(tag)"`, `focus:outline-none focus:ring-1 focus:ring-indigo-500`. Checked row identity uses the pink user-tag palette via a STATIC class binding (do NOT build the class string dynamically): apply `border-pink-800 bg-pink-900/50 text-pink-300` when `checkedTags.has(tag)`, else `border-gray-700 bg-gray-800 text-gray-300`. Use a computed helper or a `:class="checkedTags.has(tag) ? 'border-pink-800 bg-pink-900/50 text-pink-300' : 'border-gray-700 bg-gray-800 text-gray-300'"` (both literal strings present in source → Tailwind v4 keeps them).
    3. Empty state (when `availableUserTags.length === 0`): centered `px-4 py-6 text-center`; heading `text-sm text-gray-400` reading EXACTLY `No tags yet`; body `text-xs text-gray-500` reading EXACTLY `Add tags to songs in the Songs panel to filter by them here.`

    Typography rule (UI-SPEC): rows/labels use weight 400 (regular); emphasis (heading, toggle-on caption) uses 600 (`font-semibold`). Do NOT use `font-medium` (500) anywhere in this new component.
    Commit `feat(12-03): shared TagFilterChecklist component`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TagFilterChecklist.vue exists and defines emits `update:checkedTags`, `update:hide`, and `clear`
    - Contains literal classes `bg-pink-900/50` AND `text-pink-300` AND `border-pink-800` for the checked state (static strings, not concatenated)
    - Contains `max-h-48 overflow-y-auto`
    - Contains `focus:ring-indigo-500` on the checkbox/toggle controls
    - Contains the exact strings `Hide tags`, `Clear tags`, `No tags yet`, and `Add tags to songs in the Songs panel to filter by them here.`
    - Contains the gray link class `text-xs text-gray-500 hover:text-gray-300 transition-colors` for Clear tags
    - Does NOT contain `font-medium`
    - Does NOT import from '@/stores/songs' (presentational only)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>TagFilterChecklist.vue exists, presentational, pink/indigo per UI-SPEC, type-check clean; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the two user-tag selects in SongFilters.vue for TagFilterChecklist and wire SongsView</name>
  <files>src/components/SongFilters.vue, src/views/SongsView.vue</files>
  <read_first>
    - src/components/SongFilters.vue (full file — remove lines 67-85 selects + props/emits at 96-97, 108-109)
    - src/views/SongsView.vue lines 63-76 (the SongFilters binding block) and the availableUserTags computed (~lines 226-232)
    - src/components/TagFilterChecklist.vue (the component created in Task 1 — its prop/emit contract)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md (SongFilters.vue section lines 210-220; SongsView.vue section lines 253-281)
  </read_first>
  <action>
    A) In src/components/SongFilters.vue:
    - Import the new component: `import TagFilterChecklist from '@/components/TagFilterChecklist.vue'`.
    - Remove the two user-tag `<select>` blocks (lines 67-85, the `focus:ring-pink-500` ones).
    - In their place (still inside the filter area), mount:
      ```html
      <TagFilterChecklist
        :availableUserTags="availableUserTags"
        :checkedTags="tagFilterChecked"
        :hide="tagFilterHide"
        @update:checkedTags="$emit('update:tagFilterChecked', $event)"
        @update:hide="$emit('update:tagFilterHide', $event)"
        @clear="$emit('clearTagFilter')"
      />
      ```
    - In defineProps: remove `filterTagInclude: string` and `filterTagExclude: string`; add `tagFilterChecked: Set<string>` and `tagFilterHide: boolean`. Keep `availableUserTags: string[]`.
    - In defineEmits: remove `'update:filterTagInclude'` and `'update:filterTagExclude'`; add `'update:tagFilterChecked': [value: Set<string>]`, `'update:tagFilterHide': [value: boolean]`, and `clearTagFilter: []`.
    - Keep the search input and the VW-type / Key / team-tag selects untouched (D-06). Keep the search placeholder EXACTLY `Search title, CCLI, theme, tag, category...`; you MAY add a `title="Filter by field: tag: key: type: theme: team:"` attribute to the input but MUST NOT change its height/classes.

    B) In src/views/SongsView.vue, edit the `<SongFilters ... />` block (lines 65-75):
    - Remove `v-model:filterTagInclude="songStore.filterTagInclude"` and `v-model:filterTagExclude="songStore.filterTagExclude"`.
    - Add:
      ```html
      v-model:tagFilterChecked="songStore.tagFilterChecked"
      v-model:tagFilterHide="songStore.tagFilterHide"
      @clearTagFilter="songStore.clearTagFilter()"
      ```
    - Keep `:availableUserTags="availableUserTags"` (computed already exists). No other bindings change.
    Commit `feat(12-03): Songs panel uses shared TagFilterChecklist`.
  </action>
  <verify>
    <automated>npm run type-check && npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "filterTagInclude\|filterTagExclude" src/components/SongFilters.vue src/views/SongsView.vue` returns NO matches
    - src/components/SongFilters.vue imports and mounts `TagFilterChecklist` and emits `update:tagFilterChecked`, `update:tagFilterHide`, `clearTagFilter`
    - src/components/SongFilters.vue still contains the VW-type, Key, and team-tag `<select>` elements (D-06 — grep for `All types`, `All keys`, `All tags`)
    - src/views/SongsView.vue binds `v-model:tagFilterChecked="songStore.tagFilterChecked"`, `v-model:tagFilterHide="songStore.tagFilterHide"`, and `@clearTagFilter="songStore.clearTagFilter()"`
    - Search placeholder text `Search title, CCLI, theme, tag, category...` still present and input classes unchanged
    - `npm run type-check` exits 0 and `npm run build-only` exits 0
  </acceptance_criteria>
  <done>Songs panel renders the shared checklist bound to the store; old selects gone; dropdowns retained; build clean; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → checklist controls | Checkbox/toggle state flows to shared store filter (client-side only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | tag labels rendered in checklist rows | mitigate | Vue text interpolation (`{{ tag }}`) auto-escapes; tags are never rendered via v-html. No XSS surface. |
| T-12-09 | Elevation of Privilege | shared filter state coupling picker ↔ panel | accept | Cross-surface single shared state is an intentional user decision (D-14); no privilege boundary crossed — same user, same session. |
</threat_model>

<verification>
- `npm run type-check` — exits 0
- `npm run build-only` — exits 0
- No `filterTagInclude`/`filterTagExclude` references remain in SongFilters.vue or SongsView.vue
- Manual: Songs panel shows checklist; checking/hiding/clearing behaves per D-09/D-10/D-11
</verification>

<success_criteria>
- Single shared checklist component created and consumed by the Songs panel (D-07/D-08/D-14)
- OR show / Hide invert / Clear behaviors wired to the store (D-09/D-10/D-11)
- VW-type/Key/team-tag dropdowns retained (D-06)
- Pink user-tag identity + indigo focus + empty state per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-03-SUMMARY.md`
</output>
