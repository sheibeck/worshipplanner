---
phase: 14-in-app-quarterly-availability-editor
plan: 03
type: tdd
wave: 2
depends_on: [14-01]
files_modified:
  - src/stores/quarters.ts
  - src/stores/__tests__/quarters.test.ts
autonomous: true
requirements: [D-03, D-05, D-06]   # REQUIREMENTS.md absent — CONTEXT.md decision IDs are the traceability keys
user_setup: []

must_haves:
  truths:
    - "Saving one person's availability writes their blackoutDates/pairedWith/frequencyTier/note without rewriting other people's entries"
    - "Adding a pairing partner reciprocally adds this person's id to that partner's pairedWith"
    - "Removing a pairing partner reciprocally strips this person's id from that partner's pairedWith (add+remove, not add-only)"
    - "Traceability: enforces D-03 (store surface for drawer controls), D-05 (writes quarter-scoped frequencyTier), and D-06 (bidirectional must-serve-with pairing)"
  artifacts:
    - path: "src/stores/quarters.ts"
      provides: "setPersonAvailability(quarterId, personId, {blackoutDates, pairedWith, frequencyTier, note}) + registration in returned store object"
      contains: "setPersonAvailability"
    - path: "src/stores/__tests__/quarters.test.ts"
      provides: "add + remove reciprocal-pairing test cases for setPersonAvailability"
      contains: "setPersonAvailability"
  key_links:
    - from: "src/stores/quarters.ts::setPersonAvailability"
      to: "Firestore quarters/{id}.personQuarterData"
      via: "updateDoc with personQuarterData.{personId} + personQuarterData.{partnerId}.pairedWith dot-paths"
      pattern: "personQuarterData\\.\\$\\{"
---

<objective>
Add the single-person quarter-data write action the drawer calls on Save (D-03), performing a symmetric bidirectional pairing sync — add AND remove reciprocals (D-06) — via targeted Firestore dot-path updates so only the edited person and their pairing partners change.

Purpose: `applyCsvToQuarter`'s existing merge is add-only (CSV never explicitly un-pairs). The drawer's pairing-chip ✕ is an explicit remove, so this needs a genuine `added`/`removed` diff. Isolating it in the store (unit-tested against mocked Firestore) settles the write contract before the drawer consumes it.
Output: `setPersonAvailability` store action + add/remove reciprocal-pairing test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-CONTEXT.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md

<interfaces>
<!-- quarters.ts already imports: updateDoc, doc, serverTimestamp, collection, onSnapshot, addDoc, setDoc, query, orderBy (lines 1-19) -->
<!-- getQuarter(quarterId): Quarter                     (line 59) — read `previous` pairedWith before diffing -->
<!-- orgId: Ref<string|null>                             (line 32) — guard `if (!orgId.value) return` -->
<!-- assignPerson dot-path write convention (lines 189-203): -->
<!--   updateDoc(doc(db,'organizations',orgId.value,'quarters',quarterId), { [`calendar.${date}.${roleId}`]: value, updatedAt: serverTimestamp() }) -->
<!-- FrequencyTier imported from '@/types/roster' (added in Plan 14-01) -->
<!-- Store returns an object at line ~276 listing exposed actions (applyCsvToQuarter at ~286) -->
</interfaces>

**Concurrency note (from 14-RESEARCH Pattern 3):** Write `personQuarterData.${personId}` as ONE whole nested object (safe — only one leader edits one person's drawer at a time). For OTHER people's reciprocal pairing changes, write the narrower `personQuarterData.${partnerId}.pairedWith` dot-path only — never touch a partner's `blackoutDates`/`frequencyTier`/`note`. Never rewrite the whole `personQuarterData` map (that reintroduces the last-write-wins race `T-13-09-02`'s dot-path convention avoids).
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: setPersonAvailability with symmetric add+remove pairing sync (RED→GREEN)</name>
  <files>src/stores/quarters.ts, src/stores/__tests__/quarters.test.ts</files>
  <read_first>
    - src/stores/quarters.ts — `applyCsvToQuarter` (lines 117-153) for the add-only bidirectional merge shape to extend; `assignPerson`/`clearAssignment` (lines 189-218) for the `updateDoc(doc(...), { [dotPath]: value, updatedAt: serverTimestamp() })` convention; `getQuarter` (lines 59-63); the store's returned object (line ~276) where actions are registered
    - src/stores/__tests__/quarters.test.ts — the firebase/firestore mock (lines 20-40, `updateDoc` is a `vi.fn`) and existing action-test structure; assertions inspect the object passed to the mocked `updateDoc`
    - .planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md — Pattern 3 (lines 276-318) for the full worked `added`/`removed` diff implementation and the whole-object-vs-dot-path reasoning
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/stores/quarters.ts` (lines 58-120) for line-accurate analog references
  </read_first>
  <behavior>
    - Own entry: after `setPersonAvailability(qId, 'julia', {blackoutDates:['2026-07-19'], pairedWith:['dean'], frequencyTier:'regular', note:'x'})`, the mocked `updateDoc` is called with a key `personQuarterData.julia` whose value equals `{ personId:'julia', blackoutDates:['2026-07-19'], pairedWith:['dean'], frequencyTier:'regular', note:'x' }` plus `updatedAt`.
    - Reciprocal add: when `'dean'` was NOT previously in julia's pairedWith, the same `updateDoc` payload includes `dean`'s reciprocal — either `personQuarterData.dean` (whole object seeded with defaults) or `personQuarterData.dean.pairedWith` — containing `'julia'`.
    - Reciprocal remove: if julia previously paired with `'lisa'` and the new `pairedWith` omits `'lisa'`, the payload includes `personQuarterData.lisa.pairedWith` with `'julia'` stripped out.
    - Scoping: no key rewrites the entire `personQuarterData` map; partner writes only touch `.pairedWith`.
  </behavior>
  <action>
    RED: add `describe('setPersonAvailability', ...)` cases to `quarters.test.ts` (name includes `"setPersonAvailability"`). Seed a quarter via the existing snapshot-mock harness with julia paired to `['lisa']`, call the action, and assert on the object captured by the mocked `updateDoc` for: own-entry value, reciprocal add of a new partner, and reciprocal remove of a dropped partner.
    GREEN: add `async function setPersonAvailability(quarterId: string, personId: string, data: { blackoutDates: string[]; pairedWith: string[]; frequencyTier: FrequencyTier; note: string }): Promise<void>`. Guard `if (!orgId.value) return`. Read `previous = getQuarter(quarterId).personQuarterData[personId]?.pairedWith ?? []`. Compute `added = data.pairedWith.filter(id => !previous.includes(id))` and `removed = previous.filter(id => !data.pairedWith.includes(id))`. Build an `updates` record starting with `[`personQuarterData.${personId}`]: { personId, ...data }` and `updatedAt: serverTimestamp()`. For each `added` partner not already listing `personId`, set `[`personQuarterData.${partnerId}`]` to a whole object seeded from the partner's existing entry (defaulting `blackoutDates:[]`, `frequencyTier:'regular'`, `note:''`) with `personId` merged into `pairedWith`. For each `removed` partner that has an existing entry, set `[`personQuarterData.${partnerId}.pairedWith`]` to their array with `personId` filtered out. Issue one `updateDoc(doc(db,'organizations',orgId.value,'quarters',quarterId), updates)`. Import `FrequencyTier` from `@/types/roster`. Register `setPersonAvailability` in the store's returned object next to `applyCsvToQuarter`.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/quarters.test.ts -t "setPersonAvailability"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/__tests__/quarters.test.ts -t "setPersonAvailability"` exits 0 (covers own-entry, reciprocal add, reciprocal remove)
    - `npx vitest run src/stores/__tests__/quarters.test.ts` full file exits 0 (no regression)
    - `grep -n "setPersonAvailability" src/stores/quarters.ts` matches BOTH the function definition AND the returned-object registration
    - No `updateDoc` payload key equals the bare `personQuarterData` map (grep the implementation to confirm only `personQuarterData.${...}` dot-paths are written)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>`setPersonAvailability` implemented with symmetric add+remove reciprocal pairing via scoped dot-paths, registered on the store; add/remove tests green; full quarters suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Availability drawer (client) → Firestore `quarters/{id}` | Editor-role user mutates quarter-scoped availability data; write already gated by the existing org/editor Firestore rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering (data loss) | whole-map rewrite clobbering concurrent edits | mitigate | Use targeted `personQuarterData.${id}` / `personQuarterData.${id}.pairedWith` dot-paths (never the whole map), matching the `T-13-09-02` convention; asserted by the no-bare-map-key acceptance criterion. |
| T-14-03-02 | Tampering | stale one-directional pairing ("pairing ghost") | mitigate | Symmetric `added`/`removed` diff writes reciprocal add AND remove; covered by the reciprocal-remove test case (14-RESEARCH Pitfall 2). |
| T-14-03-03 | Elevation of Privilege | unauthorized write to another org's quarter | accept | Existing catch-all `organizations/{orgId}` editor-only Firestore rule (verified present) already gates this collection; no new top-level collection introduced. Re-confirmed by `npm run test:rules` at the phase gate. |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/quarters.test.ts` green (incl. new setPersonAvailability cases)
- `npm run type-check` green
- Implementation writes only scoped dot-paths, never the whole `personQuarterData` map
</verification>

<success_criteria>
- One-person save persists all four quarter-scoped fields plus reciprocal pairing add/remove
- No concurrent-edit-clobbering whole-map write introduced
</success_criteria>

<output>
Create `.planning/phases/14-in-app-quarterly-availability-editor/14-03-SUMMARY.md` when done.
</output>
