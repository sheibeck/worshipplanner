---
phase: 11-song-catalog-service-planner-improvements
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/SongTable.vue
  - src/components/SongFilters.vue
  - src/components/SongSlideOver.vue
  - src/views/SongsView.vue
autonomous: false
requirements: [D-03, D-04, D-06, D-07]
must_haves:
  truths:
    - "Every song-list column (Title, Category, Key, CCLI, Last Used) can be sorted ascending/descending"
    - "Each song-list row shows themes and user tags as pills, visually distinct from team tags"
    - "A user can add/remove tags in the song editor form"
    - "A user can add/remove a tag inline on a song-list row"
    - "A user can select multiple songs and apply/remove a tag to all at once"
    - "The song list can be filtered to hide-by-tag and show-only-by-tag"
  artifacts:
    - path: "src/components/SongTable.vue"
      provides: "all-column sort, themes/tags pills, inline tag edit, bulk-select column"
      contains: "'title' | 'category' | 'key' | 'ccli' | 'lastUsed'"
    - path: "src/components/SongFilters.vue"
      provides: "include/exclude user-tag filter controls"
      contains: "filterTagInclude"
    - path: "src/components/SongSlideOver.vue"
      provides: "user-tag editing in the song form"
      contains: "toggleUserTag"
    - path: "src/views/SongsView.vue"
      provides: "availableUserTags + bulk-tag action host wiring"
      contains: "availableUserTags"
  key_links:
    - from: "src/components/SongFilters.vue"
      to: "songStore.filterTagInclude / filterTagExclude"
      via: "v-model on include/exclude selects"
      pattern: "filterTagInclude|filterTagExclude"
    - from: "src/components/SongTable.vue inline/bulk tag edit"
      to: "songStore.updateSong"
      via: "tag mutation persist"
      pattern: "updateSong\\("
---

<objective>
Deliver the song-catalog browsing UX: all-column sorting (D-07), themes + user-tag pills distinct from team tags (D-06), tag editing in three places — editor form, inline row, and bulk multi-select (D-04) — and hide/show tag filtering in the song list (D-03 list side).

Purpose: Surfaces existing metadata that was stored but never shown, and gives the user full control over tagging and filtering their catalog.
Output: A SongTable with sortable columns, three pill types, inline + bulk tag editing; a SongFilters with include/exclude tag controls; a SongSlideOver form with tag editing; SongsView hosting the bulk-tag action state.

This plan is NOT autonomous — Task 3 ends in a human-verify checkpoint for the sorting/tagging/bulk UX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-song-catalog-service-planner-improvements/11-CONTEXT.md
@.planning/phases/11-song-catalog-service-planner-improvements/11-PATTERNS.md

<interfaces>
Depends on Plan 11-01 outputs:
- `Song.tags: string[]` now exists (src/types/song.ts)
- `songStore.filterTagInclude` and `songStore.filterTagExclude` refs exist and drive `filteredSongs`
- `TeamTagPill` now accepts `variant?: 'team' | 'theme' | 'user'` (default 'team')

From src/utils/songSearch.ts (already imported in SongTable for key extraction):
```typescript
export function getPrimaryKey(song: Song): string
```

From src/stores/songs.ts:
```typescript
async function updateSong(id: string, data: Partial<UpsertSongInput>): Promise<void>  // confirm exact name/signature in-file
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: All-column sort + themes/user-tag pills in SongTable</name>
  <read_first>
    - src/components/SongTable.vue (lines 75-102 Title sort-arrow SVG; lines 105-119 currently-unsortable headers; lines 164-173 the teamTags-only Tags cell; lines 203-225 sortField/sortDir/toggleSort/sortedSongs; lines 228-268 IntersectionObserver batching)
    - src/components/TeamTagPill.vue (the new variant prop from Plan 11-01)
    - src/utils/songSearch.ts (line 44 getPrimaryKey — already imported)
    - 11-PATTERNS.md section "src/components/SongTable.vue" (exact sort + pills excerpts)
  </read_first>
  <action>
    **All-column sort (D-07).** Widen the type and comparator (SongTable.vue:203-225):
    ```typescript
    type SortField = 'title' | 'category' | 'key' | 'ccli' | 'lastUsed'
    ```
    Default stays `sortField='title'`, `sortDir='asc'` so load looks familiar. Replace the naive `a[sortField].toLowerCase()` comparator with per-field extractors:
    ```typescript
    function sortKey(song: Song): string | number {
      switch (sortField.value) {
        case 'category': return song.vwTypes[0] ?? 99
        case 'key':      return getPrimaryKey(song).toLowerCase()
        case 'ccli':     return Number(song.ccliNumber) || 0
        case 'lastUsed': return song.lastUsedAt?.toMillis() ?? 0
        default:         return song.title.toLowerCase()
      }
    }
    ```
    In `sortedSongs`, compare with `localeCompare` for strings and numeric subtraction for numbers, honoring `sortDir`. Make ALL headers (Title, Category, Key, CCLI, Last Used) clickable calling `toggleSort('<field>')`. Extract the Title sort-arrow SVG block (SongTable.vue:75-102) into a reusable inline snippet/component rendered per sortable header, showing the arrow only on the active `sortField` with direction from `sortDir` (replicate per Discretion D, CONTEXT :55).

    **Pills (D-06).** In the Tags cell (SongTable.vue:164-173) render three groups using the TeamTagPill variant prop:
    ```html
    <TeamTagPill v-for="t in song.teamTags" :key="'tm-'+t" :tag="t" variant="team" />
    <TeamTagPill v-for="t in song.themes" :key="'th-'+t" :tag="t" variant="theme" />
    <TeamTagPill v-for="t in song.tags" :key="'us-'+t" :tag="t" variant="user" />
    ```
    Keep the em-dash fallback only when all three arrays are empty.
  </action>
  <verify>
    <automated>cd C:/projects/worshipplanner && npx vue-tsc --noEmit -p tsconfig.app.json 2>&1 | grep -i "SongTable" || echo "no SongTable type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SongTable.vue` `SortField` type is `'title' | 'category' | 'key' | 'ccli' | 'lastUsed'`
    - The comparator uses per-field extraction (references `getPrimaryKey`, `lastUsedAt`, `vwTypes[0]`, and `Number(` for ccli) — not a single `a[sortField]` access
    - All five headers call `toggleSort(...)` and render a direction arrow only on the active field
    - The Tags cell renders `variant="theme"` for `song.themes` and `variant="user"` for `song.tags`
    - `npx vue-tsc --noEmit` reports no SongTable errors
  </acceptance_criteria>
  <done>All columns sort with a familiar Title-asc default; themes and user tags render as distinct pills.</done>
</task>

<task type="auto">
  <name>Task 2: Tag editing — editor form (D-04a) + include/exclude filter (D-03) + availableUserTags wiring</name>
  <read_first>
    - src/components/SongSlideOver.vue (lines 149-163 tag toggle UI; lines 248-257 FormState; lines 261-288 emptyForm/toForm; lines 291,307,388-392 themesInput comma-parse; lines 319 PREDEFINED_TAGS; lines 369-376 toggleSongTag; lines 407-420 save payload)
    - src/components/SongFilters.vue (the existing team-tag `<select>` filter — the analog for the new include/exclude selects)
    - src/views/SongsView.vue (lines 65-81 SongFilters/SongTable wiring; line 142 BatchQuickAssign import; the availableTags derivation ~line 184)
    - 11-PATTERNS.md sections "src/components/SongSlideOver.vue" and "src/views/SongsView.vue" and "src/stores/songs.ts" (filter state)
  </read_first>
  <action>
    **Editor form (D-04a).** In SongSlideOver.vue add user-tag editing paralleling the existing `teamTags` toggle:
    - Add `tags: string[]` to the `FormState` interface (SongSlideOver.vue:248-257) and initialize it in `emptyForm()` (to `[]`) and `toForm()` (from `song.tags ?? []`) (SongSlideOver.vue:261-288).
    - Add a `toggleUserTag(tag)` function cloning `toggleSongTag` (SongSlideOver.vue:369-376) but operating on `form.value.tags`.
    - Because user tags are ad-hoc (e.g. "Christmas") unlike `PREDEFINED_TAGS`, add a free-text add input modeled on the `themesInput` comma-parse pattern (SongSlideOver.vue:291,307,388-392): a text field that on submit splits/pushes new tags into `form.value.tags`, plus removable chips for existing tags.
    - Add `tags: form.value.tags` to the save `data` object (SongSlideOver.vue:407-420).

    **Include/exclude filter (D-03 list side).** In SongFilters.vue, clone the existing team-tag `<select>` twice: one bound `v-model` to `songStore.filterTagInclude` (label "Show only tag") and one to `songStore.filterTagExclude` (label "Hide tag"), each with a blank/"All" option. Options come from a new `availableUserTags` prop.

    **SongsView wiring.** In SongsView.vue add a computed `availableUserTags` derived from all songs' `tags` (dedup + sort), paralleling the existing `availableTags` (~line 184), and pass it to `<SongFilters :available-user-tags="availableUserTags" />`. Ensure `filterTagInclude`/`filterTagExclude` are the store refs already exported by Plan 11-01 (do not re-declare local ones).
  </action>
  <verify>
    <automated>cd C:/projects/worshipplanner && npx vue-tsc --noEmit -p tsconfig.app.json 2>&1 | grep -iE "SongSlideOver|SongFilters|SongsView" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SongSlideOver.vue` contains `toggleUserTag` and `tags:` in FormState, emptyForm, toForm, and the save payload
    - `src/components/SongFilters.vue` binds two controls to `filterTagInclude` and `filterTagExclude`
    - `src/views/SongsView.vue` contains a computed `availableUserTags` and passes it to SongFilters
    - No local re-declaration of `filterTagInclude`/`filterTagExclude` in the view (uses the store refs from Plan 11-01)
    - `npx vue-tsc --noEmit` reports no errors in the three files
  </acceptance_criteria>
  <done>Tags editable in the song form; include/exclude tag filter live in the song list; availableUserTags feeds the filter options.</done>
</task>

<task type="auto">
  <name>Task 3: Inline row tag add/remove (D-04b) + bulk multi-select tag apply/remove (D-04c)</name>
  <read_first>
    - src/components/SongTable.vue (lines 123-128 row `@select` emit on click; lines 164-173 Tags cell to augment with inline "+"; the header row to add a checkbox column)
    - src/views/SongsView.vue (line 142 BatchQuickAssign import — the existing multi-select action-bar precedent to model)
    - src/stores/songs.ts (updateSong signature — the persist call for tag mutations)
    - 11-PATTERNS.md section "src/components/SongTable.vue" (bulk-tag selection guidance) and "src/views/SongsView.vue"
  </read_first>
  <action>
    **Inline add/remove (D-04b).** In the SongTable Tags cell, make each user-tag pill removable (small x that splices `song.tags` and calls `songStore.updateSong(song.id, { tags })`) and add a small "+" affordance that opens a tiny inline input to add a tag, persisting via the same `updateSong` call. Guard these controls with `@click.stop` so they do NOT trigger the row `@select` (SongTable.vue:123-128).

    **Bulk multi-select (D-04c).** Follow the affordance "most consistent with existing SongTable.vue" (Discretion, CONTEXT :51) — add a checkbox column:
    - Add a leading `<th>`/`<td>` checkbox column. Each row checkbox has `@click.stop` and toggles the song id in a `selectedIds` set. Row-body click still emits `@select` to open the slide-over (selection ≠ opening).
    - Emit selection changes up to SongsView (e.g. `@toggle-select="(id) => ..."` or a `v-model:selected-ids`), OR keep `selectedIds` in the table and emit an event when the user triggers a bulk action. Choose the pattern matching BatchQuickAssign's precedent.
    - In SongsView.vue, host the bulk-tag action bar (visible when `selectedIds.size > 0`), modeled on `BatchQuickAssign.vue` (SongsView.vue:142): a tag input + "Apply to selected" and "Remove from selected" buttons that iterate the selected songs and call `songStore.updateSong(id, { tags })` with the tag added/removed. Clear the selection after applying.
  </action>
  <verify>
    <automated>cd C:/projects/worshipplanner && grep -n "updateSong\|selectedIds\|@click.stop" src/components/SongTable.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SongTable.vue` has a checkbox column with `@click.stop` on the checkbox
    - Inline user-tag pills are removable and there is a "+" add affordance, both calling `songStore.updateSong` and both `@click.stop` (do not trigger row select)
    - `src/views/SongsView.vue` hosts a bulk-tag action bar that iterates selected songs and calls `updateSong` to apply/remove a tag, then clears selection
    - Row-body click still emits `@select` (opening the slide-over) — selection and opening are distinct
    - `npx vue-tsc --noEmit -p tsconfig.app.json` reports no errors in SongTable/SongsView
  </acceptance_criteria>
  <done>Tags editable inline per row and in bulk across a multi-select, without breaking row-click-to-open.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify column sort, pills, tag editing, bulk, and filter UX</name>
  <action>Pause for human verification. Follow the numbered steps in <how-to-verify> and do not resume until the human signals per <resume-signal>.</action>
  <what-built>
    SongTable columns are all sortable with a Title-asc default; themes and user tags show as distinct pills alongside team tags; tags are editable in the song form, inline per row, and in bulk via a checkbox multi-select action bar; the song list filters by show-only-tag and hide-tag.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the Songs list.
    2. Click each column header (Title, Category, Key, CCLI, Last Used); confirm it sorts and the arrow flips on a second click. Confirm default load is Title ascending.
    3. Confirm each row shows team tags, themes, and user tags as three visually-distinct pill colors.
    4. Open a song, add a user tag "Christmas" in the editor, save; confirm the pill appears on the row.
    5. On a row, use the inline "+" to add a tag and the pill "x" to remove one; confirm it persists after refresh.
    6. Select 2-3 songs via checkboxes; use the bulk action bar to apply "Christmas" to all; confirm all three now show it. Then bulk-remove it.
    7. Use "Show only tag = Christmas" — only tagged songs remain. Switch to "Hide tag = Christmas" — those songs disappear.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which step misbehaved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tag input (form/inline/bulk) → Firestore | User-entered tag strings persisted via updateSong |
| Tag/theme strings → DOM render | Tag values rendered as pills in the table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Cross-Site Scripting | SongTable / TeamTagPill pill render | mitigate | All tag/theme text rendered via Vue `{{ }}` interpolation and `:tag` prop (auto-escaped); no `v-html` introduced anywhere in the tag path |
| T-11-09 | Elevation of Privilege | updateSong tag writes (inline/bulk/form) | accept | Reuses existing `updateSong` store method subject to existing Firestore org-ownership rules; no new unauthenticated write path added |
| T-11-10 | Tampering | bulk apply/remove iteration | accept | Bulk action only reads/writes tags on songs the user already sees in their org list; scoped to selected ids; existing rules enforce ownership |
</threat_model>

<verification>
- `npx vue-tsc --noEmit -p tsconfig.app.json` reports no errors in the four files
- Grep confirms the widened `SortField`, `variant="theme"`/`variant="user"`, `toggleUserTag`, `filterTagInclude`/`filterTagExclude` binding, `availableUserTags`, `selectedIds`, and `updateSong` tag persistence
- Human-verify checkpoint approved for sort/pills/tag-edit/bulk/filter UX
</verification>

<success_criteria>
- All five columns sortable, Title-asc default
- Three distinct pill types on each row
- Tags editable in form, inline, and bulk
- Show-only and hide tag filters work in the song list
</success_criteria>

<output>
After completion, create `.planning/phases/11-song-catalog-service-planner-improvements/11-03-SUMMARY.md`
</output>
