---
phase: 16.1-song-list-tags-columns-customization
plan: 05
type: execute
wave: 3
depends_on: [16.1-01, 16.1-02, 16.1-04]
files_modified:
  - src/components/SongTable.vue
  - src/components/VwExplainer.vue
  - src/components/__tests__/SongTable.test.ts
autonomous: false
requirements: [D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-16, D-17, D-18]

must_haves:
  truths:
    - "The Songs list renders Themes as its own column, separate from Tags (D-07/D-12)"
    - "Both Tags and Themes columns are inline-editable (add/remove pills), mirroring today's user-tag edit (D-13)"
    - "A settings cog lets the user show/hide every column except Title; choices persist and rehydrate (D-08/D-09/D-10)"
    - "When VW mode is OFF, the Category column and its VW badge are hidden; when ON, an explainer of the 1-2-3 method is reachable (D-16/D-17)"
    - "The explainer teaches the app's 1-2-3 (1=Call to Worship, 2=Intimate, 3=Ascription) and does NOT introduce a 1-5 scale (D-18)"
  artifacts:
    - path: "src/components/SongTable.vue"
      provides: "Themes column, cog popover, per-column v-if gating, inline theme+tag edit, VW-gated Category column"
      contains: "columnVisibility"
    - path: "src/components/VwExplainer.vue"
      provides: "self-contained 1-2-3 explainer popover/modal"
      contains: "Call to Worship"
    - path: "src/components/__tests__/SongTable.test.ts"
      provides: "component coverage for column-visibility gating, VW-off Category hiding, and the Tags/Themes split"
      contains: "columnVisibility"
  key_links:
    - from: "src/components/SongTable.vue"
      to: "songStore.columnVisibility"
      via: "v-if per column + cog checkboxes calling toggleColumn"
      pattern: "columnVisibility"
    - from: "src/components/SongTable.vue Category column"
      to: "authStore.vwModeEnabled"
      via: "v-if gate on the Category th/td"
      pattern: "vwModeEnabled"
    - from: "src/components/SongTable.vue Themes remove handler"
      to: "songStore.updateSong themes + removedThemes"
      via: "removing a theme also records it in removedThemes (D-14 tie-in)"
      pattern: "removedThemes"
---

<objective>
Rebuild the Songs list table into two distinct inline-editable Tags and Themes columns, add a column-visibility cog, gate the Category column behind VW mode, and ship the 1-2-3 explainer.

Purpose: This is the primary UI surface (R-02/R-03/R-04). It consumes the folded tags (plan 01), the columnVisibility store (plan 04), and the vwModeEnabled flag (plan 02). Team pills disappear (folded into the flat Tags column); Themes leaves the combined cell and becomes its own inline-editable column. The cog reuses SongFilters conventions and the gray-950/900/800 palette. The explainer teaches the app's existing 1-2-3 only.

Output: reworked SongTable, a new VwExplainer component, and the first-ever SongTable component test (Wave 0 gap). The component test is written first (TDD-style, mirroring plan 03) so the per-task sampling loop has a real file to run from Task 1 onward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-CONTEXT.md
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-RESEARCH.md

<explainer_copy>
Drafted VW explainer copy for user review (D-17/D-18 — teaches 1-2-3 only, NOT 1-5):

Heading: "The 1-2-3 worship flow"
Body:
"This app plans a service as a journey from gathering together toward pure adoration of God —
moving from the horizontal (us, our testimony) to the vertical (God alone).
 • Type 1 — Call to Worship (Gather): bright, inviting songs that gather the congregation.
 • Type 2 — Intimate (Testimony): reflective songs that voice our shared response.
 • Type 3 — Ascription (Arrive): songs of pure ascription and adoration directed to God.
A common flow is 1 → 2 → 2 → 3, ending on a Type 3 so the service arrives at undivided worship.
You can turn this methodology off anytime in Settings."
</explainer_copy>

<interfaces>
From src/components/SongTable.vue:
  - Columns today: checkbox, Title, Category (SongBadge), Key, CCLI, Last Used, Tags (combined
    teamTags+themes+tags pills, inline user-tag add/remove), trailing chevron.
  - Inline-tag trio: openInlineEdit(songId)/commitInlineTag(song)/removeUserTag(song, tag) lines 396-426,
    using inlineEditSongId/inlineTagInput refs — the template to generalize per column.
  - sortField type 'title'|'category'|'key'|'ccli'|'lastUsed' (line 315); sortKey (330-338).
  - Imports SongBadge, TeamTagPill, useSongStore. Add useAuthStore for vwModeEnabled.

From src/stores/songs.ts (plan 04): columnVisibility, toggleColumn, resetColumns; updateSong(id, {themes, removedThemes}).
From src/stores/auth.ts (plan 02): vwModeEnabled.
Precedent component test: src/components/__tests__/SongBadge.test.ts (prop-driven mount pattern).
Pitfall 1: use static full Tailwind class strings for any new pill/cog state — no interpolation.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: SongTable component test (RED) + Tags/Themes split, VW-gated Category, per-column visibility (GREEN)</name>
  <read_first>
    - src/components/__tests__/SongBadge.test.ts (prop-driven mount precedent)
    - src/components/SongTable.vue (whole template + script)
    - src/stores/songs.ts (columnVisibility/toggleColumn from plan 04)
    - src/stores/auth.ts (vwModeEnabled)
  </read_first>
  <behavior>
    - With columnVisibility.themes=false, the Themes column header does not render.
    - With authStore.vwModeEnabled=false, the Category header does not render.
    - A song with tags and themes renders its tags in the Tags cell and themes in the Themes cell (no team pills).
  </behavior>
  <action>
    RED first: create `src/components/__tests__/SongTable.test.ts` mounting SongTable with @vue/test-utils and a Pinia test instance (createTestingPinia or a real store seeded), mirroring SongBadge.test.ts setup. Cover the three behaviors above: column-visibility hiding a column, VW-off hiding the Category column, and Tags/Themes rendering in their own cells. Stub authStore.vwModeEnabled and songStore.columnVisibility as needed. These fail against today's combined-cell markup. GREEN: rework SongTable — import useAuthStore; add a Themes `<th>`/`<td>` column immediately before Tags; in the Tags cell remove the `song.teamTags` TeamTagPill loop (team pills are gone — folded into tags), render `song.tags` as removable pink pills and `song.themes` as teal pills in the new Themes column; wrap each column's `<th>`/`<td>` (category, key, ccli, lastUsed, tags, themes) in `v-if="songStore.columnVisibility.<key>"`, leaving checkbox, Title, and trailing chevron always rendered; additionally gate the Category column with `v-if="authStore.vwModeEnabled && songStore.columnVisibility.category"` (D-16); add a `themes` case to sortField/sortKey (sort by themes.join); update the em-dash fallback per split cell. Use static Tailwind classes only. Run the test to GREEN. Commit test-first then implementation.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongTable.test.ts</automated>
    <human-check>On the Songs page with VW ON, confirm separate Tags and Themes columns render; toggle VW OFF in Settings and confirm the Category column and its badge disappear while data is intact on toggle back ON.</human-check>
  </verify>
  <acceptance_criteria>
    - New file src/components/__tests__/SongTable.test.ts exists with the three behavior cases.
    - SongTable.vue has distinct Themes and Tags `<th>` columns; no `song.teamTags` reference remains in the template.
    - Category th/td is wrapped in a vwModeEnabled gate; each non-Title data column has a columnVisibility v-if.
    - `npx vitest run src/components/__tests__/SongTable.test.ts` exits 0 and `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>SongTable has component coverage (created first) plus the two-column Tags/Themes layout with VW-gated Category and per-column visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Column cog + inline Themes editing + VwExplainer</name>
  <read_first>
    - src/components/SongTable.vue (inline-tag trio 385-426; header row markup; final layout from Task 1)
    - src/components/__tests__/SongTable.test.ts (created in Task 1 — keep it green)
    - src/components/SongFilters.vue (dropdown/popover affordance + palette conventions)
    - src/components/TagFilterChecklist.vue (fixed-height popover pattern, if referenced for cog styling)
  </read_first>
  <action>
    Add a settings-cog button in the table header area that opens a small popover (SongFilters/TagFilterChecklist palette: gray-900 panel, gray-800 borders) listing each toggleable column (Category, Key, CCLI, Last Used, Tags, Themes) as a checkbox bound to songStore.columnVisibility via toggleColumn; include a "Reset" action (resetColumns). Title is not listed. Generalize the inline-tag trio to also edit Themes: parameterize the edit handlers by field ('tags' | 'themes') so the Themes column gets the same add/remove UX (openInlineEdit(songId, field)/commit/remove). The Themes remove handler must call updateSong with BOTH the filtered themes AND `removedThemes: [...existing, removedTheme]` (dedup) so removed themes don't reappear on reimport (D-14 tie-in). Create `src/components/VwExplainer.vue` — a self-contained popover/modal rendering the drafted copy from <explainer_copy>; trigger it from a small help/info icon next to the Category column header, rendered only when authStore.vwModeEnabled. Static Tailwind classes only. Keep the Task 1 SongTable.test.ts green.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongTable.test.ts</automated>
    <human-check>Open the cog, toggle each column off/on and confirm the table updates and the choice survives reload; open the Category help icon and confirm the explainer copy reads the 1-2-3 (not 1-5); add and remove a Theme pill inline.</human-check>
  </verify>
  <acceptance_criteria>
    - Cog popover lists exactly the six toggleable columns (no Title) and a Reset.
    - VwExplainer.vue exists and its text contains "Call to Worship", "Intimate", "Ascription" and does NOT contain a "1-5" or "Type 4"/"Type 5" scale.
    - Themes column supports inline add/remove; removing a theme records it in removedThemes.
    - `npx vitest run src/components/__tests__/SongTable.test.ts` exits 0.
  </acceptance_criteria>
  <done>Cog, inline Themes edit with removal-tracking, and the VW explainer ship.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two-column Tags/Themes song list with a column-visibility cog, VW-gated Category column, inline Themes editing, and the 1-2-3 explainer popover.</what-built>
  <how-to-verify>
    1. Open the Songs page (VW mode ON in Settings). Confirm Tags and Themes are separate columns and no gray "team" pills appear.
    2. Click the settings cog; toggle Category, Key, Themes off and on; reload the page and confirm your choices persisted.
    3. Open the help/info icon by the Category header; confirm the explainer names 1 = Call to Worship, 2 = Intimate, 3 = Ascription and does not mention a 1-5 scale. Suggest any copy edits now.
    4. Inline-add and inline-remove a Theme pill on a song; confirm it saves.
    5. In Settings toggle VW mode OFF; return to Songs and confirm the Category column + badge are hidden; toggle back ON and confirm they return unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe copy/UX changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline edit → Firestore | tag/theme edits write org-scoped song docs via updateSong |
| localStorage → UI | column-visibility preference drives rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-09 | Tampering | inline tag/theme write | mitigate | updateSong writes are gated by existing org-editor Firestore rules; values render as text-only pills (no HTML injection). |
| T-16.1-10 | Tampering | localStorage column prefs | accept | Cosmetic column show/hide only; no data/auth impact. |

Severity LOW: view customization plus text-field edits on an already org-gated subcollection.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/SongTable.test.ts` exits 0.
- `npm run type-check` exits 0.
- Human checkpoint approved (explainer copy + cog + VW-off gating).
</verification>

<success_criteria>
- Separate inline-editable Tags/Themes columns, a persistent column cog, VW-gated Category, and an accurate 1-2-3 explainer.
</success_criteria>

<output>
Create `.planning/phases/16.1-song-list-tags-columns-customization/16.1-05-SUMMARY.md` when done.
</output>
</content>
