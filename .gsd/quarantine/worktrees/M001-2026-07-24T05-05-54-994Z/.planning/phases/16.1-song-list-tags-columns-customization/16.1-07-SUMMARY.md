---
phase: 16.1-song-list-tags-columns-customization
plan: 07
subsystem: ui
tags: [vue, pinia, song-search, vw-mode, tags]

# Dependency graph
requires:
  - phase: 16.1-01
    provides: "Song.tags folds legacy teamTags on read (D-01)"
  - phase: 16.1-02
    provides: "authStore.vwModeEnabled (D-15/D-16)"
  - phase: 16.1-03
    provides: "songMatchesQuery(song, query, vwModeEnabled) VW-gated type: prefix (unit-tested)"
  - phase: 16.1-04
    provides: "songs.ts columnVisibility state (this plan does not touch it)"
provides:
  - "SongSlotPicker.vue reads folded tags only (themes ∪ tags), no teamTags pill rows"
  - "SongSlotPicker.vue SongBadge occurrences gated on authStore.vwModeEnabled (D-16)"
  - "SongSlotPicker.vue and songs.ts filteredSongs both thread authStore.vwModeEnabled into songMatchesQuery"
  - "ServiceEditorView.vue assigned-song SongBadge gated on authStore.vwModeEnabled"
  - "ServiceEditorView.vue orchestra AI-library filter reads song.tags instead of song.teamTags (D-04 ripple)"
affects: ["16.1-08 (cross-surface checkpoint)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "VW badge gating pattern: v-if=\"authStore.vwModeEnabled\" (combined with existing conditions via &&) on every SongBadge occurrence"
    - "songMatchesQuery third-arg threading: every search-surface caller passes authStore.vwModeEnabled explicitly"

key-files:
  created: []
  modified:
    - src/components/SongSlotPicker.vue
    - src/stores/songs.ts
    - src/views/ServiceEditorView.vue

key-decisions:
  - "Left src/views/__tests__/ServiceEditorView.test.ts untouched — its mockSongs fixture's teamTags: [] is still a valid required Song field (teamTags remains in the Song type for legacy read-fold per D-01), and no test in this file asserts on teamTags/orchestra-filter/badge-visibility behavior, so no update was needed."
  - "songs.ts filteredSongs resolves useAuthStore() once per computed re-evaluation (matches existing per-call pattern in tagFilterStorageKey/columnStorageKey rather than hoisting to module scope)."

requirements-completed: [D-02, D-04, D-11, D-16]

# Metrics
duration: 12min
completed: 2026-07-13
---

# Phase 16.1 Plan 07: Picker + Service-Editor Tag Repoint & VW-Gate Summary

**SongSlotPicker and ServiceEditorView repointed to folded song.tags (teamTags pills/orchestra filter removed), every SongBadge gated on authStore.vwModeEnabled, and songMatchesQuery's VW gate threaded into both the Songs-list store search and the picker search.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-13T04:21:00Z
- **Completed:** 2026-07-13T04:33:18Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- Removed all `song.teamTags` reads from SongSlotPicker.vue (three pill rows, availableTags union, tagFilteredSongs include/exclude scans) — themes ∪ tags is now the only tag union in the picker.
- Gated all four SongBadge occurrences (three in SongSlotPicker.vue, one in ServiceEditorView.vue) on `authStore.vwModeEnabled` so VW/Category badges hide app-wide when VW mode is off (D-16).
- Threaded `authStore.vwModeEnabled` as the third arg to every `songMatchesQuery` call site: SongSlotPicker's `searchResults` and songs.ts's `filteredSongs` — both now hide the `type:` search prefix when VW mode is off.
- Repointed both ServiceEditorView.vue orchestra AI-library filters from `s.teamTags.includes('Orchestra')` to `s.tags.includes('Orchestra')` (D-04 ripple), leaving `AVAILABLE_TEAMS`/service team scheduling untouched (D-02).

## Task Commits

1. **Task 1: SongSlotPicker + Songs-list store — fold team pills, gate badges, VW-gate both search callers** - `06c49ed` (feat)
2. **Task 2: ServiceEditorView — gate badge + orchestra AI filter over tags** - `cb707ec` (feat)

**Plan metadata:** (SUMMARY commit follows this file)

## Files Created/Modified
- `src/components/SongSlotPicker.vue` - Removed teamTags pill rows/scans; gated 3 SongBadge occurrences on vwModeEnabled; search call passes vwModeEnabled
- `src/stores/songs.ts` - filteredSongs resolves authStore and passes vwModeEnabled to songMatchesQuery
- `src/views/ServiceEditorView.vue` - Gated assigned-song SongBadge on vwModeEnabled; both orchestra AI filters read s.tags

## Decisions Made
- Left the ServiceEditorView test fixture's `teamTags: []` field as-is (still required by the `Song` type; no assertions depend on it in this file).
- Resolved `useAuthStore()` inline inside `filteredSongs`'s computed body (consistent with the store's existing per-call `useAuthStore()` pattern elsewhere in songs.ts) rather than importing/caching at module scope.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Picker and service editor are fully repointed to the folded tag model; no remaining `teamTags` references in either file.
- Both Songs-list and picker searches consistently VW-gate the `type:` prefix, ready for plan 08's cross-surface checkpoint to verify end-to-end behavior with VW mode toggled on/off.
- `npm run type-check` and `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` both exit 0.

---
*Phase: 16.1-song-list-tags-columns-customization*
*Completed: 2026-07-13*
