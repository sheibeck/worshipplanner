---
phase: 16.1-song-list-tags-columns-customization
plan: 06
type: execute
wave: 2
depends_on: [16.1-01, 16.1-02]
files_modified:
  - src/views/SongsView.vue
  - src/components/SongFilters.vue
  - src/components/SongSlideOver.vue
autonomous: true
requirements: [D-01, D-12, D-16]

must_haves:
  truths:
    - "The Songs-panel tag-filter checklist offers the union of themes ∪ tags (team half gone after the fold) (D-12)"
    - "When VW mode is OFF, the VW-type filter dropdown and the Add/Edit-Song VW Category selector are hidden (D-16)"
    - "The Add/Edit Song drawer no longer has a separate 'Team Tags' section — those tags fold into the one flat tag set (D-01)"
  artifacts:
    - path: "src/views/SongsView.vue"
      provides: "availableUserTags union without teamTags"
      contains: "themes"
    - path: "src/components/SongFilters.vue"
      provides: "VW-type select gated by vwModeEnabled"
      contains: "vwModeEnabled"
    - path: "src/components/SongSlideOver.vue"
      provides: "team-tag section folded into user tags; VW Category selector gated"
      contains: "vwModeEnabled"
  key_links:
    - from: "src/components/SongFilters.vue VW-type select"
      to: "authStore.vwModeEnabled"
      via: "v-if gate"
      pattern: "vwModeEnabled"
    - from: "src/components/SongSlideOver.vue onSave"
      to: "form.tags"
      via: "predefined team names committed into the flat tags set, no teamTags write"
      pattern: "tags"
---

<objective>
Repoint the Songs-panel supporting components (filter row, view host, and the Add/Edit-Song drawer) to the folded tag model and gate their VW indicators.

Purpose: Complements plan 05 by handling the remaining Songs-panel surfaces. SongsView's tag-filter option union drops the redundant teamTags half (D-12). SongFilters' VW-type dropdown hides when VW mode is off (D-16). SongSlideOver retires its dedicated "Team Tags" toggle section (D-01 — songs have exactly one tag set) and gates its VW Category selector (D-16, research assumption A1 confirmed: hide the edit-form VW selector when VW mode is off for app-wide consistency). This plan only depends on the Wave-1 foundations (plan 01 fold, plan 02 vwModeEnabled) and touches no file owned by the other Wave-2 plans (03/04), so it runs in Wave 2.

Output: three repointed Songs-panel components, all VW-gated where applicable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-CONTEXT.md
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-RESEARCH.md

<interfaces>
From src/views/SongsView.vue:
  - availableUserTags computed (lines 223-234): scans song.teamTags ∪ themes ∪ tags — drop the teamTags line.
  - passes availableUserTags + v-model:filterVwType to SongFilters.
From src/components/SongFilters.vue:
  - VW-type <select> (lines 34-45) bound via onVwTypeChange; add useAuthStore + v-if gate.
From src/components/SongSlideOver.vue:
  - "Song-level Team Tags" section (lines 147-164) with PREDEFINED_TAGS ['Choir','Orchestra','Hymn'] and
    toggleSongTag; availableTags computed scans s.teamTags + arrangement teamTags (lines 365-377).
  - VW Category selector (lines 106-121) + vwTypeClasses/toggleVwType.
  - onSave (lines 457-511) currently builds teamTags = union of form.teamTags + arrangement teamTags and
    writes teamTags. form/emptyForm/songToForm carry teamTags (lines 288-332).
Note: Arrangement.teamTags field is intentionally LEFT in place this phase (dead-weight from PC import,
  planner discretion per RESEARCH Open Question 1) — do not remove ArrangementAccordion.
From src/stores/auth.ts (plan 02): vwModeEnabled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SongsView union + SongFilters VW-type gating</name>
  <read_first>
    - src/views/SongsView.vue (availableUserTags 223-234)
    - src/components/SongFilters.vue (VW-type select 34-45; script 72-103)
    - src/stores/auth.ts (vwModeEnabled)
  </read_first>
  <action>
    In SongsView.availableUserTags, remove the `song.teamTags` scan line so the option set is `themes ∪ tags` only (D-12). In SongFilters.vue, import useAuthStore and wrap the VW-type `<select>` (All types / Type 1-3 / Uncategorized) in `v-if="authStore.vwModeEnabled"` (D-16). Leave the Key filter and TagFilterChecklist untouched. Optionally trim "category" from the search placeholder when VW is off (discretionary — keep simple).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <human-check>With VW OFF, confirm the type dropdown is gone from the Songs filter row; with VW ON it returns. Confirm the tag-filter checklist still lists themes and tags.</human-check>
  </verify>
  <acceptance_criteria>
    - SongsView.availableUserTags no longer references song.teamTags.
    - SongFilters VW-type select is wrapped in a vwModeEnabled v-if.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Filter options reflect the folded union and the VW-type dropdown is VW-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Fold SongSlideOver team tags into the flat tag set + gate VW selector</name>
  <read_first>
    - src/components/SongSlideOver.vue (whole file)
    - src/stores/auth.ts (vwModeEnabled)
  </read_first>
  <action>
    Remove the dedicated "Song-level Team Tags" section (the PREDEFINED_TAGS toggle buttons) — songs have one flat tag set (D-01). Fold its capability into the existing "User Tags" free-text editor: seed the tag type-ahead datalist with the predefined names (Choir/Orchestra/Hymn) plus songStore.allUserTags so users can still quickly apply them as ordinary tags. Delete `form.teamTags`, `emptyForm`/`songToForm` teamTags handling, `toggleSongTag`, and the availableTags teamTags scan; in onSave stop building/writing a `teamTags` field entirely (write only the flat `tags`). Gate the "VW Category" selector block with `v-if="authStore.vwModeEnabled"` (D-16 / research A1). Keep arrangements and Arrangement.teamTags handling in songToForm/onSave intact (arrangement-level field is retained this phase). Static Tailwind classes only.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <human-check>Open Add Song and Edit Song: confirm there is no separate "Team Tags" section (only User Tags), that adding "Orchestra" as a tag works, and that with VW OFF the "VW Category" selector is hidden.</human-check>
  </verify>
  <acceptance_criteria>
    - SongSlideOver.vue no longer renders a "Team Tags" section and onSave writes no teamTags field.
    - The VW Category selector is wrapped in a vwModeEnabled v-if.
    - `npm run type-check` exits 0 (Song.teamTags still declared until plan 08; this component simply stops using it).
  </acceptance_criteria>
  <done>The editor uses one flat tag set and hides VW category UI when VW is off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Add/Edit Song form → Firestore | writes org-scoped song docs via addSong/updateSong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-11 | Tampering | song form write | mitigate | Writes gated by existing org-editor Firestore rules; tags render as text-only pills. |
| T-16.1-12 | Spoofing | VW-off gating | accept | Gating is cosmetic (hides indicators); underlying vwTypes data retained, no auth impact. |

Severity LOW: UI gating + form-field consolidation on an already org-gated subcollection.
</threat_model>

<verification>
- `npm run type-check` exits 0.
- Manual: VW dropdown/selector hidden when VW off; no Team Tags section in editor.
</verification>

<success_criteria>
- Songs-panel filter/view/editor use the folded single tag set and hide VW indicators when VW mode is off.
</success_criteria>

<output>
Create `.planning/phases/16.1-song-list-tags-columns-customization/16.1-06-SUMMARY.md` when done.
</output>
</content>
