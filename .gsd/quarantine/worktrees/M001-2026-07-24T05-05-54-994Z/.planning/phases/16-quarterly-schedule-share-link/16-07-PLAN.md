---
phase: 16-quarterly-schedule-share-link
plan: 07
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/QuarterGrid.vue
  - src/components/__tests__/QuarterGrid.test.ts
autonomous: true
requirements: [R-13, R-14, R-05]
must_haves:
  truths:
    - "Clicking anywhere in a scheduled-group cell opens the group editor (R-13)"
    - "A person's remove (x) pill removes only that person without opening the editor (R-13)"
    - "The group editor opens as a right-side slide-out panel; the expand-underneath row is gone (R-14)"
    - "The grid's tier reads come from PersonQuarterData.roleFrequency (R-05)"
  artifacts:
    - path: "src/components/QuarterGrid.vue"
      provides: "Teleport slide-out group editor + whole-cell click + roleFrequency tier reads"
      contains: "Teleport"
  key_links:
    - from: "src/components/QuarterGrid.vue"
      to: "quartersStore.assignPerson/clearAssignment/swapAssignment"
      via: "slide-out panel actions (unchanged)"
      pattern: "assignPerson|clearAssignment|swapAssignment"
---

<objective>
Rework `QuarterGrid.vue`'s group editor: verify/keep the whole-cell click target (R-13) and
replace the expand-underneath row with a right-side Teleport slide-out panel (R-14), reusing
`AvailabilityDrawer.vue`'s exact slide-out markup. Repoint the grid's tier reads to
`roleFrequency` (R-05).

Purpose: Consistent slide-out editing pattern; the whole cell is a hit target; the editor no
longer shoves the grid around.
Output: Reworked `QuarterGrid.vue` (+ tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
Slide-out markup to copy verbatim (src/components/AvailabilityDrawer.vue lines 1-31):
Teleport to="body" + backdrop `fixed inset-0 z-40 bg-black/60` + panel `fixed top-0 right-0 bottom-0 z-50 w-full max-w-lg bg-gray-900 border-l border-gray-700 shadow-2xl` + the enter/leave Transition classes.

QuarterGrid.vue (grep-confirmed):
- Whole-cell target ALREADY present: outer `<button class="w-full text-left ...">` wrapping full cell content, `@click="toggleCell(date, role.id)"`; remove-pill `<span ... @click.stop="onClear(...)">` (line ~51). R-13 = VERIFY + regression test, likely no restructure.
- Expand-underneath row to REPLACE (lines 81-184): `<tr v-if="expandedCell">` holding current-assignments list, swap dropdown, add-person select, gap-filling blacked-out/available lists. Move this CONTENT unchanged into the Teleport slide-out.
- tierOf (line ~306-309): repoint to `roleFrequency[roleId]?.tier`.
- Store actions to keep verbatim: quartersStore.assignPerson/clearAssignment/swapAssignment (lines ~371-391).
Slide-out title copy (UI-SPEC): "{Role name} — {formatted date}"; close = icon-only × with aria-label="Close editor".
Mobile: reuse AvailabilityDrawer Teleport-to-body pattern (full-width sheet acceptable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace expand-underneath row with right-side Teleport slide-out; repoint tierOf</name>
  <files>src/components/QuarterGrid.vue</files>
  <read_first>
    - src/components/QuarterGrid.vue (full file — cell button, expandedCell row 81-184, tierOf, store-action calls)
    - src/components/AvailabilityDrawer.vue lines 1-31 (Teleport/backdrop/panel/Transition markup to copy verbatim)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/components/QuarterGrid.vue")
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md ("QuarterGrid slide-out" note — reuse exact classes, do not invent a second slide-out language)
  </read_first>
  <action>
    Replace the `<tr v-if="expandedCell">` expand-underneath row (lines 81-184) with a
    `<Teleport to="body">` slide-out using AvailabilityDrawer's exact backdrop (`fixed inset-0
    z-40 bg-black/60`, `@click` closes) and right-anchored panel (`fixed top-0 right-0 bottom-0
    z-50 w-full max-w-lg bg-gray-900 border-l border-gray-700 shadow-2xl`) + the same Transition
    enter/leave classes. Move the panel CONTENT (current-assignments list, swap dropdown,
    add-person select, gap-filling blacked-out/available lists) into the slide-out unchanged; keep
    the quartersStore.assignPerson/clearAssignment/swapAssignment calls exactly as-is. Drive
    open/close from the existing `expandedCell` state (rename to a drawer-open ref if clearer).
    Panel header title = "{Role name} — {formatted date}" using the existing formatter; add an
    icon-only × close button with `aria-label="Close editor"`. Preserve the whole-cell `@click`
    on the outer cell button and the remove-pill's `@click.stop` (R-13 — do NOT remove the stop
    modifier; empty cell space must still open the editor). Repoint `tierOf` to
    `roleFrequency[roleId]?.tier ?? 'regular'`. Use static Tailwind class maps for any dynamic
    classes (purge safety).
  </action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | head -20 && grep -c "expandedCell.*tr\|<tr v-if=\"expandedCell\"" src/components/QuarterGrid.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Teleport" src/components/QuarterGrid.vue` >= 1 and the panel uses `right-0 bottom-0 z-50` + `max-w-lg` (matches AvailabilityDrawer)
    - The `<tr v-if="expandedCell">` expand-underneath row no longer exists (grep returns 0)
    - Outer cell `@click` opens the editor; remove-pill retains `@click.stop` (grep confirms `@click.stop` still present)
    - `grep -c "roleTiers\|frequencyTier" src/components/QuarterGrid.vue` === 0; `grep -n "roleFrequency" src/components/QuarterGrid.vue` shows the tier read
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>Group editor is a right-side slide-out; whole cell opens it; remove-pill still quick-removes; tier reads from roleFrequency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression tests — whole-cell opens slide-out, remove-pill doesn't, slide-out actions work</name>
  <files>src/components/__tests__/QuarterGrid.test.ts</files>
  <read_first>
    - src/components/__tests__/QuarterGrid.test.ts (existing cases to extend)
    - src/components/QuarterGrid.vue (after Task 1 — cell markup + slide-out)
    - .planning/phases/16-quarterly-schedule-share-link/16-VALIDATION.md (Per-Task Verification Map — R-13/R-14 component test expectations)
  </read_first>
  <action>
    Extend QuarterGrid.test.ts: (a) clicking a group cell (including its empty area) opens the
    slide-out panel (assert the Teleported panel renders); (b) clicking a person's remove (x) pill
    calls clearAssignment for that person and does NOT open the slide-out (assert panel absent /
    @click.stop effective); (c) the slide-out's add-person / swap / clear actions still invoke
    quartersStore.assignPerson/swapAssignment/clearAssignment. Keep assertions deterministic
    (mount with a stub store); no snapshot-only tests.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/QuarterGrid.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/QuarterGrid.test.ts` passes
    - A test asserts whole-cell click opens the slide-out; a separate test asserts remove-pill click does NOT open it and calls clearAssignment
    - A test asserts a slide-out action (assign/swap/clear) invokes the corresponding store method
  </acceptance_criteria>
  <done>Deterministic tests cover R-13 whole-cell + remove-pill isolation and R-14 slide-out actions; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated editor → quarters store | Grid edits gated by app-shell auth + editor role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07-01 | Tampering | remove-pill accidentally opening editor / editor swallowing quick-remove | mitigate | @click.stop preserved on remove-pill; regression test asserts the two paths stay separate |
| T-16-07-02 | Denial of Service | Teleport backdrop trapping interaction (can't close) | mitigate | Backdrop `@click` + header × both close the panel — mirrors the proven AvailabilityDrawer pattern |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/QuarterGrid.test.ts` green; `npx vue-tsc --build` green.
- Manual: click any part of a cell → right slide-out opens; click a name's × → only that person removed, no panel.
</verification>

<success_criteria>
- Whole-cell click opens a right-side slide-out group editor; remove-pill isolated; expand-underneath gone; tier from roleFrequency.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-07-SUMMARY.md` when done.
</output>
