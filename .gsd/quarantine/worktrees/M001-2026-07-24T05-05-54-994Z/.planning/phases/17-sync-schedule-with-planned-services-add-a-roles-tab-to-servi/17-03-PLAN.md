---
phase: 17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - src/stores/services.ts
  - src/stores/__tests__/services.test.ts
autonomous: true
requirements: [CR-03, CR-04]
must_haves:
  truths:
    - "setRoleOverride writes ONLY the scoped roleAssignmentOverrides.{roleId} dot-path key, never the whole map, so concurrent overrides of different roles cannot clobber each other"
    - "clearRoleOverride removes a single role key via deleteField(), leaving other roles' overrides intact"
    - "createShareToken's serviceSnapshot gains a roleAssignments array carrying resolved person NAMES only — never email/phone/pcPersonId"
    - "createShareToken additionally writes a serviceShares/{slug}__service-{date} memorable-URL doc"
    - "the memorable-URL slug/serviceShares write is wrapped so its failure never fails the already-succeeded opaque shareTokens write"
  artifacts:
    - src/stores/services.ts
    - src/stores/__tests__/services.test.ts
  key_links:
    - "createShareToken calls resolveServiceRoleAssignments(service, quartersStore.quarters, rosterStore.roles) then maps effectivePersonIds -> names via a Map<personId, name>"
    - "serviceShares doc id is `${slug}__service-${service.date}`, mirroring quarterShares `${slug}__q{N}-{year}`"
---

<objective>
Add the Service store's write surface for Phase 17: per-role override writes that never mutate the schedule, and an extended share-token flow that embeds who-is-serving (names only) into the public snapshot plus a memorable-URL `serviceShares` doc.

Purpose: This is the persistence layer the Roles tab (17-04) and the public share view (17-05) both consume. It applies the two hard patterns this codebase has repeatedly guarded (scoped dot-path writes to avoid clobber; names-only denormalized public snapshots to avoid PII leakage).
Output: `setRoleOverride`, `clearRoleOverride`, and an extended `createShareToken` in `src/stores/services.ts`, with store tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-RESEARCH.md
@.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md
@src/stores/services.ts
@src/stores/quarters.ts
@src/utils/serviceRoles.ts
@src/utils/slug.ts
</context>

## Artifacts this phase produces (this plan's slice)

- **This plan:** `services.ts` gains `setRoleOverride(serviceId, roleId, personIds)`, `clearRoleOverride(serviceId, roleId)`, and an extended `createShareToken` that (a) adds `roleAssignments: Array<{roleId, roleName, group, personNames: string[]}>` to `serviceSnapshot` and (b) writes `serviceShares/{slug}__service-{date}`. New imports into `services.ts`: `deleteField`, `getDoc`, `deriveSlug`, `claimSlug`, `useRosterStore`, `useQuartersStore`, `resolveServiceRoleAssignments`.
- Consumed from earlier plans: `Service.roleAssignmentOverrides` + `resolveServiceRoleAssignments` (17-01); `serviceShares` rules + reserved slug (17-02).

<assumption_delta_decision>
Primary noun: the planned Service. Transition: singular→plural (music-only → music + people-per-role). Decision: **ADD-ALONGSIDE / PROMOTE the field, never the document** — writes land on one optional sparse field (`roleAssignmentOverrides`) via scoped dot-path; the Quarter/schedule is read-only from the Service's perspective and is never mutated by any function in this plan. Accepted, low-risk: no existing Service primary key changes; the field is orthogonal to `slots` (music). Nothing here forces a later merge of the Service and Quarter collections.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setRoleOverride / clearRoleOverride via scoped dot-path writes</name>
  <read_first>
    - src/stores/services.ts (updateService ~lines 78-84 — the whole-field-replace path to AVOID for overrides; the import block ~lines 3-15; createShareToken ~137-176)
    - src/stores/quarters.ts (assignPerson / clearAssignment / swapAssignment ~lines 347-392 — the scoped `calendar.${date}.${roleId}` dot-path pattern to mirror)
    - src/stores/__tests__/services.test.ts (existing `vi.mock('firebase/firestore', …)` conventions; how updateDoc calls are asserted)
    - .planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md (section "src/stores/services.ts (extend)")
  </read_first>
  <files>src/stores/services.ts, src/stores/__tests__/services.test.ts</files>
  <behavior>
    - setRoleOverride(serviceId, roleId, personIds): the updateDoc payload contains exactly one assignment key, the scoped dot-path for that single role, plus updatedAt — NOT a whole-map object.
    - clearRoleOverride(serviceId, roleId): the updateDoc payload sets that single role's dot-path key to a delete-field sentinel, leaving sibling role keys untouched.
    - Both no-op early when orgId is unset (mirroring existing store guards).
    - Store tests assert the exact key shape written for each and that no other role key is present in the payload.
  </behavior>
  <action>
    Add two async actions to `services.ts` and expose them from the store's return object.
    `setRoleOverride(serviceId: string, roleId: string, personIds: string[])` — guard on orgId, then `updateDoc(doc(db,'organizations',orgId.value,'services',serviceId), { [computed scoped dot-path for roleAssignmentOverrides keyed by roleId]: personIds, updatedAt: serverTimestamp() })`. The dot-path key is the `roleAssignmentOverrides` map path scoped to the single roleId — mirror `quarters.ts::assignPerson`'s `calendar.${date}.${roleId}` construction exactly (D-01: scoped write, never a whole-map rewrite, to prevent the concurrent-editor clobber this codebase has fixed repeatedly — see STATE.md T-13-09-02).
    `clearRoleOverride(serviceId: string, roleId: string)` — same scoped key set to `deleteField()`.
    Add `deleteField` to the `firebase/firestore` import block (not currently imported).
    Extend `src/stores/__tests__/services.test.ts` (new cases in the existing file, do NOT create a new file) using its existing firestore mock: assert setRoleOverride's updateDoc payload has exactly the single scoped roleId key (and updatedAt) and no whole-map object; assert clearRoleOverride writes the delete-field sentinel for that one key. Implements CR-03 (per-service override without schedule mutation).
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "setRoleOverride" src/stores/services.ts` and `grep -n "clearRoleOverride" src/stores/services.ts` both match, and both appear in the store's returned object.
    - `grep -n "deleteField" src/stores/services.ts` matches (imported and used by clearRoleOverride).
    - A test asserts setRoleOverride's updateDoc payload contains a single key whose name is the scoped `roleAssignmentOverrides`-plus-roleId dot-path (not the bare `roleAssignmentOverrides` whole-map key).
    - A test asserts clearRoleOverride uses the delete-field sentinel for that one scoped key.
    - `npx vitest run src/stores/__tests__/services.test.ts` passes; `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>Override writes are scoped, delete-field-based for clear, and proven by tests to never rewrite the whole overrides map.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend createShareToken with names-only roleAssignments + serviceShares memorable-URL write</name>
  <read_first>
    - src/stores/services.ts (createShareToken ~lines 137-176 — the existing serviceSnapshot shape + token generation to extend)
    - src/stores/quarters.ts (finalizeAndShare ~lines 397-484 — the nameById Map name-resolution ~408-415 and the try/catch slug-claim + quarterShares write ~442-481 to mirror)
    - src/utils/serviceRoles.ts (resolveServiceRoleAssignments — consumed here)
    - src/utils/slug.ts (deriveSlug / claimSlug)
    - src/stores/roster.ts (people/roles getters) and src/stores/quarters.ts (quarters getter) — sources for the resolve inputs
    - .planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md (section "Name-resolution pattern to copy for roleAssignments" and "Memorable-URL soft-fail wrapper to copy")
  </read_first>
  <files>src/stores/services.ts, src/stores/__tests__/services.test.ts</files>
  <behavior>
    - createShareToken's written serviceSnapshot gains a roleAssignments array of `{roleId, roleName, group, personNames}` computed from resolveServiceRoleAssignments' effectivePersonIds mapped to names.
    - personNames contains only display names — the written payload contains no email, phone, or pcPersonId anywhere.
    - After the opaque shareTokens write, a second write to serviceShares/{slug}__service-{date} occurs, resolving/claiming the org slug like finalizeAndShare.
    - If the slug/serviceShares step throws, the function still returns the token successfully (soft-fail).
    - A test asserts roleAssignments is present with names only and that the payload has no PII keys; a test asserts the soft-fail (serviceShares write rejects → token still returned).
  </behavior>
  <action>
    Extend `createShareToken(service, orgIdValue)` in `services.ts`:
    (a) Build `const nameById = new Map(rosterStore.people.map(p => [p.id, p.name]))` (mirror finalizeAndShare exactly — names ONLY, never the raw Person object; this is the PII guard, D-04 / STATE.md D-24). Compute `resolved = resolveServiceRoleAssignments(service, quartersStore.quarters, rosterStore.roles)` and add to the existing serviceSnapshot a `roleAssignments` array of `{ roleId, roleName, group, personNames: effectivePersonIds.map(id => nameById.get(id) ?? id) }`. This is a non-breaking addition to the existing shareTokens doc shape (no rules change — already public-read).
    (b) After the opaque `setDoc(shareTokens/token, …)` succeeds, add a try/catch soft-fail block mirroring finalizeAndShare ~442-481: resolve the org doc via getDoc; if no slug, derive via deriveSlug(orgName) (fallback 'org') and claim via claimSlug, persisting slug back to the org doc; then setDoc `serviceShares/${slug}__service-${service.date}` with `{ orgId: orgIdValue, orgSlug: slug, serviceSnapshot: <same snapshot incl. roleAssignments>, token, updatedAt: serverTimestamp() }`. Wrap this whole step in try/catch that logs and swallows so a memorable-URL failure never fails the already-succeeded token share (D-04 soft-fail).
    Add imports to `services.ts`: `getDoc`, `deleteField` (if not added in Task 1), `deriveSlug`, `claimSlug`, `useRosterStore`, `useQuartersStore`, `resolveServiceRoleAssignments`.
    Extend the store test file: assert the shareTokens serviceSnapshot includes roleAssignments with personNames and that the serialized written payload contains no `email`/`phone`/`pcPersonId` keys; assert that when the serviceShares setDoc rejects, createShareToken still resolves to the token (soft-fail). Implements CR-04 (public share carries who-is-serving, names only).
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleAssignments" src/stores/services.ts` matches inside createShareToken's snapshot construction.
    - `grep -n "serviceShares" src/stores/services.ts` matches (the memorable-URL setDoc).
    - `grep -n "resolveServiceRoleAssignments" src/stores/services.ts` matches (imported and called).
    - A test asserts the written serviceSnapshot carries roleAssignments with personNames and that the payload JSON contains no `email`, `phone`, or `pcPersonId` key.
    - A test asserts createShareToken still returns the token when the serviceShares write rejects (soft-fail wrapper works).
    - `npx vitest run src/stores/__tests__/services.test.ts` passes; `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>createShareToken embeds names-only who-is-serving into both the opaque and memorable share docs, with a soft-fail memorable-URL write that never breaks the token share.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated editor → services doc (override write) | Editor writes a scoped override key on the org-scoped Service doc. Firestore rules already enforce editor-only write / member read. |
| authenticated editor → shareTokens + serviceShares (share write) | Editor denormalizes a names-only snapshot into public-read docs. |
| public snapshot payload → unauthenticated readers (later, via 17-05) | Whatever names/ids are written here become world-readable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03-01 | Information Disclosure | roleAssignments in public snapshot | high | mitigate | Resolve personId→name via a Map and embed ONLY `personNames`; never the raw Person object. Test asserts the written payload has no email/phone/pcPersonId key (D-04 / STATE.md D-24 PII precedent). |
| T-17-03-02 | Tampering | concurrent override writes (whole-map clobber) | high | mitigate | setRoleOverride/clearRoleOverride write only the scoped `roleAssignmentOverrides.{roleId}` dot-path; test asserts no whole-map key is written (D-01, Pitfall 1). |
| T-17-03-03 | Denial of Service | memorable-URL slug/serviceShares failure breaking share | medium | mitigate | Soft-fail try/catch around the slug-claim + serviceShares write; the opaque token share has already succeeded and is returned regardless (D-04). |
| T-17-03-04 | Tampering | cross-org overwrite of serviceShares doc | high | transfer | Enforced by the serviceShares Firestore rules created + deployed in 17-02 (editor-of-embedded-orgId, orgId immutable on update); this plan writes only its own org's doc. |
| T-17-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (17-RESEARCH.md: zero new dependencies). |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/services.test.ts` green (override-scope, PII-free snapshot, and soft-fail cases).
- `npx vue-tsc --build` green.
- Per-wave gate: `npm run test:unit` green after this wave merges.
</verification>

<success_criteria>
- Override writes are scoped and never rewrite the whole map; clear uses deleteField.
- The share flow embeds names-only who-is-serving into both share docs and soft-fails the memorable-URL step.
</success_criteria>

<output>
Create `.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-03-SUMMARY.md` when done.
</output>
