---
phase: 12-advanced-song-search-and-multi-select-persistent-tag-filteri
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
closes_gap: "UAT test 3 — filter header must not grow unbounded; picker must share the combined union control (Option A)"
files_modified:
  - src/components/TagFilterChecklist.vue
  - src/components/SongSlotPicker.vue
autonomous: true
requirements: [D-07, D-08, D-14]
must_haves:
  truths:
    - "The tag control is a dropdown/popover with a fixed-height trigger — the filter header no longer grows taller as the number of tags increases"
    - "The Hide toggle, Clear action, and the checkbox rows all live INSIDE the popover panel"
    - "Inverted (hide) mode is visible on the trigger even when the popover is closed"
    - "The service-plan picker filters its songs across the union of teamTags ∪ themes ∪ tags, matching the Songs panel"
    - "The picker's tag list is the de-duplicated, sorted union of the three fields"
    - "The component stays presentational (no store import) and its prop/emit contract is unchanged, so both surfaces reuse it unmodified (D-07/D-14)"
  artifacts:
    - path: "src/components/TagFilterChecklist.vue"
      provides: "Dropdown/popover tag control with fixed-height trigger; hide + clear + checklist contained in the panel"
      contains: "max-h-48"
    - path: "src/components/SongSlotPicker.vue"
      provides: "availableTags + tagFilteredSongs widened to the three-field union"
      contains: "teamTags"
  key_links:
    - from: "src/components/TagFilterChecklist.vue trigger"
      to: "internal open ref"
      via: "click toggles the contained popover panel"
      pattern: "open"
    - from: "src/components/SongSlotPicker.vue tagFilteredSongs"
      to: "song.teamTags / song.themes / song.tags"
      via: "carriesChecked union match against songStore.tagFilterChecked"
      pattern: "song.*teamTags"
---

<objective>
Close the remaining UAT test-3 gap parts: (1) convert the shared `TagFilterChecklist.vue` from an always-inline list into a dropdown/popover with a FIXED-HEIGHT trigger so the filter header stays a constant height regardless of tag count (the core "grows and grows" complaint) — keeping its prop/emit contract identical so both mounting surfaces inherit the popover for free; (2) widen the service-plan picker (`SongSlotPicker.vue`) so its tag list and its `tagFilteredSongs` match the union of `teamTags ∪ themes ∪ tags`, consistent with the Songs panel.

Purpose: A contained, non-growing tag control shared across both surfaces, filtering across all three tag-like fields.
Output: Edited `TagFilterChecklist.vue` (popover), edited `SongSlotPicker.vue` (union).

Scope guard (Option A): Presentational component only — do NOT import the store into TagFilterChecklist. Do NOT change the component's props/emits (both call sites must keep working unmodified). Keep the three Song fields separate — no data-model merge, no PC-import or orchestra-filter changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UAT.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UI-SPEC.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-04-SUMMARY.md

<interfaces>
TagFilterChecklist.vue CURRENT contract (KEEP IT IDENTICAL — do not rename or add props/emits):
```typescript
const props = defineProps<{
  availableUserTags: string[]   // now carries the combined union at call sites; keep the name
  checkedTags: Set<string>
  hide: boolean
}>()
const emit = defineEmits<{
  'update:checkedTags': [value: Set<string>]
  'update:hide': [value: boolean]
  clear: []
}>()
function toggleTag(tag: string) {
  const next = new Set(props.checkedTags)
  next.has(tag) ? next.delete(tag) : next.add(tag)
  emit('update:checkedTags', next)
}
```
Existing visual tokens to preserve: checked row `border-pink-800 bg-pink-900/50 text-pink-300`, unchecked `border-gray-700 bg-gray-800 text-gray-300`, checkbox `focus:ring-1 focus:ring-indigo-500`, "Clear tags" link `text-xs text-gray-500 hover:text-gray-300 transition-colors`, `(hiding)` caption `text-xs font-semibold text-indigo-300`, empty state copy `No tags yet` / `Add tags to songs in the Songs panel to filter by them here.`. Typography: weights 400/600 ONLY — no `font-medium`.

Existing close-on-outside-click pattern already used in this codebase (SongSlotPicker.vue lines 28-32): a transparent full-screen backdrop `<div class="fixed inset-0 z-30" @click="close">` beneath a higher-z panel. Reuse this pattern for the popover so it works identically inside the picker's `overflow-y-auto` Teleported dropdown and in the Songs-panel row.

SongSlotPicker.vue CURRENT tag logic to widen:
```typescript
// availableTags computed (lines 247-253) — currently song.tags only:
const availableTags = computed<string[]>(() => {
  const tagSet = new Set<string>()
  for (const song of props.songs) { for (const tag of (song.tags ?? [])) tagSet.add(tag) }
  return Array.from(tagSet).sort()
})
// tagFilteredSongs computed (lines 256-264) — currently song.tags only:
const carriesChecked = (s.tags ?? []).some((t) => checked.has(t))
```
The `<TagFilterChecklist :availableUserTags="availableTags" ... />` mount (lines 50-57) stays; only the computeds change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert TagFilterChecklist.vue into a fixed-height-trigger dropdown/popover</name>
  <files>src/components/TagFilterChecklist.vue</files>
  <read_first>
    - src/components/TagFilterChecklist.vue (full current file — 70 lines)
    - src/components/SongSlotPicker.vue lines 25-58 (Teleport dropdown + backdrop-close pattern + sticky bar the checklist sits in)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UI-SPEC.md (Color/Typography/Interaction contract — pink identity, indigo focus, 400/600 weights, bounded scroll)
  </read_first>
  <action>
    Refactor TagFilterChecklist.vue so it renders a fixed-height TRIGGER plus a CONTAINED popover PANEL, instead of an always-expanded inline list. Keep it presentational (NO `@/stores/songs` import) and keep props/emits EXACTLY as they are today.

    Add internal-only state: `import { ref } from 'vue'` and `const open = ref(false)`.

    Template structure:
    1. Wrapper: `<div class="relative">`.
    2. TRIGGER button (fixed height, aligns with the sibling `<select>`s): a `<button type="button" @click="open = !open">` using the same control chrome as the filter selects — `rounded-md bg-gray-800 border border-gray-700 px-3 py-2 text-sm text-gray-100 focus:outline-none focus:ring-1 focus:ring-indigo-500`. Button label:
       - Base text `Tags`.
       - When `checkedTags.size > 0`, append the count, e.g. `Tags (2)`.
       - When `hide` is true, show the persistent `(hiding)` caption INSIDE the trigger (`text-xs font-semibold text-indigo-300`) so inverted mode is visible while the popover is CLOSED (satisfies the UI-SPEC requirement that inverted mode not rely on a scrolled-away toggle).
       - Include a small caret/chevron SVG at Claude's discretion (inline SVG, heroicons-style, `h-4 w-4 text-gray-500`).
       The trigger's height MUST NOT change with tag count — this is the fix for the "header grows and grows" complaint.
    3. When `open`, render a transparent full-screen backdrop `<div class="fixed inset-0 z-30" @click="open = false"></div>` (reuse the SongSlotPicker close pattern) followed by the POPOVER PANEL positioned relative to the wrapper: `<div class="absolute z-40 mt-1 w-56 rounded-md bg-gray-800 border border-gray-700 shadow-xl p-2">` (width/placement at Claude's discretion, but it must overlay — not push — surrounding content).
    4. Inside the panel, render the EXISTING content unchanged in behavior:
       - Header row: the "Hide tags" checkbox + label (`title="Invert: hide checked tags instead of showing only them"`, indigo-accented) with the `(hiding)` caption when `hide`, and the "Clear tags" gray link (`text-xs text-gray-500 hover:text-gray-300 transition-colors`, `title="Clear tag filter"`, `@click="$emit('clear')"`).
       - The scrollable checklist `max-h-48 overflow-y-auto`: one `<label>` row per tag, `py-1` density, checkbox `:checked="checkedTags.has(tag)" @change="toggleTag(tag)"`, checked-row static classes `border-pink-800 bg-pink-900/50 text-pink-300` else `border-gray-700 bg-gray-800 text-gray-300` (literal strings, not concatenated — Tailwind v4 purge-safety).
       - The empty state (`availableUserTags.length === 0`): `No tags yet` heading + `Add tags to songs in the Songs panel to filter by them here.` body.
    5. Do NOT close the panel when toggling a checkbox or the hide toggle (multi-select must stay open); only the backdrop click, the trigger click, or the Clear action need not force-close (Clear may leave it open at discretion).

    Typography: weights 400/600 only — NO `font-medium`. Do not add a store import. Do not change the prop/emit names or signatures.
    Commit `fix(12-07): make TagFilterChecklist a fixed-height dropdown/popover so the filter header stops growing`.
  </action>
  <verify>
    <automated>npm run type-check && npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TagFilterChecklist.vue contains an internal `open` ref and a `<button>` trigger toggling it
    - The trigger renders `Tags`, a count when `checkedTags.size > 0`, and a `(hiding)` indicator when `hide` is true
    - The checklist rows, Hide toggle, and Clear action are inside a conditionally-rendered (`v-if="open"`) panel; the panel uses `max-h-48 overflow-y-auto`
    - A `fixed inset-0` backdrop closes the panel on outside click
    - defineProps still declares exactly `availableUserTags`, `checkedTags`, `hide`; defineEmits still exactly `update:checkedTags`, `update:hide`, `clear` (contract unchanged)
    - Does NOT import from `@/stores/songs`; does NOT contain `font-medium`; retains `border-pink-800 bg-pink-900/50 text-pink-300` and `focus:ring-indigo-500`
    - `npm run type-check` exits 0 and `npm run build-only` exits 0
  </acceptance_criteria>
  <done>The tag control is a contained popover with a fixed-height trigger; header no longer grows; both surfaces inherit it unmodified; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the picker's tag list and filter to the three-field union</name>
  <files>src/components/SongSlotPicker.vue</files>
  <read_first>
    - src/components/SongSlotPicker.vue (lines 244-264 availableTags + tagFilteredSongs; confirm the checklist mount at 50-57 is untouched)
    - src/stores/songs.ts filteredSongs union logic (mirror the same three-field carriesChecked so both surfaces behave identically)
  </read_first>
  <action>
    In src/components/SongSlotPicker.vue, widen ONLY the two tag computeds to the union of `teamTags ∪ themes ∪ tags`:

    A) `availableTags` computed — collect from all three fields:
    ```typescript
    const availableTags = computed<string[]>(() => {
      const tagSet = new Set<string>()
      for (const song of props.songs) {
        for (const t of (song.teamTags ?? [])) tagSet.add(t)
        for (const t of (song.themes ?? [])) tagSet.add(t)
        for (const t of (song.tags ?? [])) tagSet.add(t)
      }
      return Array.from(tagSet).sort()
    })
    ```

    B) `tagFilteredSongs` computed — widen `carriesChecked` to the union (keep the `checked.size === 0 → return props.songs` short-circuit and the `hide ? !carriesChecked : carriesChecked` semantics):
    ```typescript
    const carriesChecked =
      (s.teamTags ?? []).some((t) => checked.has(t)) ||
      (s.themes ?? []).some((t) => checked.has(t)) ||
      (s.tags ?? []).some((t) => checked.has(t))
    ```

    Leave everything else untouched: the `<TagFilterChecklist :availableUserTags="availableTags" ... />` mount (prop name kept), the sticky bar classes, `searchResults`, the IntersectionObserver load-more machinery, the orchestra-first sort, and `isNonOrchestraSong` (Phase 10 orchestra logic — out of scope, do NOT change).
    Commit `fix(12-07): widen picker tag list and filter to teamTags∪themes∪tags union`.
  </action>
  <verify>
    <automated>npm run type-check && npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SongSlotPicker.vue `availableTags` computed collects from `song.teamTags`, `song.themes`, AND `song.tags`
    - `tagFilteredSongs` `carriesChecked` references `s.teamTags`, `s.themes`, AND `s.tags`, keeping the size-0 short-circuit and hide/show semantics
    - The `<TagFilterChecklist>` mount and sticky-bar classes are unchanged; `isNonOrchestraSong` / orchestra sort untouched
    - `npm run type-check` exits 0 and `npm run build-only` exits 0
    - `npx vitest run src/stores/__tests__/songs.test.ts` still passes (no store regression)
  </acceptance_criteria>
  <done>Picker filters and lists tags across all three fields, matching the Songs panel; orchestra logic intact; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → popover controls | Trigger/checkbox/toggle state flows to the shared store filter (client-side only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Tampering | union tag values rendered in popover rows/trigger | mitigate | Vue text interpolation auto-escapes; no v-html. Popover adds no new sink. |
| T-12-15 | Denial of Service | very large union tag list in the popover | accept | Panel is `max-h-48 overflow-y-auto` bounded scroll; render cost is the same list size already shown inline today, now contained. |
</threat_model>

<verification>
- `npm run type-check` — exits 0
- `npm run build-only` — exits 0
- `npx vitest run src/stores/__tests__/songs.test.ts` — green (picker shares store state; confirms no regression)
- Manual (re-UAT): opening the tag control on both surfaces shows a popover; adding many tags does NOT grow the filter header; hide mode is visible on the closed trigger; picker filters by team tags, themes, and user tags
</verification>

<success_criteria>
- Tag control is a fixed-height-trigger dropdown/popover; filter header height is constant regardless of tag count (D-07/D-14)
- Hide + Clear + checkboxes contained in the panel; inverted mode visible on the closed trigger
- Picker tag list and filter widened to the three-field union, matching the Songs panel
- Component stays presentational with an unchanged prop/emit contract; orchestra logic and data model untouched (Option A)
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-07-SUMMARY.md`
</output>
