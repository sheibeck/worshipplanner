---
phase: 12-advanced-song-search-and-multi-select-persistent-tag-filteri
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/components/SongSlotPicker.vue
autonomous: true
requirements: [D-07, D-09, D-10, D-11, D-14]
must_haves:
  truths:
    - "The service-plan song picker shows the same shared TagFilterChecklist instead of its two old Only/Hide selects"
    - "The picker's tag filter reads and writes the SAME shared store state as the Songs panel (a tag checked in one surface is checked in the other)"
    - "The picker's search box inherits the upgraded field-scoped/AND search engine automatically"
    - "Toggling Hide / Clear in the picker behaves identically to the Songs panel"
    - "Load-more batching still resets when the tag filter changes"
  artifacts:
    - path: "src/components/SongSlotPicker.vue"
      provides: "Picker consuming shared search engine + shared TagFilterChecklist + shared store tag state"
      contains: "TagFilterChecklist"
  key_links:
    - from: "src/components/SongSlotPicker.vue"
      to: "songStore.tagFilterChecked / tagFilterHide / clearTagFilter"
      via: "useSongStore + v-model on TagFilterChecklist"
      pattern: "tagFilterChecked"
    - from: "src/components/SongSlotPicker.vue tagFilteredSongs"
      to: "shared checked Set + hide flag"
      via: "OR-in-show / exclude-in-hide (mirrors store filteredSongs)"
      pattern: "tagFilterHide"
---

<objective>
Make the service-plan picker (`SongSlotPicker.vue`) consume the SHARED tag filter (D-14): replace its two local `<select>`s and local `includeTag`/`excludeTag` state with the shared `TagFilterChecklist` bound to the store's `tagFilterChecked`/`tagFilterHide`, and rework its local `tagFilteredSongs` to use the same OR/hide logic as the store so both surfaces behave identically. The picker's search box already calls `songMatchesQuery`, so it inherits Plan 01's upgrade for free (D-07) — no engine change here.

Purpose: One search behavior and one remembered tag filter across the picker and the Songs panel.
Output: Edited `SongSlotPicker.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UI-SPEC.md

<interfaces>
Shared store (Plan 02, src/stores/songs.ts): tagFilterChecked: Ref<Set<string>>, tagFilterHide: Ref<boolean>, clearTagFilter(): void.
Shared component (Plan 03, src/components/TagFilterChecklist.vue): props { availableUserTags: string[]; checkedTags: Set<string>; hide: boolean }; emits update:checkedTags, update:hide, clear.

Current SongSlotPicker.vue (verified):
- Imports: line 212 `import { ref, computed, watch, nextTick, onMounted, onUnmounted } from 'vue'`; line 214 `import { songMatchesQuery, getPrimaryKey } from '@/utils/songSearch'`; line 219 `import TeamTagPill`. Does NOT yet import useSongStore or TagFilterChecklist.
- Sticky bar: lines 40-65, `sticky top-0 z-10 bg-gray-800 border-b border-gray-700 p-2 space-y-1.5`, search input (lines 41-47, keep as-is), the two tag `<select>`s (lines 49-64) to REMOVE.
- Local state: `includeTag`/`excludeTag` (lines 249-250) to REMOVE; `availableTags` computed (lines 253-259) — KEEP (used to feed the checklist's availableUserTags; it already collects distinct Song.tags).
- `tagFilteredSongs` (lines 262-268) — REWORK to shared OR/hide logic.
- `searchResults` (lines 276-291) reads from tagFilteredSongs — leave its structure.
- Reset watches: lines 335-338 watch searchQuery / includeTag / excludeTag / props.songs. REMOVE the includeTag/excludeTag watches; ADD watches on the shared store tag state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace picker-local tag selects + state with shared store state and OR/hide logic</name>
  <files>src/components/SongSlotPicker.vue</files>
  <read_first>
    - src/components/SongSlotPicker.vue lines 38-66 (sticky bar template), 210-338 (imports, tag state, tagFilteredSongs, searchResults, load-more watches)
    - src/stores/songs.ts (the shared tagFilterChecked/tagFilterHide/clearTagFilter from Plan 02 — the OR/hide logic to mirror in filteredSongs)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md (SongSlotPicker.vue section lines 224-249)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UI-SPEC.md (sticky header placement line 114; keep compact p-2, no vertical bloat)
  </read_first>
  <action>
    In src/components/SongSlotPicker.vue `<script setup>`:
    1. Add imports:
       - `import { useSongStore } from '@/stores/songs'`
       - `import TagFilterChecklist from '@/components/TagFilterChecklist.vue'`
       - and instantiate `const songStore = useSongStore()` near the other setup consts.
    2. Remove `const includeTag = ref('')` and `const excludeTag = ref('')` (lines 249-250).
    3. Keep `availableTags` computed (lines 253-259) — it already returns distinct sorted Song.tags from props.songs; this feeds the checklist.
    4. Rework `tagFilteredSongs` (lines 262-268) to use the SHARED store state with the SAME OR/hide semantics as the store's filteredSongs (D-09/D-10):
       ```typescript
       const tagFilteredSongs = computed<Song[]>(() => {
         const checked = songStore.tagFilterChecked
         const hide = songStore.tagFilterHide
         if (checked.size === 0) return props.songs
         return props.songs.filter((s) => {
           const carriesChecked = (s.tags ?? []).some((t) => checked.has(t))
           return hide ? !carriesChecked : carriesChecked
         })
       })
       ```
    5. Update load-more reset watches (lines 335-338): remove `watch(includeTag, ...)` and `watch(excludeTag, ...)`; add:
       ```typescript
       watch(() => songStore.tagFilterChecked, () => { visibleCount.value = BATCH_SIZE }, { deep: true })
       watch(() => songStore.tagFilterHide, () => { visibleCount.value = BATCH_SIZE })
       ```
       Keep the `searchQuery` and `props.songs` watches.
    Do NOT change `searchResults` logic (it already calls `songMatchesQuery`, inheriting Plan 01) or the load-more machinery. Commit `feat(12-04): picker consumes shared tag-filter state`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "includeTag\|excludeTag" src/components/SongSlotPicker.vue` returns NO matches
    - src/components/SongSlotPicker.vue imports `useSongStore` and `TagFilterChecklist`
    - tagFilteredSongs references `songStore.tagFilterChecked` and `songStore.tagFilterHide` with `hide ? !carriesChecked : carriesChecked`
    - Load-more watches now watch `songStore.tagFilterChecked` (deep) and `songStore.tagFilterHide`, not includeTag/excludeTag
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Picker uses shared store tag state with OR/hide logic; local tag selects/state removed; type-check clean; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Mount TagFilterChecklist in the picker's sticky bar (compact)</name>
  <files>src/components/SongSlotPicker.vue</files>
  <read_first>
    - src/components/SongSlotPicker.vue lines 38-66 (sticky bar template — the two selects to replace)
    - src/components/TagFilterChecklist.vue (prop/emit contract)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UI-SPEC.md (sticky header, quick-task 260701-awp: keep p-2, do not reintroduce vertical bloat; checklist scroll-containment)
  </read_first>
  <action>
    In the sticky bar (lines 40-65) of src/components/SongSlotPicker.vue:
    - Keep the search input (lines 41-47) exactly as-is (placeholder `Search songs...`, existing classes). You MAY add `title="Filter by field: tag: key: type: theme: team:"` to the input but MUST NOT change its height/classes.
    - Remove the two tag `<select>` blocks (lines 49-64, the whole `<div class="flex items-center gap-2">...</div>`).
    - Mount the shared checklist in their place, bound to the store:
      ```html
      <TagFilterChecklist
        :availableUserTags="availableTags"
        :checkedTags="songStore.tagFilterChecked"
        :hide="songStore.tagFilterHide"
        @update:checkedTags="songStore.tagFilterChecked = $event"
        @update:hide="songStore.tagFilterHide = $event"
        @clear="songStore.clearTagFilter()"
      />
      ```
    - Keep the sticky container classes EXACTLY `sticky top-0 z-10 bg-gray-800 border-b border-gray-700 p-2 space-y-1.5` (established by quick-task 260701-awp). Do NOT add extra padding or wrappers that grow the header height beyond what the checklist's own `max-h-48` scroll container needs.
    Commit `feat(12-04): mount shared TagFilterChecklist in picker sticky bar`.
  </action>
  <verify>
    <automated>npm run type-check && npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SongSlotPicker.vue template contains `<TagFilterChecklist` bound to `songStore.tagFilterChecked`, `songStore.tagFilterHide`, and `@clear="songStore.clearTagFilter()"`
    - The two old `<select>` blocks (with `Show all tags` / `Hide no tags` options) are gone (grep for `Show all tags` and `Hide no tags` returns NO matches)
    - The sticky container still uses `sticky top-0 z-10 bg-gray-800 border-b border-gray-700 p-2 space-y-1.5`
    - Search input placeholder `Search songs...` unchanged
    - `npm run type-check` exits 0 and `npm run build-only` exits 0
  </acceptance_criteria>
  <done>Picker sticky bar renders the shared checklist bound to the store; old selects removed; compact header preserved; build clean; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → picker checklist | Checkbox/toggle state flows to the same shared store filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Information Disclosure | shared tag filter applies across picker ↔ Songs panel | accept | Cross-surface single shared state is the explicit user decision (D-14); same user/session, no data boundary crossed. Persistence scoping handled in Plan 02 (per user/org key). |
| T-12-11 | Tampering | tag labels rendered in picker checklist | mitigate | Rendered via Vue text interpolation in the shared TagFilterChecklist (auto-escaped, no v-html) — inherited from Plan 03. |
</threat_model>

<verification>
- `npm run type-check` — exits 0
- `npm run build-only` — exits 0
- No `includeTag`/`excludeTag` or `Show all tags`/`Hide no tags` references remain
- Manual: a tag checked in the Songs panel is checked in the picker and vice versa; search syntax (`tag:`, `key:E`, `Type 1`) works in the picker box
</verification>

<success_criteria>
- Picker consumes shared search engine (D-07, inherited) and shared TagFilterChecklist + store state (D-14)
- OR show / Hide invert / Clear behave identically to the Songs panel (D-09/D-10/D-11)
- Compact sticky header preserved (no vertical bloat)
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-04-SUMMARY.md`
</output>
