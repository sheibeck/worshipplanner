---
phase: 12-advanced-song-search-and-multi-select-persistent-tag-filteri
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/songSearch.ts
  - src/utils/__tests__/songSearch.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07]
must_haves:
  truths:
    - "Searching 'tag:Orchestra key:E' matches only songs carrying user-tag containing 'orchestra' AND an arrangement in key E"
    - "Searching 'Type 1' matches songs in VW category 1 (natural phrase, no colon needed)"
    - "Searching 'Key A' matches songs with an arrangement in key A"
    - "Searching a bare term still matches across the full existing field set (title, ccli, author, themes, teamTags, vwTypes, tags, notes, key)"
    - "key: filter stays exact (key:E matches E, not Em or Eb)"
  artifacts:
    - path: "src/utils/songSearch.ts"
      provides: "Multi-term AND + field-scoped prefix + phrase-aware songMatchesQuery"
      contains: "songMatchesQuery"
    - path: "src/utils/__tests__/songSearch.test.ts"
      provides: "Tests for prefixes, phrases, multi-term AND"
      contains: "tag:"
  key_links:
    - from: "src/utils/songSearch.ts"
      to: "Song fields (tags, themes, teamTags, vwTypes, arrangements.key)"
      via: "per-prefix matcher functions"
      pattern: "type|key|tag|theme|team"
---

<objective>
Extend the shared search engine `songMatchesQuery()` from a single whole-string substring match into a multi-term AND model with field-scoped prefixes (`type:`/`key:`/`tag:`/`theme:`/`team:`) and natural two-word phrase recognition (`Type 1`, `Key A`). This is the shared foundation both surfaces consume, so it must keep the exact same `(song, query): boolean` signature (D-07).

Purpose: Make any song findable by name or any known metadata, with combinable precision filters, identically in the picker and the Songs panel.
Output: An upgraded `songSearch.ts` plus expanded tests. No caller changes required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md

<interfaces>
From src/types/song.ts — the Song fields this engine reads (do NOT change these):
- title: string
- ccliNumber: string | number
- author?: string
- themes: string[]           (theme: prefix; also bare)
- teamTags: string[]         (team: prefix; also bare)
- vwTypes: VWType[]          (type: prefix; VWType is 1 | 2 | 3)
- tags?: string[]            (tag: prefix — USER tags; also bare)
- notes?: string
- arrangements: Arrangement[] with .key: string   (key: prefix, EXACT match; also bare)
VW_TYPE_LABELS: Record<VWType, string> — imported from '@/types/song', e.g. { 1: 'call to worship', 2: 'intimate', 3: '...' }

Signature MUST stay: `export function songMatchesQuery(song: Song, query: string): boolean`
Callers (unchanged): src/stores/songs.ts line 40; src/components/SongSlotPicker.vue line ~280.
Keep getPrimaryArrangement / getPrimaryKey (lines 37-48) untouched.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing tests for prefixes, phrases, and multi-term AND</name>
  <files>src/utils/__tests__/songSearch.test.ts</files>
  <read_first>
    - src/utils/__tests__/songSearch.test.ts (existing tests — you EXTEND, do not delete the existing describe blocks or the makeSong helper)
    - src/utils/songSearch.ts (current single-substring implementation, lines 10-31)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md (songSearch.ts section, lines 30-87 — prefix→field mapping table and implementation shape)
  </read_first>
  <behavior>
    Add a NEW `describe('songMatchesQuery — field-scoped + phrases (Phase 12)', ...)` block to the EXISTING test file, reusing the existing `makeSong()` helper. Cover:
    - Field-scoped prefixes (D-03/D-04):
      - `songMatchesQuery(makeSong({ tags: ['Orchestra'] }), 'tag:orch')` === true (case-insensitive substring)
      - `songMatchesQuery(makeSong({ tags: ['Orchestra'] }), 'tag:xyz')` === false
      - `songMatchesQuery(makeSong({ themes: ['Adoration'] }), 'theme:ador')` === true
      - `songMatchesQuery(makeSong({ teamTags: ['Choir'] }), 'team:cho')` === true
      - `songMatchesQuery(makeSong({ vwTypes: [1] }), 'type:1')` === true; `type:2` === false
      - key exactness: song with arrangements keys 'G','A' → `key:a` === true, `key:e` === false; a song with key 'Em' → `key:e` === false (exact, D-04)
    - Space-tolerant prefix (D-02): `key: E` and `key:E` behave identically. Test both `'key: a'` (with space) and `'key:a'` on the same song → both true.
    - Natural two-word phrases (D-05): `songMatchesQuery(makeSong({ vwTypes: [1] }), 'Type 1')` === true; `songMatchesQuery(song-with-key-A, 'Key A')` === true. A lone bare number/letter is NOT inferred: `songMatchesQuery(makeSong({ vwTypes: [2], title:'Zzz', tags:[], themes:[], teamTags:[], ccliNumber:'', author:'', notes:'', arrangements: [] }), '1')` === false (no type inference from a lone '1').
    - Multi-term AND (D-01): `songMatchesQuery(makeSong({ tags:['Orchestra'], arrangements:[{ id:'a', name:'x', key:'E', bpm:null, lengthSeconds:null, chordChartUrl:'', notes:'', teamTags:[] }] }), 'tag:orch key:E')` === true; changing key term to `key:G` on the same song === false (both terms must match).
    - Bare terms still work: keep at least one assertion that a bare multi-word query where each word matches somewhere ANDs correctly, e.g. song title 'Amazing Grace' → `'amazing grace'` === true, and `'amazing xylophone'` === false.
  </behavior>
  <action>
    Append the new describe block to src/utils/__tests__/songSearch.test.ts. Do NOT modify src/utils/songSearch.ts yet. Run the tests and confirm the NEW assertions FAIL (the current implementation does whole-string substring, so `tag:orch` will not match, `Type 1` will not match, `tag:orch key:E` will not AND). Existing tests must still PASS. Commit with message `test(12-01): add failing tests for field-scoped search + phrases`.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/songSearch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - songSearch.test.ts contains the string `field-scoped` in a describe title
    - songSearch.test.ts contains at least one assertion with `'tag:orch'`, one with `'Type 1'`, and one with `'tag:orch key:E'`
    - Running vitest shows the new Phase 12 assertions FAILING and the pre-existing assertions PASSING
    - src/utils/songSearch.ts is unchanged in this task (git diff shows only the test file changed)
  </acceptance_criteria>
  <done>New tests exist and fail; existing tests still pass; test file committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement multi-term AND + prefix parser + phrase pre-parse</name>
  <files>src/utils/songSearch.ts, src/utils/__tests__/songSearch.test.ts</files>
  <read_first>
    - src/utils/songSearch.ts (the function you are rewriting, lines 10-31)
    - src/utils/__tests__/songSearch.test.ts (the tests you must make pass — including the new Phase 12 block)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-PATTERNS.md (prefix→field mapping table lines 62-70, implementation shape lines 72-87)
  </read_first>
  <action>
    Rewrite `songMatchesQuery(song, query)` in src/utils/songSearch.ts to this pipeline (keep signature and the `if (!q) return true` empty-query short-circuit for the whole query):

    1. PHRASE PRE-PARSE (D-05, run on the raw query BEFORE splitting): replace occurrences of the two natural two-word phrases with their colon form. Use word-boundary, case-insensitive regexes so only these two patterns are special-cased:
       - `/\btype\s+([1-3])\b/gi` → `type:$1`
       - `/\bkey\s+([a-g](?:#|b)?m?)\b/gi` → `key:$1`
       Do NOT infer type/key from a lone number or letter — only the explicit two-word `type N` / `key X` forms. (A lone `1` or `A` must fall through to bare matching.)

    2. TOKENIZE (D-01/D-02): split the phrase-normalized query on whitespace, but FIRST normalize `prefix: value` (space after colon) into `prefix:value` so `key: E` tokenizes as one token. Do this by first collapsing `/\b(type|key|tag|theme|team):\s+/gi` → `$1:` (remove the space right after a recognized prefix colon), then `.trim().split(/\s+/)`. Drop empty tokens.

    3. MATCH EACH TOKEN (AND — D-01): return `tokens.every((tok) => matchesToken(song, tok))`. Empty token list (blank query) already handled by the early `return true`.

    4. `matchesToken(song, token)`:
       - Parse `token` against `/^(type|key|tag|theme|team):(.*)$/i`. If it matches, dispatch to the field matcher with the trimmed value:
         - `type` → `song.vwTypes.some((vw) => String(vw) === value || VW_TYPE_LABELS[vw].toLowerCase().includes(value.toLowerCase()))`
         - `key` → EXACT case-insensitive (D-04): `song.arrangements.some((a) => a.key.toLowerCase() === value.toLowerCase())`
         - `tag` → `(song.tags ?? []).some((t) => t.toLowerCase().includes(value.toLowerCase()))`
         - `theme` → `song.themes.some((t) => t.toLowerCase().includes(value.toLowerCase()))`
         - `team` → `song.teamTags.some((t) => t.toLowerCase().includes(value.toLowerCase()))`
         - If value is empty after trim, treat the token as non-constraining → return true (e.g. a dangling `tag:` matches everything, mirroring empty-query behavior).
       - If NOT a recognized prefix token, run the existing full-field bare match against the whole token (the current body: title, ccliNumber, author, themes, teamTags, vwTypes number+label, tags, notes, arrangement key EXACT). Refactor the existing body into a `matchesBareTerm(song, term)` helper that returns boolean; reuse it here.

    Keep imports exactly `import type { Arrangement, Song } from '@/types/song'` and `import { VW_TYPE_LABELS } from '@/types/song'`. Do NOT touch getPrimaryArrangement / getPrimaryKey. Guard against ReDoS: the regexes above are anchored/bounded and operate on user query text only (short strings) — do not introduce nested quantifiers or catastrophic backtracking patterns.

    Run tests until all pass. Commit `feat(12-01): multi-term AND search with field-scoped prefixes and phrases`.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/songSearch.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/songSearch.ts contains a regex matching `^(type|key|tag|theme|team):` (grep: `type\|key\|tag\|theme\|team`)
    - src/utils/songSearch.ts contains `.every(` (multi-term AND)
    - src/utils/songSearch.ts contains a phrase pre-parse for `type` and `key` (grep finds both `type\\s` and `key\\s` regex literals, or equivalent)
    - `npx vitest run src/utils/__tests__/songSearch.test.ts` exits 0 (all tests pass)
    - `npm run type-check` exits 0
    - Function signature line `export function songMatchesQuery(song: Song, query: string): boolean` still present
  </acceptance_criteria>
  <done>All songSearch tests pass, type-check clean, signature unchanged, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → search input | Untrusted free-text query string flows into the parser/regexes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Denial of Service (ReDoS) | songMatchesQuery regex parsing of user query | mitigate | Use only anchored/bounded regexes (`/^(type\|key\|tag\|theme\|team):/`, `/\btype\s+([1-3])\b/`, `/\bkey\s+([a-g](?:#\|b)?m?)\b/`) with no nested quantifiers; queries are short UI strings. No catastrophic backtracking possible. |
| T-12-02 | Tampering / Injection | search token values used as substring/exact string comparisons | accept | Values are only compared via String.includes / === against in-memory song fields — never interpolated into a query language, DOM, or eval. No injection surface. Client-side only. |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/songSearch.test.ts` — all pass
- `npm run type-check` — exits 0
- Manual spot check: bare, prefixed, phrase, and combined queries behave per D-01–D-05
</verification>

<success_criteria>
- Multi-term AND: every whitespace-separated token must match (D-01)
- Field-scoped prefixes `type:`/`key:`/`tag:`/`theme:`/`team:` with space tolerance (D-02/D-03)
- Correct value matching: substring for type/tag/theme/team, exact for key (D-04)
- Phrase pre-parse for `Type N` / `Key X` only (D-05)
- Signature unchanged so both surfaces inherit the upgrade (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-01-SUMMARY.md`
</output>
