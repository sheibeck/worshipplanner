# v2.6 — Deferred Human UAT (batched)

**Policy:** Per the owner's explicit instruction for this autonomous v2.6 run, per-phase human/visual
verification is DEFERRED and batched into one round after Phase 103. Every phase's *automated*
verification (verifier + tests + type-check + code review) still ran and passed per phase; only the
browser/visual click-through items are collected here for the owner to run at milestone end (matching the
v1.4–v2.5 owner-attributed precedent).

**Run these together after Phase 103, before completing the milestone.** Owner-run.

---

## Phase 101 — Owner Console Bible-API toggle (R295, R301)

- [ ] Open the Owner Console → **Organizations** tab in a real browser. Click a row's chevron to open the
      **OrgConfigDrawer**. Confirm an **"Enable Bible API"** checkbox renders directly below "Enable AI
      features" with matching visual style, unchecked when the org's `bibleApiEnabled` is absent/false.
- [ ] Toggle it: it flips, briefly shows "Enabling/Disabling Bible API…", and the Organizations list row
      gains/loses an indigo **"Bible API"** badge next to the org name **without a page refresh**.
- [ ] Confirm the helper copy reads that it governs the paid ESV/NLT auto-fetch and that OFF orgs use the
      manual BibleGateway/paste path (does not imply scripture is unavailable when off).

## Phase 102 — Gated scripture fetch dispatcher (R296, R297)

*No standalone visual UAT — Phase 102 has no new visible UI (enabled path unchanged; disabled path is a
no-op that Phase 103 fills). Its behavior is fully covered by automated tests and is exercised by the
Phase 103 checks below (a disabled org making no ESV/NLT call and falling to the manual path).* Note: the
**server esv/nlt gate deploys with `functions:api`** — until that deploy, only the client gate is live.

## Phase 103 — Manual fallback when Bible API is off (R298, R299, R300)

Set up a test org with Bible API **OFF** (Owner Console → toggle off), then in a service:

- [ ] **Scripture slide (disabled org):** the editor shows the "Bible API is off…" intro, a single **"Open
      in BibleGateway"** link that opens the *entered reference* in a new tab, and a paste box. Paste text →
      it lands on the slide. Edit the reference afterward → the pasted text is NOT wiped (CR-01 fix).
- [ ] **Congregational reading (disabled org, AI ON):** paste a passage, run **Split with AI** → it splits
      the pasted text. Then edit the paste box → the split is NOT discarded (CR-02 fix).
- [ ] **Congregational reading (disabled org, AI OFF):** the "Split with AI" button is hidden, but paste
      still works and lands on the reading (gate independence).
- [ ] **Settings:** with Bible API off, the **"Bible Translation"** card is hidden in that org's Settings;
      re-enable Bible API → the card returns with its prior value (R300).
- [ ] **Enabled org (no regression):** an org with Bible API ON still auto-fetches ESV/NLT exactly as before.

## Cross-cutting

- [ ] After the owner-gated deploy of `functions:api` (server esv/nlt gate) + hosting, re-confirm a disabled
      org gets 403 from the proxy if a client ever bypasses the dispatcher (defense-in-depth), and Berean
      (or the chosen production org) is explicitly enabled via the Owner Console so its scripture auto-fetch
      keeps working.
