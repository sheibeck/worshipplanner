---
milestone: v2.13
name: Volunteer Self-Service & Multi-Church Access
audited: 2026-09-06
status: passed
scores:
  requirements: 12/12 (code-verified)
  phases: 3/3 verified
  integration: 14/14 cross-phase links wired
deploy: PENDING owner confirmation (do not deploy without explicit go-ahead)
uat: deferred-batched (see v2.13-DEFERRED-UAT.md)
---

# Milestone v2.13 — Audit: PASSED (code); UAT + deploy PENDING

**Scope:** Phases 128–130, requirements R394–R405. Built autonomously via `/gsd-autonomous` with per-phase
human UAT deferred to a single batched pass (owner-authorized; deferrals labeled PENDING, not accepted).

## Phase verifications (all PASSED, code-level)
| Phase | Reqs | Verify | Review | Notes |
|-------|------|--------|--------|-------|
| 128 Self-service magic-link request (security-critical) | R394–R399 | PASSED 13/13 | 3 Critical + warnings FIXED | Public endpoint: enumeration-safe (body+timing), rate-limited (per-email + per-org), roster-gated |
| 129 Admin resend (email + copy) | R400–R402 | PASSED 8/8 | 0 Critical; 2 warnings FIXED | Authenticated editor; reuses shared mint core (R402); honest errors |
| 130 Multi-church switcher + sidebar label | R403–R405 | PASSED 4/4 | 1 Critical + 2 warnings FIXED | orgName projection; client-side filter (never selectOrg); sidebar church-name |

## Requirements coverage: 12/12 (code)
R394–R405 all mapped to a phase, implemented in shipped code, and confirmed with a verified cross-phase
touchpoint (integration check). No orphans.

## Cross-phase integration: PASS
14 links wired (shared `mintVolunteerLink` core reused by 128+129; `orgName`
rehearseAccess→services→mySchedule→MyScheduleView/AppSidebar; full request→verify→my-schedule→service E2E;
slug round-trip via continueUrl; admin RosterView→adminVolunteerLink; router coherence). Callable
client/server contracts match field-for-field. 0 orphaned, 0 missing.

## Test baseline
Full app suite green except the single documented `src/storage.rules.test.ts` (Storage-emulator limitation,
pre-existing). Functions suite green. `npm run type-check` clean. Security ALLOW/DENY + timing matrices pass.

## Security
Phase 128's public endpoint carried a full threat model + ALLOW/DENY/timing tests; an adversarial review
found and FIXED 3 Criticals (unverified-failure enumeration, email-normalization oracle, un-padded hit
branch) + a per-org throttle. Phase 129 documents the accepted editor→volunteer impersonation tradeoff.
Phase 130 adds only a non-sensitive `orgName` (already public via orgSlugs), client-side filter, no new
endpoint/rule.

## PENDING before ship (NOT done)
- **Batched human UAT** — see `.planning/v2.13-DEFERRED-UAT.md` (real Resend delivery beyond the owner
  inbox needs DNS domain verification, backlog 999.6; felt rate-limit; multi-church-account switcher;
  cross-route sidebar label; the find-church name-vs-slug UX gap).
- **Owner decisions:** (a) one-time backfill of `orgName` onto pre-v2.13 `rehearseAccess` docs; (b) whether
  to add a church-name search to find-church (currently slug/link only).
- **Owner console prerequisite (shared with v2.12):** enable Email-link (passwordless) sign-in + authorized
  domains in the Firebase console.
- **Production deploy** — owner-gated, per-deploy confirmation. Deploy step MUST run
  `npm --prefix functions run build` first (no predeploy hook), then
  `firebase deploy --only functions,firestore:rules,firestore:indexes,hosting` (indexes: none new this
  milestone; rules: no change; functions: 2 new callables + orgName field is rules-compatible).

## Verdict
**PASSED** at the code/verification level. Milestone is NOT complete/archived and NOT deployed — awaiting
the batched UAT pass and explicit owner deploy confirmation.
