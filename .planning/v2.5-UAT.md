# v2.5 UAT — Invite Email & Non-Google Onboarding

**Owner decision (2026-08-31):** verify before deploy; keep milestone OPEN until deployed. No prod deploy
or milestone close until this UAT is satisfied.

> **Why UAT splits in two:** real email delivery + real Auth-account provisioning only happen through the
> deployed Cloud Function with the owner prereqs done (Email/Password provider ON, Resend DNS verified,
> onboarding-emails toggle ON). Everything else is verifiable locally now. Automated coverage already
> passing: `functions/src/inviteOnboarding.test.ts` (17), appConfig drift-guards, `OnboardingConfigCard.test.ts`,
> mounted `TeamView.test.ts` block, new `LoginView.test.ts`; `npm run type-check` clean.

## Part A — verifiable NOW (local, no deploy, no prod writes)

- [ ] **A1. LoginView invitee hint (R292)** — `npm run dev`, open the login page. The sign-in form shows the
      new one-line hint pointing invitees at the emailed link / "Forgot password?" reset. (No auth needed.)
- [ ] **A2. Owner Console toggle renders (R293)** — sign in as super-admin → Owner Console → Configuration
      tab → the **Onboarding emails** card is present, shows the `(default)` badge, and is **OFF** by default.
      *(Toggling it writes to prod `appConfig/global` — reversible; only flip if you're ready.)*
- [ ] **A3. Unit evidence** — `npx vitest run functions/src/inviteOnboarding.test.ts src/views` is green
      (proves: gmail vs non-Google branching, createUser + reset-link, CR-01 invite-existence gate, the
      3 honest TeamView copy states, R294 best-effort resilience, the `auth/operation-not-allowed` message).

## Part B — verifiable only AFTER owner prereqs + deploy

**Prereqs (owner):** (1) enable Firebase Auth **Email/Password** provider for `worship-planner-bc515`;
(2) complete **Resend DNS domain verification** (`functions/DEPLOY-EMAIL-DOMAIN.md`); (3) flip the Owner
Console **onboarding-emails toggle ON**; (4) deploy `functions:sendInviteOnboardingEmail` (+ the appConfig
client build) and hosting (confirm-then-deploy).

- [ ] **B1. Non-Gmail invite → set-password email (R290, R291)** — invite `someone@<non-gmail-domain>` from
      TeamView. TeamView shows "Invite email sent to …". The invitee receives a "set your password" email;
      clicking the link lets them set a password; they then sign in with email + that password and land in
      the org (membership granted via `ensureUserDocument`).
- [ ] **B2. Gmail invite → notify only (R289)** — invite `someone@gmail.com`. They receive a "sign in with
      Google" notice (no password step); no Auth email/password account is pre-created.
- [ ] **B3. Toggle OFF suppresses email (R293)** — with the toggle OFF, invite someone: TeamView shows the
      "onboarding emails are turned off — tell them to sign in" copy; no email is sent; the invite is still
      recorded (they appear in pending invites).
- [ ] **B4. Best-effort resilience (R294)** — an invite always succeeds and the person appears in pending
      invites even if the email path fails; Google sign-in and existing invite-acceptance still work.
- [ ] **B5. operation-not-allowed message (R292)** — (only observable if the Email/Password provider is
      OFF) attempting email sign-in shows the actionable "…ask your administrator to enable it" message,
      not a generic failure. Enabling the provider (B-prereq 1) resolves it.

## Sign-off
- [ ] Part A verified (local)
- [ ] Owner prereqs 1–3 done
- [ ] Deploy done (confirm-then-deploy)
- [ ] Part B verified (prod)
- [ ] → then `/gsd-complete-milestone v2.5`
