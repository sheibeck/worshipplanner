# v2.10 Deferred Verification (batched to milestone end)

Per owner instruction (2026-09-04): run v2.10 autonomously, **defer human/visual UAT to the end** for
one owner pass. Automated tests + the code-review+fix chain gated every phase inline; this file collects
the small set of items that benefit from a human eyeball. None are known defects — all phases passed
automated verification.

## Status
**DEPLOYED to production 2026-09-05** (coordinated firestore.rules + functions + hosting). A post-deploy
subscription-teardown regression was found by the owner during UAT, hotfixed, and redeployed (hosting) —
**owner-verified fixed live 2026-09-05** (all three reported symptoms gone: nav data persistence,
GettingStarted flap, church-switch schedule/quarters). See the HOTFIX entry in the v2-10 memory note +
`.planning/debug/v2-10-shared-store-teardown-race.md`.

**Owner UAT: PASSED** (2026-09-05) — the heavy real-use exercise (adding volunteers/roles/teams, church
switching, dashboard/songs/services/schedule navigation) also implicitly exercised the rules-guarded
editing paths with no issue. The security-specific items below remain available to eyeball at the owner's
discretion but are non-blocking; milestone closed on this acceptance.

## Phase 117 — Security: Proxy Auth, Rate Limits & Quotas
- Fully code/test verifiable — verifier found NO human-UAT items (pure backend). 660 functions tests green.
- (Optional, post-deploy) confirm a real Planning Center import still works end-to-end once deployed
  (the auth gate now requires a valid app token — it already did for the client, so no expected change).

## Phase 118 — Security: Firestore Rules & Public Share Hardening
- Rules suite 242/242, app suite green, type-check clean — verifier found NO blocking human-UAT items.
- **[optional, visual]** Open a real shared service link (`ShareView`) and the `?view=stage` page and
  eyeball that free-text notes / slot bodies / stage-marker notes no longer appear (R346 + the WR-01
  follow-up). Automated component tests already assert their absence; this is a belt-and-suspenders look.
- (Optional, post-deploy) after the rules deploy, confirm ordinary draft editing + Planning Center
  export + reopen still work in the live app (R341 guard is scoped to createdBy/createdAt only; no live
  write path emits those, verified — but a live smoke is cheap insurance for a rules change).

## Phase 119 — Architecture: Correctness, Batching & Store-Ownership
- (pending)

## Phase 120 — Architecture: God-Module Decomposition
- (pending)
