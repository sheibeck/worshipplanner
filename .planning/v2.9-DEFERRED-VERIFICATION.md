# v2.9 — Deferred (Batched) Human/Hardware UAT

**Owner instruction (2026-09-03):** run all v2.9 phases autonomously and **defer UAT to the end**. Each
phase below is code-complete and auto-verified green (type-check + full app suite at the documented
baseline; code review findings fixed). The items here need real hardware / human eyes and are batched for
one owner pass at milestone end — they did NOT block the autonomous build.

Run these on the **church Mac + projector** (the external-link hardware), Chrome/Edge, ideally the real
**3-monitor** setup.

---

## Phase 114 — Multi-Monitor Assignment Rework (auto-verified 2026-09-03; hardware UAT pending)

Source: `.planning/phases/114-multi-monitor-assignment-rework/114-VERIFICATION.md` (status: human_needed).
Automated: R325/R328/R338 fully proven by unit tests; R324/R326/R327 proven in code + mocks, real-display
behavior below.

1. **N-monitor listing + roles stick (R324/R326/R328):** open Monitor Setup on the 3-monitor Mac; confirm
   all three displays list; assign **two Audience + one Confidence**; Save; reload/reopen — assignments
   persist with **no** false "your monitors changed" prompt on the unchanged layout.
2. **Placement (R327):** Go live; each output window opens fullscreen on **its assigned physical display**
   (both Audience monitors on their own screens, Confidence on its), **without manual dragging**. If Window
   Management permission is denied, the per-window "Go fullscreen" fallback still works.
3. **Nicknames (R338):** typed nicknames show on the setup cards + Run displays panel and survive a reload;
   a blank nickname falls back to the OS label / "Unknown".

*Result: [ ] pass / [ ] issues — ______________________________________________*

---

## Phase 115 — Live-Output Readability & Layout (auto-verified 2026-09-04; hardware UAT pending)

Source: `.planning/phases/115-live-output-readability-layout/115-VERIFICATION.md` (status: human_needed).
Automated: 17/17 code-verifiable must-haves proven (auto-fit math, `--slide-font-scale`→`--slide-fit-scale`
migration, Size-control + fontScale-model removal, `scale(0.8)` hack removal, filmstrip/preview/next-item
UI) — type-check clean + full app suite at baseline. Code-review findings fixed (WR-01/03, IN-01/02;
WR-02 a documented behavior-preservation skip). The 3 items below need the real church Mac + projector.

1. **WYSIWYG at projection distance (R329):** on the church Mac + projector, compare the **Audience** and
   **Confidence** output slide text against the **Run-screen previews/thumbnails** for the same slide at
   real projection distance. Text size/position should match — the auto-fit computed once against the
   canonical 1280×720 stage should look identical across projector, band monitor, and Run previews.
2. **macOS scrollbar stays visible (R332):** on the real macOS Mac (Chrome/Safari), scroll the in-item
   filmstrip horizontally. The horizontal scrollbar must stay visible (not auto-hide via macOS overlay
   scrollbars) and the right-edge fade should cue off-screen content. *(This is the exact defect R332
   targets — the CSS fix (`overflow-x: scroll` + non-overlay `::-webkit-scrollbar` rules) needs a real
   macOS check to confirm it holds.)*
3. **At-a-glance legibility (R330):** on the church Mac + projector at projection distance, confirm the
   enlarged filmstrip thumbnails (`w-48`) and the smaller On-screen preview pane are subjectively legible
   during a live run — a projectionist can read thumbnail content without squinting, and the On-screen
   pane no longer crowds the filmstrip. Exact ratio/px tuning was deferred to this UAT.

*Result: [ ] pass / [ ] issues — ______________________________________________*

---

*(Phase 116 UAT items appended as that phase completes.)*
