# v2.14 Deferred Verification (batched to milestone end)

Per owner instruction 2026-09-07 ("use /gsd-autonomous, defer uat to the end"): phases are built +
auto-verified green; real-hardware / human / visual UAT is batched here for one owner pass at milestone
end. Phases are executed, not fully closed — do NOT re-run them; close them via this batched UAT.

---

## Phase 131 — Trivial Wins (Auto Share-Link & Stage Layout Auto-Populate)

### R421 auto-share-link — narrow residual (owner-aware, accepted 2026-09-07)
- **Scope shipped:** keep `createService()` mint; idempotent fail-closed self-heal in `markAsPlanned()`;
  seed script (`functions/seed-emulator-data.mjs`) fixed to mint a token. Draft-sharing unchanged.
- **UAT check:** Open a few of your OLDER, already-locked/Planned services (created before 2026-08-17)
  that were never shared. Confirm the share link is present/usable. Expected: new services and newly-locked
  services self-heal automatically. **Residual:** a pre-2026-08-17, already-Planned, never-shared service
  won't self-heal on mere viewing (it can't re-enter `markAsPlanned`) — the manual "Share Link" button
  still covers it. **If any such old service shows NO link on open and that bothers you**, we add an
  "on-open" self-heal in ServiceEditorView (a small follow-up). Otherwise, no action.

### R420 stage-layout auto-populate — visual check
- **UAT check:** Open a service with assigned roles/instruments whose Stage Layout canvas is empty →
  confirm markers auto-seed from the roster. Then rearrange markers manually, change the roster, revisit →
  confirm your manual placement is NOT wiped/regenerated/duplicated (one-time seed only).
- **WR-01 fix (persisted flag):** Delete ALL seeded markers, then reload the page → confirm they do NOT
  resurrect (a persisted `stageLayoutAutoSeeded` flag now makes the seed truly once-per-service). Automated
  tests prove this structurally; the visual confirm is the belt-and-suspenders check.

### Phase 131 verifier-flagged human items (status human_needed, 11/11 code must-haves verified)
1. Visual plausibility of the seeded stage-layout grid on a real service.
2. Manual drag/revisit doesn't disturb the layout (automated tests already prove this structurally).
3. Run the seed script against a LIVE emulator to confirm the minted token actually resolves in ShareView
   (the seed token pair was verified statically, not against a running emulator).

---

## Phase 132 — Services Page UX Alignment & Verbiage Cleanup (code-verified 7/10; 3 visual items)

1. **R406/R407 — Services page phone width (~360px):** open `/services` on a phone-width viewport →
   confirm the tabs scroll/wrap and the Share Link / New Service buttons are usable and don't overflow the
   page body; the new "Services" header reads consistently with Songs/Volunteers/Settings.
2. **R407 — Services page desktop:** confirm the desktop layout is visually unchanged from before the
   restructure (no double divider under the header; tabs + buttons where they were).
3. **R408 — /share zebra:** open a shared service-link (`/share/...`) page → confirm the alternating
   light-gray banding is subtle, correctly scoped to the service-plan rows, and NO notes/free-text is
   visible (SEC-S-04 PII gate confirmed intact in code).
- **R409** (banner verbiage) is fully code-verified — the Planned/locked banner no longer says "Planning
  Center already has this plan." and genuine PC export/import copy is unchanged; no visual UAT needed, but a
  glance at a locked service's banner is a nice confirm.
