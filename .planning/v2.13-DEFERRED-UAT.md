# v2.13 Volunteer Self-Service & Multi-Church Access — Deferred UAT & Follow-ups (PENDING)

**Status:** PENDING (owed) — not accepted, not passed. Owner authorized a single batched human-UAT pass
at the end of the autonomous build (`/gsd-autonomous`, defer per-phase UAT). These items MUST be
performed/cleared before the milestone ships to production.

> Automated verification + code review + security-fix passes ran per phase. This file collects only the
> items that genuinely need a human, a live external service, or owner console setup.

---

## Phase 128 — Self-Service Magic-Link Request (public, security-critical)

**Owner prerequisite (shared with v2.12, still required for the feature to work in prod):**
- [ ] Firebase Console → Authentication → Sign-in method → Email/Password → enable **"Email link
      (passwordless sign-in)"** + prod domain in Authorized domains. (Same toggle v2.12 needs.)

**Human UAT (live service — can't be exercised in unit tests):**
- [ ] Real Resend email delivery + inbox click-through: request a link for a rostered email at
      `/{church-slug}/volunteer`, confirm exactly one email arrives and its link completes sign-in.
      (Resend is test-mode in prod → only the owner inbox receives real emails until DNS domain
      verification, backlog 999.6.)
- [ ] Felt end-to-end rate-limit: submit repeatedly for the same email+church and confirm throttling
      with NO user-visible membership signal (same "check your email" confirmation throughout); confirm
      the new per-org daily throttle (default 300/day) behaves.
- [ ] Enumeration-safety in the wild: a roster email and a non-roster email under the same church show
      the identical confirmation + comparable response time.
- [ ] Login-page "Are you a volunteer?" → "find your church" lookup resolves a real slug and routes to
      `/{slug}/volunteer`; an unknown church name shows the not-found state (no crash/redirect loop).
- [ ] `/volunteer/verify` expired/invalid link shows "Request a new link" and returns to the same
      church's request in one tap.

**Observations / accepted limitations (not blockers):**
- `orgSlugs.name` denormalization is applied at slug-claim time, so **already-claimed slugs (e.g. an
  existing `berean`) will not display the church name** on the public request page until the slug is
  re-claimed (or backfilled). Degrades gracefully (page works without the name). Decide at UAT whether to
  backfill existing orgSlugs docs with `name`.

**Deferred hardening (non-urgent, from 128-REVIEW):**
- [ ] WR-03: the 1200ms timing-pad target is a static constant — add telemetry to confirm real hit-branch
      latency stays under it (else the pad is a no-op for slow sends). IN-01 (duplicated `isShallowValidEmail`
      across the two functions TS projects) + IN-02 (cosmetic slug/orgId mismatch) — cleanup, no security impact.

---

## Phase 129 — Admin Resend (Email & Copy)

**Live UAT during the build (owner, 2026-09-06) — CONFIRMED WORKING locally:**
- [x] Copy sign-in link (after the stale-`functions/lib` rebuild — the emulator had not hot-loaded the new callable).
- [x] Self-service find-church → `/{slug}/volunteer` → request → magic-link **sign-in end to end** (once `orgSlugs`
      was seeded — see the seed fix below).
- [x] Admin "Email sign-in link" delivered to **sheibeck@gmail.com** (the Resend account owner).

**Resend test-mode caveat (NOT a bug — same as v2.12/999.6):**
- In test-mode Resend delivers ONLY to the account owner (sheibeck@gmail.com) or `delivered@resend.dev`; any
  other domain (e.g. the seed's `@example.com` volunteers) returns a **422** and no email. The public
  self-service path correctly SWALLOWS this (enumeration-safe generic response, CR-01) — the 422 is a
  server log only. The admin path shows an honest "couldn't send" error (correct for a trusted editor).
  Real volunteer delivery needs Resend DNS domain verification (backlog 999.6).

**Human UAT still owed:**
- [ ] Admin Email + Copy on a real browser/deploy for a volunteer whose email actually receives (post-DNS).

**Product gaps surfaced (decide at UAT — candidate follow-ups):**
- [ ] Find-church input says "Church name or sign-in link" but only resolves a **slug or a link** (no
      name→slug lookup). Either clarify the copy or add a real church-name search.
- [ ] An org can have `organizations/{id}.slug` WITHOUT an `orgSlugs/{slug}` registry entry (seed-created
      orgs, or orgs whose slug predates `claimSlug`). Self-service 404s for them. Consider a prod backfill
      or a lazy auto-claim of the registry doc when an editor loads an org that has a slug but no registry.
      (Local dev fixed via the seed change below.)

**Fixed during the build (owner-surfaced 2026-09-06):**
- [x] `seed:emulator` now writes the `orgSlugs/{slug}` registry (was only writing `organizations.slug`),
      so seeded orgs resolve for memorable share links + the self-service page. *(commit in log)*

---

## Phase 130 — Multi-Church Volunteer Switcher

**Human UAT owed:**
- [ ] A real volunteer on ≥2 churches' rosters (locked services) sees the My Schedule church `<select>` filter
      + the sidebar church-name label ("Multiple churches" when all-selected, the church name when one is
      picked); a single-church volunteer sees NO switcher (unchanged).
- [ ] R404 cross-route: the selected-church label is consistent when opening the volunteer service view
      (satisfied globally via the AppShell sidebar reading the same mySchedule store — confirm visually).

**Owner decision (candidate follow-up):**
- [ ] Pre-orgName `rehearseAccess` docs (services locked before v2.13, or re-synced while the org name was
      unavailable) lack `orgName` until re-projected → church labels fall back to "Unnamed church"/"Your
      church". Decide whether to run a one-time backfill of `orgName` onto existing rehearseAccess docs.
      (New locks / song-file re-syncs populate it automatically.)
