# v2.12 Rehearse Mode — Deferred UAT & Follow-ups (PENDING)

**Status:** PENDING (owed) — not accepted, not passed. Owner authorized a single batched human-UAT pass at
the end of the autonomous build (defer per-phase UAT). These items MUST be performed/cleared before the
milestone ships to production.

> Automated verification passed per phase (goal-backward + tests). This file collects only the items that
> genuinely need a human, a live external service, or owner console setup — plus low-severity hardening
> follow-ups surfaced by the review/verify gates.

---

## Phase 125 — Passwordless Magic-Link Access & Scoped Read Isolation

**Owner prerequisite (blocks the feature end-to-end until done):**
- [ ] Firebase Console → Authentication → Sign-in method → **Email/Password → enable "Email link
      (passwordless sign-in)"** for `worship-planner-bc515`, and add the app's hosting domain to
      **Authorized domains**. Until this is done, every volunteer sign-in fails with
      `auth/operation-not-allowed` (handled gracefully in the UI, but the feature is non-functional).

**Human UAT:**
- [ ] Trigger a real reminder/share email to a rostered volunteer and click the emailed magic sign-in link
      end to end: exactly one email arrives (no second Firebase-branded email), the link lands on
      `/volunteer/verify`, completes sign-in with no password, redirects to `/volunteer` with the user chip,
      a refresh preserves the session, and Sign out returns to the guidance screen. (Requires live Resend
      send + real inbox — depends on the prerequisite above and on Resend DNS domain verification, backlog 999.6.)

**Test-hardening follow-up (low severity — production rule confirmed correct):**
- [ ] `src/rules.test.ts` R377 DENY cases (cross-org / Draft / reopened / not-assigned / enumeration) should
      set `email_verified: true` in their fixtures so each proves its own isolation property rather than
      passing via the CR-01 `email_verified` gate. (Verifier WARNING; the live rule is correct by inspection
      + 11/11 rules tests.)

**Security backlog item surfaced during 125 review (out of this phase's scope):**
- [ ] IN-03: the pre-existing `invites/{email}` / `inviteLookup/{email}` firestore.rules arms trust
      `request.auth.token.email` without `email_verified` (same class as CR-01, narrower blast radius).
      Do NOT change blindly — a password-reset-link completion verifies the email, but this needs care so
      it doesn't break v2.5 invite onboarding. Evaluate as a dedicated security backlog item.

---

## Phase 126 — My Schedule (Volunteer Home)

**Owner prerequisite (blocks the feature until done):**
- [ ] Deploy the new `rehearseAccess` COLLECTION_GROUP composite index + the get/list rule split:
      `firebase deploy --only firestore:indexes,firestore:rules`. Confirm the index reaches **Enabled**
      in Firebase Console → Firestore → Indexes. My Schedule's list query fails until the index is Enabled.

**Human UAT:**
- [ ] After deploy, sign in as a real rostered volunteer via a live magic link and confirm My Schedule
      lists exactly their assigned **Planned** services, grouped This week / Later this month + past, with
      accurate role chips / readiness / countdown, and that **Rehearse →** navigates (to the Phase-127 view).

**⚠ OWNER DECISION NEEDED (go/no-go) — surface at the halt/compact review:**
- [ ] **Venue / time-of-day / call-time on the service card.** ROADMAP SC#4 lists "time · venue" and
      "call time," but **no such fields exist in the `Service`/`OrgSettings` data model**. This build
      **omits them gracefully** (no placeholder) rather than inventing a planner-side data-entry surface
      (which would be scope creep — a new editable field + UI). **Decide:** (a) ACCEPT the omission for
      v2.12 (recommended — keeps scope tight; add later if churches ask), or (b) add a small follow-up to
      introduce `Service.venue` / call-time (+ editor UI + projection field). RESEARCH (A3) flagged this as
      needing an explicit go/no-go; deferred to your review, not decided unilaterally.

---

## Phase 127 — Volunteer Service View (Rehearse / Order of Service / Stage Layout)

**Human UAT (real device — can't be exercised in jsdom):**
- [ ] On desktop AND a phone: open a rehearse service, view + **print** a PDF chart, **play** an MP3, change
      **speed**, toggle **Loop**, open an **external link** (new tab); confirm the read-only **Order of Service**
      and **Stage Layout** tabs render; confirm the mobile drill-down reflow + pinned player.
- [ ] **Cross-browser native PDF page-nav:** confirm the browser's built-in "Page X of Y" viewer chrome
      actually appears on Chrome / Edge / Safari-iOS (R387 delegates page-nav to the native viewer; some
      embedded viewers show only a scroll thumb). If inadequate on a target browser, add a lightweight
      fallback affordance (follow-up).

## Milestone follow-ups FIXED during the build (owner-surfaced 2026-09-06)

- [x] **"My Schedule" nav entry for signed-in members** — added so an admin/planner who also serves can
      reach their own schedule in-app (there was previously no entry point). *(fixed — see commit log)*
- [x] **Magic-link delivery wired (R375 gap)** — Phase 125 built the `{{rehearse_link}}` token but nothing
      used it (no default template, not in the composer palette, not in the auto lock/reminder emails), so
      volunteers never received it. Now wired into the composer token palette + the default reminder/share/
      lock (and cron-reminder) emails so the link ships in the emails volunteers already get. *(fixed — see commit log)*

> **Note on the magic-link flow (for owner testing):** the link is delivered via the v1.7 volunteer
> messaging emails — sent when a planner sends a reminder/share from the ✉ composer, when a service is first
> locked, or by the scheduled-reminder cron (OFF by default). Resend is still test-mode, so real sends
> reach only the Resend account owner's inbox (sheibeck@gmail.com) until DNS domain verification (backlog 999.6).
> Local emulator testing: the Auth emulator prints the sign-in link to its logs (no real email needed).

---
*Created 2026-09-06 during the v2.12 autonomous build.*
