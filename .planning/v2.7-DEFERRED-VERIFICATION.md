# v2.7 Deferred Verification

Per owner instruction (2026-08-31): the autonomous v2.7 run **defers all verification to the end** —
per-phase automated `gsd-verifier` goal-backward verification and human/visual UAT are batched here
rather than gated per phase. Code review (+fix) still ran inline per phase during the build.

At milestone end: run `/gsd-audit-milestone` (automated goal + gate verification across all phases),
then work these human-UAT items with the owner.

## Status by phase

### Phase 104 — Notification & Multi-Church Foundations — IMPLEMENTED + REVIEWED
- Built: 104-01 (notification system, R309/R310), 104-02 (church switcher, R311/R312). type-check clean;
  only the 2 known baselines fail (`storage.rules.test.ts`, stale duplicate `stores/appConfig.test.ts`).
- Code review: 8 findings (1 Critical R312 listener leak in TeamView/GettingStarted, 4 Warning, 3 Info) —
  **all fixed + committed + re-verified**.
- Automated `gsd-verifier`: NOT yet run (deferred).
- **Human UAT (104-D7):** on a real multi-church account, switch churches from the sidebar user menu and
  confirm (a) no stale-data flash from the previous church anywhere (incl. TeamView `/admins` and the
  getting-started home), (b) the correct role shows per church, (c) the switch-failure toast auto-dismisses,
  (d) the "monitors not configured" warning auto-clears once monitors are configured and is dismissible.

### Phase 105 — Presentation Blackout & Inline Black Slide — IMPLEMENTED + REVIEWED
- Built: 105-01 (blackout data model + numbering exclusion), 105-02 (rendering + authoring UI), 105-03
  (go-to-black → Audience only). type-check clean; only the 2 known baselines fail.
- Code review: 3 findings (1 Critical — assembler leaked a background/`From group` chip onto blackout
  slides; 2 Warning) — **all fixed + committed + re-verified**.
- Automated `gsd-verifier`: NOT yet run (deferred).
- **Human UAT (105-D):** (a) in the Song Lyrics editor, insert a black slide between two lyric slides —
  confirm it makes NO new service section, reorders/duplicates/deletes like any slide, and section
  numbering stays correct with a blackout between numbered sections; (b) confirm a blackout slide renders
  solid black (no text/background/label) on the Audience output, the Confidence monitor, the in-app
  preview, and the SlideCard grid; (c) during Run, press "Go to black" and confirm ONLY the Audience
  output goes black while the Confidence monitor keeps showing the current/upcoming slide; (d) visual
  backstop: on the confidence NEXT pane, an upcoming blackout slide vs "no next slide" reads clearly.

### Phase 106 — Per-Item Loop Playback — IMPLEMENTED + REVIEWED
- Built: 106-01 (loop config model + authoring UI, R307), 106-02 (run-time loop timer + go-to-black
  pause, R306/R308). type-check clean; only the 2 known baselines fail.
- Code review: 0 Critical, 2 Warning (reconcile not reactive to mid-run slide-count growth; endRehearsal
  missing defensive disarm), 1 Info (dead code) — **all fixed + committed + re-verified**. Core timer
  invariants (single interval, single-writer via postIndex, wrap-to-first, go-to-black pause) held under
  adversarial review.
- Automated `gsd-verifier`: NOT yet run (deferred). The go-to-black-pause behavior IS covered by an
  automated output-window test (both fake windows open, asserting posted state).
- **Human UAT (106-D):** (a) mark an item as Loop, set an interval (preset + a custom value) — confirm it
  persists across reload; (b) Run the service: the looping multi-slide item auto-advances and loops back
  to its FIRST slide at the interval, never crosses into the next item; a single-slide looping item is a
  no-op; (c) manual nav / switching to another item / leaving the Run screen stops the loop cleanly (no
  runaway advancing, no leaked timer); (d) **[106-02 Task 4 — real second-display spot check]** on real
  hardware, press "Go to black" during a loop and confirm the loop PAUSES (audience frozen black, index
  doesn't advance) and RESUMES on restore.

<!-- Phase 107 appended when complete. -->


