---
phase: 21-powerpoint-import-announcements-and-sermon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/slide.ts
  - src/types/service.ts
  - src/types/importedDeck.ts
  - src/utils/slotTypes.ts
  - src/utils/__tests__/slotTypes.test.ts
  - src/stores/importedSlides.ts
  - src/utils/slideshowAssembler.ts
  - src/utils/__tests__/slideshowAssembler.test.ts
  - src/composables/useSlideshowAssembly.ts
  - src/components/SlideshowPreview.vue
  - src/components/__tests__/SlideshowPreview.test.ts
autonomous: true
requirements: [R010, R011, R012]
must_haves:
  truths:
    - "An IMPORTED slot referencing a persisted imported deck expands into N AssembledSlides via assembleSlideshow, one per deck slide, in deck order (mirrors the SCRIPTURE slot)."
    - "An imported deck can hold both text slides (contentKind 'text') and image slides (contentKind 'image'); the slideshow preview renders an image card for image slides and a text card for text slides."
    - "The importedSlides store subscribes to, creates, updates, and reads organizations/{orgId}/importedSlides docs (same lifecycle contract as scriptureSlides)."
  artifacts:
    - "src/types/slide.ts — ImageSlide interface added and included in the Slide union"
    - "src/types/service.ts — 'IMPORTED' in SlotKind, ImportedSlot in ServiceSlot union"
    - "src/types/importedDeck.ts — ImportedDeck interface"
    - "src/stores/importedSlides.ts — Pinia store"
    - "src/utils/slideshowAssembler.ts — IMPORTED case and AssemblyInputs.importedDecksById"
  key_links:
    - "assembleSlideshow IMPORTED case must key off slot.importId and read inputs.importedDecksById — a mismatch silently drops imported slides."
    - "useSlideshowAssembly must build importedDecksById from the importedSlides store subscription so live decks reach the pure assembler."
---

<objective>
Establish the client-side data model and read/assembly path for imported PowerPoint decks by extending the existing unified slide model and slot union exactly the way the SCRIPTURE slot kind was added in Phase 19/20 — no parallel model, no new assembly paradigm (honors D001 unified slide model, D003/D004).

Purpose: Every downstream plan (upload util, parse function consumer, editor, service wiring) depends on these types, the importedSlides store, and the assembly/preview branches existing first. This plan delivers the complete READ path (subscribe deck → map → assemble → preview); the WRITE path (upload/parse/create) arrives in plan 21-05.
Output: ImageSlide + ImportedSlot + ImportedDeck types, the importedSlides Pinia store, the IMPORTED assembly case, and the SlideshowPreview image render branch — all compiling with green unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-powerpoint-import-announcements-and-sermon/21-RESEARCH.md
</context>

<artifacts_this_phase>
Phase 21 produces (this plan marked ✅): SlotKind 'IMPORTED' ✅, ImageSlide ✅, ImportedSlot ✅, importedDeck.ts ✅, importedSlides store ✅, IMPORTED assembly case ✅, SlideshowPreview image branch ✅; parsePptx Cloud Function (21-04), pptxParser.ts / mapAstToSlides (21-04), storage.rules + getStorage/getFunctions bootstrap (21-02), functions test infra + fixtures (21-03), pptxUpload util + PptxImportModal (21-05), ImportedSlideEditor + ServiceEditorView wiring (21-06).
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: Add ImageSlide, IMPORTED SlotKind, ImportedSlot, ImportedDeck, and the IMPORTED slot factory/label</name>
  <files>src/types/slide.ts, src/types/service.ts, src/types/importedDeck.ts, src/utils/slotTypes.ts, src/utils/__tests__/slotTypes.test.ts</files>
  <read_first>
    - src/types/slide.ts (SlideContentKind already declares 'image' unimplemented; SlideBase; TextSlide; Slide union)
    - src/types/service.ts (SlotKind, ScriptureSlot, ServiceSlot union, ServiceSection)
    - src/types/scriptureReading.ts (the deck shape ImportedDeck mirrors)
    - src/utils/slotTypes.ts (slotLabel switch, createSlot switch, sectionFields pattern)
    - src/utils/__tests__/slotTypes.test.ts (createSlot/slotLabel test patterns)
    - .planning/phases/21-powerpoint-import-announcements-and-sermon/21-RESEARCH.md (Code Examples: "Extending the Slide union with ImageSlide", "Extending SlotKind and ServiceSlot")
  </read_first>
  <action>
    In src/types/slide.ts add interface ImageSlide extends SlideBase with contentKind 'image', imageUrl string, optional altText string; add ImageSlide to the exported Slide union so it becomes LyricSlide | CopyrightSlide | ScriptureSlide | TextSlide | ImageSlide. Do not remove or rename existing members.
    In src/types/service.ts add 'IMPORTED' to the SlotKind union; add interface ImportedSlot with kind 'IMPORTED', position number, importId string | null, optional section ServiceSection; add ImportedSlot to the ServiceSlot union.
    Create src/types/importedDeck.ts exporting interface ImportedDeck mirroring ScriptureReading: id string, sourceFileName string, section ServiceSection, slides array of (TextSlide | ImageSlide), createdAt Timestamp, updatedAt Timestamp (import Timestamp from firebase/firestore, ServiceSection from ./service, TextSlide and ImageSlide from ./slide). Add a source-of-truth doc comment noting it is the imported-PPTX analogue of ScriptureReading.
    In src/utils/slotTypes.ts add a case 'IMPORTED' to slotLabel returning the label 'Imported Slides'; add a case 'IMPORTED' to createSlot returning an object with kind 'IMPORTED', position 0, importId null, spreading sectionFields (same section-omission pattern used by the other kinds). Because both switches have no default case, adding 'IMPORTED' to SlotKind will force these edits to compile.
    Extend src/utils/__tests__/slotTypes.test.ts with cases asserting createSlot('IMPORTED') yields kind 'IMPORTED' with importId null and position 0, createSlot('IMPORTED', undefined, 'pre-service') carries section 'pre-service', createSlot('IMPORTED') omits the section key when no section is passed, and slotLabel over an IMPORTED slot returns 'Imported Slides'.
  </action>
  <acceptance_criteria>
    - src/types/slide.ts exports an ImageSlide interface whose contentKind is the literal 'image', and the Slide union includes ImageSlide.
    - src/types/service.ts SlotKind includes 'IMPORTED' and ServiceSlot union includes ImportedSlot with an importId field typed string | null.
    - src/types/importedDeck.ts exports ImportedDeck with a slides field typed as an array of TextSlide | ImageSlide.
    - createSlot('IMPORTED') returns { kind: 'IMPORTED', position: 0, importId: null } with no section key; passing a section adds it.
    - slotLabel returns 'Imported Slides' for an IMPORTED slot.
    - vue-tsc build passes (exhaustive switches compile with the new kind).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slotTypes.test.ts && npx vue-tsc --build</automated>
  </verify>
  <done>New types compile, slot factory/label handle IMPORTED, and slotTypes tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the importedSlides Pinia store (mirror scriptureSlides)</name>
  <files>src/stores/importedSlides.ts</files>
  <read_first>
    - src/stores/scriptureSlides.ts (the store this mirrors: subscribeReadings/unsubscribeReadings/createReading/updateReading/getReading, onSnapshot + orderBy updatedAt desc, serverTimestamp on create/update)
    - src/types/importedDeck.ts (ImportedDeck — created in Task 1)
    - src/firebase/index.ts (db export)
  </read_first>
  <action>
    Create src/stores/importedSlides.ts defining a Pinia store useImportedSlides that mirrors useScriptureSlides against the organizations/{orgId}/importedSlides subcollection. Provide: a reactive decks ref (ImportedDeck array) and isLoading ref; subscribeDecks(orgId) using onSnapshot over a query ordered by updatedAt desc, mapping doc id plus data into ImportedDeck; unsubscribeDecks() clearing the subscription and state; createDeck(orgId, data) via addDoc with serverTimestamp createdAt/updatedAt returning the new doc id, where data is Omit<ImportedDeck,'id'|'createdAt'|'updatedAt'>; updateDeck(orgId, importId, partial) via updateDoc setting updatedAt serverTimestamp; getDeck(orgId, importId) via getDoc returning ImportedDeck or null. Use the same import surface from firebase/firestore as scriptureSlides.ts.
  </action>
  <acceptance_criteria>
    - useImportedSlides exposes subscribeDecks, unsubscribeDecks, createDeck, updateDeck, getDeck, decks, isLoading.
    - createDeck writes to the organizations/{orgId}/importedSlides collection path and stamps createdAt and updatedAt with serverTimestamp.
    - updateDeck stamps updatedAt with serverTimestamp and targets a doc in the same collection.
    - The module type-checks under vue-tsc.
  </acceptance_criteria>
  <verify>
    <automated>npx vue-tsc --build</automated>
  </verify>
  <done>importedSlides store compiles and exposes the full subscribe/create/update/get surface used by later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the assembly engine, composable, and preview for IMPORTED / image slides</name>
  <files>src/utils/slideshowAssembler.ts, src/utils/__tests__/slideshowAssembler.test.ts, src/composables/useSlideshowAssembly.ts, src/components/SlideshowPreview.vue, src/components/__tests__/SlideshowPreview.test.ts</files>
  <behavior>
    - An IMPORTED slot whose importId resolves to a deck in importedDecksById emits one AssembledSlide per deck slide, preserving deck order, with slotKind 'IMPORTED', the slot's section, and sourceId equal to the importId.
    - An IMPORTED slot with importId null, or whose importId is absent from importedDecksById, contributes zero slides (skipped, not errored) — same tolerance as the SCRIPTURE case.
    - SlideshowPreview renders an <img> whose src is the slide imageUrl for an image slide, and continues to render the existing text card for a text slide.
  </behavior>
  <read_first>
    - src/utils/slideshowAssembler.ts (AssemblyInputs interface; the SCRIPTURE case at reading.slides.forEach; the emit helper signature)
    - src/utils/__tests__/slideshowAssembler.test.ts (makeInputs helper, scriptureSlot helper, the "scripture resolution" describe block to mirror)
    - src/composables/useSlideshowAssembly.ts (scriptureReadingsById computed map built from the scripture store; how the store is subscribed by resolvedOrgId watch; the assembleSlideshow call site)
    - src/components/SlideshowPreview.vue (cardKind function, CardKind type, the scripture/text render branches)
    - src/components/__tests__/SlideshowPreview.test.ts (existing render assertions)
    - src/types/importedDeck.ts and src/stores/importedSlides.ts (from Tasks 1-2)
  </read_first>
  <action>
    In src/utils/slideshowAssembler.ts import ImportedDeck; add importedDecksById (Map string to ImportedDeck) to the AssemblyInputs interface; add a case 'IMPORTED' to the switch that mirrors the SCRIPTURE case — break when slot.importId is falsy, look up the deck in inputs.importedDecksById, break when absent, else deck.slides.forEach destructuring out id and position and emitting the rest with sourceId slot.importId and localSeq as the iteration index.
    In src/utils/__tests__/slideshowAssembler.test.ts add importedDecksById: new Map() to the makeInputs default object; add an importedSlot helper and a describe block asserting: a populated IMPORTED slot emits one AssembledSlide per deck slide in order with slotKind 'IMPORTED' and sourceId equal to importId; an IMPORTED slot with importId null contributes nothing; an IMPORTED slot whose importId is missing from the map contributes nothing; mixed text+image deck slides pass their contentKind through unchanged.
    In src/composables/useSlideshowAssembly.ts import useImportedSlides; subscribe its decks alongside the scripture subscription in the resolvedOrgId watch; add an importedDecksById computed Map keyed by deck id; pass importedDecksById into the assembleSlideshow inputs object.
    In src/components/SlideshowPreview.vue add 'image' to the CardKind type; extend the cardKind function so an image slide returns 'image'; add a render branch before the trailing text fallback that renders an img element bound to the slide imageUrl with the altText (default empty) as alt text, styled consistently with the existing card body. Import ImageSlide from the slide types for the narrowing cast.
    In src/components/__tests__/SlideshowPreview.test.ts add an assertion that an assembled section containing an image slide renders an img element whose src equals the slide imageUrl.
  </action>
  <acceptance_criteria>
    - assembleSlideshow expands a populated IMPORTED slot into one AssembledSlide per deck slide, in deck order, with slotKind 'IMPORTED' and sourceId equal to the importId.
    - An IMPORTED slot with importId null or an unresolved importId yields zero slides.
    - makeInputs in the assembler test supplies importedDecksById and all existing assembler tests remain green.
    - useSlideshowAssembly passes importedDecksById into assembleSlideshow and subscribes the importedSlides store per org.
    - SlideshowPreview renders an img with the correct src for an image slide; existing text/scripture/lyric cards still render.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideshowAssembler.test.ts src/components/__tests__/SlideshowPreview.test.ts && npx vue-tsc --build</automated>
  </verify>
  <done>IMPORTED slots assemble like SCRIPTURE slots, the composable feeds live decks in, image slides render in the preview, and all touched unit tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is pure client-side type/model extension; no new external input crosses a trust boundary. Upload, parse, and Storage boundaries are introduced in 21-02, 21-04, 21-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01-01 | Tampering | ImageSlide.imageUrl rendered as <img> src | low | mitigate | imageUrl values originate only from org-scoped Storage paths resolved via getDownloadURL in later plans; the preview performs no HTML injection (bound attribute, not v-html). Cross-org access is enforced by storage.rules (21-02). |
</threat_model>

<verification>
- npx vue-tsc --build passes across the touched files.
- npx vitest run over slotTypes, slideshowAssembler, and SlideshowPreview test files is green.
- No existing assembler test regresses after adding importedDecksById to makeInputs.
</verification>

<success_criteria>
ImageSlide, ImportedSlot, ImportedDeck, and the importedSlides store exist and compile; assembleSlideshow expands IMPORTED slots identically to SCRIPTURE; the preview renders image slides; all touched unit suites pass.
</success_criteria>

<output>
Create `.planning/phases/21-powerpoint-import-announcements-and-sermon/21-01-SUMMARY.md` when done.
</output>
