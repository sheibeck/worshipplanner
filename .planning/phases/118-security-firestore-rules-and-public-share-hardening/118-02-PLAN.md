---
phase: 118-security-firestore-rules-and-public-share-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/services.ts
  - src/stores/quarters.ts
  - src/views/ShareView.vue
  - src/views/__tests__/ShareView.test.ts
  - src/stores/__tests__/services.sharePii.test.ts
autonomous: true
requirements: [R346, R347]

must_haves:
  truths:
    - "buildServiceSnapshot's public projection carries no free-text service-level notes and no per-slot notes/body (R346)"
    - "ShareView renders no free-text notes/slot-body even when handed a legacy share doc that still contains them (R346)"
    - "A unit test proves the projection omits the free-text fields, and a component test proves they never render (R346)"
    - "Both memorable-share-id construction sites carry a re-acceptance comment and the phase SUMMARY records the written rationale — no id-format change (R347)"
  artifacts:
    - src/stores/services.ts
    - src/views/ShareView.vue
    - src/stores/__tests__/services.sharePii.test.ts
    - src/views/__tests__/ShareView.test.ts
  key_links:
    - "buildServiceSnapshot allowlist projection <-> the existing roleAssignments/stageLayout names-only allowlist (services.ts:133-172)"
    - "ShareView free-text render gate <-> already-deployed serviceShares docs that still contain notes"
    - "R347 re-acceptance comment <-> id sites services.ts:770 and quarters.ts:416"
---

<objective>
Close the two public-share-page findings from the v2.8 review: free-text notes/slot-body PII on the
public ShareView (R346, real frontend/projection fix) and guessable memorable-URL share ids (R347,
re-accept in writing per the locked CONTEXT branch — the ids are already deployed in shared links and
SEC-S-01's list-lock already removed enumeration).

Purpose: An org editor's free-text can carry PII (a phone number typed into a note) that renders
verbatim to an unauthenticated public page. The fix mirrors the deliberately-guarded, confirmed-sound
roleAssignments/stageLayout names-only allowlist already in this file — only fields meant to be public
reach the page. This plan's tests run in the APP suite (`npx vitest run`), which excludes the rules
harness, so R346 gets its own component/unit coverage that the rules suite cannot provide.

Output: An allowlist-shaped public service snapshot, a render-side gate covering already-deployed
share docs, new app-suite tests proving non-render, and R347's written re-acceptance (SUMMARY + code
comments) with no id-format change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-security-firestore-rules-and-public-share-hardening/118-CONTEXT.md
@.planning/milestones/v2.8-phases/112-security-review/112-SECURITY-REVIEW.md
@CLAUDE.md
@src/stores/services.ts
@src/views/ShareView.vue
@src/views/QuarterShareView.vue

# Interface / idioms this plan mirrors (read before editing):
# - services.ts:76-99 ServiceSnapshot interface; :108-192 buildServiceSnapshot (the R346 target).
# - services.ts:133-172 roleAssignments + stageLayout names-only field-allowlist — the pattern to mirror.
# - services.ts:180-182 the snapshot return (`notes: service.notes` is the free-text service-level leak).
# - ShareView.vue:104-105 per-slot free-text render; :110-113 service-level notes section (the render leak).
# - services.ts:770 serviceShares id `${slug}__service-${service.date}` (R347 comment site).
# - quarters.ts:416 quarterShares id `${slug}__q${quarter}-${year}` (R347 comment site).
# - Test patterns to mirror: src/stores/__tests__/services.stageLayout.test.ts (projection unit test);
#   src/views/__tests__/ShareView.test.ts (component render test; already has a mockSnapshot with notes).
</context>

<tasks>

<task type="auto">
  <name>Task 1: R346 — allowlist-shape the public service snapshot (drop free-text notes/slot-body)</name>
  <files>src/stores/services.ts, src/stores/__tests__/services.sharePii.test.ts</files>
  <action>
Implement the projection half of R346 (SEC-S-04). In `buildServiceSnapshot` (services.ts:108-192),
shape the public payload allowlist-style so free-text never reaches the page, per the locked CONTEXT
R346 decision ("only fields meant to be public reach the page, rather than a blocklist"), mirroring
the confirmed-sound roleAssignments/stageLayout names-only allowlist already in this function
(:133-172):
- Replace the raw per-slot spread (`slotsWithBpm`, built at :119-131 which does `{ ...slot, bpm }`)
  with an explicit per-slot field-allowlist projection that includes ONLY the structured display
  fields each slot kind needs on the share page — e.g. `kind`, `position`, and the kind-specific
  display fields ShareView actually reads: SONG (`songId`, `songTitle`, `songKey`, `bpm`), SCRIPTURE
  (`book`, `chapter`, `verseStart`, `verseEnd`), HYMN (`hymnName`, `hymnNumber`, `verses`), MISC
  (`label`), plus whatever ShareView.vue renders per kind. EXCLUDE the free-text `notes` and legacy
  `body` fields from every slot. Cross-read ShareView.vue's template (:52-105) to enumerate exactly
  which slot fields it renders so the allowlist is complete — a missing structured field would blank
  the share page, an included free-text field would defeat the fix.
- Drop the service-level free-text `notes` from the returned snapshot: remove `notes: service.notes`
  from the return object (:181). Also remove `notes` from the `ServiceSnapshot` interface (:84) so the
  type enforces the omission.
- Preserve everything else exactly: `date`, `name`, `progression`, `teams`, `sermonPassage`, `status`,
  `roleAssignments`, and the conditional `stageLayout` spread are unchanged.
Keep comments short per the convention; one line citing R346/SEC-S-04 and the mirrored allowlist.

Then create `src/stores/__tests__/services.sharePii.test.ts`, mirroring
`services.stageLayout.test.ts`'s setup (the same `vi.mock('firebase/firestore')`, `@/firebase`,
songs/roster/quarters store mocks, `setActivePinia`, and a `makeService` helper). Prove the projection
omits free-text: build a service whose service-level `notes` is a fake phone number and whose slots
carry `notes`/`body` free text, call `buildServiceSnapshot`, and assert `'notes' in snapshot === false`
and that no returned slot has a `notes` or `body` key (and that the PII string appears nowhere in
`JSON.stringify(snapshot)`), while asserting the structured fields (songTitle, book, etc.) ARE present.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.sharePii.test.ts</automated>
  </verify>
  <done>buildServiceSnapshot returns no top-level notes and no per-slot notes/body; structured display fields are preserved; the sharePii unit test passes.</done>
</task>

<task type="auto">
  <name>Task 2: R346 — render-side gate in ShareView so legacy share docs cannot leak either</name>
  <files>src/views/ShareView.vue, src/views/__tests__/ShareView.test.ts</files>
  <action>
Implement the render half of R346. The projection fix (Task 1) only affects NEWLY written share docs;
already-deployed `serviceShares`/`shareTokens` docs in production still carry free-text notes baked
into their stored snapshot. So `ShareView.vue` must also stop rendering the free-text fields:
- Remove the per-slot free-text render at :104-105 (`<p v-if="(slot.notes ?? slot.body)?.trim()" ...>`).
- Remove the service-level Notes section at :109-113 (`<div v-if="serviceSnapshot.notes" ...>` block).
Leave every structured render (song title/key, scripture ref, hymn, Who's Serving roleAssignments,
stage layout) untouched. Short comment citing R346/SEC-S-04.

QuarterShareView.vue needs NO change — it renders names-only from an already-allowlisted calendar
(SEC-S-05 confirmed sound, no free-text render path); do not touch it.

Then extend `src/views/__tests__/ShareView.test.ts`: the existing `mockSnapshot` already includes
`notes: 'Remember to mic the choir'` and slots. Add (or adjust) a test that mounts ShareView with a
snapshot whose service-level `notes` and a slot's `notes`/`body` contain a recognizable PII marker,
awaits `flushPromises()`, and asserts that marker text does NOT appear in the rendered
`wrapper.text()` — proving the gate holds even for a doc that still contains the free-text. If any
existing assertion expects notes to render, update it to reflect the new gated behavior (cite R346).
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ShareView.test.ts</automated>
  </verify>
  <done>ShareView renders no free-text notes/slot-body even when the snapshot contains them; structured sections still render; the ShareView component test passes; QuarterShareView untouched.</done>
</task>

<task type="auto">
  <name>Task 3: R347 — re-accept guessable memorable share ids in writing (comments + SUMMARY), no id change</name>
  <files>src/stores/services.ts, src/stores/quarters.ts</files>
  <action>
Implement R347 (SEC-S-02) via the locked low-risk CONTEXT branch: explicitly re-accept the residual
guessability in writing. Do NOT change the share-id format — `${slug}__service-${date}` and
`${slug}__q${quarter}-${year}` are already deployed and embedded in shared links; changing them would
break every existing link.
- Add a short re-acceptance comment at the id-construction site in services.ts:770 (the
  `setDoc(doc(db, 'serviceShares', \`${slug}__service-${service.date}\`), ...)` call) and at
  quarters.ts:416 (the `quarterShares` `${slug}__q${quarter}-${year}` setDoc). Each comment states the
  re-acceptance rationale concisely: memorable ids are deterministic/guessable by design; the format is
  frozen because it is deployed in live links; and SEC-S-01's list-lock (Phase 113, get/list split on
  the share collections) already removed enumeration, so exploiting this now requires knowing both the
  exact public slug AND a plausible exact date with zero enumeration — combined residual risk Low.
  Keep it short per the comment convention; the full rationale lives in the SUMMARY.
- Record the written re-acceptance in this plan's SUMMARY (Claude's-discretion location per CONTEXT):
  a short "R347 re-acceptance" section capturing the same rationale, the finding id (SEC-S-02), and the
  explicit statement that the residual is ACCEPTED, not mitigated, this phase.
Do NOT add a token component or otherwise alter the ids (the CONTEXT permits a fully
backward-compatible additive token for NEW shares only, but the default and lower-risk choice is the
documented re-acceptance — take the default).
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts src/stores/__tests__/quarters.test.ts</automated>
  </verify>
  <done>Both id sites carry the re-acceptance comment; the SUMMARY records the written rationale; the id strings are byte-for-byte unchanged; the existing services/quarters store tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org editor free-text input → public share doc | editor-typed notes/slot-body persisted into a share snapshot |
| public share doc → unauthenticated public share page | anyone with (or guessing) the URL renders the snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-05 | Information Disclosure | free-text notes/slot-body PII on public ShareView | low | mitigate | R346: allowlist-shape buildServiceSnapshot (exclude service-level notes + per-slot notes/body) so new share docs carry no free-text; ShareView render gate drops the free-text render so already-deployed docs don't leak either. Unit test (projection) + component test (non-render) pin it. |
| T-118-06 | Information Disclosure | guessable memorable share ids (enumeration by slug + plausible date) | medium | accept | R347 (locked branch): memorable ids are already deployed & embedded in shared links (format change breaks them); SEC-S-01's list-lock already removed enumeration, leaving only slug + exact-date guessing with zero enumeration — combined residual Low. Re-accepted in writing (SUMMARY) + code comments at both id sites. No id-format change. |
| T-118-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (frontend projection + docs only); supply-chain gate N/A. |

No HIGH left unmitigated. The R347 residual is the locked low-risk CONTEXT branch, re-accepted in
writing with rationale rather than left unmitigated-and-silent.
</threat_model>

<verification>
Run the targeted app-suite tests for the changed surfaces:
`npx vitest run src/stores/__tests__/services.sharePii.test.ts src/views/__tests__/ShareView.test.ts`.
Then run the full app suite once for regression: `npx vitest run` (this EXCLUDES the rules harness per
vite.config.ts). Baseline discipline (CLAUDE.md): `src/storage.rules.test.ts` is the only accepted
baseline failure — it is not touched by this plan and must not be chased. Any NEW app-suite failure is
a regression and blocks the plan.

Type-check gate (CLAUDE.md, authoritative): run `npm run type-check` (vue-tsc --build, which
typechecks the test files too) — removing `notes` from the `ServiceSnapshot` interface must not leave a
dangling reference anywhere. The `-p tsconfig.app.json` form is NOT sufficient evidence.
</verification>

<success_criteria>
- R346: the public service snapshot carries no free-text service-level notes and no per-slot notes/body; ShareView renders none of them even for a legacy doc that still contains them; unit + component tests prove it.
- R347: both memorable-share-id sites carry a re-acceptance comment, the SUMMARY records the written rationale, and the id format is unchanged (no deployed link breaks).
- `npx vitest run` passes (storage.rules.test.ts remains the only accepted baseline failure); `npm run type-check` is clean.
</success_criteria>

<output>
Create `.planning/phases/118-security-firestore-rules-and-public-share-hardening/118-02-SUMMARY.md` when done
</output>
