---
phase: 73-multi-org-storage-auth-claim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/orgMembershipClaims.ts
  - functions/src/orgMembershipClaims.test.ts
autonomous: true
requirements: [R207, R208]
must_haves:
  truths:
    - "On any organizations/{orgId}/members/{uid} write (primary OR non-primary, create/update/delete), the user's claim carries an additive orgs map with an entry for every org they currently belong to, alongside the UNCHANGED primary orgId/role keys (R207)"
    - "A membership delete recomputes orgs from the SURVIVING member docs so a removed org no longer appears in orgs (R208)"
    - "A primary-org membership delete clears orgId/role but preserves a still-valid second-org entry in orgs — the primary-clear and orgs-recompute are independent (R208)"
    - "A pre-existing superAdmin claim survives every widened claim write, and a widened write never wipes it (R208/R175)"
    - "A non-primary org join adds that org to orgs without dropping the first org — proving orgs is never derived from the overwrite-broken users/{uid}.orgIds"
  artifacts:
    - "functions/src/orgMembershipClaims.ts widened: buildOrgsMapClaim shared builder, per-uid surviving-members scan, handler that merges primary + orgs"
    - "functions/src/orgMembershipClaims.test.ts extended with multi-org / delete-recompute / primary-clear-keep-orgs / superAdmin-preservation cases"
  key_links:
    - "orgs map computed from collectionGroup('members').get() filtered client-side to doc.id === uid — NEVER from users/{uid}.orgIds (structurally overwrite-broken per RESEARCH)"
    - "every claim write routes through mergeAndSetCustomClaims / clearClaimKeys (claimsHelpers.ts) so superAdmin is preserved"
    - "decideMembershipClaim's RETURN CONTRACT is unchanged (primary {orgId, role} only) so the untouched backfill and its exact-match tests stay green this wave"
---

<objective>
Widen the org-membership claim writer so that on any `organizations/{orgId}/members/{uid}` write it recomputes a full additive `orgs: {orgId: role}` map for the user (every org they belong to) alongside the UNCHANGED primary `{orgId, role}` keys, and writes it via the merge-preserving helpers so `superAdmin` is never wiped (R207, R208).

Purpose: This is the substrate Phase 74 needs before any admin is assigned into a second org — without it, `storage.rules` (widened in plan 73-02) has nothing to authorize a non-primary org against. Closes backlog 999.5's writer half.

Output: `functions/src/orgMembershipClaims.ts` widened (new shared `orgs`-map builder + surviving-members scan + widened handler); `functions/src/orgMembershipClaims.test.ts` extended with the correctness-critical cases (multi-org build, delete-recompute drops the removed org, primary-clear keeps a surviving second org, superAdmin preserved).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Load-bearing correctness guidance for this phase (read before implementing):
@.planning/phases/73-multi-org-storage-auth-claim/73-RESEARCH.md
@.planning/phases/73-multi-org-storage-auth-claim/73-CONTEXT.md
@.planning/phases/73-multi-org-storage-auth-claim/73-PATTERNS.md

# The files this plan extends in place (self-analogs — extend, do not rewrite from scratch):
@functions/src/orgMembershipClaims.ts
@functions/src/orgMembershipClaims.test.ts
@functions/src/claimsHelpers.ts
# Reuse the collectionGroup('members') + resolveOrgId enumeration idiom from:
@functions/src/backfillOrgClaims.ts

# LOCKED SHARED CONTRACT (also consumed by plan 73-02's storage.rules, in parallel this wave):
#   - The new claim key is named exactly `orgs` (locked in 73-CONTEXT.md; storage.rules reads request.auth.token.orgs).
#   - orgs value encoding = the FULL normalized role string ('editor' | 'viewer'), matching the primary role
#     normalization (admin -> editor). This resolves the "value encoding" discretion in 73-CONTEXT.md — do NOT use a
#     short code. (storage.rules only checks presence != null, so the value never affects authz, but pinning it keeps
#     trigger/backfill/tests aligned.)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared orgs-map builder + surviving-members scan (no contract change to decideMembershipClaim)</name>
  <files>functions/src/orgMembershipClaims.ts, functions/src/orgMembershipClaims.test.ts</files>
  <behavior>
    - buildOrgsMapClaim([{orgId:'orgA', role:'editor'}, {orgId:'orgB', role:'viewer'}]) returns { orgA:'editor', orgB:'viewer' }.
    - buildOrgsMapClaim normalizes a legacy 'admin' role to 'editor' (same rule as buildOrgMembershipClaim).
    - buildOrgsMapClaim skips any membership whose role is undefined (a live members doc with no role field never enters the map).
    - buildOrgsMapClaim([]) returns {} (a user with no surviving memberships yields an empty map, not null).
    - The per-uid scan helper reads collectionGroup('members').get(), keeps only docs where doc.id === uid, applies the resolveOrgId structural guard, and builds the map via buildOrgsMapClaim — proven by a test whose member snapshot contains docs for TWO uids and only the target uid's orgs appear.
    - Delete-staleness: given a survivors snapshot that NO LONGER contains the just-removed org's member doc, the computed orgs map does NOT contain that org (the removed org disappears).
    - The scan NEVER reads users/{uid}.orgIds — assert the users collection is never queried by the orgs scan helper.
  </behavior>
  <action>
    In `functions/src/orgMembershipClaims.ts`: (1) Export a new key constant `ORGS_CLAIM_KEY = "orgs"` and a widened type (e.g. `OrgMembershipClaims = OrgMembershipClaim & { orgs: Record<string, OrgMembershipRole> }`) — ADDITIVE; do not alter the existing `ORG_CLAIM_KEYS`, `OrgMembershipClaim`, or `buildOrgMembershipClaim` semantics (R207 additive shape). (2) Add an exported PURE builder `buildOrgsMapClaim(memberships: Array<{ orgId: string; role: string | undefined }>): Record<string, OrgMembershipRole>` that normalizes each role exactly as `buildOrgMembershipClaim` does (admin becomes editor), skips entries whose role is undefined, and folds the rest into an `{ [orgId]: role }` map. This is the single shared no-drift primitive that plan 73-03's backfill will also import (D-11) — the orgs shape and normalization live ONLY here. (3) Export the `resolveOrgId(memberDoc)` structural guard (mirror the exact 4-line guard in `backfillOrgClaims.ts` lines 71-76: parent.parent must be a doc whose parent collection is named `organizations`) so both the trigger and the backfill can share ONE guard. (4) Add an async helper `computeOrgsClaimForUid(uid)` that runs `getFirestore().collectionGroup("members").get()`, filters to `doc.id === uid`, applies `resolveOrgId`, reads each doc's `role`, and returns `buildOrgsMapClaim(...)`. Add a short code comment (mirroring backfillOrgClaims.ts's D-10 scale note) flagging that this unfiltered per-write scan is proportionate at current scale and that a uid-field + collection-group-index redesign is the documented scale-out path if a future cost audit finds it material — do NOT build that scale-out now. Per RESEARCH Pattern 2, do NOT attempt a documentId equality filter on the collection group (it cannot match across parents); the unfiltered scan + client-side `doc.id === uid` filter is correct. Read RESEARCH Pattern 1 for why users/{uid}.orgIds is unusable here. Leave `decideMembershipClaim`'s SIGNATURE and RETURN SHAPE completely unchanged in this task (its {orgId, role} contract is depended on by the untouched backfill and its exact-match tests). In `functions/src/orgMembershipClaims.test.ts`: extend the existing mocking seams (mirror `backfillOrgClaims.test.ts`'s `collectionGroup('members')` mock shape and `fakeMemberDoc` structural chain) and add the behavior cases above.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/orgMembershipClaims.test.ts</automated>
  </verify>
  <done>buildOrgsMapClaim + computeOrgsClaimForUid + exported resolveOrgId exist and pass the listed behavior tests; the orgs scan is proven to use collectionGroup (never users.orgIds) and to drop a removed org; decideMembershipClaim is byte-unchanged in behavior; all pre-existing tests in the file still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen syncOrgMembershipClaimHandler to recompute orgs on every write and merge it with the primary decision</name>
  <files>functions/src/orgMembershipClaims.ts, functions/src/orgMembershipClaims.test.ts</files>
  <behavior>
    - Create/update on the user's PRIMARY org: the single write carries { orgId, role, orgs } where orgs includes the primary org (multi-org set). setCustomUserClaims is called once.
    - Non-primary org JOIN (write to orgB while primary stays orgA): orgs gains orgB AND keeps orgA; the primary orgId/role keys are unchanged. Proves orgs is recomputed on non-primary writes and never sourced from the one-element users/{uid}.orgIds.
    - Primary-org DELETE while the user still belongs to a second org: orgId/role are cleared, but orgs is recomputed from survivors and STILL contains the second org (primary-clear and orgs-recompute are independent — never blanket-clear orgs).
    - Primary-org DELETE when the user belongs to NO other org: orgId/role cleared, orgs becomes {} (or the key is removed) — the user is left with no org access.
    - superAdmin preserved (direction A): a widened create/update on an account that already has superAdmin:true leaves superAdmin:true intact in the written claims.
    - superAdmin preserved (direction B): a primary-org delete on a superAdmin account clears orgId/role, recomputes orgs, and leaves superAdmin:true intact.
    - Idempotency: when the primary already matches AND the recomputed orgs deep-equals the existing orgs, no setCustomUserClaims call is made (skip).
    - no-user-doc / missing-role remain conservative skips with no write (unchanged from current behavior).
  </behavior>
  <action>
    Rewrite `syncOrgMembershipClaimHandler` so it (a) calls the UNCHANGED `decideMembershipClaim` for the primary decision, (b) ALWAYS calls `computeOrgsClaimForUid(uid)` to get the desired orgs map (this is what makes a non-primary write update orgs — see RESEARCH System Architecture Diagram step 2, "every write, regardless of primary/non-primary"), and (c) reconciles the two into ONE write. Rules: on primary decision `set`, write `mergeAndSetCustomClaims(uid, { ...decision.claims, orgs: desiredOrgs })` (one merge carries primary + orgs and preserves superAdmin). On primary decision `clear` (genuine primary-membership delete), clear the primary keys AND set orgs to the recomputed value independently — either two calls (`clearClaimKeys(uid, ORG_CLAIM_KEYS)` then `mergeAndSetCustomClaims(uid, { orgs: desiredOrgs })`) or one combined read-modify-write that deletes orgId/role and sets orgs — both are acceptable per RESEARCH Pattern 3; NEVER blanket-clear orgs (Pitfall 2). On primary decision `skip` with reason `not-primary-org` or `already-current`, still write `mergeAndSetCustomClaims(uid, { orgs: desiredOrgs })` when the desired orgs differs from the existing orgs (compare via a deep-equal of the current claim's orgs against desiredOrgs); if orgs is unchanged too, return a genuine skip with no write. On `skip` with reason `no-user-doc` or `missing-role`, keep the existing conservative no-write skip (do not touch orgs). Keep the whole body wrapped in the existing try/catch returning `{ action: "failed" }` (never throw out of the trigger — T-40-08). Update the module KNOWN LIMITATION docblock (lines ~82-88) to describe the widened multi-org behavior instead of the retired single-primary-org limitation. Extend `orgMembershipClaims.test.ts` with the behavior cases above, reusing `mockAuth({ existingClaims })` for the superAdmin and idempotency assertions and the `collectionGroup('members')` mock for the survivors scan.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/orgMembershipClaims.test.ts</automated>
  </verify>
  <done>All behavior cases pass; the primary-clear-keep-second-org test and both superAdmin-preservation tests are green; a non-primary join adds orgB without dropping orgA; idempotency (primary+orgs unchanged) makes zero setCustomUserClaims calls; every pre-existing test in the file still passes; `cd functions && npm run build` (tsc) is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore members doc write → Cloud Function trigger | The triggering write is trusted (server-side rules gate who can write members docs); the trigger independently re-derives the claim rather than trusting event payload alone |
| Cloud Function → Firebase Auth custom claims | The claim is written server-side via the Admin SDK only — never client-writable; it is the authority storage.rules later reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering / Elevation of Privilege | syncOrgMembershipClaimHandler write path | high | mitigate | Every write routes through mergeAndSetCustomClaims / clearClaimKeys (claimsHelpers.ts) so a widened write never wipes superAdmin and a delete never blanket-wipes a still-valid orgs entry — proven by the two superAdmin-preservation tests and the primary-clear-keep-second-org test |
| T-73-03 | Tampering (stale-claim access-after-removal) | computeOrgsClaimForUid / handler delete path | high | mitigate | orgs is recomputed from the SURVIVING members docs via collectionGroup on every write (never from the overwrite-broken users/{uid}.orgIds) so a removed org disappears immediately — proven by the delete-recompute test; strong-consistency makes the post-commit read race-free |
| T-73-04 | Tampering (concurrent same-uid claim writers racing) | claimsHelpers read-then-write | low | accept | Pre-existing, documented residual (claimsHelpers.ts comment) — no compare-and-swap primitive in the Admin SDK; NOT introduced or widened by this phase; revisit only if a real collision is observed |
</threat_model>

<verification>
- `cd functions && npx vitest run src/orgMembershipClaims.test.ts` — all new + existing cases green.
- `cd functions && npx vitest run src/backfillOrgClaims.test.ts` — MUST stay green (proves decideMembershipClaim's contract is unchanged this wave; the backfill is untouched here).
- `cd functions && npm run build` — functions TypeScript compile clean.
- Manual assertion review (RESEARCH Sampling Rate "Phase gate"): confirm the superAdmin-preservation and delete/primary-clear cases assert on the ACTUAL written claim object, not merely that a call happened.
</verification>

<success_criteria>
The claim carries every org a user belongs to plus its role in an additive orgs map alongside the unchanged primary keys (R207); the writer recomputes the full set on any members write from surviving docs and preserves superAdmin via the shared merge helper, with a primary-membership delete clearing primary keys yet keeping a surviving second org (R208).
</success_criteria>

<output>
Create `.planning/phases/73-multi-org-storage-auth-claim/73-01-SUMMARY.md` when done.
</output>
