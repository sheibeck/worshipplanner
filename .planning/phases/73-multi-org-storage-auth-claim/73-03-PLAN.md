---
phase: 73-multi-org-storage-auth-claim
plan: 03
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - functions/src/backfillOrgClaims.ts
  - functions/src/backfillOrgClaims.test.ts
  - functions/DEPLOY-ORG-CLAIMS.md
autonomous: true
requirements: [R210, R208]
must_haves:
  truths:
    - "Running the backfill (dry-run by default, --apply-gated) recomputes the widened claim (adds the orgs map, preserves the primary keys) for every existing user (R210)"
    - "The backfill derives orgs from ONE collectionGroup('members') scan grouped by uid in memory — no per-doc rescan (R210, RESEARCH Pattern 4)"
    - "A second run reports every already-current account skipped and writes zero claims — idempotent (R210)"
    - "The backfill writes via mergeAndSetCustomClaims (NOT bare setCustomUserClaims), so a superAdmin account survives a backfill run with superAdmin intact (R208/R175)"
    - "DEPLOY-ORG-CLAIMS.md documents the owner-run order: deploy widened writer -> backfill dry-run then --apply -> deploy storage.rules, with the exact commands and why that order avoids an access gap; everything ships UNDEPLOYED"
  artifacts:
    - "functions/src/backfillOrgClaims.ts widened: group-by-uid, shared buildOrgsMapClaim, mergeAndSetCustomClaims write path"
    - "functions/src/backfillOrgClaims.test.ts extended: multi-org add, idempotency across two runs, superAdmin preserved, dry-run no-write, single-scan"
    - "functions/DEPLOY-ORG-CLAIMS.md updated with the Phase 73 widened-claim rollout order"
  key_links:
    - "backfill imports buildOrgsMapClaim + resolveOrgId from ./orgMembershipClaims (single shared orgs shape — no drift with the trigger, D-11)"
    - "backfill switches its write call site from getAuth().setCustomUserClaims to mergeAndSetCustomClaims (superAdmin preservation)"
---

<objective>
Extend the owner-run backfill to recompute the widened claim (add the `orgs` map, preserve the primary keys) for every existing user — grouping ONE `collectionGroup('members')` scan by uid in memory (no per-doc rescan), reusing the shared `buildOrgsMapClaim` builder from plan 73-01 (no drift with the trigger), switching the write path to `mergeAndSetCustomClaims` so `superAdmin` is preserved, and staying idempotent (skip-if-matching). Then update the deploy runbook with the exact owner-run rollout order (R210, R208).

Purpose: The trigger only fires on FUTURE member writes; existing accounts need the backfill to gain the `orgs` map without a manual per-user step. Everything ships built + tested + UNDEPLOYED (hand-over deploy).

Output: `functions/src/backfillOrgClaims.ts` + `.test.ts` widened; `functions/DEPLOY-ORG-CLAIMS.md` updated with the Phase 73 deploy order and commands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Load-bearing correctness guidance (read Pattern 4 + Open Question 2 before editing):
@.planning/phases/73-multi-org-storage-auth-claim/73-RESEARCH.md
@.planning/phases/73-multi-org-storage-auth-claim/73-CONTEXT.md
@.planning/phases/73-multi-org-storage-auth-claim/73-PATTERNS.md

# The files this plan extends in place (self-analogs):
@functions/src/backfillOrgClaims.ts
@functions/src/backfillOrgClaims.test.ts
@functions/DEPLOY-ORG-CLAIMS.md

# Depends on plan 73-01 (Wave 1): imports the new shared buildOrgsMapClaim + resolveOrgId from:
@functions/src/orgMembershipClaims.ts
@functions/src/claimsHelpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the backfill — group-by-uid, shared orgs builder, mergeAndSetCustomClaims, idempotent</name>
  <files>functions/src/backfillOrgClaims.ts, functions/src/backfillOrgClaims.test.ts</files>
  <behavior>
    - Apply mode, a user in TWO orgs (orgA primary + orgB): ONE write for that uid carrying { orgId: orgA, role, orgs: { orgA, orgB } }; orgs has both entries.
    - Single scan: collectionGroup('members').get() is called exactly ONCE per run regardless of how many orgs any user belongs to (grouped by uid in memory — RESEARCH Pattern 4). Assert the collectionGroup spy is called once even for a multi-org user.
    - Idempotency: an immediate second run against the same stateful auth reports processed 0 / skipped N and calls setCustomUserClaims zero times (the orgs deep-equal is part of skip-if-matching).
    - superAdmin preserved: an account whose existing claims include superAdmin:true is written via mergeAndSetCustomClaims and the resulting claims still include superAdmin:true (R208) — this is the regression the bare setCustomUserClaims call would have caused.
    - Dry run (default): same processed/skipped classification, setCustomUserClaims never called.
    - Structural guard + missing-user-doc + per-uid failure isolation continue to behave as before (extend/keep the existing tests, updating exact-match assertions to include the orgs map).
  </behavior>
  <action>
    Restructure `backfillOrgMembershipClaims` so it: (1) fetches `collectionGroup('members').get()` ONCE (as today, line 96), (2) groups the surviving docs by uid IN MEMORY into a per-uid membership list using the shared `resolveOrgId` guard imported from `./orgMembershipClaims` (consolidate — remove the local duplicate copy so there is one guard), (3) for each uid computes the desired widened claim: the PRIMARY decision via the shared `decideMembershipClaim` on the user's primary-org membership (unchanged primary logic, D-11) AND the `orgs` map via the shared `buildOrgsMapClaim(memberships)` imported from `./orgMembershipClaims` (single shared orgs shape — do NOT reimplement orgs normalization here; RESEARCH Anti-Pattern "a second implementation of what orgs a user belongs to"), (4) reconciles into one patch — when the primary decision is `set`, patch is the primary claims plus `orgs`; when it is a skip that still leaves orgs to add/update, patch is just `{ orgs }`; skip-if-matching when the existing claims already deep-equal both the primary keys AND the orgs map, (5) writes via `mergeAndSetCustomClaims(uid, patch)` — NOT the bare `getAuth().setCustomUserClaims(uid, decision.claims)` at the current line 116 (RESEARCH Open Question 2 / Pitfall: the bare call silently wipes superAdmin now that Phase 68 exists). Do NOT call the trigger's per-write orgs scan helper inside the loop (that would rescan per doc — RESEARCH Pattern 4 anti-pattern); use the in-memory grouping. Keep the dry-run default, `--apply` gate, `processed`/`skipped`/`failed` counters, per-uid try/catch failure isolation, the `runBackfillCli` banner + non-zero exit path, and the `require.main === module` guard all intact (SCALE note D-10 still applies — no cursor/pagination). Update the module header comment and the `backfillOrgMembershipClaims` docblock to describe the widened orgs behavior. Extend `backfillOrgClaims.test.ts` reusing `mockFirestore`/`fakeMemberDoc`/`statefulAuth` for the behavior cases above, and update the existing exact-match `toHaveBeenCalledWith` assertions to the widened claim shape.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/backfillOrgClaims.test.ts</automated>
  </verify>
  <done>The backfill adds orgs from a single grouped scan, is idempotent across two runs, preserves superAdmin via mergeAndSetCustomClaims, and writes nothing in dry-run; the collectionGroup scan is proven called once for a multi-org user; all existing behavior (guards, failure isolation, CLI abort path) still passes; `cd functions && npm run build` is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update DEPLOY-ORG-CLAIMS.md with the Phase 73 widened-claim rollout order</name>
  <files>functions/DEPLOY-ORG-CLAIMS.md</files>
  <action>
    Add a new Phase 73 rollout section to `functions/DEPLOY-ORG-CLAIMS.md` (append — preserve the existing Phase 40 Deploy 1/2 content and its soak/token-refresh prose; do not rewrite it). Document the exact owner-run order and why it avoids an access gap: STEP 1 deploy the widened writer first — `firebase deploy --only functions:syncOrgMembershipClaim --project worship-planner-bc515` — so every NEW member write immediately carries the `orgs` map; STEP 2 run the backfill dry-run then apply from `functions/` after `npm run build` (`node lib/backfillOrgClaims.js` then, once the dry-run summary looks right, `node lib/backfillOrgClaims.js --apply`) to populate `orgs` for existing accounts, noting idempotency (a repeat run reports all skipped) and the non-zero exit on any failure; STEP 3 deploy the widened rules last — `firebase deploy --only storage --project worship-planner-bc515` — so the `orgs`-map authorization arm goes live only after the claims exist. Explain WHY this order is safe: the storage.rules legacy arm (R211) keeps single-org sessions working throughout, so there is no access gap while claims migrate, and deploying the writer before the rules means no token is ever authorized against an `orgs` map that was never written. Preserve/point to the existing 1-hour soak + forced-token-refresh guidance (a claim only lands on a token when it is next minted). State explicitly that NOTHING in this file is run by this phase — everything ships built + tested + UNDEPLOYED (hand-over), consistent with the standing grant. Update the stale "Known limitation #1 — Multi-org users" language to note it is CLOSED by this widening once STEP 1-3 are deployed.
  </action>
  <verify>
    <automated>node -e "const d=require('fs').readFileSync('functions/DEPLOY-ORG-CLAIMS.md','utf8'); for (const t of ['functions:syncOrgMembershipClaim','--only storage','--apply']) if(!d.includes(t)){console.error('missing',t);process.exit(1);} console.log('runbook ok')"</automated>
  </verify>
  <done>The runbook contains the three-step widened-claim order with exact commands, the rationale for the order (no access gap; legacy arm bridges rollout), the preserved soak guidance, and the hand-over/UNDEPLOYED statement; the stale multi-org known-limitation note is marked closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner-run CLI (Admin SDK) → Firebase Auth custom claims | The backfill runs with elevated Admin credentials outside the request path; dry-run default prevents an accidental write; it writes only the claim the trigger would also write |
| Runbook prose → owner's manual deploy actions | The runbook is the only interface between this phase's UNDEPLOYED artifacts and production; a wrong/mis-ordered instruction is itself the risk surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Tampering / Elevation of Privilege (superAdmin wipe) | backfill write call site | high | mitigate | Switch from bare setCustomUserClaims to mergeAndSetCustomClaims so a backfill run never wipes a superAdmin grant — proven by the superAdmin-preserved backfill test (closes the latent R208/R175 violation RESEARCH Open Question 2 flags now that Phase 68 exists) |
| T-73-07 | Tampering (inconsistent authorization via drift) | backfill vs trigger orgs computation | medium | mitigate | The orgs shape/normalization has ONE implementation (buildOrgsMapClaim), imported by both trigger and backfill; the backfill reuses decideMembershipClaim for the primary decision — no second "what orgs does this user belong to" implementation (D-11); idempotency test pins the shared result |
| T-73-SC | Tampering (supply chain) | npm dependencies | low | accept | No new npm package is introduced this phase (RESEARCH Standard Stack: pure extension of already-vetted firebase-admin / firebase-functions) — no install task, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- `cd functions && npx vitest run src/backfillOrgClaims.test.ts` — widened backfill green (multi-org add, idempotent, superAdmin preserved, dry-run no-write, single scan).
- `cd functions && npx vitest run` — full functions suite green (both orgMembershipClaims + backfill).
- `cd functions && npm run build` — functions TypeScript compile clean.
- Manual: read the runbook's Phase 73 section and confirm the three-step order + rationale + UNDEPLOYED statement are present and internally consistent.
</verification>

<success_criteria>
An idempotent, dry-run-by-default, owner-run backfill recomputes the widened claim for every existing user from a single grouped scan and preserves superAdmin via the shared merge helper (R210, R208); the deploy runbook hands the owner the exact widened-writer -> backfill -> storage.rules order with commands and the no-access-gap rationale, everything UNDEPLOYED.
</success_criteria>

<output>
Create `.planning/phases/73-multi-org-storage-auth-claim/73-03-SUMMARY.md` when done.
</output>
