---
phase: 73-multi-org-storage-auth-claim
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - storage.rules
  - src/storage.rules.test.ts
autonomous: true
requirements: [R209, R211]
must_haves:
  truths:
    - "A multi-org user whose claim orgs map carries orgA + orgB is ALLOWED to read/write Storage under BOTH orgs/orgA/** and orgs/orgB/** (R209)"
    - "A user whose orgs map lacks orgC is DENIED under orgs/orgC/** — cross-org DENY (R209)"
    - "A legacy token carrying only orgId/role (no orgs key at all) is STILL ALLOWED on its primary org — backward-compat legacy arm (R211)"
    - "A legacy token (no orgs) does not error-deny: the map index is guarded by a non-null check so absence of the orgs key returns a clean result, not a rules evaluation error"
    - "No cross-service Firestore existence read is present in storage.rules — the existing static-assertion guard still passes"
  artifacts:
    - "storage.rules isOrgMemberByClaim widened with the orgs-map arm ORed with the unchanged legacy arm; module comments updated"
    - "src/storage.rules.test.ts extended with multi-org ALLOW, cross-org DENY, legacy-claim ALLOW, and an extended static guard asserting request.auth.token.orgs is read"
  key_links:
    - "isOrgMemberByClaim guards orgs non-null BEFORE indexing orgs[orgId] (Pitfall 4), then ORs the unchanged legacy arm (orgId == requested orgId AND role != null)"
    - "the legacy arm compares against the SAME requested path orgId as the new arm, so it can never authorize a different org"
---

<objective>
Widen `storage.rules`' `isOrgMemberByClaim(orgId)` to authorize against the full multi-org `orgs` claim map (a new arm `orgs != null && orgs[orgId] != null`) ORed with the UNCHANGED legacy arm (`orgId == requested orgId && role != null`), and prove it with genuine emulator tests: multi-org ALLOW on both orgs, cross-org DENY, and legacy-claim ALLOW (R209, R211).

Purpose: This is the authorization half of backlog 999.5 — it lets a genuinely multi-org user (whose claim plan 73-01 widens) keep Storage access to every org, while the legacy arm keeps not-yet-backfilled single-org sessions working with no access gap during rollout.

Output: `storage.rules` widened (membership predicate + module comments); `src/storage.rules.test.ts` extended with the multi-org ALLOW / cross-org DENY / legacy-claim ALLOW cases and an extended static-assertion guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Load-bearing correctness guidance (read the Code Examples + Pitfall 4 before editing):
@.planning/phases/73-multi-org-storage-auth-claim/73-RESEARCH.md
@.planning/phases/73-multi-org-storage-auth-claim/73-CONTEXT.md
@.planning/phases/73-multi-org-storage-auth-claim/73-PATTERNS.md

# The files this plan extends in place (self-analogs):
@storage.rules
@src/storage.rules.test.ts

# LOCKED SHARED CONTRACT (also produced by plan 73-01's writer, in parallel this wave):
#   - The claim key is named exactly `orgs` and is a map { [orgId]: role }. storage.rules reads
#     request.auth.token.orgs and only ever checks PRESENCE (orgs[orgId] != null) — the role VALUE is never
#     inspected by the rule (mirrors the legacy arm's role != null presence check), so the value encoding chosen
#     by plan 73-01 cannot affect this rule.
#   - RESEARCH "Code Examples" gives the exact widened isOrgMemberByClaim text and the exact
#     testEnv.authenticatedContext(uid, claims) minting idiom — follow them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen isOrgMemberByClaim with the orgs-map arm (null-guarded) and update the module comments</name>
  <files>storage.rules</files>
  <action>
    Widen `isOrgMemberByClaim(orgId)` (storage.rules lines 28-32) to return true when `request.auth != null` AND EITHER (new multi-org arm, R209) the token carries a non-null `orgs` map that has a non-null entry for the requested `orgId`, OR (legacy arm, R211) the existing conjunction that the token's `orgId` equals the requested `orgId` and the token's `role` is non-null. Order the new arm to short-circuit-guard the map: check that the orgs map is non-null BEFORE indexing it by `orgId`, because indexing into an absent orgs key evaluates the engine into a hard deny rather than a clean false (RESEARCH Pitfall 4) — this is the same null-guard discipline firestore.rules already uses. Keep the legacy arm exactly as-is and OR (never AND) the two arms so a not-yet-backfilled token still authorizes its primary org. Do NOT touch `isOrgMember` (it stays a thin wrapper delegating to `isOrgMemberByClaim`), and do NOT touch any size-cap conjunct in the four allow clauses. Do NOT reintroduce any cross-service Firestore existence read or the removed by-Firestore membership helper — that class of check is inert in the Storage emulator and is the documented deny-everyone hazard the guard test forbids (RESEARCH Pitfall 3). Update the two stale comment blocks — the top-of-file "v1.5 claim migration — COMPLETE" block (lines 7-14) and the "KNOWN LIMITATION (D-01/D-04)" block directly above the function (lines 23-27) — to describe the widened orgs-map arm plus the still-required legacy arm for rollout backward-compat, replacing the retired single-primary-org limitation language.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('storage.rules','utf8').split('\n').map(l=>l.replace(/\/\/.*$/,'')).join(' ').replace(/\s+/g,' '); if(!s.includes('request.auth.token.orgs'))process.exit(1); if(s.includes('firestore.'+'exists('))process.exit(2); console.log('rule ok')"</automated>
  </verify>
  <done>isOrgMemberByClaim reads request.auth.token.orgs (guarded) ORed with the unchanged legacy orgId/role arm; no cross-service existence read is present; module comments describe the widened arm; the rules file still parses (validated by the emulator load in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Extend the emulator rules suite — multi-org ALLOW, cross-org DENY, legacy-claim ALLOW, static guard</name>
  <files>src/storage.rules.test.ts</files>
  <action>
    Extend `src/storage.rules.test.ts` reusing its existing `testEnv.authenticatedContext(uid, claims)` minting idiom and `assertSucceeds`/`assertFails` + `SMALL_BYTES` fixtures (no members doc is seeded — membership is claim-only). Add: (1) MULTI-ORG ALLOW — mint a context with claims `{ orgId: 'orgA', role: 'editor', orgs: { orgA: 'editor', orgB: 'viewer' } }` and assert the same user succeeds writing AND reading under BOTH `orgs/orgA/**` and `orgs/orgB/**` (R209). (2) CROSS-ORG DENY — with that same multi-org claim (orgs has orgA+orgB but NOT orgC), assert a write under `orgs/orgC/**` fails (R209). (3) LEGACY-CLAIM ALLOW — mint a context with ONLY `{ orgId: 'orgA', role: 'editor' }` (no orgs key at all) and assert it still succeeds on its primary org `orgs/orgA/**`, proving the backward-compat arm and that an absent orgs key does not error-deny (R211, Pitfall 4). Extend the existing static-assertion guard test (the "claim-only membership" describe block, lines 195-236) by adding a positive assertion that the comments-stripped rule source contains `request.auth.token.orgs`, and KEEP the existing negative assertions (no cross-service existence read, no by-Firestore helper, no databases-documents path) and the isOrgMember-delegates-to-isOrgMemberByClaim regex assertion unchanged. Do NOT remove or weaken the vestigial firestore setup in beforeAll (out of scope).
  </action>
  <verify>
    <automated>npm run test:rules</automated>
  </verify>
  <done>The three new emulator cases (multi-org ALLOW on both paths, cross-org DENY, legacy-claim ALLOW) pass, and the extended static guard asserts request.auth.token.orgs while keeping all existing negative assertions green. The ONLY tolerated failures are the 2 pre-existing documented baseline failures in this file (the Storage-emulator cross-service `exists` limitation, unrelated to the claim arm per CLAUDE.md) — no NEW failure is introduced. If an emulator is already running, `npx vitest run --config vitest.rules.config.ts` (optionally scoped with `-t`) is the equivalent per CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Storage request → storage.rules | The client presents a server-signed ID token; the rule authorizes solely on `request.auth.token` claims — never a client-writable field, never a cross-service call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-02 | Elevation of Privilege / Information Disclosure (cross-org access) | storage.rules isOrgMemberByClaim | high | mitigate | Authorization requires the requested `orgId` to be present in the token's `orgs` map (or match the primary orgId in the legacy arm); both arms compare against the SAME requested path parameter, so no token can reach an org it does not carry — proven by the cross-org DENY emulator test |
| T-73-05 | Tampering / Denial of Service (deny-everyone regression) | storage.rules | high | mitigate | The new arm is a pure claim read; NO cross-service Firestore existence read is added (inert in the Storage emulator = untestable = the documented production incident). The existing static-assertion guard test keeps this enforced |
| T-73-06 | Denial of legitimate access (null-index evaluation error) | isOrgMemberByClaim orgs indexing | medium | mitigate | The orgs map is null-guarded before indexing (Pitfall 4), so a legacy token with no orgs key returns a clean result and still authorizes via the legacy arm — proven by the legacy-claim ALLOW test |
</threat_model>

<verification>
- `npm run test:rules` (or `npx vitest run --config vitest.rules.config.ts` against a running emulator) — the three new cases + extended static guard green; only the 2 documented baseline failures remain.
- `npm run type-check` (root vue-tsc --build) — clean (covers src/storage.rules.test.ts).
- Manual: read the widened isOrgMemberByClaim and confirm the null-guard precedes the map index and the legacy arm is ORed, not ANDed.
</verification>

<success_criteria>
storage.rules' isOrgMemberByClaim checks the requested orgId against the full multi-org claim set with genuine multi-org ALLOW and cross-org DENY emulator proof (R209); a legacy single-org claim shape (orgId/role only) still authorizes its primary org so there is no Storage-access gap during rollout (R211).
</success_criteria>

<output>
Create `.planning/phases/73-multi-org-storage-auth-claim/73-02-SUMMARY.md` when done.
</output>
