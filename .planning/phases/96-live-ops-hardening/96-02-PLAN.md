---
phase: 96-live-ops-hardening
plan: 02
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/views/__tests__/RunControlView.output.test.ts
autonomous: true
requirements: [R273, R274]

must_haves:
  truths:
    - "HARNESS UPGRADES (foundational): FakeWin gains a mutable `closed: boolean` (default false) so a test flips openedWins[i].closed = true to simulate a closed output; createFakeChannel captures the message listener and exposes deliver(data) that invokes it as { data } so a test can fire a { type:'hello' } and drive the control's onHello(resendCurrent); installGetScreenDetails is upgraded to resolve a { screens, addEventListener, removeEventListener } object that CAPTURES the screenschange listener (and re-reads a MUTABLE screens set) plus exposes removeEventListener as a spy; the mockSlides fixture is extended to >=3 slides so navigation advances the index beyond 0. These upgrades keep every pre-existing describe block green."
    - "CLOSED DETECTION + PER-ROLE REOPEN (R274): with fake timers SCOPED to this block (vi.useFakeTimers in beforeEach, vi.useRealTimers in afterEach; drive the async getScreenDetails + poll via vi.advanceTimersByTimeAsync so microtasks flush between timer ticks), after a matched goLive (run-status-placed) flipping the AUDIENCE handle closed and advancing ~1s surfaces run-output-closed-audience + run-reopen-audience but NOT the confidence row; clicking run-reopen-audience calls window.open again for 'wp-audience' ONLY (confidence NOT reopened), and — on a non-null handle — clears the amber row back to the green Audience line."
    - "POSITION PRESERVED (R273/R274): navigate to a non-zero index (dispatch ArrowRight keydowns so postIndex advances index to e.g. 2), record it, flip the audience handle closed + advance the poll, click run-reopen-audience, then fire fake.deliver({ type:'hello' }) to simulate the reopened output's mount handshake; assert the LAST { type:'state' } message in fake.posted has index === the pre-close index (the reopened output re-syncs to the exact current slide) — proving position is restored by the handshake with NOTHING persisted."
    - "MONITOR-UNPLUG (R274): after a matched goLive, invoking the CAPTURED screenschange listener directly (no timers) with the live screen set mutated so matchMapping returns needs-reprompt surfaces run-reassign-banner (heading 'Your monitor setup changed', an a[data-to=\"/monitor-setup\"] link); invoking it with a still-matching set surfaces NO run-reassign-banner (no false alarm). PRECEDENCE: with BOTH an unplug (screenschange → needs-reprompt) AND a closed audience window (poll), run-reassign-banner shows and run-reopen-audience is SUPPRESSED."
    - "NO-LEAK / SINGLE-TEARDOWN (R274 endurance): the captured screenschange removeEventListener spy is called on the Escape→run-exit-confirm exit AND on wrapper.unmount(); after a deliberate exit, advancing timers surfaces NO reopen chip (the poll was cleared, so a closed handle left non-nulled by closeOutputs cannot re-surface a chip); RAPID NAVIGATION (R273): a burst of ArrowRight/ArrowLeft keydowns yields fake.posted state messages with STRICTLY INCREASING seq and the correct final index (no drop/reorder). CLIENT-ONLY: the suite adds NO Firestore/emulator dependency and this plan does NOT run npm run test:rules."
  artifacts:
    - "src/views/__tests__/RunControlView.output.test.ts (extended: the FakeWin.closed + fake-channel deliver + upgraded installGetScreenDetails + >=3-slide fixture harness changes, plus new describe blocks for closed detection + per-role reopen, position-preserved, monitor-unplug + precedence, no-leak/single-teardown, and rapid-nav sync)"
  key_links:
    - "openedWins[i].closed = true → vi.advanceTimersByTimeAsync(1000) → the impl poll latches audienceClosed/confidenceClosed → run-output-closed-{role} + run-reopen-{role} render (only in placed AND !monitorChanged)."
    - "run-reopen-audience click → impl reopenOutput('audience') → window.open('…wp-audience…','wp-audience',…) exactly once more (confidence not re-opened) → non-null handle clears audienceClosed; fake.deliver({type:'hello'}) → onHello(resendCurrent) → fake.posted last state.index === pre-close index."
    - "the captured screenschange listener (from the upgraded installGetScreenDetails) invoked directly with a mutated screens set → impl onScreensChange → matchMapping needs-reprompt → run-reassign-banner; the removeEventListener spy asserted called on run-exit-confirm and on wrapper.unmount()."
    - "ArrowRight/ArrowLeft keydown dispatch → impl goBySlide → postIndex → fake.posted state messages with monotonic seq; localStorage.clear() in beforeEach keeps the fallback cases mapping-free; NO Firestore/emulator import is added → npm run test:rules NOT required."
---

<objective>
Extend the existing Phase 95 RunControlView output suite (src/views/__tests__/RunControlView.output.test.ts)
with the behavioral coverage that PROVES Phase 96's live-ops hardening: a closed output window is detected and
reopened for that role alone without losing position, a monitor unplug is detected and surfaces the reassign
banner (with the closed-vs-unplug precedence), the poll and the screenschange listener are torn down exactly
once (no leak, no chip re-surfacing after exit), rapid navigation stays in sync, and the whole feature remains
client-only (no rules suite needed).

Purpose: R274 (one-click reopen/reassign without losing the slide) and R273 (single source of truth; a
reopened output re-syncs to the exact current index via the existing handshake). Every window, screen, timer,
and channel message is faked — no real window is opened, no real getScreenDetails is called, and no Firestore
or emulator is touched. This suite is where the #1 phase risk (a leaked interval / listener, or a reopen chip
re-surfacing for a deliberately-closed window) is caught.

Output: an extended src/views/__tests__/RunControlView.output.test.ts. All harness upgrades land here (Task 1)
and are consumed by the monitor-unplug + precedence + no-leak coverage (Task 2). Real close→reopen and
physical monitor-unplug recovery remain the deferred milestone-end human UAT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-live-ops-hardening/96-CONTEXT.md
@.planning/phases/96-live-ops-hardening/96-UI-SPEC.md
@.planning/phases/96-live-ops-hardening/96-PATTERNS.md
@src/views/RunControlView.vue
@src/views/__tests__/RunControlView.output.test.ts
@src/views/__tests__/MonitorSetupView.test.ts
@src/utils/monitorConfig.ts
@src/utils/runChannel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the harness + prove closed detection, per-role reopen, and position preservation (R274/R273)</name>
  <files>src/views/__tests__/RunControlView.output.test.ts</files>
  <read_first>
    - src/views/__tests__/RunControlView.output.test.ts (the whole file) — the existing harness to EXTEND, not
      replace: the vi.hoisted mockService/mockSlides (:45-72), the vue-router mock (:78-84), the useServiceAssembly
      mock fixing serviceId=service-1/org-1 (:88-98), createFakeChannel with posted[]+close (:114-126), makeScreen/
      screenA/screenB (:129-134), the FakeWin type + makeFakeWin + openedWins (:138-152), installGetScreenDetails/
      installDenied/installDeferred (:154-182), seedMatchingMapping/seedNonMatchingMapping (:184-205), the AUDIENCE_URL/
      CONFIDENCE_URL constants (:207-208), openSpy + mountView + goLive (:210-236), the beforeEach/afterEach
      (:238-251), and the existing describe blocks (:253-523) that MUST stay green.
    - src/views/RunControlView.vue (96-01) — the Task 1 script surface: audienceClosed/confidenceClosed refs, the ~1s
      startClosedPoll reading outputWindows[name]?.closed, reopenOutput(role) reusing openWindow + the held
      liveScreenDetails.screens, onScreensChange + monitorChanged/reassignRole, stopRecoveryWatchers in
      confirmExit/onUnmounted; and the Task 2 template testids run-output-closed-{role} / run-reopen-{role} /
      run-reassign-banner. Confirm reopenOutput opens 'wp-audience'/'wp-confidence' with the same URLs.
    - src/utils/runChannel.ts — postState posts { type:'state', index, blackout, seq } and the incoming message
      dispatch delivers a { type:'hello' } to helloCallback (:127-135); the control registers handle.onHello(
      resendCurrent) onMounted, so firing a hello through the channel makes resendCurrent post the CURRENT index.
    - src/views/__tests__/MonitorSetupView.test.ts — the per-test install/delete of a fake ScreenDetails carrying
      addEventListener/removeEventListener + how it captures + fires the screenschange listener (mirror for Task 2's
      harness, but land the UPGRADE to installGetScreenDetails here in Task 1 so both tasks share it).
  </read_first>
  <action>
    HARNESS UPGRADES (do these first — they are shared by both tasks and MUST keep every pre-existing describe green):
    - FakeWin.closed: add `closed: boolean` to the FakeWin type and initialize it false in makeFakeWin. (The impl
      poll reads outputWindows[name]?.closed; a test flips openedWins[i].closed = true to simulate a closed output.)
      Existing tests never set it → the poll never latches for them.
    - createFakeChannel deliver: capture the message listener (addEventListener('message', cb) → store cb) and return
      a deliver(data: unknown) helper that calls cb?.({ data }). Keep postMessage/posted/close as-is. mountView already
      returns { wrapper, fake }; fake now also exposes deliver. (Firing deliver({ type:'hello' }) makes the control's
      onHello(resendCurrent) resend the current index.)
    - installGetScreenDetails upgrade: resolve a ScreenDetails-like object rather than a bare { screens }. Back it with
      a MUTABLE screens array and a captured screenschange listener:
        function installGetScreenDetails(initialScreens) { let live = [...initialScreens]; let listener = () =&gt; {};
        const details = { get screens() { return live }, addEventListener: vi.fn((t, l) =&gt; { if (t ===
        'screenschange') listener = l }), removeEventListener: vi.fn() }; const control = { setScreens(next) { live =
        next }, fireScreensChange() { listener() } }; (window as …).getScreenDetails = vi.fn(() =&gt;
        Promise.resolve(details)); return { details, control } }.
      This keeps the matched/fallback tests working (they only read .screens) while giving Task 2 the captured
      listener + a way to mutate the live set. installDenied/installDeferred are unchanged.
    - mockSlides fixture: extend to at least 3 assembled slides (same shape, slotIndex 0 is fine — only length matters
      for goBySlide) so ArrowRight navigation advances index beyond 0. Existing tests do not assert slide count.
    - A goLiveFake(wrapper) helper for the fake-timer path: with vi.useFakeTimers() active, mount is settled and the
      Go-live click resolved via await vi.advanceTimersByTimeAsync(0) (flushes the getScreenDetails().then), so the
      outputs open and startClosedPoll is created under fake timers.

    NEW describe blocks (this task):
    1. CLOSED DETECTION (R274): scope vi.useFakeTimers() in beforeEach + vi.useRealTimers() in afterEach for THIS
       block. seedMatchingMapping + installGetScreenDetails([screenA,screenB]); mount; drive Go-live via
       advanceTimersByTimeAsync(0) → run-status-placed. Flip openedWins[0].closed = true (audience opened first) →
       await vi.advanceTimersByTimeAsync(1000). Assert run-output-closed-audience + run-reopen-audience EXIST and the
       confidence row does NOT (only the affected line turns amber); run-status-placed still present (cluster stays
       placed). A second case flips the confidence handle and asserts the mirror.
    2. PER-ROLE REOPEN (R274): from the closed-audience state above, record openSpy.mock.calls.length, click
       run-reopen-audience, await a tick; assert window.open was called ONE more time with (AUDIENCE_URL,'wp-audience',
       …) and NOT with CONFIDENCE_URL; assert run-output-closed-audience is gone (non-null reopen cleared the flag).
       A refused-reopen case: openSpy.mockReturnValueOnce(null) before the reopen click → run-output-closed-audience
       REMAINS (honesty: a null handle never clears the amber row).
    3. POSITION PRESERVED (R273/R274): matched goLive; dispatch two ArrowRight keydowns on document so postIndex
       advances index to 2 (record it); flip openedWins[0].closed=true + advance the poll; click run-reopen-audience;
       fire fake.deliver({ type:'hello' }); assert the LAST { type:'state' } message in fake.posted has index === 2 —
       the reopened output re-syncs to the exact pre-close slide through the handshake, with nothing persisted.

    Reuse the existing openSpy (vi.spyOn(window,'open').mockImplementation(makeFakeWin)) from the shared beforeEach.
    Keep localStorage.clear() + openedWins=[] semantics intact. Never open a real window or call a real getScreenDetails.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-reopen-audience" src/views/__tests__/RunControlView.output.test.ts &amp;&amp; grep -q "advanceTimersByTimeAsync" src/views/__tests__/RunControlView.output.test.ts &amp;&amp; grep -q "closed = true" src/views/__tests__/RunControlView.output.test.ts &amp;&amp; grep -q "deliver" src/views/__tests__/RunControlView.output.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - FakeWin has a mutable `closed`, createFakeChannel exposes deliver(data), installGetScreenDetails resolves a
      { screens (mutable), addEventListener (capturing), removeEventListener } object, and mockSlides has >=3 slides —
      and every pre-existing describe block still passes.
    - With fake timers scoped to the block, flipping one handle closed + advancing ~1s surfaces run-output-closed-{role}
      + run-reopen-{role} for THAT role only (cluster stays placed); clicking run-reopen-{role} re-opens that role's
      window only (not the sibling) and clears the amber row on a non-null handle, while a null-return reopen keeps it.
    - The position-preserved test proves the reopened output re-syncs to the pre-close index: after ArrowRight→2,
      close→reopen→deliver({type:'hello'}), the last state message in fake.posted has index === 2.
    - `npx vitest run src/views/__tests__/RunControlView.output.test.ts` passes; `npx vitest run` shows only the
      baseline (src/storage.rules.test.ts); `npm run type-check` is clean; no `--dir src`; npm run test:rules NOT run.
  </acceptance_criteria>
  <done>The harness gains a mutable-closed fake window, a channel deliver() to fire hello, a screenschange-capturing getScreenDetails fake, and a multi-slide fixture; the new blocks prove closed detection (per-line amber, cluster stays placed), per-role reopen (that role only, honest on a null handle), and position preservation (reopened output re-syncs to the exact pre-close index via the handshake) — all pre-existing tests still green, type-check clean, no baseline regression.</done>
</task>

<task type="auto">
  <name>Task 2: Prove monitor-unplug reassign, closed-vs-unplug precedence, no-leak teardown, and rapid-nav sync (R274/R273)</name>
  <files>src/views/__tests__/RunControlView.output.test.ts</files>
  <read_first>
    - src/views/__tests__/RunControlView.output.test.ts — the Task 1 harness upgrades (the upgraded
      installGetScreenDetails returning { details, control } with control.setScreens/fireScreensChange + the captured
      removeEventListener spy on details; FakeWin.closed; fake.deliver; the fake-timer poll helper). Reuse them; add
      NO new harness. Also the existing close-on-exit idiom (Escape → run-exit-confirm via
      document.body.querySelector, :510-522) for driving exit, and the stale-resolution tests (:450-497) for the
      unmount idiom.
    - src/views/RunControlView.vue (96-01) — onScreensChange re-runs matchMapping(loadMapping(), liveScreenDetails.
      screens) and sets monitorChanged + reassignRole; the template gates the Reopen chip on !monitorChanged
      (precedence); stopRecoveryWatchers removes the screenschange listener in confirmExit AND onUnmounted; the
      run-reassign-banner markup + the a[data-to="/monitor-setup"] link (RouterLink stub).
    - src/utils/monitorConfig.ts — matchMapping returns needs-reprompt when a saved fingerprint is no longer among the
      live screens; seedMatchingMapping stores computeFingerprint(screenA)/(screenB), so setting the live set to e.g.
      [screenA] only (screenB unplugged) makes matchMapping needs-reprompt while [screenA,screenB] stays matched.
  </read_first>
  <action>
    Add these describe blocks, reusing the Task 1 harness (the screenschange tests need NO fake timers — invoke the
    captured listener directly; only the precedence test that also drives the poll needs fake timers, scoped locally):

    4. MONITOR-UNPLUG → REASSIGN (R274): seedMatchingMapping; const { control } = installGetScreenDetails([screenA,
       screenB]); matched goLive (run-status-placed). Simulate the unplug: control.setScreens([screenA]) (screenB gone)
       then control.fireScreensChange(); await a tick. Assert run-reassign-banner EXISTS with heading text "Your
       monitor setup changed" and an a[data-to="/monitor-setup"] link. A NO-FALSE-ALARM case: after the same matched
       goLive, control.fireScreensChange() WITHOUT changing the set (still [screenA,screenB]) → run-reassign-banner does
       NOT exist (a benign refresh raises no banner).
    5. PRECEDENCE (R274, 96-UI-SPEC §B): with fake timers scoped locally — matched goLive; flip openedWins[0].closed =
       true + advance the poll (audience closed) AND control.setScreens([screenA]) + control.fireScreensChange()
       (monitor changed). Assert run-reassign-banner EXISTS and run-reopen-audience does NOT (the reassign banner wins;
       the reopen chip is suppressed until reassignment resolves).
    6. NO-LEAK / SINGLE-TEARDOWN (R274 endurance):
       - removeEventListener on EXIT: matched goLive; capture the details.removeEventListener spy; drive Escape →
         run-exit-confirm (the existing idiom); assert details.removeEventListener was called with 'screenschange'.
       - removeEventListener on UNMOUNT: matched goLive; wrapper.unmount(); assert details.removeEventListener called.
       - poll cleared on exit (the load-bearing gotcha): with fake timers, matched goLive; drive Escape →
         run-exit-confirm; THEN flip openedWins[0].closed = true and advance timers ~2s; assert run-output-closed-audience
         does NOT appear — the poll was cleared on exit, so closeOutputs() leaving the handle non-nulled cannot
         re-surface a reopen chip after a deliberate exit. (Query the component before it is fully gone, or assert the
         interval callback no longer mutates by checking no chip via the wrapper prior to navigation completing — follow
         the existing exit test's ordering.)
    7. RAPID NAV SYNC (R273): matched goLive; dispatch a burst of ArrowRight (x3) then ArrowLeft (x1) keydowns on
       document; collect the { type:'state' } messages from fake.posted; assert their seq values are STRICTLY
       INCREASING (no drop/reorder) and the final message's index is the expected value (3 forward, 1 back → 2).
       This confirms the single-writer channel keeps the outputs in sync under rapid navigation with no heartbeat added.

    CLIENT-ONLY re-confirmation: add a top-of-file comment (and keep it true) that this suite imports NO Firestore /
    emulator module and asserts NO server-side persistence — R273/R274 are pure client + BroadcastChannel + localStorage,
    so `npm run test:rules` is NOT part of this phase's gates. Do not import firebase/firestore anywhere in the file.

    Every screen/window/timer/message stays faked; never open a real window, call a real getScreenDetails, or touch a
    real BroadcastChannel/Firestore.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-reassign-banner" src/views/__tests__/RunControlView.output.test.ts &amp;&amp; grep -q "fireScreensChange" src/views/__tests__/RunControlView.output.test.ts &amp;&amp; grep -q "removeEventListener" src/views/__tests__/RunControlView.output.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A screenschange whose live set drops an assigned monitor surfaces run-reassign-banner ("Your monitor setup
      changed" + the /monitor-setup link); a still-matching screenschange surfaces NO banner (no false alarm).
    - PRECEDENCE: with both a closed audience window AND a monitor change, run-reassign-banner shows and
      run-reopen-audience is suppressed.
    - The captured details.removeEventListener('screenschange', …) spy fires on the run-exit-confirm exit AND on
      wrapper.unmount(); after a deliberate exit, advancing timers surfaces NO reopen chip (the poll was cleared).
    - Rapid ArrowRight/ArrowLeft navigation produces fake.posted state messages with strictly increasing seq and the
      correct final index; the suite adds no Firestore/emulator import.
    - `npx vitest run src/views/__tests__/RunControlView.output.test.ts` passes; `npx vitest run` shows only the
      baseline; `npm run type-check` is clean; no `--dir src`; npm run test:rules is NOT run.
  </acceptance_criteria>
  <done>The suite proves the monitor-unplug reassign banner appears on needs-reprompt and stays silent on a benign refresh, the reassign banner takes precedence over a simultaneous reopen chip, the screenschange listener and poll are torn down exactly once (on exit AND unmount, with no chip re-surfacing after a deliberate exit), and rapid navigation stays in sync with monotonic seq — client-only, type-check clean, no baseline regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → impl poll (fake timers) | The closed-detection tests drive the real ~1s setInterval via vi.advanceTimersByTimeAsync so a regression that failed to detect a closed handle, or that let the poll leak past teardown, fails here rather than in production |
| test harness → captured screenschange listener | installGetScreenDetails captures the listener + a mutable screens set so an unplug (matchMapping needs-reprompt) and a benign refresh are both exercised against the REAL matchMapping, not a mock |
| test harness → fake channel deliver(hello) | Firing a hello through the fake channel exercises the REAL onHello→resendCurrent handshake, proving position is restored by the channel and NOT persisted anywhere |
| test file → Firestore/emulator | The suite imports NO firebase/firestore/emulator module, holding the client-only line (success criterion 4) — a regression that added server-side persistence would force an import that is absent here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-96-11 | Denial of Service | leaked poll interval / screenschange listener regression | high | mitigate | The no-leak block asserts removeEventListener fires on BOTH the exit and the unmount path, and that advancing timers after a deliberate exit surfaces NO reopen chip — so a regression that dropped a teardown site (leaking an interval/listener over a long service, or re-surfacing a chip for a deliberately-closed window) fails here. |
| T-96-12 | Repudiation | false "reopened / recovered" claim regression | high | mitigate | The reopen block asserts a null-return reopen keeps the amber closed row (never clears it) and that reopen re-opens ONLY the affected role; a regression that flipped the line to green on a refused reopen, or reopened the wrong/both windows, fails here. |
| T-96-13 | Tampering | position-loss regression on reopen | high | mitigate | The position-preserved test navigates to a non-zero index, closes+reopens, fires hello, and asserts the resent state index equals the pre-close index — so a regression that persisted/reset the index, or broke the hello→resend handshake, fails here. |
| T-96-14 | Repudiation | false-alarm / missed reassign regression | medium | mitigate | The unplug block asserts the reassign banner appears on needs-reprompt and is ABSENT on a still-matching refresh, and that the banner takes precedence over a simultaneous reopen chip — catching both a missed detection and a noisy false alarm. |
| T-96-15 | Information Disclosure | client-only line regression | low | mitigate | The suite imports no Firestore/emulator module and asserts position/monitor state stay in-channel/localStorage; a regression adding server-side persistence would need an import absent here. npm run test:rules is intentionally not a gate for this phase. |
| T-96-SC | Tampering | package installs (npm) | n/a | accept | No package-manager installs — the suite exercises existing in-repo modules only; the supply-chain / legitimacy checkpoint is not applicable. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/RunControlView.output.test.ts` is green (the pre-96 blocks + the new closed/
  reopen/position/unplug/precedence/no-leak/rapid-nav blocks); `npx vitest run` shows only the baseline
  (src/storage.rules.test.ts); `npm run type-check` (vue-tsc --build; NODE_OPTIONS=--max-old-space-size=8192 only if
  OOM; NO Array.prototype.at) is clean. No `--dir src`. `npm run test:rules` is NOT run — no rules change exists.
- Fake timers are SCOPED to the poll-driving blocks (vi.useFakeTimers in beforeEach / vi.useRealTimers in afterEach)
  and advanced with vi.advanceTimersByTimeAsync so the getScreenDetails().then microtasks flush; the screenschange
  tests invoke the captured listener directly with NO timers, avoiding the flushPromises/fake-timer interaction.
- Manual check: every window/screen/timer/channel message is faked; the reopened-output re-sync is proven via the real
  onHello→resendCurrent handshake (nothing persisted); the removeEventListener spy is asserted on both teardown paths;
  no firebase/firestore/emulator import was added.
- HUMAN-UAT: none introduced by these test-only tasks — the real close→reopen-onto-the-right-monitor, the physical
  monitor unplug + reassign, and a full realistic-length service remain the deferred milestone-end UAT items from 96-01.
</verification>

<success_criteria>
- Behavioral coverage proves R274 (closed-window detection + per-role reopen with position preserved; monitor-unplug
  detection + reassign banner with correct precedence) and R273 (a reopened output re-syncs to the exact current index
  via the handshake; rapid navigation stays in sync with monotonic seq), with the poll + screenschange listener torn
  down exactly once and no reopen chip re-surfacing after a deliberate exit.
- Every window/screen/timer/channel message is faked, the suite adds no Firestore/emulator dependency, and no baseline
  regression is introduced (only src/storage.rules.test.ts fails); npm run test:rules is not required.
</success_criteria>

<output>
Create `.planning/phases/96-live-ops-hardening/96-02-SUMMARY.md` when done.
</output>