---
phase: 139-rehearsal-report-times
plan: 02
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/SettingsView.vue
  - src/components/ServiceCard.vue
  - src/components/ScheduleServiceCard.vue
  - src/views/MyScheduleView.vue
  - src/views/ShareView.vue
  - src/views/DashboardView.vue
  - src/views/VolunteerServiceView.vue
  - src/components/__tests__/ScheduleServiceCard.test.ts
autonomous: true
requirements: [R429, R430, R431, R433]

must_haves:
  truths:
    - "An editor can add, edit, and remove multiple dated rehearsals and set one report time in the service editor, gated by canEditService, persisted via the existing updateService autosave path."
    - "An editor can set org-level default rehearsal time(s) and a default report time in Settings, persisted via updateOrgSettings, with Saved!/error feedback."
    - "Every read-only surface (dashboard, services-list card, My Schedule, volunteer service view, public share/plan) shows the service's report time and dated rehearsals alongside the date, in chronological order, via the shared formatter."
  artifacts:
    - src/components/__tests__/ScheduleServiceCard.test.ts
    - src/views/ServiceEditorView.vue
    - src/views/SettingsView.vue
    - src/components/ServiceCard.vue
    - src/components/ScheduleServiceCard.vue
    - src/views/MyScheduleView.vue
    - src/views/ShareView.vue
    - src/views/DashboardView.vue
    - src/views/VolunteerServiceView.vue
  key_links:
    - "MyScheduleView passes :report-time and :rehearsals at ALL 3 ScheduleServiceCard call sites (This Week / Later This Month / Past)."
    - "Every display + editor site imports formatWallClockTime/sortRehearsals from src/utils/rehearsalTimes.ts — no 6th hand-rolled date/time copy."
    - "Editor writes route through the existing debounced-autosave → updateService(id, { rehearsals, reportTime }) path; Settings writes through updateOrgSettings dot-path — no new save mechanism."
---

<objective>
Build the human-facing half: the service-editor "Times" subsection (multiple dated rehearsals + one report time), the Settings "Rehearsal & Report Defaults" section, and rehearsal/report-time display at every surface where a service's date already appears. Consumes the types, util, and projections landed in 139-01.

Purpose: Deliver R429 (org-defaults UI), R430/R431 (per-service editor CRUD), and R433 (display everywhere the date shows) — closing the ScheduleServiceCard "no time-of-day field yet" TODO.
Output: Editor + Settings input UIs; time display on ServiceCard, ScheduleServiceCard (My Schedule ×3 call sites), ShareView, DashboardView, VolunteerServiceView; a ScheduleServiceCard component test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-rehearsal-report-times/139-CONTEXT.md
@.planning/phases/139-rehearsal-report-times/139-RESEARCH.md
@.planning/phases/139-rehearsal-report-times/139-PATTERNS.md
@.planning/phases/139-rehearsal-report-times/139-VALIDATION.md
@.planning/phases/139-rehearsal-report-times/139-01-PLAN.md
</context>

Consumes from 139-01: `sortRehearsals`, `formatWallClockTime`, `Rehearsal` (src/utils/rehearsalTimes.ts); `Service.rehearsals`/`reportTime`; `OrgSettings.rehearsalTimeDefaults`/`reportTimeDefault`; `ServiceSnapshot`/`RehearseAccessDoc` carrying both fields.

<tasks>

<task type="auto">
  <name>Task 1: Service-editor "Times" subsection + Settings "Rehearsal & Report Defaults" section</name>
  <files>src/views/ServiceEditorView.vue, src/views/SettingsView.vue</files>
  <read_first>
    - .planning/phases/139-rehearsal-report-times/139-RESEARCH.md ("Service editor 'Times' UI placement" + "Settings UI pattern to mirror" — the canEditService gate and updateOrgSettings dot-path write)
    - .planning/phases/139-rehearsal-report-times/139-PATTERNS.md (the SettingsView and ServiceEditorView pattern sections, incl. the onSaveRehearsalDefaults handler shape)
    - src/views/ServiceEditorView.vue (lines ~30-92 date-picker header block; line ~2203 canEditService computed; ~2694-2702 date-change/autosave wiring)
    - src/views/SettingsView.vue (lines ~424-510 Messaging section markup; ~1121-1217 the messaging/reminder save handlers to mirror)
    - src/utils/rehearsalTimes.ts (sortRehearsals for editor-side ordering)
  </read_first>
  <action>
    ServiceEditorView: add a "Times" subsection in the header area near the existing date picker. Bind rehearsal rows to `localService.value.rehearsals` (each row a native `<input type="date">` + `<input type="time">`) with add / edit / remove affordances — mint a new row id via `crypto.randomUUID()` on add, and default new rows to date '' / a blank or copied time. Add a single `<input type="time">` bound to `localService.value.reportTime`. Order the rows the editor shows via sortRehearsals (the editor shows undated rows too, so the planner can date each seeded default). Gate every edit affordance behind the EXISTING `canEditService` computed (reuse verbatim, same `v-else` pattern the date picker uses at ~line 52 — no new gating logic; draft-status enforcement already lives in firestore.rules). Route all writes through the EXISTING debounced-autosave path so it lands as `updateService(id, { rehearsals, reportTime })` — updateService already calls maybeRefreshShareLink, so the share projection refreshes automatically; add no new save/dirty-tracking mechanism.
    SettingsView: add a "Rehearsal & Report Defaults" section mirroring the Messaging section structure (h2 label + inputs + a timed "Saved!"/error line). Provide an add/remove list of time-only `<input type="time">` rows bound to a local `rehearsalTimeDefaultsInput` array, plus one `<input type="time">` for `reportTimeDefault`. On save, call `authStore.updateOrgSettings({ 'settings.rehearsalTimeDefaults': ..., 'settings.reportTimeDefault': ... })` with the dot-path keys, local revert-on-failure, and timed Saved! feedback — mirroring the SettingsView.vue:1152-1168 handler shape. Do not read these org defaults from any display component (copy-at-create lives in createService per 139-01).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <human-check>DEFERRED to batched UAT (VALIDATION.md Manual-Only, R429): in Settings set default rehearsal time(s) + a report time, save, reload — defaults persist; create a new service and confirm rehearsal rows + report time pre-fill; in the editor add/edit/remove rehearsals and set the report time, confirm autosave.</human-check>
  </verify>
  <done>ServiceEditorView shows a canEditService-gated "Times" subsection writing rehearsals/reportTime through updateService; SettingsView has a "Rehearsal & Report Defaults" section persisting via updateOrgSettings with Saved!/error feedback; `npm run type-check` is clean; bare `npx vitest run` shows no new failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Time display on projection-fed read-only surfaces (My Schedule / volunteer / share) + ScheduleServiceCard component test</name>
  <files>src/components/ScheduleServiceCard.vue, src/views/MyScheduleView.vue, src/views/ShareView.vue, src/views/VolunteerServiceView.vue, src/components/__tests__/ScheduleServiceCard.test.ts</files>
  <read_first>
    - .planning/phases/139-rehearsal-report-times/139-RESEARCH.md (§6 display table rows 5/6/7; Pitfall 4 — all 3 ScheduleServiceCard call sites)
    - .planning/phases/139-rehearsal-report-times/139-PATTERNS.md (ScheduleServiceCard, MyScheduleView, ShareView, VolunteerServiceView sections)
    - src/components/ScheduleServiceCard.vue (defineProps ~103-111; parsedDate ~117-126; the lines 40-41 "no time-of-day" TODO this closes)
    - src/views/MyScheduleView.vue (the 3 `<ScheduleServiceCard>` call sites ~59-73 / 83-97 / 124-134)
    - src/views/ShareView.vue (formattedDate off serviceSnapshot.value.date ~181-194)
    - src/views/VolunteerServiceView.vue (formattedDate off doc.serviceDate ~46, 218-222)
    - src/stores/mySchedule.ts (~23 — MyScheduleDoc = RehearseAccessDoc & { orgId })
    - src/utils/rehearsalTimes.ts (formatWallClockTime, sortRehearsals)
    - a nearby @vue/test-utils component test for mount style (e.g. src/views/__tests__/RosterView.test.ts)
  </read_first>
  <behavior>
    - ScheduleServiceCard: given props reportTime '08:00' and rehearsals [{ id, date: '2026-09-11', time: '19:00' }], the rendered text contains the formatted report time ("8:00 AM") and the formatted rehearsal time ("7:00 PM").
    - ScheduleServiceCard: given no reportTime and empty/undefined rehearsals, neither a report-time nor a rehearsal line renders (no empty/blank artifacts).
  </behavior>
  <action>
    Add two new optional props to ScheduleServiceCard.vue — `reportTime?: string` and `rehearsals?: { id: string; date: string; time: string }[]` — and render them next to the existing date via formatWallClockTime (and sortRehearsals for the rehearsal list), following the same computed style as its monthLabel/dayNumber; this closes the lines 40-41 TODO. In MyScheduleView.vue, pass `:report-time="doc.reportTime"` and `:rehearsals="doc.rehearsals"` at ALL 3 `<ScheduleServiceCard>` call sites (This Week / Later This Month / Past) — grep for `<ScheduleServiceCard` and confirm three matches are updated before finishing (Pitfall 4: missing one leaves that bucket silently blank). doc already carries the fields once 139-01's buildRehearseAccess is in. In ShareView.vue, add a sibling computed reading `serviceSnapshot.value.rehearsals`/`.reportTime` (already filtered+sorted by buildServiceSnapshot) through formatWallClockTime, rendered next to formattedDate. In VolunteerServiceView.vue, add the equivalent computed(s) off `doc.rehearsals`/`.reportTime`. All these surfaces are projection-fed, so undated rows are already filtered — render as-is; do NOT re-filter or re-sort. Then write src/components/__tests__/ScheduleServiceCard.test.ts (@vue/test-utils mount) covering both behavior cases via wrapper.text().
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ScheduleServiceCard.test.ts</automated>
    <human-check>DEFERRED to batched UAT (VALIDATION.md Manual-Only, R433): after adding times to a service AND re-locking it (RehearseAccessDoc only refreshes at markAsPlanned), confirm My Schedule, the volunteer service view, and the public share/plan page each show the report time + dated rehearsals alongside the date, chronologically.</human-check>
  </verify>
  <done>ScheduleServiceCard renders report/rehearsal times via the shared formatter (TODO closed); all 3 MyScheduleView call sites pass the new props; ShareView and VolunteerServiceView render the times next to the date; the component test passes; `npm run type-check` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Time display on live-Service surfaces (services-list card + dashboard)</name>
  <files>src/components/ServiceCard.vue, src/views/DashboardView.vue</files>
  <read_first>
    - .planning/phases/139-rehearsal-report-times/139-RESEARCH.md (§6 display table rows 1/2/3 — these read the live Service directly, no projection hop)
    - .planning/phases/139-rehearsal-report-times/139-PATTERNS.md (ServiceCard section — parsedDate/formattedDate computed to sit beside)
    - src/components/ServiceCard.vue (lines ~123-140 parsedDate/formattedDate)
    - src/views/DashboardView.vue (next-service ~47, following-services ~84, the local formatServiceDate ~377-396)
    - src/utils/rehearsalTimes.ts (formatWallClockTime, sortRehearsals)
  </read_first>
  <action>
    ServiceCard.vue: add a sibling computed next to formattedDate reading `props.service.reportTime` (via formatWallClockTime) and `props.service.rehearsals` — since this reads the LIVE Service (not a projection), filter to dated rows (`date !== ''`) and run sortRehearsals before formatting, matching the projection behavior so undated seeded rows don't show on this read-only card. Render the times next to the existing formattedDate. DashboardView.vue: at both the "Next service" card (~line 47) and the "Following services" list (~line 84), render the same dated-and-sorted report/rehearsal times next to the existing formatServiceDate output, using formatWallClockTime/sortRehearsals from the shared util (do not add a local date/time copy). Import the helpers from src/utils/rehearsalTimes.ts in both files.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <human-check>DEFERRED to batched UAT (VALIDATION.md Manual-Only, R433): confirm the services-list card and both dashboard service surfaces show the report time + dated rehearsals next to the date, chronologically, with undated rows hidden.</human-check>
  </verify>
  <done>ServiceCard and both DashboardView service surfaces render dated, sorted report/rehearsal times next to the date via the shared util; undated rows are hidden on these read-only surfaces; `npm run type-check` clean; bare `npx vitest run` shows no new failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore `services/{id}` | Editor "Times" subsection writes `rehearsals`/`reportTime` via updateService |
| client → Firestore `organizations/{orgId}.settings` | Settings section writes the two default fields via updateOrgSettings |
| projection → public reader | ShareView (ServiceSnapshot) and My Schedule / volunteer view (RehearseAccessDoc) render the times to volunteer/public viewers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-04 | Elevation of privilege | ServiceEditorView "Times" subsection | low | mitigate | Reuse the existing `canEditService = isEditor && !isLocked` computed verbatim to gate every edit affordance (same gate the date picker uses); server-side, firestore.rules already restricts `services/{docId}` writes to org editors and to draft status. No new client-trusted path. |
| T-139-05 | Tampering | SettingsView defaults section | low | accept | `updateOrgSettings` writes `settings.*` under the existing org-editor rule; the two new keys are ordinary editor-writable settings (RESEARCH verified they are not in the super-admin `lifecycleFields()` exclusion). No new rule. |
| T-139-06 | Information disclosure | rehearsal/report times rendered on public ShareView + volunteer surfaces | low | accept | Display of intended-public wall-clock scheduling times for a single venue (R429–R433); not PII. The projection layer (139-01) already filters undated/half-entered rows so no partial editor state leaks. |
| T-139-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase (pure Vue/TS display + form wiring); no package legitimacy audit required. |
</threat_model>

<verification>
- Per task: the task's `<automated>` gate passes (`npx vitest run <file>` and/or `npm run type-check`).
- Per wave: bare `npx vitest run` shows only the documented `storage.rules.test.ts` baseline failure; `npm run type-check` (vue-tsc --build, per CLAUDE.md) is clean — this is the real gate that catches template bindings referencing the new fields.
- Deferred human/visual UAT (batched, VALIDATION.md Manual-Only): cross-surface visual placement on dashboard / My Schedule / volunteer view / share page, and the org-defaults + editor interaction UX. A UAT session must RE-LOCK a service after adding times to see them on My Schedule / volunteer view (RehearseAccessDoc refreshes only at markAsPlanned — an existing, accepted latency contract).
</verification>

<success_criteria>
- Editor "Times" subsection adds/edits/removes multiple dated rehearsals + one report time, gated by canEditService, persisted via the existing updateService autosave.
- Settings "Rehearsal & Report Defaults" section persists rehearsalTimeDefaults + reportTimeDefault via updateOrgSettings with feedback.
- All 5 read-only surfaces (ServiceCard, ScheduleServiceCard via My Schedule ×3, ShareView, DashboardView, VolunteerServiceView) show report time + dated rehearsals next to the date, chronologically, undated hidden.
- ScheduleServiceCard component test green; `npm run type-check` clean; full app suite shows only the documented baseline failure.
</success_criteria>

<output>
Create `.planning/phases/139-rehearsal-report-times/139-02-SUMMARY.md` when done.
</output>
