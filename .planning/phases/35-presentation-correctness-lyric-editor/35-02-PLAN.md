---
phase: 35-presentation-correctness-lyric-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/__tests__/slideshowAssembler.test.ts
  - src/utils/__tests__/slideGroupMaterializer.test.ts
autonomous: true
requirements: [R060]

must_haves:
  truths:
    # ── Edge-probe: covered (R060) ──────────────────────────────────────────
    - "R060/boundary — The leading copyright entry sits at the group's first index and the trailing at its last; both are emitted unconditionally on all three construction paths."
    - "R060/adjacency — A song with exactly one section produces copyright → lyric → copyright; the two copyright slides never merge or collapse into one."
    - "R060/empty — An empty performanceOrder still produces the full bracket — two copyright slides with no lyric between — on all three paths."
    - "R060/ordering — The bracket's position is structural, not sorted: leading emitted before the section loop, trailing after it."
    # ── UI-SPEC § UI Considerations — E2, all 8 (7 per-element + 1 Reconciliation) ──
    - "UI/E2/empty — A song group with zero lyric sections still receives two copyright entries bracketing nothing; rebuildSongGroup pushes a leading copyright unconditionally (slideGroupMaterializer.ts:562-566) and a trailing one unconditionally (:619-623), and the fallback path's two emitFallback calls (slideshowAssembler.ts:379,393) are likewise not gated on order.length. Two adjacent copyright slides with no lyric between them is a degenerate-but-valid render, not a defect."
    - "UI/E2/loading — Each copyright entry resolves independently and needs only songLyricsById.get(songId) to exist (resolveEntryContent's 'copyright' case, :133-137); it is never partially gated on individual sections having loaded."
    - "UI/E2/error — An unresolvable lyrics doc omits BOTH copyright entries symmetrically, never one without its pair, matching the engine's documented omit-never-placeholder contract (slideshowAssembler.ts:105-111)."
    - "UI/E2/populated — Normal case: first slide = copyright, N lyric slides, last slide = copyright — unchanged, already-shipped behavior on both paths."
    - "UI/E2/partial — A copyright entry's resolution depends only on the lyrics document existing, never on which individual sections have resolved, so a partially-loaded song can never produce a broken half-bracket."
    - "UI/E2/overflow — The copyright slide's own fine-print rendering (PresentationViewer.vue:74-81) is pre-existing and untouched; this plan verifies WHETHER two copyright slides emit, never how one renders."
    - "UI/E2/zero-one-many — Every song group receives exactly two copyright entries regardless of lyric-section count (zero, one, or many)."
    # ── Edge-probe / UI-SPEC: backstops (flat scalars) ──────────────────────
    - statement: "R060/precision — A song whose copyright object is empty or partially filled still renders both bracket slides, with absent fields omitted rather than rendered as blank lines or the literal undefined."
      verification: backstop
    - statement: "UI/E2/long-text — A song with an unusually long title, a long author list, or many copyrightLines must not overflow the projected copyright slide or push the CCLI license number off-screen; the license number is the one element that must always be visible. Needs a real projector or fixed viewport; it cannot be settled in jsdom."
      verification: backstop

  artifacts:
    - "src/utils/__tests__/slideshowAssembler.test.ts — new describe block 'R060 — copyright bracket (fallback path)'"
    - "src/utils/__tests__/slideGroupMaterializer.test.ts — new describe block 'R060 — copyright bracket (materialized paths)'"
    - "NO production source file is modified by this plan. R060 is closed by a regression test, not an implementation."

  key_links:
    - "All three construction paths must stay in agreement: slideshowAssembler.ts:379/:393 (fallback), slideGroupMaterializer.ts:54/:66 (fresh derive), slideGroupMaterializer.ts:562-566/:619-623 (rebuild). A test that pins only one leaves the other two free to drift."
    - "rebuildSongGroup's self-healing is the guard that keeps corrupted stored data converging back to exactly two copyright entries; if that behavior regresses, a hand-edited or partially-written group silently loses its bracket."
    - "ensureGroupMaterialized's zero-slide bypass (useSlideshowAssembly.ts:388-427) is currently unreachable for SONG slots only because every call site is gated behind canMutateGroup, which excludes song groups (R054). Relaxing R054 without revisiting this would reopen a path that persists a bracket-less song group."

  prohibitions:
    - statement: "No UI copy, code comment, SUMMARY, or rationale may state or imply that CCLI requires copyright on the first and last slide. R060 exceeds the documented minimum deliberately, and CCLI's primary license text has now failed retrieval twice. Safety-margin framing only."
      status: kept
      verification: "grep -rEin 'ccli (requires|mandates|requirement)|licen[cs]e requires' src/ returns 0 matches, including inside the new test descriptions and comments added by this plan"
    - statement: "The paste must be completable in every case. The override checkbox is always available and never conditional on any other field — no state may leave a user holding pasted text they cannot save."
      status: kept
      verification: "Not exercised by this plan (no paste surface touched); carried so no task in this plan introduces a gate that would violate it."
    - statement: "ccliParser.test.ts and songSectionOrder.test.ts's normalizeParsedSections tests must not be edited — they are outside this phase's scope. The 9 parsing/save tests moved out of LyricPasteDialog.test.ts must survive assertion-for-assertion, not be weakened during the modal→inline conversion."
      status: kept
      verification: "git diff --name-only HEAD -- src/utils/__tests__/ccliParser.test.ts src/utils/__tests__/songSectionOrder.test.ts is empty"
---

<objective>
R060 — copyright/CCLI information visible on the first **and** last slide of every song group.

★ **THIS REQUIREMENT NEEDS A TEST, NOT AN IMPLEMENTATION.** 35-RESEARCH.md traced every
group-construction path this codebase has and found all three already emit a leading **and** a
trailing copyright entry unconditionally — for empty `performanceOrder`, for empty copyright
objects, and for corrupted stored data (the rebuild path actively **self-heals** stray extra
entries back to exactly two). **Adding a new emission call would triple-emit copyright slides.**
If any task in this plan proposes new emission code, that is the defect, not the fix.

Purpose: pin the bracket so a future refactor cannot quietly drop it, and cover the degenerate
cases nothing currently asserts.

Output: two new describe blocks. Zero production-source changes.

**Framing constraint (P-01, and it is not optional):** R060 **exceeds** the documented convention
(which is at least once per song, typically the last slide). It is a deliberate safety margin for
mid-deck starts and songs cut short. CCLI's primary license text has now failed retrieval **twice**
(most recently 2026-08-03). No test name, comment, commit message, or SUMMARY line produced by this
plan may frame first-and-last placement as a licensing obligation. Write "safety margin."
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-presentation-correctness-lyric-editor/35-RESEARCH.md
@.planning/phases/35-presentation-correctness-lyric-editor/35-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan produces **no new runtime symbols** — that is the point of it.

| Artifact | Kind | Home |
|---|---|---|
| `describe('R060 — copyright bracket (fallback path)')` | new test block | `src/utils/__tests__/slideshowAssembler.test.ts` |
| `describe('R060 — copyright bracket (materialized paths)')` | new test block | `src/utils/__tests__/slideGroupMaterializer.test.ts` |
| `isCopyrightSlide` / `copyrightIndices` | test-local helper(s) | the two test files (test scope only — do not export) |

Phase-wide artifacts produced by sibling plans, for cross-reference: plan 01 adds
`presentStartIndex`, the `initialIndex` prop, `onPresent`; plans 03/04 add
`src/components/LyricPasteRegion.vue`, `src/components/__tests__/LyricPasteRegion.test.ts`, the refs
`pasteMode` / `rawText` / `overrideCopyright` / `pasteSaveError`, and the `paste-*` `data-testid`
family (`paste-region`, `paste-textarea`, `paste-summary-line`, `paste-section-chip-{i}`,
`paste-copyright-warning`, `paste-copyright-override`, `paste-save-error`, `paste-cancel-btn`,
`paste-replace-btn`, `paste-back-btn`, `lyrics-paste-header`, `paste-no-sections-warning`).
</artifacts_this_phase_produces>

<sanctioned_test_edits>
★ This plan edits **no pre-existing test**. Every change is an addition of a new describe block.

| Edit | Status |
|---|---|
| New `R060 —` describe blocks appended to the two util test files | ✅ Additions only |
| Every pre-existing test in `slideshowAssembler.test.ts` (12 describe blocks) and `slideGroupMaterializer.test.ts` (19 describe blocks) | ❌ Must pass **UNMODIFIED**. Several already cover the populated-order bracket; do not consolidate, rename, or "improve" them. |
| `src/utils/__tests__/ccliParser.test.ts` (19 tests) and `src/utils/__tests__/songSectionOrder.test.ts`'s `normalizeParsedSections` tests (8) | ❌ **Must not be touched at all** (P-03). |

If a pre-existing test in either file goes red, that is a signal the change is wrong — **stop** and
re-examine rather than adjusting the test.
</sanctioned_test_edits>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R060 — pin the copyright bracket on the fallback (not-yet-materialized) path</name>
  <files>src/utils/__tests__/slideshowAssembler.test.ts</files>

  <read_first>
    - `src/utils/slideshowAssembler.ts` lines 50-70 (`buildCopyrightSlideContent`) and lines 370-396
      (the SONG case: the `emitFallback` call before the section loop, the loop, and the
      `emitFallback` call after it) — **read to confirm, then change nothing in this file**
    - `src/utils/__tests__/slideshowAssembler.test.ts` lines 1-120 (the `makeService`,
      `makeSongLyrics`, `songSlot`, `makeInputs`, `makeSlideGroup` helpers this task reuses) and
      lines 179-215 (the existing populated-order bracket test, which must stay unmodified)
    - `src/types/slide.ts` lines 55-70 — **critical discriminator note:** `CopyrightSlide.contentKind`
      is `'lyric'`, the same value `LyricSlide` carries. `contentKind` cannot tell them apart. A
      copyright slide is identified by the presence of `ccliSongNumber`; a lyric slide by the presence
      of `sectionId`.
    - `.planning/phases/35-presentation-correctness-lyric-editor/35-RESEARCH.md` § Common Pitfalls,
      Pitfall 1 ("Implementing R060 as new code instead of a regression test") and Pitfall 4
      ("Justifying R060 as a CCLI requirement in code or UI copy")
  </read_first>

  <behavior>
    - Test 1 (empty order — the actual coverage gap): a SONG slot whose lyrics have
      `performanceOrder: []` and whose slot has no entry in `inputs.groupsBySlotId` assembles to
      exactly 2 slides, both copyright, adjacent, with nothing between them.
    - Test 2 (one section — adjacency): `performanceOrder: ['verse-1']` assembles to exactly 3 slides
      in the order copyright, lyric, copyright. The two copyright slides do not merge into one.
    - Test 3 (boundary): for orders of length 0, 1, 2 and 5, the FIRST assembled slide and the LAST
      assembled slide are both copyright slides, and no slide strictly between them is.
    - Test 4 (structural ordering): the bracket's position does not depend on any sort. Assert the
      copyright indices are exactly `[0, result.length - 1]`.
    - Test 5 (empty copyright object): a lyrics doc whose `copyright` has an empty `title`, an empty
      `authors` array, an empty `ccliSongNumber`, empty `copyrightLines` and an empty
      `ccliLicenseNumber` STILL produces both bracket slides. Assert the count and positions, and that
      neither slide carries the string `undefined` in any of its string fields.
    - Test 6 (symmetric omission): a SONG slot whose `songId` is absent from `inputs.songLyricsById`
      emits ZERO slides — never one copyright without its pair.
  </behavior>

  <action>
    Modify only `src/utils/__tests__/slideshowAssembler.test.ts`. Append a new top-level describe
    block titled `assembleSlideshow — R060 copyright bracket (fallback path)`. Place it after the
    existing `assembleSlideshow — song resolution` block so related coverage stays adjacent.

    Add one file-local helper inside that block that classifies an `AssembledSlide` as a copyright
    slide by testing for the presence of the `ccliSongNumber` property on `assembled.slide`, and a
    second that returns the array of indices at which copyright slides occur. Do NOT discriminate on
    `contentKind` — `CopyrightSlide` and `LyricSlide` both declare it as `'lyric'`, so a
    `contentKind` check would classify every lyric slide as copyright and the tests would pass
    vacuously. Do not export either helper.

    Build every fixture from the file's existing `makeService`, `songSlot`, `makeSongLyrics` and
    `makeInputs` helpers. Note `makeSongLyrics`'s default `performanceOrder` is already `[]`, so the
    empty-order case needs no override. Leave `inputs.groupsBySlotId` at its empty default — that is
    what routes assembly down the fallback path rather than the stored-group path.

    Cover all six behaviors above. For Test 5, construct the empty-copyright lyrics by overriding the
    whole `copyright` object with empty strings and empty arrays rather than by deleting keys, since
    the type requires every field present.

    **Add no code to `src/utils/slideshowAssembler.ts`.** Research traced this path exhaustively:
    `emitFallback` is called once before the section loop and once after it, neither call gated on
    `order.length`. A third emission call — even one framed as defensive — produces three or more
    copyright slides per group, which is a visible regression. If a test in this task fails, the
    correct response is to re-read the assembler and fix the TEST's expectation, not to add emission.

    Name the tests and write any comments in safety-margin terms. Do not describe first-and-last
    placement as an obligation imposed by a licensing body anywhere in this file; the project's own
    position is that the placement exceeds the documented convention by choice.
  </action>

  <verify>
    <automated>npx vitest run src/utils/__tests__/slideshowAssembler.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Behavior assertion: `git diff --name-only HEAD -- src/utils/slideshowAssembler.ts` is **empty** —
      no production code was added for R060.
    - Test command: `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts` is green with a
      non-zero passing count.
    - Test command: `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts -t "R060"` matches
      at least 6 tests and passes. A "0 passed" result here is a FAILURE, not a pass.
    - Source assertion: `grep -c "contentKind" ` over the new describe block's line range returns `0` —
      the classifier does not use the ambiguous discriminator.
    - Source assertion: `grep -rEin 'ccli (requires|mandates|requirement)|licen[cs]e requires' src/utils/__tests__/slideshowAssembler.test.ts`
      returns 0 matches (P-01). This grep targets `src/` only; the plan document lives under
      `.planning/` and cannot self-invalidate it.
    - Behavior assertion: `git diff src/utils/__tests__/slideshowAssembler.test.ts` shows additions
      only — no pre-existing test line is modified or deleted.
  </acceptance_criteria>

  <done>
    The fallback path's copyright bracket is pinned for empty, single-section, multi-section and
    empty-copyright songs, and the symmetric-omission contract is asserted. `slideshowAssembler.ts`
    itself is byte-identical to how the task found it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R060 — pin the copyright bracket on both materialized paths, including self-healing</name>
  <files>src/utils/__tests__/slideGroupMaterializer.test.ts</files>

  <read_first>
    - `src/utils/slideGroupMaterializer.ts` lines 40-70 (`deriveGroupEntries`'s SONG case: the
      unconditional leading push, the section loop, the unconditional trailing push) and lines
      476-500 plus 550-630 (`rebuildSongGroup`: `storedCopyrightEntries`, `leadingCopyright`,
      `trailingCopyright`, the two `merged.push` calls that mint a fresh entry when a stored one is
      absent) — **read to confirm, then change nothing in this file**
    - `src/utils/__tests__/slideGroupMaterializer.test.ts` lines 1-60 (the `makeSongLyrics`,
      `mockTimestamp` helpers), lines 151-193 (the existing `deriveGroupEntries — SONG` block that
      must stay unmodified), and lines 307-340 (the `rebuildSongGroup` block's `makeStoredSongGroup`
      and `twoSectionStoredSlides` fixtures this task reuses)
    - `.planning/phases/35-presentation-correctness-lyric-editor/35-RESEARCH.md` § Architecture
      Patterns, "System Architecture Diagram — R060's two independent bracket-producing paths", and
      § Common Pitfalls, Pitfall 2 (the `ensureGroupMaterialized` zero-slide bypass)
  </read_first>

  <behavior>
    - Test 1 (fresh derive, empty order): `deriveGroupEntries` for a SONG slot whose lyrics have
      `performanceOrder: []` returns exactly 2 entries, both `sourceRef.kind === 'copyright'`, with
      `order` values `[0, 1]`.
    - Test 2 (fresh derive, one section): `performanceOrder: ['verse-1']` returns exactly 3 entries
      with kinds `['copyright', 'lyric', 'copyright']`. The two copyright entries have distinct `id`s
      and do not collapse into one.
    - Test 3 (fresh derive, five sections): the first and last entries are copyright and no entry
      between them is; `order` is a contiguous 0..n sequence.
    - Test 4 (rebuild self-heal, ONE stored copyright entry): a stored group holding only a leading
      copyright entry plus lyric entries rebuilds to exactly 2 copyright entries — the missing
      trailing one is freshly minted (a new uuid, not a reuse of the leading entry's id).
    - Test 5 (rebuild self-heal, ZERO stored copyright entries): a stored group with lyric entries
      only rebuilds to exactly 2 copyright entries, both freshly minted.
    - Test 6 (rebuild self-heal, THREE stored copyright entries — corrupted/hand-edited data): the
      rebuild output has exactly 2. The FIRST stored copyright entry becomes the leading one and the
      LAST becomes the trailing one, both keeping their original ids; the middle one is silently
      dropped rather than carried.
    - Test 7 (rebuild, empty order): a stored group whose song's `performanceOrder` is now `[]` still
      rebuilds to a bracket — 2 copyright entries with no lyric entry between them.
    - Test 8 (rebuild, boundary/ordering): across all the above, the copyright entries' positions in
      the returned `slides` array are exactly index `0` and index `length - 1`, and `order` values are
      contiguous from 0.
  </behavior>

  <action>
    Modify only `src/utils/__tests__/slideGroupMaterializer.test.ts`. Append a new top-level describe
    block titled `R060 — copyright bracket (materialized paths)` with two nested describes,
    `deriveGroupEntries — fresh materialization` and `rebuildSongGroup — self-healing`.

    Discriminate copyright entries by `entry.sourceRef.kind === 'copyright'` — on this path the source
    ref is a clean discriminator, unlike the assembled-slide path in Task 1. Add a file-local helper
    that returns the indices of copyright entries in a `GroupSlideEntry[]`; do not export it.

    Reuse the file's existing `makeSongLyrics`, `songSlot`, `makeInputs`, `makeGroup` helpers and the
    `rebuildSongGroup(group, slot, inputs)` call shape the existing `rebuildSongGroup` describe block
    already uses. For the corrupted-data fixtures, hand-build the stored `slides` arrays in the same
    literal style as the existing `twoSectionStoredSlides` constant, giving each copyright entry a
    distinguishable id (for example `e-copyright-lead`, `e-copyright-middle`, `e-copyright-trail`) so
    Test 6 can assert WHICH two survived, not merely how many.

    For Test 4 and Test 5, assert the minted entry is genuinely new by checking its id is not equal to
    any stored entry's id, rather than by matching a uuid pattern.

    **Add no code to `src/utils/slideGroupMaterializer.ts`.** Both pushes in `deriveGroupEntries` and
    both pushes in `rebuildSongGroup`'s merge are already unconditional. A third push produces three
    or more copyright entries per group. If a test here fails, re-read the materializer and correct
    the test's expectation; do not add emission.

    Do not touch `src/composables/useSlideshowAssembly.ts`. Research flagged `ensureGroupMaterialized`'s
    zero-slide bypass as a theoretical route to a bracket-less group, and proved it currently
    unreachable for SONG slots because every call site sits behind `canMutateGroup`, which excludes
    song groups (R054). It is recorded as a regression hazard for a future phase, not a defect to fix
    here. Add one short comment at the top of the new describe block naming that dependency, so a
    later phase relaxing R054 finds the note.

    Name the tests and write any comments in safety-margin terms. Do not describe first-and-last
    placement as an obligation imposed by a licensing body anywhere in this file.
  </action>

  <verify>
    <automated>npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Behavior assertion: `git diff --name-only HEAD -- src/utils/slideGroupMaterializer.ts src/composables/useSlideshowAssembly.ts src/utils/slideshowAssembler.ts`
      is **empty** — R060 was closed with tests only, on every path.
    - Test command: `npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts` is green with a
      non-zero passing count.
    - Test command: `npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts -t "R060"`
      matches at least 8 tests and passes. A "0 passed" result here is a FAILURE.
    - Source assertion: `grep -rEin 'ccli (requires|mandates|requirement)|licen[cs]e requires' src/utils/__tests__/slideGroupMaterializer.test.ts`
      returns 0 matches (P-01).
    - Behavior assertion: `git diff src/utils/__tests__/slideGroupMaterializer.test.ts` shows additions
      only — no pre-existing test line is modified or deleted.
    - Type gate: `npm run type-check` (`vue-tsc --build`) exits 0. Per CLAUDE.md this is the ONLY
      sufficient type gate; it typechecks test files, which `-p tsconfig.app.json` silently skips.
    - Full suite: `npx vitest run src/` shows no failures beyond the known baseline
      (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts` — 9 tests / 2 files).
  </acceptance_criteria>

  <done>
    Both materialized paths are pinned: fresh derivation brackets any order length including zero,
    and rebuild converges to exactly two copyright entries from 0, 1, 2 or 3 stored ones, keeping the
    first and last and dropping the middle. No production file changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → `rebuildSongGroup` | Stored `SlideGroup.slides` arrives from Firestore and may be corrupted, hand-edited, or partially written. It crosses into pure rebuild logic that must not trust its shape. |
| Assembled deck → projector | Copyright attribution reaches a public-facing projected surface. Its absence is the risk this requirement addresses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05 | Tampering | `rebuildSongGroup` stored-entry merge | medium | mitigate | Tests 4-6 pin the existing self-healing: 0, 1, or 3+ stored copyright entries all converge to exactly 2, with the middle entry dropped rather than carried. A hand-edited or partially-written group cannot strip a song's attribution. |
| T-35-06 | Repudiation | Copyright/CCLI attribution on projected slides | medium | mitigate | The bracket is pinned on all three construction paths, so attribution cannot be silently lost by a refactor of any one of them. Note the disposition is about the project's own attribution policy, not about satisfying an external mandate. |
| T-35-07 | Information disclosure | Copyright slide fields with an empty `copyright` object | low | mitigate | Test 5 asserts absent fields are omitted rather than rendered as the literal `undefined`, so an unpopulated song cannot leak internal placeholder text onto a projector. |
| T-35-08 | Denial of service | `ensureGroupMaterialized` zero-slide bypass | low | accept | Currently unreachable for SONG slots — every call site is gated behind `canMutateGroup`, which excludes song groups (R054). Accepted with a recorded comment in the new describe block so a future phase relaxing R054 is warned. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages (35-RESEARCH.md § Package Legitimacy Audit: "Not applicable"). No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
1. `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts src/utils/__tests__/slideGroupMaterializer.test.ts` — green.
2. `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts src/utils/__tests__/slideGroupMaterializer.test.ts -t "R060"` — at least 14 tests matched and passing.
3. `git diff --name-only HEAD -- src/utils/slideshowAssembler.ts src/utils/slideGroupMaterializer.ts src/composables/useSlideshowAssembly.ts` — **empty**. This is the single most important check in the plan.
4. `npm run type-check` — exits 0. **Not** `-p tsconfig.app.json`.
5. `npx vitest run src/` — no failures beyond the 2-file baseline.
6. `grep -rEin 'ccli (requires|mandates|requirement)|licen[cs]e requires' src/` — 0 matches (P-01).
7. `git diff --name-only HEAD -- src/utils/__tests__/ccliParser.test.ts src/utils/__tests__/songSectionOrder.test.ts` — empty (P-03).
</verification>

<success_criteria>
- Every song group provably carries a copyright entry at its first and last index, on all three
  construction paths, for orders of length 0, 1, 2 and 5.
- Corrupted stored data (0, 1, or 3+ copyright entries) converges to exactly 2 on rebuild, keeping
  the first and last.
- An empty copyright object still produces both bracket slides with no `undefined` text.
- An unresolvable lyrics doc omits both copyright slides symmetrically.
- **No production source file was modified.**
- No artifact of this plan frames the placement as a licensing obligation.
</success_criteria>

<output>
Create `.planning/phases/35-presentation-correctness-lyric-editor/35-02-SUMMARY.md` when done.
State plainly that R060 was closed by regression test and that no emission code was added, and
record the `git diff --name-only` result for the two utility source files as the evidence. Describe
the first-and-last placement as a safety margin that exceeds the documented convention. Record that
CCLI's primary license text failed retrieval a second time (2026-08-03) and that nothing in this
phase depends on it.
</output>
