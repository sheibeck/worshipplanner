---
phase: 68-super-admin-access-gate
plan: 05
type: execute
wave: 3
depends_on: [68-02, 68-03]
files_modified:
  - functions/DEPLOY-SUPER-ADMIN.md
autonomous: true
requirements: [R174, R176, R178, R179]

must_haves:
  truths:
    - "A single owner-handoff runbook records the EXACT, ordered commands to deploy the rules, deploy the two Functions, and bootstrap the first super-admin — and states plainly that nothing in the repo runs them."
    - "The runbook sequences the Functions deploy BEFORE the bootstrap run, and documents that the bootstrap also writes the claim directly as a belt-and-suspenders against a mis-ordered deploy."
    - "The runbook documents the revoked-token residual ≤1hr window rather than promising instantaneous cutoff."
  artifacts:
    - functions/DEPLOY-SUPER-ADMIN.md
  key_links:
    - "The documented deploy targets (firestore:rules; functions:syncSuperAdminClaim,setSuperAdminClaim) and bootstrap path (lib/bootstrapSuperAdmin.js) match the artifacts Plans 02 and 03 produced."
---

<objective>
Produce the owner hand-over runbook `functions/DEPLOY-SUPER-ADMIN.md` — the single non-executing document that records exactly how the owner deploys and bootstraps this phase (R174/R176/R178/R179 deployment).

Per the v1.9 standing autonomy grant, this whole phase is the HAND-OVER category (auth + rules): every artifact ships built + tested + UNDEPLOYED, and the owner runs every deploy and the first bootstrap. This plan writes NO code and executes NO deploy — it only documents the commands, following the `functions/DEPLOY-ORG-CLAIMS.md` precedent (a standalone runbook next to the scripts, not a note buried in a SUMMARY).

It runs in Wave 3 (after Plans 02 and 03) so it documents the real deployed surface — the two Function exports and the two rules blocks that now exist.

Purpose: The owner has one authoritative, correctly-ordered checklist to take Phase 68 live safely.
Output: `functions/DEPLOY-SUPER-ADMIN.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-super-admin-access-gate/68-CONTEXT.md
@.planning/phases/68-super-admin-access-gate/68-RESEARCH.md

# Precedent runbook to mirror (executor reads directly):
@functions/DEPLOY-ORG-CLAIMS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the owner hand-over deploy + bootstrap runbook</name>
  <files>functions/DEPLOY-SUPER-ADMIN.md</files>
  <action>Create functions/DEPLOY-SUPER-ADMIN.md mirroring the structure and tone of functions/DEPLOY-ORG-CLAIMS.md. It MUST open with a prominent banner stating NOTHING IN THIS REPOSITORY RUNS ANY COMMAND IN THIS FILE — Phase 68 ships built + tested + UNDEPLOYED under the v1.9 standing autonomy grant (reference STATE.md), and reaching this handed-over state IS the phase goal. It must NOT instruct any .env.local or functions/.env writes.

Document these owner-run steps, in this order:
1. Pre-flight: admin credentials available (GOOGLE_APPLICATION_CREDENTIALS or `gcloud auth application-default login`) and `firebase login`; confirm the project with `firebase use` (default `worship-planner-bc515`); full suite green locally first — `cd functions && npm test`, the rules suite `npx vitest run --config vitest.rules.config.ts` (emulator up; not `npm run test:rules` if one is already running — CLAUDE.md), and `npm run type-check` (the vue-tsc --build form).
2. Deploy the rules: `firebase deploy --only firestore:rules --project worship-planner-bc515`. Note this activates the isSuperAdmin() gate on appConfig/* and superAdmins/* (additive — it only restricts two previously catch-all-denied collections, so it cannot lock any existing user out).
3. Deploy the two Functions: `firebase deploy --only functions:syncSuperAdminClaim,functions:setSuperAdminClaim --project worship-planner-bc515`. State explicitly this must happen BEFORE the bootstrap run so the trigger exists — and note that the bootstrap script also writes the claim directly (Pitfall 6) as a safeguard against a mis-ordered deploy.
4. Bootstrap the first super-admin: `cd functions && npm run build`, then the dry run `node lib/bootstrapSuperAdmin.js --email <owner-email>` (read the printed project id + DRY RUN banner), then `node lib/bootstrapSuperAdmin.js --email <owner-email> --apply`. Note the single --apply gate is deliberate (consistent with backfillOrgClaims.ts; an extra confirm flag was considered and rejected).
5. First-login refresh: the owner signs out/in (or reloads) so getIdTokenResult(true) mints a token carrying superAdmin and the Owner Console nav entry appears.

Add a "Known limitations / residual risks" section covering: (a) the revoked-token residual — revokeRefreshTokens invalidates the session at the next server-verified check, but an already-issued ID token keeps its baked-in superAdmin for up to its ~1hr remaining lifetime; frame this as a bounded, documented window, not instantaneous cutoff (Pitfall 5 / Open Question 1); (b) grant propagation — a grant lands on the target's next token refresh; (c) the client route guard is convenience only, the enforced boundary is the rules + the onCall re-check.

Do NOT include a `firebase deploy --only hosting` step — per the hand-over scope, only rules + the two Functions + the bootstrap are handed over here.</action>
  <verify>
    <automated>test -f functions/DEPLOY-SUPER-ADMIN.md && grep -q "firestore:rules" functions/DEPLOY-SUPER-ADMIN.md && grep -q "functions:syncSuperAdminClaim" functions/DEPLOY-SUPER-ADMIN.md && grep -q "bootstrapSuperAdmin.js" functions/DEPLOY-SUPER-ADMIN.md</automated>
  </verify>
  <done>The runbook exists and records the three deploy/bootstrap command families (rules deploy, the two-Function deploy, the --apply bootstrap) in the correct order, with the residual-window note and the "nothing here is run by the repo" banner; no hosting deploy and no env-file writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner shell → production Firebase project | The owner runs privileged deploys + the first-super-admin bootstrap by hand; a mis-ordered or under-documented step can either fail silently (no claim lands) or over-grant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-04b | Elevation of Privilege | first-super-admin bootstrap path | high | mitigate | Runbook prescribes the owner-run-once, non-deployed bootstrap and the exact ordered sequence; no in-app path grants the first super-admin. |
| T-68-06b | Tampering | deploy ordering (bootstrap before trigger) | medium | mitigate | Runbook sequences the Functions deploy before the bootstrap AND documents that the bootstrap writes the claim directly as a safeguard (Pitfall 6). |
| T-68-05c | Elevation of Privilege / Tampering | revoke residual window | medium | mitigate | Runbook documents the ≤1hr baked-in-token residual explicitly rather than promising instantaneous cutoff (Pitfall 5 / Open Q1). |
| T-68-SC | Tampering | npm installs | low | accept | Documentation only — zero new dependencies, no install step. |
</threat_model>

<verification>
Automated: the presence check in Task 1's verify. This plan is documentation only — no runtime behavior, no manual UAT of its own. The deploys/bootstrap it documents are the owner's steps, surfaced in the `/gsd-verify-work 68` hand-over.
</verification>

<success_criteria>
- `functions/DEPLOY-SUPER-ADMIN.md` exists, mirrors the DEPLOY-ORG-CLAIMS.md precedent, and records the exact rules deploy, two-Function deploy, and --apply bootstrap commands in the correct order.
- It states plainly that nothing in the repo runs these commands (v1.9 hand-over) and instructs no env-file writes.
- It documents deploy ordering (Functions before bootstrap) and the revoked-token ≤1hr residual window.
- No `firebase deploy --only hosting` step.
</success_criteria>

<output>
Create `.planning/phases/68-super-admin-access-gate/68-05-SUMMARY.md` when done.
</output>