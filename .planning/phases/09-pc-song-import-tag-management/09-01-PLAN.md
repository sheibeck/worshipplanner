---
phase: 09-pc-song-import-tag-management
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/song.ts
  - src/stores/songs.ts
  - src/stores/__tests__/songs.test.ts
autonomous: true
requirements:
  - soft-delete
  - hidden-exclusion
  - re-import-safe
  - upsert-store

must_haves:
  truths:
    - "Songs with hidden: true do not appear in filteredSongs"
    - "Songs with hidden: true do not appear in AI suggestions (filteredSongs is what AI receives)"
    - "deleteSong sets hidden: true via updateDoc, does NOT call deleteDoc"
    - "restoreSong sets hidden: false via updateDoc"
    - "upsertSongs creates new Firestore docs for new songs and updates existing docs without clobbering hidden status"
    - "Existing songs without pcSongId/hidden fields continue to work (undefined treated as null/false)"
  artifacts:
    - path: src/types/song.ts
      provides: "Song interface with pcSongId and hidden fields"
      contains: "pcSongId"
    - path: src/stores/songs.ts
      provides: "soft-delete, restore, upsert, hidden-aware filter"
      exports:
        - deleteSong
        - restoreSong
        - upsertSongs
        - filteredSongs
    - path: src/stores/__tests__/songs.test.ts
      provides: "Tests for all new store behavior"
  key_links:
    - from: src/stores/songs.ts
      to: filteredSongs
      via: "!song.hidden check added to computed"
      pattern: "hidden"
    - from: src/stores/songs.ts
      to: deleteSong
      via: "updateDoc with hidden:true instead of deleteDoc"
      pattern: "updateDoc.*hidden.*true"
---

<objective>
Extend the Song data model and store with soft-delete, restore, and upsert capabilities. All new store logic is test-driven.

Purpose: The hidden flag and upsertSongs store method are the foundational contracts that Plans 02 and 03 depend on.
Output: Updated Song type, updated store with 3 new methods, updated tests covering all new behavior.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-pc-song-import-tag-management/09-CONTEXT.md
@.planning/phases/09-pc-song-import-tag-management/09-VALIDATION.md
@src/types/song.ts
@src/stores/songs.ts
@src/stores/__tests__/songs.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Song type + update store with soft-delete and upsert</name>
  <files>src/types/song.ts, src/stores/songs.ts, src/stores/__tests__/songs.test.ts</files>
  <behavior>
    Song type changes:
    - Add `pcSongId: string | null` to Song interface
    - Add `hidden: boolean` to Song interface (default false for existing songs — handle missing field in code)

    Store changes:
    - `filteredSongs` computed: add `&& !song.hidden` condition (treat undefined as false: `song.hidden !== true`)
    - `deleteSong(id)`: change from `deleteDoc` to `updateDoc(ref, { hidden: true, updatedAt: serverTimestamp() })`
    - Add `restoreSong(id)`: calls `updateDoc(ref, { hidden: false, updatedAt: serverTimestamp() })`
    - Add `upsertSongs(songs: UpsertSongInput[])`: for each song in the array:
      - Match against `store.songs` using: (1) pcSongId if provided, (2) ccliNumber if non-empty, (3) case-insensitive title
      - If match found: call `updateDoc` with the new data EXCEPT preserve `hidden`, preserve user-set `vwType` if incoming vwType is null (only set vwType from upsert when it has a value)
      - If no match: call `addDoc` with `{ ...song, hidden: false, createdAt: serverTimestamp(), updatedAt: serverTimestamp() }`
      - Batch using writeBatch for efficiency (500 ops per batch, same pattern as existing importSongs)

    New type: `UpsertSongInput = Omit<Song, 'id' | 'createdAt' | 'updatedAt'>` (same shape as SongInput but includes pcSongId and hidden)

    Tests to write BEFORE implementing (RED):
    - `filteredSongs excludes songs where hidden is true`
    - `filteredSongs includes songs where hidden is false`
    - `filteredSongs includes songs where hidden is undefined (legacy docs)`
    - `deleteSong calls updateDoc with hidden:true, not deleteDoc`
    - `restoreSong calls updateDoc with hidden:false`
    - `upsertSongs creates new doc when no match found`
    - `upsertSongs updates existing doc when pcSongId matches`
    - `upsertSongs updates existing doc when ccliNumber matches`
    - `upsertSongs preserves hidden:true when updating existing song`
    - `upsertSongs only sets vwType when incoming vwType is non-null`

    Update existing `deleteSong` test: it should now verify `updateDoc` was called (not `deleteDoc`).
  </behavior>
  <action>
    1. Write all failing tests in `src/stores/__tests__/songs.test.ts` — run `npx vitest run src/stores/__tests__/songs.test.ts` to confirm RED.
    2. Extend `Song` interface in `src/types/song.ts` with `pcSongId: string | null` and `hidden: boolean`.
    3. Update `src/stores/songs.ts`:
       - Add `UpsertSongInput` type
       - Update `filteredSongs` computed to add `song.hidden !== true` check
       - Replace `deleteSong` body: `updateDoc(doc(db, 'organizations', orgId.value!, 'songs', id), { hidden: true, updatedAt: serverTimestamp() })`
       - Add `restoreSong(id: string)`: same pattern with `{ hidden: false, updatedAt: serverTimestamp() }`
       - Add `upsertSongs(songsData: UpsertSongInput[])`: match-and-update or create logic in Firestore batches. Match order: pcSongId → ccliNumber (non-empty) → title (lowercase). For updates: spread incoming data but use `hidden: existingSong.hidden` (preserve) and only set `vwType` if `incomingData.vwType !== null`. Chunk at 499 ops using writeBatch.
       - Export `restoreSong` and `upsertSongs` in the return object
    4. Run tests to confirm GREEN: `npx vitest run src/stores/__tests__/songs.test.ts`

    NOTE: The existing `makeSong()` helper in the test file does not include `pcSongId` or `hidden` — add those fields to the helper with defaults (`pcSongId: null, hidden: false`) so existing tests continue passing.

    NOTE: The `upsertSongs` matching logic reads from `songs.value` (the in-memory reactive store) — this is safe because the store is already subscribed before upsert is called. Build a lookup Map keyed by pcSongId and ccliNumber for O(1) matching.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/songs.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All new tests pass (GREEN). Existing tests still pass. `filteredSongs` excludes hidden songs. `deleteSong` uses `updateDoc`. `restoreSong` and `upsertSongs` exported from store. TypeScript compiles with no errors: `npx tsc --noEmit`.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/stores/__tests__/songs.test.ts` passes
- `npx tsc --noEmit` produces no errors
- `song.hidden !== true` present in filteredSongs computed
- `deleteDoc` is no longer called by `deleteSong`
- `restoreSong` and `upsertSongs` are in the store's return object
</verification>

<success_criteria>
- Song interface has `pcSongId: string | null` and `hidden: boolean`
- filteredSongs excludes hidden songs (hidden songs invisible to UI and AI)
- deleteSong is now a soft-delete (sets hidden:true)
- restoreSong sets hidden:false
- upsertSongs does additive/upsert with preserved hidden status and conditional vwType
- All tests green, TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-pc-song-import-tag-management/09-01-SUMMARY.md`
</output>
