---
phase: 52-default-service-template
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServicesView.vue
  - src/views/__tests__/ServicesView.test.ts
  - src/views/SettingsView.vue
  - src/views/__tests__/SettingsView.test.ts
autonomous: true
requirements: [R113]

must_haves:
  truths:
    - "The Services page shows an editor-gated cog control that opens the service-template-editor slide-out (R113)"
    - "A viewer (non-editor) does not see the cog (v-if=authStore.isEditor), matching the New Service button convention (R113)"
    - "The Settings page no longer renders a Services template card — no open-template-editor button, no template-summary (R113)"
    - "The relocated editor is structurally unchanged — it teleports to body and works identically from the Services page host (R113)"
  artifacts:
    - "src/views/ServicesView.vue: cog trigger (open-template-editor) + templateEditorOpen ref + <ServiceTemplateEditor :is-open @close> mount"
    - "src/views/__tests__/ServicesView.test.ts (NEW): cog exists, editor-gated, opens the editor"
    - "src/views/SettingsView.vue: Services card + ServiceTemplateEditor mount/import + templateEditorOpen ref + templateSummary computed + now-dead groupBySection/SERVICE_SECTIONS imports removed"
  key_links:
    - "ServicesView cog @click='templateEditorOpen = true' → <ServiceTemplateEditor :is-open='templateEditorOpen' @close='templateEditorOpen = false'>"
---

<objective>
Relocate the default-service-template editor from the main Settings page to an editor-gated cog on the Services page per R113. The editor component (ServiceTemplateEditor.vue) is host-agnostic (Teleport to body, no scrim), so only its trigger and mount move — no structural change to the editor. This plan is independent of the util/store/editor-internal work (plans 52-01, 52-02) and runs in Wave 1 alongside 52-01: it touches only the two view files and their tests, which no other plan modifies.

Purpose: Put the template editor where it is used (the Services page, next to "New Service"), and remove it from the settings surface where it does not belong.

Output: A cog trigger + editor mount on ServicesView, a new ServicesView.test.ts, a stripped SettingsView (card + all card-only code removed), and a relocated/removed SettingsView test block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-default-service-template/52-RESEARCH.md

# Gate discipline (CLAUDE.md): type gate is `npm run type-check` (vue-tsc --build, checks test files).
# App suite: `npx vitest run --dir src --exclude '**/rules.test.ts'`. Baseline exactly 2 known-failing files.
# The editor is teleported to <body>: mounting-host tests query the opened panel through a DOMWrapper over document.body.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServicesView — add the editor-gated cog trigger + editor mount; new ServicesView.test.ts</name>
  <files>src/views/ServicesView.vue, src/views/__tests__/ServicesView.test.ts</files>
  <read_first>
    - src/views/ServicesView.vue (action bar with the flex-1 spacer + New Service button @39-52; the NewServiceDialog mount area @165-172; script imports/refs @176-198)
    - src/components/settings/ServiceTemplateEditor.vue (public interface: defineProps<{ isOpen: boolean }> @223, defineEmits<{ close: [] }> @224; data-testid="service-template-editor" @17; close testid service-template-editor-close @26)
    - src/views/__tests__/SettingsView.test.ts (the teleport body() helper + open/close pattern @493-513 — mirror it for the new file)
  </read_first>
  <action>
    Per R113. In src/views/ServicesView.vue:
    - Action bar (~:39-51): between the `<div class="flex-1" />` spacer and the "New Service" button, add a cog/settings icon button. Give it v-if="authStore.isEditor" (matching the New Service button's convention — a viewer does not see it), type="button", aria-label="Edit default service template", data-testid="open-template-editor", @click="templateEditorOpen = true", and an inline cog SVG (Claude's Discretion on the exact icon; a standard gear/settings glyph). Style it as a neutral secondary control next to the indigo New Service button (e.g. border border-gray-700 text-gray-300 hover:bg-gray-800 rounded-md, matching the mb-1 alignment).
    - Mount (~:165-172): next to the NewServiceDialog mount, add `<ServiceTemplateEditor :is-open="templateEditorOpen" @close="templateEditorOpen = false" />`. It teleports to body, so placement inside the AppShell template is fine.
    - Script (~:176-198): import ServiceTemplateEditor from '@/components/settings/ServiceTemplateEditor.vue'; add `const templateEditorOpen = ref(false)` (ref is already imported). authStore is already available.
    - Create NEW src/views/__tests__/ServicesView.test.ts. Mirror SettingsView.test.ts's teleport approach: a body() DOMWrapper over document.body, enableAutoUnmount(afterEach), and mock the stores ServicesView imports (auth, services, router, songs/roster/quarters as needed — follow the mocking already used by an existing view test; keep the mock minimal so the component mounts). Assert: (1) with isEditor true, [data-testid="open-template-editor"] exists; (2) clicking it makes body() [data-testid="service-template-editor"] exist, and the close control (service-template-editor-close) closes it; (3) with isEditor false, the cog is absent. The editor's own internal open/close/reset is already covered by ServiceTemplateEditor.test.ts — do not re-test it here.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServicesView.test.ts && npm run type-check</automated>
  </verify>
  <done>ServicesView renders an editor-gated cog (open-template-editor) that opens/closes the teleported service-template-editor; a non-editor never sees the cog; the new test file passes; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsView — remove the Services card and all card-only code; relocate its test block</name>
  <files>src/views/SettingsView.vue, src/views/__tests__/SettingsView.test.ts</files>
  <read_first>
    - src/views/SettingsView.vue (Services card markup @462-479; editor mount @482; imports ServiceTemplateEditor @492, groupBySection @495, SERVICE_SECTIONS @496; templateEditorOpen ref @597; templateSummary computed @599-613)
    - src/views/__tests__/SettingsView.test.ts (the whole 'SettingsView Services card (R086)' describe @439-518)
  </read_first>
  <action>
    Per R113. In src/views/SettingsView.vue remove the Services card entirely and everything that existed only to serve it:
    - Delete the Services card `<div>` block (~:462-479): the heading "Services", the description paragraph, the template-summary paragraph, and the "Edit Default Template" button (open-template-editor).
    - Delete the `<ServiceTemplateEditor :is-open ... @close ... />` mount (~:482).
    - Delete the imports that become dead: `ServiceTemplateEditor` (~:492), `groupBySection` (~:495), and `SERVICE_SECTIONS` (~:496). Verified: groupBySection and SERVICE_SECTIONS are used in this file ONLY by templateSummary (no other reference), so both are dead once the card goes.
    - Delete the `templateEditorOpen` ref (~:597) and the `templateSummary` computed (~:599-613), plus their `// ── Services / default service template state` comment banner (~:593-595).
    - Leave the `ref`/`computed`/`watch` vue imports (used elsewhere in this file) and all other settings sections untouched.
    - In src/views/__tests__/SettingsView.test.ts: remove the entire 'SettingsView Services card (R086)' describe block (~:439-518) and any now-unused mock/helper that only that block referenced (e.g. mockDefaultServiceTemplate if nothing else uses it — check before deleting). The relocated coverage now lives in ServicesView.test.ts (Task 1). Optionally add one negative assertion in an existing SettingsView describe that [data-testid="open-template-editor"] and [data-testid="template-summary"] do NOT exist, proving the card is gone.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/SettingsView.test.ts && npm run type-check</automated>
  </verify>
  <done>SettingsView renders no Services card (no open-template-editor, no template-summary); ServiceTemplateEditor, groupBySection, SERVICE_SECTIONS imports and the templateEditorOpen ref + templateSummary computed are gone; no unused-import/lint or vue-tsc errors; SettingsView.test.ts passes with the Services-card block removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Services page cog (client) → template editor → Firestore | The relocated control gates who can open the editor whose Save writes settings.defaultServiceTemplate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-06 | Elevation of Privilege | relocated cog trigger on ServicesView | low | mitigate | Cog carries v-if="authStore.isEditor" (a viewer cannot open the editor), and the editor's Save is itself gated by authStore.isEditor plus the unchanged server-side Firestore rule on organizations/{orgId}. Relocation moves the trigger only; it changes no write path or rule. Client gate is defence-in-depth, not the boundary. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/ServicesView.test.ts src/views/__tests__/SettingsView.test.ts` green.
- `npm run type-check` (vue-tsc --build) clean — no unused ServiceTemplateEditor/groupBySection/SERVICE_SECTIONS imports left in SettingsView.
- Full app suite `npx vitest run --dir src --exclude '**/rules.test.ts'` at the exactly-2-file baseline.
- Manual (deferred per v1.6 grant): open the Services page, click the cog → the template slide-out opens; confirm the Settings page no longer has a Services template card.
</verification>

<success_criteria>
R113: the default-service-template editor is reached from an editor-gated cog on the Services page and is no longer presented on the main Settings page; the editor component is structurally unchanged (mount point moved only).
</success_criteria>

<output>
Create `.planning/phases/52-default-service-template/52-03-SUMMARY.md` when done.
</output>
