---
phase: 36-ui-rework-service-order-contextual-action-bars
plan: 03
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - src/components/slides/SlidesTab.vue
  - src/components/slides/__tests__/SlidesTab.test.ts
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R068, R069]

must_haves:
  truths:
    - "The page header's action slot is driven by `activeActionItems`, not by four unconditional `v-if` buttons — changing tabs changes which actions render, with no second bar and no new row."
    - "`Suggest All Songs` and `Copy for PC`/`Export to PC` do not render on the Slides tab or the Roles tab (R068's concrete acceptance criterion, ROADMAP criterion 2)."
    - "`▶ Present` renders in the page-level header row, immediately left of `Save`, only while the Slides tab is active (design 1a, ROADMAP criterion 3) — and no longer inside the Slides tab's own content."
    - "Pressing the relocated Present button still opens `PresentationViewer` at the same computed start index: bar → `slidesTabRef.onPresentClick()` → SlidesTab's existing `present` emit → the view's existing `onPresent(startIndex)`. No handler was reimplemented."
    - "The Roles tab renders zero action-bar buttons and no empty bordered box, while `Undo`, `Mark as Planned` and the lifecycle-error line still render on every tab exactly as today."
    - "GATING PRESERVED, MOUNTED PROOF: for a viewer and for a locked editor, the set of header action buttons rendered per tab is unchanged from the pre-phase behaviour — including that the export-or-copy control still renders for them."
    - "R071's no-credentials note travels with `Copy for PC` into the bar with its `data-testid`, its copy and its live Settings router-link intact, still gated on `canEditService && !hasPcCredentials` (34-12)."
    - "The R071 note is absent on the Slides and Roles tabs for free — the bar renders a hint slot only when its item is in the list."
    - "34-10 SURVIVES: the save-status bar keeps `v-if=\"canEditService && congregationalSlotIndex === null\"` on the wrapper and its chrome-only conditional; at idle it renders an element with no chrome classes and a mounted `SaveStatusIndicator`. No `v-if` is added to that wrapper."
    - "The tab strip reads Service Order · Slides · Roles for an editor and Service Order · Slides for a viewer (R069, ROADMAP criterion 5); the Roles button keeps its `authStore.isEditor` gate and only changes position."
    - "R068 edge/idempotency, mounted: switching Service Order → Slides → Roles → Service Order returns the header to the identical button set it started with."
    - "The `activeTab` union type is unchanged — it is order-independent by definition, and the two `v-show` panels were not reordered."
    - statement: "The consolidated header row must not force horizontal scrolling at the narrowest supported viewport; the pre-phase header's wrapping behaviour is not regressed."
      verification: backstop
  artifacts:
    - "src/components/slides/SlidesTab.vue — Present wrapper deleted, `canPresent`/`onPresentClick` exposed"
    - "src/views/ServiceEditorView.vue — `ContextualActionBar` mounted in the header, four button blocks removed, `slidesTabRef` added, tab strip reordered"
    - "src/views/__tests__/ServiceEditorView.test.ts — per-tab action coverage, 34-10 guard, tab-order assertions"
    - "src/components/slides/__tests__/SlidesTab.test.ts — Present coverage moved to the exposed API"
  key_links:
    - "`slidesTabRef` → `SlidesTab.defineExpose({ canPresent, onPresentClick })` — the concrete plumbing the Present relocation requires; without it the header button cannot gate or fire correctly"
    - "`<template #hint-copy-pc>` → the `pc-credentials-missing-note` span moved verbatim from the header — the mechanism that keeps R071's router-link alive inside a declarative list"
    - "the save-status bar block sits between the header and the tab strip and is NOT part of the bar — do not fold it in"
  prohibitions:
    - "Relocating a control must not widen or narrow who can press it. The mounted per-tab button sets for viewer and locked-editor must match the pre-phase behaviour, not the UI-SPEC's illustrative code."
    - "Do not add a `v-if` to the save-status bar wrapper, and do not 'simplify' its two-part condition. Doing so restores the empty bordered box owner finding F4 reported and costs a screen-reader announcement."
    - "Do not move `Print`, `Share`, `Undo`, `Mark as Planned` or `Delete service` into the bar or anywhere else. They are outside this phase's named scope even though `Print`/`Share` also leak across tabs today — flagged, not fixed."
  assumptions:
    - "★ R069 EDGE, DELIBERATELY LEFT UNRESOLVED: the edge probe returned R069's row as `unclassified`. It is NOT auto-backstopped and NOT auto-covered. What is planned is only what R069 literally states and what the ROADMAP's criterion 5 restates — the rendered order of three existing buttons. Any edge behaviour the `unclassified` row might have meant (deep links to a tab, keyboard tab-cycling order, persistence of the active tab across reloads) is unspecified in every source artifact and is not invented here. Surface this to the checker rather than treating it as covered."
    - "The header slot is the SAME visual row the page header already uses — there is no wireframe for a second bar, and R068's cross-tab pattern is original design work (UI-SPEC Finding 1). Recorded so no reviewer reads a wireframe basis into it."
---

<objective>
Wire the shared bar into the page header, relocate `▶ Present` from the Slides tab into that header, and
swap the last two tabs. This is where R068 and R069 actually land — and where the phase's central
invariant is most likely to break silently.

Purpose: ROADMAP criteria 2, 3 and 5.
Output: `ServiceEditorView`'s header stops rendering four unconditional buttons and starts rendering one
per-tab list; `SlidesTab` loses its local Present wrapper and gains two exposed members.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-UI-SPEC.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-CONTEXT.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-02-SUMMARY.md
@src/components/actionBarItems.ts
@src/views/serviceEditorActionBar.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SlidesTab exposes canPresent/onPresentClick and drops its local Present wrapper</name>
  <files>src/components/slides/SlidesTab.vue, src/components/slides/__tests__/SlidesTab.test.ts</files>

  <read_first>
    - `src/components/slides/SlidesTab.vue` lines 1–30 (the wrapper being deleted and the root layout it
      sits in), lines 200–210 (`canPresent`), lines 405–430 (`presentStartIndex`, `onPresentClick`),
      line 524 (the existing `defineExpose`)
    - `src/components/slides/__tests__/SlidesTab.test.ts` around lines 510–540, 565–575, 645–665 and
      1050–1060 — every existing Present assertion
    - `36-UI-SPEC.md` § Wireframe Findings, Finding 3 and § Phase-Specific Component Contracts §4
  </read_first>

  <behavior>
    - `SlidesTab` renders no Present button of its own.
    - `canPresent` is readable from the component instance and is true exactly when the assembled
      slideshow is non-empty.
    - Calling `onPresentClick()` from outside emits `present` carrying the same computed start index it
      carries today.
    - The rail/grid/drawer layout below is otherwise unchanged.
  </behavior>

  <action>
    Delete the entire flex wrapper that currently sits directly inside the tab root and holds the
    Present CTA — the `div` with the `justify-end`/`border-b border-gray-800 px-3 py-2 flex-none`
    classes, and the `button` inside it, and the head comment block that explains that button's
    placement. After the deletion the tab root's first child is the `div` with class
    `flex flex-1 min-h-0` that holds the rail and the grid. The root element keeps its own classes and
    `data-testid="slides-tab"` unchanged.
    <!-- planner-discipline-allow: present-slideshow-cta -->

    Leave `canPresent`, `presentStartIndex` and `onPresentClick` defined in the script with their bodies
    unchanged — `onPresentClick` still does nothing but emit `present` with `presentStartIndex.value`.
    Leave the `present` emit declaration unchanged.

    Extend the existing `defineExpose` call to add `canPresent` and `onPresentClick` alongside the four
    members it already exposes. Do not remove or rename any existing exposed member.

    Add a short comment above `canPresent` recording that the control it gates now lives in
    `ServiceEditorView`'s page header per design 1a, and that this component still owns the condition and
    the emit — only the rendered button moved.

    Update `SlidesTab.test.ts`: the assertions that found the Present CTA by testid become assertions
    against the exposed API — existence checks become `expect(wrapper.vm.canPresent)` checks, and the
    click-then-assert-emit tests call `wrapper.vm.onPresentClick()` instead of triggering a DOM click.
    Add one assertion that no button with the old CTA testid renders. **Comment each edit as a
    moved-control edit**: the condition, the emit and the payload are all identical, only the render
    site changed.
  </action>

  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlidesTab.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `wrapper.find('[data-testid="present-slideshow-cta"]').exists()` is `false`.
    - `wrapper.vm.canPresent` is `false` with an empty `assembledSlideshow` and `true` with a non-empty
      one.
    - `wrapper.vm.onPresentClick()` emits exactly one `present` event whose payload equals the same start
      index the pre-phase click test asserted (reuse that fixture's expected value unchanged — a changed
      expected value would mean a behaviour change, not a moved control).
    - The four previously-exposed members are still exposed (`selectedSlotId`, `selectedSlideId`,
      `requestEditInScripture`, `selectSlideById`) — assert each is defined on `wrapper.vm`.
    - The rail and the grid still render; `[data-testid="slides-tab-content"]` still exists.
  </acceptance_criteria>

  <done>
    `SlidesTab` owns the Present condition and emit but renders no Present button. The header can now
    drive it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ServiceEditorView's header renders the per-tab ContextualActionBar</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>

  <read_first>
    - `src/views/ServiceEditorView.vue` lines 96–246 (the header action row — every button being removed
      and every one being kept), lines 249–299 (the save-status bar and its 34-10/34-07 comment block —
      read it in full before touching anything nearby), lines 1315–1345 (the SlidesTab mount and
      `PresentationViewer`), lines 1455–1475 (`activeTab`, store wiring), lines 1697–1700, 2074–2082,
      2125–2132 (`serviceSaveStatusVisible`), 1855–1870 (`onPresent`, `presentStartIndex`)
    - `src/views/__tests__/ServiceEditorView.test.ts` lines 560–690 (copy/export/credentials-note
      coverage), 3500–3520 (the compound Suggest disabled binding), 3750–3775, 4585–4610 (the header
      Save-area composition test)
    - `src/views/serviceEditorActionBar.ts` and `src/components/ContextualActionBar.vue` (from `36-02`)
    - `36-UI-SPEC.md` § Phase-Specific Component Contracts §2 and §3, § Empty / Edge States rows E1, E2,
      E3, E7, E8
    - `36-02-SUMMARY.md` — specifically the recorded export/copy gate divergence
  </read_first>

  <behavior>
    - Service Order (editor, unlocked, no credentials): suggest, copy, save render — plus the R071 note.
    - Service Order (editor, unlocked, credentialed): suggest, export, save render — no R071 note.
    - Service Order (viewer, or editor on a locked service): only the export-or-copy control renders in
      the bar; no suggest, no save, no note.
    - Slides (editor): Present and Save render; no suggest, no export/copy, no note.
    - Roles: the bar renders zero buttons; `Undo` and `Mark as Planned` still render.
    - Pressing Present opens the presentation viewer at the computed start index.
    - The save-status bar behaves exactly as `34-10` left it.
  </behavior>

  <action>
    Import `ContextualActionBar` and `buildActionBarItems`. Add
    `const slidesTabRef = ref<InstanceType<typeof SlidesTab> | null>(null)` and bind `ref="slidesTabRef"`
    on the existing `<SlidesTab>` element; change nothing else about that element's props or listeners.

    Add a single `activeActionItems` computed that calls `buildActionBarItems(activeTab.value, { ... })`,
    filling the context from values the view already holds: `canEditService`, `hasService` from
    `localService`, `hasSermonContext`, `aiSuggestingAll`, `authStore.hasPcCredentials`, `isExporting`,
    `pcCopied`, `localService.status`, `isDirty`, `isSaving`, and `canPresent` read as
    `slidesTabRef?.canPresent ?? false`. The `handlers` object passes the EXISTING functions by
    reference — `suggestAllSongs`, `onExportToPC`, `onCopyForPC`, `onSave` — plus, for present, a
    zero-argument arrow that calls `slidesTabRef?.onPresentClick()`. That arrow is the only new function
    in this task and it contains no logic: `onPresentClick` still emits, and the view's existing
    `@present="onPresent"` listener still receives the start index and opens the viewer. Do not call
    `onPresent` directly from the bar — routing through the emit is what keeps the start-index
    computation in one place.

    In the header's action row: KEEP the `Undo` button, the `Mark as Planned` button and the
    `lifecycle-error` span exactly as they are, in place, with their gates untouched. DELETE the four
    button elements for suggest, export, copy and save — the whole elements including their class
    strings and inline SVGs, which now live in `ContextualActionBar`. In their place mount
    `<ContextualActionBar :items="activeActionItems">` and give it one slot child:
    `<template #hint-copy-pc>` wrapping the existing `pc-credentials-missing-note` span MOVED VERBATIM
    from its current position — same `data-testid`, same `v-if="canEditService && !authStore.hasPcCredentials"`,
    same copy, same `<router-link :to="{ name: 'settings' }">` and same classes. Moving rather than
    rewriting it is what keeps `34-12`/R071 intact.

    Do NOT touch the save-status bar block or its comment. Its wrapper condition and its chrome-only
    `:class` conditional stay byte-for-byte as `34-10`/`34-07` left them. If the surrounding markup must
    be re-indented, re-indent only — do not merge, gate or simplify that element.
    <!-- planner-discipline-allow: service-save-status-bar -->

    Update `ServiceEditorView.test.ts`. The export/copy/credentials-note assertions should pass
    unchanged because both testids were preserved deliberately — if any of them fails, treat that as a
    real regression, not as a selector to update. Add a new describe block covering the per-tab matrix
    in the acceptance criteria below. **Comment every edited assertion with which category it falls in**
    — moved control, or behaviour change. There should be no behaviour-change edits in this task; if one
    appears necessary, stop and record why rather than editing the assertion.
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Service Order + editor + unlocked + no credentials: the bar contains buttons whose text includes
      `Suggest All Songs`, `[data-testid="copy-pc-btn"]` exists, a `Save` button exists, and
      `[data-testid="pc-credentials-missing-note"]` exists and contains a link to the settings route.
    - Service Order + editor + unlocked + credentialed: `[data-testid="export-pc-btn"]` exists and
      `[data-testid="pc-credentials-missing-note"]` does not.
    - After clicking the Slides tab: no button text includes `Suggest All Songs`, neither
      `[data-testid="copy-pc-btn"]` nor `[data-testid="export-pc-btn"]` exists, and
      `[data-testid="pc-credentials-missing-note"]` does not exist. **This is ROADMAP criterion 2's
      mounted proof; the data-level proof is `36-02`'s.**
    - After clicking the Roles tab: the same three absences hold, AND
      `[data-testid="contextual-action-bar"]` exists with zero child buttons, AND a `Mark as Planned`
      button and (given a `previousService`) an `Undo` button still render.
    - Slides tab: `[data-testid="action-bar-item-present"]` exists in the page header — assert it is a
      descendant of the header's action container and NOT of `[data-testid="slides-tab"]`. Its sibling
      order places it immediately before the `Save` button.
    - Triggering a click on the present item results in `PresentationViewer` being rendered; assert the
      `initial-index` prop equals the value the pre-phase Slides-tab click test asserted.
    - Viewer (`isEditor` false) on Service Order: no `Suggest All Songs`, no `Save`, no credentials note,
      but the export-or-copy control DOES render — assert its presence explicitly, citing that this is
      the preserved pre-phase gate (`36-02-SUMMARY.md`'s recorded divergence).
    - Locked editor (status `planned`) on Service Order: same expectation as the viewer row above.
    - 34-10 guard: `[data-testid="service-save-status-bar"]` exists for an editor with
      `congregationalSlotIndex` null; at idle its class attribute is empty (no `border`, `bg-`, `px-`,
      `py-`, `sticky`, `mb-`), and `SaveStatusIndicator` is mounted inside it. Assert this as a named
      regression guard referencing 34-10.
    - Idempotency: capture the Service Order header button texts, click Slides, click Roles, click
      Service Order, capture again — the two arrays are equal.
  </acceptance_criteria>

  <done>
    The header shows only the active tab's actions, Present lives beside Save on Slides, R071's note
    travels with Copy for PC, and 34-10's chrome-strip is proven intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reorder the tab strip to Service Order · Slides · Roles (R069)</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>

  <read_first>
    - `src/views/ServiceEditorView.vue` lines 661–699 (all three tab buttons and the comment above the
      Slides button), line 1469 (the `activeTab` union), lines 1231–1234 and 1321 (the two `v-show`
      panel wrappers)
    - `src/views/__tests__/ServiceEditorView.test.ts` lines 1760–1790 and 1855–1870 (the order assertion
      and the index-based first-tab selection)
    - `36-UI-SPEC.md` § Phase-Specific Component Contracts §1 (including the note that the wireframe
      draws the OLD order and the requirement wins)
  </read_first>

  <action>
    Inside the tab-strip container, move the Slides `button` block so it sits between the Service Order
    button and the Roles button. The Roles button keeps its `v-if="authStore.isEditor"` gate verbatim;
    the Slides button keeps no gate, so it stays visible to viewers. Keep every class string, `:class`
    expression and `@click` assignment on all three buttons exactly as they are — this is a reposition,
    not a restyle. Move the explanatory comment that belongs to the Slides button along with it, and
    update the tab-strip container's own leading comment, which currently names only Service Order and
    Roles, to name all three in their new order.

    Do NOT reorder the two `v-show` panel wrappers — DOM order of `v-show` siblings has no bearing on
    which is visible, and moving them would create a large, meaningless diff in the file's biggest
    region. Do NOT change the `activeTab` union: it is a literal union and is order-independent.
    Introduce no `role="tab"`/`role="tablist"` semantics; none exist today and adding them is not in
    scope.

    Update the two order-dependent assertions in `ServiceEditorView.test.ts` — the array-equality order
    check and the index-based first-tab lookup. **Comment both as moved-control edits**: the buttons,
    their gates and their click behaviour are unchanged; only their rendered order is.
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Editor: the filtered tab-button text array equals `['Service Order', 'Slides', 'Roles']`.
    - Viewer: the filtered tab-button text array equals `['Service Order', 'Slides']` — the Roles gate
      is unchanged.
    - Clicking each of the three buttons still shows the matching panel:
      `[data-testid="service-order-panel"]`, the Slides panel containing `[data-testid="slides-tab"]`,
      and the Roles panel containing `[data-testid="role-override-picker"]` for an editor with roles.
    - `grep -c "'service-order' | 'roles' | 'slides'" src/views/ServiceEditorView.vue` is at least 1 —
      the union is untouched.
    - `grep -c 'role="tablist"' src/views/ServiceEditorView.vue` is 0 — no ARIA semantics were
      introduced.
  </acceptance_criteria>

  <done>
    Roles is last. Nothing else about the tabs changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer / locked editor → header action controls | the set of offered write actions is decided here |
| header bar → `SlidesTab` exposed API | a page-level control now drives a child component's behaviour |
| R071 note → settings route | a link surfaced only to editors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03-01 | Elevation of Privilege | per-tab header action rendering | critical | mitigate | Mounted per-tab matrix asserts the viewer and locked-editor button sets match the pre-phase behaviour, on top of `36-02`'s pure gating matrix. Two independent proofs at two levels, because this is the one failure mode the phase is most likely to hide. |
| T-36-03-02 | Elevation of Privilege | `defineExpose` widening `SlidesTab`'s public surface | medium | mitigate | Only `canPresent` (a boolean computed) and `onPresentClick` (emit-only, no writes) are added. Present has no editor gate today and gains none; the exposed function performs no mutation. |
| T-36-03-03 | Information Disclosure | R071 credentials note | low | mitigate | The note names no secret — it states credentials are absent and links to Settings. Gate `canEditService && !hasPcCredentials` moved verbatim from 34-12 and asserted on all three tabs. |
| T-36-03-04 | Denial of Service | `aria-live` save-status region | medium | mitigate | Named regression guard asserts the wrapper stays mounted at idle with zero chrome classes. Unmounting it would cost the first status announcement of every session (34-10's recorded R041 trade-off). |
| T-36-03-05 | Tampering | tab reorder touching the `activeTab` union | low | mitigate | Grep assertion that the literal union is unchanged; panels deliberately not reordered. |
| — | Supply chain | n/a | — | n/a | No package-manager install in this plan. No `T-36-SC` row claimed. |
</threat_model>

## Artifacts this phase produces

| Artifact | Kind | Detail |
|---|---|---|
| `slidesTabRef` | ref | `ServiceEditorView` → `SlidesTab` instance handle |
| `SlidesTab.canPresent` | exposed | boolean computed, newly exposed |
| `SlidesTab.onPresentClick` | exposed | emit-only function, newly exposed |
| `activeActionItems` | computed | `ServiceEditorView`; `buildActionBarItems(activeTab, ctx)` |
| `contextual-action-bar` | testid | mounted in the existing page-header action row |
| `action-bar-item-present` | testid | Present's new home, in the page header |
| `export-pc-btn` / `copy-pc-btn` | testid | preserved, now rendered by the bar |
| `pc-credentials-missing-note` | testid | preserved, moved into the bar's `hint-copy-pc` slot |
| (removed) the Slides tab's local Present CTA | testid | deleted from `SlidesTab.vue` |
| tab strip order | markup | Service Order · Slides · Roles |

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts src/components/slides/__tests__/SlidesTab.test.ts` passes.
- `npx vitest run src/components/__tests__/ContextualActionBar.test.ts src/views/__tests__/serviceEditorActionBar.test.ts`
  still passes — `36-02`'s contract was consumed, not changed.
</verification>

<success_criteria>
- ROADMAP criterion 2 (only relevant actions per tab) is true in the mounted app.
- ROADMAP criterion 3 (Present per design 1a) is true.
- ROADMAP criterion 5 (Roles last) is true.
- 34-10 and 34-12 both provably survive.
</success_criteria>

<output>
Create `.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-03-SUMMARY.md` when done.
It MUST list every edited existing test assertion with its category — **moved control** or **behaviour
change** — and state plainly if the count of behaviour-change edits is anything other than zero.
</output>
