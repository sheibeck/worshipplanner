---
phase: 141-vamp-slide-assignment-live-playback
plan: 04
type: execute
wave: 2
depends_on: [141-01]
files_modified:
  - src/types/vamp.ts
  - src/stores/vamps.ts
  - src/stores/__tests__/vamps.test.ts
  - src/components/VampSlideOver.vue
  - src/components/__tests__/VampSlideOver.test.ts
autonomous: true
requirements: [R440]

must_haves:
  truths:
    # R440 goal-backward (CONTEXT.md "Delete warning (R440)")
    - "Opening the Delete-vamp confirm runs a best-effort scan (useVampStore().countAssignments) of the org's slideGroups for entries whose vampId matches, joined to services with date >= todayYmd(), counting DISTINCT upcoming services; the confirm then reads `Assigned in {N} upcoming service{s} — those slides keep their audio.` (singular for N=1) above the unchanged Phase 140 body."
    - "If the scan fails the confirm shows the generic `May be assigned to slides.` line instead; with zero upcoming assignments no extra line renders. Deletion is NEVER blocked — the Delete button works while the scan is pending, after it fails, and whatever the count is."
    - "deleteVamp keeps the MP3 in Storage when ANY assignment exists (any service, upcoming or past) and deletes both the doc and the MP3 when nothing is assigned (Phase 140 behavior); the Firestore doc is always deleted. When the pre-delete scan itself fails, the MP3 is kept (fail-safe toward assigned slides keeping their audio)."
    - "The upcoming-count shown in the UI and the any-assignment Storage-keep decision are two derived values of ONE scan shape (VampAssignmentScan { assignedAnywhere, upcomingServiceCount }) — never conflated (RESEARCH Assumption A2 / Open Question 1)."
    - "Already-assigned slides keep playing after the vamp is deleted: their denormalized audioUrl still resolves to the kept Storage object, and the editor row shows `Vamp: {vampLabel}` with the `(no longer in library)` hint (Plan 141-01)."
    # Edge probe (R440) — lifted verbatim as covered truths
    - "A service dated today counts as upcoming: the scan uses date >= todayYmd()"
    - "With zero assignments the delete confirm shows no warning line and deleteVamp removes both the doc and the Storage MP3"
    - "The warning count is of distinct serviceIds — a vamp on several slides of one service counts once; ordering is irrelevant"
    - "deleteVamp on an already-deleted vamp resolves without throwing (Firestore deleteDoc is idempotent; Storage delete failures are logged and swallowed)"
    # UI-SPEC UI Considerations — E7 Delete-confirm warning (covered)
    - "E7 overflow: Single warning line inside the existing rounded-lg bg-red-900/20 block; wraps naturally, no fixed height"
    - "E7 long-text: Copy is fixed with a numeric slot; singular/plural branch (`1 upcoming service` / `N upcoming services`); scan failure → `May be assigned to slides.`"
    # Edge probe backstop (R440 concurrency) — flat scalar marker
    - { statement: "An assignment created between the pre-delete scan and the delete may lose its MP3 — accepted residual; verify at UAT that a normal delete-after-assign keeps the file", verification: backstop }
  artifacts:
    - src/types/vamp.ts
    - src/stores/vamps.ts
    - src/components/VampSlideOver.vue
    - src/stores/__tests__/vamps.test.ts
    - src/components/__tests__/VampSlideOver.test.ts
  key_links:
    - "vamps.ts countAssignments(vampId): getDocs(collection(db, 'organizations', org, 'slideGroups')) → filter `slides.some(e => e.vampId === vampId)` → Set<serviceId> → getDoc each service → count `exists() && data.date >= todayYmd()`; try/catch returns null (mirrors services.ts resyncRehearseAccessForSong's shape)."
    - "vamps.ts deleteVamp(id): `scan = await countAssignments(id)`; `keepAttachment = scan === null || scan.assignedAnywhere`; deleteObject only when !keepAttachment; deleteDoc always."
    - "VampSlideOver.vue: watch(showDeleteConfirm) → on true: reset + `vampStore.countAssignments(effectiveId)` once → affectedServiceCount / scanFailed drive the §7 warning line; onDelete() unchanged (still just `deleteVamp(id)` + emit)."
    - "todayYmd is imported from @/utils/myScheduleGrouping (the codebase's single YYYY-MM-DD comparison idiom) — no new date helper."
  prohibitions:
    - { statement: "Deletion is never blocked by the assignment scan — no scan result, pending state, or scan failure may disable or skip the Delete action", status: enforced, verification: "VampSlideOver.test.ts asserts deleteVamp is called and `deleted` emitted while countAssignments is still pending AND after it rejected/returned null" }
    - { statement: "Never conflate the upcoming-only count with the any-assignment Storage-keep decision", status: enforced, verification: "vamps.test.ts asserts a vamp assigned ONLY to a past-dated service reports upcomingServiceCount 0 AND deleteVamp still keeps the MP3" }
    - { statement: "Never add firestore.exists() (or any cross-service call) to storage.rules, and never change firestore.rules — the scan is a client read already permitted to org members", status: enforced, verification: "storage.rules and firestore.rules are not in files_modified" }
    - { statement: "Never introduce a Cloud Function or a denormalized assignment index for the count — it is a best-effort client scan (RESEARCH Don't Hand-Roll)", status: enforced, verification: "no files under functions/ in the diff; countAssignments lives in src/stores/vamps.ts" }
---

<objective>
Deliver R440: before a vamp is deleted, the editor sees how many upcoming services have it assigned
(best-effort, never blocking), and the delete keeps the MP3 in Storage whenever any slide anywhere still
references the vamp so those slides keep playing. Adds `VampAssignmentScan` + `useVampStore().
countAssignments(vampId)` (one org-scoped `getDocs` scan mirroring `resyncRehearseAccessForSong`), branches
`deleteVamp`'s Storage cascade on that scan, and prepends the UI-SPEC §7 warning line to `VampSlideOver`'s
existing delete confirm.

Purpose: Deleting a library entry must not silently break a planned service, and the editor must be told
the blast radius before confirming — while never being prevented from deleting.
Output: `src/types/vamp.ts` `VampAssignmentScan`; `countAssignments` + conditional-keep `deleteVamp` in
`src/stores/vamps.ts`; `vamp-delete-warning` / `vamp-delete-warning-generic` in `VampSlideOver.vue`;
extended `vamps.test.ts` and `VampSlideOver.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-vamp-slide-assignment-live-playback/141-CONTEXT.md
@.planning/phases/141-vamp-slide-assignment-live-playback/141-RESEARCH.md
@.planning/phases/141-vamp-slide-assignment-live-playback/141-PATTERNS.md
@.planning/phases/141-vamp-slide-assignment-live-playback/141-UI-SPEC.md
@.planning/phases/141-vamp-slide-assignment-live-playback/141-VALIDATION.md
@.planning/phases/140-vamps-library-crud-storage/140-01-SUMMARY.md
@.planning/phases/140-vamps-library-crud-storage/140-03-SUMMARY.md
@.planning/phases/141-vamp-slide-assignment-live-playback/141-01-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide symbol inventory (this plan's items marked **(this plan)**):

- 141-01: `GroupSlideEntry.vampId?` / `vampLabel?` (this plan's scan filters on `vampId`); `VampPicker.vue`; `EditSlideDrawer.vue` assign/change/clear + `(no longer in library)` hint; `ServiceEditorView.vue` editor-gated vamp subscribe.
- 141-02: `SlideCanvas.suppressAudio`; four output call sites.
- 141-03: `useRunControl.ts` audio state + `AudioPlayerHandle`; `RunControlView.vue` single `AudioPlayer` + blocked banner; `RunHeader.vue` arm toggle/indicators; `RunPreviewPair.vue` ♪ badges; `RunControlView.audio.test.ts`.
- **(this plan)** `src/types/vamp.ts` — `export interface VampAssignmentScan { assignedAnywhere: boolean; upcomingServiceCount: number }`.
- **(this plan)** `src/stores/vamps.ts` — `countAssignments(vampId: string): Promise<VampAssignmentScan | null>` (returned from the store); `deleteVamp(id)` signature unchanged, behavior now conditional (Storage cascade only when the scan proves no assignment).
- **(this plan)** `src/components/VampSlideOver.vue` — refs `affectedServiceCount: Ref<number | null>`, `scanFailed: Ref<boolean>`; `vamp-delete-warning`, `vamp-delete-warning-generic`. Copy: `Assigned in {N} upcoming service{s} — those slides keep their audio.`, `May be assigned to slides.`
- **(this plan)** tests: `describe('countAssignments (R440)')` + `describe('deleteVamp — conditional Storage keep (R440)')` in `vamps.test.ts` (with `getDocs`/`getDoc` added to the `firebase/firestore` mock factory); `describe('VampSlideOver — R440 delete warning')` in `VampSlideOver.test.ts` (with `countAssignments` added to the store mock).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VampAssignmentScan + useVampStore.countAssignments (best-effort scan) + deleteVamp conditional Storage keep</name>
  <files>src/types/vamp.ts, src/stores/vamps.ts, src/stores/__tests__/vamps.test.ts</files>
  <read_first>
    - .planning/phases/141-vamp-slide-assignment-live-playback/141-CONTEXT.md ("Delete warning (R440)" — the three locked decisions: best-effort count of distinct upcoming services; keep the MP3 when ANY assignment exists (any service); deletion never blocked)
    - .planning/phases/141-vamp-slide-assignment-live-playback/141-RESEARCH.md ("Code Examples → resyncRehearseAccessForSong's cross-collection scan shape" — the `countAssignments` sketch and its note that the upcoming count and the any-assignment decision are DIFFERENT signals; "Assumptions Log" A2; "Open Questions" 1 — one scan, two derived values)
    - .planning/phases/141-vamp-slide-assignment-live-playback/141-PATTERNS.md (`src/stores/vamps.ts` section — the existing `deleteVamp` to branch and the "Task-boundary note")
    - src/stores/vamps.ts (the whole file — imports from `firebase/firestore` (add `getDocs`, `getDoc`), `orgId`, `deleteVamp` lines 93-104, the return block)
    - src/stores/services.ts (`resyncRehearseAccessForSong`, ~line 791 — the `getDocs` → filter → per-id `getDoc` → try/catch shape to narrow)
    - src/utils/myScheduleGrouping.ts (line 23 — `todayYmd(now = new Date())`; plain string `>=` on `YYYY-MM-DD`)
    - src/types/slideGroup.ts (`SlideGroup.serviceId`, `slides: GroupSlideEntry[]`, `GroupSlideEntry.vampId?` from 141-01) and src/types/service.ts (line 221 — `Service.date: string`)
    - src/stores/__tests__/vamps.test.ts (lines 1-40 — the `firebase/firestore` and `firebase/storage` mock factories; NOTE `getDocs`/`getDoc` are NOT mocked yet and must be added; lines 40-75 — `makeVamp`/`triggerSnapshot`; the existing `deleteVamp` describe to extend)
  </read_first>
  <behavior>
    - `countAssignments('vamp-1')` with `getDocs` resolving three slideGroups — `svc-A` (two slides referencing vamp-1), `svc-B` (one slide referencing vamp-1), `svc-C` (none) — and `getDoc` resolving `svc-A` dated today (`todayYmd()`) and `svc-B` dated yesterday → returns `{ assignedAnywhere: true, upcomingServiceCount: 1 }`; `getDoc` was called once per distinct serviceId (twice), never for `svc-C`.
    - `countAssignments` with `getDoc` for a service that does not `exists()` → that service is not counted.
    - `countAssignments` with no matching slideGroups → `{ assignedAnywhere: false, upcomingServiceCount: 0 }` and `getDoc` is not called.
    - `countAssignments` when `getDocs` rejects → resolves `null` (does not throw); `console.error` was called.
    - `countAssignments` with `orgId` unset (store never subscribed) → resolves `null` without calling `getDocs`.
    - `deleteVamp('vamp-1')` for a vamp with an attachment when the scan reports `assignedAnywhere: true` (even with `upcomingServiceCount: 0` — a past-only assignment) → `deleteObject` NOT called, `deleteDoc` called with the vamp doc path.
    - `deleteVamp` when the scan reports `assignedAnywhere: false` → `deleteObject` called with the attachment's `storagePath` BEFORE `deleteDoc` (Phase 140 behavior preserved).
    - `deleteVamp` when the scan returns `null` (scan failed) → `deleteObject` NOT called (fail-safe keep), `deleteDoc` still called.
    - `deleteVamp('missing-id')` (no such vamp in `vamps`) → resolves without throwing; `deleteObject` not called; `deleteDoc` called (idempotent).
  </behavior>
  <action>
    In src/types/vamp.ts add `export interface VampAssignmentScan { assignedAnywhere: boolean; upcomingServiceCount:
    number }` with a two-line doc comment ("R440 — one org-wide slideGroups scan, two derived values: the
    any-assignment Storage-keep decision and the upcoming-services warning count. Never conflate them.").

    In src/stores/vamps.ts import `getDocs`, `getDoc` from `firebase/firestore`, `todayYmd` from
    `@/utils/myScheduleGrouping`, `type { SlideGroup }` from `@/types/slideGroup`, `type { Service }` from
    `@/types/service`, and `type { VampAssignmentScan }` from `@/types/vamp`. Add `async function
    countAssignments(vampId: string): Promise<VampAssignmentScan | null>`: return `null` when `!orgId.value`;
    inside a `try`: `groupsSnap = await getDocs(collection(db, 'organizations', org, 'slideGroups'))`; build
    `serviceIds = new Set<string>()` from docs whose `(d.data() as SlideGroup).slides?.some((e) => e.vampId ===
    vampId)` (guard `slides` being undefined on a legacy doc), adding `data.serviceId` only when it is a
    non-empty string; if the set is empty return `{ assignedAnywhere: false, upcomingServiceCount: 0 }`;
    otherwise `today = todayYmd()`, `snaps = await Promise.all([...serviceIds].map((sid) => getDoc(doc(db,
    'organizations', org, 'services', sid))))`, `upcomingServiceCount = snaps.filter((s) => s.exists() &&
    ((s.data() as Service).date ?? '') >= today).length`, return `{ assignedAnywhere: true, upcomingServiceCount }`.
    `catch (err)` → `console.error('countAssignments: scan failed:', err)` and return `null`. Comment: one
    line pointing at the precedent ("Mirrors services.ts resyncRehearseAccessForSong — best-effort, advisory,
    never blocks the delete"). Change `deleteVamp(id)`: after the `orgId` guard and the `vamp` lookup, `const
    scan = await countAssignments(id)`; `const keepAttachment = scan === null || scan.assignedAnywhere`; run
    the existing `deleteObject` try/catch ONLY when `!keepAttachment && vamp?.attachment?.storagePath`; always
    `await deleteDoc(...)`. One-line comment on the branch: "R440 — assigned slides keep their denormalized
    audioUrl, so the object must outlive the doc; a failed scan keeps it too (fail-safe)". Add
    `countAssignments` to the returned object.

    In src/stores/__tests__/vamps.test.ts extend the `firebase/firestore` mock factory with `getDocs: vi.fn(() =>
    Promise.resolve({ docs: [] }))` and `getDoc: vi.fn(() => Promise.resolve({ exists: () => false, data: () =>
    ({}) }))`, then in each new test drive them via `vi.mocked(getDocs).mockResolvedValueOnce(...)` /
    `vi.mocked(getDoc).mockImplementation(...)` (import them from `firebase/firestore` in the test). Build
    slideGroup doc fakes as `{ id, data: () => ({ serviceId, slides: [{ id, vampId }] }) }` and service fakes as
    `{ exists: () => true, data: () => ({ date }) }`; compute today's date with `todayYmd()` and yesterday by
    subtracting a day. Add `describe('countAssignments (R440)')` and `describe('deleteVamp — conditional Storage
    keep (R440)')` implementing every `<behavior>` bullet; for the keep cases subscribe the store and
    `triggerSnapshot([makeVamp({ attachment: { storagePath: 'orgs/org-1/vamp-files/vamp-1/u1/a.mp3', … } })])`
    first, and spy `console.error` with `vi.spyOn(console, 'error').mockImplementation(() => {})` for the
    failure case.
  </action>
  <acceptance_criteria>
    - `grep -c "export interface VampAssignmentScan" src/types/vamp.ts` == 1.
    - `grep -c "async function countAssignments" src/stores/vamps.ts` == 1; `grep -c "todayYmd" src/stores/vamps.ts` >= 2 (import + use); `grep -c "getDocs(collection(db, 'organizations', org, 'slideGroups'))" src/stores/vamps.ts` == 1; `grep -c "const keepAttachment = scan === null || scan.assignedAnywhere" src/stores/vamps.ts` == 1; the return block includes `countAssignments`.
    - `awk '/async function deleteVamp/,/^  }/' src/stores/vamps.ts | grep -c "deleteDoc"` == 1 (always deleted, exactly one call site) and `| grep -c "deleteObject"` == 1 (still one cascade site, now guarded).
    - `npx vitest run src/stores/__tests__/vamps.test.ts -t "R440"` passes ≥ 9 tests (the nine `<behavior>` bullets); the whole file stays green (the pre-existing Phase 140 `deleteVamp` cascade test still passes because the default `getDocs` mock returns no assignments).
    - No change to `firestore.rules`, `storage.rules`, or anything under `functions/`.
    - `npm run type-check` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/stores/__tests__/vamps.test.ts</automated>
  </verify>
  <done>`countAssignments` returns `{ assignedAnywhere, upcomingServiceCount }` (distinct services, `date >= today`, fail-open `null`), and `deleteVamp` keeps the MP3 whenever any assignment exists or the scan failed while always deleting the doc; vamps.test.ts fully green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VampSlideOver delete-confirm warning line (N upcoming / singular / scan failed / none) — never blocking</name>
  <files>src/components/VampSlideOver.vue, src/components/__tests__/VampSlideOver.test.ts</files>
  <read_first>
    - .planning/phases/141-vamp-slide-assignment-live-playback/141-UI-SPEC.md (§7 R440 delete-confirm warning line markup + testids; Copywriting Contract rows "Delete confirmation — assigned upcoming / scan failed / nothing assigned")
    - .planning/phases/141-vamp-slide-assignment-live-playback/141-PATTERNS.md (`src/components/VampSlideOver.vue` section — "Call countAssignments when the delete-confirm state opens (not on every render)")
    - src/components/VampSlideOver.vue (lines 246-283 — the Phase 140 delete block: `vamp-delete-button` → `showDeleteConfirm`, the `vamp-delete-confirm` card with `Delete "{{ form.name }}"? This cannot be undone.`, Cancel/Delete buttons, `isDeleting`; lines 335-345 — `showDeleteConfirm`/`localId`/`effectiveId`; lines 360-372 — the `watch(() => props.open)` reset block; lines 408-420 — `onDelete()`)
    - src/components/__tests__/VampSlideOver.test.ts (lines 9-25 — the `mockVampStore` singleton (`addVamp`/`updateVamp`/`deleteVamp`, `vamps` getter) — add `countAssignments`; the existing "the Delete-vamp button reveals a confirm card, and confirming calls deleteVamp then emits deleted" test to mirror)
  </read_first>
  <behavior>
    - Opening the drawer for an existing vamp and clicking `vamp-delete-button` calls `mockCountAssignments` exactly once with the vamp's id (not on mount, not on re-render); clicking Cancel then Delete again calls it again (once per confirm open).
    - With `countAssignments` resolving `{ assignedAnywhere: true, upcomingServiceCount: 3 }` the confirm shows `vamp-delete-warning` with text `Assigned in 3 upcoming services — those slides keep their audio.` above the unchanged `Delete "Open Response"? This cannot be undone.` body.
    - With `upcomingServiceCount: 1` the text is `Assigned in 1 upcoming service — those slides keep their audio.` (singular).
    - With `{ assignedAnywhere: true, upcomingServiceCount: 0 }` or `{ assignedAnywhere: false, upcomingServiceCount: 0 }` neither warning element renders (Phase 140 copy only).
    - With `countAssignments` resolving `null` (or rejecting) the confirm shows `vamp-delete-warning-generic` with `May be assigned to slides.` and no `vamp-delete-warning`.
    - While `countAssignments` is still pending (an unresolved promise), the `vamp-delete-confirm-button` is enabled; clicking it calls `mockDeleteVamp` with the id and emits `deleted` — deletion is never blocked by the scan.
    - After a scan failure, clicking Delete still calls `mockDeleteVamp` and emits `deleted`.
    - Reopening the drawer (`open` false → true) resets `affectedServiceCount` to null and `scanFailed` to false (no stale warning from a previous vamp).
  </behavior>
  <action>
    In src/components/VampSlideOver.vue add `const affectedServiceCount = ref<number | null>(null)` and `const scanFailed
    = ref(false)`. Add `watch(showDeleteConfirm, async (open) => { if (!open) return; affectedServiceCount.value =
    null; scanFailed.value = false; const id = effectiveId.value; if (!id) return; try { const scan = await
    vampStore.countAssignments(id); if (scan === null) { scanFailed.value = true } else {
    affectedServiceCount.value = scan.upcomingServiceCount } } catch { scanFailed.value = true } })` — one scan
    per confirm open, best-effort, and NOTHING in `onDelete()` reads it (leave `onDelete` byte-for-byte unchanged
    so deletion can never be gated on the scan). Reset both refs in the existing `watch(() => props.open)` block
    beside `showDeleteConfirm.value = false`. Template — replace the confirm card's contents with 141-UI-SPEC §7
    verbatim: keep the wrapper `<div v-else class="rounded-lg bg-red-900/20 border border-red-800 p-4"
    data-testid="vamp-delete-confirm">`, prepend `<p v-if="affectedServiceCount != null && affectedServiceCount > 0"
    class="text-sm text-amber-300 mb-2" data-testid="vamp-delete-warning">Assigned in {{ affectedServiceCount }}
    upcoming service{{ affectedServiceCount === 1 ? '' : 's' }} — those slides keep their audio.</p>` and
    `<p v-else-if="scanFailed" class="text-sm text-amber-300 mb-2" data-testid="vamp-delete-warning-generic">May
    be assigned to slides.</p>`, then the existing `Delete "…"? This cannot be undone.` paragraph and the
    unchanged Cancel/Delete buttons (the Delete button's `:disabled` stays `isDeleting` ONLY).

    In src/components/__tests__/VampSlideOver.test.ts add `const mockCountAssignments = vi.fn(() => Promise.resolve({
    assignedAnywhere: false, upcomingServiceCount: 0 }))` to `mockVampStore` as `countAssignments`, reset it in
    `beforeEach`, and add `describe('VampSlideOver — R440 delete warning')` with one `it` per `<behavior>`
    bullet. For the pending case use `mockCountAssignments.mockReturnValueOnce(new Promise(() => {}))`; for the
    failure case `mockRejectedValueOnce(new Error('denied'))` and `mockResolvedValueOnce(null)` (both must
    produce the generic line). Use `flushPromises()` after opening the confirm.
  </action>
  <acceptance_criteria>
    - `grep -c 'data-testid="vamp-delete-warning"' src/components/VampSlideOver.vue` == 1; `grep -c 'data-testid="vamp-delete-warning-generic"' src/components/VampSlideOver.vue` == 1; the file contains `those slides keep their audio.` and `May be assigned to slides.` and `affectedServiceCount === 1 ? '' : 's'`.
    <!-- planner-discipline-allow: countAssignments -->
    <!-- planner-discipline-allow: vampStore.countAssignments( -->
    - `grep -c "vampStore.countAssignments(" src/components/VampSlideOver.vue` == 1 (the single confirm-open scan call site).
    - Region-scoped: the scan is NOT called from the delete handler — `awk '/async function onDelete/,/^}/' src/components/VampSlideOver.vue | grep -c "countAssignments"` == 0.
    - `awk '/async function onDelete/,/^}/' src/components/VampSlideOver.vue | grep -c "deleteVamp(id)"` == 1 (the delete handler is unchanged).
    - The Delete button's disabled binding is unchanged: `grep -c ':disabled="isDeleting"' src/components/VampSlideOver.vue` == 1.
    - `npx vitest run src/components/__tests__/VampSlideOver.test.ts -t "R440"` passes ≥ 8 tests; the whole file stays green (the Phase 140 delete test still passes with the default resolving mock).
    - `npm run type-check` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/VampSlideOver.test.ts</automated>
  </verify>
  <done>The delete confirm shows the correct singular/plural upcoming-service warning, the generic fallback on scan failure, and nothing when unassigned; the scan runs once per confirm open and never gates Delete; VampSlideOver.test.ts fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor client → Firestore `organizations/{orgId}/slideGroups` (list) + `services/{id}` (get) | The R440 scan — org-scoped reads already permitted to org members by the existing rules (`slideGroups` read: `isOrgMember`; `services` read: `isOrgMember`); no rule change |
| editor client → Firestore `organizations/{orgId}/vamps/{id}` delete + Storage `orgs/{orgId}/vamp-files/…` delete | The existing Phase 140 delete path, now conditional on the scan for the Storage half |
| scan result → UI copy | Advisory only; the Delete action never consumes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-141-13 | Tampering | Storage cascade orphaning an assigned slide's audio | medium | mitigate | `deleteVamp` keeps the MP3 whenever the scan finds ANY assignment (any service) and also when the scan fails (`scan === null` → keep); only a proven-unassigned vamp cascades. Test-proven for past-only, upcoming, none, and failed-scan cases. |
| T-141-14 | Tampering (race) | assignment created between the pre-delete scan and the delete | low | accept | Accepted residual per CONTEXT.md ("best-effort") — the scan re-runs inside `deleteVamp` itself (not reusing the confirm-time result), narrowing the window to the delete call; recorded as the `verification: backstop` edge for UAT. |
| T-141-15 | Information disclosure | org-wide `slideGroups` list read by an editor | low | accept | Path-scoped to the editor's own org; the same data the editor can already open service-by-service; permitted by the existing member-read rule. No cross-org path is constructible (org comes from the store's `orgId`). |
| T-141-16 | Denial of service | scan cost (reads every slideGroup in the org + one `getDoc` per affected service) | low | accept | Runs only on delete-confirm open and inside the delete itself — a rare editor action; failure returns `null` and never blocks; no Cloud Function or index introduced (RESEARCH Don't Hand-Roll). |
| T-141-17 | Repudiation | misleading count (displayed 0 upcoming while the MP3 is kept for a past assignment) | low | mitigate | Two derived values of one scan (`VampAssignmentScan`), never conflated; the warning copy states the case that applies (`those slides keep their audio`), and the kept-vs-deleted decision is test-proven independent of the displayed count. |
| T-141-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies. |
</threat_model>

<verification>
- Per task: the task's `<automated>` command green.
- Per plan: bare `npx vitest run` shows only the documented `src/storage.rules.test.ts` baseline failure AND `npm run type-check` (vue-tsc --build) is clean. `firestore.rules`/`storage.rules` untouched, so `npm run test:rules` is not required for this plan (Phase 140-02's rules proofs still cover the collection).
- Goal-backward: every R440 truth, all four lifted edges, and both E7 rows are demonstrated by `vamps.test.ts` and `VampSlideOver.test.ts`; the concurrency backstop (`verification: backstop`) and the deployed delete-after-assign keeps-the-file check are batched by the orchestrator into `.planning/v2.15-DEFERRED-VERIFICATION.md`.
</verification>

<success_criteria>
- `countAssignments` (distinct upcoming services, `date >= todayYmd()`, fail-open null) and the conditional-keep `deleteVamp` exist and are test-proven; `VampSlideOver` prepends the exact §7 copy and never gates Delete.
- No rules, functions, or assembler changes; both suites green; full app suite at baseline; type-check clean.
</success_criteria>

<output>
Create `.planning/phases/141-vamp-slide-assignment-live-playback/141-04-SUMMARY.md` when done.
</output>
