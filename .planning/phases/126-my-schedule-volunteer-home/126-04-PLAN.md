---
phase: 126-my-schedule-volunteer-home
plan: 04
type: execute
wave: 3
depends_on: [126-02, 126-03]
files_modified:
  - src/components/ScheduleServiceCard.vue
  - src/views/MyScheduleView.vue
  - src/router/index.ts
  - src/views/VolunteerLinkCompleteView.vue
  - src/views/__tests__/MyScheduleView.test.ts
  - src/router/__tests__/router.test.ts
autonomous: true
requirements: [R378, R380, R381, R382, R383]

must_haves:
  truths:
    - "After a successful magic-link sign-in a volunteer lands on /my-schedule (not /volunteer)"
    - "My Schedule renders the volunteer's assigned Planned services grouped This week / Later this month, soonest first, with a Next up accent variant + pill on the single soonest upcoming card, and past services under a collapsed Past services section"
    - "A section with zero services is not rendered (no empty header)"
    - "Each upcoming card shows the date block, service name, the volunteer's own role chips (instrument glyph via roleChipIcon + StageKindIcon), song/chart/track counts, the readiness indicator, and the countdown; time·venue and call time render only when present and are omitted (no placeholder) when absent"
    - "The whole card is a single interactive link with an aria-label carrying full context; readiness and role-chip icons are aria-hidden"
    - "Rehearse → (upcoming) and Open service (past) navigate to /volunteer/service/:serviceId by literal path"
    - "When the constrained query returns zero docs, an empty state renders with the signed-in email interpolated and a 'Check a different email' action; the greeting/summary/sections are replaced"
    - "The greeting shows 'Good {morning/afternoon/evening}, {firstName}' using first-name-only extraction (never 'Dana R.'), the summary line pluralizes service/services, and the roster-built footer note renders in the populated state"
    - "Loading shows the spinner + 'Loading your schedule…'; a query failure shows the load-failure copy + a Retry action"
    - "Card three zones stack vertically below sm with a full-width CTA; the top-bar email line hides below sm"
    - {statement: "an unusually long custom role name truncates cleanly inside its chip at both desktop and mobile widths", verification: backstop}
  artifacts:
    - "src/components/ScheduleServiceCard.vue — the three-zone service card (upcoming + past variants)"
    - "src/views/MyScheduleView.vue — top bar, greeting, summary, sections, cards, empty/loading/error states, footer"
    - "src/router/index.ts — /my-schedule route registered against MyScheduleView.vue"
    - "src/views/VolunteerLinkCompleteView.vue — post-sign-in redirect updated to my-schedule"
    - "src/views/__tests__/MyScheduleView.test.ts — empty / populated / check-different-email / Rehearse-target tests"
    - "src/router/__tests__/router.test.ts — /my-schedule resolves to name 'my-schedule'"
  key_links:
    - "MyScheduleView consumes the Plan-03 mySchedule store + Plan-02 groupMySchedule/readinessOf/countdownLabel/roleChipIcon; the card selects rolesByEmailLower[ownEmailLower] for its own chips"
    - "The Rehearse/Open link uses a literal path push/`to` to /volunteer/service/:serviceId — it resolves to the Plan-03 placeholder now and to the Phase-127 view later, with no client change"
    - "The /my-schedule route + landing redirect are registered in this plan (same plan as MyScheduleView.vue) so no route ever lazy-imports a nonexistent file"
---

<objective>
Build the volunteer-facing My Schedule page per the UI-SPEC design contract: a dark, read-and-go landing that lists a volunteer's assigned Planned services grouped and prioritized, each card carrying roles, counts, readiness, and a countdown, with a clear Rehearse/Open action, an empty state, and a "check a different email" affordance. Register the `/my-schedule` route (against this view) and make it the post-sign-in landing.

Purpose: this is the phase's user-visible deliverable — everything Plans 01-03 built (the safe read, the projection field, the pure helpers, the store, the placeholder route) converges here into the screen R378-R383 describe.

Output: `src/views/MyScheduleView.vue` + `src/components/ScheduleServiceCard.vue`, the `/my-schedule` route + landing redirect, and component tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-my-schedule-volunteer-home/126-UI-SPEC.md
@.planning/phases/126-my-schedule-volunteer-home/126-PATTERNS.md
@.planning/phases/126-my-schedule-volunteer-home/126-RESEARCH.md
@src/views/ServicesView.vue
@src/views/VolunteerSignInView.vue
@src/components/ServiceCard.vue
@src/components/stage/StageKindIcon.vue
@src/stores/mySchedule.ts
@src/utils/myScheduleGrouping.ts
@src/utils/roleChipIcon.ts
@src/utils/rehearseAccess.ts
@src/router/index.ts
@src/views/VolunteerLinkCompleteView.vue
</context>

## Artifacts this phase produces (phase-wide roll-up)

- **Plan 01:** the constrained `list` rule arm, the COLLECTION_GROUP index, the R378 rules test, and `rolesByEmailLower`.
- **Plan 02:** pure helpers — `myScheduleGrouping.ts` + `roleChipIcon.ts` with tests.
- **Plan 03:** the My Schedule store, the build-safe `/volunteer/service/:serviceId` placeholder route + view.
- **Plan 04 (this plan):** the My Schedule view (`src/views/MyScheduleView.vue`), the service card (`src/components/ScheduleServiceCard.vue`), the `/my-schedule` route + post-sign-in landing redirect, and component tests.

<tasks>

<task type="auto">
  <name>Task 1: ScheduleServiceCard.vue — the three-zone service card</name>
  <files>src/components/ScheduleServiceCard.vue</files>
  <read_first>
    - 126-UI-SPEC.md §4 (three-zone anatomy: date block / middle / right), §5 (past variant), §6 (readiness states + copy), §11 (mobile reflow), the Color/Typography/Icon Inventory sections, and the Copywriting Contract (countdown, readiness, meta-counts, Rehearse/Open labels)
    - src/components/ServiceCard.vue (the whole-card-is-one-link + literal-path navigation convention — but note UI-SPEC §4 wants the ENTIRE card as one link, NOT the outside-the-link footer-button structure)
    - src/components/stage/StageKindIcon.vue (the role-chip glyph component to reuse) and src/utils/roleChipIcon.ts (the name→glyph mapping)
    - src/utils/myScheduleGrouping.ts (readinessOf, countdownLabel) and src/utils/rehearseAccess.ts (RehearseAccessDoc/RehearseSong shape)
  </read_first>
  <action>
    Create src/components/ScheduleServiceCard.vue accepting a card view-model prop (the RehearseAccessDoc-derived fields plus the volunteer's own resolved roles list, an isNextUp flag, and an isPast flag). Render the three zones per UI-SPEC §4: Zone 1 date block (month / big font-mono day / weekday), Zone 2 middle (service name + Next up pill when isNextUp; the optional time·venue line rendered only when present; the "You're on:" role chips built by pairing roleChipIcon(roleName) with StageKindIcon; the "{N} songs · {X} of {Y} charts · {Z} tracks" meta row derived from songs/attachments; the readiness line via readinessOf with the exact §6 icon+copy+color per state), Zone 3 right (countdown via countdownLabel; the Rehearse → CTA — outlined-accent on the Next-up card, quiet text-accent otherwise; the optional "Call time" line only when present). Compute song/chart/track counts inline from songs (songs.length; count of songs having ≥1 pdf attachment as "X of Y charts"; total mp3 attachments as tracks) per UI-SPEC Data Dependencies. Implement the past variant (§5): dimmed title, no Next up, "Open service" plain-text link instead of Rehearse. Make the ENTIRE card a single interactive link (router-link or the app's literal-path idiom) to `/volunteer/service/${serviceId}` — never a nested button inside a link — with an aria-label carrying service name + date + countdown; mark readiness and role-chip icons aria-hidden. Apply the §11 mobile reflow (flex-col below sm, date block collapses to one inline row, CTA becomes full-width, chips flex-wrap). Omit time·venue and call time entirely when absent (no em-dash/TBD placeholder). Use only the app's existing dark-theme Tailwind tokens named in the UI-SPEC Color section.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` clean with the new component.
    - The card is a single link to /volunteer/service/:serviceId with an aria-label; readiness/role-chip icons are aria-hidden.
    - Upcoming vs past vs Next-up variants render per UI-SPEC §4/§5; time·venue and call time are omitted when absent.
    - Counts and readiness are derived from songs[].attachments (no new data field required).
  </acceptance_criteria>
  <done>A reusable, accessible, responsive service card matching the UI-SPEC anatomy, driven purely by projection data + Plan 02 helpers.</done>
</task>

<task type="auto">
  <name>Task 2: MyScheduleView.vue + /my-schedule route + land on My Schedule</name>
  <files>src/views/MyScheduleView.vue, src/router/index.ts, src/views/VolunteerLinkCompleteView.vue</files>
  <read_first>
    - 126-UI-SPEC.md §1 (top bar + user-chip menu), §2 (greeting + summary + first-name extraction), §3 (section headers, only-when-non-empty), §7 (check-a-different-email affordance), §8 (empty state), §9 (footer note), §10 (loading), §12 (routing note), and the Copywriting Contract
    - src/views/VolunteerSignInView.vue (displayLabel/initials computeds + the avatar formula + handleSignOut calling volunteerAuth.signOut(); RESEARCH Pitfall 5: extract name.split(/\s+/)[0] for the greeting, do NOT reuse displayLabel's "Dana R." output)
    - src/views/ServicesView.vue (loading/empty-state/section-header shells + the showPast collapse toggle pattern for the Past services section)
    - src/stores/mySchedule.ts (docs/isLoading/error + loadMySchedule) and src/utils/myScheduleGrouping.ts (groupMySchedule) + src/utils/roleChipIcon.ts
    - src/router/index.ts lines 154-172 (volunteer route shape) and 205-287 (beforeEach; the /login zero-membership redirect to 'volunteer-home' at 278); src/views/VolunteerLinkCompleteView.vue line 81 (`router.replace({ name: 'volunteer-home' })`)
  </read_first>
  <action>
    Create src/views/MyScheduleView.vue: on mount call the Plan-03 store's loadMySchedule(); compute myEmailLower from auth.currentUser.email.toLowerCase(); pass docs through groupMySchedule(now) and render, in fixed order, the This week / Later this month sections (each only when non-empty) of ScheduleServiceCard (isNextUp = serviceId === group.nextUpId) and a collapsed-by-default Past services section (reuse ServicesView.vue's showPast toggle idiom) with isPast cards. For each card resolve the volunteer's own roles = doc.rolesByEmailLower[myEmailLower] ?? []. Build the top bar (§1) with the brand mark + a user-chip button opening a small menu (role="menu") with "Check a different email" (routes to /volunteer, VolunteerSignInView's remember-a-different-email flow — reused, not rebuilt) and "Sign out" (volunteerAuth.signOut()). Render the greeting (§2) "Good {morning/afternoon/evening}, {firstName}" — time-of-day from new Date(), firstName via name.split(/\s+/)[0] with a "Welcome" fallback (Pitfall 5) — and the summary line pluralizing service/services with the next-up countdown. Render the §8 empty state (replacing greeting+sections) with the signed-in email interpolated and a "Check a different email" button when docs is empty. Render the §10 loading state (spinner reused from VolunteerLinkCompleteView + "Loading your schedule…") while isLoading, and the load-failure copy + a Retry action (calls loadMySchedule again) when error is set. Render the §9 footer roster-built note (populated state only) with the signed-in email interpolated and a live "check a different email" link.

    Register the route + landing (R378): in src/router/index.ts add path '/my-schedule', name 'my-schedule', component lazy-importing '../views/MyScheduleView.vue', `meta: { requiresAuth: true, isVolunteerRoute: true }`. Change VolunteerLinkCompleteView.vue's success redirect (line 81) and the router beforeEach /login zero-membership redirect (line 278) from name 'volunteer-home' to name 'my-schedule'. Leave /volunteer (VolunteerSignInView) registered — it still serves the not-yet-signed-in guidance and the check-a-different-email flow this view links to.
  </action>
  <verify>
    <automated>npm run type-check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` clean; `npm run build` green (the /my-schedule route imports a file that now exists).
    - The view renders grouped sections (non-empty only), the Next up variant on the soonest card, a collapsed Past section, the empty/loading/error states, and the footer note per the UI-SPEC.
    - The greeting uses first-name-only extraction (not "Dana R.").
    - VolunteerLinkCompleteView + the /login zero-membership redirect both target 'my-schedule'.
  </acceptance_criteria>
  <done>My Schedule is the signed-in volunteer's landing, rendering their grouped assignments with all card content, empty/loading/error handling, and the check-a-different-email affordance.</done>
</task>

<task type="auto">
  <name>Task 3: Component + router tests for My Schedule</name>
  <files>src/views/__tests__/MyScheduleView.test.ts, src/router/__tests__/router.test.ts</files>
  <read_first>
    - existing src/views/__tests__/*.test.ts (the component-test harness: mounting, store mocking, Pinia setup conventions)
    - src/views/MyScheduleView.vue + src/components/ScheduleServiceCard.vue (this plan's Tasks 1-2)
    - src/stores/mySchedule.ts (the state shape to mock/seed)
    - src/router/__tests__/router.test.ts (the createRouter/mock harness to extend for the /my-schedule resolution)
  </read_first>
  <action>
    Create src/views/__tests__/MyScheduleView.test.ts mounting MyScheduleView with a mocked mySchedule store: (a) empty state — docs = [] renders the §8 empty state with the signed-in email interpolated and a "Check a different email" control; (b) populated — a set of docs renders grouped section headers and the Next up pill on the soonest card, and each card's Rehearse link `to`/href points at /volunteer/service/:serviceId; (c) loading — isLoading = true shows "Loading your schedule…"; (d) error — error set shows the load-failure copy + a Retry control. Mock auth.currentUser.email so myEmailLower resolves and rolesByEmailLower[myEmailLower] drives the chips. Extend src/router/__tests__/router.test.ts to assert resolving '/my-schedule' matches name 'my-schedule'.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/MyScheduleView.test.ts src/router/__tests__/router.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/views/__tests__/MyScheduleView.test.ts src/router/__tests__/router.test.ts` green (empty, populated + Rehearse-target, loading, error, and /my-schedule resolution).
    - `npm run type-check` clean.
    - The bare app suite (`npx vitest run`) shows no new failures beyond the documented `storage.rules.test.ts` baseline.
  </acceptance_criteria>
  <done>My Schedule's states and its Rehearse navigation target are held under automated component + router tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| volunteer client → rendered UI | The view renders projection data already authorized by the Plan-01 rule; the only judgment is which subset of rolesByEmailLower to display |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-07 | Information Disclosure | MyScheduleView role chips (rolesByEmailLower selection) | low | accept | The card displays only rolesByEmailLower[ownEmailLower]; the whole doc is already a document-level grant (Plan-01 rule), so selecting the requester's own key is a UX choice, not a security boundary — filtering client-side would be security theater (RESEARCH Open Question 1). |
| T-126-06 | Denial of Service / build integrity | /my-schedule route + Rehearse link | low | mitigate | The Rehearse/Open link uses a literal path to /volunteer/service/:serviceId (resolves to the Plan-03 placeholder); the /my-schedule route imports MyScheduleView.vue which is authored in this same plan, so `npm run build` stays green (verified in Task 2). |
| T-126-SC | Tampering (supply chain) | npm installs | n/a | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/MyScheduleView.test.ts src/router/__tests__/router.test.ts` green.
- `npm run type-check` clean; `npm run build` green.
- `npx vitest run` (bare app suite) shows no new failures beyond the documented `storage.rules.test.ts` baseline.
- Manual (per 126-VALIDATION.md): sign in as a rostered volunteer via a live magic link and confirm only Planned assigned services appear, grouped, with correct roles/readiness/countdown, and Rehearse → navigates.
</verification>

<success_criteria>
- A signed-in volunteer lands on My Schedule and sees their assigned Planned services grouped soonest-first with a Next up badge (R378, R380).
- Each card shows date/name/role chips/counts/readiness/countdown, with time·venue and call time gracefully omitted when unmodeled (R381).
- Rehearse → / Open service navigate to the per-service route (R382).
- Empty state + check-a-different-email are present when no assignment matches (R383).
</success_criteria>

<output>
Create `.planning/phases/126-my-schedule-volunteer-home/126-04-SUMMARY.md` when done.
</output>
