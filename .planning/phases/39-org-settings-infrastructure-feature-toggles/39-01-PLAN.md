---
phase: 39-org-settings-infrastructure-feature-toggles
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/views/__tests__/SettingsView.test.ts
  - src/views/__tests__/SongsView.test.ts
autonomous: true
requirements: [R073, R089]

must_haves:
  truths:
    - "`src/views/__tests__/SettingsView.test.ts` exists and passes, so every later wave has a runnable home for the R073 dot-path-payload assertion and the R089 credential-retention assertion"
    - "`src/views/__tests__/SongsView.test.ts` exists and passes, so the R089 song-import hide assertion has a runnable home"
    - "Both harnesses mock `@/stores/auth` with a forward-compatible `settings` object exposing `aiEnabled`, `pcEnabled` and `vwModeEnabled`, so no later wave has to rewrite the mock factory to add the toggles"
    - "Both harnesses assert only behavior that is true TODAY — neither references a ref, handler or element this phase has not built yet, so Wave 0 lands green on unmodified source"
  artifacts:
    - "src/views/__tests__/SettingsView.test.ts (new)"
    - "src/views/__tests__/SongsView.test.ts (new)"
  key_links:
    - "The auth-store mock's `settings` object is the seam every later wave writes assertions against — if it is missing a key, Waves 2 and 3 silently read `undefined` instead of a boolean"
    - "`findImportSongsButton(wrapper)` helper in SongsView.test.ts is the selector Wave 2 reuses for the absent-when-`pcEnabled`-false case"
---

<objective>
Create the two test files `39-VALIDATION.md` § Wave 0 Requirements confirmed are MISSING, as
runnable harnesses against **unmodified** source. Nothing in this plan changes application code.

Purpose: `39-VALIDATION.md`'s per-requirement map routes two assertions — R089 credential
retention and R089 song-import hiding — to files that do not exist. Without them, Waves 2 and 3
would have to invent a mount harness while also implementing a feature, which is exactly the
"Wave 0 gap" the validation contract exists to close.

Output: two new `__tests__` files, each with a working `mount()` harness, store mocks whose shape
already anticipates `settings`, and assertions of current behavior only.

**Verified before planning (direct filesystem check, and re-confirmed during this planning pass):**
`src/views/__tests__/SettingsView.test.ts` — does not exist. `src/views/__tests__/SongsView.test.ts`
— does not exist. `src/views/__tests__/serviceEditorActionBar.test.ts` — exists (Wave 2 extends it,
does not create it).

**Decision traceability note:** `39-CONTEXT.md` records its locked decisions as prose bullets under
`## Implementation Decisions`, not numbered `D-NN` identifiers. Tasks therefore cite decisions by
section heading plus the decision's own verbatim phrase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-CONTEXT.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-PATTERNS.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-VALIDATION.md

Note: `.planning/STATE.md` is deliberately NOT `@`-referenced anywhere in this phase — it is
~65k tokens and would consume most of an executor's budget. The one binding rule from it is quoted
inline: the v1.5 **standing autonomy grant** authorizes deferring human-verify checkpoints into
`.planning/PENDING-VERIFICATION.md`, and forbids ever recording a deferred check as passed.
</context>

<artifacts_this_phase_produces>
Full symbol inventory for Phase 39 (repeated in every plan so any executor sees the whole surface):

| Symbol | Kind | Location | Plan |
|---|---|---|---|
| `Organization` | interface | `src/types/organization.ts` (new) | 39-02 |
| `OrgSettings` | interface | `src/types/organization.ts` (new) | 39-02 |
| `DEFAULT_ORG_SETTINGS` | const | `src/types/organization.ts` (new) | 39-02 |
| `settings` | store ref + export | `src/stores/auth.ts` | 39-02 |
| `isAiEnabled()` | private guard fn | `src/utils/claudeApi.ts` | 39-04 |
| `pcEnabled` | field on `ActionBarContext` | `src/views/serviceEditorActionBar.ts` | 39-05 |
| `aiEnabledInput` / `aiSavedFeedback` / `aiSaveError` | refs | `src/views/SettingsView.vue` | 39-03 |
| `pcEnabledInput` / `pcEnabledSavedFeedback` / `pcEnabledSaveError` | refs | `src/views/SettingsView.vue` | 39-03 |
| `onToggleAiEnabled()` | handler | `src/views/SettingsView.vue` | 39-03 |
| `onTogglePcEnabled()` | handler | `src/views/SettingsView.vue` | 39-03 |
| `src/views/__tests__/SettingsView.test.ts` | test file (new) | — | 39-01 |
| `src/views/__tests__/SongsView.test.ts` | test file (new) | — | 39-01 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the SettingsView test harness</name>

  <files>src/views/__tests__/SettingsView.test.ts</files>

  <read_first>
    - `src/views/SettingsView.vue` — the component under test. Read its `<script setup>` import
      block (lines ~219-226) to learn every module the harness must mock, and its template
      (lines ~60-215) for the two headings the harness asserts.
    - `src/views/__tests__/RosterView.test.ts` lines 1-40 — the analog named by 39-PATTERNS.md:
      the closest full-`mount()` view test with a store-mock harness in this codebase. Copy its
      `vi.mock('@/stores/auth', () => ({ useAuthStore: () => ({ ... }) }))` shape.
    - `src/stores/__tests__/auth.test.ts` lines 31-49 — the `vi.mock('firebase/firestore', ...)`
      block. 39-PATTERNS.md says copy it verbatim rather than rewriting it.
  </read_first>

  <action>
Create the file. It must mount `SettingsView.vue` successfully against source as it exists right
now, with no application-code changes.

Mocks required, derived from `SettingsView.vue`'s actual import list:
  - `@/stores/auth` — a `useAuthStore` factory returning an object literal. Expose `orgId`,
    `orgName`, `orgSlug`, `isEditor`, `hasPcCredentials`, `pcAppId`, `pcSecret`, `vwModeEnabled`,
    `setPcCredentials`, and — critically — a `settings` object with the three keys `aiEnabled`,
    `pcEnabled`, `vwModeEnabled`. Back each of those with a module-scope mutable variable exposed
    through a getter, following the getter-mock precedent 39-PATTERNS.md cites from
    `src/components/__tests__/SongTable.test.ts:39`, so a later wave can flip a toggle between
    assertions without rebuilding the mock. `settings` does not exist on the real store yet; it is
    seeded here on purpose so Waves 2 and 3 add assertions, not plumbing.
  - `firebase/firestore` — copy `auth.test.ts`'s block; at minimum `doc`, `updateDoc`, `getDoc`
    must be `vi.fn()` spies, and `updateDoc` must be reachable from the test body so a later wave
    can assert its payload shape.
  - `@/firebase` — return a bare `db` object.
  - `@/utils/planningCenterApi` — stub `validatePcCredentials`.
  - `@/utils/slug` — stub `deriveSlug` and `claimSlug`.
  - `AppShell` — stub through `global.stubs` rather than mocking the module, matching how other
    view tests keep the shell out of the assertion surface.

Assertions in this task cover ONLY behavior that is true today: the `Planning Center Integration`
heading renders, and the `Vertical Worship` heading renders. Do not assert anything about an AI
section, an AI toggle, or a Planning Center enable toggle — none of those exist until 39-03, and a
Wave 0 file that fails is worse than no file.

Add a short head comment stating this file was created by Wave 0 of Phase 39 as a harness, that
its auth-store mock deliberately carries a `settings` shape the real store does not yet have, and
that Waves 2/3 extend it.

Per the standing convention in this repo (`__tests__/` sibling directory, `.test.ts` suffix), the
placement is `src/views/__tests__/SettingsView.test.ts` — this is the "Test file placement" item
39-CONTEXT.md § Claude's Discretion leaves open, resolved to the existing convention.
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/SettingsView.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/views/__tests__/SettingsView.test.ts` exits 0 with at least 2 passing tests.
    - Source assertion: `grep -c "aiEnabled" src/views/__tests__/SettingsView.test.ts` returns at
      least 1 — the mock's `settings` shape is present.
    - Source assertion: `grep -c "pcEnabled" src/views/__tests__/SettingsView.test.ts` returns at
      least 1.
    - Source assertion: the file references none of the not-yet-built symbols —
      `grep -cE "aiEnabledInput|pcEnabledInput|onToggleAiEnabled|onTogglePcEnabled" src/views/__tests__/SettingsView.test.ts`
      returns 0.
    - `git status --porcelain src/views/SettingsView.vue` is empty — this task changed no
      application code.
  </acceptance_criteria>

  <done>
The file exists, passes standalone, its auth-store mock already carries `settings.aiEnabled` /
`settings.pcEnabled` / `settings.vwModeEnabled`, and no application source was touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the SongsView test harness</name>

  <files>src/views/__tests__/SongsView.test.ts</files>

  <read_first>
    - `src/views/SongsView.vue` — lines 30-55 for the Planning Center import trigger (the `<button>`
      whose click sets `importModalOpen = true`; note its visible label is `Import Songs`, NOT
      "Import from Planning Center"), and lines ~175-205 for the import list the harness must mock
      (`useRoute`/`useRouter`, `useSongStore`, `AppShell`, `SongFilters`, `SongTable`,
      `SongSlideOver`, `BatchQuickAssign`, `PcImportModal`).
    - `src/views/__tests__/RosterView.test.ts` lines 1-40 — the analog 39-PATTERNS.md assigns to
      this file: store-mock factories plus `mount(Component)`.
  </read_first>

  <action>
Create the file. It must mount `SongsView.vue` against unmodified source.

Mocks required, derived from `SongsView.vue`'s actual imports:
  - `@/stores/auth` — same forward-compatible factory shape as Task 1, including the `settings`
    object with `aiEnabled` / `pcEnabled` / `vwModeEnabled` behind getters over module-scope
    variables.
  - `@/stores/songs` — a `useSongStore` factory returning the minimum surface `SongsView.vue`
    reads: an empty `songs` array plus whatever computeds/actions the component dereferences during
    mount. Read the component and mock exactly what it touches; do not guess.
  - `vue-router` — stub `useRoute` (return an object with an empty `query`) and `useRouter` (return
    an object with `replace`/`push` spies).
  - Stub the heavy children through `global.stubs`: `AppShell`, `SongFilters`, `SongTable`,
    `SongSlideOver`, `BatchQuickAssign`, `PcImportModal`. Stubbing keeps the harness fast and
    scopes the assertion to SongsView's own template.

Export (or define at module scope) a helper `findImportSongsButton(wrapper)` that locates the PC
import trigger by filtering `wrapper.findAll('button')` on visible text containing `Import Songs`.
Wave 2 reuses this exact helper for its absent-case assertion, so the two waves cannot drift onto
different selectors. Deliberately do NOT add a `data-testid` to `SongsView.vue` for this — Wave 0
changes no application source, and a text-match helper shared by both waves is sufficient.

Assert only current behavior: with the default mock (`settings.pcEnabled` true), the import trigger
is found. Add the same Wave 0 head comment as Task 1.
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/SongsView.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/views/__tests__/SongsView.test.ts` exits 0 with at least 1 passing test.
    - Source assertion: `grep -c "findImportSongsButton" src/views/__tests__/SongsView.test.ts`
      returns at least 2 (definition plus one use).
    - Source assertion: `grep -c "pcEnabled" src/views/__tests__/SongsView.test.ts` returns at
      least 1.
    - `git status --porcelain src/views/SongsView.vue` is empty — no application code changed.
    - Both Wave 0 files run together clean:
      `npx vitest run src/views/__tests__/SettingsView.test.ts src/views/__tests__/SongsView.test.ts`
      exits 0.
  </acceptance_criteria>

  <done>
The file exists, passes standalone, shares the `settings`-shaped auth mock with Task 1, and exposes
the selector helper Wave 2 will reuse.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | This plan adds test files only. No runtime code path, no network call, no Firestore write, no new input surface. |

## STRIDE Threat Register

Stable IDs are shared across all Phase 39 plans. Rows below are the ones in scope for this plan.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-07 | Tampering | Wave 0 harness mocks (`vi.mock('@/stores/auth')` in both new files) | low | mitigate | A mock that diverges from the real store shape produces vacuously-passing tests in later waves. Mitigated by seeding the mock's `settings` object with exactly the three keys `DEFAULT_ORG_SETTINGS` will carry in 39-02, and by 39-06's phase gate running `npm run type-check` (the `vue-tsc --build` form, which typechecks test files) over the finished phase. |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed by this phase. `39-RESEARCH.md` § Package Legitimacy Audit records "Not applicable — this phase adds zero new npm/pip/cargo packages." No install task exists in any Phase 39 plan, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/SettingsView.test.ts src/views/__tests__/SongsView.test.ts`
  exits 0.
- `git status --porcelain src/` shows only the two new test files.
- The full suite is unchanged from its documented baseline:
  `npx vitest run --dir src --exclude '**/rules.test.ts'` fails in exactly the two known files
  (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`) and no others.
</verification>

<success_criteria>
Both files listed as MISSING in `39-VALIDATION.md` § Wave 0 Requirements now exist, pass, and carry
a `settings`-shaped auth-store mock. No application source file was modified.
</success_criteria>

<output>
Create `.planning/phases/39-org-settings-infrastructure-feature-toggles/39-01-SUMMARY.md` when done.
</output>
