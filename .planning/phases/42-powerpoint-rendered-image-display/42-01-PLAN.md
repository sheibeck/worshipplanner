---
phase: 42-powerpoint-rendered-image-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - src/rules.test.ts
  - .planning/PENDING-VERIFICATION.md
autonomous: true
requirements: [R080]

must_haves:
  truths:
    - "An org EDITOR's client-SDK write to organizations/{orgId}/pptxRenders/{importId} is DENIED by the emulator after this plan, and the same assertion was proven RED (the write SUCCEEDED) against the pre-change rules file before the fix landed."
    - "An org member holding only the 'viewer' role can READ organizations/{orgId}/pptxRenders/{importId} — the read grant is member-tier, not editor-tier."
    - "A signed-in editor of a DIFFERENT org, and an unauthenticated caller, are both DENIED reading organizations/{orgId}/pptxRenders/{importId}."
    - "The generic single-segment wildcard's write clause excludes 'services', 'slideGroups' AND 'pptxRenders', and its comment states the third exclusion is load-bearing in the same voice as the first two."
    - "The rules suite runs green in the Firestore emulator end-to-end — a suite that only exercises DENY cases is not accepted as evidence (CLAUDE.md)."
    - "Nothing in this plan runs `firebase deploy`; the rules change ships built, tested and undeployed, folded into Phase 41's ALREADY-PENDING single rules deploy rather than a second handoff."
  artifacts:
    - "firestore.rules — a dedicated `match /organizations/{orgId}/pptxRenders/{importId}` block granting read to org members, and `collection != 'pptxRenders'` appended to the generic wildcard's write clause"
    - "src/rules.test.ts — a `describe('pptxRenders — org-member read, no client write')` block with one ALLOW and at least three DENY cases"
    - ".planning/PENDING-VERIFICATION.md — Phase 41's existing deploy checkbox AMENDED (not duplicated) to state that the pptxRenders clauses ride in the same deploy"
  key_links:
    - "firestore.rules generic wildcard (lines 198-203) → OR-evaluation → any narrower pptxRenders block. The wildcard wins on write unless explicitly excluded; this is the entire mechanism T-42-01 exploits."
    - "src/rules.test.ts's seedDoc/seedMembershipDoc helpers (lines 32-51) → the new describe block. Reuse verbatim; do not build a second harness."
    - ".planning/PENDING-VERIFICATION.md Phase 41 entry (line 783 onward) → the one owner deploy that carries BOTH phases' rules changes."
  prohibitions:
    - "Never relax or widen a Firestore security rule in order to make a test pass."
    - "Never run a deploy command; every deployable artifact ships built, tested and undeployed with the exact command handed to the owner."
---

<objective>
Close the live `pptxRenders` write hole and make render-status readability an intentional,
member-tier grant instead of an accident of wildcard fallthrough — proving both against the real
Firestore emulator, with the write-hole assertion demonstrated RED before the fix.

Implements D-01, D-02, D-03, D-17, D-18 (planner-assigned IDs for 42-CONTEXT.md's unnumbered
decisions — see the Source Audit below).

Purpose: every other plan in this phase reads `organizations/{orgId}/pptxRenders/{importId}` from a
browser client. That read works today only by accident, and the same accident grants an org editor
WRITE access — letting a client fake a `ready` flip, which `functions/src/index.ts:342` names as the
one thing the render service "must never be able to produce" (T-37-15). This plan is sequenced first
because the phase's core factual premise is contested by the codebase's own comments and must be
settled by an executed emulator assertion, not a reading.

Output: a built, tested, UNDEPLOYED `firestore.rules`; a new ALLOW/DENY suite; an amended Phase 41
deploy handoff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42-powerpoint-rendered-image-display/42-CONTEXT.md
@.planning/phases/42-powerpoint-rendered-image-display/42-RESEARCH.md
@.planning/phases/42-powerpoint-rendered-image-display/42-VALIDATION.md
@CLAUDE.md
</context>

## Source Audit — Phase 42 plan set (canonical; other plans reference this section)

### Planner-assigned decision IDs

42-CONTEXT.md predates the `D-NN` convention and states its decisions as prose bullets under section
headings. The IDs below were assigned by the planner so every locked decision is individually
traceable. Each row quotes enough of the source bullet to be unambiguous.

| ID | 42-CONTEXT.md decision | Plan |
|----|------------------------|------|
| D-01 | Add `&& collection != 'pptxRenders'` to the generic wildcard's **write** clause (SUPERSEDED correction block) | 42-01 |
| D-02 | Add `match /organizations/{orgId}/pptxRenders/{importId} { allow read: if isOrgMember(orgId); }` — **read only**, no create/update/delete | 42-01 |
| D-03 | Prove the rule with emulator-backed **ALLOW and DENY** cases in `src/rules.test.ts` | 42-01 |
| D-04 | Subscribe with `onSnapshot`, not a one-shot `getDoc` | 42-02, 42-08 |
| D-05 | When `renderedCount` disagrees with `deck.slides.length`, **`renderedCount` wins** | 42-03, 42-04, 42-05 |
| D-06 | Surplus **parsed** slides are retained in the document for search/labels but **never drawn** | 42-03, 42-05 |
| D-07 | Surplus **rendered** pages beyond the parsed count render as slides with no text label, not dropped | 42-03, 42-05 |
| D-08 | The reconciliation lives in **one shared helper** consumed by both `slideGroupMaterializer.ts` and `slideshowAssembler.ts` | 42-03, 42-04, 42-05 |
| D-09 | Fold **both** `status` and `renderedCount` into the `IMPORTED` branch of `sourceSignature` | 42-04 |
| D-10 | "Exactly once" falls out of the signature being stable within a status — no separate one-shot flag | 42-04, 42-08 |
| D-11 | **User-added slides survive a rebuild** — Phase 24 D-02 still governs | 42-04 |
| D-12 | A `failed → ready` transition is treated identically to `pending → ready` — no special case | 42-04, 42-08 |
| D-13 | Grid, pending: an explicit placeholder naming the state — never a blank tile, never stale parsed text | 42-06 |
| D-14 | Grid, failed: an explicit failure state surfacing `failureReason` | 42-06 |
| D-15 | Presenter: same explicit states; never silently show nothing; never skip the slide | 42-07 |
| D-16 | Decks with no `renderImportId` keep the existing parsed-text path unchanged — not a failure state | 42-03, 42-05 |
| D-17 | **Storage needs no change** — `orgs/{orgId}/{allPaths=**}` already covers rendered pages | 42-01 |
| D-18 | **Amend** Phase 41's `.planning/PENDING-VERIFICATION.md` entry rather than adding a second handoff | 42-01 |
| D-19 | *(Claude's discretion)* naming and file placement of the shared reconciliation helper | 42-03 |
| D-20 | *(Claude's discretion)* where the `pptxRenders` subscription lives — store vs. composable | 42-02, 42-08 |
| D-21 | *(Claude's discretion)* exact visual treatment of pending/failed, subject to 42-UI-SPEC.md | 42-06, 42-07 |

**Deferred Ideas — MUST NOT appear in any plan:** a re-render/retry affordance for a failed deck;
opening `pptxRenders` to client writes; exposing render progress as a percentage; closing backlog
999.3 (this phase resolves the *read* half incidentally; the backlog item stays open).

### Coverage table

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Criterion 1 — rendered slide images shown in the slide grid AND the presenter, not the parsed-text fallback | 42-03, 42-05, 42-06, 42-07, 42-08 |
| GOAL | Criterion 2 — pending/failed shows an explicit state, never blank/broken/misleadingly-stale | 42-03, 42-06, 42-07 |
| GOAL | Criterion 3 — new `IMPORTED`-branch logic in BOTH engines reconciles render count vs parsed count, proven by a count-disagreement test | 42-03, 42-04, 42-05 |
| GOAL | Criterion 4 — `sourceSignature` folds in render status; rebuild fires exactly once on `pending → ready` | 42-04, 42-08 |
| REQ | R079 | 42-02, 42-03, 42-04, 42-05, 42-08 |
| REQ | R080 | 42-01, 42-02, 42-03, 42-06, 42-07 |
| RESEARCH | Pitfall 1 — no positional pairing between `deck.slides[i]` and rendered page `i+1` | 42-03 |
| RESEARCH | Pitfall 2 — the generic-wildcard write hole (★ correction) | 42-01 |
| RESEARCH | Pitfall 3 — gate on `status === 'ready'`, never on `renderedCount` truthiness | 42-03 |
| RESEARCH | Pitfall 4 — unbounded `getDownloadURL()` calls on every reactive recompute | 42-08 |
| RESEARCH | Pitfall 5 — leaked `onSnapshot` listeners across service navigations | 42-02, 42-08 |
| RESEARCH | Pattern 1 — deterministic rendered-page Storage path, 1-based, 4-digit pad | 42-02 |
| RESEARCH | Pattern 2 — async URL resolution owned by the composable, cached by `${importId}:${renderedCount}` | 42-08 |
| RESEARCH | Pattern 3 — the one shared reconciliation helper and its decision table | 42-03 |
| RESEARCH | Assumption A1 — `rendered-page-N` identity stability across rebuilds | 42-04 (explicit idempotence test) |
| RESEARCH | Assumption A2 — per-`importId` listeners vs one batched `in` query | 42-02 (default chosen, rationale recorded) |
| RESEARCH | Assumption A3 — optional render-state fields vs a new `SlideContentKind` | 42-03 (confirmed as a planner decision) |
| RESEARCH | Open Question 2 — ready-state slides all take the generic `'IMAGE'` eyebrow | 42-03, 42-06 |
| RESEARCH | Open Question 3 — no new Firestore composite index needed | 42-02 (recorded, no work) |
| RESEARCH | Package Legitimacy Audit — "Not applicable, zero new npm packages" | this plan set installs nothing (T-42-SC) |
| RESEARCH | Wave 0 gap — the rules probe | 42-01 Task 1 |
| RESEARCH | Wave 0 gap — `vi.mock('@/stores/pptxRenders')` in `useSlideshowAssembly.test.ts` | 42-02 Task 3 |
| RESEARCH | Wave 0 gap — whether component suites need a `getDownloadURL` mock | 42-02 Task 3 |
| CONTEXT | D-01 … D-21 | see the decision table above — all 21 assigned |

No item is MISSING. No item was deferred by the planner.

### Canon-referral breadcrumbs (prohibition probe, Stage 2)

Two recalled prohibitions were dropped as canon-referrals rather than authored into
`must_haves.prohibitions`: cross-tenant read/write authorization (OWASP A01 / ASVS V4) is handled
structurally by this phase's `<threat_model>` (T-42-01/02/03) and does not need restating as a
prohibition; secret handling and generic input validation (ASVS V5, CLAUDE.md's `.env.local` rule)
introduce no new surface this phase — it reads only.

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Note |
|---|---|---|
| `firestore.rules` → `match /organizations/{orgId}/pptxRenders/{importId}` | new rules block | `allow read: if isOrgMember(orgId);` — read only |
| `firestore.rules` → `collection != 'pptxRenders'` | new clause on the existing generic wildcard's `allow write` | third load-bearing exclusion |
| `src/rules.test.ts` → `describe('pptxRenders — org-member read, no client write')` | new test block | 1 ALLOW + 3 DENY |
| `.planning/PENDING-VERIFICATION.md` → amended Phase 41 deploy checkbox | doc edit | no second handoff |

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — prove the pptxRenders write hole exists, in the emulator, before changing anything</name>
  <files>src/rules.test.ts</files>
  <read_first>
    - `src/rules.test.ts` lines 1-51 — the `initializeTestEnvironment` setup and the `seedMembershipDoc` / `seedDoc` helpers to reuse verbatim
    - `src/rules.test.ts` lines 780-949 — the `serviceShareLinks` describe block, the freshest ALLOW/DENY shape in the file (Phase 41)
    - `firestore.rules` lines 173-203 — the generic single-segment wildcard and its "LOAD-BEARING" comment
    - `functions/src/index.ts` lines 138-165 — `PptxRenderDoc`'s real field shape and `pptxRenderDocRef`'s canonical path
    - `CLAUDE.md` § Testing — the rules-suite command and the "a test explained away as an environment quirk is an untested assertion" rule
    - `.planning/phases/42-powerpoint-rendered-image-display/42-VALIDATION.md` § Wave 0 Requirements
  </read_first>
  <action>
    Add a new `describe('pptxRenders — org-member read, no client write')` block at the end of
    `src/rules.test.ts`, reusing the existing `seedDoc` and `seedMembershipDoc` helpers verbatim — do
    not build a second harness.

    Write ONE probe case first, and write it to assert the CURRENT (pre-fix) behaviour so it passes
    against the unmodified rules file: seed `organizations/orgA/pptxRenders/import-1` with
    `{ status: 'pending', storagePath: 'orgs/orgA/pptx-imports/import-1/source.pptx' }`, seed
    `orgA/userA` with role `editor`, then have `testEnv.authenticatedContext('userA')` attempt
    `updateDoc` of `{ status: 'ready', renderedCount: 99 }` on that document and wrap it in
    `assertSucceeds`. Name the case so it reads as a probe, e.g.
    `'PROBE (pre-fix) — an org editor CAN currently write a pptxRenders doc via the generic wildcard'`.

    Run the rules suite and record the exact pass/fail output in the plan SUMMARY. This is the
    phase's contested factual premise (D-01): `functions/src/index.ts:144-148` asserts the opposite,
    and the first draft of 42-CONTEXT.md asserted the opposite. A reading of the rules file is not
    evidence.

    **STOP CONDITION — this is a real branch, not a formality.** If the probe FAILS (the write is
    already denied), do not proceed to Task 2. Record the finding in the SUMMARY, state that D-01's
    premise is false, and report `## PREMISE FALSIFIED` so the phase can be re-cut: the write-side
    half of the rules task evaporates and T-42-01 drops out of the threat model.

    Per CLAUDE.md, run the rules suite with `npm run test:rules`; if that reports a port conflict
    because an emulator is already running, fall back to
    `npx vitest run --config vitest.rules.config.ts` against the running one.

    Commit this probe on its own so the RED→GREEN transition in Task 2 is visible in history.
  </action>
  <verify>
    <automated>npm run test:rules</automated>
  </verify>
  <acceptance_criteria>
    - `src/rules.test.ts` contains a case whose name contains the substring `PROBE (pre-fix)` and whose body calls `assertSucceeds` on an `updateDoc` against `organizations/orgA/pptxRenders/import-1` from an editor-role member of `orgA`.
    - The rules suite executes against a live Firestore emulator and that probe case PASSES against the unmodified `firestore.rules`, and the pass is quoted verbatim in the plan SUMMARY.
    - `git diff --stat` for this commit shows `src/rules.test.ts` as the only changed file — `firestore.rules` is untouched at this point.
    - If the probe instead fails, no `firestore.rules` edit is made and the run reports `## PREMISE FALSIFIED`.
  </acceptance_criteria>
  <done>An executed emulator assertion — not a code reading — establishes whether an org editor can write a `pptxRenders` document today, and the result is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Close the write hole, grant member-tier read, and flip the suite to ALLOW + DENY</name>
  <files>firestore.rules, src/rules.test.ts</files>
  <read_first>
    - `firestore.rules` lines 1-30 — `isSignedIn` / `isOrgMember` / `isOrgEditor` helper definitions
    - `firestore.rules` lines 155-204 — the `songs`/`lyrics` nested-block precedent for where a dedicated block sits inside `match /organizations/{orgId}`, and the generic wildcard immediately after it
    - `firestore.rules` lines 316-320 — the catch-all deny, to confirm the new block sits well above it
    - `src/rules.test.ts` — the probe block added in Task 1
    - `.planning/phases/42-powerpoint-rendered-image-display/42-PATTERNS.md` § `firestore.rules` — the exact two-edit shape and comment convention to replicate
  </read_first>
  <action>
    Make exactly two edits to `firestore.rules`, both inside the existing `match /organizations/{orgId}`
    nesting, and nothing else.

    **Edit 1 (D-02).** Add a dedicated block alongside the `songs`/`services`/`slideGroups` blocks,
    BEFORE the generic wildcard:
    `match /pptxRenders/{importId} { allow read: if isOrgMember(orgId); }`.
    Grant read only. Do not add `create`, `update`, `delete` or a bare `write` — nothing client-side
    writes this document and opening it is threat T-42-01. Member tier (not editor) is deliberate: a
    viewer already sees the deck's parsed content and structure, so render status carries no
    additional sensitivity.

    **Edit 2 (D-01).** Append `&& collection != 'pptxRenders'` to the generic wildcard's `allow write`
    clause at lines 198-203, so it reads `collection != 'services' && collection != 'slideGroups' &&
    collection != 'pptxRenders'`. Extend the block's existing "★ … are all LOAD-BEARING. Do not
    remove either." comment to name the third exclusion in the same voice, and state why: Firestore
    rules are OR-evaluated, so without this exclusion the read-only intent of Edit 1 is a complete
    no-op for writes and an org editor can forge a `ready` flip (T-42-01, inheriting T-37-15).

    Per D-17 make NO change to `storage.rules` — rendered pages already fall under
    `match /orgs/{orgId}/{allPaths=**} { allow read: if isOrgMember(orgId); }`.

    Then rewrite the test block: flip the Task 1 probe from `assertSucceeds` to `assertFails`, rename
    it to drop the `PROBE (pre-fix)` marker and name the guarantee instead (an org editor cannot
    write a render document), and add these cases:
    - ALLOW — a **viewer**-role member of `orgA` reads `organizations/orgA/pptxRenders/import-1`. The
      viewer role is the point: it proves the grant is member-tier, not editor-tier (D-02).
    - DENY — an editor of `orgB` (with a seeded `orgB` membership and no `orgA` membership) reads
      the same `orgA` document (T-42-03).
    - DENY — `testEnv.unauthenticatedContext()` reads the same document.
    - DENY — a viewer-role member of `orgA` attempts `setDoc` creating a NEW
      `organizations/orgA/pptxRenders/import-2`, confirming the block grants no create path.

    Run the whole rules suite; it must be green end-to-end. A deny-only suite is not evidence
    (CLAUDE.md) — the ALLOW case must be observed passing.
  </action>
  <verify>
    <automated>npm run test:rules</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' firestore.rules | grep -c "collection != 'pptxRenders'"` returns 1.
    - `grep -v '^\s*//' firestore.rules | grep -c 'match /pptxRenders/{importId}'` returns 1, and the lines within that block declare `allow read` and no `allow write`/`create`/`update`/`delete`.
    - `git diff firestore.rules` shows changes confined to the `match /organizations/{orgId}` nesting; `storage.rules` is not in the diff at all.
    - The rules suite runs against a live emulator and reports 0 failures, including the ALLOW case (viewer-role member reads) observed passing, and the previously-succeeding editor-write case now asserted with `assertFails`.
    - The SUMMARY records the before/after case counts for `src/rules.test.ts` in the same style `41-01-SUMMARY.md` used.
  </acceptance_criteria>
  <done>An org editor can no longer write a render document; an org viewer can read one; a foreign-org editor and an anonymous caller can read neither — all four proven in the emulator.</done>
</task>

<task type="auto">
  <name>Task 3: Amend Phase 41's pending deploy handoff — do not add a second one</name>
  <files>.planning/PENDING-VERIFICATION.md</files>
  <read_first>
    - `.planning/PENDING-VERIFICATION.md` lines 783-818 — Phase 41's "Sharing Correctness" entry, its `41-REVIEW-FIX` update block, and the single `Deploy the updated firestore.rules` checkbox
    - `.planning/STATE.md` § ★★ STANDING AUTONOMY GRANT — "NO DEPLOYS. `firebase deploy` and `gcloud run deploy` remain the owner's step"
    - `.planning/phases/42-powerpoint-rendered-image-display/42-CONTEXT.md` § SUPERSEDED correction — "Amend Phase 41's entry … rather than adding a second handoff"
  </read_first>
  <action>
    Edit the EXISTING Phase 41 deploy checkbox in `.planning/PENDING-VERIFICATION.md` in place
    (D-18). Do not create a `## Phase 42` deploy section and do not add a second
    `firebase deploy --only firestore:rules` checkbox — one deploy carries both phases' clauses, and
    a duplicate handoff invites the owner to run it twice or to run one and believe both are covered.

    Add a clearly-marked update block under the Phase 41 heading, in the same voice as the existing
    `41-REVIEW-FIX` update block, stating: (a) Phase 42 added two more clauses to the same file — the
    `pptxRenders` member-read block and the generic-wildcard `pptxRenders` write exclusion; (b) the
    write exclusion closes a hole that is **live in production right now**, so until this deploy runs
    an org editor can forge a render `ready` flip in their own org; (c) the verification the owner can
    perform after deploying — confirm `firestore.rules` in the console shows both the
    `match /pptxRenders/{importId}` read block and `collection != 'pptxRenders'` on the wildcard's
    write clause.

    Extend the existing checkbox's own text so a reader who only skims the checkboxes still learns
    that Phase 42's clauses ride along. Leave Phase 41's load-bearing ordering constraint (rules
    before hosting) intact and unedited.

    Run this plan's own commit without any deploy command. Per the standing autonomy grant this is a
    deferred, disclosed gate — never recorded as passed.
  </action>
  <verify>
    <automated>grep -c 'firebase deploy --only firestore:rules' .planning/PENDING-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'firebase deploy --only firestore:rules' .planning/PENDING-VERIFICATION.md` returns exactly the same number it returned before this task (no new deploy checkbox was introduced) — capture the pre-edit count first and assert equality in the SUMMARY.
    - `grep -c 'pptxRenders' .planning/PENDING-VERIFICATION.md` returns a value greater than 0, and every occurrence is inside the Phase 41 entry, not a new Phase 42 section.
    - `git log --oneline -n 20` for this phase contains no commit whose diff invokes `firebase deploy` or `gcloud run deploy`; `git diff` for this plan touches only `firestore.rules`, `src/rules.test.ts` and `.planning/PENDING-VERIFICATION.md`.
    - The amended text names the production exposure explicitly (an org editor can forge a `ready` flip until the deploy runs).
  </acceptance_criteria>
  <done>The owner has exactly one rules deploy to run, and its entry tells them what Phase 42 added and how to verify it landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → Firestore | An authenticated org member's client SDK reaches `organizations/{orgId}/pptxRenders/{importId}` directly; only `firestore.rules` stands between them and the document |
| render service (Admin SDK) → Firestore | The Cloud Function/Cloud Run pair writes render outcomes bypassing rules entirely — its writes are the trusted ones this phase must keep exclusive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | `firestore.rules:198-203` generic wildcard `allow write` | high | mitigate | **Pre-existing and LIVE in production.** An org editor can `setDoc`/`updateDoc` `organizations/{orgId}/pptxRenders/{importId}` today, forging `status: 'ready'` and a `renderedCount` — exactly threat T-37-15, which `functions/src/index.ts:342` says the render service "must never be able to produce." Task 2 appends `collection != 'pptxRenders'` to the write exclusion. Task 1 proves the hole with an emulator write that SUCCEEDS pre-fix; Task 2 flips the same assertion to `assertFails`. Ships undeployed — see T-42-08. |
| T-42-02 | Information Disclosure | `organizations/{orgId}/pptxRenders/{importId}` read grant | low | accept | Render metadata (`status`, `renderedCount`, `failureReason`) becomes readable by any org member including viewers. Accepted: a viewer already reads the deck's full parsed content and structure through `importedSlides`/`slideGroups`, so render status carries strictly less sensitivity. Made intentional rather than accidental by Task 2's explicit `allow read: if isOrgMember(orgId)` block. |
| T-42-03 | Information Disclosure | cross-org read of `organizations/{orgId}/pptxRenders/{importId}` | medium | mitigate | A signed-in editor of another org, or an anonymous caller, guessing an `importId`. Mitigated by `isOrgMember(orgId)` evaluated against the path's own `orgId` segment; proven by two DENY cases in Task 2. Defense in depth: `importId` is a `crypto.randomUUID()` (`pptxUpload.ts::generateImportId`), so the id is not enumerable. |
| T-42-08 | Elevation of Privilege | the deploy step itself | medium | transfer | The fix is inert until `firebase deploy --only firestore:rules` runs, which is the owner's step under the v1.5 standing autonomy grant. Transferred to the owner via the AMENDED Phase 41 handoff (Task 3), which states the production exposure in plain terms so the deploy is not deprioritised as cosmetic. |
| T-42-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. `42-RESEARCH.md § Package Legitimacy Audit` records zero new dependencies — every primitive is already a project dependency. Nothing to gate. |
</threat_model>

<verification>
- `npm run test:rules` green against a live Firestore emulator, ALLOW case observed passing.
- The Task 1 probe pass (pre-fix) and the Task 2 flip (post-fix) both quoted in the SUMMARY.
- `npx vitest run` unaffected — `src/rules.test.ts` is excluded from the app suite by `vite.config.ts`,
  so a clean app run proves nothing here and must not be cited as evidence for this plan.
- Known-failing app-suite baseline is 3 files (`src/storage.rules.test.ts`,
  `src/views/__tests__/RosterView.test.ts`, `render-service/src/render.test.ts`) — do not chase them.
- `git log` shows no deploy command was run.
</verification>

<success_criteria>
- An org editor's write to a `pptxRenders` document is denied, with the pre-fix success recorded as the regression proof.
- An org viewer can read a `pptxRenders` document; a foreign-org editor and an anonymous caller cannot.
- `firestore.rules` carries a third load-bearing exclusion documented in the same voice as the first two.
- `storage.rules` is untouched.
- Exactly one owner deploy remains outstanding, and its handoff names Phase 42's clauses.
</success_criteria>

<output>
Create `.planning/phases/42-powerpoint-rendered-image-display/42-01-SUMMARY.md` when done.
</output>
