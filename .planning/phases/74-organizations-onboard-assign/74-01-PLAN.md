---
phase: 74-organizations-onboard-assign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/orgTemplateSeed.ts
  - functions/src/orgTemplateSeed.test.ts
  - functions/src/orgProvisioning.ts
  - functions/src/orgProvisioning.test.ts
  - functions/src/index.ts
autonomous: true
requirements: [R196, R197, R198, R199, R200, R201, R202, R203, R204, R205, R206]
user_setup: []

must_haves:
  truths:
    - "Every callable rejects an unauthenticated caller, a caller whose token lacks superAdmin, and a caller with no superAdmins/{callerUid} doc (R200/R204)"
    - "onboardOrganization creates organizations/{orgId} with {name, createdAt, createdBy} + deep-merged OrgSettings whose defaultServiceTemplate is the 9-entry seeded template, then assigns the first editor member — in one flow (R197/R198/R199)"
    - "A duplicate normalizeOrgName collision throws already-exists and creates no second org (R201)"
    - "Assigning an org to a user who already has orgIds:['orgA'] yields orgIds:['orgA','orgB'] via FieldValue.arrayUnion — never an overwrite (R206)"
    - "assignOrgAdmin for an unknown email writes invites/{email} + inviteLookup/{email} and returns {status:'invited'}, never a members write, never a silent failure (R205)"
    - "assignOrgAdmin for an existing account writes members/{uid} at editor and returns {status:'added'} (R203)"
    - "listOrganizations returns [{orgId,name,createdAt,memberCount}] with server-computed member counts (R196)"
  artifacts:
    - functions/src/orgProvisioning.ts
    - functions/src/orgProvisioning.test.ts
    - functions/src/orgTemplateSeed.ts
    - functions/src/orgTemplateSeed.test.ts
  key_links:
    - "functions/src/index.ts re-exports onboardOrganization, assignOrgAdmin, listOrganizations (deployed names)"
    - "assignAdminCore is the SINGLE shared helper called by BOTH onboardOrganization and assignOrgAdmin (no copy-pasted overwrite path)"
    - "the members/{uid} write is what fires syncOrgMembershipClaim (orgMembershipClaims.ts) — the callable never writes custom claims itself"
---

<objective>
Build the three super-admin-gated org-provisioning callables in a new `functions/src/orgProvisioning.ts`
— `onboardOrganization`, `assignOrgAdmin`, `listOrganizations` — plus a data-only ported seed module
(`orgTemplateSeed.ts`) and comprehensive mocked-Admin-SDK unit tests. This is the entire server surface
for Phase 74; the Wave 2 UI (Plan 02) consumes the callable contracts this plan fixes.

Purpose: Give super-admins a rules-bypassing, server-verified path to list every org, onboard a church
end-to-end (org + settings + seeded template + first admin), and assign additional editor admins by email
— with the R206 additive `arrayUnion` guarantee and the R201/R202 uniqueness-and-no-half-created-org
invariant enforced where the client can never bypass them.

Output: `functions/src/orgTemplateSeed.ts` (+ test), `functions/src/orgProvisioning.ts` (+ test), and the
three-line export wiring in `functions/src/index.ts`. Callables ship built + tested + UNDEPLOYED (deploy is
handed over — see success criteria).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/74-organizations-onboard-assign/74-CONTEXT.md
@.planning/phases/74-organizations-onboard-assign/74-RESEARCH.md
@.planning/phases/74-organizations-onboard-assign/74-PATTERNS.md
@.planning/phases/74-organizations-onboard-assign/74-VALIDATION.md

# The exact analog to mirror (caller gate, getUserByEmail, testable-handler/onCall split):
@functions/src/superAdminClaims.ts
@functions/src/superAdminClaims.test.ts
# The get-then-write Firestore transaction precedent (checkAndConsumeRateLimit, index.ts:343-373)
# and the export-wiring block (index.ts:3300-3324):
@functions/src/index.ts
# The claim-sync trigger the members write fires (this callable must NOT touch claims):
@functions/src/orgMembershipClaims.ts
# The client-util-port precedent (why functions duplicates, not imports, pure src/ helpers):
@functions/src/serviceRoles.ts
# Canonical org/member/invite/inviteLookup/orgIds write shapes (and the OVERWRITE bug NOT to copy):
@src/stores/auth.ts
# normalizeOrgName (port byte-for-byte) + claimOrgName's create-only semantics to replicate:
@src/utils/orgName.ts
# OrgSettings + DEFAULT_ORG_SETTINGS + storage location (organizations/{orgId}.settings, nested):
@src/types/organization.ts
# buildSuggestedTemplateEntries()/buildSlots('1-2-2-3') — the 9-entry seed to port; ServiceSection union:
@src/utils/slotTypes.ts
@src/types/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the suggested-template + default-settings seed into functions (R197/R198)</name>
  <files>functions/src/orgTemplateSeed.ts, functions/src/orgTemplateSeed.test.ts</files>
  <action>
    Create `functions/src/orgTemplateSeed.ts` following the `serviceRoles.ts` client-util-port precedent
    exactly: a top-of-file docblock explaining that `functions/` is a standalone TS project (its own
    tsconfig, include ["src"], no `@/` alias — it cannot import from the client `src/` tree), so this file
    is a DUPLICATE of the pure client helpers, kept in lockstep to avoid seeding a new org's template
    differently from a normally-created org's.

    Export `buildSuggestedTemplateEntries(): PortedTemplateEntry[]` — a DATA-ONLY port of
    src/utils/slotTypes.ts's `buildSuggestedTemplateEntries()`. There is NO VW-typing/`buildSlots` logic to
    port: `buildSlots('1-2-2-3')` reduces to a FIXED 9-entry {kind, section} sequence. Encode the sequence
    as a static table in this exact order — SONG/worship, SCRIPTURE/worship, SONG/worship, PRAYER/worship,
    SCRIPTURE/worship, SONG/worship, SONG/worship, MESSAGE/message, SONG/sending — and map each row to
    `{ id: randomUUID(), kind, section }`. Use `randomUUID` imported from `node:crypto` (not a global
    `crypto`, to avoid runtime-global ambiguity). Declare a minimal `PortedTemplateEntry` type mirroring
    src/types/organization.ts's `ServiceTemplateEntry` fields the port touches (`id`, `kind`, optional
    `section`); type `kind` against the SlotKind union from src/types/service.ts (`'SONG' | 'SCRIPTURE' |
    'PRAYER' | 'MESSAGE' | 'ANNOUNCEMENTS' | 'MISC' | 'HYMN' | 'IMPORTED'`) and `section` against the
    ServiceSection union (`'pre-service' | 'worship' | 'message' | 'sending' | 'post-service'`) — re-read
    src/types/service.ts to confirm both unions before finalizing (RESEARCH Assumption A3).

    Also export `buildDefaultOrgSettings(): PortedOrgSettings` — a data-only port of
    src/types/organization.ts's `DEFAULT_ORG_SETTINGS` object literal (aiEnabled:true, pcEnabled:true,
    vwModeEnabled:true, bibleVersion:'NLT', slideTypography {fontFamily:'Inter', fontWeight:400,
    fontScale:'md'}, messaging {enabled:false, lockNotifyDefault:false, reminderEnabled:false,
    reminderDaysBefore:7}, timezone:'America/Chicago') with `defaultServiceTemplate` set to a fresh
    `buildSuggestedTemplateEntries()`. Keep it PURE — no Firestore/Auth access in this module (the
    onboard handler in Task 2 writes the returned object).

    Create `functions/src/orgTemplateSeed.test.ts` mirroring the repo's plain-vitest unit style. Pin the
    template byte-identical to the client's shape: assert `buildSuggestedTemplateEntries().map(e => ({
    kind: e.kind, section: e.section }))` deep-equals the known 9-row {kind, section} table above (order
    included); assert length is 9; assert every `id` is a distinct non-empty string (ids are fresh UUIDs on
    both sides and are NOT compared cross-module). Assert `buildDefaultOrgSettings()` matches the ported
    default literal and that its `defaultServiceTemplate` is that same 9-entry sequence.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/orgTemplateSeed.test.ts</automated>
  </verify>
  <done>orgTemplateSeed.ts exports the pure ported buildSuggestedTemplateEntries()/buildDefaultOrgSettings(); the pin test proves the 9-entry {kind,section} sequence and the default settings shape, and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the three super-admin-gated callables + shared assignAdminCore + export wiring (R196-R206)</name>
  <files>functions/src/orgProvisioning.ts, functions/src/index.ts</files>
  <action>
    Create `functions/src/orgProvisioning.ts` mirroring `superAdminClaims.ts`'s structure verbatim: import
    `onCall, HttpsError, type CallableRequest` from firebase-functions/v2/https, `getAuth` from
    firebase-admin/auth, `getFirestore, FieldValue` from firebase-admin/firestore; do NOT call
    initializeApp() at module scope (carry the same explanatory comment superAdminClaims.ts:9-13 uses).
    Import the pure ports from ./orgTemplateSeed. Port `normalizeOrgName` byte-for-byte from
    src/utils/orgName.ts into this module (or a small shared helper) — it is a pure function with no
    Firestore imports; do not reimplement its slug-fallback differently.

    Use the testable-handler-separated-from-onCall-wrapper split for all three: export
    `onboardOrganizationHandler`/`assignOrgAdminHandler`/`listOrganizationsHandler` functions, then bind
    `export const onboardOrganization = onCall(onboardOrganizationHandler)` etc.

    SHARED CALLER GATE (apply verbatim in all three handlers, per R200/R204 and PATTERNS §Shared): reject
    `!request.auth` with HttpsError('unauthenticated', 'Sign in required.'); reject
    `request.auth.token.superAdmin !== true` with HttpsError('permission-denied', 'You must be a
    super-admin.'); then independently re-read getFirestore().collection('superAdmins').doc(request.auth.uid)
    and reject with permission-denied if it does not exist. Factor this into one internal
    `assertSuperAdminCaller(request)` returning the caller uid, so the dual re-verification never drifts
    between handlers.

    SHARED assignAdminCore(db, orgId, email, callerUid) — the SINGLE place any members/invite write happens
    (R199/R203/R205/R206; RESEARCH Pattern 2). Normalize email (trim + toLowerCase). Resolve the target via
    getAuth().getUserByEmail(normalizedEmail). In the catch, discriminate the error code: only
    `(err as {code?:string})?.code === 'auth/user-not-found'` takes the invite branch; RETHROW any other
    code (do NOT blanket-catch like setSuperAdminClaimHandler — RESEARCH Pitfall 5, T-74-05). No-account
    branch: batch-write organizations/{orgId}/invites/{normalizedEmail} = {role:'editor', invitedAt:
    serverTimestamp(), invitedBy: callerUid} AND inviteLookup/{normalizedEmail} = {orgId, role:'editor'};
    return {status:'invited'}. Existing-account branch: batch-write organizations/{orgId}/members/{uid} =
    {role:'editor', joinedAt: serverTimestamp(), displayName: targetUser.displayName ?? '', email:
    normalizedEmail} AND update users/{uid} with orgIds set to FieldValue.arrayUnion(orgId) — MUST be
    arrayUnion, never a literal-array set/overwrite (R206 / T-74-03; src/stores/auth.ts:426,455 do the wrong
    thing — do not copy it). Return {status:'added', uid}. The callable never writes custom claims — the
    members write fires syncOrgMembershipClaim automatically.

    onboardOrganizationHandler({name, adminEmail}): assert caller. Validate `name` is a non-empty string and
    `adminEmail` is a non-empty string (HttpsError('invalid-argument', ...) on wrong type — mirror
    superAdminClaims.ts:131-145 strict-typing discipline). Compute nameKey = normalizeOrgName(name). Mint the
    org id BEFORE the transaction via db.collection('organizations').doc() (client-side id, no round trip).
    Run ONE Firestore transaction (RESEARCH Open Question 1 — prefer the single-transaction design over
    CONTEXT's literal transaction-then-batch split, to remove Pitfall 1's crash window; note this choice in a
    code comment): tx.get(orgNames/{nameKey}); if it exists and its orgId !== our minted orgId → throw
    HttpsError('already-exists', 'That church name is taken.') (idempotent when it equals our orgId); else
    tx.set(orgNames/{nameKey}, {orgId}) AND tx.set(organizations/{orgId}, {name, createdAt:
    serverTimestamp(), createdBy: callerUid, settings: buildDefaultOrgSettings()}). All tx.get calls precede
    all tx writes (hard Firestore constraint). AFTER the transaction commits, call assignAdminCore(db,
    orgId, adminEmail, callerUid) (getUserByEmail is a network call — it must stay OUTSIDE the transaction
    body). Return {status, orgId, name} where status is the assignAdminCore outcome's 'added'|'invited'.

    assignOrgAdminHandler({orgId, email}): assert caller. Validate orgId and email are non-empty strings.
    Guard against an orphaned membership under a typo'd id (T-74-06 / RESEARCH V5): read
    organizations/{orgId} and throw HttpsError('not-found', ...) if it does not exist BEFORE calling
    assignAdminCore. Then return assignAdminCore(db, orgId, email, callerUid). (onboard skips this existence
    check — it just created the org.)

    listOrganizationsHandler(): assert caller. Read all organizations docs; for each, compute member count
    server-side via the members subcollection `.count().get()` aggregate (RESEARCH Pattern 3), gathered with
    Promise.all. Return {organizations: [{orgId, name, createdAt, memberCount}]}. If `.count()` proves
    unavailable in the test mock, the fallback is `.get()` then `.size` — functionally identical.

    Wire exports in functions/src/index.ts: add `import { onboardOrganization, assignOrgAdmin,
    listOrganizations } from "./orgProvisioning";` near the other function imports (index.ts:17-18) and add
    `export { onboardOrganization, assignOrgAdmin, listOrganizations };` in the export block
    (index.ts:3311-3324), with a short comment matching the existing block style. Export only the deployed
    onCall wrappers — the handlers stay importable only for tests.
  </action>
  <verify>
    <automated>cd functions && npm run build</automated>
  </verify>
  <done>orgProvisioning.ts compiles clean under `tsc`; the three onCall callables are exported from index.ts; assignAdminCore is the single shared helper used by both onboard and assign; onboard uses one transaction (name + org + settings) with the assignment after it.</done>
</task>

<task type="auto">
  <name>Task 3: Comprehensive mocked-Admin-SDK unit tests for the callables (Nyquist Wave 0)</name>
  <files>functions/src/orgProvisioning.test.ts</files>
  <action>
    Create `functions/src/orgProvisioning.test.ts` mirroring superAdminClaims.test.ts's harness: vi.mock
    'firebase-admin/auth' (getAuth → {getUserByEmail}), vi.mock 'firebase-admin/firestore' (getFirestore,
    FieldValue with serverTimestamp → 'SERVER_TIMESTAMP_SENTINEL' AND arrayUnion → a tagged sentinel like
    `(v) => ({ __arrayUnion: v })`). Build a fake Firestore whose collection('superAdmins').doc(callerUid)
    .get() returns {exists} to drive the caller gate, plus per-collection doc spies for organizations /
    members / invites / inviteLookup / users, and a runTransaction seam `runTransaction: vi.fn((fn) =>
    fn(txMock))` where txMock.get/set are spies (no existing functions test mocks runTransaction — invent it
    here; check index.test.ts for any checkAndConsumeRateLimit coverage first). Add a `fakeRequest` builder
    like superAdminClaims.test.ts:156-170.

    Cover, at minimum (each an explicit test):
    - CALLER GATE (R200/R204, T-74-01/T-74-02): each of the three handlers rejects request.auth=null
      (unauthenticated), rejects token without superAdmin (permission-denied, never reads Firestore target),
      and rejects when superAdmins/{callerUid} does not exist (permission-denied).
    - DUPLICATE NAME (R201, T-74-04): onboard where tx.get(orgNames/{key}) returns an existing doc with a
      DIFFERENT orgId rejects with code 'already-exists' and performs no organizations/{orgId} set.
    - RETRY / no half-created org (R202): onboard where the orgNames doc exists with the SAME minted orgId is
      idempotent (proceeds, no already-exists throw). Assert a fresh onboard of an available name writes both
      orgNames/{key} and organizations/{orgId} inside the same transaction.
    - ONBOARD happy path (R197/R198/R199): organizations/{orgId} is set with {name, createdAt sentinel,
      createdBy, settings} where settings.defaultServiceTemplate is the 9-entry seed; and assignAdminCore is
      exercised (first admin added → members write + arrayUnion).
    - ADDITIVE arrayUnion (R206, T-74-03 — highest-value test): assignOrgAdmin (and onboard's first-admin
      path) for an existing user writes users/{uid} with orgIds === FieldValue.arrayUnion(orgId) — assert the
      exact mocked arrayUnion sentinel was passed, NOT a literal `[orgId]` set. Include a case framed as "the
      user already belongs to another org" to make the additive intent explicit.
    - EXISTING account (R203): assignOrgAdmin resolves the email, writes members/{uid} {role:'editor',
      joinedAt, displayName from UserRecord, email}, returns {status:'added'}.
    - NO ACCOUNT invite (R205): getUserByEmail rejects with code 'auth/user-not-found' → writes
      invites/{email} + inviteLookup/{email}, returns {status:'invited'}, writes NO members doc, never throws.
    - AUTH-ERROR discrimination (T-74-05, RESEARCH Pitfall 5): getUserByEmail rejects with a NON
      user-not-found code (e.g. a generic network error) → the handler THROWS (does NOT return 'invited' and
      does NOT write invite artifacts).
    - ORPHAN guard (T-74-06): assignOrgAdmin with an orgId whose organizations/{orgId} does not exist throws
      'not-found' and writes no members doc.
    - LIST (R196): listOrganizations returns [{orgId,name,createdAt,memberCount}] for N mocked orgs with the
      mocked member counts.

    Use rejects.toMatchObject({code: ...}) for gate/error assertions and toHaveBeenCalledWith(...) on the
    write/tx spies for shape assertions, matching superAdminClaims.test.ts's assertion style.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/orgProvisioning.test.ts</automated>
  </verify>
  <done>All callable behaviors above are pinned and green; the additive-arrayUnion, duplicate-name, no-account-invite, auth-error-discrimination, and orphan-guard tests all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (super-admin session) → onCall callable | Client-supplied `{name, adminEmail}` / `{orgId, email}` selectors and the caller's ID token cross here; nothing the client sends about its own authority is trusted. |
| callable → Firestore / Auth (Admin SDK) | Privileged, rules-bypassing reads/writes to organizations/orgNames/members/invites/inviteLookup/users and Auth `getUserByEmail`. |

Accepted-risk note: a compromised super-admin token is a full-platform admin-assignment primitive by
design (a super-admin may assign to ANY org) — the same blast radius as the existing `setSuperAdminClaim`
callable. This is the pre-existing accepted shape of the `superAdmin` claim, not new risk from this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-01 | Elevation of Privilege | all three onCall handlers | high | mitigate | Dual caller gate (`assertSuperAdminCaller`): reject `!request.auth`, reject `token.superAdmin !== true`, AND fresh re-read of `superAdmins/{callerUid}` — verbatim mirror of setSuperAdminClaimHandler. Tested (Task 3 caller-gate cases). |
| T-74-02 | Spoofing / Elevation | assignOrgAdmin, onboardOrganization | high | mitigate | Never trust a client-supplied uid or superAdmin flag: target resolved only via getUserByEmail; strict type-checks reject malformed `name`/`email`/`orgId`; the callable never writes custom claims (trigger owns them). Tested (gate + arg-validation cases). |
| T-74-03 | Tampering (data integrity) | assignAdminCore users/{uid}.orgIds | high | mitigate | Single shared helper writes orgIds via FieldValue.arrayUnion — never a literal-array overwrite (R206). A user already in another org keeps it. Tested (additive-arrayUnion multi-org case — highest value). |
| T-74-04 | Tampering | onboardOrganization orgNames uniqueness | high | mitigate | get-then-create inside ONE Firestore transaction (existence check + orgNames claim + org doc + settings), no pre-check `.get()` outside the transaction (TOCTOU-safe). A duplicate name creates no second org; a retry strands nothing. Tested (duplicate-name + retry cases). |
| T-74-05 | Repudiation / DoS | assignAdminCore getUserByEmail catch | medium | mitigate | Discriminate `err.code === 'auth/user-not-found'` before the invite branch; rethrow any other code so a real Auth failure surfaces instead of masquerading as a successful `invited`. Tested (non-user-not-found → throws). |
| T-74-06 | Tampering / DoS | assignOrgAdmin orgId handling | medium | mitigate | Explicit organizations/{orgId} existence check before writing members/{uid}, rejecting a typo'd/nonexistent org id so no orphaned membership is created. Tested (nonexistent orgId → not-found, no write). |
| T-74-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase — firebase-admin/firebase-functions are already installed, in-use deps (RESEARCH: Package Legitimacy Audit N/A). No install task, so no [ASSUMED]/[SUS] checkpoint needed. |
</threat_model>

<verification>
- `cd functions && npx vitest run src/orgTemplateSeed.test.ts src/orgProvisioning.test.ts` — all green.
- `cd functions && npm run build` (tsc) — clean.
- `cd functions && npx vitest run` — full functions suite green (no regression in the 11 existing test files).
- The three deployed names appear in functions/src/index.ts's export block.
</verification>

<success_criteria>
- listOrganizations returns [{orgId,name,createdAt,memberCount}] server-side (R196).
- onboardOrganization creates org + deep-merged OrgSettings + 9-entry seeded template + first editor admin
  in one flow, all through the super-admin-gated callable (R197/R198/R199/R200).
- Duplicate church name → already-exists, no second org; retry after fixing input strands nothing (R201/R202).
- assignOrgAdmin: existing account → editor membership (added); no account → invite artifacts + invited;
  multi-org user → additive arrayUnion (R203/R204/R205/R206).
- No firestore.rules/storage.rules change (Admin SDK bypasses rules; assigned admin reads via existing
  isOrgMember). No custom-claim write from any callable (trigger owns claims).
- DEPLOY = HAND OVER: callables ship built + tested + UNDEPLOYED. Record in the SUMMARY the exact command
  for the owner: `firebase deploy --only functions:onboardOrganization,functions:assignOrgAdmin,functions:listOrganizations`.
</success_criteria>

<output>
Create `.planning/phases/74-organizations-onboard-assign/74-01-SUMMARY.md` when done, including the exact
undeployed-hand-over deploy command above.
</output>
