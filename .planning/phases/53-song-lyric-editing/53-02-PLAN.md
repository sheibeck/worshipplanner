---
phase: 53-song-lyric-editing
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - src/utils/slideshowAssembler.ts
  - src/utils/__tests__/slideshowAssembler.test.ts
  - src/utils/__tests__/slideGroupMaterializer.test.ts
autonomous: true
requirements: [R117, R118]
must_haves:
  truths:
    - "A split lyric section (slideBreaks present) emits N slides on the STORED-GROUP path, ids `${entry.id}:${i}`; an UNSPLIT section emits ONE slide with id `entry.id` byte-identical to today — R117 + BWC"
    - "A split lyric section emits N slides on the FALLBACK (no materialized group) path with distinct stable ids; an unsplit section is byte-identical to today's single `${slot.id}:${localSeq}` slide — R117 + BWC"
    - "Both assembler paths agree slide-for-slide (same count and same lines) for the same split section — the D1 dual-path lockstep discipline"
    - "A performanceOrder repeat of a split section emits ALL N slides on BOTH occurrences with distinct ids (`${entryA.id}:i` vs `${entryB.id}:i`) — R118, no duplicateRow or group-model change"
    - "The stored slide-group model (deriveGroupEntries/rebuildSongGroup/sourceSignature) is unchanged — the split resolves live at assembly"
  artifacts:
    - src/utils/slideshowAssembler.ts
    - src/utils/__tests__/slideshowAssembler.test.ts
    - src/utils/__tests__/slideGroupMaterializer.test.ts
  key_links:
    - "Both lyric-emission sites consume the SAME sliceSectionIntoSlides helper from Plan 01 — the split's meaning is defined once"
    - "emitFromGroup id override: split slide id = `${entry.id}:${i}`; unsplit keeps `entry.id` (Phase 23 WR-02 media keying preserved); media resolved once from the real `entry` so background/bed audio stay continuous across the split"
---

<objective>
Wire the split into the slide assembler — the single load-bearing seam where a `LyricSection` becomes
slides — realised at the TWO in-lockstep lyric-emission call sites in `assembleSlideshow`. Both back
onto Plan 01's `sliceSectionIntoSlides`, so an unsplit section is byte-identical to today and a split
section emits N slides. R118 (duplicate a split as one unit) is proven here to fall out automatically
from the D-02 reference model — no `duplicateRow` change, no stored slide-group change.

Purpose: This is the highest-risk change in the phase (a critical, dual-path pure assembler). Getting
the two paths to agree slide-for-slide and keeping the unsplit path byte-identical is what makes every
existing production song render unchanged while newly-split sections project as N slides.

Output: `assembleSlideshow` resolves splits live; `SlideGrid.vue` (Slides tab) and `PresentationViewer`
both show N tiles with no separate grid change, because both render `assembleSlideshow`'s output.

Depends on Plan 01: imports `sliceSectionIntoSlides` from `src/utils/songSectionOrder.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-song-lyric-editing/53-RESEARCH.md

@src/utils/slideshowAssembler.ts
@src/utils/__tests__/slideshowAssembler.test.ts
@src/utils/songSectionOrder.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slice at the stored-group lyric-emission site + emitFromGroup id override (R117)</name>
  <files>src/utils/slideshowAssembler.ts, src/utils/__tests__/slideshowAssembler.test.ts</files>
  <read_first>
    - src/utils/slideshowAssembler.ts (resolveEntryContent lyric case :178-190; emitFromGroup :407-453; the stored-group entry loop :513-518)
    - src/utils/songSectionOrder.ts (the sliceSectionIntoSlides export from Plan 01)
    - src/utils/__tests__/slideshowAssembler.test.ts (fixture helpers makeSlideGroup :121, makeGroupSlideEntry :133, makeSongLyrics :43; the existing lyric-entry stored-group tests around :1087-:1140)
    - 53-RESEARCH.md § "The Assembler Seam" and § "Split slide id contract (WR-02 safe)"
  </read_first>
  <behavior>
    RED first — add to slideshowAssembler.test.ts before wiring:
    - Stored-group path, section with slideBreaks [4] on an 8-line section, one lyric entry -> emits 2 lyric slides; ids `${entry.id}:0` and `${entry.id}:1`; first slide lines are lines[0..3], second lines[4..7]; sectionLabel on both is the stored section.label.
    - Regression / BWC: stored-group path, section with NO slideBreaks -> emits exactly ONE lyric slide whose id is `entry.id` (byte-identical to today); assert against the existing chorus/verse entry fixtures.
    - The split slides all resolve the same group media (background/bed audio) — assert the shared background/groupId is present on each split slide when the group has one.
  </behavior>
  <action>
Import `sliceSectionIntoSlides` from `@/utils/songSectionOrder` into `src/utils/slideshowAssembler.ts`.

Thread an optional slide-id override into `emitFromGroup`: add a trailing parameter `idOverride?: string`
and use `idOverride ?? entry.id` for BOTH the slide's `id` and the `AssembledSlide.groupSlideId`. When no
override is passed (every non-lyric entry, and every unsplit lyric section), behavior is byte-identical to
today (id = groupSlideId = entry.id) — this preserves the Phase 23 WR-02 media-keying invariant. Media is
still resolved from the real `entry` (`resolveEntryMedia(group, entry, song)`), so all N split slides share
the section's background/bed audio.

In the stored-group entry loop (`slideshowAssembler.ts:513-518`), special-case
`entry.sourceRef.kind === 'lyric'`: resolve the section from `inputs.songLyricsById` (same lookup
resolveEntryContent's lyric case uses); if it does not resolve, `continue` (omit, as today). Call
`sliceSectionIntoSlides(section)` to get the line-groups. For each group at index `i`, build the
`LyricSlide` content (contentKind 'lyric', sectionId = section.id, sectionLabel = section.label — the
STORED label, BWC, lines = the group). Compute the slide id: when there is exactly one group, pass NO
override so the slide keeps `entry.id` verbatim; when there are multiple groups, pass `${entry.id}:${i}`.
Call `emitFromGroup(slot, index, group, entry, content, slideId)` per group. Leave the non-lyric branch of
the loop exactly as-is (still `resolveEntryContent` + `emitFromGroup`). Do NOT alter
`resolveEntryContent`'s own lyric case — it stays valid for any direct caller; the loop now owns lyric
slicing. Make NO change to `slideGroupMaterializer.ts` (deriveGroupEntries / rebuildSongGroup /
sourceSignature) — the split is resolved live, never stored.
  </action>
  <verify>
    <automated>npx vitest run --dir src src/utils/__tests__/slideshowAssembler.test.ts</automated>
  </verify>
  <done>A split section emits N stored-group lyric slides with ids `${entry.id}:${i}` and correctly partitioned lines; an unsplit section emits one slide with id `entry.id` byte-identical to today; split slides share the section's group media. Test file passes; npm run type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slice at the fallback path + dual-path lockstep + R118 duplicate proof + BWC (R117, R118)</name>
  <files>src/utils/slideshowAssembler.ts, src/utils/__tests__/slideshowAssembler.test.ts, src/utils/__tests__/slideGroupMaterializer.test.ts</files>
  <read_first>
    - src/utils/slideshowAssembler.ts (emitFallback :381-405; the fallback SONG case :524-551; the dual-path lockstep comment :556-565)
    - src/utils/__tests__/slideshowAssembler.test.ts (fallback SONG tests; makeService :27, makeInputs :111)
    - src/utils/__tests__/slideGroupMaterializer.test.ts (SONG deriveGroupEntries repeat-entry tests)
    - 53-RESEARCH.md § "The Assembler Seam" R118 paragraph + § "Common Pitfalls" (Pitfall 2 diverging paths)
  </read_first>
  <behavior>
    RED first — add before wiring:
    - Fallback path (no materialized group for the slot), section with slideBreaks [4] on 8 lines ->
      emits 2 lyric fallback slides with distinct, stable ids (`${slot.id}:${localSeq}` for consecutive
      localSeq), lines partitioned [0..3] / [4..7], between the leading and trailing copyright slides.
    - BWC: fallback path, unsplit section -> exactly one fallback lyric slide, byte-identical id/lines to
      today.
    - Dual-path lockstep: for the SAME split section, the stored-group path and the fallback path emit the
      SAME number of lyric slides with the SAME lines in the same order (ids differ by scheme; count+lines
      must match).
    - R118 duplicate proof: a performanceOrder that references a split section TWICE (via a stored group
      whose deriveGroupEntries emitted two lyric entries, and via the fallback order [sectionId, sectionId])
      -> BOTH occurrences emit all N split slides; the two occurrences' slide ids are distinct
      (`${entryA.id}:i` vs `${entryB.id}:i` on the stored path; distinct consecutive localSeq on fallback).
      No change to duplicateRow.
    - slideGroupMaterializer.test.ts: assert deriveGroupEntries still emits exactly one lyric entry per
      performanceOrder occurrence of a split section (the group model is unchanged by this phase).
  </behavior>
  <action>
In the fallback SONG case (`slideshowAssembler.ts:537-547`), replace the single per-section
`emitFallback` with: call `sliceSectionIntoSlides(section)` and emit one `emitFallback` per line-group,
building the `LyricSlide` content per group (contentKind 'lyric', sectionId, sectionLabel = section.label,
lines = the group) and advancing the existing `localSeq++` per group so fallback ids stay distinct and
stable (`${slot.id}:${localSeq}`). An unsplit section yields exactly one group -> one emitFallback with the
current localSeq -> byte-identical to today. This keeps the two paths in lockstep: both `:513-518` (Plan 02
Task 1) and `:537-547` now derive slides through the same `sliceSectionIntoSlides`, honoring the D1
"agree slide-for-slide" discipline documented at `:556-565`.

Add the dual-path lockstep test, the BWC regression test, and the R118 duplicate proof described in
behavior. In `slideGroupMaterializer.test.ts`, add an assertion that a split section referenced twice in
performanceOrder still yields exactly one lyric entry per occurrence (documents that R118 needs zero
group-model change). Make NO production change to `slideGroupMaterializer.ts`.
  </action>
  <verify>
    <automated>npx vitest run --dir src src/utils/__tests__/slideshowAssembler.test.ts src/utils/__tests__/slideGroupMaterializer.test.ts</automated>
  </verify>
  <done>The fallback path slices split sections into N slides (unsplit byte-identical); both paths agree slide-for-slide for the same split section; a duplicated (repeated) split section emits all N slides on both occurrences with distinct ids and no duplicateRow/group-model change. Test files pass; npm run type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted `slideBreaks` → live assembly | The assembler reads user-authored break indices off the lyrics doc at presentation time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-04 | Tampering | assembleSlideshow lyric emission | low | mitigate | Slicing goes through Plan 01's read-tolerant `sliceSectionIntoSlides`; a corrupt/legacy `slideBreaks` collapses to one slide, never throws mid-assembly |
| T-53-05 | Denial of Service | Slide id churn / media re-init (Phase 23 WR-02) | low | mitigate | Split ids are derived positionally (`${entry.id}:${i}` / incrementing `localSeq`), never randomized at emit time, so Vue media-child keys stay stable across recomputes |
| T-53-06 | Information Disclosure | Cross-slot/cross-song content bleed | low | accept | Section resolved via the entry's own `sourceRef.songId`/`sectionId` against `inputs.songLyricsById`; unchanged lookup, no new cross-references introduced |

_No package installs in this phase; no supply-chain (T-*-SC) threat applies._
</threat_model>

<verification>
- `npx vitest run --dir src src/utils/__tests__/slideshowAssembler.test.ts src/utils/__tests__/slideGroupMaterializer.test.ts` green.
- Per-wave merge gate: `npx vitest run --dir src --exclude '**/rules.test.ts'` at the exactly-2-file
  baseline (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`) + `npm run type-check`
  clean.
- Manual "split an 8-line chorus, present, see two slides" and "duplicate the split, present, both
  occurrences show both slides" are the phase-level UAT items in `53-VALIDATION.md`.
</verification>

<success_criteria>
- Both lyric-emission sites slice through `sliceSectionIntoSlides`; unsplit sections byte-identical on
  both paths; split sections emit N slides.
- The two paths agree slide-for-slide for the same split section.
- A duplicated split section emits all N slides on both occurrences (R118) with no duplicateRow or
  slide-group-model change.
- Target test files green; `npm run type-check` clean.
</success_criteria>

<output>
Create `.planning/phases/53-song-lyric-editing/53-02-SUMMARY.md` when done.
</output>
