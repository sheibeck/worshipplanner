---
phase: 14-in-app-quarterly-availability-editor
plan: 04
type: execute
wave: 2
depends_on: [14-02]
files_modified:
  - src/components/RosterImportModal.vue
autonomous: false
requirements: [D-08, D-09, D-10, D-11]   # REQUIREMENTS.md absent — CONTEXT.md decision IDs are the traceability keys
user_setup:
  - service: planning-center
    why: "Selective import reads worship-team positions and their current assignees from a live Planning Center org"
    env_vars: []
    dashboard_config:
      - task: "Have a worship team with at least one individually-scheduled position (e.g. Guitar/Sound) AND a group position (Choir or Orchestra) so exclusion can be verified"
        location: "Planning Center → Services → Teams"

must_haves:
  truths:
    - "The PC import flow asks the leader to pick a service type, then a worship team, then which of that team's positions to include (choir/orchestra left unchecked)"
    - "Each included position is mapped to a WorshipPlanner Role, and imported people receive that Role's id in their roles[]"
    - "Only people currently serving the included positions are imported — the whole-directory fetchAndMapPeople call is gone from this modal"
    - "Traceability: enforces D-08 (scope by team AND role/position), D-09 (choir/orchestra excluded), D-10 (positions mapped to Role), and D-11 (modify existing RosterImportModal.vue path)"
  artifacts:
    - path: "src/components/RosterImportModal.vue"
      provides: "Multi-step selective import (service type → team → positions+role-mapping → preview) replacing the whole-directory fetch"
      contains: "fetchPeopleForTeamPositions"
  key_links:
    - from: "src/components/RosterImportModal.vue"
      to: "fetchPeopleForTeamPositions"
      via: "final fetch after position selection, replacing fetchAndMapPeople"
      pattern: "fetchPeopleForTeamPositions"
---

<objective>
Replace `RosterImportModal.vue`'s whole-church import with a scoped team→position→people flow (D-08/D-09/D-10/D-11): the leader picks a service type, a worship team, then checks which individually-scheduled positions to import and maps each to a WorshipPlanner Role. Only people serving those positions import; choir/orchestra are simply left unchecked.

Purpose: The worship volunteer list is small and music-only; pulling the entire PC directory is noise. This scopes the import to exactly the people WorshipPlanner schedules by individual.
Output: Extended `RosterImportModal.vue` with new selection steps, position→Role mapping, and the `fetchPeopleForTeamPositions`-based fetch; whole-directory `fetchAndMapPeople` call removed from this modal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-CONTEXT.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md

<interfaces>
<!-- PC client functions (all already exist except the new one from Plan 14-02): -->
<!--   fetchServiceTypes(appId, secret): Promise<{id,name}[]> -->
<!--   fetchServiceTypeTeams(appId, secret, serviceTypeId): Promise<{id,name}[]> -->
<!--   fetchTeamPositions(appId, secret, teamId): Promise<{id,name}[]> -->
<!--   fetchPeopleForTeamPositions(appId, secret, teamId, selectedPositionIds: Set<string>): Promise<{pcPersonId,name}[]>  (NEW, Plan 14-02) -->
<!-- UpsertPersonInput (src/types/roster.ts line 70): { name; email; phone?; roles?: string[]; frequencyTargetN?; pcPersonId?: string|null } -->
<!-- rosterStore.roles: Role[]  (id, name, group) — the mapping targets -->
<!-- rosterStore.upsertPeople(mapped) — unchanged import writer -->
<!-- Existing local modal internals to reuse: classifyPeople(mapped, existing) (line 276), onConfirmImport, preview ref {toAdd,toUpdate}, step state machine, resetToIdle() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Selective team/position import flow with position→Role mapping</name>
  <files>src/components/RosterImportModal.vue</files>
  <read_first>
    - src/components/RosterImportModal.vue — the `Step` union + state machine (lines 241-266), `onStartFetch` try/catch/step-transition (lines 293-310), `classifyPeople`/`onConfirmImport`/`upsertPeople` (lines 268-322, reuse unchanged), and the template step-branch blocks (lines 55-157) whose container conventions each new step must follow
    - src/stores/roster.ts — `roles` list and `upsertPeople` signature (the mapping targets and the unchanged writer)
    - src/utils/planningCenterApi.ts — `fetchServiceTypes`/`fetchServiceTypeTeams`/`fetchTeamPositions` request shapes and the new `fetchPeopleForTeamPositions` (from Plan 14-02)
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/components/RosterImportModal.vue` (lines 325-371) for the exact step/handler analogs
    - .planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md — System Architecture Diagram "Selective Planning Center import" (lines 170-189) and Anti-Patterns (line 376, do NOT auto-detect choir/orchestra)
  </read_first>
  <action>
    Extend the `Step` union with `'selectServiceType' | 'selectTeam' | 'selectPositions'` inserted between `'idle'` and `'fetching'`. Add refs: `serviceTypes`, `teams`, `positions` (each `{id,name}[]`), `selectedServiceTypeId`, `selectedTeamId`, `selectedPositionIds` (a `Set<string>` or reactive boolean map), and `positionRoleMap` (`Record<positionId, roleId>`). On modal open, the existing `resetToIdle()` MUST also clear every one of these new refs (freshly derived each open — never cached; 14-RESEARCH Security Domain). Wire handlers mirroring `onStartFetch`'s try/catch/step shape: `onStartSelectiveImport` → `fetchServiceTypes` → `'selectServiceType'`; `onSelectServiceType(id)` → `fetchServiceTypeTeams` → `'selectTeam'`; `onSelectTeam(id)` → `fetchTeamPositions` → `'selectPositions'`. On `'selectPositions'`, render a checkbox per position plus, next to each CHECKED position, a `<select>` bound to `positionRoleMap[positionId]` listing `rosterStore.roles` (the leader maps each individually-scheduled position to a Role; leaving choir/orchestra unchecked excludes them — do NOT auto-detect them). A confirm handler (`onConfirmPositions`) calls `fetchPeopleForTeamPositions(creds.appId, creds.secret, selectedTeamId, new Set(checkedPositionIds))`, then builds `UpsertPersonInput[]` mapping each returned `{pcPersonId, name}` to `{ name, email: '', pcPersonId, roles: <distinct Role ids from every selected position that person served> }`. Because `fetchPeopleForTeamPositions` dedupes people but returns only name/id, associate roles by re-deriving them during the final build (either have the fetch return the matched position ids per person, or run the checked positions through a second pass) — the executor's discretion; the REQUIRED outcome is a person serving two selected positions gets both mapped Role ids. Set the result into the existing `mappedPeople` ref, call the existing `classifyPeople`/`preview` path, and transition to `'preview'`. From `'preview'` onward reuse `onConfirmImport` + `upsertPeople` UNCHANGED. Remove the `fetchAndMapPeople` import and its call from this modal (D-11 — replaced, not supplemented). Email is intentionally left `''` for the selective path (leader edits later, mirroring the phone='' precedent) — this is acceptable Claude's-discretion scope, not a regression.
  </action>
  <verify>
    <automated>npm run type-check && npx vitest run src/utils/__tests__/planningCenterApi.test.ts -t "fetchPeopleForTeamPositions"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetchPeopleForTeamPositions" src/components/RosterImportModal.vue` matches
    - `grep -c "fetchAndMapPeople" src/components/RosterImportModal.vue` returns 0 (whole-directory call removed per D-11)
    - `grep -n "selectPositions" src/components/RosterImportModal.vue` matches (new step exists)
    - `resetToIdle` clears the new selection refs (grep the function body for `selectedPositionIds` / `positionRoleMap` resets)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Modal drives service-type→team→positions selection with per-position Role mapping, imports only selected-position assignees via `fetchPeopleForTeamPositions`, reuses the existing preview/confirm/upsert path, and no longer references `fetchAndMapPeople`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify selective PC import against a live Planning Center org</name>
  <action>Human verifies the selective import end-to-end: pick a service type and worship team, include only individually-scheduled positions (choir/orchestra left unchecked) with Role mapping, and confirm only the correct scoped assignees import.</action>
  <what-built>Selective PC import: service-type → worship-team → individually-scheduled positions (with Role mapping), importing only current assignees and excluding choir/orchestra.</what-built>
  <how-to-verify>
    1. Ensure PC credentials are configured (Settings) and the connected org has a worship team with at least one individually-scheduled position (e.g. Guitar or Sound) AND a group position (Choir or Orchestra).
    2. Open the roster page → "Import from Planning Center".
    3. Pick a service type, then the worship team.
    4. Check only the individually-scheduled position(s); LEAVE Choir/Orchestra unchecked. Map each checked position to a WorshipPlanner Role.
    5. Confirm and review the preview — verify the people listed are exactly those currently serving the checked positions, and that NO choir/orchestra-only people appear.
    6. Complete the import and confirm the roster shows the imported people with the mapped roles.
  </how-to-verify>
  <resume-signal>Type "approved" if only the correct scoped people imported, or describe what imported incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorshipPlanner client → Planning Center Services v2 | Third-party person data fetched over Basic Auth and written into the roster on confirm. |
| Import modal → Firestore `people` collection | Editor-role user writes roster records via the existing `upsertPeople` path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Information Disclosure | stale `selectedPositionIds`/`positionRoleMap` leaking a prior team's selection into a new import | mitigate | `resetToIdle()` clears all new selection refs on every modal open; asserted by the reset acceptance criterion (14-RESEARCH Security Domain). |
| T-14-04-02 | Tampering | over-broad roster write (importing more people than selected) | mitigate | People come solely from `fetchPeopleForTeamPositions` filtered by the checked positions (tested in Plan 14-02); human-verify checkpoint confirms scope against a live org. |
| T-14-04-03 | Information Disclosure | PC credential handling | accept | Reuses existing `authStore.pcCredentials` + masked-after-save UI from Phase 8; no new credential handling. |
| T-14-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this plan; if an install becomes necessary, run the Package Legitimacy Gate first (blocking human checkpoint for any [ASSUMED]/[SUS]). |
</threat_model>

<verification>
- `npm run type-check` green
- `fetchAndMapPeople` no longer referenced in the modal; `fetchPeopleForTeamPositions` wired
- Human-verify: live scoped import excludes choir/orchestra, imports correct assignees with mapped roles
</verification>

<success_criteria>
- Import is scoped by team + individually-scheduled position, with per-position Role mapping
- Choir/orchestra people are not imported
- Existing preview/confirm/upsert behavior preserved
</success_criteria>

<output>
Create `.planning/phases/14-in-app-quarterly-availability-editor/14-04-SUMMARY.md` when done.
</output>
