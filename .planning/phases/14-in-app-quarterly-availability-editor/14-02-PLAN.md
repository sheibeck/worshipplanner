---
phase: 14-in-app-quarterly-availability-editor
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/planningCenterApi.ts
  - src/utils/__tests__/planningCenterApi.test.ts
autonomous: true
requirements: [D-08, D-09, D-10]   # REQUIREMENTS.md absent — CONTEXT.md decision IDs are the traceability keys
user_setup:
  - service: planning-center
    why: "Selective import reads worship-team position assignments from Planning Center Services v2 (reuses existing Phase 8 credentials — no new setup for the fetch itself; live verification happens in Plan 04)"
    env_vars: []

must_haves:
  truths:
    - "fetchPeopleForTeamPositions returns only people whose assignment's team_position id is in the caller's selectedPositionIds set"
    - "A person serving two selected positions is returned exactly once (deduped by person id)"
    - "Pagination follows links.next across pages exactly like fetchAllPeople"
    - "Traceability: enforces D-08 (scope by team AND role/position), D-09 (choir/orchestra excluded), and D-10 (current position servers via person_team_position_assignments)"
  artifacts:
    - path: "src/utils/planningCenterApi.ts"
      provides: "fetchPeopleForTeamPositions(appId, secret, teamId, selectedPositionIds)"
      exports: ["fetchPeopleForTeamPositions"]
    - path: "src/utils/__tests__/planningCenterApi.test.ts"
      provides: "filter + dedupe + pagination test cases for the new function"
      contains: "fetchPeopleForTeamPositions"
  key_links:
    - from: "src/utils/planningCenterApi.ts"
      to: "/teams/{teamId}/person_team_position_assignments?include=person"
      via: "fetch with basicAuthHeader over PC_BASE_URL"
      pattern: "person_team_position_assignments"
---

<objective>
Add the scoped Planning Center fetch that powers selective import (D-08/D-09/D-10): given a worship team and the leader's chosen individually-scheduled position ids, return the distinct people currently serving those positions — excluding everyone else (choir/orchestra positions are simply never in the selected set).

Purpose: This replaces the whole-church `fetchAndMapPeople` pull. Isolating it as a pure, unit-tested API client function lets Plan 04 wire it into the modal against a settled contract.
Output: New `fetchPeopleForTeamPositions` function + its filter/dedupe/pagination test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-CONTEXT.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md

<interfaces>
<!-- Existing module surface (src/utils/planningCenterApi.ts): -->
<!--   export const PC_BASE_URL = '/api/planningcenter/services/v2' -->
<!--   function basicAuthHeader(appId, secret): string  (module-private) -->
<!--   fetchServiceTypeTeams(appId, secret, serviceTypeId): Promise<{id,name}[]>  (line 106) -->
<!--   fetchTeamPositions(appId, secret, teamId): Promise<{id,name}[]>            (line 591, team-scoped, no service-type needed) -->
<!--   fetchAllPeople(appId, secret): Promise<PcPerson[]>  (line 1020 — the while(url)+links.next+429-retry loop to copy) -->
<!-- PC endpoint (team-scoped — use THIS one, not the service_type-scoped sibling; 14-RESEARCH Pitfall 4): -->
<!--   GET /teams/{team_id}/person_team_position_assignments?include=person&per_page=100 -->
<!--   assignment.relationships.person.data.id, assignment.relationships.team_position.data.id -->
<!--   response.included[] carries the Person resources (type 'Person', attributes.name|first_name|last_name) -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: fetchPeopleForTeamPositions with filter + dedupe + pagination (RED→GREEN)</name>
  <files>src/utils/planningCenterApi.ts, src/utils/__tests__/planningCenterApi.test.ts</files>
  <read_first>
    - src/utils/planningCenterApi.ts — `PC_BASE_URL` + `basicAuthHeader` (lines 13-20), `fetchTeamPositions` (lines 591-610) for the team-scoped request shape, and `fetchAllPeople` (lines 1020-1062) for the exact `while (url)` + `links.next` rewrite + 429/`Retry-After` retry loop to copy
    - src/utils/__tests__/planningCenterApi.test.ts — existing `fetchAllPeople`/`fetchTeamPositions` fetch-mock + pagination assertions to mirror (global `fetch` is mocked here; follow the same mocking style)
    - .planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md — Architecture Patterns Pattern 4 (lines 320-373) for the full worked function using `include=person` to avoid an N+1 per-person fetch
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/utils/planningCenterApi.ts` (lines 184-244) for analog line references and the throw-on-non-ok convention
  </read_first>
  <behavior>
    - Filter: only assignments whose `relationships.team_position.data.id` is in the passed `selectedPositionIds` Set contribute a person; assignments for unselected positions are ignored.
    - Dedupe: a person appearing in two assignments (two selected positions) appears once in the returned array.
    - Pagination: when the first response has `links.next`, the function fetches the next page (URL rewritten from the absolute PC host to `PC_BASE_URL`) and merges results; stops when `links.next` is absent.
    - Name resolution: person name comes from `included[]` (`attributes.name` trimmed, else `first_name + ' ' + last_name` trimmed).
  </behavior>
  <action>
    RED: add an `it(...)` block (name including `"fetchPeopleForTeamPositions"`) that mocks `fetch` to return a two-page assignment payload with an `included` Person array — one person on two selected positions, one person on an unselected position — and asserts the result includes the multi-position person exactly once and excludes the unselected-position person, and that both pages were fetched.
    GREEN: export `async function fetchPeopleForTeamPositions(appId: string, secret: string, teamId: string, selectedPositionIds: Set<string>): Promise<Array<{ pcPersonId: string; name: string }>>`. Build the initial URL `` `${PC_BASE_URL}/teams/${teamId}/person_team_position_assignments?include=person&per_page=100` ``. Reuse `basicAuthHeader(appId, secret)` for the `Authorization` header. Copy `fetchAllPeople`'s `while (url)` loop with the 429/`Retry-After` retry and the `links.next.replace('https://api.planningcenteronline.com/services/v2', PC_BASE_URL)` rewrite. On non-ok response `throw new Error('Failed to fetch team position assignments: ' + status)`. Per page: build a `Map` of included people by id, iterate `json.data`, skip assignments whose `team_position.data.id` is not in `selectedPositionIds`, resolve the person from the included map, and set into a `Map<pcPersonId, name>` (which dedupes). Return `Array.from(...)` mapped to `{ pcPersonId, name }`. Do NOT fetch emails here — emails, if needed downstream, are Plan 04's concern.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/planningCenterApi.test.ts -t "fetchPeopleForTeamPositions"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/planningCenterApi.test.ts -t "fetchPeopleForTeamPositions"` exits 0
    - `npx vitest run src/utils/__tests__/planningCenterApi.test.ts` full file exits 0 (no regression)
    - `grep -n "export async function fetchPeopleForTeamPositions" src/utils/planningCenterApi.ts` matches
    - `grep -n "person_team_position_assignments" src/utils/planningCenterApi.ts` matches, and the URL segment is `/teams/${teamId}/` (team-scoped, NOT `/service_types/`)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>`fetchPeopleForTeamPositions` exported using the team-scoped endpoint with correct filter, dedupe, and pagination; new test case green; full PC-api suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorshipPlanner client → Planning Center Services v2 | Third-party data (person names/ids) fetched over Basic Auth via the existing `/api/planningcenter` proxy; the response is untrusted input the app later writes into the roster (in Plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Information Disclosure | over-broad import (returning people beyond the selected positions) | mitigate | The function returns a person ONLY when their assignment's `team_position.data.id` is in the caller-supplied `selectedPositionIds` — asserted by the filter test case. Choir/orchestra positions are excluded by simply not being selected (D-09). |
| T-14-02-02 | Information Disclosure | PC credential handling in the new fetch | accept | Reuses the existing `basicAuthHeader` + `/api/planningcenter` proxy already audited in Phase 8; no new credential storage or logging is introduced. |
| T-14-02-03 | Denial of Service | PC rate limiting during pagination | mitigate | Copies `fetchAllPeople`'s 429/`Retry-After` backoff loop verbatim — no new unthrottled request pattern. |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/planningCenterApi.test.ts` green (incl. new case)
- `npm run type-check` green
- Endpoint is the team-scoped `/teams/{teamId}/person_team_position_assignments` (Pitfall 4 avoided)
</verification>

<success_criteria>
- Selective fetch returns exactly the distinct people serving the selected positions, paginated
- No new credential-handling or unthrottled-request surface introduced
</success_criteria>

<output>
Create `.planning/phases/14-in-app-quarterly-availability-editor/14-02-SUMMARY.md` when done.
</output>
