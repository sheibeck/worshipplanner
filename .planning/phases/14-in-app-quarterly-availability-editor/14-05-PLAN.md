---
phase: 14-in-app-quarterly-availability-editor
plan: 05
type: execute
wave: 3
depends_on: [14-01, 14-03]
files_modified:
  - src/components/AvailabilityDrawer.vue
  - src/components/__tests__/AvailabilityDrawer.test.ts
  - src/components/QuarterGrid.vue
  - src/components/__tests__/QuarterGrid.test.ts
autonomous: true
requirements: [D-01, D-03, D-04, D-06, D-07]   # REQUIREMENTS.md absent — CONTEXT.md decision IDs are the traceability keys
user_setup: []

must_haves:
  truths:
    - "Opening the drawer for a person pre-populates the frequency control, Sundays-only calendar, pairing chips, and note from that person's existing PersonQuarterData"
    - "Only real service dates render as clickable Sundays; toggling one writes exactly that YYYY-MM-DD into blackoutDates"
    - "Saving persists all quarter-scoped fields via setPersonAvailability and the standing frequencyTargetN via rosterStore.updatePerson"
    - "An 'out'-tier person never appears in QuarterGrid's manual gap-filling candidate list"
  artifacts:
    - path: "src/components/AvailabilityDrawer.vue"
      provides: "Variant A right-drawer editor with frequency segmented control, Sundays-only calendar + Nth-Sunday chips + range block, pairing typeahead, quarter note"
      min_lines: 120
    - path: "src/components/__tests__/AvailabilityDrawer.test.ts"
      provides: "round-trip pre-populate + Sundays-only calendar correctness + save-call tests"
      contains: "calendar"
    - path: "src/components/QuarterGrid.vue"
      provides: "availableUnassigned excludes 'out'-tier people"
      contains: "frequencyTier"
    - path: "src/components/__tests__/QuarterGrid.test.ts"
      provides: "'out'-tier exclusion assertion for the manual candidate list"
      contains: "out tier"
  key_links:
    - from: "src/components/AvailabilityDrawer.vue"
      to: "quartersStore.setPersonAvailability"
      via: "onSave handler"
      pattern: "setPersonAvailability"
    - from: "src/components/QuarterGrid.vue"
      to: "PersonQuarterData.frequencyTier"
      via: "availableUnassigned filter"
      pattern: "frequencyTier"
---

<objective>
Build the Variant A right-drawer availability editor (D-01/D-03) that reads/writes one person's quarter data, and close the D-04 gap in the manual gap-filling list so an 'out'-tier person is never a selectable candidate anywhere (14-RESEARCH Pitfall 3).

Purpose: The drawer is the primary input path replacing the CSV round-trip — constrained controls guarantee compatible data. The QuarterGrid fix ensures "fully excluded" holds for manual picks, not just auto-proposals.
Output: `AvailabilityDrawer.vue` + its tests; `QuarterGrid.vue` `availableUnassigned` 'out' filter + a new focused test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-CONTEXT.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md
@.planning/sketches/001-availability-editor/index.html
@docs/Sample Frequency Notes.csv

<interfaces>
<!-- quartersStore.setPersonAvailability(quarterId, personId, {blackoutDates, pairedWith, frequencyTier, note})  (Plan 14-03) -->
<!-- quartersStore.getQuarter(quarterId): Quarter  -> .serviceDates: string[], .personQuarterData[personId]?: PersonQuarterData -->
<!-- rosterStore.people: Person[]  (standing name/email/roles/frequencyTargetN); rosterStore.updatePerson(id, patch) -->
<!-- FrequencyTier = 'regular' | 'fillin' | 'out'  (@/types/roster, Plan 14-01) -->
<!-- Sketch FREQ presets → {N, tier}: weekly{1,regular} biweek{2,regular} monthly{4,regular} fillin{0,fillin} out{0,out}; FREQ_ORDER order -->
<!-- ordinalOf(iso) = Math.floor((day-1)/7)+1  (which Nth Sunday of its month) -->
<!-- Overlay analog: RosterImportModal.vue Teleport/Transition shell (lines 1-49, 220-266) — adapt centered→right-drawer -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AvailabilityDrawer.vue — right-drawer editor with all D-03 controls</name>
  <files>src/components/AvailabilityDrawer.vue</files>
  <read_first>
    - .planning/sketches/001-availability-editor/index.html — the authoritative control logic to port to Vue: `FREQ`/`FREQ_ORDER` (lines 242-249), `editorHtml` (335-380), `freqReadout` (381-389), `calHtml` (390-399), `ordFullySelected`/`toggleNth` (404-407, 425-430), `applyRange` (431-433), `pairSearch`/`addPair`/`removePair` (436-450), `setFreq`/`setN`/`toggleSunday` (417-424). This is the design source (no UI-SPEC exists) — its JS functions map ~1:1 to Vue methods.
    - src/components/RosterImportModal.vue — Teleport/Transition overlay shell + props/emit + reset-on-open watcher (lines 1-49, 220-266) to adapt from centered modal to right drawer
    - src/components/QuarterGrid.vue — existing scoped-store-action call style and the `?.blackoutDates.includes(date) ?? false` optional-chaining default (line 254) for a person with no entry yet
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/components/AvailabilityDrawer.vue` (lines 248-294) for analog mapping and the anti-pattern "do NOT call expandBlackoutCell"
    - .planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md — Pattern 1 (drawer controlled by openPersonId) + Code Examples onSave (lines 439-456)
  </read_first>
  <action>
    Create `AvailabilityDrawer.vue` (`<script setup lang="ts">`). Props: `quarterId: string | null`, `personId: string | null`. Emit `close: []`. Render a `<Teleport to="body">` with a scrim + a right-drawer panel (`fixed top-0 right-0 bottom-0`, `w-full max-w-lg`, slide-in `translateX` Transition) — adapt the RosterImportModal shell to the sketch's `.drawer`/`.drawer-scrim` positioning; `v-if` on `personId`. On `personId` change (watcher), load a local reactive `draft` from `rosterStore.people.find(personId)` (standing: name/email/roles/`frequencyTargetN`) plus `quartersStore.getQuarter(quarterId).personQuarterData[personId] ?? { blackoutDates: [], pairedWith: [], frequencyTier: 'regular', note: '' }` (default missing fields via `?? 'regular'` / `?? ''`). Implement the controls porting the sketch logic:
    - Frequency: render `FREQ_ORDER` as a segmented button group; selecting a preset sets BOTH `draft.frequencyTier` (from the preset's tier) and, for regular presets, `draft.frequencyTargetN` (the preset's N). Show the live readout "≈ X of N Sundays" computed as `min(serviceDates.length - blackoutDates.length, ceil(serviceDates.length / frequencyTargetN))`; for `fillin` show "Only scheduled to fill gaps", for `out` show "Excluded from every proposal this quarter." Include the advanced raw 1-in-N number input (min 1 max 8) that sets `draft.frequencyTargetN` and maps back to the nearest regular preset for display.
    - Sundays-only calendar: iterate `quarter.serviceDates` directly (one button per date, grouped by month label) — NEVER a generic date-picker. Clicking toggles the date in `draft.blackoutDates`. Do NOT call `expandBlackoutCell` (drawer never emits free-text range syntax — 14-RESEARCH Anti-Patterns).
    - Nth-Sunday chips: for n in 1..5, a chip that toggles every serviceDate whose `ordinalOf` equals n; chip shows "on" when that ordinal group is fully blacked out (`ordFullySelected`).
    - Range block: two `<input type="date">` + a "Block Sundays in range" button that adds every serviceDate between the bounds to `blackoutDates`; plus a "Clear all" button.
    - Must-serve-with typeahead: a text input + computed filtered list of active roster people (exclude self and already-paired) + a `v-if` dropdown; selecting adds a bidirectional chip; each chip has a ✕ to remove. Plain v-model + computed — no autocomplete library.
    - Quarter note: a `<textarea>` bound to `draft.note` (D-07 — for the leader, never auto-scheduled).
    - Save: an `onSave` that awaits `quartersStore.setPersonAvailability(quarterId, personId, { blackoutDates, pairedWith, frequencyTier, note })`, and if `draft.frequencyTargetN` changed from the loaded value awaits `rosterStore.updatePerson(personId, { frequencyTargetN })`, then `emit('close')`. Never write `frequencyTargetN` through the quarters store, and never write quarter-scoped fields through the roster store (standing-vs-quarter split, Phase 13 D-18).
    Match the existing dark-theme utility-class conventions used by RosterImportModal/QuarterGrid.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AvailabilityDrawer.vue` exists, uses `<Teleport to="body">`, and is gated on `personId`
    - `grep -n "setPersonAvailability" src/components/AvailabilityDrawer.vue` and `grep -n "updatePerson" src/components/AvailabilityDrawer.vue` both match
    - `grep -c "expandBlackoutCell" src/components/AvailabilityDrawer.vue` returns 0 (calendar toggles serviceDates directly)
    - Calendar renders one clickable element per `quarter.serviceDates` entry (grep the template for a `v-for` over `serviceDates`)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Right-drawer editor renders all D-03 controls, loads a person's standing+quarter data, and saves through the correct standing/quarter store split; type-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AvailabilityDrawer.test.ts — round-trip + Sundays-only calendar</name>
  <files>src/components/__tests__/AvailabilityDrawer.test.ts</files>
  <read_first>
    - src/components/__tests__/CsvImportModal.test.ts and src/components/__tests__/ServiceCard.test.ts — Vue Test Utils mount + store-seeding + emit-assertion conventions in this codebase
    - src/stores/__tests__/quarters.test.ts — how the quarters store is seeded/mocked in tests (reuse the same approach so the drawer's store reads resolve)
    - src/components/AvailabilityDrawer.vue — the component under test (its props, draft loading, and save handler)
  </read_first>
  <behavior>
    - Pre-populate: mounting the drawer for a person whose `personQuarterData` has `blackoutDates: ['2026-07-19']`, `frequencyTier: 'fillin'`, `pairedWith: ['dean']`, `note: 'x'` renders the calendar with 2026-07-19 marked blacked-out, the fill-in preset active, a Dean pairing chip, and the note text.
    - Calendar correctness (test name includes "calendar"): the number of clickable Sunday buttons equals `quarter.serviceDates.length`; clicking a not-yet-blocked date adds exactly that ISO date to the draft blackout set; no non-serviceDate is rendered as clickable.
    - Save: invoking save calls `quartersStore.setPersonAvailability` with the current draft's `blackoutDates`/`pairedWith`/`frequencyTier`/`note`.
  </behavior>
  <action>
    Create `AvailabilityDrawer.test.ts`. Seed a pinia store (or spy on `setPersonAvailability` via `@pinia/testing` or a store mock, following the analog tests) so the drawer resolves `getQuarter(quarterId)` with a known `serviceDates` array (use the sketch's 13 Q3-2026 Sundays) and a seeded `personQuarterData` entry. Mount with `quarterId`/`personId` props. Write the three behavior cases above; the calendar case's `it(...)` name must contain the substring `"calendar"`. Assert the save spy received the expected payload.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts` exits 0
    - `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts -t "calendar"` exits 0
    - Test asserts clickable-Sunday count equals `serviceDates.length` and that saving calls `setPersonAvailability`
  </acceptance_criteria>
  <done>Drawer round-trip, Sundays-only calendar correctness, and save-call are covered and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: QuarterGrid 'out'-tier exclusion in manual candidate list</name>
  <files>src/components/QuarterGrid.vue, src/components/__tests__/QuarterGrid.test.ts</files>
  <read_first>
    - src/components/QuarterGrid.vue — `isBlackedOut`/`hasRole`/`availableUnassigned` (lines 253-268) and `blackedOutToday` (lines 272-277, leave unchanged)
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/components/QuarterGrid.vue` (lines 375-409) for the exact `frequencyTierOf` helper + filter extension
    - src/components/__tests__/ServiceCard.test.ts — mount + store-seed conventions for a new component test
  </read_first>
  <behavior>
    - An active person who has the role and is not blacked out but whose `personQuarterData[id].frequencyTier === 'out'` does NOT appear in `availableUnassigned(date, roleId)` / the manual gap-filling candidate list.
    - A person with no `frequencyTier` (absent) still appears (defaults 'regular').
  </behavior>
  <action>
    In `QuarterGrid.vue`, add a `frequencyTierOf(personId): FrequencyTier` helper reading `props.quarter.personQuarterData[personId]?.frequencyTier ?? 'regular'` (import `FrequencyTier` from `@/types/roster`), and add `&& frequencyTierOf(p.id) !== 'out'` to the `availableUnassigned` filter predicate. Do NOT change `blackedOutToday` (a different, display-only list). Create `QuarterGrid.test.ts` (new file — none exists; this is also noted as a pre-existing Phase 13 test gap, only the 'out' assertion is required here) with an `it(...)` whose name includes `"out tier"` asserting the exclusion behavior above by seeding an 'out'-tier person and confirming they are not offered as a manual candidate.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/QuarterGrid.test.ts -t "out tier"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "frequencyTier" src/components/QuarterGrid.vue` matches inside `availableUnassigned`'s helper/filter
    - `npx vitest run src/components/__tests__/QuarterGrid.test.ts -t "out tier"` exits 0
    - `blackedOutToday` is unchanged (grep shows no `frequencyTier` added to it)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>'out'-tier people are excluded from the manual gap-filling candidate list; new focused test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Availability drawer (client) → Firestore `quarters/{id}` + `people/{id}` | Editor-role user writes quarter-scoped availability (via `setPersonAvailability`) and standing `frequencyTargetN` (via `updatePerson`); both gated by the existing org/editor Firestore rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | mixing standing vs quarter-scoped writes (e.g. writing frequencyTargetN into quarter data) | mitigate | `onSave` routes `frequencyTargetN` through `rosterStore.updatePerson` and everything else through `quartersStore.setPersonAvailability`; enforced by the read_first split guidance and the setPersonAvailability/updatePerson grep criteria. |
| T-14-05-02 | Information Disclosure | 'out'-tier person surfacing as a manual candidate despite being excluded from proposals | mitigate | `availableUnassigned` gains the `frequencyTier !== 'out'` filter; asserted by the "out tier" test (14-RESEARCH Pitfall 3). |
| T-14-05-03 | Input Validation | free-text availability re-introducing dirty data | accept | Controls are UI-constrained (finite serviceDates buttons, fixed 5-option segmented control, typeahead over existing roster ids); no free-text parsing on the primary path, which removes the CSV path's validation burden (only the note is free-text and is never auto-scheduled). |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts` green
- `npx vitest run src/components/__tests__/QuarterGrid.test.ts` green
- `npm run type-check` green
- Drawer never calls `expandBlackoutCell`; save honors the standing/quarter split
</verification>

<success_criteria>
- Drawer reads and writes one person's availability with all D-03 controls, validated against real sample-note patterns
- 'out'-tier people excluded from manual candidate lists, matching auto-proposal exclusion
</success_criteria>

<output>
Create `.planning/phases/14-in-app-quarterly-availability-editor/14-05-SUMMARY.md` when done.
</output>
