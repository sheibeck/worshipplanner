---
phase: 14-in-app-quarterly-availability-editor
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/roster.ts
  - src/utils/scheduler.ts
  - src/utils/__tests__/scheduler.test.ts
autonomous: true
requirements: [D-04, D-05]   # REQUIREMENTS.md absent — CONTEXT.md decision IDs are the traceability keys (per 14-RESEARCH.md §Phase Requirements)
user_setup: []

must_haves:
  truths:
    - "An 'out'-tier person never appears in any generated proposal (calendar or as a pairing-propagation target) for that quarter"
    - "A 'fillin'-tier person is scheduled ONLY when zero regular-tier candidates can fill a given role/date"
    - "A person with no frequencyTier set (pre-migration data) is treated exactly as 'regular' — existing proposals are unchanged"
  artifacts:
    - path: "src/types/roster.ts"
      provides: "FrequencyTier union + PersonQuarterData.frequencyTier/note (optional, quarter-scoped)"
      contains: "FrequencyTier"
    - path: "src/utils/scheduler.ts"
      provides: "Two-pass (regular→fillin) fill loop with 'out' exclusion and 'out'-partner pairing conflict"
      contains: "tierOf"
    - path: "src/utils/__tests__/scheduler.test.ts"
      provides: "fill-in last-resort + out-exclusion test cases"
      contains: "out tier"
  key_links:
    - from: "src/utils/scheduler.ts"
      to: "PersonQuarterData.frequencyTier"
      via: "tierOf(personId) reading pqdById"
      pattern: "frequencyTier"
---

<objective>
Establish the `frequencyTier` data contract and teach the scheduler the two behaviors D-04/D-05 require: `fillin` = last-resort (scheduled only when no regular candidate exists), `out` = fully excluded (never proposed, and excluded as a pairing partner).

Purpose: This is the foundation every downstream tier-consumer (store, drawer, roster table, gap-filling grid) reads. Getting the type + the pure scheduler logic right first means every consumer builds against a settled contract.
Output: Extended `roster.ts` types, extended `scheduler.ts` two-pass fill, new scheduler test cases proving fill-in and out semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-CONTEXT.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-VALIDATION.md

<interfaces>
<!-- Current PersonQuarterData (src/types/roster.ts lines 38-42) — extend in place: -->
<!--   personId: string; blackoutDates: string[]; pairedWith: string[] -->
<!-- proposeQuarterSchedule signature (src/utils/scheduler.ts line 24): -->
<!--   proposeQuarterSchedule(people: Person[], serviceDates: string[], -->
<!--     resolveRolesForDate: (date) => RoleSlotConfig[], personQuarterData: PersonQuarterData[], -->
<!--     existingCalendar?: QuarterCalendar): ProposeResult -->
<!-- Existing closures inside it: isBlackedOut(personId,date), partnersOf(personId), pqdById Map -->
</interfaces>

**DECISION for the executor (field optionality):** Add `frequencyTier` and `note` as OPTIONAL fields (`frequencyTier?: FrequencyTier`, `note?: string`). This is deliberate: existing `PersonQuarterData` literal-construction sites (`src/stores/quarters.ts` lines 129 & 140, `src/components/VolunteerCsvImportModal.vue` line 501, and the test factories) must keep compiling untouched so the build stays green across plan boundaries. Every READ site defaults via `?? 'regular'` / `?? ''` — never assume the field is present (pre-migration Phase 13 data lacks it). Do NOT make these fields required; doing so would force cross-plan edits to files this plan does not own.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FrequencyTier type + optional PersonQuarterData fields</name>
  <files>src/types/roster.ts</files>
  <read_first>
    - src/types/roster.ts — see the current `Person` (lines 13-27, `frequencyTargetN` stays standing/unchanged) and `PersonQuarterData` (lines 38-42) shapes before editing
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/types/roster.ts` (lines 23-54) for the exact extension shape and the standing-vs-quarter-scoped rationale
  </read_first>
  <action>
    Export a new `FrequencyTier` type equal to the string union `'regular' | 'fillin' | 'out'`. Extend the existing `PersonQuarterData` interface with two OPTIONAL fields: `frequencyTier?: FrequencyTier` (quarter-scoped, defaults to `'regular'` on read, resets each new quarter — per D-05/A1) and `note?: string` (free-text quarter note, never auto-scheduled, per D-03/D-07). Leave `Person` unchanged — `frequencyTargetN` remains the standing 1-in-N cadence, meaningful only when tier is `'regular'`. Add a doc comment on `frequencyTier` noting it is quarter-scoped and defaults `'regular'` when absent. Do NOT touch any construction site — optionality is precisely what keeps `applyCsvToQuarter` and `VolunteerCsvImportModal.vue` compiling.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export type FrequencyTier" src/types/roster.ts` returns a match equal to the `'regular' | 'fillin' | 'out'` union
    - `grep -n "frequencyTier?" src/types/roster.ts` and `grep -n "note?" src/types/roster.ts` both match inside the `PersonQuarterData` interface
    - `npm run type-check` exits 0 with no edits to `quarters.ts`, `VolunteerCsvImportModal.vue`, or any test file
  </acceptance_criteria>
  <done>`FrequencyTier` exported; `PersonQuarterData` has optional `frequencyTier`/`note`; type-check green; no construction site touched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Two-pass tier-aware scheduler fill (RED→GREEN)</name>
  <files>src/utils/scheduler.ts, src/utils/__tests__/scheduler.test.ts</files>
  <read_first>
    - src/utils/scheduler.ts — the existing single-pass candidate `while` loop (lines 91-117), the `isBlackedOut`/`partnersOf` closures (lines 32-34), and `propagatePairing`'s partner-eligibility check (lines 64-89) that reports `pairingConflicts` with a `reason` string
    - src/utils/__tests__/scheduler.test.ts — the `makePerson`/`makePQD` factories (lines 5-30) and existing `describe`/`it` structure to mirror; note `makePQD` currently omits `frequencyTier` (fine — it stays optional)
    - .planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md — Architecture Patterns Pattern 2 (lines 228-274) for the full worked two-pass loop and the `propagatePairing` `'out'`-partner extension
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/utils/scheduler.ts` (lines 124-181) for the `tierOf` helper placement and exact diff points
  </read_first>
  <behavior>
    - Fill-in last resort: given one regular-tier and one fillin-tier candidate both eligible for a role/date, the regular-tier person is chosen; the fillin-tier person is chosen only on a date/role where the regular-tier person is blacked out or already assigned and no other regular candidate exists.
    - Out exclusion: an `'out'`-tier person eligible by role never appears in any `calendar` cell nor in `unfilled` as a filler; if a scheduled person is paired with an `'out'`-tier partner, that partner is NOT force-scheduled and a `pairingConflicts` entry with `reason: 'partner out this quarter'` is produced.
    - Default safety: a candidate whose `PersonQuarterData` has no `frequencyTier` (or has no entry at all) is scheduled as `'regular'` — an all-regular fixture produces the identical calendar it did before this change.
  </behavior>
  <action>
    RED first: add test cases to `scheduler.test.ts` under new `it(...)` blocks whose names include the substrings `"fillin"` and `"out tier"` (so `-t "fillin"` / `-t "out tier"` select them). Build fixtures with `makePQD({ personId, frequencyTier: 'fillin' })` etc. Assert the three behaviors above against `proposeQuarterSchedule(...)`'s returned `calendar`, `unfilled`, and `pairingConflicts`.
    GREEN: in `scheduler.ts`, add a `tierOf(personId): FrequencyTier` closure next to `isBlackedOut`/`partnersOf`, returning `pqdById.get(personId)?.frequencyTier ?? 'regular'`. Refactor the per-role `while` loop into a two-pass candidate search: build an `eligible(tier)` filter (adds `tierOf(p.id) === tier` to the existing `active`/role/`!isBlackedOut`/`!alreadyInRole` predicate); take `eligible('regular')` first, and only if it is empty take `eligible('fillin')`. `'out'`-tier people are excluded from both passes unconditionally. Keep the existing deficit-scoring sort ONLY for regular-tier candidates — for fillin-tier candidates set `deficit` to `0` so they are tie-broken purely by `(servedCount asc, name asc)` (their standing `frequencyTargetN` is meaningless). In `propagatePairing`, add — right after the existing `isBlackedOut(partnerId, date)` conflict branch — a branch `if (tierOf(partnerId) === 'out')` that pushes `{ date, personId, partnerId, reason: 'partner out this quarter' }` and `continue`s. Import `FrequencyTier` from `@/types/roster`.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/scheduler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/scheduler.test.ts -t "fillin"` exits 0
    - `npx vitest run src/utils/__tests__/scheduler.test.ts -t "out tier"` exits 0
    - `npx vitest run src/utils/__tests__/scheduler.test.ts` full file exits 0 (no pre-existing case regressed)
    - `grep -n "tierOf" src/utils/scheduler.ts` matches; `grep -n "partner out this quarter" src/utils/scheduler.ts` matches
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Two-pass fill + out-exclusion + out-partner pairing conflict implemented; new fillin/out test cases green; full scheduler suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is pure in-memory type + scheduler logic; no I/O, no network, no persistence, no user input crosses a boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | `proposeQuarterSchedule` tier logic | accept | Pure deterministic function over already-authorized store data; malformed/absent `frequencyTier` is defaulted to `'regular'` via `?? 'regular'`, so no crash or privilege change is possible. Covered by the default-safety test case. |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/scheduler.test.ts` green (incl. new fillin + out cases)
- `npm run type-check` green
- No construction site (`quarters.ts`, `VolunteerCsvImportModal.vue`, test factories) modified
</verification>

<success_criteria>
- `FrequencyTier` type exported and `PersonQuarterData` carries optional `frequencyTier`/`note`
- Scheduler proposes fill-in people only as a last resort and never proposes or force-pairs an 'out'-tier person
- Absent-tier data behaves identically to prior behavior (no regression)
</success_criteria>

<output>
Create `.planning/phases/14-in-app-quarterly-availability-editor/14-01-SUMMARY.md` when done.
</output>
