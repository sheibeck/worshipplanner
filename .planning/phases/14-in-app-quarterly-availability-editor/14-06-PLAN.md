---
phase: 14-in-app-quarterly-availability-editor
plan: 06
type: execute
wave: 4
depends_on: [14-01, 14-05]
files_modified:
  - src/components/AvailabilityRosterTable.vue
  - src/views/QuarterView.vue
autonomous: false
requirements: [D-02, D-03]   # REQUIREMENTS.md absent — CONTEXT.md decision IDs are the traceability keys
user_setup: []

must_haves:
  truths:
    - "On the Quarter/Schedule page, after selecting a quarter, a full-width scannable volunteer roster table lists each active person with roles, frequency, unavailable-summary, pairing, and status"
    - "Clicking a person's row opens the availability drawer for that person in the selected quarter"
    - "Search and All/Needs input/Out this quarter filters narrow the roster table"
    - "Editing and saving in the drawer updates the row without a page reload"
  artifacts:
    - path: "src/components/AvailabilityRosterTable.vue"
      provides: "Read-only roster table with search + filter chips that emits select(personId)"
      min_lines: 60
    - path: "src/views/QuarterView.vue"
      provides: "AvailabilityRosterTable + AvailabilityDrawer mounted, controlled by openPersonId"
      contains: "AvailabilityDrawer"
  key_links:
    - from: "src/views/QuarterView.vue"
      to: "AvailabilityDrawer"
      via: "openPersonId ref set by AvailabilityRosterTable @select"
      pattern: "openPersonId"
---

<objective>
Mount the availability editing surface on the Quarter/Schedule page (D-02): a full-width roster table (D-03) whose rows open the Plan 05 drawer per person for the selected quarter. This is the wiring that makes the in-app editor the primary availability input path.

Purpose: D-02 locates availability entry where the quarter is chosen and scheduling happens. The table keeps the roster scannable; the drawer slides in on row click (Variant A).
Output: `AvailabilityRosterTable.vue` + `QuarterView.vue` wiring; end-to-end human verification against the real sample notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-CONTEXT.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md
@.planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md
@.planning/sketches/001-availability-editor/index.html
@docs/Sample Frequency Notes.csv

<interfaces>
<!-- AvailabilityDrawer.vue (Plan 14-05): props { quarterId: string|null, personId: string|null }, emit close -->
<!-- rosterStore.activePeople: Person[]; quartersStore.getQuarter(id).personQuarterData[personId]? -->
<!-- Sketch display helpers to port (index.html): blackoutSummary (281-295), freqBadge (297-304), statusPill (305-310), roleChips (311), pairChipsHtml/firstName (325, 408-411) -->
<!-- Sketch table columns (line 326): Volunteer | Roles | Frequency | Unavailable | Pairing | Status | › -->
<!-- Sketch toolbar (174-179): search input + chips All / Needs input / Out this quarter -->
<!-- QuarterView.vue: existing VolunteerCsvImportModal mount + csvModalOpen ref (lines 288-293, 432-437); selectedQuarter; <template v-if="selectedQuarter"> at line 107; QuarterGrid mount ~273-279 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AvailabilityRosterTable.vue — scannable roster with search + filter chips</name>
  <files>src/components/AvailabilityRosterTable.vue</files>
  <read_first>
    - .planning/sketches/001-availability-editor/index.html — Variant A table + toolbar markup and the display helpers to port: `rowHtml`/`tableHead`/`renderTable` (314-332), `blackoutSummary` (281-295), `freqBadge` (297-304), `statusPill` (305-310), `roleChips` (311), and the toolbar (174-179). This is the design source.
    - src/components/QuarterGrid.vue — helper-function style (lines 243-259, e.g. `personName`) and the dark-theme table markup conventions to match
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/components/AvailabilityRosterTable.vue` (lines 298-321) for the analog mapping
  </read_first>
  <action>
    Create `AvailabilityRosterTable.vue` (`<script setup lang="ts">`). Props: `quarter: Quarter | null`. Emit `select: [personId: string]`. Render the sketch's table (columns Volunteer | Roles | Frequency | Unavailable | Pairing | Status | ›) over `rosterStore.activePeople`, reading each person's quarter-scoped data from `props.quarter?.personQuarterData[person.id]` (default missing `frequencyTier`/`note`/`blackoutDates`/`pairedWith` via `?? 'regular'` / `?? ''` / `?? []`). Port the sketch's display helpers to Vue computed/methods: `blackoutSummary` (detect dominant Nth-Sunday pattern + extra-date count), `freqBadge` ("Every week · ≈N" etc., "fill-in", "—" for out), `statusPill` (Regular/Fill-in/Out this quarter), role chips, and a pairing summary ("↔ Dean, Lisa"). A row click emits `select(person.id)`. Add a toolbar: a search `<input>` (local ref) filtering by name, and three filter chips — `All`, `Needs input` (people with no `personQuarterData` entry yet this quarter), `Out this quarter` (`frequencyTier === 'out'`) — driving a `computed` filtered list. No table/typeahead library; plain refs + computed. Match dark-theme utility classes used elsewhere.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AvailabilityRosterTable.vue` exists and emits `select` with a person id on row click (grep for `emit('select'` or a typed `defineEmits` `select`)
    - Renders one row per `rosterStore.activePeople` entry with the six data columns
    - Filter chips include "Needs input" and "Out this quarter" logic (grep for `frequencyTier` and a no-entry/needs-input predicate)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Roster table lists active people with per-quarter summaries, supports search + the three filter chips, and emits the selected person id.</done>
</task>

<task type="auto">
  <name>Task 2: QuarterView.vue — mount roster table + drawer</name>
  <files>src/views/QuarterView.vue</files>
  <read_first>
    - src/views/QuarterView.vue — the `VolunteerCsvImportModal` mount + `csvModalOpen` ref pattern (lines 288-293, 432-437), the imports block (lines 305-314), `selectedQuarter`, the `<template v-if="selectedQuarter">` block (line 107), and the `QuarterGrid` mount (~273-279) for placement
    - .planning/phases/14-in-app-quarterly-availability-editor/14-PATTERNS.md — §`src/views/QuarterView.vue` (lines 413-457) for the exact mount + `openPersonId` wiring
    - .planning/phases/14-in-app-quarterly-availability-editor/14-RESEARCH.md — Pattern 1 (drawer controlled by an `openPersonId` ref at the view level)
  </read_first>
  <action>
    Import `AvailabilityRosterTable` and `AvailabilityDrawer`. Add `const openPersonId = ref<string | null>(null)`. Inside the `<template v-if="selectedQuarter">` block, add a "Volunteer Availability" section mounting `<AvailabilityRosterTable :quarter="selectedQuarter" @select="openPersonId = $event" />` (co-located with the scheduling controls per D-02 — place near the QuarterGrid section). Mount `<AvailabilityDrawer :quarter-id="selectedQuarter?.id ?? null" :person-id="openPersonId" @close="openPersonId = null" />`. Rely on the already-subscribed `quartersStore`/`rosterStore` (no new subscription). Do not alter existing CSV-import or QuarterGrid wiring — the CSV path stays as a secondary option.
  </action>
  <verify>
    <automated>npm run type-check && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AvailabilityRosterTable" src/views/QuarterView.vue` and `grep -n "AvailabilityDrawer" src/views/QuarterView.vue` both match (import + template)
    - `grep -n "openPersonId" src/views/QuarterView.vue` matches the ref, the `@select` binding, and the drawer `:person-id`
    - Existing `VolunteerCsvImportModal` mount is still present (CSV path preserved)
    - `npm run type-check` exits 0 and `npm run test:unit` exits 0 (full suite)
  </acceptance_criteria>
  <done>Quarter page shows the availability roster table and opens the drawer per person; full unit suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the in-app availability editor end-to-end against real sample notes</name>
  <action>Human verifies the roster table + drawer on the Quarter page by reproducing each real pattern from docs/Sample Frequency Notes.csv (frequency presets, Nth-Sunday chips, range block, bidirectional pairing add/remove, out-this-quarter exclusion) and confirming saves round-trip.</action>
  <what-built>The in-app quarterly availability editor: a roster table on the Quarter page whose rows open a right drawer to set frequency, Sundays-only blackout dates (Nth-Sunday chips + range block), must-serve-with pairings, and a quarter note — writing straight into PersonQuarterData.</what-built>
  <how-to-verify>
    Open the Quarter/Schedule page, select the active quarter, and reproduce these real patterns from `docs/Sample Frequency Notes.csv` using the drawer controls (confirm each is expressible without falling back to CSV):
    1. Anne — Monthly, unavailable 1st Sundays: pick "Monthly", click the "1st Sundays" chip. Save; confirm the row summary reflects it.
    2. Krystyn — available ONLY the 2nd week: block the 1st/3rd/4th/5th Sunday chips (leave 2nd). Confirm the calendar shows only 2nd Sundays open.
    3. David DeBoer — fill-in only: pick "As-needed (fill-in)"; confirm status pill shows "Fill-in".
    4. John Segard — out this quarter: pick "Out this quarter"; confirm status pill shows "Out this quarter" and he is absent from a regenerated proposal AND from a manual gap-fill candidate list.
    5. Julia Woodard — must serve with Dean/Lisa + gone by Aug 16: add Dean and Lisa pairing chips (confirm reciprocal chips appear on their rows), and use the range block to block Sundays from Aug 16 through quarter end.
    6. Remove one pairing chip and confirm the partner's reciprocal chip also disappears.
  </how-to-verify>
  <resume-signal>Type "approved" if every pattern is expressible and saves correctly, or describe any pattern that could not be entered or any save/round-trip issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quarter page (client) → Firestore `quarters/{id}` + `people/{id}` | Editor-role user opens per-person drawers and saves availability; writes gated by the existing org/editor Firestore rule (no new collection). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Access Control | roster table / drawer exposing another org's people | accept | Both read from the already-org-scoped `rosterStore`/`quartersStore` subscriptions; no new data path or collection is introduced, so the existing `organizations/{orgId}` editor-only rule fully applies. Re-confirmed by `npm run test:rules` at the phase gate. |
| T-14-06-02 | Tampering | pairing edit round-trip failing to reflect in the UI (stale row) | mitigate | Rows read live store state updated by `setPersonAvailability`'s reciprocal writes (Plan 14-03); human-verify step 6 explicitly checks reciprocal chip removal. |
</threat_model>

<verification>
- `npm run type-check` green; `npm run test:unit` full suite green
- `npm run test:rules` green (phase gate — no new collection, existing editor rule covers new fields)
- Human-verify: all sample-note patterns expressible; save + reciprocal pairing round-trip correct
</verification>

<success_criteria>
- The in-app editor is the primary availability input path on the Quarter page
- Every real sample-note pattern is expressible with the constrained controls (no CSV fallback needed)
- Edits persist and round-trip live into the roster table
</success_criteria>

<output>
Create `.planning/phases/14-in-app-quarterly-availability-editor/14-06-SUMMARY.md` when done.
</output>
