---
phase: 92-monitor-configuration-screen
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - src/views/__tests__/MonitorSetupView.test.ts
  - src/components/__tests__/AppSidebar.test.ts
autonomous: true
requirements: [R267, R268, R269]

must_haves:
  truths:
    - "Automated jsdom tests prove MonitorSetupView renders the three permission paths: granted (getScreenDetails resolves with fake ScreenDetailed[]), denied (rejects), and unavailable (property absent from window), matching R267/R269 first-class states."
    - "A test proves role assignment persists via monitorConfig and a reload with an unchanged layout renders the 'already configured' State B2 (matchMapping 'matched'), satisfying R268 silent reuse."
    - "A test proves that when the saved layout no longer matches the live screens, State B3 (layout-changed banner + blank grid) renders instead of pre-guessing, satisfying R268 re-prompt-on-change."
    - "A test asserts the permission call is invoked SYNCHRONOUSLY from the Detect click handler — getScreenDetails is called before any awaited microtask resolves (before await flushPromises), guarding the Pitfall 1 user-activation contract."
    - "A test proves Save stays disabled until Audience and Confidence point at two DIFFERENT monitors, and that on a simulated storage no-op the non-blocking amber not-persisted warning renders instead of the green 'Saved for this device' (resolves the 92-UI-SPEC save round-trip item)."
    - "A test proves the AppSidebar 'Monitor Setup' nav entry is visible to a non-editor org member (isEditor false, orgId set) and hidden when orgId is null — verifying the R275 orgId gate divergence from the editor-only neighbors."
  artifacts:
    - src/views/__tests__/MonitorSetupView.test.ts
    - src/components/__tests__/AppSidebar.test.ts
  key_links:
    - "vi mock / stub of window.getScreenDetails driving the granted/denied/unavailable branches."
    - "jsdom localStorage (clear() in beforeEach) as the real persistence backend for the round-trip and match/reprompt assertions."
    - "The auth-store getter mock (from SettingsView.test.ts, minus Firestore) toggling orgId/isEditor to prove the nav gate."
---

<objective>
Author the behavioral test coverage for the Monitor Setup screen and its nav entry built in 92-01.
Prove the three permission paths render, role assignment persists and reloads matched, a changed
layout re-prompts blank, Save validates two-different-monitors and honors the save round-trip
warning, the permission call fires synchronously from the click handler, and the nav entry is
visible to any org member (not just editors).

Purpose: Lock the R267/R268/R269 behavior with automated jsdom tests so the autonomous run has proof
for the mocked paths (real permission grant/deny + real multi-monitor hardware + the drag/fullscreen
fallback remain HUMAN-UAT items — see verification note).

Output: `src/views/__tests__/MonitorSetupView.test.ts` and `src/components/__tests__/AppSidebar.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-monitor-configuration-screen/92-CONTEXT.md
@.planning/phases/92-monitor-configuration-screen/92-UI-SPEC.md
@.planning/phases/92-monitor-configuration-screen/92-PATTERNS.md
@src/utils/monitorConfig.ts
@src/views/MonitorSetupView.vue
@src/components/AppSidebar.vue
@src/views/__tests__/SettingsView.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author MonitorSetupView.test.ts (three paths + persistence round-trip + synchronous-call)</name>
  <files>src/views/__tests__/MonitorSetupView.test.ts</files>
  <read_first>
    - src/views/__tests__/SettingsView.test.ts (mount + flushPromises + enableAutoUnmount harness; the module-scope getter-based @/stores/auth mock — reuse this, DROP the firebase/firestore and @/firebase blocks since monitorConfig is pure/localStorage)
    - src/views/MonitorSetupView.vue (the state discriminant, the Detect handler, the save-outcome states, the testids/labels it exposes)
    - src/utils/monitorConfig.ts (MONITOR_CONFIG_STORAGE_KEY, computeFingerprint, MonitorMapping shape — for seeding a matching/stale saved mapping and reading back the persisted value)
    - .planning/phases/92-monitor-configuration-screen/92-CONTEXT.md (Verification section — the exact mock strategy for the three cases)
  </read_first>
  <action>
    Create MonitorSetupView.test.ts using the SettingsView.test.ts harness shape (describe/it, mount +
    flushPromises, enableAutoUnmount(afterEach)) but with a MUCH smaller mock: reuse the module-scope
    getter-based @/stores/auth mock (orgId, isEditor, orgName) and OMIT the firebase/firestore and
    @/firebase mocks entirely — monitorConfig is pure localStorage. Do NOT mock @/utils/monitorConfig;
    exercise the real module against jsdom's localStorage, calling localStorage.clear() in beforeEach.

    Provide a helper that builds fake screens (objects shaped like ScreenDetailed: label, width,
    height, left, top, isPrimary) and installs window.getScreenDetails for each case. Granted: define
    window.getScreenDetails as a vi.fn returning a resolved promise of an object whose screens is the
    fake array and which supports addEventListener (a stub is fine). Denied: window.getScreenDetails is
    a vi.fn returning a rejected promise. Unavailable: delete window.getScreenDetails (or ensure the
    property is absent) so the feature-detect fails. Restore/cleanup window.getScreenDetails in
    afterEach so cases do not bleed.

    Write these tests:
    - Unavailable path: with getScreenDetails absent, mount and assert the fallback panel renders with
      the unavailable copy and NO Detect button is present.
    - Denied path: with getScreenDetails rejecting, click Detect (or auto-detect), await flushPromises,
      and assert the fallback panel renders with the denied copy.
    - Granted / fresh (State B): with getScreenDetails resolving two distinct screens and localStorage
      empty, assert the monitor grid renders one card per screen with nothing pre-selected.
    - Synchronous-call contract: in the granted case, trigger the Detect click handler and assert
      window.getScreenDetails was called BEFORE awaiting flushPromises (i.e. the mock's call count is 1
      synchronously after the click, before any awaited microtask) — encoding the Pitfall 1 rule.
    - Persistence + matched reload (State B2): assign Audience to one card and Confidence to the other,
      click Save, await, then assert localStorage under MONITOR_CONFIG_STORAGE_KEY holds a mapping whose
      assignments set-equals the chosen fingerprints/roles AND the confirmation shows 'Saved for this
      device'. Then remount with the SAME fake screens and assert State B2 ('already configured') renders
      without the editable grid.
    - Layout-changed reprompt (State B3): seed localStorage with a saved mapping (via the real
      saveMapping or a direct setItem of a valid MonitorMapping) whose fingerprints do NOT match the
      live fake screens, mount granted, and assert the amber layout-changed banner renders above an
      editable grid with nothing pre-selected.
    - Same-monitor validation: assign both Audience and Confidence to the same card and assert the Save
      button is disabled and the inline validation copy is shown.
    - Save round-trip warning: force a persistence no-op (e.g. stub Storage.prototype.setItem to throw,
      or pass a failing storage seam) so loadMapping read-back does not reflect the write, click Save,
      await, and assert the non-blocking amber not-persisted warning renders and 'Saved for this device'
      does NOT appear.
    Prefer querying by data-testid / role / accessible text over brittle class selectors.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/MonitorSetupView.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - All three permission paths (granted/denied/unavailable) have passing assertions.
    - The synchronous-call test asserts getScreenDetails fires before any awaited flushPromises.
    - Persistence test reads MONITOR_CONFIG_STORAGE_KEY back and a same-layout remount renders State B2.
    - A stale-mapping mount renders State B3 with a blank grid; same-monitor selection disables Save;
      a forced storage no-op renders the not-persisted warning instead of the green confirmation.
    - `npx vitest run src/views/__tests__/MonitorSetupView.test.ts` passes and `npm run type-check` is clean.
  </acceptance_criteria>
  <done>The view's granted/denied/unavailable, persistence round-trip, match/reprompt, validation, and synchronous-call behaviors are proven by passing jsdom tests.</done>
</task>

<task type="auto">
  <name>Task 2: Author AppSidebar.test.ts proving the orgId-gated Monitor Setup nav entry</name>
  <files>src/components/__tests__/AppSidebar.test.ts</files>
  <read_first>
    - src/components/AppSidebar.vue (the navItems computed; the new orgId-gated Monitor Setup item; useRoute/useRouter/router-link usage)
    - src/views/__tests__/SettingsView.test.ts (the module-scope getter-based @/stores/auth mock shape to reuse; how getters are flipped between assertions)
  </read_first>
  <action>
    Create AppSidebar.test.ts mounting AppSidebar with a router-link global stub (a component stub or
    RouterLinkStub) and stubbed useRoute/useRouter so the computed navItems evaluates without a real
    router. Reuse the module-scope getter-based @/stores/auth mock from SettingsView.test.ts, exposing
    at least orgId, isEditor, isSuperAdmin, orgName, user, and a logout fn — set the mutable backing
    vars per test. Pass the required sidebarOpen prop.

    Tests:
    - Viewer visibility: with isEditor false and orgId set to a non-null value, mount and assert a nav
      link to '/monitor-setup' labeled 'Monitor Setup' is present (proving the divergence: it does NOT
      require isEditor, unlike the Settings/Admins items which must be ABSENT in this same state).
    - Hidden without org: with orgId null, assert no '/monitor-setup' link is rendered.
    Query by the link's `to`/href and its visible text rather than by index, so reordering nav items
    does not break the test.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AppSidebar.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - With isEditor false and orgId set, the Monitor Setup link to '/monitor-setup' is present while
      the editor-only Settings/Admins links are absent.
    - With orgId null, no Monitor Setup link renders.
    - `npx vitest run src/components/__tests__/AppSidebar.test.ts` passes and `npm run type-check` is clean.
  </acceptance_criteria>
  <done>The Monitor Setup nav entry's orgId gate (any member, not editor-only) is proven by passing tests.</done>
</task>

</tasks>

<verification>
- `npx vitest run` shows the same baseline as before (src/storage.rules.test.ts) plus the two new
  passing test files; no `--dir src`.
- `npm run type-check` (vue-tsc --build) passes clean.
- HUMAN-UAT (deferred, mark human_needed): real window-management grant/deny on Chrome/Edge, actual
  multi-monitor detection, long real-world monitor-label truncation, 3+ monitor overflow, and the
  drag+fullscreen manual fallback on real hardware — none provable by jsdom unit tests; the verifier
  flags these and the autonomous run defers them to the milestone-end UAT pass.
</verification>

<success_criteria>
- The three permission paths, persistence round-trip, matched reuse, changed-layout reprompt,
  two-different-monitor validation, save round-trip warning, and synchronous-call contract are all
  covered by passing automated tests (R267, R268, R269).
- The orgId-gated nav entry is proven visible to a non-editor org member (R267/R275).
- No baseline regression in `npx vitest run`; type-check clean.
</success_criteria>

<output>
Create `.planning/phases/92-monitor-configuration-screen/92-02-SUMMARY.md` when done.
</output>
