---
phase: 119-architecture-correctness-batching-store-ownership-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/services.ts
  - src/stores/songs.ts
  - src/components/PcImportModal.vue
  - src/composables/useSlideshowAssembly.ts
  - src/stores/songLyrics.ts
  - src/stores/__tests__/services.test.ts
  - src/stores/__tests__/songs.test.ts
  - src/components/__tests__/PcImportModal.test.ts
  - src/composables/__tests__/useSlideshowAssembly.test.ts
  - src/stores/__tests__/songLyrics.test.ts
autonomous: true
requirements: [R349, R350, R351]

must_haves:
  truths:
    - "A single failing song write inside recomputeLastUsedFor no longer prevents the remaining songs from getting their correct lastUsedAt, and the failed song id(s) are logged (R349)"
    - "A large Planning Center import writes in writeBatch chunks of <=499 ops, and a failing chunk no longer stops later chunks (R350)"
    - "The PC import UI surfaces per-song added/updated/failed counts instead of one bad write silently stopping the rest (R350)"
    - "The lyrics query has a single source of truth shared by useSlideshowAssembly's default subscriber and songLyricsStore.subscribeLyrics, with no limit(1) drift (R351)"
  artifacts:
    - src/stores/services.ts
    - src/stores/songs.ts
    - src/components/PcImportModal.vue
    - src/composables/useSlideshowAssembly.ts
    - src/stores/songLyrics.ts
  key_links:
    - "PcImportModal.onConfirmImport consumes upsertSongs' returned per-song summary"
    - "defaultLyricsSubscriber and subscribeLyrics both build the lyrics query via one shared lyricsQuery()"
---

<objective>
Fix the three data-layer correctness/robustness findings that live entirely in the stores + their
direct consumers: per-item failure isolation in the last-used recompute (R349/ARCH-011), batched +
per-song-reported Planning Center song import (R350/ARCH-014), and a de-duplicated lyrics subscription
with no limit(1) drift (R351/ARCH-009).

Purpose: Close three behavior-preserving robustness/consistency gaps flagged by the v2.8 architectural
review without changing ordinary app behavior.
Output: Hardened `recomputeLastUsedFor`, `upsertSongs`, PC import UI feedback, and a single shared lyrics
query — all with regression tests, the app suite staying green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Per-finding detail (locations, problem, recommendation)
@.planning/milestones/v2.8-phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md
@.planning/phases/119-architecture-correctness-batching-store-ownership-fixes/119-CONTEXT.md

# Files this plan changes + the sibling patterns to copy
@src/stores/services.ts
@src/stores/songs.ts
@src/components/PcImportModal.vue
@src/composables/useSlideshowAssembly.ts
@src/stores/songLyrics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-item failure isolation in recomputeLastUsedFor (R349)</name>
  <files>src/stores/services.ts, src/stores/__tests__/services.test.ts</files>
  <behavior>
    - When songStore.updateSong rejects for one songId in affectedSongIds, every other songId in the list still receives its updateSong call (the loop does not abort early).
    - The specific failed song id(s) are logged via console.error.
    - The computeLastUsedDate derivation and the Timestamp.fromMillis conversion are unchanged; a null maxDate still writes lastUsedAt: null.
    - Callers markAsPlanned / reopenService keep their existing outer try/catch (swallow-and-continue intent preserved).
  </behavior>
  <action>
    In recomputeLastUsedFor (src/stores/services.ts, the affectedSongIds loop) wrap each per-song
    `await songStore.updateSong(songId, { lastUsedAt })` in its own try/catch so one song's write
    failing no longer aborts the rest of the loop. On a caught error, collect the songId into a local
    failed-ids array and console.error the failed id(s) after the loop (short comment citing R349 /
    ARCH-011 per the CONVENTIONS comment rule — rationale belongs in phase docs, not a long inline
    comment). Do NOT change computeLastUsedDate, the Timestamp conversion, or the callers' outer
    try/catch. This preserves the existing swallow-and-continue intent while isolating per item.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts</automated>
  </verify>
  <done>One song's updateSong rejection no longer prevents the remaining affectedSongIds from being written; failed song id(s) are logged; existing services store tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Batch + per-song-reported Planning Center import (R350)</name>
  <files>src/stores/songs.ts, src/components/PcImportModal.vue, src/stores/__tests__/songs.test.ts, src/components/__tests__/PcImportModal.test.ts</files>
  <behavior>
    - upsertSongs writes via writeBatch chunks of at most 499 combined ops (batch.update for an existing match, batch.set for a new doc), mirroring the sibling importSongs chunking.
    - Each batch.commit() is isolated in its own try/catch so a failing chunk does not stop later chunks.
    - upsertSongs returns a summary { added, updated, failed } where failed lists the songs that were in a rejected chunk (title + error).
    - Re-import of the same list stays idempotency-safe (byPcSongId -> byCcliNumber -> byTitle match logic unchanged).
    - PcImportModal's 'done' step shows added / updated / failed counts; it routes to 'error' only on a total or non-recoverable failure.
  </behavior>
  <action>
    Refactor upsertSongs (src/stores/songs.ts) to chunk its writes into writeBatch batches of <=499
    combined operations, following the sibling importSongs pattern in the same file. Resolve each
    incoming song's existing match exactly as today (byPcSongId, then byCcliNumber, then byTitle) and
    build the same updateData / create payload as today, but stage them with batch.update(existingRef,
    updateData) or batch.set(doc(collection(db,'organizations',orgId,'songs')), createData) rather than
    awaiting a per-song updateDoc/addDoc. Wrap each batch.commit() in try/catch so a rejected chunk does
    not abort the remaining chunks (this is the fix for "one bad write silently stopping the rest").
    Return a summary object { added, updated, failed: Array of { title, error } } that tracks which
    songs were in a failed chunk. Keep all writes under organizations/{orgId}/songs — auth/org scoping
    is unchanged. In PcImportModal.onConfirmImport, consume the returned summary and extend the existing
    'done' step count line to also show a failed count (with the failed titles when failed > 0); keep
    routing to the 'error' step only for a thrown/non-recoverable failure. Short comment cites R350 /
    ARCH-014.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/songs.test.ts src/components/__tests__/PcImportModal.test.ts</automated>
  </verify>
  <done>upsertSongs writes in <=499-op writeBatch chunks, isolates a failing chunk, and returns added/updated/failed counts; PcImportModal shows them; the existing upsertSongs tests are migrated to read writes from mockBatchOps (batch.set/batch.update) and the full songs + PcImportModal suites are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Single source of truth for the lyrics query (R351)</name>
  <files>src/stores/songLyrics.ts, src/composables/useSlideshowAssembly.ts, src/composables/__tests__/useSlideshowAssembly.test.ts, src/stores/__tests__/songLyrics.test.ts</files>
  <behavior>
    - songLyricsStore.subscribeLyrics and useSlideshowAssembly's defaultLyricsSubscriber build their lyrics query from one shared function; the limit(1) that existed only in the composable is gone.
    - Newest-createdAt-wins is preserved: a multi-doc, createdAt-desc snapshot resolves to docs[0] in the composable, exactly as the store already does.
    - The composable keeps its empty -> null result and its performanceOrder default-to-[] behavior, and its injectable LyricsSubscriber seam is intact.
  </behavior>
  <action>
    Eliminate the limit(1) query drift by giving both call sites one shared query builder. In
    src/stores/songLyrics.ts, export a small function (e.g. lyricsQuery(orgId, songId)) that returns
    query(collection(db,'organizations',orgId,'songs',songId,'lyrics'), orderBy('createdAt','desc'))
    with NO limit, and have subscribeLyrics call it. In src/composables/useSlideshowAssembly.ts,
    replace defaultLyricsSubscriber's inline query (which currently adds limit(1)) with a call to the
    shared lyricsQuery, and take the newest doc client-side via snap.docs[0] — behavior-preserving,
    since newest-createdAt-wins is identical to the old limit(1) result and matches the store. Preserve
    the empty -> null and performanceOrder default behavior and the injectable LyricsSubscriber option.
    Short comment cites R351 / ARCH-009 (single source of truth for the query).
  </action>
  <verify>
    <automated>npx vitest run src/composables/__tests__/useSlideshowAssembly.test.ts src/stores/__tests__/songLyrics.test.ts</automated>
  </verify>
  <done>defaultLyricsSubscriber and subscribeLyrics derive their query from one shared lyricsQuery(); the limit(1) drift is gone; newest-wins behavior is preserved; both suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client SDK -> Firestore | Song/lyrics/service reads and writes cross here; scoping is enforced by firestore.rules (unchanged this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-01 | Tampering | upsertSongs batching (R350) | low | accept | Writes stay under organizations/{orgId}/songs; batching changes only how writes are grouped, not the target path or the firestore.rules gate — no auth surface change. |
| T-119-02 | Information Disclosure | shared lyricsQuery (R351) | low | accept | The shared query targets the same org-scoped lyrics subcollection as before; dropping limit(1) reads more docs from the same authorized path, no new data exposed. |

No npm/pip/cargo installs in this plan — no supply-chain (T-*-SC) checkpoint required; no new external surface.
</threat_model>

<verification>
- Full app suite green: `npx vitest run` (excludes firestore rules + render-service + storage.rules per vite.config.ts — a bare run should be fully green).
- Types clean: `npm run type-check` (vue-tsc --build; typechecks test files too, per CLAUDE.md).
</verification>

<success_criteria>
- recomputeLastUsedFor isolates a per-song failure and logs failed ids (R349).
- upsertSongs batches (<=499) with per-chunk isolation and returns per-song added/updated/failed counts surfaced in PcImportModal (R350).
- The lyrics query is defined once and shared; no limit(1) drift; newest-wins preserved (R351).
- Behavior otherwise unchanged; app suite + type-check green.
</success_criteria>

<output>
Create `.planning/phases/119-architecture-correctness-batching-store-ownership-fixes/119-01-SUMMARY.md` when done
</output>
