---
phase: 135-dashboard-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["135-01"]
files_modified:
  - src/views/DashboardView.vue
  - src/utils/unconfirmedAssignments.ts
  - src/utils/__tests__/unconfirmedAssignments.test.ts
  - src/composables/useUnconfirmedVolunteers.ts
autonomous: true
requirements: [R417]
must_haves:
  truths:
    - "The dashboard shows an Unconfirmed volunteers card listing who has not yet confirmed for upcoming services, reading the Phase 133 confirmations subcollection + the rehearseAccess roleAssignmentsByEmailLower projection (R417)."
    - "An assignment counts as unconfirmed when it has NO confirmation doc or the doc's status is needsReconfirmation (reusing ConfirmationStatus/confirmationKey verbatim from @/utils/confirmations)."
    - "Fan-out reads are bounded to the next ≤6 upcoming Planned services (same window discipline as the feed); listeners are torn down when orgId or the service window changes."
    - "The card is ALWAYS shown when there are upcoming services, with a positive empty sub-state ('Everyone's confirmed for the next N services.') when nothing is unconfirmed."
    - "The list caps at 8 rows with a '+N more — view roster' trailing link; long person/service names truncate (min-w-0 truncate)."
  artifacts:
    - "src/utils/unconfirmedAssignments.ts — pure diff of roleAssignmentsByEmailLower vs confirmation statuses"
    - "src/utils/__tests__/unconfirmedAssignments.test.ts — unit tests (no-doc, needsReconfirmation, confirmed excluded)"
    - "src/composables/useUnconfirmedVolunteers.ts — bounded ≤6-service fan-out (rehearseAccess doc + confirmations subcollection) with lifecycle teardown"
    - "src/views/DashboardView.vue — Unconfirmed volunteers attention card"
  key_links:
    - "roleAssignmentsByEmailLower (rehearseAccess doc) + confirmations subcollection → unconfirmedAssignments diff"
    - "emailLower → rosterStore.people (lowercased email) → person display name"
    - "each unconfirmed row router-links to /services/{serviceId}"
---

<objective>
Add the Unconfirmed volunteers attention card to the dashboard (R417): for the next ≤6 upcoming Planned
services, surface every assigned volunteer who has not confirmed, reading the Phase 133 confirmations
subcollection and the rehearseAccess roleAssignmentsByEmailLower projection. Bounded, cost-hardened fan-out.

Purpose: give a planner an at-a-glance list of who still needs to confirm, linking straight to the service.
Output: a pure diff util (tested), a bounded read composable, and the attention card in DashboardView.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-dashboard-overhaul/135-CONTEXT.md
@.planning/phases/135-dashboard-overhaul/135-UI-SPEC.md
@src/views/DashboardView.vue
@src/utils/confirmations.ts
@src/utils/rehearseAccess.ts
</context>

<artifacts_produced>
- src/utils/unconfirmedAssignments.ts (new) — `unconfirmedAssignments(roleAssignmentsByEmailLower,
  confirmationStatuses)` returning `{ emailLower, roleId, roleName }[]`.
- src/utils/__tests__/unconfirmedAssignments.test.ts (new).
- src/composables/useUnconfirmedVolunteers.ts (new) — bounded ≤6 upcoming-Planned fan-out over rehearseAccess
  docs + confirmations subcollections; returns enriched unconfirmed rows + a loading/error flag.
- src/views/DashboardView.vue (modified) — Unconfirmed volunteers attention card inside the populated feed branch.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure unconfirmed-assignment diff util</name>
  <files>src/utils/unconfirmedAssignments.ts, src/utils/__tests__/unconfirmedAssignments.test.ts</files>
  <read_first>
    - src/utils/confirmations.ts:14-42 (ConfirmationStatus = 'confirmed' | 'needsReconfirmation'; 'unconfirmed'
      is the implicit no-doc default; confirmationKey(roleId, emailLower) = `${roleId}_${emailLower}`)
    - src/utils/rehearseAccess.ts:83-91 (roleAssignmentsByEmailLower?: Record<emailLower, {roleId, roleName}[]>
      — optional; absent on pre-Phase-133 docs until relocked)
    - src/views/ServiceEditorView.vue:4319-4328 (confirmationStatusFor — the existing 'unconfirmed' default
      semantics to mirror: no doc OR no email → unconfirmed)
  </read_first>
  <behavior>
    - unconfirmedAssignments(roleAssignmentsByEmailLower, confirmationStatuses: Map<string, ConfirmationStatus>)
      returns a `{ emailLower, roleId, roleName }[]`.
    - Test: an (emailLower, roleId) pair with NO entry in the statuses map → included (implicit unconfirmed).
    - Test: a pair whose status is 'needsReconfirmation' → included.
    - Test: a pair whose status is 'confirmed' → excluded.
    - Test: roleAssignmentsByEmailLower undefined/empty → returns [] (graceful for pre-Phase-133 docs).
    - Test: multiple roles for one email → one row per (email, role) pair.
    - Order is stable (input iteration order) so the caller can slice deterministically.
  </behavior>
  <action>
    Create src/utils/unconfirmedAssignments.ts as a pure, store-free module. Import ConfirmationStatus and
    confirmationKey from '@/utils/confirmations' — reuse confirmationKey verbatim to build the lookup key; do NOT
    reimplement the `${roleId}_${emailLower}` join. Iterate roleAssignmentsByEmailLower (guard undefined → []),
    and for each { roleId, roleName } under an emailLower, look up confirmationStatuses.get(confirmationKey(roleId,
    emailLower)); push the row when the status is undefined or 'needsReconfirmation'. Write tests first (RED)
    covering every behavior bullet, then implement to green.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/unconfirmedAssignments.test.ts && npm run type-check</automated>
  </verify>
  <done>All diff tests pass; confirmationKey is reused (not reimplemented); type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded fan-out composable (rehearseAccess + confirmations, ≤6 Planned services)</name>
  <files>src/composables/useUnconfirmedVolunteers.ts</files>
  <read_first>
    - src/views/ServiceEditorView.vue:4264-4306 (the LIVE confirmations onSnapshot read pattern to mirror:
      collection(db,'organizations',orgId,'services',svcId,'confirmations'), map roleId+emailLower+status →
      Map via confirmationKey, console.error on failure, teardown before re-subscribe)
    - src/composables/useServicePresence.ts:39-96,150-157 (the multi-listener lifecycle discipline: track
      unsubscribes, stop() before start(), onUnmounted teardown — adapt for N services, READ-ONLY)
    - src/utils/rehearseAccess.ts:52-91 (rehearseAccess doc path organizations/{orgId}/rehearseAccess/{serviceId}
      and roleAssignmentsByEmailLower field)
    - src/utils/unconfirmedAssignments.ts (Task 1)
    - 135-UI-SPEC.md:175-205 (Widget 2 data/window/states)
  </read_first>
  <action>
    Create src/composables/useUnconfirmedVolunteers.ts exporting useUnconfirmedVolunteers(orgId, services) where
    services is a reactive getter for the bounded window (the caller passes the next ≤6 upcoming Planned services).
    For each service in the window, open TWO live onSnapshot listeners: (a) the rehearseAccess doc to read
    roleAssignmentsByEmailLower, and (b) the confirmations subcollection built into a Map<confirmationKey,
    ConfirmationStatus> exactly as ServiceEditorView does. Recompute that service's unconfirmed rows via
    unconfirmedAssignments(...) whenever either snapshot updates. Enrich each row with serviceId, serviceName, and
    serviceDate (from the passed service). Expose a flat reactive list across all services plus a `loading` flag
    (true until the first snapshot of each service resolves) and an `error` flag (set on any listener error,
    console.error like the existing convention). Manage lifecycle rigorously: keep a per-serviceId unsubscribe
    handle set; on any change to orgId or the service-id window, tear down listeners for services no longer in the
    window (and all of them on orgId change) before opening new ones; onUnmounted, tear down everything. This is a
    READ path only — never setDoc/getDoc-write anything. Keep the fan-out bounded: never open listeners for more
    than the passed window (the caller guarantees ≤6).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/composables/useUnconfirmedVolunteers.ts','utf8'); if(/setDoc|deleteDoc/.test(s)) throw new Error('composable must be read-only'); if(!/unconfirmedAssignments/.test(s)) throw new Error('pure diff not used'); if(!/onUnmounted/.test(s)) throw new Error('missing teardown'); console.log('ok')" && npm run type-check</automated>
  </verify>
  <done>Composable opens ≤6-service read-only fan-out (rehearseAccess + confirmations), recomputes via the pure
  diff, exposes rows + loading/error, and tears listeners down on window/org change and unmount; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Unconfirmed volunteers attention card in DashboardView</name>
  <files>src/views/DashboardView.vue</files>
  <read_first>
    - src/views/DashboardView.vue (post-Plan-01 shape: single-column flow, feed branch, orgId watch that
      subscribes rosterStore — reuse rosterStore.people for emailLower→name)
    - src/composables/useUnconfirmedVolunteers.ts (Task 2)
    - src/views/ServiceEditorView.vue:4330-4355 (CONFIRMATION_CHIP_CLASS recipes + personName lookup to mirror
      the identical chip vocabulary)
    - 135-UI-SPEC.md:107-117 (attention-cards row grid grid-cols-1 lg:grid-cols-2 gap-6), :175-205 (Widget 2
      card shell, header + count badge, row format, cap 8 + "+N more", states), :90-98 (empty/loading/error copy)
  </read_first>
  <action>
    Compute the bounded window in DashboardView: attentionServices = upcomingServices filtered to status
    'planned' (confirm the exact Planned literal against src/types/service.ts) sliced to the first 6, exposed as a
    getter. Call useUnconfirmedVolunteers(() => authStore.orgId, () => attentionServices). Add the attention-cards
    row grid (`grid grid-cols-1 lg:grid-cols-2 gap-6`) INSIDE the populated feed branch (below the feed, so it is
    automatically hidden by the R419 empty branch). Render the Unconfirmed volunteers card using the identical
    shell `rounded-lg border border-gray-800 bg-gray-900 p-5`, an uppercase `text-xs font-semibold tracking-widest
    text-gray-500` "Unconfirmed volunteers" header with a trailing amber count badge when >0. Each row is a
    router-link to `/services/{serviceId}` showing personName (map row.emailLower → rosterStore.people by
    lowercased email, falling back to the email) · roleName · serviceName, formatServiceDate — with the exact
    confirmation chip vocabulary from ServiceEditorView (gray bg-gray-800/text-gray-400/border-gray-700 for
    implicit-unconfirmed). Apply min-w-0 truncate to the name/service spans. Cap at 8 rows; if more, append a
    "+N more — view roster" link. States: populated (rows); empty (0 unconfirmed) keeps the card visible with
    "Everyone's confirmed for the next {N} service{s}." in text-sm text-gray-400; loading shows "Checking
    confirmations…" in text-sm text-gray-500; error shows "Couldn't load unconfirmed volunteers right now." in
    text-gray-500 text-sm. Reuse the ScheduleServiceCard focus-ring utility on each row router-link. Leave a
    single-column placeholder note where Plan 03's presence card will sit (do NOT build it here).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/DashboardView.vue','utf8'); if(!/useUnconfirmedVolunteers/.test(s)) throw new Error('unconfirmed card not wired'); if(!/Everyone.s confirmed/.test(s)) throw new Error('positive empty sub-state missing'); console.log('ok')" && npm run type-check</automated>
    <human-check>With upcoming Planned services that have assignments: unconfirmed people appear with the same gray/amber chip vocabulary as the roster tab; confirming a volunteer removes them live; with everyone confirmed the card stays and shows the positive sub-state; clicking a row opens that service.</human-check>
  </verify>
  <done>Unconfirmed volunteers card renders inside the feed branch, bounded to ≤6 Planned services, always shown
  with a positive empty sub-state, capped at 8 rows, rows link to the service; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authStore.orgId → confirmations/rehearseAccess reads | The dashboard reads other users' confirmation state; reads must stay scoped to the current org's service paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-135-03 | Information Disclosure | confirmations / rehearseAccess fan-out | medium | mitigate | All listeners are built on organizations/{authStore.orgId}/… paths gated by the existing org-scoped firestore.rules (post-v2.8 SEC-S-01 fix); no personId/email beyond the already-projected emailLower is surfaced; names resolve locally from rosterStore. |
| T-135-04 | Denial of Service (cost) | bounded fan-out | medium | mitigate | Window capped to ≤6 Planned services; listeners torn down on window/org change and unmount — no unbounded per-service reads (this is a cost-hardened Blaze app). |

No package installs this plan — no supply-chain threat.
</threat_model>

<verification>
- `npx vitest run` shows only the known baseline failure (src/storage.rules.test.ts); the new diff tests pass.
- `npm run type-check` clean.
- useUnconfirmedVolunteers contains no setDoc/deleteDoc (read-only) and reuses the pure diff.
</verification>

<success_criteria>
Unconfirmed volunteers card reads Phase 133 confirmations + rehearseAccess roleAssignmentsByEmailLower bounded
to ≤6 Planned upcoming services, treats no-doc/needsReconfirmation as unconfirmed, always shows with a positive
empty sub-state, caps at 8 rows, and links each row to its service (R417). Type-check and unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/135-dashboard-overhaul/135-02-SUMMARY.md` when done.
</output>
