---
phase: 120-architecture-god-module-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/useAiSongSuggestions.ts
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements: [R358, R360]

must_haves:
  truths:
    - "AI song-suggestion behavior (Suggest All, per-slot fetch, accept, reject) is unchanged after extraction — ServiceEditorView.test.ts passes with only import/wiring test changes, not behavior changes (R358)."
    - "ServiceEditorView.vue is measurably smaller than its pre-extraction 4606 lines, with the before/after count reported (R358)."
    - "The three src/utils files that read useAuthStore for read-only settings gating (claudeApi.ts, messaging.ts, scriptureApi.ts) are recorded as a sanctioned exception in ARCHITECTURE.md's Utility Layer section (R360)."
  artifacts:
    - "src/composables/useAiSongSuggestions.ts (new composable holding the moved AI-suggestion state and functions)"
    - "src/views/ServiceEditorView.vue (rewired to consume the composable; AI cluster removed)"
    - ".planning/codebase/ARCHITECTURE.md (Utility Layer block gains the sanctioned-exception note)"
  key_links:
    - "useAiSongSuggestions() is called at the top level of ServiceEditorView.vue's <script setup>, before activeActionItems, and its returns are destructured to the SAME top-level binding names (aiDraftSongs, aiSuggestingAll, aiSongCache, aiPerSlotLoading, aiPerSlotResults, aiPerSlotError, suggestAllSongs, fetchAiForSlot, acceptAiSong, rejectAiSong) that the template, activeActionItems, and existing tests already reference."
---

<objective>
Extract the AI song-suggestion responsibility out of the ServiceEditorView.vue god module into a
dedicated `useAiSongSuggestions` composable (R358, ARCH-006), continuing the established
`useAutoSave`/`useSlideshowAssembly` extraction pattern, and document the sanctioned utils→useAuthStore
dependency-direction exception in ARCHITECTURE.md (R360, ARCH-020).

Purpose: Begin decomposing the largest frontend god module by moving one cohesive, self-contained
feature cluster into a composable, and close the ARCH-020 nit by recording the intentional read-only
store reads rather than rippling every call site. This is the milestone's final phase — the goal is a
behavior-preserving MOVE that shrinks the view and establishes the pattern, not a rewrite.
Output: One new composable, a rewired (smaller) view, wiring-only test changes, and one documentation note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-architecture-god-module-decomposition/120-CONTEXT.md
@CLAUDE.md
@src/composables/useAutoSave.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AI song-suggestions into a useAiSongSuggestions composable (R358)</name>
  <files>src/composables/useAiSongSuggestions.ts, src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
    Per D-R358, extract the AI song-suggestion cluster from ServiceEditorView.vue into a new composable
    `src/composables/useAiSongSuggestions.ts`, mirroring the parameters-in / reactive-surface-out shape of
    `useAutoSave.ts`. Behavior-preserving MOVE only — copy the logic verbatim, change nothing about how it
    behaves.

    Move these six reactive state refs (currently declared around ServiceEditorView.vue lines 2400-2410)
    into the composable: aiDraftSongs, aiSuggestingAll, aiSongCache, aiPerSlotLoading, aiPerSlotResults,
    aiPerSlotError. Move the private helper aiCacheKey and the four functions suggestAllSongs (~3520),
    fetchAiForSlot (~3599), acceptAiSong (~3681), and rejectAiSong (~3691). The composable imports
    getSongSuggestions and the AiSongSuggestion type from '@/utils/claudeApi' and getPrimaryKey from
    '@/utils/songSearch' directly (do not thread these as params — they are stateless module imports).

    The composable signature accepts the view-owned reactive dependencies as an options object:
    localService (Ref), canEditService (ComputedRef), hasSermonContext (ComputedRef), recentServiceSongIds
    (ComputedRef), the song store instance (for its aiCandidateSongs getter and songs array), and an
    onSelectSong callback (index, {id,title,key}) => void. It returns an object exposing the six state refs
    plus suggestAllSongs, fetchAiForSlot, acceptAiSong, and rejectAiSong.

    In ServiceEditorView.vue, replace the removed declarations with one top-level call:
    const { aiDraftSongs, aiSuggestingAll, aiSongCache, aiPerSlotLoading, aiPerSlotResults, aiPerSlotError,
    suggestAllSongs, fetchAiForSlot, acceptAiSong, rejectAiSong } = useAiSongSuggestions({ ... }). CRITICAL
    ORDERING: these destructured bindings are referenced by the activeActionItems computed at line ~2606
    (which reads aiSuggestingAll and passes suggestAllSongs into its handlers object at ~2631), so the
    composable call MUST appear before activeActionItems. Its inputs must therefore also be defined before
    it: hasSermonContext (~2597) is already above; onSelectSong is a hoisted function declaration so its
    reference is safe; but recentServiceSongIds is currently a computed defined at ~2644 — move that computed
    declaration up so it precedes the useAiSongSuggestions call. Do not change recentServiceSongIds' body.

    Keep every template binding identical — the SongSlotPicker props/emits at ServiceEditorView.vue ~1159-1165
    (:aiSuggestions, :aiLoading, :aiError, @requestAiSuggestions) and the Suggest-All action reference the
    same names, now satisfied by the destructured composable returns. No defineExpose is needed: script-setup
    top-level bindings remain accessible on wrapper.vm, which is how the tests reach fetchAiForSlot.

    For ServiceEditorView.test.ts: the AI tests (e.g. the fetchAiForSlot(0) call at ~914-915 and the
    getSongSuggestions songLibrary-argument assertions) mock '@/utils/claudeApi' at the MODULE level, so the
    mock still intercepts the composable's call unchanged — expect zero or import/wiring-only edits. Only
    touch the test if a binding path changed; do not alter any behavioral assertion. If wrapper.vm access
    to any AI binding breaks, fix it as wiring, not by weakening the assertion.

    Do NOT touch the Planning Center export cluster, the scripture handlers, or any other view responsibility
    — this phase extracts exactly one responsibility (AI suggestions) per D-R358 "be surgical".
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>
    useAiSongSuggestions.ts exists and owns the six AI refs + aiCacheKey + the four AI functions;
    ServiceEditorView.vue consumes it via one top-level destructured call placed before activeActionItems;
    `npm run type-check` (full vue-tsc --build) is clean; the ServiceEditorView.test.ts AI-suggestion tests
    pass with only import/wiring changes; and `wc -l src/views/ServiceEditorView.vue` reports fewer than
    4606 lines (record before/after in the SUMMARY).
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the sanctioned utils→useAuthStore exception in ARCHITECTURE.md (R360)</name>
  <files>.planning/codebase/ARCHITECTURE.md</files>
  <action>
    Per D-R360 (take the criterion's document-as-sanctioned branch — a pass-in refactor would ripple every
    call site for a Low nit on the milestone's last phase), add a sanctioned-exception note to the Utility
    Layer subsection of ARCHITECTURE.md's Layers section (the block at lines ~110-115, "Depends on: Types,
    Firebase (via stores)"). Record that three utility files — src/utils/claudeApi.ts (isAiEnabled),
    src/utils/messaging.ts (isMessagingEnabled), src/utils/scriptureApi.ts — deliberately read useAuthStore()
    to check a read-only boolean settings gate, and that this is a sanctioned exception to the documented
    utils-are-called-by-stores direction: it is read-only (never a mutation), carries no correctness or
    data-integrity risk, and cannot cause a circular-import failure because Pinia stores are legitimately
    callable from anywhere once createPinia() is installed. Cite ARCH-020 and R360. Keep the note short per
    the project comment/documentation convention (rationale in the doc, not a sprawling essay).

    The optional pass-in refactor of one util (D-R360 bonus) is explicitly NOT required and is out of scope
    for this documentation-only task — documenting all three is the accepted deliverable. Do not modify any
    src/utils/*.ts file in this task.
  </action>
  <verify>
    <automated>grep -qiE 'useAuthStore|ARCH-020|R360' .planning/codebase/ARCHITECTURE.md && grep -qE 'claudeApi|messaging|scriptureApi' .planning/codebase/ARCHITECTURE.md && echo OK</automated>
  </verify>
  <done>
    ARCHITECTURE.md's Utility Layer section documents claudeApi.ts, messaging.ts, and scriptureApi.ts as a
    sanctioned read-only useAuthStore() exception, citing ARCH-020/R360 with the read-only / no-circular-risk
    rationale. No src/ file was changed by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client ↔ Firebase | Unchanged. The composable runs entirely client-side and makes the exact same authStore/songStore reads and getSongSuggestions call the inline code did — no new surface crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-01 | Tampering | useAiSongSuggestions composable (moved AI-suggestion logic) | low | accept | Pure behavior-preserving move; no auth, scope, or data-path change. The existing ServiceEditorView.test.ts AI-suggestion tests (fetchAiForSlot, getSongSuggestions songLibrary assertions) prove behavior is unchanged. |
| T-120-02 | Information Disclosure | ARCHITECTURE.md doc note (R360) | low | accept | Documentation-only. Records an already-existing read-only store read as sanctioned; no code or behavior changes, so no new disclosure path is created. |
</threat_model>

<verification>
- `npm run type-check` (full vue-tsc --build per CLAUDE.md — typechecks test files too) is clean.
- `npx vitest run` app suite: the only accepted baseline failure is `src/storage.rules.test.ts`; the
  ServiceEditorView.test.ts AI-suggestion tests are green with wiring-only changes.
- `wc -l src/views/ServiceEditorView.vue` reports fewer lines than the pre-extraction 4606.
- ARCHITECTURE.md's Utility Layer block names all three utils as a sanctioned read-only exception.
</verification>

<success_criteria>
R358: one cohesive AI-suggestion responsibility is extracted from ServiceEditorView.vue into
useAiSongSuggestions.ts; the view is measurably smaller; behavior is unchanged (tests pass with only
import/wiring changes). R360: the three utils→useAuthStore reads are documented as a sanctioned exception
in ARCHITECTURE.md. Type-check and the app suite (1-file baseline) pass.
</success_criteria>

<output>
Create `.planning/phases/120-architecture-god-module-decomposition/120-01-SUMMARY.md` when done,
recording the before/after line count of ServiceEditorView.vue.
</output>
