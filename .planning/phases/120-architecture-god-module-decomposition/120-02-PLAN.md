---
phase: 120-architecture-god-module-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/cleanupSweeps.ts
  - functions/src/index.ts
  - functions/src/index.test.ts
autonomous: true
requirements: [R359]

must_haves:
  truths:
    - "The four scheduled cleanup sweeps live in functions/src/cleanupSweeps.ts and behave identically — functions/src/index.test.ts's cleanup tests pass unchanged in behavior after re-pointing their imports (R359)."
    - "Every moved deploy-facing cloud function (cleanupExpiredMedia, cleanupOrphanRenders, cleanupOrphanBackgrounds, cleanupPptxSources) is re-exported from index.ts so `firebase deploy` still finds it (R359)."
    - "`cd functions && npm run build` (tsc) succeeds, proving the new module compiles and every re-export resolves — the functions test suite alone does NOT prove this (R359)."
  artifacts:
    - "functions/src/cleanupSweeps.ts (new module holding the four cleanup handlers, their onSchedule wrappers, cleanup-only constants/types, and readDeleteCap)"
    - "functions/src/index.ts (cleanup logic removed; the four onSchedule wrappers re-exported from ./cleanupSweeps; previewCleanupDryRunHandler imports the four handlers from ./cleanupSweeps)"
  key_links:
    - "index.ts re-exports the four onSchedule wrappers on a single line: export { cleanupExpiredMedia, cleanupOrphanRenders, cleanupOrphanBackgrounds, cleanupPptxSources } from ./cleanupSweeps — matching the orgProvisioning.ts re-export pattern."
    - "previewCleanupDryRunHandler (which stays in index.ts) imports cleanupExpiredMediaHandler, cleanupOrphanRendersHandler, cleanupOrphanBackgroundsHandler, and cleanupPptxSourcesHandler from ./cleanupSweeps."
---

<objective>
Extract the four scheduled cleanup sweeps out of the functions/src/index.ts god module into a dedicated
`functions/src/cleanupSweeps.ts` module (R359, ARCH-010), matching the sibling
`orgProvisioning.ts`/`orgMembershipClaims.ts`/`superAdminClaims.ts` extraction pattern (implementation in
its own module, re-exported from index.ts), then verify the re-export trap is closed.

Purpose: Begin decomposing the largest backend god module by moving one cohesive concern (the four cleanup
sweeps) into its own file. This is a behavior-preserving MOVE — no Cloud Function's behavior, signature, or
deploy name changes. THE #1 RISK is the re-export trap: a moved `export const` that is not re-exported from
index.ts silently vanishes from `firebase deploy`, and the functions test suite does NOT catch it
(handler-direct tests pass anyway). Task 2 is a dedicated, mandatory verification of that trap.
Output: One new module, a slimmer index.ts with the four re-exports wired, re-pointed test imports, and a
build-proven, suite-green result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-architecture-god-module-decomposition/120-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the four cleanup sweeps into functions/src/cleanupSweeps.ts and re-export from index.ts (R359)</name>
  <files>functions/src/cleanupSweeps.ts, functions/src/index.ts, functions/src/index.test.ts</files>
  <action>
    Per D-R359 (planner's pick: the four cleanup sweeps — one cohesive theme, and they do NOT share too many
    local helpers, so the PPTX-pipeline alternative is not needed), create functions/src/cleanupSweeps.ts and
    MOVE the cleanup region currently spanning index.ts lines ~1059-1733 into it. Behavior-preserving move —
    copy each handler body verbatim; change no logic, no schedule, no guard, no deploy name.

    Move into cleanupSweeps.ts, in order:
    - readDeleteCap (~1061) — it is used ONLY by the four cleanup handlers (all four call sites are inside
      them), so it belongs in this module, not index.ts.
    - RETENTION_DAYS, MEDIA_PATH_GUARD, readMediaRetentionDays, the CleanupSummary interface, and the
      cleanupExpiredMediaHandler body plus the cleanupExpiredMedia onSchedule wrapper.
    - The OrphanCleanupSummary interface plus cleanupOrphanRendersHandler plus the cleanupOrphanRenders wrapper.
    - BACKGROUND_RETENTION_DAYS, the background retention reader, the BACKGROUND_PATH_GUARD constant, the
      backgrounds summary type, cleanupOrphanBackgroundsHandler plus the cleanupOrphanBackgrounds wrapper.
    - PPTX_SOURCE_RETENTION_DAYS, its retention reader, the PptxSourceCleanupSummary interface,
      cleanupPptxSourcesHandler plus the cleanupPptxSources wrapper.

    cleanupSweeps.ts imports its own dependencies directly: onSchedule from firebase-functions/v2/scheduler;
    getFirestore from firebase-admin/firestore; getStorage from firebase-admin/storage; and getAppConfig plus
    the AppConfig type from ./appConfig. Keep the exact export-const-onSchedule declaration shape for all four
    wrappers, and keep the exported-async-function <name>Handler shape for all four handlers (the sibling
    pattern exports the testable handler separately from the deployed wrapper).

    In index.ts:
    - Delete the moved region (~1059-1733).
    - Add a re-export block for the four DEPLOY-FACING wrappers on a SINGLE line, mirroring the
      orgProvisioning re-export near the bottom of index.ts (~3066):
      export { cleanupExpiredMedia, cleanupOrphanRenders, cleanupOrphanBackgrounds, cleanupPptxSources } from "./cleanupSweeps";
      A short comment above it should note (in your own words, by concept) that a moved cloud function absent
      from this re-export is dropped at deploy time — matching the intent of the existing
      inviteOnboarding/orgProvisioning re-export comments. Keep the export on one line so the deploy-name set is
      greppable.
    - previewCleanupDryRunHandler stays in index.ts (it is the on-demand preview concern, not a scheduled
      sweep). Add an import at the top of index.ts pulling the four handlers it calls from ./cleanupSweeps:
      cleanupExpiredMediaHandler, cleanupOrphanRendersHandler, cleanupOrphanBackgroundsHandler,
      cleanupPptxSourcesHandler. Leave previewCleanupDryRun, its request/response types, CleanupPreviewType,
      and CLEANUP_PREVIEW_TYPES exactly where they are.

    In index.test.ts: the cleanup symbols are currently imported from "./index" (the import block ending at
    ~60). Re-point ONLY the moved symbols — cleanupExpiredMediaHandler, cleanupOrphanRendersHandler,
    cleanupOrphanBackgroundsHandler, cleanupPptxSourcesHandler, RETENTION_DAYS, MEDIA_PATH_GUARD,
    readMediaRetentionDays, BACKGROUND_RETENTION_DAYS, PPTX_SOURCE_RETENTION_DAYS (and any moved summary types
    the test names) — to import from "./cleanupSweeps" instead. This is an import/wiring change ONLY; change no
    assertion. previewCleanupDryRunHandler and PreviewCleanupDryRunRequest stay imported from "./index". Do not
    touch appConfig.test.ts or backfillOrgClaims.ts (their readDeleteCap/MEDIA_PATH_GUARD mentions are comments,
    not imports).

    Do NOT extract any other concern (API proxy, PPTX pipeline, cron, messaging) — exactly one concern this
    phase, per D-R359.
  </action>
  <verify>
    <automated>cd functions && npm run build</automated>
    <automated>cd functions && npm test</automated>
  </verify>
  <done>
    functions/src/cleanupSweeps.ts owns the four handlers plus wrappers plus readDeleteCap plus the cleanup
    constants/types; index.ts no longer declares them, re-exports the four wrappers from ./cleanupSweeps on one
    line, and previewCleanupDryRunHandler imports the four handlers from ./cleanupSweeps; index.test.ts imports
    the moved symbols from ./cleanupSweeps with no assertion changes; `cd functions && npm run build` succeeds
    and `cd functions && npm test` is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the re-export trap is closed — the #1 risk (R359)</name>
  <files>functions/src/index.ts</files>
  <action>
    This is the mandatory, dedicated re-export-trap verification called out in D-R359 — the functions test
    suite does NOT prove deploy-safety, so all three checks below are required; do not rely on the suite alone.

    (a) Positive-grep confirmation: prove that each of the four moved deploy-facing wrapper names
    (cleanupExpiredMedia, cleanupOrphanRenders, cleanupOrphanBackgrounds, cleanupPptxSources) still appears in
    index.ts. The word-boundary grep loop in the verify block matches the bare wrapper name only — a word
    boundary does not fall between the final letter and the `Handler` suffix, so the loop cannot be satisfied
    by the previewCleanupDryRun handler-import line; the only place a bare wrapper name survives in index.ts is
    the ./cleanupSweeps re-export line.
    (b) Build proof: `cd functions && npm run build` (tsc) must succeed — this proves the new module compiles
    and the re-exports plus previewCleanupDryRun's handler imports actually resolve.
    (c) Suite proof: `cd functions && npm test` must be green.

    If (a) fails, the fix is to add the missing name to the index.ts re-export line (Task 1's key link), not to
    weaken the check. Record all three results in the SUMMARY as the explicit deploy-safety evidence.
  </action>
  <verify>
    <automated>for f in cleanupExpiredMedia cleanupOrphanRenders cleanupOrphanBackgrounds cleanupPptxSources; do grep -qE "\b$f\b" functions/src/index.ts || { echo "MISSING re-export: $f"; exit 1; }; done; echo "all four cleanup functions present in index.ts"</automated>
    <automated>cd functions && npm run build</automated>
    <automated>cd functions && npm test</automated>
  </verify>
  <done>
    All four bare wrapper names are present in index.ts (the re-export loop passes); `cd functions && npm run
    build` succeeds (re-exports and handler imports resolve); `cd functions && npm test` is green. The SUMMARY
    records all three as the deploy-safety evidence for the re-export trap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Functions deploy manifest ↔ Firebase | A moved `export const` cloud function that is not re-exported from index.ts is silently absent from the deploy manifest — the function disappears from production. This is the boundary the re-export verification defends. |
| scheduled sweep ↔ Cloud Storage | Unchanged. Handler bodies, path guards, and the fail-closed dry-run defaults move verbatim; no deletion behavior or scope changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-03 | Denial of Service (availability) | moved cleanup cloud functions (cleanupExpiredMedia, cleanupOrphanRenders, cleanupOrphanBackgrounds, cleanupPptxSources) | high | mitigate | A missing re-export drops a scheduled cleanup sweep from production, so Storage resumes growing unbounded (the v1.8 cost surface reopens). Mitigated by Task 2's dedicated verification: word-boundary grep that every wrapper name survives in index.ts, plus `cd functions && npm run build` (proves re-exports resolve), plus `cd functions && npm test`. Note: this is an availability risk, not a data-exposure one. |
| T-120-04 | Tampering | cleanupSweeps.ts handler bodies | low | accept | Pure move. The deletion guards (MEDIA_PATH_GUARD, BACKGROUND_PATH_GUARD, fail-closed dryRun default, per-run delete cap) move verbatim; index.test.ts's cleanup suite proves behavior is unchanged. |
</threat_model>

<verification>
- `cd functions && npm run build` (tsc) succeeds — the module compiles and every re-export + handler import
  resolves (the functions suite alone does NOT prove this).
- `cd functions && npm test` is green (the separate functions suite per CLAUDE.md — not the root app suite).
- The re-export loop confirms all four bare wrapper names survive in index.ts.
- No Cloud Function behavior, signature, schedule, or deploy name changed.
</verification>

<success_criteria>
R359: the four scheduled cleanup sweeps are extracted from functions/src/index.ts into
functions/src/cleanupSweeps.ts, matching the orgProvisioning re-export pattern; every moved cloud function is
re-exported from index.ts; `cd functions && npm run build` succeeds and the functions suite passes with
behavior unchanged; the re-export trap is explicitly verified.
</success_criteria>

<output>
Create `.planning/phases/120-architecture-god-module-decomposition/120-02-SUMMARY.md` when done, recording
the re-export-trap evidence (grep loop result, build result, test result).
</output>
