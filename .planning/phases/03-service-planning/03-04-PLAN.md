---
phase: 03-service-planning
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/views/ServicesView.vue
  - src/components/ServiceCard.vue
  - src/components/NewServiceDialog.vue
  - src/components/RotationTable.vue
autonomous: true
requirements:
  - CAL-01

must_haves:
  truths:
    - "User sees a list of service week cards with upcoming services shown first"
    - "Each card shows date, progression pattern, song titles, and status"
    - "Clicking a card navigates to the full service editor"
    - "New Service button opens a date picker dialog defaulting to next Sunday"
    - "Creating a new service navigates to the editor for that service"
    - "Past services appear in a separate section below upcoming services"
    - "Seasonal rotation table shows songs as rows and weeks as columns"
    - "Repeated songs across consecutive weeks are highlighted in the rotation table"
  artifacts:
    - path: "src/views/ServicesView.vue"
      provides: "Services list page with week cards, new service dialog, and rotation table tab"
      min_lines: 100
    - path: "src/components/ServiceCard.vue"
      provides: "Week card showing date, progression, songs, status at a glance"
      min_lines: 40
    - path: "src/components/NewServiceDialog.vue"
      provides: "Date picker dialog for creating a new service"
      min_lines: 40
    - path: "src/components/RotationTable.vue"
      provides: "Seasonal song rotation table with weeks as columns and songs as rows"
      min_lines: 60
  key_links:
    - from: "src/views/ServicesView.vue"
      to: "src/stores/services.ts"
      via: "useServiceStore for service list data and createService"
      pattern: "useServiceStore"
    - from: "src/components/ServiceCard.vue"
      to: "src/router/index.ts"
      via: "router-link to /services/:id"
      pattern: "router-link.*services"
    - from: "src/components/RotationTable.vue"
      to: "src/utils/rotationTable.ts"
      via: "computeRotationTable for song usage data"
      pattern: "computeRotationTable"
---

<objective>
Build the ServicesView list page with service week cards, new service creation dialog, and seasonal song rotation table.

Purpose: This is the entry point for service planning — where users see all their planned and past services at a glance, create new services, and spot song rotation patterns through the seasonal overview table.

Output: 4 Vue components delivering the complete services list and calendar experience.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-service-planning/03-CONTEXT.md
@.planning/phases/03-service-planning/03-RESEARCH.md
@.planning/phases/03-service-planning/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/service.ts (Plan 01):
```typescript
export type Progression = '1-2-2-3' | '1-2-3-3'
export type ServiceStatus = 'draft' | 'planned'
export interface SongSlot { kind: 'SONG'; position: number; requiredVwType: VWType; songId: string | null; songTitle: string | null; songKey: string | null }
export interface Service { id: string; date: string; progression: Progression; teams: string[]; status: ServiceStatus; slots: ServiceSlot[]; sermonPassage: ScriptureRef | null; notes: string; createdAt: Timestamp; updatedAt: Timestamp }
```

From src/utils/slotTypes.ts (Plan 01):
```typescript
export const SLOT_LABELS: Record<number, string>
export function buildSlots(progression: Progression): ServiceSlot[]
```

From src/utils/rotationTable.ts (Plan 01):
```typescript
export interface RotationEntry { songId: string; songTitle: string; dates: string[] }
export function computeRotationTable(services: Service[]): RotationEntry[]
```

From src/stores/services.ts (Plan 02):
```typescript
export const useServiceStore = defineStore('services', () => {
  const services: Ref<Service[]>
  const isLoading: Ref<boolean>
  function subscribe(orgId: string): void
  async function createService(data): Promise<string>
})
```

From src/components/SongBadge.vue (existing):
```vue
<SongBadge :type="vwType" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServicesView with ServiceCard list and NewServiceDialog</name>
  <files>src/views/ServicesView.vue, src/components/ServiceCard.vue, src/components/NewServiceDialog.vue</files>
  <action>
    **ServiceCard.vue:**
    Create a card component for displaying a service week summary.

    Props: `service: Service`

    Card layout (horizontal):
    - Left: Date formatted as "Sun, Mar 8" (short) with year only if not current year
    - Center: Progression badge ("1-2-2-3" or "1-2-3-3"), then song titles listed vertically (from song slots only, showing songTitle or "Empty" placeholder for unfilled slots)
    - Right: Status badge (planned = green, draft = gray) — use static class lookup for Tailwind v4 purge safety
    - Whole card is a `<router-link :to="'/services/' + service.id">` — click navigates to editor

    Dark mode: gray-900 card background, gray-800 border, hover:bg-gray-800/50 transition. Text-gray-100 date, text-gray-400 song titles, text-gray-500 empty slot placeholders.

    **NewServiceDialog.vue:**
    Create a dialog for setting up a new service.

    Props: `open: boolean`
    Emits: `close`, `create(data: { date: string, progression: Progression, teams: string[] })`

    Dialog content:
    1. Date input: `<input type="date">` defaulting to next Sunday (use `nextSunday()` helper — compute with `new Date()` + `getDay()` arithmetic per RESEARCH.md example, no library needed)
    2. Progression selector: Two radio buttons — "1-2-2-3" and "1-2-3-3" (default to "1-2-2-3"). Show brief description: "1-2-2-3: Call to Worship, Intimate, Intimate, Ascription" and "1-2-3-3: Call to Worship, Intimate, Ascription, Ascription"
    3. Team checkboxes: Choir, Orchestra, Special Service (all unchecked by default)
    4. Create button (indigo-600) and Cancel button (gray-700)

    Dialog uses `<Teleport to="body">` + `<Transition>` (same pattern as SongSlideOver and CsvImportModal). Semi-transparent backdrop. Centered card dialog. Dark mode: gray-900 dialog background, gray-800 borders.

    **ServicesView.vue:**
    Create the services list page.

    Structure:
    1. Wrap in `<AppShell>`
    2. On mount: resolve orgId from user doc, subscribe `serviceStore` and `songStore` (same pattern as SongsView)
    3. Header: "Services" title + "New Service" button (indigo-600)

    Service list sections:
    - Compute `upcomingServices` = services where `date >= today` sorted ascending (nearest first)
    - Compute `pastServices` = services where `date < today` sorted descending (most recent first)
    - "Upcoming" section with ServiceCard for each upcoming service (show next 4-6 weeks). If empty, show empty state: "No upcoming services. Create your first service to get started." with a button linking to New Service dialog.
    - "Past Services" section header with ServiceCard for each past service (collapsed by default, "Show past services" toggle)

    New Service flow:
    - Click "New Service" opens NewServiceDialog
    - On `create` event: call `serviceStore.createService(data)`, receive doc id, navigate to `/services/${id}`

    **Tab/section for rotation table:**
    - Two tabs or sections at the top: "Services" (list view, default) and "Rotation" (seasonal table)
    - Use simple tab buttons — no router-based tabs, just a local `activeTab` ref
    - When "Rotation" tab is active, render RotationTable component (Task 2)

    Dark mode: gray-950 background, consistent with all other views.
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>ServicesView renders service cards grouped by upcoming/past, New Service dialog creates services with date/progression/teams, clicking a card navigates to /services/:id. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: RotationTable seasonal song rotation view</name>
  <files>src/components/RotationTable.vue</files>
  <action>
    Create `src/components/RotationTable.vue` — a seasonal/quarterly overview showing song usage patterns.

    **Props:**
    - `services: Service[]` — all services to analyze

    **Layout:**
    - A table with weeks as columns and songs as rows (per CONTEXT.md locked decision)
    - Column headers: service dates formatted as "Mar 8", "Mar 15", "Mar 22", etc.
    - Row headers: song titles (only songs that appear in at least one service in the range)
    - Cells: filled indicator (colored dot or cell highlight) where a song appears in that week, empty otherwise

    **Computation:**
    - Call `computeRotationTable(services)` from `src/utils/rotationTable.ts` to get rotation entries
    - Build a date columns array from the services (sorted ascending by date)
    - For each song row, mark which date columns have that song

    **Highlighting repeats:**
    - If a song appears in 2+ consecutive weeks, highlight those cells with amber/yellow background to flag potential rotation issues
    - This is the key differentiator — it directly answers "are we repeating songs too much?"

    **Handling large libraries (Claude's discretion):**
    - Only show songs that appear at least once in the displayed date range (not all 200 songs)
    - If more than 12 columns (weeks), show a horizontal scroll container
    - If more than 30 song rows, add a search/filter input above the table to narrow by song title

    **Dark mode:** gray-900 table background, gray-800 cell borders, gray-700 header row. Filled cells: indigo-900/50 background with indigo-400 dot. Consecutive repeat highlight: amber-900/30 background. Text-gray-100 song titles, text-gray-400 dates.

    **Empty state:** If no services exist, show "No services planned yet. Create services to see your song rotation patterns."
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>RotationTable displays songs as rows and weeks as columns, highlights consecutive repeats in amber, only shows songs that appear in at least one service. Build succeeds.</done>
</task>

</tasks>

<verification>
npx vite build 2>&1 | tail -5
npx vitest run --reporter=verbose 2>&1 | tail -10
</verification>

<success_criteria>
- ServicesView shows upcoming services first, past services below
- Each ServiceCard displays date, progression, song titles, status at a glance
- Clicking a card navigates to /services/:id
- New Service dialog defaults to next Sunday and lets user pick progression + teams
- Creating a service navigates to the editor
- Rotation table shows songs as rows and weeks as columns
- Consecutive song repeats highlighted in amber
- Production build succeeds
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-planning/03-04-SUMMARY.md`
</output>
