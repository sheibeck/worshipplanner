---
phase: 142-slides-tab-panel-ux-from-claude-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/slideGroups.ts
  - src/stores/__tests__/slideGroups.test.ts
  - src/utils/__tests__/slideshowAssembler.test.ts
  - src/components/slides/BackgroundControl.vue
  - src/components/slides/__tests__/BackgroundControl.test.ts
  - src/components/slides/SlotVideoOutputControl.vue
  - src/components/slides/__tests__/SlotVideoOutputControl.test.ts
autonomous: true
requirements: [R032, R055, R425, R437]
tags: [vue, pinia, firestore, popover, background, video-output]

must_haves:
  truths:
    - "Assigning a vamp with no MP3 to a group whose slideGroups doc ALREADY EXISTS writes bedVampId + bedVampLabel to Firestore and deletes any stale bedAudioUrl (CONTEXT.md › Audio slot: 'A vamp with no MP3 may be assigned')"
    - "A Track upload or an MP3-having vamp assignment against an existing doc still takes the existing bedAudioUrl branch — the new branch only fires when bedVampId is present and bedAudioUrl is absent"
    - "Live playback (slideshowAssembler) resolves NO audioUrl and NO audioLoop for a group with bedVampId set and bedAudioUrl absent — silently skips the bed, no throw"
    - "BackgroundControl with variant omitted (or variant='panel') renders byte-for-byte as today — SongLyricEditor.vue's song-level call site is unaffected and the existing BackgroundControl.test.ts describe stays green unmodified"
    - "BackgroundControl variant='chip-popover' renders None · recent thumbnails · ＋ upload tiles and the 'Recent in this service · drop a file on ＋' caption (UI-SPEC §4); ＋ accepts a dropped image file"
    - "Background popover — inherited from song: no tiles, no upload affordance; explainer 'Managed on the song — edit it from the song's Lyrics tab.' renders instead (CONTEXT.md › Background & Display chips)"
    - "SlotVideoOutputControl renders Full-screen / Banner as thumbnail tiles inside a role=radiogroup; the caption is dynamic — 'Fills the screen on every output.' for fullscreen, 'Lower third over the live camera feed. Video output only.' for banner (UI-SPEC §3)"
    - "SlotVideoOutputControl's select()/mode/change emit are unchanged — the same videoOutput.mode write as today"
    - "empty — Background popover: no group.backgroundImageUrl and no song inheritance → None tile is the highlighted one, plus recents + upload tile, no filename row"
    - "empty — Background popover, zero recents: renders only None + upload tile (two tiles is a valid minimum, no empty-row filler element)"
    - "loading — Background popover recents: derived synchronously from already-loaded data, no loading state exists"
    - "error — Background upload: useBackgroundUpload's existing error ref renders below the tiles via the existing background-control-upload-error element"
    - "populated — Display popover: two tiles, the active one carries border-indigo-600, hint text switches per selection"
    - "populated — Background popover: None tile + up to 4 recent thumbnails + upload tile; the swatch whose url equals imageUrl carries border-indigo-600"
    - "partial — Background inherited from song while mid-upload: not reachable — inherited groups render no upload affordance at all"
    - "overflow — Display tile hint text: fixed two-line max copy with leading-[1.45], never user text"
    - "zero-one-many — Background recents: capped to 4 thumbnails; a 5th+ distinct background simply does not appear"
    - "empty — Display popover: not reachable — videoOutput.mode defaults to fullscreen so a tile is always selected"
    - "loading — Display popover: no async content, opens synchronously"
    - "partial — Display popover: not reachable — two-value radio has no half-set state"
    - "long-text — Display popover: tile labels and hint copy are fixed strings, never user text"
    - "error — Display popover: the only failure is the videoOutput emit's downstream write, which is covered by the Firestore write-failure backstop carried in Plan 04's must_haves (the tile derives from slot.videoOutput, so it stays on the stored value)"
  artifacts:
    - path: "src/stores/slideGroups.ts"
      provides: "setGroupBedMedia third patch branch (bedVampId present, bedAudioUrl absent → write id/label, deleteField bedAudioUrl)"
    - path: "src/stores/__tests__/slideGroups.test.ts"
      provides: "existing-doc no-MP3 vamp regression tests (the fixture that exposes the bug)"
    - path: "src/utils/__tests__/slideshowAssembler.test.ts"
      provides: "bedVampId-without-bedAudioUrl tolerance case"
    - path: "src/components/slides/BackgroundControl.vue"
      provides: "variant prop ('panel' | 'chip-popover'), recents prop, drop target, swatch testids"
    - path: "src/components/slides/__tests__/BackgroundControl.test.ts"
      provides: "chip-popover variant describe; panel-variant describe untouched"
    - path: "src/components/slides/SlotVideoOutputControl.vue"
      provides: "tile template + sizeHint computed"
    - path: "src/components/slides/__tests__/SlotVideoOutputControl.test.ts"
      provides: "dynamic-caption + tile-state tests"
  key_links:
    - from: "setGroupBedMedia existing.exists() branch"
      to: "updateDoc payload"
      via: "third else-if keyed on patch.bedVampId, after the bedAudioUrl !== undefined check"
    - from: "BackgroundControl chip-popover tiles"
      to: "attach / remove emits"
      via: "selectNone → emit('remove') when imageUrl set; selectRecent → emit('attach', url); drop/file-input → uploadBackground → emit('attach')"
    - from: "SlotVideoOutputControl tiles"
      to: "change emit"
      via: "unchanged select(next)"
---

<objective>
Land the three leaf changes that everything else in Phase 142 builds on, each independently testable and
none of them touching the group strip yet: (1) the one real logic bug — `setGroupBedMedia` silently drops
a no-MP3 vamp assignment against an existing group document (RESEARCH.md Finding 4 / Pitfall 1); (2)
`BackgroundControl.vue`'s additive `variant="chip-popover"` branch (UI-SPEC §4) so the Background popover
has content, while the default `panel` variant stays byte-identical for `SongLyricEditor.vue`; (3)
`SlotVideoOutputControl.vue`'s tile restyle + dynamic `sizeHint` caption (UI-SPEC §3).

Decision areas implemented (142-CONTEXT.md): "Audio slot (Track ⟂ Vamp)" — the no-MP3 vamp write path;
"Background & Display chips" — popover contents for Background (None · recents · ＋ upload/drop, inherited
explainer) and Display (tiles + hint copy, same `videoOutput.mode` write).

Purpose: every other plan is additive on top of a correct write path and finished popover bodies. Doing
these first means Wave 2's strip composes finished parts instead of stubs.
Output: fixed store + regression tests; `BackgroundControl` with `variant`/`recents`; restyled
`SlotVideoOutputControl`; all four touched test suites green; `SlideGrid.test.ts` still green (the panel
variant is unchanged, and the video-output row/buttons keep their testids).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Project docs (do NOT read `.planning/STATE.md` whole — it is ~450KB; read only its YAML frontmatter and
first ~120 lines if needed. ROADMAP.md: only the `### Phase 142` section, ~line 1099):
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-CONTEXT.md
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-UI-SPEC.md
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-PATTERNS.md
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md

RESEARCH.md sections worth re-reading before Task 1: "Pitfall 1" and "Finding 4"
(.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-RESEARCH.md).

Project rules (CLAUDE.md): type-check gate is `npm run type-check` (vue-tsc --build, includes tests) — never
`-p tsconfig.app.json` alone. App suite is bare `npx vitest run` (2-file baseline: only
`src/storage.rules.test.ts` fails). Comments stay short; rationale → ADR / map docs
(`.planning/codebase/CONVENTIONS.md` "Comment Convention"). If executing in a fresh worktree, run the
`worktree-env-setup` skill first (`.env.local` is required for the unit suite to load).
</context>

## Artifacts this phase produces (phase-wide inventory; items marked ★ are created by THIS plan)

| Artifact | Kind | Where |
|---|---|---|
| ★ `setGroupBedMedia` third patch branch (`else if (patch.bedVampId)` → `bedVampId`, `bedVampLabel`, `bedAudioUrl: deleteField()`) | store logic | `src/stores/slideGroups.ts` (Plan 01) |
| ★ `BackgroundControl` props `variant?: 'panel' \| 'chip-popover'` (default `'panel'`), `recents?: { url: string; label: string }[]` (default `[]`); `caption`/`addLabel` become optional (default `''`) | component props | `src/components/slides/BackgroundControl.vue` (Plan 01) |
| ★ `BackgroundControl` internals: `isDragOver` ref, `swatches` computed, `selectNone()`, `selectRecent(r)`, `onDrop(e)`, shared `uploadFile(file)` | component logic | Plan 01 |
| ★ testids `background-control-swatch-none`, `background-control-swatch-recent` (+ `data-url`), `background-control-swatch-upload`, `background-control-chip-caption`, `background-control-inherited-explainer` (chip-popover variant only) | testids | Plan 01 |
| ★ `SlotVideoOutputControl` `sizeHint` computed; tile template; `role="radiogroup"` wrapper | component | `src/components/slides/SlotVideoOutputControl.vue` (Plan 01) |
| `VampPicker` prop `allowUnattached?: boolean` (default `false`) | component prop | Plan 02 |
| `AudioPlayer` emit `loadedmetadata: [durationSeconds: number]` | component emit | Plan 02 |
| `SlideGroupMusicControl` rework: `audioTab` derivation (`'none' \| 'track' \| 'vamp'`), `selectedTab` ref, `setAudioTab(id)`, `duration` ref + `formatDuration`, new emit `close: []`, `slideCount` prop REMOVED; testids `group-music-audio-tab-none/-track/-vamp`, `group-music-track-row/-filename/-duration/-replace/-remove`, `group-music-vamp-warning` | component | Plan 02 |
| `SlideGroupSetupStrip.vue` (NEW) — props `selectedSlot`, `group`, `editable`, `slideCount`, `orgId`, `inheritedBackground?`, `recentBackgrounds?`, `vamps?`, `vampsLoading?`; emits `attach-music`, `remove-music`, `attach-vamp`, `attach-background`, `remove-background`, `video-output-change`; slot `trailing`; testids `slide-group-setup-strip`, `slide-group-setup-chip-{display,background,audio}`, `slide-group-setup-popover-{display,background,audio}`, `slide-group-setup-caption` | component | Plan 03 |
| `src/components/slides/__tests__/SlideGroupSetupStrip.test.ts` (NEW) | test file | Plan 03 |
| `SlideGrid.vue`: panel gate → `Boolean(selectedSlot)`; mounts the strip; new prop `recentBackgrounds?`; `onAttachGroupVamp` no-URL guard removed; retired `showGroupMusicControl`, `showGroupBackgroundControl`, `showVideoOutputControl`, `groupBackgroundCaption`; retired testids `slide-grid-group-background`, `slide-grid-group-background-caption` | component | Plan 04 |
| `SlidesTab.vue`: `recentBackgrounds` computed → `:recent-backgrounds` on `SlideGrid` | component | Plan 04 |
| `.planning/codebase/ARCHITECTURE.md`: new `### src/components/slides/SlideGroupSetupStrip.vue` entry; SlideGrid entry gate note updated | map doc | Plans 03/04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setGroupBedMedia — third patch branch for a no-MP3 vamp against an existing group doc, with the existing-doc regression test and the assembler tolerance case</name>
  <files>src/stores/slideGroups.ts, src/stores/__tests__/slideGroups.test.ts, src/utils/__tests__/slideshowAssembler.test.ts</files>
  <read_first>
    - src/stores/slideGroups.ts lines 116-181 (the `BedMediaPatch` interface and `setGroupBedMedia` — the `existing.exists()` branch is the one with the gap; the `setDoc` branch already handles this case via `stripUndefined`)
    - src/stores/__tests__/slideGroups.test.ts lines 1-88 (the `firebase/firestore` mock: `getDoc` defaults to `exists: () => false`; `deleteField` returns the sentinel string `'__deleteField__'`; `makeGroupDoc()` fixture) and lines 512-637 (the existing `setGroupBedMedia — group-level vamp bed (260918-nm2)` describe — copy its `vi.mocked(getDoc).mockResolvedValueOnce({ exists: () => true, ... })` shape exactly)
    - src/utils/slideshowAssembler.ts lines 265-310 (`resolveEntryMedia` — verify, do not change: `audioFromBed` requires `group.bedAudioUrl` truthy, so a no-URL bed yields no `audioUrl`/`audioLoop`)
    - src/utils/__tests__/slideshowAssembler.test.ts lines 2030-2075 (the existing vamp-bed case to mirror: `makeSlideGroup({ ..., bedAudioUrl, bedVampId, bedVampLabel })` + `assembleSlideshow(service, makeInputs({...}))`)
    - .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-RESEARCH.md "Pitfall 1" (why a fresh-doc fixture passes by accident)
  </read_first>
  <behavior>
    - Test A (the bug's regression): `getDoc` resolves `exists: () => true` with `makeGroupDoc({ bedAudioUrl: 'https://example.com/old.mp3' })`; calling `setGroupBedMedia('org-1','slot-1', { serviceId: 'service-1', bedVampId: 'vamp-3', bedVampLabel: 'Vamp C · C' })` (NO `bedAudioUrl` key) → `updateDoc` called once; payload has `bedVampId === 'vamp-3'`, `bedVampLabel === 'Vamp C · C'`, `bedAudioUrl === '__deleteField__'`, `updatedAt` defined, and `'slides' in payload === false`
    - Test B (same patch shape, existing doc with NO prior media): identical assertions — `bedAudioUrl` is still `'__deleteField__'` (explicit clear, never a bare omission)
    - Test C (guard the branch ordering): existing doc + `{ serviceId, bedAudioUrl: 'https://cdn.example/x.mp3', bedVampId: 'vamp-1', bedVampLabel: 'A · G' }` → payload `bedAudioUrl` is the URL (not the sentinel) — i.e. the existing branch still wins when a URL is present
    - Test D (fresh doc, `exists: () => false`): `{ serviceId, bedVampId, bedVampLabel }` → `setDoc` payload contains `bedVampId`/`bedVampLabel` and has no `bedAudioUrl` key at all (`stripUndefined` path, unchanged — proves the setDoc branch needs no change)
    - Assembler test E: a group with `bedVampId: 'vamp-1'`, `bedVampLabel: 'Pad · C'` and NO `bedAudioUrl` → for its lyric entry, `result[0].slide.audioUrl` is `undefined`, `result[0].slide.audioLoop` is falsy, `result[0].audioFromBed` is `false`
  </behavior>
  <action>
    In `src/stores/slideGroups.ts`, inside `setGroupBedMedia`'s `if (existing.exists())` block, add a THIRD branch to the existing `if (patch.clearAudio) { ... } else if (patch.bedAudioUrl !== undefined) { ... }` chain: `else if (patch.bedVampId) { update.bedVampId = patch.bedVampId; update.bedVampLabel = patch.bedVampLabel ?? ''; update.bedAudioUrl = deleteField() }`. It MUST come after the `bedAudioUrl !== undefined` check (so Track uploads and MP3-having vamp assignments keep taking the existing branch) and it MUST `deleteField()` `bedAudioUrl` explicitly — a bare omission would leave a stale Track/prior-vamp URL playing under the new vamp's amber "no MP3" chip (RESEARCH.md Pitfall 1). Add one short inline comment on the new branch (e.g. "no-MP3 vamp: keep id/label, clear any stale URL (142)") — rationale already lives in ADR-0168 cited above the function; do not add a paragraph. Do not touch the `setDoc` branch or `BedMediaPatch`.

    In `src/stores/__tests__/slideGroups.test.ts`, add a new `describe('setGroupBedMedia — no-MP3 vamp against an existing doc (Phase 142)')` after the existing 260918-nm2 describe, with Tests A–D from `<behavior>`, using the file's existing `vi.mocked(getDoc).mockResolvedValueOnce({ exists: () => true, id: 'slot-1', data: () => makeGroupDoc({...}) } as ...)` idiom and reading the payload via `vi.mocked(updateDoc).mock.calls[0]![1] as unknown as Record<string, unknown>` (for Test D read `vi.mocked(setDoc).mock.calls[0]![1]`). Test A's fixture must carry a prior `bedAudioUrl` — that is the realistic case and the one a fresh-doc fixture would have passed by accident.

    In `src/utils/__tests__/slideshowAssembler.test.ts`, add Test E next to the existing vamp-bed case (~line 2030), titled to say "bedVampId with no bedAudioUrl resolves no audio and no loop (142 — live playback tolerates a no-MP3 vamp bed)". No change to `slideshowAssembler.ts`.
  </action>
  <verify>
    <automated>cd /c/projects/worshipplanner && npx vitest run src/stores/__tests__/slideGroups.test.ts src/utils/__tests__/slideshowAssembler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "else if (patch.bedVampId)" src/stores/slideGroups.ts` outputs `1`
    - In `src/stores/slideGroups.ts`, the line containing `else if (patch.bedVampId)` has a HIGHER line number than the line containing `else if (patch.bedAudioUrl !== undefined)` (branch order preserved)
    - `grep -c "no-MP3 vamp against an existing doc (Phase 142)" src/stores/__tests__/slideGroups.test.ts` outputs `1`
    - `grep -c "bedVampId with no bedAudioUrl" src/utils/__tests__/slideshowAssembler.test.ts` outputs `1`
    - `npx vitest run src/stores/__tests__/slideGroups.test.ts src/utils/__tests__/slideshowAssembler.test.ts` exits 0 with at least 5 new passing tests
    - `git diff --stat -- src/utils/slideshowAssembler.ts` is empty (no assembler change)
  </acceptance_criteria>
  <done>The no-MP3 vamp patch against an existing doc writes id + label and deletes the stale URL; the existing-URL branch still wins when a URL is present; assembler tolerance proven by a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BackgroundControl — additive `variant="chip-popover"` branch (swatches · recents · ＋ upload/drop · inherited explainer), panel variant byte-identical</name>
  <files>src/components/slides/BackgroundControl.vue, src/components/slides/__tests__/BackgroundControl.test.ts</files>
  <read_first>
    - src/components/slides/BackgroundControl.vue (whole file, 151 lines — the `flush` prop's doc-comment style is the pattern for the new `variant` prop; `onFileSelected`, `useBackgroundUpload`, the `inheritedFrom` no-override guard, and the root-level progress/error `<p>`s that must stay shared by both variants)
    - .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-UI-SPEC.md "### 4. Background popover content" (the exact markup, classes, testids, and copy for the new branch — use it verbatim), "## Copywriting Contract" rows for Background, and "## Testid Contract" (panel-variant testids preserved; chip-popover testids new)
    - src/components/slides/__tests__/BackgroundControl.test.ts lines 1-60 (the `useBackgroundUpload` mock, `makeFile`, `selectFile` helpers, and the mount shape) — the existing describe is the regression suite for the panel variant and must not be edited
    - src/components/SongLyricEditor.vue lines 36-50 (the song-level call site that must keep rendering identically — it passes `image-url`, `caption`, `add-label`, `remove-label`, `is-editor`, `org-id`, nothing else)
  </read_first>
  <behavior>
    - Panel variant regression: every pre-existing test in `BackgroundControl.test.ts` passes unmodified
    - chip-popover, no imageUrl, no inheritedFrom, recents = 2 entries, isEditor true → renders `background-control-swatch-none` (with `border-indigo-600`), two `background-control-swatch-recent` elements (each with `data-url`), `background-control-swatch-upload` (aria-label "Upload a background image") containing `background-control-input`, and `background-control-chip-caption` with text "Recent in this service · drop a file on ＋"; NO `background-control-add`, `background-control-caption`, `background-control-image`, `background-control-filename`, `background-control-remove`
    - chip-popover, imageUrl equals recents[1].url → that recent swatch carries `border-indigo-600`; the None swatch does not
    - chip-popover, imageUrl set but NOT in recents → a `background-control-swatch-recent` with `data-url === imageUrl` renders FIRST (the current background is always a visible, highlighted tile)
    - chip-popover, recents = [] and no imageUrl → exactly None + upload tiles, no recent swatches
    - chip-popover, recents = 6 entries → at most 4 `background-control-swatch-recent`
    - clicking None with imageUrl set emits `remove` once; clicking None with no imageUrl emits nothing
    - clicking a recent swatch emits `attach` once with that url; clicking the recent whose url === imageUrl emits nothing
    - drop event on the tile row with `dataTransfer.files = [imageFile]` calls `uploadBackground(file, orgId)` and emits `attach` with the resolved URL; a rejected upload emits nothing and the error renders in `background-control-upload-error`
    - dragover on the tile row adds `border-indigo-500` to the upload tile; dragleave removes it
    - chip-popover with `inheritedFrom` → no swatches, no `background-control-input`, no chip caption; `background-control-inherited-explainer` with text "Managed on the song — edit it from the song's Lyrics tab."
    - chip-popover with isEditor false and no inheritedFrom → no tiles render, the chip caption still renders
  </behavior>
  <action>
    Props (`withDefaults(defineProps<...>())`): add `variant?: 'panel' | 'chip-popover'` (default `'panel'`) and `recents?: { url: string; label: string }[]` (default `() => []`); make `caption?: string` and `addLabel?: string` optional with default `''` (the chip-popover branch does not use them; existing call sites pass both, so the panel variant is unchanged). Give `variant` a doc comment in the same shape as the existing `flush` comment (why: a second call site outside this phase's scope must render pixel-identically), kept to 3–4 lines.

    Template: keep the root `<div ... data-testid="background-control">`. Wrap the two EXISTING `<template v-if="imageUrl">` / `<template v-else>` blocks in a single `<template v-else>` and insert, above them, `<template v-if="variant === 'chip-popover'">` carrying UI-SPEC §4's markup verbatim: the `flex flex-col gap-2` column; the tile row `v-if="isEditor && !inheritedFrom"` with `@dragover.prevent="isDragOver = true"`, `@dragleave="isDragOver = false"`, `@drop.prevent="onDrop"`; the None button (`h-8 w-11 flex-none rounded-md border ...`, class `border-indigo-600` when `!imageUrl` else `border-gray-700`, `data-testid="background-control-swatch-none"`, text `None`, `@click="selectNone"`); the `v-for="r in swatches"` recent buttons (`data-testid="background-control-swatch-recent"`, `:data-url="r.url"`, `:style="{ backgroundImage: \`url(${r.url})\` }"`, `border-2 border-indigo-600` when `imageUrl === r.url` else `border border-gray-700`, `:aria-label="r.label"`, `@click="selectRecent(r)"`); the upload `<label>` (`data-testid="background-control-swatch-upload"`, `aria-label="Upload a background image"`, dashed border, `border-indigo-500 bg-indigo-950/20` while `isDragOver` else `border-gray-700 hover:border-indigo-700`, glyph `＋`, containing the hidden `<input type="file" accept="image/*" data-testid="background-control-input" @change="onFileSelected">`); then `<p v-if="!inheritedFrom" data-testid="background-control-chip-caption">Recent in this service · drop a file on ＋</p>` and `<p v-else data-testid="background-control-inherited-explainer">Managed on the song — edit it from the song's Lyrics tab.</p>` with the §4 classes. Leave the root-level upload-progress / upload-error `<p>`s where they are (after both branches) so both variants share them.

    Script: add `const isDragOver = ref(false)`; `const swatches = computed(() => { const list = props.recents.slice(0, 4); if (props.imageUrl && !list.some((r) => r.url === props.imageUrl)) return [{ url: props.imageUrl, label: fileName.value }, ...list].slice(0, 4); return list })` — planner discretion: CONTEXT.md says "the currently-set one is highlighted", which is only possible if it is in the tile set, so the current URL is guaranteed a tile; `function selectNone() { if (props.imageUrl) emit('remove') }`; `function selectRecent(r: { url: string }) { if (r.url !== props.imageUrl) emit('attach', r.url) }`; extract the body of `onFileSelected` after the `input.value = ''` / file guard into `async function uploadFile(file: File)` (reset → try uploadBackground → emit attach; catch → nothing, keep the existing comment's wording about a failed upload never clearing an existing background) and call it from both `onFileSelected` and `function onDrop(event: DragEvent) { isDragOver.value = false; const file = event.dataTransfer?.files?.[0]; if (file) void uploadFile(file) }`. Import `ref` alongside `computed`. No new composable, no store import — the control stays emit-only (PATTERNS.md "Emit-only child, single write owner").

    Tests: append a new `describe('BackgroundControl — variant="chip-popover" (Phase 142)')` to `BackgroundControl.test.ts` covering every line of `<behavior>`; drive the drop case with `wrapper.get('[data-testid="background-control-swatch-upload"]').element.parentElement` (the tile row) via `await row.trigger('drop', { dataTransfer: { files: [file] } })` (vue-test-utils merges the passed object onto the event, so `event.dataTransfer.files[0]` resolves). Do not edit any existing test in the file.
  </action>
  <verify>
    <automated>cd /c/projects/worshipplanner && npx vitest run src/components/slides/__tests__/BackgroundControl.test.ts src/components/slides/__tests__/SlideGrid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "variant === 'chip-popover'" src/components/slides/BackgroundControl.vue` outputs `1`
    - `grep -c 'data-testid="background-control-swatch-none"' src/components/slides/BackgroundControl.vue` outputs `1`; same for `-swatch-recent`, `-swatch-upload`, `-chip-caption`, `-inherited-explainer` (each `1`)
    - `grep -c "Managed on the song — edit it from the song's Lyrics tab." src/components/slides/BackgroundControl.vue` outputs `1`
    - `grep -c "Recent in this service · drop a file on ＋" src/components/slides/BackgroundControl.vue` outputs `1`
    - `grep -c "useBackgroundUpload" src/components/slides/BackgroundControl.vue` outputs `2` (import + call — no second upload path)
    - `grep -c "import.*from '@/stores" src/components/slides/BackgroundControl.vue` outputs `0` (still emit-only)
    - `git diff -U0 -- src/components/slides/__tests__/BackgroundControl.test.ts | grep '^-[^-]' | wc -l` outputs `0` (no existing test line removed)
    - `npx vitest run src/components/slides/__tests__/BackgroundControl.test.ts src/components/slides/__tests__/SlideGrid.test.ts` exits 0
  </acceptance_criteria>
  <done>`variant="chip-popover"` renders the §4 popover body with recents, current-URL highlight, None/recent selection emits, and drop-to-upload; the default variant and its 16 existing tests are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SlotVideoOutputControl — Full-screen / Banner thumbnail tiles + dynamic sizeHint caption (script unchanged)</name>
  <files>src/components/slides/SlotVideoOutputControl.vue, src/components/slides/__tests__/SlotVideoOutputControl.test.ts</files>
  <read_first>
    - src/components/slides/SlotVideoOutputControl.vue (whole file, 57 lines — keep `mode`, `select()`, the `change` emit, the `editable` prop and the `:disabled="!editable"` bindings exactly; only the template's visual structure and the caption change)
    - .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-UI-SPEC.md "### 3. Display popover content" (the tile markup verbatim) and the Copywriting Contract rows "Display popover — Full-screen hint" / "— Banner hint"
    - src/components/slides/__tests__/SlotVideoOutputControl.test.ts (whole file, 64 lines — `mountControl(videoOutput?, editable)` helper; the last test asserts the old static caption and must be replaced)
    - docs/design/slides-tab.dc.html lines 2276-2283 (`sizeHint` in the mockup's `renderVals` — the two hint strings' source of truth)
  </read_first>
  <behavior>
    - with no `slot.videoOutput`: fullscreen tile has `aria-checked="true"` and class `border-indigo-600`; banner tile has `aria-checked="false"` and class `border-gray-700`; caption text is exactly "Fills the screen on every output."
    - with `mode: 'banner'`: banner tile active (`border-indigo-600`), caption is exactly "Lower third over the live camera feed. Video output only."
    - the two tiles sit inside an element with `role="radiogroup"` and `aria-label="Video output display mode"`
    - clicking Banner while fullscreen emits `change` with `{ mode: 'banner' }`; clicking the active tile emits nothing; `editable: false` emits nothing while both tiles still render (all pre-existing tests keep passing)
  </behavior>
  <action>
    Replace the template with UI-SPEC §3's markup: root `<div class="flex flex-col gap-2" data-testid="slot-video-output-row">`; a `<div class="flex gap-2" role="radiogroup" aria-label="Video output display mode">` wrapping two `<button type="button" role="radio">` tiles — each `flex-1 flex flex-col gap-1.5 rounded-md border p-1.5 transition-colors`, active class `border-indigo-600 bg-indigo-950/30`, inactive `border-gray-700 bg-gray-900 hover:border-gray-600`, keeping `data-testid="slot-video-output-fullscreen-btn"` / `"slot-video-output-banner-btn"`, `:aria-checked`, `:disabled="!editable"` (defensive — RESEARCH.md Open Question 2: the chip wrapper is the real lockedness gate), and `@click="select(...)"`; inside each tile the thumbnail box (`relative h-9 rounded border border-gray-700 bg-gray-950 overflow-hidden` with the full-stage `absolute inset-1 rounded-sm bg-indigo-900/60` fill for Full-screen and the lower-third `absolute inset-x-1 bottom-1 h-2.5 rounded-sm bg-indigo-900/60` bar for Banner) and the `text-[11px] text-gray-200` label "Full-screen" / "Banner"; then `<p class="text-[10.5px] leading-[1.45] text-gray-500" data-testid="slot-video-output-caption">{{ sizeHint }}</p>`.

    Script: add `const sizeHint = computed(() => mode.value === 'banner' ? 'Lower third over the live camera feed. Video output only.' : 'Fills the screen on every output.')`. Nothing else changes. Replace the file's head comment (the "Two-segment pill toggle (137-UI-SPEC.md Surface 1)" HTML comment) with one short line noting the tiles come from 142-UI-SPEC §3 — no paragraph.

    Tests: replace the single 'caption copy states scope' test with two tests (fullscreen hint / banner hint, exact strings) and add one test asserting the active tile carries `border-indigo-600` and the radiogroup wrapper's `aria-label`. Leave the other six tests unmodified.
  </action>
  <verify>
    <automated>cd /c/projects/worshipplanner && npx vitest run src/components/slides/__tests__/SlotVideoOutputControl.test.ts src/components/slides/__tests__/SlideGrid.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Fills the screen on every output." src/components/slides/SlotVideoOutputControl.vue` outputs `1`
    - `grep -c "Lower third over the live camera feed. Video output only." src/components/slides/SlotVideoOutputControl.vue` outputs `1`
    - `grep -c 'role="radiogroup"' src/components/slides/SlotVideoOutputControl.vue` outputs `1`
    - `grep -c 'data-testid="slot-video-output-fullscreen-btn"' src/components/slides/SlotVideoOutputControl.vue` outputs `1`; same for `-banner-btn`, `-caption`, `-row` (each `1`)
    - `grep -c "emit('change', { mode: next })" src/components/slides/SlotVideoOutputControl.vue` outputs `1` (emit shape unchanged)
    - `npx vitest run src/components/slides/__tests__/SlotVideoOutputControl.test.ts src/components/slides/__tests__/SlideGrid.test.ts` exits 0
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Display tiles render with the mockup's hint copy per selected mode; the emit contract and the four preserved testids are intact; the plan ends type-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Firestore (`slideGroups/{slotId}`) | `setGroupBedMedia`/`setGroupBackground` write directly through the client SDK; `firestore.rules` `isOrgEditor(orgId) && parentDraft(...)` is the enforcement point (unchanged) |
| browser → Cloud Storage (`orgs/{orgId}/backgrounds/...`) | `useBackgroundUpload` validates MIME + size before upload (unchanged, reused by the new drop target) |
| user file drop → `BackgroundControl.onDrop` | untrusted `File` from `dataTransfer` crosses into the existing upload composable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-01 | Tampering | `setGroupBedMedia` third branch | high | mitigate | `update.bedAudioUrl = deleteField()` in the new branch — a stale Track/prior-vamp URL must never keep playing under a no-MP3 vamp chip (Task 1 Test A asserts the sentinel) |
| T-142-02 | Tampering (input validation, ASVS V5) | `BackgroundControl.onDrop` | medium | mitigate | dropped `File` goes through the same `uploadBackground(file, orgId)` → existing `validate()` (image MIME + `BACKGROUND_MAX_BYTES`); no second upload path is added (Task 2 acceptance: `useBackgroundUpload` referenced exactly twice) |
| T-142-03 | Elevation of Privilege | `BackgroundControl` chip-popover tiles | medium | mitigate | tile row gated `v-if="isEditor && !inheritedFrom"`; the eventual write still passes `SlideGrid`'s `canWriteGroupMedia` re-check and `firestore.rules` — UI gating is UX, not the boundary |
| T-142-04 | Information Disclosure | `recents` thumbnails | low | accept | URLs shown are backgrounds already rendered in the same service's slideshow for the same org viewer; no new read, no cross-org data |
| T-142-05 | Tampering (XSS) | `backgroundImage: url(...)` style binding, chip/tile text | low | mitigate | Vue style-object binding and text interpolation only; no `v-html`; URLs originate from our own Storage upload path |
| T-142-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/slideGroups.test.ts src/utils/__tests__/slideshowAssembler.test.ts src/components/slides/__tests__/BackgroundControl.test.ts src/components/slides/__tests__/SlotVideoOutputControl.test.ts src/components/slides/__tests__/SlideGrid.test.ts` exits 0 (SlideGrid.test.ts proves the panel variant and the video-output testids are still what the grid's current template expects)
- `npm run type-check` exits 0 (vue-tsc --build — includes the three edited test files)
- `git diff --stat` touches only the seven files in `files_modified`
</verification>

<success_criteria>
- setGroupBedMedia writes a no-MP3 vamp against an existing doc (id + label, URL deleted) — regression-tested on the existing-doc fixture
- BackgroundControl has a working `chip-popover` variant and an untouched `panel` variant
- SlotVideoOutputControl renders tiles with the two mockup hint strings, same emit
- All touched suites + SlideGrid.test.ts green; type-check clean
</success_criteria>

<output>
Create `.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-01-SUMMARY.md` when done
</output>
