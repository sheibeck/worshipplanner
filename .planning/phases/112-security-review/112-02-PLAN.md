---
phase: 112-security-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/112-security-review/112-FINDINGS-auth-functions.md
autonomous: true
requirements: [R322]

must_haves:
  truths:
    - "auth/custom-claims (src/stores/auth.ts), route guards (src/router/index.ts), and Cloud Functions authorization (functions/src/**) are each reviewed with severity-ranked findings."
    - "ARCH-005 (undeployed org-provisioning Cloud Functions) is assessed under a security lens with its own explicit severity call."
    - "ARCH-018 (super-admin universal isOrgEditor grant) is re-evaluated as a genuine privilege-scope security finding with its own explicit severity call, not merely echoed as an accepted architectural note."
    - "Every finding carries a severity (per CONTEXT rubric) and a concrete location (file:line, function name, or route)."
    - "No files under src/, functions/, firestore.rules, or storage.rules were modified; no deploy occurred."
  artifacts:
    - .planning/phases/112-security-review/112-FINDINGS-auth-functions.md
  key_links:
    - "Server-side re-verification (verifyAppCaller/resolveOrgId, assertSuperAdminCaller) is assessed against the client-side claims in src/stores/auth.ts — an authz gap here is a real bypass."
    - "Route-guard meta (requiresAuth/requiresEditor/requiresSuperAdmin) is checked against the callable/rule enforcement it fronts (guards are convenience, not the boundary)."
---

<objective>
Conduct the security review pass for **auth/custom-claims + route guards + Cloud Functions authorization** (ROADMAP areas 2 and 4 of 6), folding in the Phase 110 handoff items ARCH-005 and ARCH-018, and write the disjoint per-plan findings file `112-FINDINGS-auth-functions.md`.

Purpose: Surface auth-bypass, privilege-scope, and function-authorization findings — including a security-lens assessment of the undeployed provisioning functions and the super-admin universal-grant residual — for the Plan 04 consolidation and Phase 113 remediation.
Output: `.planning/phases/112-security-review/112-FINDINGS-auth-functions.md`.

This is a REVIEW-ONLY plan. The executor reads source/functions ITSELF (it is a subagent and CANNOT spawn sub-agents). It changes NO code and deploys NOTHING.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-security-review/112-CONTEXT.md
@.planning/phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review auth/custom-claims, route guards, and Cloud Functions authorization; write severity-ranked findings</name>
  <files>.planning/phases/112-security-review/112-FINDINGS-auth-functions.md</files>
  <read_first>
    - src/stores/auth.ts (946 lines) — custom-claim handling: isSuperAdmin/orgId/role refs (~111-160), refreshOrgClaim/claim retry (~277-325), loadOrgContext + claim/orgIds reconciliation (~420-445), and the onAuthStateChanged flow (~600-616). The claim is convenience only; note where client code assumes a claim that the server must actually enforce.
    - src/router/index.ts — every route's meta (requiresAuth / requiresEditor / requiresSuperAdmin) and the beforeEach guard; verify which routes are intentionally auth-only (present/audience/run, per R261/R267/R270/R275 comments) and whether any privileged route lacks a guard.
    - functions/src/index.ts — the callable/authz surface: verifyAppCaller (~136), resolveOrgId (~152), and every onCall/onRequest/onDocumentCreated/onSchedule handler's auth check (does it re-verify caller identity + org membership server-side?).
    - functions/src/orgProvisioning.ts — assertSuperAdminCaller (~61), resolveAdminTarget (~81), onboardOrganization/assignOrgAdmin/listOrganizations/setOrgActive handlers and their guards.
    - functions/src/claimsHelpers.ts, superAdminClaims.ts, orgMembershipClaims.ts, bootstrapSuperAdmin.ts, inviteOnboarding.ts — the claim-setting triggers/callables and their authorization.
  </read_first>
  <action>
    Review three dimensions area-by-area: (dimension A) auth/custom-claims — whether every privileged action is gated by a server-verified claim (not just a client ref), whether claim refresh/retry can be raced into an elevated state, and whether super-admin/org-role claims are set only by authorized triggers; (dimension B) route guards — whether requiresAuth/requiresEditor/requiresSuperAdmin meta matches the sensitivity of each route AND whether the real enforcement lives server-side (guards are convenience only); (dimension C) Cloud Functions authorization — for every callable/HTTP/trigger handler, whether it re-verifies caller identity and org membership server-side (verifyAppCaller/resolveOrgId/assertSuperAdminCaller) before acting, and whether any handler trusts client-supplied orgId/role. For EACH issue write a finding with a stable id (SEC-A-01, SEC-A-02, ...), a severity per the CONTEXT rubric, a concrete location (file:line / function name / route path), the observed behavior, and the impact. Group findings under "## Critical/High" and "## Medium/Low". Change NO code; deploy NOTHING.
  </action>
  <acceptance_criteria>
    - 112-FINDINGS-auth-functions.md covers auth/custom-claims, route guards, AND Cloud Functions authorization.
    - Every finding has an id, a severity, and a concrete location (file:line / function name / route).
    - Findings are grouped into "## Critical/High" and "## Medium/Low" sections.
    - `git status --porcelain -- src functions firestore.rules storage.rules` is empty.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/112-security-review/112-FINDINGS-auth-functions.md; test -f "$F" && grep -q "## Critical/High" "$F" && grep -q "## Medium/Low" "$F" && grep -qiE "functions/src/|src/router|src/stores/auth.ts" "$F"; git status --porcelain -- src functions firestore.rules storage.rules | grep -q . && echo "SOURCE MODIFIED - FAIL" || echo "no source changes OK"</automated>
  </verify>
  <done>The findings file enumerates severity-ranked, concretely-located auth/claims, route-guard, and function-authorization findings split C/H vs M/L; no code modified.</done>
</task>

<task type="auto">
  <name>Task 2: Assess ARCH-005 and re-evaluate ARCH-018 under a security lens</name>
  <files>.planning/phases/112-security-review/112-FINDINGS-auth-functions.md</files>
  <read_first>
    - .planning/phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md — the ARCH-005 detail (~211) and ARCH-018 detail (~565), and "## Handoff to Phase 112" (~685-700).
    - functions/src/orgProvisioning.ts + its re-export in functions/src/index.ts — the deploy state of onboardOrganization/assignOrgAdmin/listOrganizations/setOrgActive (built + tested, UNDEPLOYED per their hand-over notes).
    - firestore.rules lines ~28-50 (isOrgEditor / isSuperAdmin helpers, the `isSuperAdmin() ||` universal disjunct) and src/stores/auth.ts ~588-613 (the client-side super-admin membership contract).
  </read_first>
  <action>
    Write two explicit findings appended to 112-FINDINGS-auth-functions.md. For ARCH-005: assess the security implication of the org-provisioning callables being built+tested but UNDEPLOYED — whether the undeployed state itself is a risk (isolation architecture unverifiable against live prod; a stale/missing authz path), assign a severity per the CONTEXT rubric under a security (not architecture) lens, give its concrete location (functions/src/orgProvisioning.ts + the index.ts re-export), and state whether it belongs in Phase 113's Critical/High set or backlog. For ARCH-018: re-evaluate the super-admin universal `isOrgEditor` grant as a genuine privilege-scope security finding (a super-admin's client SDK could legally write a membership doc for any org; the R226 invariant holds only as client-code contract, not a rules-level invariant) — assign a fresh severity under a security lens rather than deferring to its Phase 78 "accepted" status, give its concrete location (firestore.rules:~28-43; src/stores/auth.ts:~588-613), and if rated Critical/High name the ALLOW-case / write-guard emulator test a Phase 113 fix would need. Place each in the appropriate "## Critical/High" or "## Medium/Low" section. Add an "## Artifacts this phase produces" note pointing to this file and the eventual 112-SECURITY-REVIEW.md. Change NO code; deploy NOTHING.
  </action>
  <acceptance_criteria>
    - The findings file contains an explicit ARCH-005 assessment with its own security-lens severity and concrete location.
    - The findings file contains an explicit ARCH-018 re-evaluation with its own security-lens severity and concrete location (not merely restating the Phase 78 acceptance).
    - Both are placed in the correct C/H or M/L section; any C/H names the required emulator test if rules-related.
    - `git status --porcelain -- src functions firestore.rules storage.rules` is empty.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/112-security-review/112-FINDINGS-auth-functions.md; test -f "$F" && grep -q "ARCH-005" "$F" && grep -q "ARCH-018" "$F"; git status --porcelain -- src functions firestore.rules storage.rules | grep -q . && echo "SOURCE MODIFIED - FAIL" || echo "no source changes OK"</automated>
  </verify>
  <done>ARCH-005 and ARCH-018 are each assessed with an independent security-lens severity and concrete location and slotted into C/H or M/L; no code modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client claims → server enforcement | Client-side claims/route guards are convenience; the real authz boundary is server-side (functions + rules). A gap where the server trusts a client claim is a bypass. |
| review process → source/functions tree | The review reads source; it must not modify any file (review-only guarantee). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-04 | Elevation of privilege | Cloud Functions callables trusting client orgId/role | high | mitigate | Review asserts every handler re-verifies caller + org membership server-side (verifyAppCaller/resolveOrgId/assertSuperAdminCaller); gaps become findings. |
| T-112-05 | Spoofing | route guards vs server enforcement | medium | mitigate | Review confirms guards are convenience only and the real boundary is enforced server-side; document any guard relied on as the sole gate. |
| T-112-06 | Tampering | source/functions files | high | mitigate | Review-only; acceptance gate asserts `git status --porcelain` clean for src/functions/rules. |
</threat_model>

<verification>
- Auth/claims, route guards, and function authorization all reviewed with severity + concrete location each.
- ARCH-005 and ARCH-018 assessed with independent security-lens severities and locations.
- Critical/High and Medium/Low separated.
- No source, functions, or *.rules files modified.
</verification>

<success_criteria>
`112-FINDINGS-auth-functions.md` exists with severity-ranked, concretely-located auth/claims, route-guard, and function-authorization findings (including ARCH-005 and ARCH-018), C/H vs M/L separated, and no code changes.
</success_criteria>

<output>
Create `.planning/phases/112-security-review/112-02-SUMMARY.md` when done.
</output>
