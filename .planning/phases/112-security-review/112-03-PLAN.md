---
phase: 112-security-review
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md
autonomous: true
requirements: [R322]

must_haves:
  truths:
    - "share-token / public-page exposure (ShareView, QuarterShareView, the shareTokens/serviceShareLinks/quarterShares rules, ?view=stage) is reviewed with severity-ranked findings."
    - "PII handling on public/shared surfaces (member names, emails, contact data on unauthenticated pages) is reviewed with severity-ranked findings."
    - "cost/abuse controls (the api proxy caps, per-uid rate limit, per-org email quota, global maxInstances, email/send fan-out) are reviewed with severity-ranked findings."
    - "Every finding carries a severity (per CONTEXT rubric) and a concrete location (file:line, function name, rule path, or route)."
    - "No files under src/, functions/, firestore.rules, or storage.rules were modified; no deploy occurred."
  artifacts:
    - .planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md
  key_links:
    - "The public /share/:token route (no requiresAuth) is assessed against the shareTokens rule's public-read scope and the exact fields ShareView renders — a leak here exposes data to unauthenticated viewers."
    - "The api proxy cost caps (rate limit + token clamp + org enablement) are assessed as the boundary preventing an open-relay / cost-abuse path on the owner's billed AI/Bible quota."
---

<objective>
Conduct the security review pass for **share-token / public-page exposure + PII handling + cost/abuse controls** (ROADMAP areas 5 and 6 of 6) and write the disjoint per-plan findings file `112-FINDINGS-sharetoken-pii-abuse.md`.

Purpose: Surface unauthenticated-exposure, PII-leak, and cost/abuse findings for the Plan 04 consolidation and Phase 113 remediation.
Output: `.planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md`.

This is a REVIEW-ONLY plan. The executor reads source/functions/rules ITSELF (it is a subagent and CANNOT spawn sub-agents). It changes NO code and deploys NOTHING.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-security-review/112-CONTEXT.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review share-token / public-page exposure and PII handling; write severity-ranked findings</name>
  <files>.planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md</files>
  <read_first>
    - src/router/index.ts — the /share/:token and /quarter-share/:token routes (~148-157, intentionally NO requiresAuth) and the ?view=stage present/audience routes (~79-128).
    - src/views/ShareView.vue (227 lines) — how it resolves the token (getDoc on shareTokens/{token} ~205-213), the isStageView computed (~174), and exactly which fields it renders on the public page (names, role assignments, any contact/email/PII in the "Who's Serving" section ~115).
    - src/views/QuarterShareView.vue (273 lines) — the same for quarter shares.
    - firestore.rules — shareTokens/{token} (~338-355, public read scope + editor-scoped create + revocation delete), serviceShareLinks (~356-366), quarterShares, and lockSnapshots (~236-242, the captured public snapshot content).
    - docs/adr/ — the share-revocation and public-read ADRs (ADR-0007 org-slug public read; any share-token ADR) for prior rationale.
  </read_first>
  <action>
    Review two dimensions area-by-area: (dimension A) share-token / public-page exposure — whether the public shareTokens/serviceShareLinks/quarterShares read scope exposes more than the token holder should see, whether a token can be guessed/enumerated or is non-revocable, whether ?view=stage or the snapshot docs leak beyond intent, and whether an unauthenticated viewer of /share/:token can reach any org-scoped data beyond the shared service; (dimension B) PII handling — exactly which personal fields (member names, emails, phone/contact, role assignments) render on unauthenticated public pages or land in snapshot docs, and whether that exposure is necessary and consented. For EACH issue write a finding with a stable id (SEC-S-01, SEC-S-02, ...), a severity per the CONTEXT rubric, a concrete location (file:line / rule path / route), the observed behavior, and the impact. Group findings under "## Critical/High" and "## Medium/Low". For any share/rules issue rated C/H, name the ALLOW-case emulator test a Phase 113 fix must add. Change NO code; deploy NOTHING.
  </action>
  <acceptance_criteria>
    - The findings file covers both share-token/public-page exposure AND PII handling.
    - Every finding has an id, a severity, and a concrete location.
    - Findings grouped into "## Critical/High" and "## Medium/Low"; each C/H rules-related finding names its required ALLOW-case test.
    - `git status --porcelain -- src functions firestore.rules storage.rules` is empty.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md; test -f "$F" && grep -q "## Critical/High" "$F" && grep -q "## Medium/Low" "$F" && grep -qiE "ShareView|shareTokens|/share/" "$F"; git status --porcelain -- src functions firestore.rules storage.rules | grep -q . && echo "SOURCE MODIFIED - FAIL" || echo "no source changes OK"</automated>
  </verify>
  <done>The findings file enumerates severity-ranked, concretely-located share-exposure and PII findings split C/H vs M/L; no code modified.</done>
</task>

<task type="auto">
  <name>Task 2: Review cost/abuse controls; write severity-ranked findings</name>
  <files>.planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md</files>
  <read_first>
    - functions/src/index.ts — the api proxy and its cost caps: PROXY_TARGETS/SECRET_INJECTED (~77-87), verifyAppCaller/resolveOrgId (~136-170) that make it not an open relay, readAiProxyLimits/enforceModelAndTokens with the max_tokens clamp (~184-294), checkOrgAiEnablement/checkOrgBibleEnablement fail-closed (~294-363), checkAndConsumeRateLimit per-uid fixed window R161 (~364-402), checkAndConsumeOrgEmailQuota per-org daily R171 (~403-448), setGlobalOptions maxInstances R172 (~193-204), and the api handler (~475).
    - functions/src/index.ts — the email/send fan-out: sendScheduledReminders/runScheduledMessagingCron (~1766-1930), dispatchDueScheduledMessages (~1972), queueServiceMessage (~2205-2308), sendQueuedMessage (~2446) — the caps/quotas bounding message and email volume.
    - docs/adr/0021-r164-*.md — the maxInstances ceiling rationale (referenced at index.ts ~193).
  </read_first>
  <action>
    Review the cost/abuse dimension area-by-area: whether the api proxy can be driven as an open relay or past the owner's billed AI/Bible quota (caller verification, per-uid rate limit, per-org enablement, token clamp), whether the rate-limiter's fail-open posture vs the enablement checks' fail-closed posture is correct for each path, whether the global and per-function maxInstances ceilings actually bound fan-out cost, and whether the email/message send path (per-org daily quota, scheduled cron, queue dispatch) can be abused to send excessive or unauthorized email. For EACH issue write a finding with a stable id (SEC-C-01, SEC-C-02, ...), a severity per the CONTEXT rubric, a concrete location (functions/src/index.ts:LINE or function name), the observed behavior, and the impact. Place findings in the correct "## Critical/High" or "## Medium/Low" section. Add an "## Artifacts this phase produces" note pointing to this file and the eventual 112-SECURITY-REVIEW.md. Change NO code; deploy NOTHING.
  </action>
  <acceptance_criteria>
    - The findings file contains a cost/abuse-controls review covering the api proxy caps AND the email/send fan-out.
    - Every finding has an id, a severity, and a concrete location (function name or file:line).
    - Findings placed in the correct C/H or M/L section.
    - `git status --porcelain -- src functions firestore.rules storage.rules` is empty.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md; test -f "$F" && grep -qiE "rate.?limit|maxInstances|quota|proxy|fan-out|email" "$F"; git status --porcelain -- src functions firestore.rules storage.rules | grep -q . && echo "SOURCE MODIFIED - FAIL" || echo "no source changes OK"</automated>
  </verify>
  <done>The findings file enumerates severity-ranked, concretely-located cost/abuse findings across the api proxy and email/send fan-out, placed in C/H vs M/L; no code modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated viewer → public share pages | /share/:token and /quarter-share/:token serve data to anyone with the URL; the shareTokens read scope + rendered fields define the exposure. |
| any caller → api proxy (billed quota) | The api proxy spends the owner's AI/Bible/email quota; caller verification + rate limits + token clamp are the cost/abuse boundary. |
| review process → source/functions tree | The review reads source; it must not modify any file (review-only guarantee). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-07 | Information disclosure | public share pages / snapshot docs | high | mitigate | Review asserts the shareTokens read scope + rendered fields expose only the intended shared service; PII leaks become findings. |
| T-112-08 | Denial of service | api proxy / email fan-out cost caps | high | mitigate | Review asserts caller verification + per-uid rate limit + per-org email quota + maxInstances bound cost; gaps become findings. |
| T-112-09 | Tampering | source/functions files | high | mitigate | Review-only; acceptance gate asserts `git status --porcelain` clean for src/functions/rules. |
</threat_model>

<verification>
- Share-token/public-page exposure, PII handling, and cost/abuse controls all reviewed with severity + concrete location each.
- Critical/High and Medium/Low separated; each C/H rules-related finding names its required ALLOW-case test.
- No source, functions, or *.rules files modified.
</verification>

<success_criteria>
`112-FINDINGS-sharetoken-pii-abuse.md` exists with severity-ranked, concretely-located share-exposure, PII, and cost/abuse findings, C/H vs M/L separated, and no code changes.
</success_criteria>

<output>
Create `.planning/phases/112-security-review/112-03-SUMMARY.md` when done.
</output>
