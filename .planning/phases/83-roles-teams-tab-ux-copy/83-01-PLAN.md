---
phase: 83-roles-teams-tab-ux-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RolesConfigPanel.vue
  - src/components/__tests__/RolesConfigPanel.test.ts
  - src/types/roster.ts
  - src/components/TeamsConfigPanel.vue
  - src/components/__tests__/TeamsConfigPanel.test.ts
  - src/views/RosterView.vue
  - src/views/__tests__/RosterView.test.ts
autonomous: true
requirements: [R244, R245, R246]

must_haves:
  truths:
    - "Opening the Roles or Teams tab shows the panel constrained to max-w-4xl (not full-width); the Volunteers tab table stays full-width (R244)."
    - "The Delete affordance in both RolesConfigPanel and TeamsConfigPanel renders as a real destructive button (red-fill bg-red-900/20 + text-red-400), not a text link, with the inline soft-warn confirm behavior unchanged (R245)."
    - "The Roles panel header no longer frames the default count as a soft/optional target; it states the scheduler auto-fills that count per service (R246)."
    - "RolesConfigPanel has a real unit test file (previously none existed) exercising the Delete button, the corrected copy, and add/save/delete."
  artifacts:
    - src/views/RosterView.vue
    - src/components/RolesConfigPanel.vue
    - src/components/TeamsConfigPanel.vue
    - src/components/__tests__/RolesConfigPanel.test.ts
    - src/types/roster.ts
  key_links:
    - "RosterView roles/teams v-show wrappers carry max-w-4xl; the volunteers v-show wrapper does NOT (constraint is scoped)."
    - "Both panels' existing Delete <button> @click handler (confirmDeleteId = row.role.id / row.team.id) and confirm block are untouched — class swap only."
    - "RolesConfigPanel.test.ts uses vi.mock('@/stores/roster') mirroring TeamsConfigPanel.test.ts's mount/mock pattern (roles, addRole, updateRole, deleteRole)."
---

<objective>
Ship the three surgical, client-only UI-polish edits for Phase 83 (final phase of v2.2): constrain the Roles/Teams config-tab width (R244), turn the Delete affordance in both panels into a real destructive button (R245), and correct the schedulable-roles "soft planning target" copy so it matches the scheduler's real auto-fill behavior (R246). Also close the RESEARCH-identified test gap by creating the missing RolesConfigPanel unit test, and fix the identical stale doc-comment in the type definition.

Purpose: These are owner testing-feedback items #2 (copy) and #3 (width + delete UX). Every change reuses an existing in-app precedent (SettingsView's max-w-4xl width token and its "Clear Credentials" destructive-button style); nothing new is invented. R246 is copy-only — the scheduler's behavior is confirmed correct and must NOT change.

Output: width-constrained Roles/Teams tab panels, a destructive Delete button in both panels, accurate schedulable-roles copy, a new RolesConfigPanel.test.ts, and a corrected type doc-comment. No rules/functions/deploy — client-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-roles-teams-tab-ux-copy/83-RESEARCH.md

# Source of truth for the two reused precedents (width token + destructive button):
@src/views/SettingsView.vue

# Files this plan edits:
@src/components/RolesConfigPanel.vue
@src/components/TeamsConfigPanel.vue
@src/views/RosterView.vue
@src/types/roster.ts

# Existing test to mirror for the new RolesConfigPanel test (mount + vi.mock pattern):
@src/components/__tests__/TeamsConfigPanel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RolesConfigPanel — corrected copy (R246), destructive Delete button (R245), new test, and type doc-comment fix</name>
  <files>src/components/RolesConfigPanel.vue, src/components/__tests__/RolesConfigPanel.test.ts, src/types/roster.ts</files>
  <behavior>
    New file src/components/__tests__/RolesConfigPanel.test.ts, mirroring TeamsConfigPanel.test.ts's structure (mount + Pinia + a vi.mock('@/stores/roster') returning { roles, addRole, updateRole, deleteRole }; seed 2+ roles including a `group` field, e.g. { id: 'r-1', name: 'Guitar', group: 'band', defaultCount: 2, order: 0 }):
    - Copy (R246): the header paragraph text contains "auto-fill" and "each service" (case-insensitive), and does NOT contain the old inaccurate framing.
      <!-- planner-discipline-allow: soft planning target -->
      <!-- planner-discipline-allow: not a hard cap -->
      Assert wrapper.text() (lowercased) does not include "soft planning target" nor "not a hard cap".
    - Delete button (R245): the per-row Delete trigger button's class list contains "bg-red-900/20" and "text-red-400" (and keeps the compact row sizing "text-xs", "px-3", "py-1.5"); its text is still "Delete".
    - Delete confirm flow unchanged: clicking a row's "Delete" button reveals the inline soft-warn confirm containing "Delete the '...' role?" and "This cannot be undone."; clicking "Cancel" dismisses it without calling deleteRole; re-opening and clicking "Delete Role" calls deleteRole with that role's id.
    - Save: editing a row's name input then clicking "Save Role" calls updateRole once with the trimmed draft { name, defaultCount } (not on every keystroke).
    - Add: filling the Add-Role name input and clicking the add-row "Save Role" calls addRole with { name, group, defaultCount, order }.
  </behavior>
  <action>
    Write the test FIRST (RED against the current copy and text-link classes), then apply the source edits to turn it GREEN. All three edits reuse existing app precedents per 83-RESEARCH.md — do not invent new tokens.

    1. Create src/components/__tests__/RolesConfigPanel.test.ts per the &lt;behavior&gt; block. Mirror TeamsConfigPanel.test.ts exactly for mount/setup: import mount from '@vue/test-utils', createPinia/setActivePinia from 'pinia', vi.mock('@/stores/roster') exposing roles + addRole + updateRole + deleteRole spies, and setActivePinia in beforeEach. Because RolesConfigPanel groups rows by role.group, every seeded role MUST include a valid `group` ('band' | 'vocals' | 'tech' | 'other') so its row (and therefore its Delete button) renders. Select the row Delete buttons via findAll('button').filter(b =&gt; b.text() === 'Delete').

    2. RolesConfigPanel.vue header copy (R246), the &lt;p&gt; at line 6: replace the current sentence with an accurate one confirmed by 83-RESEARCH.md's traced scheduler behavior. Recommended exact string (Claude's discretion on precise wording, but it MUST contain "auto-fill" and "each service" and MUST drop the soft/optional-target and no-cap framing): "Schedulable roles grouped by Band, Tech, and Other. Default count is the number of volunteers the scheduler auto-fills for this role each service." Do NOT touch scheduler.ts — R246 is copy-only; the auto-fill-up-to-count behavior is already correct and owner-accepted.

    3. RolesConfigPanel.vue Delete button (R245), the &lt;button&gt; at lines 43-47: this is ALREADY a real &lt;button type="button"&gt; styled as a text link — CLASS SWAP ONLY. Keep the element, its @click="confirmDeleteId = row.role.id" handler, the "Delete" text, and the entire confirmDeleteId soft-warn confirm block (lines 50-66) untouched. Change only its class from the text-link style to the destructive-button family mirroring SettingsView.vue's "Clear Credentials" button (SettingsView.vue:124-130), trimmed to the row's compact sizing: "text-xs px-3 py-1.5 rounded-md font-medium bg-red-900/20 hover:bg-red-900/40 text-red-400 transition-colors". Keep the adjacent Save button as-is.

    4. src/types/roster.ts line 9 doc-comment fix (RESEARCH Open Q1 — zero-risk, recommended in scope): the `defaultCount` field carries the identical stale "soft planning default, NOT a hard cap" framing. Reword the trailing comment to describe the real behavior, keeping the historical decision reference — e.g. "defaultCount: number // per-role auto-fill target: volunteers the scheduler fills for this role each service (D-02)". Comment-only; no type or value change.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/RolesConfigPanel.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>RolesConfigPanel.test.ts exists and fully passes; the header copy states the scheduler auto-fills the count each service (old soft/cap framing gone); the Delete button renders with bg-red-900/20 + text-red-400 at compact row sizing while its click handler and confirm block are unchanged; roster.ts:9 comment no longer contradicts the corrected copy; type-check (vue-tsc --build) is clean.</done>
</task>

<task type="auto">
  <name>Task 2: TeamsConfigPanel — mirror the destructive Delete button (R245) + one class assertion</name>
  <files>src/components/TeamsConfigPanel.vue, src/components/__tests__/TeamsConfigPanel.test.ts</files>
  <action>
    Apply the SAME R245 treatment to TeamsConfigPanel for cross-tab consistency (per CONTEXT: both tabs mirror each other). The Delete &lt;button&gt; at TeamsConfigPanel.vue lines 37-42 is ALREADY a real &lt;button type="button"&gt; with identical current text-link classes to Roles — CLASS SWAP ONLY. Keep the element, its @click="confirmDeleteId = row.team.id" handler, its per-row :aria-label (`Delete ${row.team.name} team`), the "Delete" text, and BOTH inline confirm blocks (the rename soft-warn at lines 45-62 and the delete soft-warn at lines 64-80) untouched. Change only its class to the identical destructive-button family used in Task 1: "text-xs px-3 py-1.5 rounded-md font-medium bg-red-900/20 hover:bg-red-900/40 text-red-400 transition-colors".

    Add ONE new assertion to TeamsConfigPanel.test.ts: the per-row Delete button's class list contains "bg-red-900/20" and "text-red-400". Do not alter the existing assertions — they key off button TEXT ('Delete', 'Delete Team') and aria-labels, not class names, so the class-only edit leaves them passing.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/TeamsConfigPanel.test.ts</automated>
  </verify>
  <done>Teams Delete button renders with bg-red-900/20 + text-red-400 matching the Roles panel; all existing TeamsConfigPanel.test.ts assertions (Delete text, aria-labels, rename/delete confirm flows, WR-01/02/04) still pass; the new class assertion passes.</done>
</task>

<task type="auto">
  <name>Task 3: RosterView — constrain Roles/Teams tab-panel width (R244) + scoped test</name>
  <files>src/views/RosterView.vue, src/views/__tests__/RosterView.test.ts</files>
  <action>
    R244: add class="max-w-4xl" to EXACTLY the two tab-panel wrapper divs in RosterView.vue — the &lt;div v-show="activeTab === 'roles'"&gt; (line 244) and the &lt;div v-show="activeTab === 'teams'"&gt; (line 249). Mirror SettingsView.vue:3's max-w-4xl convention. Do NOT add it to the &lt;div v-show="activeTab === 'volunteers'"&gt; wrapper (line 70) or to the RosterView page container — the Volunteers matrix legitimately stays full-width. RosterView currently uses only max-w-sm/max-w-md elsewhere and no max-w-4xl, so after this edit exactly two elements carry max-w-4xl.

    Add ONE new `it` to RosterView.test.ts (child panels are stubbed there, so this is the only automated proof the constraint is applied at the RosterView level, scoped correctly): assert wrapper.findAll('.max-w-4xl').length === 2 (the roles + teams wrappers), and that the volunteers tab wrapper does NOT carry max-w-4xl. Name the test distinctively (e.g. "R244: roles and teams tab wrappers are width-constrained but volunteers is not") so it can be targeted with vitest -t.

    Note: RosterView.test.ts is in the documented known-failing baseline (a pre-existing stale assertion, per CLAUDE.md) — do NOT attempt to fix that unrelated failure and do NOT attribute it to this phase. The verify below targets ONLY the new test by name to keep the gate clean.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RosterView.test.ts -t "R244"</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>The roles and teams v-show wrappers carry max-w-4xl; the volunteers wrapper does not; the new R244-targeted RosterView test passes; type-check is clean. The pre-existing RosterView.test.ts baseline failure remains unchanged and unrelated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds no new input surface, network call, or data write. It edits static template class attributes, one static copy string, one type doc-comment, and test files. Existing updateRole/deleteRole/addRole/updateTeam/deleteTeam store paths are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-01 | Tampering | RolesConfigPanel.vue / TeamsConfigPanel.vue / RosterView.vue (template class + static copy edits) | low | accept | Class-swap and static-copy-only changes introduce no new user input, no data binding, and no auth/rules change; the Delete @click handler and confirm blocks are unchanged. No new attack surface. |
| T-83-02 | Information Disclosure | RolesConfigPanel.vue:6 header copy (R246) | low | accept | Copy is corrected to accurately describe existing, owner-accepted scheduler behavior; it exposes no new data — it describes the same defaultCount already visible in the UI. |
| T-83-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (83-RESEARCH.md Package Legitimacy Audit: "installs no new packages"); all edits use existing Tailwind utilities and the current Vue/TS toolchain. No slopcheck needed. |
</threat_model>

<verification>
Phase-level gates (run before /gsd-verify-work):

- Targeted suite (per RESEARCH Sampling Rate): `npx vitest run src/components/__tests__/RolesConfigPanel.test.ts src/components/__tests__/TeamsConfigPanel.test.ts` — all pass.
- New R244 assertion: `npx vitest run src/views/__tests__/RosterView.test.ts -t "R244"` — passes.
- Full app suite: `npx vitest run` — returns the documented 2-file known-failing baseline ONLY (`src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts`'s pre-existing stale assertion). Per CLAUDE.md, use bare `npx vitest run` (excludes rules.test.ts + render-service/**); do NOT use `--dir src`. If RosterView.test.ts fails identically to master before these edits, that is baseline, not a regression from R244.
- Type gate: `npm run type-check` (vue-tsc --build, which also typechecks test files) — clean. The narrower `-p tsconfig.app.json` form is NOT sufficient evidence per CLAUDE.md.
- Client-only: confirm no firebase deploy / rules / functions changes are present in the diff (Deploy note: no deploy hand-over).
</verification>

<success_criteria>
- R244: Roles and Teams tab panels are constrained to max-w-4xl; the Volunteers tab table stays full-width; a scoped RosterView test proves exactly the two wrappers carry the class.
- R245: The Delete affordance in BOTH RolesConfigPanel and TeamsConfigPanel is a real destructive button (bg-red-900/20 + text-red-400, compact row sizing) with its click handler and inline soft-warn confirm behavior unchanged; asserted in both panels' tests.
- R246: The schedulable-roles header copy accurately states the scheduler auto-fills the default count each service; the old soft-target/no-cap framing is gone from both RolesConfigPanel.vue and the roster.ts type doc-comment.
- New src/components/__tests__/RolesConfigPanel.test.ts exists and fully passes; TeamsConfigPanel.test.ts survives the class-only change plus its new assertion.
- `npm run type-check` clean; `npx vitest run` at the documented 2-file baseline; no deploy/rules/functions changes.
</success_criteria>

<output>
Create `.planning/phases/83-roles-teams-tab-ux-copy/83-01-SUMMARY.md` when done.
</output>
