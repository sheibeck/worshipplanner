---
phase: 13-volunteer-scheduling-import-servers-from-planning-center-col
plan: 04
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/utils/planningCenterApi.ts
  - src/utils/__tests__/planningCenterApi.test.ts
autonomous: true
requirements: [D-13, D-14]
must_haves:
  truths:
    - "Covers CONTEXT.md decisions D-13, D-14 (mirrors this plan's requirements)"
    - "The app fetches every person from Planning Center Services v2, following pagination until exhausted"
    - "Each fetched person yields name and email; phone is NOT requested from PC (it has no phone vertex) and defaults empty"
    - "The fetch returns preview-ready UpsertPersonInput records without writing to Firestore"
    - "A 429 rate-limit response is retried respecting Retry-After"
  artifacts:
    - path: "src/utils/planningCenterApi.ts"
      provides: "fetchAllPeople, mapPcPersonToUpsert, fetchAndMapPeople appended"
      exports: ["fetchAllPeople", "mapPcPersonToUpsert", "fetchAndMapPeople"]
    - path: "src/utils/__tests__/planningCenterApi.test.ts"
      provides: "pagination + no-phone + mapping coverage"
  key_links:
    - from: "src/utils/planningCenterApi.ts"
      to: "Planning Center /services/v2/people"
      via: "fetch through /api/planningcenter proxy"
      pattern: "planningcenter/services/v2/people"
---

<objective>
Extend the existing Planning Center client with people import: a paginated `fetchAllPeople`, a per-person email fetch, a pure `mapPcPersonToUpsert`, and a `fetchAndMapPeople` orchestrator returning preview-ready `UpsertPersonInput[]`. This seeds the volunteer roster (D-13/D-14).

Purpose: Reuse the proven `fetchAllPcSongs` pagination + 429-retry + proxy-URL-rewrite pattern for people. CRITICAL: Planning Center Services v2 has NO phone-number vertex (RESEARCH.md Pitfall 5 / A1) — this plan must fetch name + email only and leave `phone` empty (app-only field), never wiring a `phone_numbers` include/endpoint that would 404.

Output: three new functions appended to `planningCenterApi.ts` + extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-RESEARCH.md
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-PATTERNS.md

<interfaces>
<!-- Reuse existing conventions in planningCenterApi.ts -->
```typescript
export const PC_BASE_URL = '/api/planningcenter/services/v2'   // already exported (line 12)
function basicAuthHeader(appId: string, secret: string): string  // already present (line 17)
```
<!-- Pagination + 429-retry + proxy-URL-rewrite reference (COPY the shape) -->
<!-- fetchAllPcSongs in src/utils/pcSongImport.ts lines 138-206 -->
<!-- Target new exports in planningCenterApi.ts: -->
```typescript
import type { UpsertPersonInput } from '@/types/roster'

interface PcPerson { id: string; attributes: { first_name?: string; last_name?: string; name?: string } }

// Paginated fetch of /services/v2/people (name only from this call). No phone vertex exists.
export async function fetchAllPeople(appId: string, secret: string): Promise<PcPerson[]>

// Pure: PC person + its emails → UpsertPersonInput. phone is ALWAYS '' (app-only, D-14).
export function mapPcPersonToUpsert(person: PcPerson, emails: string[]): UpsertPersonInput

// Orchestrator: fetch people, then fetch each person's emails (batched by 3), map, return preview list.
export async function fetchAndMapPeople(appId: string, secret: string): Promise<UpsertPersonInput[]>
```
</interfaces>

Pagination/429/rewrite reference to copy: `fetchAllPcSongs` @src/utils/pcSongImport.ts lines 138-206.
Batched per-3 nested-resource fetch to mirror: `fetchAndMapPcSongs` arrangement batching @src/utils/pcSongImport.ts lines 218-247.
Test analog: @src/utils/__tests__/pcSongImport.test.ts (fetch mock + pagination + 429 assertions).
Endpoints (RESEARCH Sources): people list `GET /services/v2/people?per_page=100`; nested emails `GET /services/v2/people/{id}/emails`. Person attributes include first_name/last_name/name; Email attribute is `address`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchAllPeople pagination + mapPcPersonToUpsert (no phone)</name>
  <files>src/utils/planningCenterApi.ts, src/utils/__tests__/planningCenterApi.test.ts</files>
  <read_first>
    - src/utils/planningCenterApi.ts (PC_BASE_URL + basicAuthHeader + existing fetch conventions; where to append)
    - src/utils/pcSongImport.ts lines 138-206 (fetchAllPcSongs pagination + 429 + links.next proxy rewrite)
    - src/utils/__tests__/pcSongImport.test.ts (how PC fetch is mocked in tests)
    - src/types/roster.ts (UpsertPersonInput shape)
    - .planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-RESEARCH.md (Pitfall 5 / Assumption A1 — no phone vertex)
  </read_first>
  <behavior>
    - fetchAllPeople follows links.next across 2 mocked pages and returns the concatenated people array
    - links.next absolute PC URL is rewritten to the /api/planningcenter proxy path before the second fetch (assert fetch called with a proxy-prefixed URL, not api.planningcenteronline.com)
    - a 429 response with Retry-After is retried (mock returns 429 once then 200)
    - mapPcPersonToUpsert builds `name` from `attributes.name` when present, else `first_name + ' ' + last_name` trimmed; sets `email` from the first supplied email; sets `phone: ''` ALWAYS; sets `pcPersonId` to the person id
    - mapPcPersonToUpsert with an empty emails array yields email '' (no throw)
  </behavior>
  <action>Append to `src/utils/planningCenterApi.ts` (do not create a new file): `fetchAllPeople(appId, secret)` copying the `fetchAllPcSongs` loop shape — build `authHeader` via the existing `basicAuthHeader`, start at `${PC_BASE_URL}/people?per_page=100`, 429-retry respecting Retry-After (max 3 attempts), push `json.data`, follow `json.links.next` rewriting `https://api.planningcenteronline.com/services/v2` → `PC_BASE_URL`, stop when no next. Do NOT add any `include=phone_numbers` or phone endpoint (RESEARCH Pitfall 5 — it 404s). Add pure `mapPcPersonToUpsert(person, emails)` returning `UpsertPersonInput` with `name` resolved (attributes.name || `${first_name} ${last_name}`.trim()), `email = emails[0] ?? ''`, `phone: ''` (with a comment: PC Services v2 has no phone vertex — D-14 app-only), `pcPersonId: person.id`, and `active`/`roles`/`frequencyTargetN` left to the store's upsert defaults (omit here). Extend `planningCenterApi.test.ts` with the behavior bullets, mocking `fetch`. Write assertions first (RED), implement to green.</action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/planningCenterApi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phone_numbers\|/phone\|phoneNumber" src/utils/planningCenterApi.ts` == 0 (no phone fetch anywhere)
    - mapPcPersonToUpsert sets `phone: ''` unconditionally (grep confirms `phone: ''` in the mapper)
    - Tests assert pagination across 2 pages, proxy-URL rewrite, and a 429 retry, all green
    - `npx vitest run src/utils/__tests__/planningCenterApi.test.ts` passes
  </acceptance_criteria>
  <done>fetchAllPeople paginates + retries like the songs fetch; mapPcPersonToUpsert produces name+email with phone always empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fetchAndMapPeople orchestrator (batched email fetch → preview list)</name>
  <files>src/utils/planningCenterApi.ts, src/utils/__tests__/planningCenterApi.test.ts</files>
  <read_first>
    - src/utils/planningCenterApi.ts (fetchAllPeople + mapPcPersonToUpsert from Task 1)
    - src/utils/pcSongImport.ts lines 218-247 (fetchAndMapPcSongs — batch-of-3 nested fetch loop to mirror)
  </read_first>
  <behavior>
    - fetchAndMapPeople fetches all people, then for each person fetches GET /services/v2/people/{id}/emails, extracts email `address` attributes, and returns UpsertPersonInput[] with name+email populated and phone ''
    - email fetches are issued in batches of 3 (assert no more than 3 concurrent email requests via mock call ordering, mirroring fetchAndMapPcSongs)
    - a person whose emails endpoint returns empty data yields email '' and is still included in the result
    - the returned array length equals the number of people fetched (no silent drops)
  </behavior>
  <action>Append `fetchAndMapPeople(appId, secret): Promise<UpsertPersonInput[]>` to `planningCenterApi.ts`. Call `fetchAllPeople`, then iterate people in batches of 3 (BATCH_SIZE=3 like fetchAndMapPcSongs) issuing `GET ${PC_BASE_URL}/people/${id}/emails` with the basic-auth header, parse `json.data.map(e => e.attributes.address)`, and call `mapPcPersonToUpsert(person, emails)` per person. Concatenate and return. Reuse the same 429-tolerant fetch helper if one is factored, else inline a simple fetch (emails rarely rate-limit at batch 3). Extend the test with the behavior bullets (RED then green).</action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/planningCenterApi.test.ts -t "people\|email"</automated>
  </verify>
  <acceptance_criteria>
    - fetchAndMapPeople returns one UpsertPersonInput per fetched person (test asserts length parity)
    - Email extraction reads the `address` attribute (grep confirms `.address` in the emails mapping)
    - Batch size is 3 (grep confirms a `BATCH_SIZE = 3` or slice-of-3 loop)
    - Full `npx vitest run src/utils/__tests__/planningCenterApi.test.ts` green; `npx vue-tsc --build` clean
  </acceptance_criteria>
  <done>fetchAndMapPeople returns preview-ready roster records (name+email, phone empty) with rate-limit-friendly batched email fetches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Planning Center API | Outbound authenticated request using org-level PC credentials (App ID + Secret) via the /api/planningcenter proxy |
| PC API → App | Inbound person/email PII (names, email addresses) crossing into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | Reuse of PC App ID + Secret for the new people path | mitigate | Reuse the EXACT existing credential storage + masked-after-save handling from Phase 8 (`authStore.pcCredentials`); no new credential storage, logging, or UI is introduced — credentials pass only into `basicAuthHeader` as before |
| T-13-04-02 | Information Disclosure | Accidentally importing PC blockouts/availability PII not in scope | mitigate | fetch only `/people` + nested `/emails`; do NOT wire Blockout/BlockoutDate vertices (D-14, RESEARCH anti-pattern) — verified by grep gate |
| T-13-04-03 | Denial of Service | PC rate limiting during large roster fetch | mitigate | 429 Retry-After retry loop on the people fetch + batch-of-3 email fetches, mirroring the proven fetchAllPcSongs limits |
| T-13-04-04 | Spoofing/Tampering | Empty/silent phone field with no error (Pitfall 5) | accept | phone is intentionally '' — documented app-only field (D-14); no fetch attempted, so no 404/silent-failure ambiguity; user edits phone manually in the Plan 07 form |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/planningCenterApi.test.ts` green (pagination, 429, batched emails)
- No phone endpoint/include anywhere in planningCenterApi.ts
- `npx vue-tsc --build` clean
</verification>

<success_criteria>
The PC client can seed the roster: `fetchAndMapPeople` returns every PC person as an `UpsertPersonInput` with name + first email and an empty (app-only) phone, following pagination and rate limits — satisfying D-13/D-14 while respecting the no-phone-vertex constraint.
</success_criteria>

<output>
Create `.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-04-SUMMARY.md` when done.
</output>
