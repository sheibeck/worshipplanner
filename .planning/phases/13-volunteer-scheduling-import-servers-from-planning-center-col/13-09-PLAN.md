---
phase: 13-volunteer-scheduling-import-servers-from-planning-center-col
plan: 09
type: execute
wave: 5
depends_on: [13-06, 13-08]
files_modified:
  - src/components/QuarterGrid.vue
  - src/views/QuarterView.vue
autonomous: false
requirements: [D-04, D-10, D-22, D-23]
must_haves:
  truths:
    - "Covers CONTEXT.md decisions D-04, D-10, D-22, D-23 (mirrors this plan's requirements)"
    - "The schedule renders as a grid: rows = service Sundays, columns = roles grouped Band/Tech/Other, cells = assigned people chips"
    - "A role cell can hold multiple people, and one person can appear in multiple role cells on the same date"
    - "Unfilled slots render as a distinct flagged empty cell; pairing conflicts render a distinct amber flag"
    - "Clicking a cell lets the leader reassign, swap, clear, or add a second person"
    - "For an unfilled or low-fill cell, the leader sees who is blacked out that date and who is available-but-unassigned for that role"
  artifacts:
    - path: "src/components/QuarterGrid.vue"
      provides: "dates×roles editable grid + per-cell edit + gap-filling panel"
      min_lines: 100
    - path: "src/views/QuarterView.vue"
      provides: "Grid mounted with the selected quarter + ProposeResult flags"
  key_links:
    - from: "src/components/QuarterGrid.vue"
      to: "src/stores/quarters.ts"
      via: "assignPerson / clearAssignment / swapAssignment"
      pattern: "assignPerson|clearAssignment|swapAssignment"
    - from: "src/components/QuarterGrid.vue"
      to: "src/stores/roster.ts"
      via: "activePeople candidate lists"
      pattern: "activePeople"
---

<objective>
Build the phase centerpiece UI: `QuarterGrid.vue` — an editable dates×roles grid (D-22) where each cell holds people chips (multi-person-per-role, D-04), unfilled slots are flagged (D-10), pairing conflicts show a distinct badge, and clicking a cell allows reassign/swap/clear/add. Beneath unfilled/low-fill cells, a gap-filling panel (D-23) shows who is blacked out that date and who is available-but-unassigned for the role, so the leader can contact people to fill gaps.

Purpose: This is where the leader spends most of their time. The store already exposes scoped cell-edit actions (Plan 06); this plan is the interactive grid over them, styled per 13-UI-SPEC (group-colored headers, chip pills, dashed unfilled cells).

Output: `QuarterGrid.vue` + its mount in `QuarterView.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-UI-SPEC.md
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-PATTERNS.md

<interfaces>
```typescript
import { useQuartersStore } from '@/stores/quarters'   // assignPerson, clearAssignment, swapAssignment, generateProposal
import { useRosterStore } from '@/stores/roster'        // activePeople, roles
import type { Quarter, Role, ProposeResult, PersonQuarterData } from '@/types/roster'

// QuarterGrid props
defineProps<{ quarter: Quarter; roles: Role[]; lastProposeResult: ProposeResult | null }>()

// A cell = quarter.calendar[date]?.[roleId] ?? []  (personId[])
// Effective role config for a date = quarter.roleOverridesByDate[date] ?? default template (role.defaultCount)
// Blacked-out-today for a date = people whose quarter.personQuarterData[personId].blackoutDates includes date
// Available-unassigned for (date, roleId) = activePeople with roleId in roles, NOT blacked out that date, NOT already in that cell
```
</interfaces>

Table markup + dark cell styling to borrow: @src/components/CsvImportModal.vue lines 143-215.
Chip/pill shape (rounded-full px-2 py-0.5 text-xs font-medium border): @src/components/TeamTagPill.vue.
Group-colored column headers (Band=blue, Tech=purple, Other=gray) via STATIC class maps: @src/components/SongBadge.vue pattern.
UI-SPEC MANDATES: cell padding `px-2 py-2` (sm token); role column headers `text-xs font-medium text-gray-400 uppercase tracking-wider`; sticky left date column `text-sm font-medium text-gray-100`; unfilled cell = `border border-dashed border-gray-700` + red "Unfilled" badge; pairing conflict = amber badge "Pairing conflict"; gap panel "Blacked out today" (gray-500 strikethrough) + "Available, not yet assigned" (green-tinted); NO confirmation for clearing a single cell (UI-SPEC — low-friction edit).
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuarterGrid render + per-cell edit</name>
  <files>src/components/QuarterGrid.vue, src/views/QuarterView.vue</files>
  <read_first>
    - src/components/CsvImportModal.vue lines 143-215 (table shell + dark cell styling)
    - src/components/TeamTagPill.vue (chip pill shape)
    - src/components/SongBadge.vue (static class-map badge for group colors)
    - src/stores/quarters.ts (assignPerson/clearAssignment/swapAssignment signatures)
    - src/views/QuarterView.vue (mount point placeholder + lastProposeResult state)
    - 13-UI-SPEC.md (Additional Component Notes: grid, cell padding, unfilled dashed cell, chip shape, group header colors)
  </read_first>
  <action>Create `src/components/QuarterGrid.vue` taking props `{ quarter, roles, lastProposeResult }`. Render a table: header row = role columns grouped by Band/Tech/Other with a group-colored header band (static class map: band→blue-900/50, tech→purple-900/50, other→gray-800) using `text-xs font-medium text-gray-400 uppercase tracking-wider`; first column sticky = each service date (weekday + date, `text-sm font-medium text-gray-100`). For each (date, role) cell compute the effective count from `quarter.roleOverridesByDate[date] ?? role.defaultCount` and the assigned personIds from `quarter.calendar[date]?.[roleId] ?? []`. Render assigned people as chips (TeamTagPill shape); when assigned count < effective count OR the cell is empty, render a dashed empty slot (`border border-dashed border-gray-700`) with a red "Unfilled" badge (cross-check against `lastProposeResult.unfilled`). Render an amber "Pairing conflict" badge on cells/dates present in `lastProposeResult.pairingConflicts`. Clicking a cell opens an inline editor (popover/menu) offering: assign a person (dropdown of eligible candidates → `quartersStore.assignPerson`), remove a chip (× on the chip → `quartersStore.clearAssignment`, NO confirmation per UI-SPEC), swap (`quartersStore.swapAssignment`), and add-a-second-person (assign again — D-04). Cell padding `px-2 py-2`. Mount `<QuarterGrid :quarter="selectedQuarter" :roles="rosterStore.roles" :lastProposeResult="lastProposeResult" />` in QuarterView at the Plan-08 placeholder.</action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | grep -E "QuarterGrid|QuarterView" | grep -qi error && echo HAS_ERR || echo TSOK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --build` clean for QuarterGrid.vue + QuarterView.vue (prints TSOK)
    - QuarterGrid calls assignPerson, clearAssignment, and swapAssignment (grep all three)
    - Unfilled cells use `border-dashed` + a "Unfilled" badge; pairing conflicts use an amber "Pairing conflict" badge (grep both strings)
    - Group header colors use static class strings (grep -c "bg-\${" QuarterGrid.vue == 0)
    - Cell padding uses `px-2 py-2` (grep confirms); clearing a chip has NO confirm modal (grep -c "confirm" around clear == 0 for the clear action)
  </acceptance_criteria>
  <done>The grid renders dates×roles with people chips, flags unfilled + pairing-conflict cells, and supports scoped assign/clear/swap/add edits.</done>
</task>

<task type="auto">
  <name>Task 2: Gap-filling panel (blacked-out + available-unassigned)</name>
  <files>src/components/QuarterGrid.vue</files>
  <read_first>
    - src/components/QuarterGrid.vue (the grid from Task 1)
    - src/views/SongsView.vue (hidden-song list styling for the strikethrough blacked-out list)
    - src/stores/roster.ts (activePeople, roles)
    - 13-UI-SPEC.md (Gap-filling panel note: two inline lists, colors)
  </read_first>
  <action>Extend `QuarterGrid.vue`: when a leader clicks/expands an unfilled or low-fill cell (or a date row), reveal a gap-filling panel directly under the row showing two lists for that (date, roleId): (1) "Blacked out today" — active people whose `quarter.personQuarterData[personId].blackoutDates` includes the date, rendered gray-500 strikethrough (mirror SongsView hidden-song styling) so the leader knows NOT to chase them; (2) "Available, not yet assigned" — `rosterStore.activePeople` who list this role, are NOT blacked out that date, and are NOT already in the cell, rendered green-tinted (green-900/30 bg, green-400 text). Each available candidate has a quick "Assign" action calling `quartersStore.assignPerson`. Compute both lists as computed helpers from `quarter.personQuarterData`, `quarter.calendar`, and `rosterStore.activePeople` — no scheduler call needed. The goal (D-23) is to see the whole schedule, find gaps, and contact people manually.</action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | grep "QuarterGrid" | grep -qi error && echo HAS_ERR || echo TSOK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --build` clean for QuarterGrid.vue (prints TSOK)
    - Gap panel renders both lists with exact labels "Blacked out today" and "Available, not yet assigned" (grep both)
    - Blacked-out list is derived from personQuarterData.blackoutDates for the date (grep confirms blackoutDates referenced)
    - Available list filters activePeople by role + not-blacked-out + not-already-in-cell (grep confirms the three conditions)
    - Available candidates offer an Assign action calling assignPerson
  </acceptance_criteria>
  <done>Unfilled/low-fill cells expose a gap-filling panel showing who is blacked out and who is available-but-unassigned, with one-click assign.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify editable quarter grid + gap panel</name>
  <action>After all automation in this plan is complete, the human verifies the editable grid and gap-filling panel behaviors listed below. This checkpoint confirms the automated grid build works.</action>
  <what-built>The editable quarter grid (dates×roles) with people chips, unfilled + pairing-conflict flags, click-to-edit cells (assign/swap/clear/add), and a gap-filling panel showing blacked-out and available-unassigned people per gap.</what-built>
  <how-to-verify>
    1. `npm run dev`, go to /schedule, select a quarter that has an imported CSV + a generated proposal.
    2. Confirm the grid shows rows = Sundays, columns = roles grouped Band(blue)/Tech(purple)/Other(gray), cells = people chips.
    3. Confirm unfilled slots show a dashed cell + red "Unfilled" badge; if any pairing conflict exists, an amber "Pairing conflict" badge appears.
    4. Click a filled cell — reassign one person, add a second person (confirm two chips coexist), then remove a chip (no confirmation prompt) and confirm it clears immediately.
    5. Add the same person to two different role cells on the same date (D-04) and confirm both hold.
    6. Expand an unfilled cell — confirm "Blacked out today" lists blacked-out people (strikethrough) and "Available, not yet assigned" lists eligible candidates with an Assign button that fills the cell.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grid UI → quarters store → Firestore | Per-cell edits write assignment data (person ids) under organizations/{orgId} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09-01 | Information Disclosure | Grid + gap panel expose roster PII (names, blackout dates) | mitigate | /schedule is editor-only (Plan 08 route meta) and reads org-scoped data; no viewer path to the grid |
| T-13-09-02 | Tampering | A cell edit corrupting an unrelated cell | mitigate | assign/clear/swap are scoped updateDocs on `calendar.${date}.${roleId}` (Plan 06) — grid never rewrites the whole calendar map |
| T-13-09-03 | Availability | Manual assignment onto a blacked-out person (hard-constraint bypass via manual edit) | accept | Manual edits are the leader's explicit override authority (D-22); the gap panel clearly marks blacked-out people (strikethrough) so an accidental assignment is visible — the scheduler still never auto-violates blackouts |
</threat_model>

<verification>
- `npx vue-tsc --build` clean for QuarterGrid + QuarterView
- Human checkpoint approved
- Grid supports multi-person cells, unfilled/conflict flags, scoped edits, and the gap panel
</verification>

<success_criteria>
The dates×roles grid is the primary, editable schedule surface (D-22): multi-person-per-role and multi-role-per-person (D-04), unfilled slots flagged (D-10), and a gap-filling panel surfacing blacked-out + available-unassigned people per gap (D-23) — all in the existing dark-mode design system.
</success_criteria>

<output>
Create `.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-09-SUMMARY.md` when done.
</output>
