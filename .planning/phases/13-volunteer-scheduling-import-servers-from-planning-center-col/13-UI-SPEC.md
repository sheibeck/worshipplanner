---
phase: 13
slug: volunteer-scheduling-import-servers-from-planning-center-col
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-07
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — shadcn does not apply (see note) |
| Preset | not applicable |
| Component library | none (hand-rolled Vue 3 SFCs + Tailwind utility classes) |
| Icon library | inline hand-drawn SVGs, Heroicons-style outline set, `stroke-width="1.5"` or `"2"` — no icon package installed |
| Font | Tailwind default `font-sans` system stack (no custom `@font-face` in `src/assets/main.css`) |

**shadcn gate note:** This is a **Vue 3 + Vite** project (not React/Next.js). `components.json` was not found, and `npx shadcn init` targets React by default — it has no first-party Vue support in this codebase's stack. The shadcn gate is therefore **not applicable**. Instead, this phase must extend the project's existing hand-built Tailwind v4 dark-mode design system (verified across `PcImportModal.vue`, `CsvImportModal.vue`, `SongTable.vue`, `SongsView.vue`, `TeamTagPill.vue`, `SongBadge.vue`). All new components in this phase (roster views, import modals, quarter grid) must match these existing conventions exactly — no new visual language is introduced.

---

## Spacing Scale

Declared values (must be multiples of 4) — **no new scale introduced; this is the scale already in use project-wide**:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal padding (`px-2 py-0.5`) |
| sm | 8px | Compact element spacing, `gap-2`/`gap-3` between inline controls |
| md | 12–16px | Table/grid cell padding (`px-4 py-3`), default element spacing |
| lg | 24px | Section padding, page header margin (`mb-6`) |
| xl | 32px | Layout gaps between major page regions |
| 2xl | 48px | Major section breaks (e.g. empty-state vertical padding `py-20`) |
| 3xl | 64px | Not used at page level in this phase |

Exceptions: **Quarter grid cells** (dates × roles) use tighter `px-3 py-2` cell padding (12px/8px) to keep a full quarter (~13 rows × ~7–10 role columns) legible without horizontal scroll on a standard laptop viewport — this is a deliberate density exception for the grid only, consistent with the existing `SongTable.vue` row density (`px-4 py-3`) being slightly loosened, not tightened, elsewhere.

---

## Typography

Reusing the exact scale already established across `PcImportModal.vue`, `SongsView.vue`, `SongTable.vue` — **no new sizes or weights introduced**.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 regular | 1.5 (`leading-normal`) |
| Label | 12px (`text-xs`) | 500 medium* | 1.4 |
| Heading | 16px (`text-base`) | 600 semibold | 1.2 (`leading-tight`) |
| Display | 20px (`text-xl`) | 600 semibold | 1.2 |

\* **Existing codebase exception, carried forward (not a new decision):** the app uses a 3rd weight — 500 medium — project-wide for buttons, table headers, and pill/badge text (e.g. `PcImportModal.vue` footer buttons, `SongTable.vue` `<th>` headers). The 2 primary weights for headings/body remain 400/600; 500 is an established UI-chrome exception this phase must match, not deviate from.

Additional established micro-pattern to reuse: table headers and grid column headers are `text-xs font-medium text-gray-400 uppercase tracking-wider` (see `SongTable.vue` `<th>`). The quarter grid's role-column headers must use this exact treatment.

Stat/count numbers (e.g. import preview counts, "N unfilled slots" summary) use 24px (`text-2xl font-bold`) per `PcImportModal.vue`'s preview panel — reuse for the grid's summary bar (unfilled count, pairing-conflict count).

---

## Color

Dark-mode palette, reusing the exact tokens already in use — **no new base tokens introduced**.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#030712` gray-950 (`bg-gray-950`, set globally on `html/body/#app`) | App background |
| Secondary (30%) | `#111827` gray-900 (cards/modals) and `#1f2937` gray-800 (inputs, nested panels, secondary buttons) | Roster list rows, quarter grid container, modal bodies, CSV drop zone, table header row background |
| Accent (10%) | `#4f46e5` indigo-600 (`bg-indigo-600 hover:bg-indigo-500`) | **Reserved exclusively for:** primary CTA buttons ("Import from Planning Center", "Import Volunteer CSV", "Generate Schedule", "Save"), the active/selected grid-cell outline when editing an assignment, checkbox/radio accent (`text-indigo-600`), focus rings (`focus:ring-indigo-500`), text links (person names that open detail, CCLI-style links), progress bar fill, and selected-row tint (`bg-indigo-900/10`) |
| Destructive | `#b91c1c` red-700 / `#991b1b` red-800 (`bg-red-700 hover:bg-red-600` for buttons; `bg-red-900/20 border-red-800` for banners) | Reserved for: "Deactivate person" confirm button, "Delete Role" confirm button, "Unfilled" slot flag/badge, blackout indicator, PC/CSV import error banners |

Accent reserved for: primary CTA buttons, selected/active grid cell, form focus rings, checkbox accents, in-page navigational links, progress indicators. **Never** applied to passive/decorative surfaces, badges, or informational chips.

**Additional established semantic colors (status-only, low-frequency, not part of the 10% accent budget — reused from existing codebase, not new):**

| Color | Existing usage | This phase's usage |
|-------|----------------|---------------------|
| green (`green-900/30` bg, `green-400` text) | Import "done"/success states, "New" row badge in `CsvImportModal.vue` | Import-complete states (roster + CSV); "Available" candidate indicator in grid gap-filling panel (D-23) |
| amber (`amber-900/20` bg, `amber-300` text) | "Batch Assign" warning button in `SongsView.vue`, CSV "Warning" row badge | "Pairing conflict" flag/badge on grid cells (D-09 partner-blackout case) |
| blue / purple (`blue-900/50`/`purple-900/50` badge pairs) | VW Type 1 / Type 2 song category badges (`SongBadge.vue`) | Role-group badges: **Band = blue**, **Tech = purple** (extends the existing Type-badge color convention to the new Band/Tech/Other groups) |
| gray (`bg-gray-800 text-gray-400 border-gray-700`) | Team tag pills (`TeamTagPill.vue`) | **Other/Scripture Reader** role-group badge (neutral, since it's a single role, not a group needing its own hue) |

---

## Copywriting Contract

This phase has multiple distinct flows (roster import, CSV import, schedule generation, manual grid editing). Table extended with one row per flow rather than a single generic CTA.

| Element | Copy |
|---------|------|
| Primary CTA — roster import | "Import from Planning Center" (mirrors `PcImportModal.vue` wording exactly) |
| Primary CTA — quarterly CSV import | "Import Volunteer CSV" |
| Primary CTA — schedule generation (first run) | "Generate Schedule" |
| Primary CTA — schedule generation (re-run with existing assignments) | "Regenerate" (destructive-adjacent — see confirmation row below) and "Fill Remaining Gaps" (non-destructive, only touches unfilled cells) |
| Primary CTA — add person manually | "Add Volunteer" |
| Primary CTA — finalize + share | "Finalize & Share" |
| Empty state heading — roster | "No volunteers yet" |
| Empty state body — roster | "Import your team from Planning Center or add people one at a time." with actions "Import from Planning Center" (primary) / "Add person manually" (secondary text link) — mirrors `SongTable.vue`'s empty-state layout exactly |
| Empty state heading — quarter grid | "No schedule generated yet" |
| Empty state body — quarter grid | "Add this quarter's volunteer CSV (blackout dates, roles, serve-with pairings), then generate a proposed schedule." primary action "Generate Schedule" |
| Error state — PC import failure | "Import failed" / "{error message}" / "Go to Settings to check your Planning Center credentials." (exact pattern reused from `PcImportModal.vue`'s error step) |
| Error state — CSV import failure/malformed | "Import failed" / "Check that your CSV matches the required columns (Name, Roles, Frequency, Blackout Dates, Serve-With) and try again." |
| Flagged state — unfilled slot (not a hard error, D-10) | Badge: "Unfilled" (red). Helper text on hover/expand: "No eligible, available person for this role on this date." Grid gap-panel (D-23) lists who's blacked out and who's available-but-unassigned directly beneath. |
| Flagged state — pairing conflict (D-09 + blackout collision) | Badge: "Pairing conflict" (amber). Helper text: "{partner name} is blacked out on this date — assign manually." |
| Destructive — deactivate person (D-20, soft-delete) | "Deactivate {name}? They'll be removed from future schedule proposals and pickers. Their history is kept and they can be reactivated anytime." Buttons: "Cancel" / "Deactivate" (red-700) — deliberately says "Deactivate" not "Delete" since D-20 is a soft-delete, mirroring the Phase 9 song hide/restore language ("Hide" not "Delete") |
| Destructive — delete a role (D-03) | "Delete the '{role name}' role? Existing assignments to this role across all quarters will be cleared. This cannot be undone." Buttons: "Cancel" / "Delete Role" (red-700) |
| Destructive — regenerate whole quarter (overwrite manual edits) | "Regenerate the full schedule? This replaces all current assignments, including any manual edits you've made. This cannot be undone." Buttons: "Cancel" / "Regenerate" (red-700) — only shown when a calendar already has manually-edited or previously-generated assignments; first-ever generation uses the plain "Generate Schedule" CTA with no confirmation |
| Non-destructive — clear a single grid cell assignment | No confirmation required. Clearing one person from a cell (or the last person, returning it to empty/unfilled) is a low-friction, easily-reversible edit — distinct from Phase 12's service-plan slot-removal gate (12-05), which governs a different data domain (removing an entire service-plan row/element). Reassigning or clearing a grid cell is closer to "editing a value" than "removing an element." |
| Import preview — ambiguous/unmatched CSV row (D-16) | Row-level actions: "Map to existing person" (dropdown of roster names) / "Create new person" — mirrors the Phase 9 `PcImportModal`/`CsvImportModal` preview-row pattern (duplicate/warning badges, per-row checkbox) |

---

## Registry Safety

Not applicable — no shadcn, no component registries (first-party or third-party) are used anywhere in this codebase.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable |

---

## Additional Component Notes (for planner/executor — grid + role/badge specifics)

These extend the template's structural sections with phase-specific detail flagged in CONTEXT.md as "Claude's Discretion — defer detailed layout to /gsd:ui-phase 13":

- **Quarter grid (D-22/D-23):** rows = service Sundays (date + weekday, `text-sm font-medium text-gray-100` left-most sticky column), columns = roles grouped by Band/Tech/Other with a group-colored header band (blue/purple/gray per the Color section above), cells = comma-or-stacked person chips (reuse the pill shape from `TeamTagPill.vue`: `rounded-full px-2 py-0.5 text-xs font-medium border`). Unfilled cells render as a dashed-border empty slot (`border border-dashed border-gray-700`) with the red "Unfilled" badge, matching the dashed-add-affordance style already used in `SongTable.vue`'s inline tag-add button.
- **Gap-filling panel (D-23):** click/expand an unfilled or low-fill cell to reveal two inline lists directly under the grid row: "Blacked out today" (gray-500 strikethrough names, mirrors `SongsView.vue`'s hidden-song list styling) and "Available, not yet assigned" (green-tinted list per the semantic-color table above), so the leader can see who to contact without leaving the grid.
- **Role badges (D-03):** static class-map pattern per `SongBadge.vue`/`TeamTagPill.vue` (never dynamically constructed Tailwind class strings, to survive Tailwind v4 purge) — Band roles blue, Tech roles purple, Other/Scripture gray, exactly mirroring the existing VW Type 1/2/3 badge convention.
- **Import modals (roster PC import + quarterly CSV import):** both must be built as near-exact structural clones of `PcImportModal.vue` (idle → fetching → preview → importing → done/error) and `CsvImportModal.vue` (select/drop-zone → parsing → preview-with-checkboxes → importing-with-progress-bar → done), respectively — same Teleport/Transition wrapper, same modal chrome (`max-w-lg`/`max-w-3xl`, `bg-gray-900 rounded-xl border border-gray-700 shadow-2xl`), same footer button layout.
- **Print/share output (D-24):** the printable roster and read-only share view must switch to the **light** palette already established in `ShareView.vue` (`bg-white text-gray-900 font-sans`, `text-xs text-gray-500 uppercase tracking-wider` section labels) — this is a deliberate, existing exception to the app's dark theme for print/public-facing surfaces, not a new decision.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
