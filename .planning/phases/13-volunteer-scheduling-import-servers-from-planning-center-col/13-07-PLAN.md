---
phase: 13-volunteer-scheduling-import-servers-from-planning-center-col
plan: 07
type: execute
wave: 3
depends_on: [13-04, 13-05]
files_modified:
  - src/views/RosterView.vue
  - src/components/RolesConfigPanel.vue
  - src/components/RosterImportModal.vue
  - src/components/AppSidebar.vue
  - src/router/index.ts
autonomous: false
requirements: [D-03, D-13, D-14, D-20]
must_haves:
  truths:
    - "A leader can view all volunteers (active + inactive), add a person manually, and edit name/email/phone/roles/frequency"
    - "Phone is an editable in-app field (never claimed from Planning Center)"
    - "A leader can deactivate a volunteer (with a soft-delete confirmation) and reactivate them"
    - "A leader can add/rename/change-count/delete schedulable roles grouped Band/Tech/Other"
    - "A leader can import volunteers from Planning Center via a preview-then-commit modal"
    - "A leader can reach the roster screen from the app sidebar (an editor-gated Roster nav item routes to /roster)"
  artifacts:
    - path: "src/views/RosterView.vue"
      provides: "Roster list, person add/edit form, deactivate/reactivate, roles config host, PC import entry"
      min_lines: 80
    - path: "src/components/RolesConfigPanel.vue"
      provides: "Add/rename/count/delete roles with group badges"
    - path: "src/components/RosterImportModal.vue"
      provides: "PC people import (fetch→preview→commit)"
    - path: "src/components/AppSidebar.vue"
      provides: "Editor-gated Roster nav item linking to /roster"
      contains: "/roster"
    - path: "src/router/index.ts"
      provides: "/roster editor route"
      contains: "roster"
  key_links:
    - from: "src/views/RosterView.vue"
      to: "src/stores/roster.ts"
      via: "useRosterStore"
      pattern: "useRosterStore"
    - from: "src/components/RosterImportModal.vue"
      to: "src/utils/planningCenterApi.ts"
      via: "fetchAndMapPeople"
      pattern: "fetchAndMapPeople"
    - from: "src/components/AppSidebar.vue"
      to: "/roster"
      via: "navItems editor-gated router-link"
      pattern: "/roster"
---

<objective>
Build the roster management surface: `RosterView.vue` (people list active/inactive, manual add/edit person form, deactivate/reactivate, roles config), `RolesConfigPanel.vue` (editable grouped role list), and `RosterImportModal.vue` (PC people import), plus the `/roster` editor route AND its editor-gated sidebar nav entry (so the screen is reachable, not URL-only). This is the secondary screen of the phase (the quarter grid is primary).

Purpose: Give leaders full roster + role control (D-03, D-13, D-14, D-20). Phone is an in-app-only editable field (D-14 — PC has no phone vertex). All visuals reuse the existing dark-mode design system exactly per 13-UI-SPEC.md — no new visual language.

Output: two components, one view, one nav-item addition, one router edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-UI-SPEC.md
@.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-PATTERNS.md

<interfaces>
```typescript
import { useRosterStore } from '@/stores/roster'         // people, roles, activePeople, addPerson, updatePerson,
                                                          // deactivatePerson, reactivatePerson, upsertPeople,
                                                          // seedDefaultRolesIfEmpty, addRole, updateRole, deleteRole
import { fetchAndMapPeople } from '@/utils/planningCenterApi'   // Plan 04
import { useAuthStore } from '@/stores/auth'              // pcCredentials, hasPcCredentials, orgId, isEditor
import { nToFrequencyLabel, frequencyLabelToN } from '@/utils/volunteerCsv'   // friendly frequency labels
import type { Person, Role } from '@/types/roster'
```
</interfaces>

Clone target for RosterImportModal (idle→fetching→preview→importing→done/error, Teleport/Transition, credential guard): @src/components/PcImportModal.vue.
Empty-state layout to mirror: @src/components/SongTable.vue empty state.
Route shape (editor-only, mirror /songs): @src/router/index.ts lines 36-40.
Sidebar nav pattern (editor-gated `authStore.isEditor` push into `navItems`, icon + label + `to`, mirror the Songs entry): @src/components/AppSidebar.vue lines 105-113.
UI-SPEC bindings (MANDATORY): dark palette (gray-950/900/800, indigo-600 accent, red-700 destructive); role-group badges Band=blue / Tech=purple / Other=gray via STATIC class maps (never dynamic Tailwind strings); copy exactly per 13-UI-SPEC Copywriting Contract ("Add Volunteer", "Save Volunteer", "Save Role", "Import from Planning Center", empty-state + deactivate + delete-role copy).
</context>

<tasks>

<task type="auto">
  <name>Task 1: RosterView + RolesConfigPanel + /roster route + sidebar nav item</name>
  <files>src/views/RosterView.vue, src/components/RolesConfigPanel.vue, src/components/AppSidebar.vue, src/router/index.ts</files>
  <read_first>
    - src/views/SongsView.vue (view scaffold: store subscribe on mount, header, dark layout)
    - src/components/SongTable.vue (list + empty-state markup + inline edit affordances + `<th>` styling)
    - src/components/TeamTagPill.vue and src/components/SongBadge.vue (static class-map badge pattern — for role-group badges)
    - src/components/AppSidebar.vue (navItems computed — the editor-gated Songs entry at lines 105-113 is the exact pattern to mirror for the Roster item)
    - src/router/index.ts (route registration + requiresEditor meta)
    - 13-UI-SPEC.md (Copywriting Contract, Color, Typography, role-badge notes)
    - src/stores/roster.ts (actions available)
  </read_first>
  <action>Create `src/views/RosterView.vue`: on mount, call `rosterStore.subscribe(authStore.orgId)` and `rosterStore.seedDefaultRolesIfEmpty()`. Render a header with an "Import from Planning Center" primary button (opens RosterImportModal, wired in Task 2) and an "Add Volunteer" primary button. Show a people list (reuse SongTable list/`<th>` styling) with active volunteers and an inactive/deactivated section (mirroring SongsView's hidden-song list styling — gray, restore action). Each person row shows name, email, phone, role-group badges, and frequency (via `nToFrequencyLabel`). Add/edit uses an inline form or slide-over with fields: name, email, phone (labeled app-only/manual), roles (multi-select of `rosterStore.roles`), frequency (select of friendly labels mapped via frequencyLabelToN); submit calls `addPerson`/`updatePerson`; primary button copy "Save Volunteer". Deactivate action opens a confirmation using the exact D-20 copy from UI-SPEC ("Deactivate {name}? They'll be removed from future schedule proposals and pickers. Their history is kept and they can be reactivated anytime." / buttons "Cancel" / "Deactivate" red-700) calling `deactivatePerson`; inactive rows show a "Reactivate" action calling `reactivatePerson`. Empty state uses UI-SPEC copy ("No volunteers yet" / body + "Import from Planning Center" primary + "Add person manually" text link). Embed `<RolesConfigPanel />` (Task-1 component) as a section. Create `src/components/RolesConfigPanel.vue`: lists `rosterStore.roles` grouped by Band/Tech/Other with static-class-map group badges (Band=blue-900/50, Tech=purple-900/50, Other=gray-800), each role editable (rename, defaultCount number input) via `updateRole`, addable via an "Add Role" row (name + group select + count) calling `addRole` ("Save Role" copy), deletable via `deleteRole` behind the D-03 confirmation copy ("Delete the '{role name}' role? Existing assignments to this role across all quarters will be cleared. This cannot be undone." / "Cancel" / "Delete Role" red-700). Register `/roster` in router/index.ts with `meta: { requiresAuth: true, requiresEditor: true }` (mirror /songs). Finally, add an editor-gated Roster nav item to `src/components/AppSidebar.vue`: inside the `navItems` computed, add an `if (authStore.isEditor) { items.push({...}) }` block (mirroring the Songs entry at lines 105-113) with `label: 'Roster'`, `to: '/roster'`, and an inline SVG icon in the same `h-4.5 w-4.5` stroke-2 style as the other entries (a people/roster-appropriate glyph) — place it adjacent to the Songs entry so the feature surfaces in the primary nav.</action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | grep -E "RosterView|RolesConfigPanel|AppSidebar|router/index" | grep -qi error && echo HAS_ERR || echo TSOK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --build` reports no errors in RosterView.vue, RolesConfigPanel.vue, AppSidebar.vue, router/index.ts (verify prints TSOK)
    - router/index.ts contains a route with path '/roster' and `requiresEditor: true`
    - AppSidebar.vue `navItems` contains a `to: '/roster'` entry gated behind `authStore.isEditor` with a `label: 'Roster'` and an icon (grep confirms `/roster` and `Roster` inside an `isEditor` push block)
    - RosterView phone field has a label/placeholder indicating manual/app-only entry (grep confirms a phone input; no `fetchAndMapPeople` claims phone)
    - Role-group badges use static class strings (no template-literal Tailwind class construction: grep -c "bg-\${" RolesConfigPanel.vue == 0)
    - Exact CTA/confirmation strings from UI-SPEC present: "Add Volunteer", "Save Volunteer", "Save Role", "Delete Role", "No volunteers yet" (grep each)
  </acceptance_criteria>
  <done>RosterView lists/edits people, toggles active status with the soft-delete confirmation, hosts an editable grouped role list, is reachable at /roster, and the /roster screen is linked from an editor-gated Roster item in the app sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: RosterImportModal (PC people import)</name>
  <files>src/components/RosterImportModal.vue, src/views/RosterView.vue</files>
  <read_first>
    - src/components/PcImportModal.vue (whole file — Step state machine, credential guard, preview counts, Teleport/Transition chrome, error step)
    - src/utils/planningCenterApi.ts (fetchAndMapPeople — Plan 04)
    - src/stores/roster.ts (upsertPeople returning {added, updated})
    - src/stores/auth.ts lines 43-57 (pcCredentials / hasPcCredentials)
    - 13-UI-SPEC.md (import-modal note + PC error copy)
  </read_first>
  <action>Create `src/components/RosterImportModal.vue` as a near-verbatim clone of `PcImportModal.vue`: same `Step` union (idle/fetching/preview/importing/done/error), same Teleport + Transition + modal chrome (`max-w-lg`, `bg-gray-900 rounded-xl border border-gray-700 shadow-2xl`), same `watch(() => props.open, reset)`. On start: guard on `authStore.hasPcCredentials`; call `fetchAndMapPeople(creds.appId, creds.secret)` → set preview list with add/update counts (compare against `rosterStore.people` by pcPersonId/normalized name); on confirm call `rosterStore.upsertPeople(previewList)` and show done with `{added, updated}` counts; on error show the UI-SPEC PC error copy ("Import failed" / "{error message}" / "Go to Settings to check your Planning Center credentials."). Primary CTA copy "Import from Planning Center". Wire the modal into `RosterView.vue` (open/close state + `@imported` refresh). The preview must NOT display or promise a phone value from PC (phone stays blank/app-only).</action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | grep "RosterImportModal" | grep -qi error && echo HAS_ERR || echo TSOK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vue-tsc --build` clean for RosterImportModal.vue and RosterView.vue (prints TSOK)
    - Modal calls `fetchAndMapPeople` and `rosterStore.upsertPeople` (grep both present)
    - Credential guard references `hasPcCredentials`/`pcCredentials` (grep confirms)
    - Error step contains the exact string "Go to Settings to check your Planning Center credentials."
    - No phone value is shown as PC-sourced in the preview (grep: preview does not bind a `.phone` from PC map; phone column absent or blank-labeled)
  </acceptance_criteria>
  <done>Leaders can import PC people through a preview-then-commit modal that upserts the roster and never fabricates phone data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify roster management screen</name>
  <action>After all automation in this plan is complete, the human verifies the /roster screen behaviors listed below. No further code changes are required before this checkpoint; it confirms the automated build works.</action>
  <what-built>The /roster screen: reachable via the sidebar Roster nav item; view/add/edit/deactivate/reactivate volunteers, edit the grouped role list, and import volunteers from Planning Center.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and sign in as an editor. Confirm a "Roster" item appears in the left sidebar and clicking it navigates to /roster (no manual URL typing).
    2. Confirm the empty state reads "No volunteers yet" if the roster is empty.
    3. Click "Add Volunteer", fill name/email/phone/roles/frequency, click "Save Volunteer" — the person appears in the active list with role-group badges (Band=blue, Tech=purple, Other=gray) and a friendly frequency label.
    4. Deactivate the person — confirm the soft-delete wording appears and, after confirming, they move to the inactive section; Reactivate returns them.
    5. In the roles panel, add a role, rename one, change a default count, and delete a role (confirm the delete-role wording).
    6. If PC credentials are configured in Settings, click "Import from Planning Center" and confirm the preview shows add/update counts and imported people have NO phone value (phone blank until entered manually).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → roster store → Firestore | Editor enters/edits roster PII (names, emails, phones) |
| PC import path | PC credentials reused to fetch people PII into the preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07-01 | Information Disclosure | Roster PII visible to non-editors | mitigate | /roster route carries `requiresEditor: true` AND the sidebar Roster nav item is gated behind `authStore.isEditor` (viewers never see the link); store writes hit the org-editor Firestore rule (RESEARCH V4) |
| T-13-07-02 | Spoofing | Fabricated/empty phone presented as authoritative PC data | mitigate | Import preview never shows a PC-sourced phone; phone is a clearly app-only editable field (D-14) — grep-gated |
| T-13-07-03 | Information Disclosure | PC credential handling in the new modal | mitigate | Modal reuses `authStore.pcCredentials` + the Phase 8 masked-credential flow; no credential entry/logging added here |
| T-13-07-04 | Tampering | Accidental hard delete of a volunteer losing history | mitigate | Deactivate is soft-delete only (no deleteDoc); confirmation copy makes the reversible nature explicit (D-20) |
</threat_model>

<verification>
- `npx vue-tsc --build` clean for all five files
- Human checkpoint approved (visual + interaction, including reaching /roster from the sidebar)
- Exact UI-SPEC copy strings present; static badge class maps; phone app-only; editor-gated Roster nav item routes to /roster
</verification>

<success_criteria>
Leaders can fully manage the roster (view/add/edit/deactivate/reactivate — D-13/D-14/D-20), edit the grouped role list (D-03), and import PC people via a preview modal that never invents phone data — all in the existing dark-mode design system, reachable from an editor-gated sidebar Roster item at the editor-only /roster route.
</success_criteria>

<output>
Create `.planning/phases/13-volunteer-scheduling-import-servers-from-planning-center-col/13-07-SUMMARY.md` when done.
</output>
</content>
</invoke>
