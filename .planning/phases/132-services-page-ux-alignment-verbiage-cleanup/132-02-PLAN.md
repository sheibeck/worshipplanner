---
phase: 132-services-page-ux-alignment-verbiage-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ShareView.vue
autonomous: true
requirements: [R408]
must_haves:
  truths:
    - "On a public shared service-link page, the service-plan slot rows alternate a subtle light-gray background (zebra striping) so the plan is easier to scan"
    - "The stripe is theme-appropriate for the public light page (subtle, low-contrast) and applies to the per-row plan list, not to section headers or the footer"
    - "The R346/SEC-S-04 render-side gate is preserved: the change adds ONLY a background class and renders no new slot field (no notes/body/free-text)"
  artifacts:
    - src/views/ShareView.vue
  key_links:
    - "The slot-row `<div v-for=\"(slot, index) in serviceSnapshot.slots\">` uses `index` parity to toggle the alternating background class"
---

<objective>
Add subtle alternating row shading (zebra striping) to the public shared service-plan rows in `src/views/ShareView.vue` (R408), so someone scanning a shared plan can keep their place row-to-row.

Purpose: the shared plan is a flat list of slot rows separated only by a hairline border; alternating backgrounds make it easier to scan.
Output: an updated `ShareView.vue` slot-row loop with an index-parity background class. Template-only, one file, no new data rendered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-services-page-ux-alignment-verbiage-cleanup/132-CONTEXT.md

# The file being changed:
@src/views/ShareView.vue
</context>

## Artifacts this phase produces (Plan 02)
- Modified `src/views/ShareView.vue`: the service-plan slot rows render an alternating light-gray background driven by row index parity, scoped to the plan-row list (not headers/footer), with no new field rendered.

<tasks>

<task type="auto">
  <name>Task 1: Zebra-stripe the shared service-plan rows (R408)</name>
  <files>src/views/ShareView.vue</files>
  <read_first>
    - src/views/ShareView.vue lines 34–137 (the portrait service-plan block: the slot-row loop `v-for="(slot, index) in serviceSnapshot.slots"` at ~line 47 whose row `<div>` has the static class `py-2.5 border-b border-gray-100`, followed by the per-kind slot templates, the "Who's Serving" block at ~line 113 which already uses `bg-gray-50`, and the footer)
    - The page's theme: the outer wrapper is `min-h-screen bg-white text-gray-900` (this is the PUBLIC light page — pick a light, low-contrast stripe token, not a dark one)
  </read_first>
  <action>
    On the slot-row `<div>` inside the `v-for="(slot, index) in serviceSnapshot.slots"` loop (the one currently classed `py-2.5 border-b border-gray-100`), add an alternating background driven by `index` parity. Keep the existing static classes and add a dynamic class binding that applies a subtle light-gray background to alternate rows (apply `bg-gray-50` — the same low-contrast surface token the "Who's Serving" block already uses — to odd-index rows, leaving even rows on the page's white background). Because the row already has a static `class`, add a separate `:class` binding expressed via `index % 2` so the two coexist. To keep the stripe reading as a full row on the narrow `max-w-2xl` column you may extend the row's horizontal padding so the shaded band is not flush to the text; that is at your discretion — the required outcome is a visible-but-subtle alternating band on the plan rows. Do NOT touch the `?view=stage` landscape block, the "Who's Serving" roles list, section headers, or the footer — R408 targets the service order/plan rows only. CRITICAL: add ONLY presentation classes. Do NOT add or re-introduce any slot free-text render (no `slot.notes`, `slot.body`, `marker.note`, or service-level Notes): ShareView.vue intentionally omits those as a render-side security gate (R346/SEC-S-04), and this task must not weaken it.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/ShareView.vue','utf8'); const zebra=/index % 2/.test(s) && /bg-gray-50/.test(s); const noNotes=!/slot\.notes|slot\.body|marker\.note/.test(s); if(!(zebra&&noNotes)){console.error('zebra='+zebra+' securityGatePreserved='+noNotes); process.exit(1);} console.log('ok');"</automated>
    <human-check>Open a shared service link (a /share URL) in the browser. Confirm the plan rows alternate a faint gray/white background that is easy on the eyes (subtle, not a strong banding), the striping covers the slot rows only (not the "Who's Serving" card, headers, or footer), and no per-item notes or free text appear.</human-check>
  </verify>
  <acceptance_criteria>
    - The `serviceSnapshot.slots` row `<div>` applies an alternating background using `index % 2` and the `bg-gray-50` token; even rows stay on the white page background.
    - Striping is scoped to the plan-row list — the `?view=stage` block, "Who's Serving" roles list, headers, and footer are unchanged.
    - No new slot free-text field is rendered (`slot.notes` / `slot.body` / `marker.note` absent), preserving the R346/SEC-S-04 render gate.
    - `npm run type-check` passes; `npx vitest run` shows only the baseline `src/storage.rules.test.ts` failure.
  </acceptance_criteria>
  <done>Shared plan slot rows alternate a subtle `bg-gray-50` background by index parity, scoped to the plan-row list; R346/SEC-S-04 gate intact (no new field rendered); type-check and suite clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public (unauthenticated) → shared snapshot | ShareView renders a public, read-only `serviceSnapshot`; the render-side R346/SEC-S-04 gate already strips per-item notes / free text / service notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-02 | Information Disclosure | ShareView slot-row render | high | mitigate | Change is a background class only; task explicitly forbids adding any `slot.notes`/`slot.body`/`marker.note`/Notes render, and an automated grep asserts they remain absent — the R346/SEC-S-04 gate is preserved |
| T-132-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (template-only edit); no new supply-chain surface |
</threat_model>

<verification>
- `npm run type-check` passes (vue-tsc --build).
- `npx vitest run` shows only the known baseline failure `src/storage.rules.test.ts`.
- Manual public-share visual check per the task human-check.
</verification>

<success_criteria>
- Shared service-link plan rows alternate a subtle light-gray background (R408).
- The public page's PII render gate (R346/SEC-S-04) is intact — no new fields rendered.
- No type or test regressions beyond the documented baseline.
</success_criteria>

<output>
Create `.planning/phases/132-services-page-ux-alignment-verbiage-cleanup/132-02-SUMMARY.md` when done.
</output>
