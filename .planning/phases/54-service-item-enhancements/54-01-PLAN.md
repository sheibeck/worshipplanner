---
phase: 54-service-item-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/slideGroupMaterializer.ts
  - src/utils/__tests__/slideGroupMaterializer.test.ts
autonomous: true
requirements: [R123]
user_setup: []

must_haves:
  truths:
    - "A newly added Miscellaneous item derives zero slides: deriveGroupEntries(MISC) returns [] (R123)"
    - "ANNOUNCEMENTS, PRAYER, MESSAGE and HYMN slots still each derive exactly one {kind:'text'} entry — unchanged"
    - "rebuildGroup(MISC) stays a no-op — an existing MISC blank auto-slide persists and a hand-added MISC slide survives untouched (BWC guard)"
    - "npm run type-check (vue-tsc --build) is clean and every switch (slot.kind) stays exhaustive with no default"
  artifacts:
    - "src/utils/slideGroupMaterializer.ts — deriveGroupEntries has a dedicated `case 'MISC': return []` branch, split out of the one-text-slide fall-through"
    - "src/utils/__tests__/slideGroupMaterializer.test.ts — a `deriveGroupEntries — MISC (R123)` describe (returns []), an ANNOUNCEMENTS regression assertion, and rebuildGroup(MISC) no-op / hand-add-survival tests"
  key_links:
    - "deriveGroupEntries(MISC) -> [] -> materializationCandidates skips zero-slide derivations (useSlideshowAssembly.ts:470-477) -> no group document created for new MISC items"
    - "rebuildGroup(MISC) remains in the no-op fall-through (slideGroupMaterializer.ts:938-943) -> existing MISC groups and hand-added slides are never rewritten"
---

<objective>
Make Miscellaneous service items default to NO slides (R123), while leaving every other item type
and all existing MISC data untouched. The only production change is a one-branch split in the pure
`deriveGroupEntries` function so a MISC slot derives `[]` instead of one blank text slide.

Purpose: A brand-new Miscellaneous item should start clean — no auto-derived blank slide — yet the
user can still hand-add slides to it, and no existing MISC item loses anything.
Output (artifacts this plan produces):
- A dedicated `case 'MISC': return []` branch in `deriveGroupEntries` (slideGroupMaterializer.ts).
- New tests pinning: MISC derives `[]`, siblings unchanged, rebuild(MISC) is a no-op preserving both
  the legacy blank slide and any hand-added slide.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-service-item-enhancements/54-RESEARCH.md
@.planning/phases/54-service-item-enhancements/54-VALIDATION.md
@CLAUDE.md

# The pure function this plan changes, and its test file
@src/utils/slideGroupMaterializer.ts
@src/utils/__tests__/slideGroupMaterializer.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin MISC-derives-nothing, sibling regression, and MISC rebuild no-op</name>
  <files>src/utils/__tests__/slideGroupMaterializer.test.ts</files>
  <read_first>
    - src/utils/__tests__/slideGroupMaterializer.test.ts — the harness: `makeInputs()` (:93-101),
      `makeGroup()` (:150-160), the existing `deriveGroupEntries — PRAYER/MESSAGE/HYMN` describe
      (:348-376), and the existing `rebuildGroup` no-op tests (~:2030-2038). Imports at the top include
      `deriveGroupEntries`, `rebuildGroup`, and `NonAssignableSlot`.
    - src/utils/slideGroupMaterializer.ts:157-162 (MISC currently in the one-text-slide fall-through)
      and :938-943 (rebuildGroup MISC no-op) — so the assertions match the seams being changed.
  </read_first>
  <behavior>
    - deriveGroupEntries({kind:'MISC', id, position:0}, makeInputs()) === [] (RED today — MISC currently
      returns one {kind:'text'} entry).
    - deriveGroupEntries({kind:'ANNOUNCEMENTS', id, position:0}, makeInputs()) has length 1 with
      sourceRef {kind:'text'} (regression — passes today, must keep passing).
    - rebuildGroup(groupWithOnlyAutoTextEntry, miscSlot, makeInputs()) === {changed:false, slides: group.slides}
      (BWC — the legacy blank auto-slide on existing MISC items persists; passes today, must keep passing).
    - rebuildGroup(groupWithHandAddedSlide {kind:'text', title:'New slide', body:''}, miscSlot, makeInputs())
      returns changed:false and the identical slides (hand-added slide survives; passes today, must keep passing).
  </behavior>
  <action>
    Add two new describes to src/utils/__tests__/slideGroupMaterializer.test.ts, next to the existing
    derive/rebuild groups, using the file's own `makeInputs`, `makeGroup`, and `NonAssignableSlot` import.

    First describe, `deriveGroupEntries — MISC (R123)`:
      - assert a MISC slot ({kind:'MISC', id:'slot-misc-0', position:0}) derives an empty array via
        toEqual([]). This is the RED driver — it fails against current source.
      - add a sibling regression assertion that an ANNOUNCEMENTS slot still derives exactly one entry
        with sourceRef toEqual({kind:'text'}) (mirror the existing PRAYER/MESSAGE assertions at :349-361).

    Second describe, `rebuildGroup — MISC no-op (R123 backward-compat)`:
      - a group whose slides are exactly [{id:'e1', order:0, sourceRef:{kind:'text'}}] (the legacy
        auto-derived blank slide) rebuilt against a MISC slot returns changed:false and slides deep-equal
        to the input group.slides (mirror the PRAYER no-op test at ~:2030-2038).
      - a group whose slides are [{id:'h1', order:0, sourceRef:{kind:'text', title:'New slide', body:''}}]
        (a hand-added slide) rebuilt against a MISC slot returns changed:false and the identical slides.

    Do NOT edit slideGroupMaterializer.ts in this task. Only the MISC-derives-[] assertion should fail;
    the three others document current behavior and must already pass.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts</automated>
  </verify>
  <done>
    The run reports exactly one new failing assertion — the MISC-derives-[] case (RED). The
    ANNOUNCEMENTS regression and both rebuild(MISC) no-op tests pass. No production file changed yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — split `case 'MISC': return []` out of the fall-through</name>
  <files>src/utils/slideGroupMaterializer.ts</files>
  <read_first>
    - src/utils/slideGroupMaterializer.ts:51-164 — the `deriveGroupEntries` switch, especially the
      `case 'PRAYER': case 'MESSAGE': case 'ANNOUNCEMENTS': case 'MISC': case 'HYMN': return [{...text...}]`
      block at :157-162.
    - 54-RESEARCH.md § "Sibling `case 'MISC':` sites — which need splitting (audit)" — ONLY
      deriveGroupEntries changes; the sourceSignature (~:242), isSlotDerivableRef (~:294), and
      rebuildGroup (~:938-943) MISC sites MUST stay as-is (rebuildGroup MISC must remain a no-op).
  </read_first>
  <action>
    In src/utils/slideGroupMaterializer.ts, split MISC out of the one-text-slide fall-through in
    `deriveGroupEntries` only. Add a `case 'MISC': return []` branch immediately above the remaining
    `case 'PRAYER': case 'MESSAGE': case 'ANNOUNCEMENTS': case 'HYMN':` group, which keeps returning the
    single `{ id: crypto.randomUUID(), order: 0, sourceRef: { kind: 'text' } }` entry. MISC must no
    longer fall through to the text entry.

    Change nothing else. Do NOT touch the MISC arms in `sourceSignature`, `isSlotDerivableRef`, or
    `rebuildGroup` — the rebuildGroup MISC no-op at :938-943 is exactly what preserves existing MISC
    groups and hand-added slides, per 54-RESEARCH.md. Keep every `switch (slot.kind)` exhaustive with no
    `default` (CLAUDE.md / Phase 43 rule), so MISC stays covered in the other three switches' text group.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts && npm run type-check</automated>
  </verify>
  <done>
    All four new assertions pass (MISC -> [], ANNOUNCEMENTS -> one text entry, both rebuild(MISC) no-op
    cases). `npm run type-check` is clean (exhaustive switch holds). No sibling MISC site was changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service editor -> materializer | Slot data (kind only) crosses into a pure derivation function; no user-typed text is interpreted here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-02 | Tampering | deriveGroupEntries MISC branch | low | accept | Pure function, deterministic; no external input, no persistence, no data deleted — existing MISC groups are left byte-identical (rebuild MISC stays a no-op), proven by the BWC tests in Task 1 |
| T-54-SC | Tampering | package installs | low | accept | This plan installs no npm packages (54-RESEARCH.md § Package Legitimacy Audit: not applicable) — no supply-chain surface |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts` — all new + existing derive/rebuild
  assertions pass.
- `npm run type-check` — clean (vue-tsc --build); every `switch (slot.kind)` remains exhaustive.
- No change to any sibling MISC site (sourceSignature / isSlotDerivableRef / rebuildGroup).
</verification>

<success_criteria>
- deriveGroupEntries(MISC) returns [] — a new Miscellaneous item derives no slides (R123).
- ANNOUNCEMENTS/PRAYER/MESSAGE/HYMN still each derive one text slide.
- Existing MISC items keep their blank auto-slide and any hand-added slide (rebuild MISC no-op) — nothing deleted.
- Type-check clean; app-suite baseline unchanged.
</success_criteria>

<output>
Create `.planning/phases/54-service-item-enhancements/54-01-SUMMARY.md` when done.
</output>
