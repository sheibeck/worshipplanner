---
phase: 25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/slide.ts
  - src/types/slideGroup.ts
  - src/utils/slideshowAssembler.ts
  - src/utils/slideGroupMaterializer.ts
  - src/utils/__tests__/slideshowAssembler.test.ts
  - src/utils/__tests__/slideGroupMaterializer.test.ts
  - src/composables/__tests__/useSlideshowAssembly.test.ts
autonomous: true
requirements: [R032, R018]

must_haves:
  truths:
    - "A `VideoSlide` variant exists in the `Slide` union and a `video`-kind `SourceRef` exists, so a dropped video can be expressed as a real slide inside a group rather than degraded to the group bed (D-17, R032)."
    - "A user-authored blank text slide carries its own title/body on the entry, so `＋ Add slide` on a SONG/SCRIPTURE/IMPORTED group produces a slide the assembler actually emits instead of silently omitting it."
    - "A group entry whose `sourceRef.kind` is `video` assembles into an `AssembledSlide` whose slide carries the entry's own video source, and a group that ALSO has a bed video does not overwrite that source with the bed's (D-17 vs D-04)."
    - "A song group carrying a user-appended video entry survives a lyric-edit reconciliation with that entry still present — never silently dropped (Phase 24 D-02's standing rule)."
    - "A scripture or imported group carrying a user-appended video entry is treated as customized, so a diverged source routes to the confirm path instead of a silent wholesale replace that would delete the video."
    - "`ImportedDeck.slides` stays typed `(TextSlide | ImageSlide)[]` — PPTX decks contain no video and the deck type is deliberately not widened (D-17)."
  artifacts:
    - "src/types/slide.ts with a `VideoSlide` interface in the `Slide` union"
    - "src/types/slideGroup.ts with `video` added to `SourceRef` and the `text` member able to carry authored content"
    - "src/utils/slideshowAssembler.ts resolving both new `SourceRef` shapes"
    - "src/utils/slideGroupMaterializer.ts retaining non-derivable entries through reconciliation"
    - "extended tests in slideshowAssembler.test.ts, slideGroupMaterializer.test.ts and useSlideshowAssembly.test.ts"
  key_links:
    - "`VideoSlide`'s own content field is named distinctly from `SlideBase.videoUrl` — `videoUrl` is the group-BED carrier that `resolveEntryMedia` fills from `group.bedVideoUrl`, so reusing that name would let a bed clobber a video slide's own source inside `emitFromGroup`'s spread"
    - "`hasCustomization` is the single gate that decides silent-replace vs confirm for scripture/imported groups — teaching it about video entries is what stops `deriveGroupEntries` from deleting them"
    - "`reconcileSongGroup` rebuilds its output from lyric+copyright entries only; any entry kind it does not know about must be explicitly carried over or it is lost"
  prohibitions:
    - "MUST NOT widen `ImportedDeck.slides` to include video. The deck type is a PPTX parse result; D-17 scopes video to group entries only."
    - "MUST NOT change the id-minting scheme in `deriveGroupEntries` or regenerate a `GroupSlideEntry.id` anywhere — Phase 23's WR-02 media-child keying contract depends on those ids being minted once."
    - "MUST NOT resolve the video-entry drop by writing to `SlideGroup.bedVideoUrl`. 25-RESEARCH.md Pattern 5 recommended that BEFORE D-17 was decided; 25-VALIDATION.md records the correction and the owner chose the real type."
---

<objective>
Land D-17: give the data model a genuine `VideoSlide`, so the approved drop-tile behaviour ("video
appends a slide") is buildable, and teach reconciliation to carry user-appended entries through
untouched. Also extend the `text` source reference so a user-authored blank slide has somewhere to
store its own words.

Purpose: every other plan in this phase that touches video or `＋ Add slide` is unbuildable until
this lands. It is deliberately its own plan in the earliest wave (25-CONTEXT.md D-17 planning
guidance: "give the type/model work its own plan and wave, landing BEFORE the drop target").
Output: two type files, two pure utility files, and three extended test suites. No UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-VALIDATION.md
@.planning/phases/24-slide-group-model-and-migration/24-CONTEXT.md
</context>

<hard_constraints>
Non-negotiable for every task in this plan:
- Do NOT run `npm run test:rules`. Do NOT start, stop, or restart the Firebase emulator — a live user
  session may hold ports 8080/9199.
- Use `npx vitest run <path>`. Never bare `vitest` (watch mode), never `npm run test:unit`.
- Sequential main-tree execution on branch `milestone/M001`. Do not create or switch branches.
- Known-failing baseline for `npx vitest run src/` is exactly **10 FILES**: 8 under
  `.gsd/quarantine/worktrees/**`, plus `src/storage.rules.test.ts` and
  `src/views/__tests__/RosterView.test.ts`. The failing TEST COUNT flaps run to run; judge against the
  FILE SET, which must not grow. Do not attempt to fix any of the 10.
- `npm run type-check` must stay at 0 errors and `npm run build` must succeed.
- **25-RESEARCH.md contains one stale statement.** Written before D-17, it recommends routing a
  dropped video to `setGroupBedMedia({ bedVideoUrl })` (its Pattern 5 / Assumption A2 / Pitfall 7).
  That is superseded. 25-VALIDATION.md's "Correction applied to the source research" is authoritative:
  audio sets the group bed, video appends a slide. Everything else in 25-RESEARCH.md stands.
</hard_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VideoSlide type, video and authored-text source refs, assembler resolution</name>
  <files>src/types/slide.ts, src/types/slideGroup.ts, src/utils/slideshowAssembler.ts, src/utils/__tests__/slideshowAssembler.test.ts</files>
  <read_first>
    - src/types/slide.ts in full — `SlideContentKind` (which already reserves the `video` literal), `SlideBase` and its `audioUrl`/`videoUrl` doc comment describing them as media CARRIERS, `ImageSlide` as the shape to mirror, and the `Slide` union
    - src/types/slideGroup.ts in full — `GroupSlideEntry`, the five-member `SourceRef` union and the doc comment explaining why `copyright` was added as its own member
    - src/utils/slideshowAssembler.ts lines 94-200 — `sourceIdForRef`, `resolveEntryContent`, `resolveEntryMedia` (note its comment "Video has no per-slide layer — it is always the group bed", which this task supersedes) and `emitFromGroup`'s spread order
    - src/utils/slideshowAssembler.ts lines 78-92 — `buildTextContentForSlot`, which returns undefined for SONG, SCRIPTURE and IMPORTED slots; that undefined is what makes a group-scoped blank slide invisible today
    - src/utils/__tests__/slideshowAssembler.test.ts — existing fixture builders and assertion style for the group-resolved path
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md § D-17 — the ripple list and the owner's acknowledged cost
  </read_first>
  <behavior>
    - A group entry with a video source ref assembles to one slide whose content kind is video and whose own source URL is the entry's.
    - A group that has BOTH a bed video and a video entry assembles the video entry with its own source intact, and the bed still lands on `videoUrl` as before for every slide in that group.
    - A video entry's assembled slide id equals the stored entry id, exactly like every other group-resolved slide.
    - The assembled provenance for a video entry carries no canonical source id, because a dropped video has no canonical record behind it.
    - A text entry carrying its own authored title and body assembles to a text slide using those values, on a SONG slot as well as on a PRAYER slot.
    - A text entry carrying no authored content still falls back to the slot-derived text exactly as it does today, so every existing PRAYER, MESSAGE and HYMN group is byte-identical.
  </behavior>
  <action>
In `src/types/slide.ts`, add a `VideoSlide` interface extending `SlideBase` with `contentKind` fixed to
the existing `video` literal, a required own-source string field, and an optional original file name
used for display. Name the required field `videoSrc` — deliberately NOT `videoUrl`. `SlideBase.videoUrl`
is the group-BED carrier that `resolveEntryMedia` populates from `group.bedVideoUrl`, and
`emitFromGroup` conditionally spreads that carrier over the resolved content; if a video slide's own
source used the same key, a group with a bed video would silently replace the slide's own footage.
State that reason in the interface doc comment so a later reader does not "tidy" the names together.
Add `VideoSlide` to the `Slide` union. Leave `SlideContentKind` alone — it already lists `video`.

Do NOT touch `src/types/importedDeck.ts`. `ImportedDeck.slides` stays `(TextSlide | ImageSlide)[]`; a
PPTX parse never yields video and widening it would spread this change into the Phase 21 import
pipeline for no gain.

In `src/types/slideGroup.ts`, extend `SourceRef` twice:
- Add a sixth member for video, carrying the same own-source field name used on `VideoSlide` plus the
  optional file name. Unlike the other members it references no canonical record — a dropped video has
  no document behind it, so the storage URL IS the reference. Say so in the union's doc comment,
  alongside the existing note about why `copyright` is its own member.
- Widen the existing `text` member with optional authored title and body fields. Today a `text` entry
  carries nothing and its content is derived entirely from the owning slot, which is correct for the
  auto-derived PRAYER/MESSAGE/HYMN entry but leaves a user-added blank slide with nowhere to store its
  words. Keep both fields optional so every stored entry written by Phase 24 stays valid unchanged.

In `src/utils/slideshowAssembler.ts`:
- Extend `sourceIdForRef` with the video case, returning the same no-canonical-record value the `text`
  case returns.
- Extend `resolveEntryContent` with a video case building the `VideoSlide` content shape from the ref,
  omitting the optional file name key entirely when absent (the conditional-spread discipline
  `buildInitialGroup` and `createSlot` already follow, so Firestore never sees an undefined value).
- Change `resolveEntryContent`'s existing `text` case so that when the ref carries authored content it
  builds the text content from the ref itself, and otherwise falls through to the current
  `buildTextContentForSlot(slot)` behaviour unchanged. Treat an authored entry with a body but no title
  as valid.
- Replace `resolveEntryMedia`'s "Video has no per-slide layer" comment with an accurate one: video is
  still the only BED-ONLY media layer (a group bed video plays across the group), and a video SLIDE is
  a separate concept carrying its own source on the slide, not through the bed carrier. Do not change
  the function's behaviour; the two concepts coexist because they use different field names.

Extend `src/utils/__tests__/slideshowAssembler.test.ts` with the behaviours above, including the
both-bed-and-video-entry case, which is the one that proves the naming decision holds.
  </action>
  <acceptance_criteria>
    - A test asserts a group with one video entry assembles to exactly one slide whose content kind is video and whose own source equals the entry's stored source.
    - A test with a bed video AND a video entry asserts the video slide's own source is unchanged and the bed still reaches the media carrier field.
    - A test asserts the video slide's id equals the stored entry id.
    - A test asserts a text entry with authored title and body on a SONG slot assembles that title and body.
    - A test asserts a text entry with no authored content on a PRAYER slot assembles exactly what it does today.
    - `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts` exits 0.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideshowAssembler.test.ts</automated>
  </verify>
  <done>A video slide and a user-authored text slide are both expressible in the model and both assemble correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconciliation carries user-appended entries instead of deleting them</name>
  <files>src/utils/slideGroupMaterializer.ts, src/utils/__tests__/slideGroupMaterializer.test.ts</files>
  <read_first>
    - src/utils/slideGroupMaterializer.ts lines 43-105 — `deriveGroupEntries`, which switches on slot kind and therefore never mints a video entry (this is correct and stays that way)
    - src/utils/slideGroupMaterializer.ts lines 178-302 — `hasCustomization`, `isLyricEntry`, `isCopyrightEntry` and the whole of `reconcileSongGroup`, in particular how `merged` is rebuilt from copyright and lyric entries only
    - src/utils/slideGroupMaterializer.ts lines 313-354 — `computeLoss` and `reconcileUnstableIdGroup`'s three branches, where the uncustomized branch replaces the stored slides wholesale with `deriveGroupEntries`
    - .planning/phases/24-slide-group-model-and-migration/24-CONTEXT.md § D-02 — "never silently drop a user's added slide, audio, label or note"
    - src/utils/__tests__/slideGroupMaterializer.test.ts — existing group and slot fixtures
  </read_first>
  <behavior>
    - A song group holding a video entry alongside its lyric and copyright entries keeps that video entry after a reconciliation triggered by an added lyric section.
    - The retained video entry keeps its own id and its own stored source; only its order number may change.
    - A song group holding a user-authored text entry keeps that entry through the same reconciliation.
    - A scripture group holding a video entry and nothing else user-authored reports as customized.
    - A scripture group holding a video entry whose source signature diverged returns the confirm-required outcome with the stored slides untouched, rather than the replaced-silently outcome.
    - The reported loss summary counts a video entry as user work, so the count shown to a user is never zero while a video entry is at stake.
    - A group with no video and no authored text reconciles byte-identically to today, on every slot kind.
  </behavior>
  <action>
In `reconcileSongGroup`, the merged output is rebuilt from the stored copyright entries and the fresh
section order, so any entry whose source ref is neither lyric nor copyright currently disappears.
Collect those other entries from the stored group, preserving their stored relative order, and append
them after the retained-but-unresolvable lyric run and before the trailing copyright entry — the exact
position and convention the existing retention rule already uses for unresolvable lyric entries. Carry
each entry by value, renumbering only its order, so its id, label, notes and own audio survive. Leave
the `changed` comparison as it is; it compares the rebuilt list against the stored one positionally and
stays correct with the extra entries present.

In `hasCustomization`, treat the presence of any entry whose source ref is not derivable from the slot
(today: a video entry, and a text entry carrying authored content) as customization. This is the single
gate `reconcileUnstableIdGroup` consults before replacing a scripture or imported group's slides
wholesale with a fresh derivation; without this, a re-split passage or a re-imported deck would delete
a user's dropped video with no confirm and no trace. Document that reasoning inline — this function
looks like a cosmetic predicate and is in fact the deletion gate.

In `computeLoss`, count those same non-derivable entries toward `customizedEntries`, so the count the
Phase 26 confirm dialog (and this phase's passive banner) shows can never read zero while real user
work is at stake.

Do NOT add video to `deriveGroupEntries`. No slot kind derives a video slide; video entries only ever
arrive by user action (a drop, in 25-06). Do NOT change `sourceSignature` — it fingerprints the SOURCE,
which a user-appended entry is not part of.

Extend `src/utils/__tests__/slideGroupMaterializer.test.ts` with the behaviours above, including the
byte-identical no-video regression case for every slot kind.
  </action>
  <acceptance_criteria>
    - A test adds a lyric section to a song group holding a video entry and asserts the video entry is still present afterwards with its original id and source.
    - A test asserts the retained video entry sits after the resolvable lyric run and before the trailing copyright entry.
    - A test asserts a user-authored text entry survives the same song reconciliation.
    - A test asserts a scripture group whose only user work is a video entry reports as customized.
    - A test asserts that same group with a diverged signature returns the confirm-required outcome and the stored slides unchanged.
    - A test asserts the loss summary counts the video entry.
    - A test asserts a group with neither video nor authored text reconciles to exactly the same result as before this change.
    - `npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideGroupMaterializer.test.ts</automated>
  </verify>
  <done>Reconciliation can no longer delete a slide the user added by hand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Composable-level guard that a dropped video survives a live reconciliation tick</name>
  <files>src/composables/__tests__/useSlideshowAssembly.test.ts</files>
  <read_first>
    - src/composables/useSlideshowAssembly.ts lines 292-384 — `reconciliationOutcomes`, `applyReconciliationOutcomes`, `pendingReconciliationsMap` and the `appliedGroupRefForSlot` guard
    - src/composables/__tests__/useSlideshowAssembly.test.ts — the `effectScope()` isolation pattern each invocation is wrapped in, and how the slideGroups store is mocked and driven
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md § Pitfall 8 — the composable test isolation gotchas that still apply
  </read_first>
  <behavior>
    - Driving the composable with a song group that holds a video entry, then changing the song's lyrics so reconciliation fires, results in a persisted slide list that still contains the video entry.
    - Driving it with a scripture group that holds a video entry and a diverged source produces a pending reconciliation for that slot and issues no slide-replacing write.
    - The assembled output for that scripture group still shows the stored slides, including the video slide, while the reconciliation is pending.
  </behavior>
  <action>
Add a describe block to `src/composables/__tests__/useSlideshowAssembly.test.ts` covering the three
behaviours above. Wrap every composable invocation in its own `effectScope()` stopped in `afterEach`,
matching the file's existing convention — a leaked watcher from a prior test fires against later test
data and corrupts assertions (this exact leak was found and fixed in 24-05).

Assert on what the store received, not on internal composable state: for the song case, inspect the
argument the mocked `replaceGroupSlides` was called with; for the scripture case, assert the mock was
not called for that slot and that `pendingReconciliations` contains an entry for it.

This task adds no production code. It is the end-to-end guard for the highest-consequence regression
this phase can produce: a video a user dropped disappearing on the next lyric edit.
  </action>
  <acceptance_criteria>
    - A test asserts the slide list passed to the slide-replacing store action still contains the video entry after a lyric change.
    - A test asserts no slide-replacing write is issued for a customized scripture group with a diverged source, and that a pending reconciliation is surfaced for it.
    - A test asserts the assembled output for that group still includes the video slide while the reconciliation is pending.
    - `npx vitest run src/composables/__tests__/useSlideshowAssembly.test.ts` exits 0.
    - `npx vitest run src/utils/ src/composables/` shows no new failing files.
    - `npm run type-check` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/composables/__tests__/useSlideshowAssembly.test.ts</automated>
  </verify>
  <done>The "never silently drop a user's slide" rule is enforced end to end, not just unit by unit.</done>
</task>

</tasks>

## Artifacts this phase produces (contributed by 25-01)

| Symbol / path | Kind | Notes |
|---|---|---|
| `VideoSlide` | new interface | `src/types/slide.ts`, sixth member of the `Slide` union |
| `SourceRef` video member | new union member | own-source URL, no canonical record behind it |
| `SourceRef` text member | widened | optional authored title/body for user-added blank slides |
| `resolveEntryContent` | changed | resolves video refs and authored-text refs |
| `sourceIdForRef` | changed | video case |
| `reconcileSongGroup` | changed | carries non-lyric, non-copyright entries through the merge |
| `hasCustomization` / `computeLoss` | changed | non-derivable entries count as user work |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored group document → assembled projector output | A malformed or unresolvable entry must degrade to omission, never to a crash mid-service. |
| automatic reconciliation → user-authored slide data | The only place in this phase where code deletes user content without a gesture. |

## STRIDE Threat Register (OWASP ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01-01 | Tampering | reconciliation silently deleting a user-appended video or authored text entry | high | mitigate | `reconcileSongGroup` carries non-derivable entries through by value; `hasCustomization` routes scripture/imported groups holding one to the confirm path instead of a silent replace. Guarded by unit tests and by the composable-level test in Task 3. |
| T-25-01-02 | Tampering | a group bed video overwriting a video slide's own source in `emitFromGroup` | high | mitigate | The slide's own source field is named distinctly from the `videoUrl` bed carrier, so the two never occupy the same key. Asserted by a both-present test. |
| T-25-01-03 | Denial of Service | an unresolvable entry breaking assembly mid-presentation | medium | mitigate | Unchanged existing contract — `resolveEntryContent` returning undefined causes the entry to be omitted, never substituted or thrown. The new video case cannot return undefined (its source is self-contained). |
| T-25-01-04 | Repudiation | a confirm dialog reporting zero slides at stake while a video is about to be replaced | medium | mitigate | `computeLoss` counts non-derivable entries, so the count a user sees reflects the real loss. |
| T-25-01-05 | Tampering | an arbitrary URL stored as a video entry source | medium | accept | Entry sources are written only by this app's own upload path (25-06 uses `useMediaUpload`, whose Storage writes are gated by `storage.rules`), and `slideGroups` writes are already gated by the org-editor Firestore rule. A user who can write the group can already write any field on it; no new boundary is created here. |
| T-25-01-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed. 25-RESEARCH.md § Package Legitimacy Audit records "Not applicable — this phase installs zero new packages". |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts src/utils/__tests__/slideGroupMaterializer.test.ts src/composables/__tests__/useSlideshowAssembly.test.ts` exits 0.
- `npx vitest run src/` fails in no more than the 10 baseline files listed in `<hard_constraints>`.
- `npm run type-check` exits 0 and `npm run build` exits 0.
- `git diff --stat src/types/importedDeck.ts` is empty — the deck type was not widened.
- `git diff --stat firestore.rules storage.rules` is empty — no rules change is needed for a new field on an existing document.

<human-check>
Deferred to the project's batch human-verify (STATE.md Deferred Verification), NOT a blocking
checkpoint. Carried from `25-VALIDATION.md` § Manual-Only Verifications:
1. Video slide playback on real projector hardware — after 25-02 and 25-06 land, append a video slide,
   enter Present mode, confirm it plays and that Phase 23's bed-video behaviour is unchanged.
</human-check>
</verification>

<success_criteria>
- The model can express a video slide, so the approved drop-tile copy is honest.
- A user-added blank slide has somewhere to keep its words.
- No reconciliation path can delete either one without asking.
</success_criteria>

<output>
Create `.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-01-SUMMARY.md` when done.
</output>
