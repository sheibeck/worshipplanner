---
phase: 06-ai-assisted-service-suggesting-and-scripture-searching
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified: []
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - AI-05
  - AI-06

must_haves:
  truths:
    - "Complete AI-assisted service planning workflow verified by human"
    - "Service editor works fully without AI context"
    - "AI suggestions are relevant to sermon theme"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete AI-assisted service planning workflow.

Purpose: Verify that AI song suggestions, scripture discovery, and the "Suggest All Songs" flow work correctly end-to-end with a real Claude API key.

Output: Verified AI integration ready for use.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ai-assisted-service-suggesting-and-scripture-searching/06-CONTEXT.md
@.planning/phases/06-ai-assisted-service-suggesting-and-scripture-searching/06-02-SUMMARY.md
@.planning/phases/06-ai-assisted-service-suggesting-and-scripture-searching/06-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete AI-assisted service planning workflow</name>
  <files></files>
  <action>
    Present the following verification steps to the user. All AI features were built in Plans 01-03:
    sermon topic field, AI song suggestions in SongSlotPicker, "Suggest All Songs" with accept/reject,
    natural language scripture search, and per-slot "Suggest Scripture" button.
    All backed by Claude Haiku API calls through claudeApi.ts.
  </action>
  <verify>Human confirms all 8 test scenarios pass</verify>
  <done>User types "approved" confirming the complete AI workflow works correctly</done>
  <what-built>
    Complete AI-assisted service planning: sermon topic field, AI song suggestions in SongSlotPicker, "Suggest All Songs" with accept/reject, natural language scripture search, and per-slot "Suggest Scripture" button. All backed by Claude Haiku API calls through claudeApi.ts.
  </what-built>
  <how-to-verify>
    Prerequisites:
    - Ensure VITE_CLAUDE_API_KEY is set in .env.local with a valid Anthropic API key
    - Run `npm run dev` to start the dev server

    Test 1 -- Service editor without AI:
    1. Open a service (or create one) in the editor
    2. Verify it works exactly as before -- song slots, scripture inputs, save, print, share all function
    3. No broken states, no empty AI sections cluttering the UI

    Test 2 -- Sermon topic field:
    1. In the service editor, find the "Sermon Context" card (was "Sermon Passage")
    2. Enter a sermon topic like "Grace and forgiveness"
    3. Verify the text input accepts and displays the text
    4. Save the service, reload the page, verify topic persists

    Test 3 -- AI song picks per slot:
    1. With sermon topic filled, click a song slot picker (the "+ Click to select a song" button)
    2. Verify an "AI Picks" section appears above "By Rotation"
    3. Verify it shows up to 3 songs with short reason text
    4. Verify shimmer loading animation appeared briefly before results loaded
    5. Click an AI pick -- verify it selects the song into the slot

    Test 4 -- Suggest All Songs:
    1. Clear all song slots in the service
    2. Click "Suggest All Songs" button in the header
    3. Verify AI drafts appear inline in each empty song slot
    4. Each draft shows song title (slightly muted), reason text, green checkmark, gray X
    5. Accept one draft -- verify it assigns the song
    6. Reject another -- verify it clears and shows the normal empty picker

    Test 5 -- AI without context:
    1. Clear the sermon topic and sermon passage
    2. Verify "Suggest All Songs" button is disabled
    3. Open a song slot picker -- verify placeholder text: "Add a sermon topic or passage for AI suggestions"

    Test 6 -- Scripture AI search:
    1. In a scripture reading slot (not sermon passage), find the search field above the structured picker
    2. Type "passages about forgiveness" and press Enter or click Search
    3. Verify 3-5 results appear with reference (e.g., "Psalm 32:1-5") and reason
    4. Click a result -- verify it populates the book/chapter/verse fields
    5. Verify sermon passage slot does NOT have the search field

    Test 7 -- Scripture suggest button:
    1. In a scripture reading slot with sermon context filled but no search query
    2. Click "Suggest scripture based on sermon"
    3. Verify results appear based on sermon topic

    Test 8 -- Error handling:
    1. Temporarily clear VITE_CLAUDE_API_KEY from .env.local
    2. Restart dev server
    3. Open song picker -- verify "Suggestions unavailable" with retry link
    4. Restore the key and restart
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification covers the full AI workflow end-to-end.
</verification>

<success_criteria>
1. All 8 test scenarios pass
2. AI suggestions are thematically relevant to the sermon context
3. No broken states when AI is unavailable
4. Existing non-AI functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-assisted-service-suggesting-and-scripture-searching/06-04-SUMMARY.md`
</output>
