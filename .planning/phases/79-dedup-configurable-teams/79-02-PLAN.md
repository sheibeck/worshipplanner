---
phase: 79-dedup-configurable-teams
plan: 02
type: execute
wave: 2
depends_on: [79-01]
files_modified:
  - src/components/TeamsConfigPanel.vue
  - src/components/__tests__/TeamsConfigPanel.test.ts
  - src/views/RosterView.vue
autonomous: true
requirements: [R228, R230]
must_haves:
  truths:
    - "A 'Teams' tab appears in RosterView's tab bar next to Volunteers and Roles and mounts the Teams editor"
    - "An admin can add, rename, and delete a team with an explicit Save (not autosave) and an inline (not modal) delete-confirmation carrying the soft-warn copy"
    - "Each team row and the Add-Team row expose a song-tag filter <select> populated from the org's song tags, defaulting to 'No filter'"
    - "Every team name input and song-tag select has a real aria-label (accessible from the start)"
    - "An org with no team docs is seeded with the 4 defaults on first load of the Teams tab"
  artifacts:
    - src/components/TeamsConfigPanel.vue
    - src/components/__tests__/TeamsConfigPanel.test.ts
  key_links:
    - "RosterView mounts TeamsConfigPanel under v-show activeTab === 'teams'"
    - "RosterView subscribes teamsStore + songStore and calls seedDefaultTeamsIfEmpty() on the first teams snapshot"
    - "TeamsConfigPanel reads songStore.allUserTags for the filter <option> list and writes via teamsStore.updateTeam/addTeam/deleteTeam"
---

<objective>
Build the Teams editor (R228) and its optional per-team song-tag filter field (R230's authoring half): a new `TeamsConfigPanel.vue` that copies `RolesConfigPanel.vue`'s shape exactly (flat list, draft + explicit Save, inline soft-warn delete-confirm, Add-row) plus a per-row song-tag `<select>`, mounted as a third "Teams" tab in `RosterView.vue` that also subscribes the teams + songs stores and seeds the defaults.

Purpose: Gives a church admin a first-class, accessible-from-the-start surface to define their own team list and attach a song-tag filter — the configuration that drives every service-planning surface (wired in 79-03).
Output: `src/components/TeamsConfigPanel.vue`, `src/components/__tests__/TeamsConfigPanel.test.ts`, and edits to `src/views/RosterView.vue`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-dedup-configurable-teams/79-CONTEXT.md
@.planning/phases/79-dedup-configurable-teams/79-UI-SPEC.md
@.planning/phases/79-dedup-configurable-teams/79-01-SUMMARY.md

# Copy target (shape EXACTLY) + mount host + store API + song-tag source:
@src/components/RolesConfigPanel.vue
@src/views/RosterView.vue
@src/stores/teams.ts
@src/types/team.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TeamsConfigPanel.vue + component test</name>
  <files>src/components/TeamsConfigPanel.vue, src/components/__tests__/TeamsConfigPanel.test.ts</files>
  <behavior>
    - Renders one editable row per team in `teamsStore.teams`, ordered; each row has a name text input (draft) and a song-tag select whose value is the team's songFilterTag ('' shows "No filter").
    - Editing a name then clicking "Save Team" calls `teamsStore.updateTeam(id, { name, songFilterTag })` — NOT on every keystroke (draft + explicit save, no autosave).
    - The song-tag select lists one `<option value="">No filter</option>` plus one option per `songStore.allUserTags` entry; with zero song tags it still renders (only "No filter"), never disabled.
    - Clicking a row Delete reveals an inline confirm panel with the R228 soft-warn copy; confirming calls `teamsStore.deleteTeam(id)`; Cancel dismisses without deleting.
    - The Add-Team row calls `teamsStore.addTeam({ name, order: maxOrder+1, songFilterTag })` with a name and clears afterward.
    - Every name input and song-tag select carries a non-empty aria-label.
  </behavior>
  <action>
    Create `src/components/TeamsConfigPanel.vue` by copying `src/components/RolesConfigPanel.vue`'s outer structure and script patterns, adapting per 79-UI-SPEC.md "Implementation Notes §1" (authoritative):

    - Outer container / header / row list classes copied verbatim: `rounded-lg border border-gray-800 overflow-hidden`; header `px-4 py-3 bg-gray-900/50 border-b border-gray-800` with `<h2 class="text-sm font-medium text-gray-300">Teams</h2>` and the sub-copy `<p class="text-xs text-gray-500 mt-0.5">` reading exactly the 79-UI-SPEC panel sub-heading; `divide-y divide-gray-800` row list; `px-4 py-4` per-row padding.
    - NO group-badge section (teams are a single flat list — drop roster's `groupOrder`/`groupBadgeClasses`/`rowsForGroup` grouping entirely). Iterate `teamsStore.teams` directly.
    - Drafts: mirror RolesConfigPanel's `watch(() => rosterStore.roles, ...)` immediate+deep drafts pattern, but the draft shape is `{ name: string; songFilterTag: string }` (empty string = No filter). Save/Saving…/Saved ✓ transient (emerald) and the delete-confirm/Add-row transient (Added ✓) all reproduced from RolesConfigPanel.
    - Row layout `flex items-center gap-3`: (1) name text input `flex-1` with the exact Roles input classes; (2) song-tag `<select>` width `w-40 sm:w-48` with the Roles input chrome (`rounded-md bg-gray-800 border border-gray-700 text-gray-100 text-sm px-3 py-1.5 focus:outline-none focus:ring-1 focus:ring-indigo-500`), options = one empty-value "No filter" option plus `v-for` over `songStore.allUserTags`; (3) "Save Team" button (indigo→emerald transient); (4) Delete link `text-xs text-red-400 hover:text-red-300`.
    - Inline delete-confirm panel: `bg-red-900/20 border border-red-800 p-3` with the exact R228 soft-warn copy from 79-UI-SPEC Copywriting Contract, buttons "Delete Team" (red) / "Cancel" (gray). Soft-warn only — NOT a hard block.
    - Add-Team row at the bottom: `<h3 class="text-xs font-medium text-gray-400 uppercase tracking-wider mb-3">Add Team</h3>` + name input (placeholder "Team name") + the same song-tag select + a "Save Team"/"Added ✓" button. On add, compute `order` as `max(existing order) + 1` (mirror RolesConfigPanel's `onAddRole`).
    - Empty state: when `teamsStore.teams.length === 0`, show "No teams yet." / "Add your first team below." (`text-xs text-gray-600`) above the always-visible Add-Team row.
    - Accessibility (the one deliberate deviation from copying Roles verbatim, per CONTEXT.md): bind a real `:aria-label` to each row name input (`Team name for {team.name}`) and song-tag select (`Song-tag filter for {team.name}`); the Add-row inputs get `aria-label="New team name"` and `aria-label="Song-tag filter for new team"`. Do NOT restructure the single-line flex row into a stacked label layout — aria-label preserves the copied visual shape.
    - Imports: `useTeamsStore` from `@/stores/teams`, `useSongStore` from `@/stores/songs`, `Team` from `@/types/team`.

    Create `src/components/__tests__/TeamsConfigPanel.test.ts`: install a real Pinia (`setActivePinia(createPinia())`) and mock `@/stores/teams` + `@/stores/songs` (mock `teams` with two sample teams incl. one with a songFilterTag, mock `allUserTags` with a couple of tags, and spy `addTeam`/`updateTeam`/`deleteTeam`). Cover the behavior cases above: row count, save calls updateTeam with the draft (not on keystroke), select renders "No filter" + one option per tag and still renders with zero tags, delete-confirm gates deleteTeam, Add-row calls addTeam, and every name input + select has a non-empty aria-label.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/TeamsConfigPanel.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>TeamsConfigPanel renders the store-driven editable list with the song-tag select, explicit Save, soft-warn delete-confirm, Add row, aria-labels; its test is green; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the Teams tab in RosterView and seed on load</name>
  <files>src/views/RosterView.vue</files>
  <action>
    In `src/views/RosterView.vue`:
    - Add a third tab button "Teams" to the tab bar (after the "Roles" button ~line 47-56), reusing the identical active/inactive tab-button classes already there.
    - Widen the `activeTab` ref union type to include `'teams'` (currently `ref<'volunteers' | 'roles'>` ~line 452).
    - Add a `<div v-show="activeTab === 'teams'">` block (next to the Roles config block ~line 233-236) mounting `<TeamsConfigPanel />`; import it from `@/components/TeamsConfigPanel.vue` next to the `RolesConfigPanel` import (~line 441).
    - Extend `initStore()` (~line 669-685) to also `teamsStore.subscribe(orgId)` and `songStore.subscribe(orgId)` (the song-tag select needs `songStore.allUserTags` populated), and seed teams with a first-snapshot watch mirroring the existing roles seed-watch: `watch(() => teamsStore.teams, () => { teamsStore.seedDefaultTeamsIfEmpty(); stop() }, ...)` — same guard/timing rationale as the roles seed (wait for the first snapshot so an org that already has teams is never double-seeded). Import `useTeamsStore` and `useSongStore` and instantiate them alongside `rosterStore`.
    - In the unmount teardown (~line 701 where `rosterStore.unsubscribeAll()` is called), also stop the teams seed-watch if still pending; do NOT call `teamsStore.unsubscribeAll()` here if songStore/teamsStore are shared — follow the existing RosterView pattern (it calls `rosterStore.unsubscribeAll()` on unmount, so mirror that for teams; leave songStore subscription management consistent with how the app already treats shared org-scoped stores via resetOrgScopedStores on church switch).
    Keep the change additive — the Volunteers and Roles tabs and their existing tests must be unaffected.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
  </verify>
  <done>RosterView shows a "Teams" tab that mounts TeamsConfigPanel; teams + songs stores are subscribed and defaults seeded on first load; full suite stays at the 2-file known-failing baseline (no NEW failures); type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → Firestore | An org editor writes team docs (name + songFilterTag) via the Teams editor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Information Disclosure / Tampering | `organizations/{orgId}/teams` writes from the editor | medium | mitigate | Reuse the existing `isOrgEditor(orgId)` wildcard (`firestore.rules:458`) — editor-only read/write, no new rule (RESEARCH V4). Client-only phase, no deploy. |
| T-79-02 | Tampering (Input Validation) | `songFilterTag` select value | low | accept | The editor writes only a `<select>` value constrained to `songStore.allUserTags` plus '' (No filter); worst case of a hand-forged tag is an empty AI pool — no exposure/escalation (RESEARCH V5). |
| T-79-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/TeamsConfigPanel.test.ts` green.
- `npx vitest run` stays at the 2-file known-failing baseline (`storage.rules.test.ts`, `RosterView.test.ts`) — the pre-existing `RosterView.test.ts` stale-assertion failure must not multiply into new failures.
- `npm run type-check` (vue-tsc --build) clean.
- Manual (deferred to /gsd-verify-work): Volunteers → Teams renders with visual parity to the Roles panel; add/rename/delete round-trips.
</verification>

<success_criteria>
- A "Teams" tab exists in RosterView and mounts an editable, accessible teams list with a per-team song-tag filter select.
- Explicit Save, inline soft-warn delete-confirm, and Add-row all behave like the Roles panel.
- Teams defaults are seeded on first load; song tags populate the filter select.
</success_criteria>

<output>
Create `.planning/phases/79-dedup-configurable-teams/79-02-SUMMARY.md` when done.
</output>
