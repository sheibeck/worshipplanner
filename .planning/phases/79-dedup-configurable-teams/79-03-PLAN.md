---
phase: 79-dedup-configurable-teams
plan: 03
type: execute
wave: 2
depends_on: [79-01]
files_modified:
  - src/components/NewServiceDialog.vue
  - src/components/__tests__/NewServiceDialog.test.ts
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
  - src/views/ServicesView.vue
autonomous: true
requirements: [R229, R230, R231, R241]
must_haves:
  truths:
    - "Both the new-service dialog and the service editor render one team checkbox per configured team from the shared teams store, not a hard-coded 4-item array"
    - "Zero configured teams shows the empty-state hint in place of the checkbox row on both surfaces"
    - "Creating a new service starts with no teams pre-selected regardless of the chosen Sunday's ordinal position"
    - "Selecting a team carrying a songFilterTag constrains AI song suggestions to that tag; multiple filtered teams union (OR); zero filtered teams uses the full pool"
    - "No hard-coded team-list literal and no duplicated Orchestra-only filter block remains in either consumer"
  artifacts:
    - src/components/NewServiceDialog.vue
    - src/views/ServiceEditorView.vue
    - src/views/ServicesView.vue
  key_links:
    - "NewServiceDialog + ServiceEditorView iterate teamsStore.teams (name/id), reading the single shared source"
    - "ServiceEditorView + ServicesView subscribe + seed teams (editor-guarded) so the checkbox row is populated"
    - "One filterSongsByTeamTags helper (union of selected teams' songFilterTag) is called from both suggestAllSongs and fetchAiForSlot"
---

<objective>
Rewire the consuming surfaces onto the shared teams store and finish the de-dup: repoint the service-plan team checkboxes in both `NewServiceDialog.vue` and `ServiceEditorView.vue` to `teamsStore.teams` (R229/R241), delete the ordinal-Sunday auto-selection so a new service starts with no teams pre-selected (R231), and replace the two duplicated Orchestra-only AI-filter blocks with one generic union-of-selected-team-tags helper (R230/R241). Update the two existing test files that this repoint would otherwise break.

Purpose: This is the payoff of Phase 79 — the hard-coded, Berean-specific team rules are gone; every team-driven surface reads from the org's own configuration. R241's single-source de-dup is the prerequisite that this plan completes, so it is sequenced with 79-01 (store) as its only dependency.
Output: edits to `NewServiceDialog.vue`, `ServiceEditorView.vue`, `ServicesView.vue`, and their test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-dedup-configurable-teams/79-CONTEXT.md
@.planning/phases/79-dedup-configurable-teams/79-UI-SPEC.md
@.planning/phases/79-dedup-configurable-teams/79-RESEARCH.md
@.planning/phases/79-dedup-configurable-teams/79-01-SUMMARY.md

# Store API + the exact consumer sites to rewire:
@src/stores/teams.ts
@src/components/NewServiceDialog.vue
@src/views/ServicesView.vue

<!-- planner-discipline-allow: AVAILABLE_TEAMS -->
<!-- planner-discipline-allow: availableTeams -->
<!-- planner-discipline-allow: isOrchestraService -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NewServiceDialog — store-driven checkboxes, empty default, test rewrite (R229, R231, R241)</name>
  <files>src/components/NewServiceDialog.vue, src/components/__tests__/NewServiceDialog.test.ts</files>
  <behavior>
    - The teams checkbox row renders one pill per `teamsStore.teams` entry (by name), not a fixed four; toggling still writes into `form.teams`.
    - With zero configured teams, the checkbox row is replaced by the empty-state hint copy from 79-UI-SPEC ("No teams configured — add teams in Volunteers → Teams.").
    - Opening the dialog on ANY Sunday (1st, 3rd, or otherwise) initializes `form.teams` to `[]` — no ordinal-based pre-selection.
    - The 'Special' free-text service-name field still appears when 'Special' is among the checked teams (literal-string coupling left unchanged this phase, per 79-UI-SPEC note).
  </behavior>
  <action>
    In `src/components/NewServiceDialog.vue`:
    - Import `useTeamsStore` from `@/stores/teams` and instantiate it in setup. Change the teams `v-for` (~line 79) from the local `availableTeams` array to `teamsStore.teams`, keying on `team.id` and using `team.name` for `:value`, the `form.teams.includes(...)` check, and the label text.
    - Delete the local `availableTeams` constant (~line 145).
    - Wrap the checkbox row so that when `teamsStore.teams.length === 0` the row is replaced by the empty-state hint (`text-xs text-gray-500`, exact 79-UI-SPEC copy).
    - R231 — remove the ordinal auto-selection entirely: delete the `sundayOrdinal()` function (~line 148), delete the ordinal branch inside `defaultForm()` so it returns `{ date, name: '', teams: [] }` unconditionally (~line 170-174), and delete the team-mutation inside the date-change `watch` (~line 190-202) — the watcher may be removed if nothing else remains in it. Delete the whole `sundayOrdinal` function, not just its callers (RESEARCH anti-pattern: no dead code). Do NOT leave the removed identifiers in comments.

    In `src/components/__tests__/NewServiceDialog.test.ts` (RESEARCH Pitfalls 2 & 5):
    - The file currently mounts with NO Pinia and NO store mocks; a component now calling `useTeamsStore()` throws without an active Pinia. Add a Pinia install (`setActivePinia(createPinia())` in a beforeEach) and `vi.mock('@/stores/teams', ...)` returning `teams` seeded to the 4 default team objects (Choir/Orchestra/Communion/Special) so the existing date/checkbox assertions still find teams. Mirror the mock style already used in `ServiceEditorView.test.ts`.
    - REWRITE (do not delete) the "NewServiceDialog default teams follow the chosen Sunday (R038 side effect)" describe block (~line 118-172): replace its ordinal assertions with the R231 behavior — `form.teams` is `[]` on create regardless of which Sunday is chosen (5th, 1st, 3rd), and no checkbox is checked on open. Keep the date-default (R038) describe block passing unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/NewServiceDialog.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>The dialog renders store-driven checkboxes with an empty-state fallback, always starts with `form.teams === []`, and the test file mounts under Pinia with the rewritten ordinal-removal block green; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: ServiceEditorView + ServicesView — store-driven checkboxes, subscribe/seed, teams mock (R229, R241)</name>
  <files>src/views/ServiceEditorView.vue, src/views/ServicesView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
    In `src/views/ServiceEditorView.vue`:
    - Import `useTeamsStore` from `@/stores/teams` and instantiate it next to the other store instances (~line 1653).
    - Repoint the editor-branch checkbox `v-for` (~line 765): iterate `teamsStore.teams` keyed on `team.id`, using `team.name` for the `:checked="localService.teams.includes(...)"` test, the `@change="toggleTeam(...)"` call, and the label text. Leave the read-only viewer branch (~line 786, iterates `localService.teams` directly) and the 'Special' free-text field (~line 778) unchanged.

    <!-- planner-discipline-allow: AVAILABLE_TEAMS -->
    - Delete the `AVAILABLE_TEAMS` constant (~line 1675). Do NOT leave the identifier in a comment.
    - When `teamsStore.teams.length === 0`, render the 79-UI-SPEC empty-state hint in place of the checkbox row (same copy as the dialog).
    - In `initStores()` (~line 2715-2736), inside the existing `if (authStore.isEditor)` block, add a guarded `if (!teamsStore.orgId) teamsStore.subscribe(orgId)` and a first-snapshot seed-watch calling `teamsStore.seedDefaultTeamsIfEmpty()` (mirror the roles seed-watch pattern; editor-guarded because seeding writes to Firestore and reads require isOrgEditor). Ensure the existing `watch(() => authStore.isEditor, ...)` re-run path also covers teams via the same idempotent `!teamsStore.orgId` guard.

    In `src/views/ServicesView.vue` (this view mounts NewServiceDialog, so the dialog's checkbox row needs team data): in `initStore()` (~line 362-366), when the user is an editor (`authStore.isEditor` — import `useAuthStore` if not already present), `teamsStore.subscribe(orgId)` and seed via the same first-snapshot seed-watch. Keep it additive to the existing `serviceStore.subscribe(orgId)`.

    In `src/views/__tests__/ServiceEditorView.test.ts` (RESEARCH Pitfall 6): add `vi.mock('@/stores/teams', ...)` alongside the existing store mocks (~line 601-617), returning `teams` seeded to the 4 default team objects and a `subscribe` spy, so the existing "teams row … Orchestra" test (~line 3148-3158) still finds the `Orchestra` label after the repoint. Add a `seedDefaultTeamsIfEmpty` no-op and `orgId` to the mock so `initStores()` runs clean.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>The service editor's checkbox row is store-driven with an empty-state fallback, teams are subscribed+seeded on both the editor and services-list surfaces (editor-guarded), the teams mock keeps the existing Orchestra-label test green; type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generic union-of-team-tags AI filter helper (R230, R241)</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <behavior>
    - A service selecting one team whose songFilterTag is set constrains the AI candidate pool to songs carrying that tag (preserves the legacy single-Orchestra case exactly when Orchestra's songFilterTag = 'Orchestra').
    - Two selected teams each with a filter tag → the pool is the UNION (OR) of songs matching either tag, never the intersection (CONTEXT.md locks union to avoid an empty pool).
    - Zero selected teams with a filter tag → the full candidate pool is used (today's non-Orchestra behavior preserved).
  </behavior>
  <action>
    In `src/views/ServiceEditorView.vue`, add a single helper (e.g. `filterSongsByTeamTags(base, selectedTeamNames)`) that builds a `Set<string>` of the `songFilterTag` values of every selected team present in `teamsStore.teams` (skipping empty/undefined tags), returns `base` unchanged when the set is empty, otherwise returns `base.filter(s => s.tags.some(t => activeTags.has(t)))`.

    <!-- planner-discipline-allow: isOrchestraService -->
    Replace BOTH duplicated Orchestra-only filter blocks — the one in `suggestAllSongs` (~line 3424-3430) and the one in `fetchAiForSlot` (~line 3536-3541) — so each computes `librarySource` by calling the shared helper with `songStore.aiCandidateSongs` and `localService.value?.teams ?? []`. Remove the two inline `isOrchestraService` locals and their ternaries entirely; there must be exactly one place that reads team filter tags. Do NOT leave the removed identifier in a comment (the completion grep asserts it is gone).

    In `src/views/__tests__/ServiceEditorView.test.ts`: add a `describe` block (e.g. tagged "song-tag filter") exercising the three behavior cases. Drive it by setting the `@/stores/teams` mock's `teams` so one team has `songFilterTag` set (single-tag = legacy Orchestra case), two teams have distinct tags (assert union), and none have tags (assert full pool). Assert against the `songLibrary` handed to the AI-suggestion call (mock `getSongSuggestions` and inspect its argument, following the file's existing AI-suggestion test pattern).
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts -t "song-tag filter"</automated>
    <automated>npm run type-check</automated>
    <automated>grep -c "isOrchestraService" src/views/ServiceEditorView.vue</automated>
    <automated>grep -rn "AVAILABLE_TEAMS\|availableTeams = \['Choir'" src/</automated>
  </verify>
  <done>One shared helper unions selected teams' filter tags and drives both AI-suggestion call sites; the song-tag-filter tests are green; `grep -c "isOrchestraService" src/views/ServiceEditorView.vue` returns 0 and the hard-coded team-list grep returns no matches (R241 de-dup complete); type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → Firestore | The editor reads the per-org teams subcollection to render checkboxes and filter AI candidates; a planner's team selection is persisted on the service doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Information Disclosure / Tampering | teams reads on the editor/services surfaces | medium | mitigate | Reads gated by the existing `isOrgEditor(orgId)` wildcard (`firestore.rules:458`); teams subscribe is placed inside the editor-only guard so a non-editor viewer never subscribes (mirrors the existing roster/quarters guard at ServiceEditorView `initStores()`). No new rule; client-only. |
| T-79-02 | Tampering | `songFilterTag` consumed by the AI filter | low | accept | The filter is a client-side narrowing of the candidate pool before the AI proxy; a forged tag only shrinks/empties the pool — the AI proxy itself is untouched and enforces its own controls (RESEARCH V5, Phase 65 caps). |
| T-79-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/NewServiceDialog.test.ts src/views/__tests__/ServiceEditorView.test.ts` green.
- R241 de-dup complete: `grep -c "isOrchestraService" src/views/ServiceEditorView.vue` returns 0; `grep -rn "AVAILABLE_TEAMS\|availableTeams = \['Choir'" src/` returns no matches.
- `npx vitest run` stays at the 2-file known-failing baseline (`storage.rules.test.ts`, `RosterView.test.ts`).
- `npm run type-check` (vue-tsc --build) clean.
- Manual (deferred to /gsd-verify-work): two orgs with different team lists see two different service-plan checkbox rows.
</verification>

<success_criteria>
- Both service-plan surfaces render checkboxes from the org's configured teams, with an empty-state fallback.
- A new service starts with no teams pre-selected on any Sunday.
- Selecting filtered teams constrains AI suggestions by the union of their song tags.
- No hard-coded team-list constant or duplicated Orchestra-filter block remains.
</success_criteria>

<output>
Create `.planning/phases/79-dedup-configurable-teams/79-03-SUMMARY.md` when done.
</output>
