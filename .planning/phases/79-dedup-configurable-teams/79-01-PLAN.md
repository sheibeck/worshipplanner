---
phase: 79-dedup-configurable-teams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/team.ts
  - src/stores/teams.ts
  - src/stores/__tests__/teams.test.ts
  - src/stores/orgScopedStores.ts
autonomous: true
requirements: [R228, R241]
must_haves:
  truths:
    - "An org with zero team docs resolves to exactly Choir, Orchestra, Communion, Special (order 0-3, no songFilterTag) after seedDefaultTeamsIfEmpty()"
    - "Calling seedDefaultTeamsIfEmpty() when the org already has teams writes nothing (idempotent, never clobbers)"
    - "addTeam / updateTeam / deleteTeam perform the matching Firestore add/update/delete on organizations/{orgId}/teams"
    - "The teams store is torn down on church switch (its unsubscribeAll runs from resetOrgScopedStores)"
  artifacts:
    - src/types/team.ts
    - src/stores/teams.ts
    - src/stores/__tests__/teams.test.ts
  key_links:
    - "teams store subscribes organizations/{orgId}/teams ordered by 'order'"
    - "useTeamsStore().unsubscribeAll() is called inside resetOrgScopedStores()"
---

<objective>
Create the single shared teams source that the whole phase depends on: a `Team` type + `DEFAULT_TEAMS` seed, a `useTeamsStore()` Pinia store mirroring `roster.ts`'s roles half exactly (per-org `teams` subcollection CRUD + idempotent seed), and its registration in the org-switch teardown. This store is the one source that R241 collapses the duplicated team-list constants onto, and the data layer behind R228's configurable team list.

Purpose: Everything else in Phase 79 (the editor panel in 79-02, the consumer rewiring in 79-03) reads from this store. It must exist and be church-switch-safe before any consumer is repointed, so nothing is ever half-wired.
Output: `src/types/team.ts`, `src/stores/teams.ts`, `src/stores/__tests__/teams.test.ts`, and a one-line edit to `src/stores/orgScopedStores.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-dedup-configurable-teams/79-CONTEXT.md
@.planning/phases/79-dedup-configurable-teams/79-RESEARCH.md

# Direct templates — copy the roles half of roster, NOT the people half:
@src/stores/roster.ts
@src/types/roster.ts
@src/stores/__tests__/roster.test.ts
@src/stores/orgScopedStores.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Team type, teams store, and its unit tests</name>
  <files>src/types/team.ts, src/stores/teams.ts, src/stores/__tests__/teams.test.ts</files>
  <behavior>
    - seedDefaultTeamsIfEmpty() on an org whose teams array is empty writes exactly 4 docs: {name:'Choir',order:0}, {name:'Orchestra',order:1}, {name:'Communion',order:2}, {name:'Special',order:3} — no songFilterTag on any (R228, RESEARCH Pitfall 4: byte-match today's list so Berean is unchanged).
    - seedDefaultTeamsIfEmpty() when teams.value.length !== 0 writes nothing (guarded, idempotent — no double-seed, never clobbers an org that already edited its list).
    - subscribe(orgId) opens an onSnapshot on organizations/{orgId}/teams ordered by 'order' and maps docs to Team objects; unsubscribeAll() tears the listener down and clears state.
    - addTeam({name, order, songFilterTag?}) calls addDoc on the teams collection; updateTeam(id, patch) calls updateDoc on the team doc (patch may set songFilterTag to '' meaning "no filter"); deleteTeam(id) calls deleteDoc on the team doc.
  </behavior>
  <action>
    Create `src/types/team.ts` exporting `interface Team { id: string; name: string; order: number; songFilterTag?: string }` and `const DEFAULT_TEAMS: Array<Omit<Team,'id'>>` = the four teams listed in the behavior block (Choir/Orchestra/Communion/Special, order 0..3, NO songFilterTag — CONTEXT.md locks "seeding the tag is optional and left to the admin"). Mirror the doc-comment style of `src/types/roster.ts`'s `Role`/`DEFAULT_ROLES`.

    Create `src/stores/teams.ts` as `defineStore('teams', ...)` by copying ONLY the roles half of `src/stores/roster.ts` and renaming role→team: a `teams = ref<Team[]>([])`, `orgId = ref<string|null>(null)`, one `Unsubscribe` handle, `subscribe(orgId)` querying `collection(db,'organizations',orgId,'teams')` with `orderBy('order')` and mapping snapshot docs to `Team` (drop roster's one-time 'vocals' migration block — teams have no equivalent), `unsubscribeAll()`, `seedDefaultTeamsIfEmpty()` (guard `if (teams.value.length !== 0) return`, then `addDoc` each DEFAULT_TEAMS entry with `createdAt`/`updatedAt: serverTimestamp()` exactly like `seedDefaultRolesIfEmpty`), and `addTeam` / `updateTeam` / `deleteTeam` copied from `addRole`/`updateRole`/`deleteRole`. Expose the same public surface names the consumers will use: `teams`, `subscribe`, `unsubscribeAll`, `seedDefaultTeamsIfEmpty`, `addTeam`, `updateTeam`, `deleteTeam`. The ordered array is `teams` (already order-sorted by the query) — consumers iterate `teamsStore.teams` and read `.name`/`.id`, mirroring how `roster.roles` is the order-sorted array. Do NOT add a people-half, no `isLoading` unless the mirror needs it, no grouping.

    Create `src/stores/__tests__/teams.test.ts` by copying the firebase/firestore + `@/firebase` mock scaffold and the snapshot-callback harness from `src/stores/__tests__/roster.test.ts` (top of file), then write the four behavior cases above. Trigger a snapshot with an empty docs array before calling `seedDefaultTeamsIfEmpty()` to assert 4 addDoc calls; trigger a snapshot with existing team docs to assert seed writes nothing; assert addTeam/updateTeam/deleteTeam call addDoc/updateDoc/deleteDoc.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/teams.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>teams.test.ts is green; the store seeds exactly the 4 defaults only when empty, never re-seeds, and CRUD maps to the right Firestore ops; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Register teams store in the church-switch teardown</name>
  <files>src/stores/orgScopedStores.ts</files>
  <action>
    In `src/stores/orgScopedStores.ts`, import `useTeamsStore` from `./teams` alongside the other store imports, and add `useTeamsStore().unsubscribeAll()` inside `resetOrgScopedStores()` next to `useRosterStore().unsubscribeAll()`. This is RESEARCH Pitfall 3 / Runtime State Inventory: a store created after commit 11064ac5 is invisible to the manual enumeration until added, and omitting it reintroduces the org-switch stale-team-flash bug that commit fixed for every other store. Keep the teardown null-guarded semantics identical to the siblings (the store's `unsubscribeAll` already no-ops when no listener is active).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>grep -c "useTeamsStore" src/stores/orgScopedStores.ts</automated>
  </verify>
  <done>`resetOrgScopedStores()` calls `useTeamsStore().unsubscribeAll()`; grep returns >=1 (import + call); type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → Firestore | An org member's client reads/writes the new `organizations/{orgId}/teams` subcollection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Information Disclosure / Tampering | `organizations/{orgId}/teams` subcollection | medium | mitigate | Reuse the existing generic per-org wildcard `match /{collection}/{docId}` at `firestore.rules:458` — it gates read+write to `isOrgEditor(orgId)` and does NOT list `teams` in its write-exclusion set, so `teams` falls through exactly like `roles`/`people`/`quarters` (RESEARCH V4, verified by direct rule read). No new rule, no deploy — this phase is client-only. |
| T-79-02 | Tampering | `songFilterTag` field on a team doc | low | accept | Field is UI-constrained to a `<select>` (79-02); a direct-Firestore-write arbitrary tag only yields a filter matching zero songs (empty AI pool) — no data exposure, no escalation. Not worth a rules-level enum this phase. |
| T-79-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: not applicable) — nothing to install or vet. |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/teams.test.ts` green.
- `npx vitest run` stays at the 2-file known-failing baseline (`storage.rules.test.ts`, `RosterView.test.ts`) — nothing new red.
- `npm run type-check` (vue-tsc --build) clean.
</verification>

<success_criteria>
- `Team` type + `DEFAULT_TEAMS` (exact 4-team byte-match) exist.
- `useTeamsStore()` seeds idempotently, never clobbers, and does per-org CRUD on the `teams` subcollection.
- `resetOrgScopedStores()` tears the teams listener down on church switch.
</success_criteria>

<output>
Create `.planning/phases/79-dedup-configurable-teams/79-01-SUMMARY.md` when done.
</output>
