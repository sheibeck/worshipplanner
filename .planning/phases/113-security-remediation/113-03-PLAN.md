---
phase: 113-security-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: [R323]
must_haves:
  truths:
    - "ONE consolidated backlog entry (a single `### Phase 999.x` heading) records all 11 Medium/Low Phase 112 security findings"
    - "The 11 findings are SEC-A-01, ARCH-018, SEC-R-03, SEC-S-02, SEC-C-01 (Medium) and SEC-ISO-05, SEC-ISO-06(residual), SEC-S-03, SEC-S-04, SEC-C-05, SEC-C-06 (Low)"
    - "The entry references 112-SECURITY-REVIEW.md as the durable detail source (no per-finding detail duplicated, no stubs)"
    - "No Medium/Low finding is fixed in code by this plan (triage only)"
    - "The 999.x number is derived at execute time via `phase.next-decimal 999` (not hard-coded)"
    - "No firebase deploy or gcloud command is run by this plan"
  artifacts:
    - .planning/ROADMAP.md
  key_links:
    - "new `### Phase 999.x` backlog heading <-> .planning/phases/112-security-review/112-SECURITY-REVIEW.md `## Medium/Low (→ backlog)` section"
---

<objective>
Triage the 11 Medium/Low security findings from the Phase 112 report into ONE consolidated backlog
entry (mirroring the existing Phase 999.4 architectural-backlog pattern), satisfying R323's "explicitly
deferred to a backlog with recorded rationale" clause for Medium/Low. No stubs, no per-finding phases,
and NONE of the Medium/Low findings are fixed in code here.

The 11 findings (from `112-SECURITY-REVIEW.md`'s `## Medium/Low (→ backlog)` section):
- **5 Medium:** SEC-A-01 (`/api/planningcenter` unauthenticated), ARCH-018 (super-admin universal
  `isOrgEditor` grant), SEC-R-03 (draft-service `createdBy` forgery), SEC-S-02 (deterministic
  memorable-URL share ids), SEC-C-01 (ESV/NLT not rate-limited).
- **6 Low:** SEC-ISO-05 (`admin`≡`editor` role), SEC-ISO-06 residual (`orgSlugs`/`orgNames` also
  listable), SEC-S-03 (share links never expire), SEC-S-04 (free-text notes unfiltered PII),
  SEC-C-05 (`queueServiceMessage` no enqueue-rate limit), SEC-C-06 (`parsePptx` no daily import quota).

(The confirmed-sound / no-action rows — SEC-A-02, ARCH-005, SEC-S-05, SEC-C-02/03/04 — are NOT part of
the 11; they need no backlog action.)

Purpose: nothing found by the Phase 112 review is silently dropped; Medium/Low have a durable pointer.
Output: one new `### Phase 999.x` section in `.planning/ROADMAP.md`, committed, UNDEPLOYED.

**This plan performs NO deploy** and touches NO source/rules/functions code — it edits
`.planning/ROADMAP.md` only. No `firebase deploy` / `gcloud`.
</objective>

## Artifacts this phase produces (this plan)

- `.planning/ROADMAP.md` — one new `### Phase 999.x` backlog entry consolidating the 11 Medium/Low
  security findings, pointing to `112-SECURITY-REVIEW.md` for full per-finding detail.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/113-security-remediation/113-CONTEXT.md
@.planning/phases/112-security-review/112-SECURITY-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ONE consolidated 999.x Medium/Low security-backlog entry to ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/112-security-review/112-SECURITY-REVIEW.md` — the `## Medium/Low (→ backlog)`
      section (SEC-A-01, ARCH-018, SEC-R-03, SEC-S-02, SEC-C-01, SEC-ISO-05, SEC-ISO-06 residual,
      SEC-S-03, SEC-S-04, SEC-C-05, SEC-C-06) plus the summary-table dispositions.
    - `.planning/ROADMAP.md` `## Backlog` section — the existing `### Phase 999.4` entry (~:749) as the
      exact structural pattern to mirror (Goal / Requirements / Plans / a Source pointer / a short
      per-tier finding list / a `Promote with /gsd-review-backlog when ready.` line), and the existing
      `### Phase 999.2` / `### Phase 999.3` headings.
    - `.planning/phases/113-security-remediation/113-CONTEXT.md` — the Medium/Low triage decision (ONE
      consolidated entry; number via `phase.next-decimal 999`; no stubs; none fixed).
  </read_first>
  <action>
    Determine the next backlog number at execute time by running the launcher's
    `query phase.next-decimal 999` (do NOT hard-code — 999.4 already exists, so it will return 999.5 or
    higher; use whatever the tool returns). Then add ONE new `### Phase 999.x: v2.8 Security Review —
    Medium/Low findings (11) (BACKLOG)` section under `.planning/ROADMAP.md`'s `## Backlog`, mirroring
    the Phase 999.4 entry's shape. Include: a one-line Goal noting it consolidates the 11 Medium/Low
    security findings deferred from Phase 113 per 113-CONTEXT.md's locked triage; `Requirements: TBD`;
    `Plans: 0 plans`; a Source line pointing to
    `.planning/phases/112-security-review/112-SECURITY-REVIEW.md`'s `## Medium/Low (→ backlog)` section
    as the durable per-finding detail (state that this entry does not duplicate that detail); a compact
    two-tier list naming all 11 by id with a few-word descriptor each (5 Medium, 6 Low, exactly as
    enumerated in this plan's objective); and a closing `Promote with /gsd-review-backlog when ready.`
    line. Do NOT create 11 separate stubs, do NOT add per-finding phases, and do NOT modify any source,
    rules, or functions file — this is a ROADMAP-only triage edit. Use the `Edit` tool to insert the new
    section (scoped insertion into `## Backlog`), never a whole-file `Write` of ROADMAP.md.
  </action>
  <verify>
    <automated>grep -c "v2.8 Security Review — Medium/Low findings" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains exactly one new `### Phase 999.x: v2.8 Security Review — Medium/Low
      findings (11) (BACKLOG)` heading (x = the value returned by `phase.next-decimal 999`).
    - All 11 finding ids appear in the entry: SEC-A-01, ARCH-018, SEC-R-03, SEC-S-02, SEC-C-01,
      SEC-ISO-05, SEC-ISO-06, SEC-S-03, SEC-S-04, SEC-C-05, SEC-C-06.
    - The entry references `112-SECURITY-REVIEW.md` and duplicates no per-finding detail (pointer only).
    - No stubs and no per-finding phases were created; no source/rules/functions file was modified.
    - No `firebase deploy` and no `gcloud` command was run by this plan.
  </acceptance_criteria>
  <done>One consolidated 999.x Medium/Low security-backlog entry exists in ROADMAP.md, referencing the Phase 112 report; nothing fixed in code, nothing deployed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This plan edits `.planning/ROADMAP.md` only — a planning document, not runtime code, rules, or config. No trust boundary is exercised. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-05 | Repudiation | dropped/untracked Medium/Low findings | low | mitigate | One durable consolidated backlog entry pointing to 112-SECURITY-REVIEW.md ensures no finding is silently lost |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs and no code changes in this plan (docs-only) — no supply-chain surface |
</threat_model>

<verification>
- `.planning/ROADMAP.md` has one new `### Phase 999.x` Medium/Low security-backlog entry naming all 11
  findings and pointing to `112-SECURITY-REVIEW.md`.
- No source/rules/functions file modified; no Medium/Low finding fixed in code.
- No `firebase deploy` / `gcloud` executed by this plan.
</verification>

<success_criteria>
- One consolidated Medium/Low security-backlog entry exists (11 findings, pointer to the Phase 112
  report), numbered via `phase.next-decimal 999`.
- No Medium/Low finding fixed in code (triage only).
- No deploy performed by this plan.
</success_criteria>

<output>
Create `.planning/phases/113-security-remediation/113-03-SUMMARY.md` when done.
</output>
