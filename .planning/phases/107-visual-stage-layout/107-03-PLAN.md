---
phase: 107-visual-stage-layout
plan: 03
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - src/stores/services.ts
  - src/stores/__tests__/services.stageLayout.test.ts
  - src/views/ShareView.vue
  - src/views/__tests__/ShareView.test.ts
  - src/components/ServicePrintLayout.vue
  - src/components/__tests__/ServicePrintLayout.test.ts
autonomous: true
requirements: [R314, R315]
must_haves:
  truths:
    - "buildServiceSnapshot() denormalizes the service's stageLayout into the frozen ServiceSnapshot (mirroring the roleAssignments projection), so the public unauthenticated ShareView renders the plot WITHOUT any new org-scoped Firestore/Storage access or new public rules (R315)."
    - "The public ShareView renders the stage plot read-only (light theme) from the snapshot, and OMITS the whole section when the snapshot has no stageLayout or zero markers."
    - "The print layout (@media print) renders a 'Stage Layout' section after Notes, break-inside-avoid, from the live service, and omits it when there are no markers (R315)."
    - "Both read-only surfaces reuse the ONE shared StageLayoutView component — same percentages, same zones, no drag/edit controls (R314 resize-stable render, R315 read-only)."
  artifacts:
    - src/stores/services.ts
    - src/stores/__tests__/services.stageLayout.test.ts
    - src/views/ShareView.vue
    - src/views/__tests__/ShareView.test.ts
    - src/components/ServicePrintLayout.vue
    - src/components/__tests__/ServicePrintLayout.test.ts
  key_links:
    - "The snapshot projection carries ONLY the 6 StageMarker fields (id,label,kind,zone,xPct,yPct) — no personId/PII is resolved (unlike roleAssignments), and nothing new reaches the public page beyond the frozen snapshot."
    - "stageLayout is added to the snapshot via a conditional spread so the key is ABSENT (never undefined) when empty — safe for the Firestore snapshot write and matching the roleAssignments?.length omit pattern already in ShareView."
    - "ShareView consumes serviceSnapshot.stageLayout only (the frozen doc); it never reads a live org-scoped collection — the app's hard public-surface rule."
    - "ServicePrintLayout reads props.service.stageLayout (live, authenticated editor context) — no snapshot needed there and no new access."
---

<objective>
Ship the read-only half of the stage layout: denormalize it into the frozen share snapshot and
render it read-only on the public share page and the print layout — reusing the single
StageLayoutView from Plan 01, with no new public rules and no new org-scoped access (R315).

Depends on 107-01 for the StageMarker type and the shared read-only StageLayoutView. Independent of
Plan 02 (disjoint files) — the two wave-2 plans run in parallel.

Purpose: Make the stage plot viewable read-only wherever the service is shared or printed so
tech/sound can reference the setup, following the app's established "denormalize into the frozen
snapshot, never widen public rules" pattern (the same one roleAssignments uses).
Output: extended ServiceSnapshot + buildServiceSnapshot(), the ShareView stage-plot section, and the
ServicePrintLayout "Stage Layout" section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-visual-stage-layout/107-CONTEXT.md
@.planning/phases/107-visual-stage-layout/107-UI-SPEC.md
@.planning/phases/107-visual-stage-layout/107-01-SUMMARY.md
@src/stores/services.ts
@src/views/ShareView.vue
@src/components/ServicePrintLayout.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Denormalize stageLayout into the frozen ServiceSnapshot</name>
  <files>src/stores/services.ts, src/stores/__tests__/services.stageLayout.test.ts</files>
  <behavior>
    - The ServiceSnapshot interface gains an optional stageLayout?: { elements: StageMarker[] } field.
    - buildServiceSnapshot(service) includes stageLayout in the returned snapshot ONLY when service.stageLayout?.elements?.length is truthy; when the service has no stageLayout or zero markers, the returned object has NO stageLayout key at all (absent, not undefined) — safe for the Firestore snapshot write and matching the roleAssignments?.length omit pattern.
    - When present, each snapshot marker carries exactly the 6 StageMarker fields (id, label, kind when set, zone, xPct, yPct) and nothing else — no personId, no extra data (mirrors the roleAssignments PII-safe projection; here there is no PII to resolve, only planner free text).
    - The projection preserves xPct/yPct verbatim (positions round-trip into the public render unchanged).
  </behavior>
  <action>
In src/stores/services.ts: import StageMarker from '@/types/service' (alongside the existing service-type imports) and add `stageLayout?: { elements: StageMarker[] }` to the `ServiceSnapshot` interface, documented as the read-only public projection (mirrors roleAssignments — published to anyone holding the share URL, so it must carry nothing beyond the marker's own display fields; there is no PII to strip because markers are planner free-text, but keep the projection explicit rather than spreading the raw field so a future non-display field added to StageMarker cannot silently leak). In buildServiceSnapshot(), build the stageLayout projection by mapping service.stageLayout.elements to `{ id, label, kind, zone, xPct, yPct }` and add it to the returned object via a CONDITIONAL SPREAD keyed on `service.stageLayout?.elements?.length` so the key is absent (never undefined) when empty — do NOT unconditionally set `stageLayout: undefined` (Firestore rejects undefined in the snapshot write). Leave the existing roleAssignments/bpm logic untouched. Write src/stores/__tests__/services.stageLayout.test.ts asserting: buildServiceSnapshot includes stageLayout with the mapped markers when the service has markers; the snapshot has no stageLayout key when the service field is absent or has zero markers; each projected marker has exactly the 6 expected keys and no others; xPct/yPct are preserved. Follow the existing buildServiceSnapshot test setup in src/stores/__tests__/services.test.ts (Pinia active, song/roster/quarters stores) — a minimal service with a stageLayout is enough; you do not need roles configured.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.stageLayout.test.ts && npm run type-check</automated>
  </verify>
  <done>ServiceSnapshot carries an optional stageLayout; buildServiceSnapshot projects only the 6 marker fields and omits the key entirely when there are no markers; tests prove present/absent/exact-shape/position-preservation; `npm run type-check` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read-only stage plot on ShareView (public) and ServicePrintLayout (print)</name>
  <files>src/views/ShareView.vue, src/views/__tests__/ShareView.test.ts, src/components/ServicePrintLayout.vue, src/components/__tests__/ServicePrintLayout.test.ts</files>
  <behavior>
    - ShareView renders a "Stage Layout" section (StageLayoutView, theme light) from serviceSnapshot.stageLayout.elements, wrapped in v-if="serviceSnapshot.stageLayout?.elements?.length" so it is OMITTED entirely when the snapshot has no layout or zero markers (matching the existing roleAssignments?.length conditional section).
    - ShareView reads ONLY serviceSnapshot (the frozen doc it already fetched) — no new getDoc, no org-scoped read.
    - A snapshot marker label containing markup renders as literal text on the public page (XSS-safe, inherited from StageLayoutView).
    - ServicePrintLayout renders a "Stage Layout" section AFTER the Notes section, titled "Stage Layout" (text-sm font-semibold text-gray-700, matching the Notes heading), break-inside-avoid, from props.service.stageLayout.elements via StageLayoutView theme light, wrapped in v-if="props.service.stageLayout?.elements?.length" so it is omitted when there are no markers.
    - Both surfaces show the two-zone plot read-only (no drag handles, no edit/delete) — reusing StageLayoutView, not a new renderer.
  </behavior>
  <action>
In src/views/ShareView.vue: import StageLayoutView from '@/components/stage/StageLayoutView.vue' and add a read-only "Stage Layout" section (light-mode container matching ShareView's other sections — rounded-lg bg-gray-50 p-4, text-sm font-semibold text-gray-700 heading) placed after the "Who's Serving" section and before the footer, rendering `<StageLayoutView :elements="serviceSnapshot.stageLayout.elements" theme="light" />` guarded by `v-if="serviceSnapshot.stageLayout?.elements?.length"`. Do NOT add any new getDoc / org-scoped read — consume only the already-fetched serviceSnapshot. In src/components/ServicePrintLayout.vue: import StageLayoutView and add a new "Stage Layout" section immediately AFTER the existing Notes section block, titled "Stage Layout" with the same heading treatment as Notes, wrapped in a `break-inside-avoid` container and `v-if="props.service.stageLayout?.elements?.length"`, rendering `<StageLayoutView :elements="props.service.stageLayout.elements" theme="light" />`. Extend src/views/__tests__/ShareView.test.ts to prove the stage section renders when serviceSnapshot.stageLayout has markers, is omitted when absent/empty, and renders a markup label as literal text. Extend src/components/__tests__/ServicePrintLayout.test.ts to prove the "Stage Layout" section renders (with markers) after Notes and is omitted when props.service has no stageLayout. Follow each file's existing test setup (ShareView.test.ts's serviceSnapshot mock; ServicePrintLayout.test.ts's mockService prop).
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ShareView.test.ts src/components/__tests__/ServicePrintLayout.test.ts && npm run type-check</automated>
  </verify>
  <done>The public ShareView renders the read-only stage plot from the frozen snapshot only and omits it when empty; the print layout renders a break-inside-avoid "Stage Layout" section after Notes and omits it when empty; both reuse StageLayoutView; a markup label renders as text; tests pass; `npm run type-check` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buildServiceSnapshot → public frozen snapshot → unauthenticated ShareView | the denormalized layout is the ONLY path the public page gets it — it must carry nothing beyond display fields |
| print layout → live service (authenticated) | print reads the live org-scoped service in the editor context — no public exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-01 | Information Disclosure | buildServiceSnapshot stageLayout projection | high | mitigate | The projection maps to exactly { id, label, kind, zone, xPct, yPct } — no personId/PII resolved and no raw-field spread; a test asserts the projected marker has only those keys, so a future non-display StageMarker field cannot silently leak to the public page. |
| T-107-03 | Tampering (XSS) | free-text label render on the public ShareView | high | mitigate | Labels rendered via StageLayoutView's text interpolation only; a ShareView test mounts a markup label and asserts literal-text output. |
| T-107-06 | Information Disclosure | new public/org-scoped read for the layout | high | mitigate | ShareView consumes only the already-fetched frozen serviceSnapshot; no new getDoc, no new firestore.rules/storage.rules block — the layout reaches the public page solely via the snapshot projection (the app's hard public-surface rule). |
| T-107-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies this plan; nothing to slopcheck. |
</threat_model>

<verification>
- `npm run type-check` clean (authoritative gate).
- `npx vitest run src/stores/__tests__/services.stageLayout.test.ts src/views/__tests__/ShareView.test.ts src/components/__tests__/ServicePrintLayout.test.ts` green.
- The snapshot projection carries only the 6 marker fields; the key is absent when empty (no undefined written).
- ShareView adds no new getDoc / org-scoped read; no new firestore.rules or storage.rules block anywhere in the phase.
- Both read-only surfaces reuse the single StageLayoutView.
</verification>

<success_criteria>
- The stage layout is denormalized into the frozen snapshot and rendered read-only on the public share page and in print (R315), reusing the shared renderer (R314 resize-stable).
- No new public rules and no new org-scoped access are introduced; the public page gets nothing beyond the frozen snapshot's display fields.
</success_criteria>

<output>
Create `.planning/phases/107-visual-stage-layout/107-03-SUMMARY.md` when done.
</output>
