---
phase: 16-quarterly-schedule-share-link
plan: 06
type: execute
wave: 2
depends_on: ["16-01", "16-03"]
files_modified:
  - src/views/RosterView.vue
  - src/components/AvailabilityRosterTable.vue
  - src/stores/roster.ts
autonomous: true
requirements: [R-04, R-05, R-11]
must_haves:
  truths:
    - "The Volunteer (roster) edit form edits only roles + name/email/active — no frequency, blackout, or pairing (D-07)"
    - "The Volunteer screen needs no quarter context (D-10)"
    - "AvailabilityRosterTable derives its frequency badges from PersonQuarterData.roleFrequency (R-05)"
    - "The roster store no longer synthesizes or persists standing per-person frequency (D-04)"
    - "Dense Volunteer-page sections (Roles config, Inactive Volunteers) are collapsible, default expanded, remembered per-user (R-11, D-17)"
  artifacts:
    - path: "src/views/RosterView.vue"
      provides: "Roles-only volunteer form + collapsible dense sections"
      contains: "CollapsibleSection"
    - path: "src/components/AvailabilityRosterTable.vue"
      provides: "Frequency badges sourced from roleFrequency"
      contains: "roleFrequency"
  key_links:
    - from: "src/views/RosterView.vue"
      to: "src/components/CollapsibleSection.vue"
      via: "wraps Roles config + Inactive Volunteers"
      pattern: "CollapsibleSection"
    - from: "src/components/AvailabilityRosterTable.vue"
      to: "PersonQuarterData.roleFrequency"
      via: "tierOf/aggregateTier/freqBadge reads"
      pattern: "roleFrequency"
---

<objective>
Reduce the Volunteer (roster) edit surface to roles-only (D-07), repoint the availability-table
badges to the new `roleFrequency` (R-05), remove the roster store's standing-frequency synthesis
(D-04), and make the Volunteer page's dense sections collapsible (R-11).

Purpose: Kill the confusing frequency duplication on the roster screen; keep frequency solely on
the Schedule screen's per-quarter editor.
Output: Reworked `RosterView.vue`, `AvailabilityRosterTable.vue`, `stores/roster.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-CONTEXT.md
@.planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
CollapsibleSection (from 16-03): props { title: string; storageKey: string }, default expanded.
RoleFrequencyEntry: { tier: FrequencyTier; n: number }; default {tier:'regular', n:4}.

RosterView.vue sections to DELETE (D-07, grep-confirmed):
- "Serve frequency by role" label (~line 326) + formRoleFrequencies v-model (~335)
- formRoleFrequencies ref + heldRolesSorted sync watcher + loadPerson frequency load (~436-484)
- save payload frequencyTargetN/roleFrequencies (~502-503)
- minRoleFrequency sort column + fn (~561-585)
KEEP: the roles checklist (drives person.roles) + its rosterStore.upsertPeople/updatePerson write.
Collapsible targets on RosterView: "Roles config panel" + "Inactive Volunteers" (mt-8 blocks). Active-people table stays always visible.

AvailabilityRosterTable.vue read sites to REPOINT (grep-confirmed):
- tierOf/aggregateTier/allRolesOut (~133-168): read pqd?.roleTiers/frequencyTier → pqd?.roleFrequency?.[roleId]?.tier
- freqLabel/freqBadge (~221-237): fall back to person.frequencyTargetN → pqd?.roleFrequency?.[roleId]?.n (default 4)

roster.ts to DELETE (D-04): roleFrequencies migration block (~59-74) + frequency synthesis in addPerson/updatePerson/upsertPeople. Keep Firestore updateDoc/dot-path conventions.
Storage keys (UI-SPEC): localStorage['roster.section.{id}'].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roles-only Volunteer form + collapsible dense sections</name>
  <files>src/views/RosterView.vue</files>
  <read_first>
    - src/views/RosterView.vue (frequency section, formRoleFrequencies, save payload, sort column, roles checklist, Roles config + Inactive Volunteers blocks)
    - src/components/CollapsibleSection.vue (props + usage, from 16-03)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/views/RosterView.vue" — exact lines to delete/keep)
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md (Copywriting Contract — roles-only form; collapsible generic pattern keeps existing section titles)
  </read_first>
  <action>
    Delete the entire "Serve frequency by role" form section, `formRoleFrequencies` ref + its sync
    watcher, the frequency load in loadPerson, the frequencyTargetN/roleFrequencies save-payload
    fields, and the `minRoleFrequency` sort column + function (D-07 — the Volunteer form edits only
    roles + name/email/active, no quarter context per D-10). Keep the roles checklist and its
    rosterStore.upsertPeople/updatePerson write path unchanged (this is the D-09 template plan 16-05
    replicates). Wrap the "Roles config panel" and "Inactive Volunteers" blocks each in a
    `<CollapsibleSection>` (import it) with `storageKey="roster.section.rolesConfig"` and
    `storageKey="roster.section.inactiveVolunteers"`, titles = existing section headings unchanged.
    The active-people table stays outside any CollapsibleSection (primary content, always visible).
  </action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | head -20 && grep -c "formRoleFrequencies\|minRoleFrequency\|Serve frequency by role" src/views/RosterView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "formRoleFrequencies\|minRoleFrequency\|Serve frequency by role" src/views/RosterView.vue` === 0
    - `grep -c "frequencyTargetN\|roleFrequencies" src/views/RosterView.vue` === 0
    - `grep -n "CollapsibleSection" src/views/RosterView.vue` shows Roles config + Inactive Volunteers wrapped with `roster.section.*` storageKeys
    - Roles checklist + its roster-store write remain present
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>Volunteer form is roles-only; Roles config + Inactive Volunteers collapsible; active-people table always visible.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint availability-table badges + strip roster-store frequency synthesis</name>
  <files>src/components/AvailabilityRosterTable.vue, src/stores/roster.ts</files>
  <read_first>
    - src/components/AvailabilityRosterTable.vue (tierOf/aggregateTier/allRolesOut/freqLabel/freqBadge read sites)
    - src/stores/roster.ts (roleFrequencies migration block ~59-74; addPerson/updatePerson/upsertPeople frequency synthesis)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (sections "AvailabilityRosterTable.vue" and "roster.ts")
    - .planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md (Pitfall 5 — grep frequencyTargetN/roleFrequencies before removing; not all reads fail type-check)
  </read_first>
  <action>
    In AvailabilityRosterTable.vue, repoint `tierOf`/`aggregateTier`/`allRolesOut` to read
    `pqd?.roleFrequency?.[roleId]?.tier ?? 'regular'` and `freqLabel`/`freqBadge` to read
    `pqd?.roleFrequency?.[roleId]?.n ?? 4` (keep the same function names, signatures, and badge
    output shape — only the field source changes). Keep this consistent with QuarterGrid's own
    tierOf repoint (plan 16-07) since both must read the single roleFrequency source. In
    stores/roster.ts, delete the `roleFrequencies` migration block and the frequency-synthesis
    logic in addPerson/updatePerson/upsertPeople; make updatePerson/upsertPeople tolerant of (i.e.
    simply not persist) any legacy frequency params so no caller breaks. Do NOT write any standing
    frequency field. Preserve the store's existing updateDoc/dot-path Firestore conventions.
  </action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | head -20 && grep -rc "roleTiers\|frequencyTier\|frequencyTargetN" src/components/AvailabilityRosterTable.vue src/stores/roster.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "roleTiers\|frequencyTier\|frequencyTargetN\|roleFrequencies" src/components/AvailabilityRosterTable.vue` === 0
    - `grep -c "roleFrequencies\|frequencyTargetN" src/stores/roster.ts` === 0 (migration block + synthesis removed)
    - `grep -n "roleFrequency" src/components/AvailabilityRosterTable.vue` shows tier + n both sourced from roleFrequency
    - `npx vue-tsc --build` exits 0; `npx vitest run src/stores/__tests__/roster.test.ts` (if present) passes
  </acceptance_criteria>
  <done>Availability-table badges read roleFrequency; roster store no longer synthesizes/persists standing frequency; type-check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated editor → roster store | Editor-gated writes only (org shell auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering | stale frequencyTargetN reads producing NaN badges after field removal | mitigate | Every read site repointed to roleFrequency with a `?? 4` default; grep-guard asserts zero legacy references (Pitfall 5) |
| T-16-06-02 | Information Disclosure | roster view exposing PII | accept | Authenticated app-shell surface for org editors; no new PII surface |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vue-tsc --build` green.
- Manual: Volunteer form shows only roles + name/email/active; availability-table badges render correct cadence from quarter data; Roles config + Inactive Volunteers collapse and remember state.
</verification>

<success_criteria>
- Volunteer form roles-only; badges sourced from roleFrequency; roster store frequency synthesis removed; dense sections collapsible.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-06-SUMMARY.md` when done.
</output>
