---
phase: 16-quarterly-schedule-share-link
plan: 08
type: execute
wave: 2
depends_on: ["16-01", "16-03"]
files_modified:
  - src/views/QuarterView.vue
autonomous: true
requirements: [R-09, R-10, R-11]
must_haves:
  truths:
    - "Selecting an existing quarter and creating a new quarter are visually distinct, non-conflated controls (R-10, D-13)"
    - "The Add-quarter action opens a small centered modal; both select and create still work (R-10)"
    - "The three dense setup sections are collapsible, default expanded, remembered per-user (R-11, D-17)"
    - "QuarterGrid stays always-visible as the primary work surface (R-09 redesign)"
  artifacts:
    - path: "src/views/QuarterView.vue"
      provides: "Quarter switcher + Add-quarter modal + collapsible setup sections"
      contains: "CollapsibleSection"
  key_links:
    - from: "src/views/QuarterView.vue"
      to: "quartersStore.createQuarter"
      via: "Add-quarter modal Create button"
      pattern: "createQuarter"
    - from: "src/views/QuarterView.vue"
      to: "src/components/CollapsibleSection.vue"
      via: "wraps 3 setup cards"
      pattern: "CollapsibleSection"
---

<objective>
Implement the Schedule-page redesign the R-09 note recommends (16-SCHEDULE-UX-NOTE.md): separate
the quarter switcher from a secondary "+ Add quarter" modal (R-10/D-13), and wrap the three dense
setup sections in CollapsibleSection (R-11), keeping QuarterGrid always-visible.

Purpose: Un-conflate select-vs-create quarter; declutter the schedule page without pivoting to a
calendar widget.
Output: Reworked `QuarterView.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-SCHEDULE-UX-NOTE.md
@.planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
CollapsibleSection (from 16-03): props { title: string; storageKey: string }, default expanded.
Centered-modal markup to copy (src/components/VolunteerCsvImportModal.vue lines 28-33):
`<div v-if="open" class="fixed inset-0 z-50 flex items-center justify-center p-4" @click.self="onClose"><div class="w-full max-w-3xl max-h-[90vh] bg-gray-900 rounded-xl border border-gray-700 shadow-2xl flex flex-col">` — shrink max-w for a small form.
Existing modal-wiring convention (QuarterView.vue ~294-299, 449-454): `:open` prop + `@close` emit; `const csvModalOpen = ref(false)`.

QuarterView.vue (grep-confirmed):
- Quarter select + create controls conflated in one card, lines ~63-105 (select ~66-73; year/quarter/"New quarter" inputs ~76-103). Split: keep <select> as lightweight "Quarter" switcher; move create into an Add-quarter modal.
- Three setup cards to wrap: "Volunteer Availability" (~109-112), "Service dates" (~115+), "Generate controls" (further down). QuarterGrid stays OUTSIDE any CollapsibleSection.
- quartersStore.createQuarter(year, quarter, label) — from 16-01 (now seeds prior-quarter data).
UI-SPEC copy: switcher label "Quarter"; button "+ Add quarter" (secondary, border-gray-700 bg-gray-800, NOT indigo); modal title "Add a new quarter"; CTA "Create quarter" (indigo-600); cancel "Don't create". Storage keys: localStorage['schedule.section.{id}'].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate quarter switcher from a secondary Add-quarter modal (D-13/R-10)</name>
  <files>src/views/QuarterView.vue</files>
  <read_first>
    - src/views/QuarterView.vue (quarter select + create card ~63-105; existing modal wiring ~294-299/449-454)
    - src/components/VolunteerCsvImportModal.vue lines 1-33 (centered-modal Teleport/backdrop shape to copy)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/views/QuarterView.vue")
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md (Copywriting Contract — add-quarter labels)
  </read_first>
  <action>
    Split the conflated quarter card: keep the `<select>` of existing quarters as a lightweight
    "Quarter" switcher (label "Quarter", no create action inside it). Remove the inline
    year/quarter/"New quarter" inputs from that card and move them into a small centered modal
    (reuse VolunteerCsvImportModal's centered-modal container classes, shrunk from max-w-3xl to a
    small form width). Add a visually secondary "+ Add quarter" button (`border-gray-700 bg-gray-800`,
    NOT indigo) that opens the modal via an `addQuarterOpen` ref. Modal contents: year input, quarter
    select, title "Add a new quarter", primary CTA "Create quarter" (indigo-600) calling
    quartersStore.createQuarter(year, quarter, label), and a "Don't create" cancel that closes without
    creating. Both existing behaviors (selecting an existing quarter, creating a new one) must keep
    working; on successful create, select the new quarter and close the modal.
  </action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | head -20 && grep -c "Add a new quarter\|+ Add quarter\|Create quarter" src/views/QuarterView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "+ Add quarter" src/views/QuarterView.vue` >= 1 and the button uses `border-gray-700 bg-gray-800` (secondary, not indigo)
    - Modal has title "Add a new quarter", CTA "Create quarter", cancel "Don't create" (exact UI-SPEC copy)
    - The quarter `<select>` no longer contains create inputs; create lives only in the modal
    - `grep -n "createQuarter" src/views/QuarterView.vue` shows the modal Create wired to the store
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>Quarter switcher and Add-quarter modal are distinct; both select and create work.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap the three dense setup sections in CollapsibleSection (R-11)</name>
  <files>src/views/QuarterView.vue</files>
  <read_first>
    - src/views/QuarterView.vue (the three setup cards + the QuarterGrid render)
    - src/components/CollapsibleSection.vue (props/usage, from 16-03)
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md (Visual Hierarchy — QuarterGrid is the always-visible focal point)
  </read_first>
  <action>
    Import CollapsibleSection and wrap each of "Volunteer Availability", "Service dates", and
    "Generate controls" in a `<CollapsibleSection>` with `storageKey="schedule.section.volunteerAvailability"`,
    `"schedule.section.serviceDates"`, `"schedule.section.generateControls"` and titles equal to the
    existing section headings. Leave `QuarterGrid` OUTSIDE any CollapsibleSection so it stays always
    visible (primary work surface, per UI-SPEC focal point). Default state expanded (D-17). Do not
    restyle the section bodies beyond wrapping them.
  </action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | head -20 && grep -c "CollapsibleSection" src/views/QuarterView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "schedule.section." src/views/QuarterView.vue` >= 3 (three distinct storageKeys)
    - QuarterGrid is NOT wrapped in a CollapsibleSection (grep the render — grid renders outside the wrappers)
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>Three setup sections collapsible with per-section persistence; QuarterGrid always visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated editor → quarters store | createQuarter/select gated by app-shell auth + editor role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08-01 | Tampering | Add-quarter modal creating a duplicate/wrong quarter | mitigate | createQuarter unchanged (16-01) generates a new doc; modal only supplies year/quarter/label; existing select/create behaviors regression-checked |
| T-16-08-02 | Information Disclosure | schedule page exposing PII | accept | Authenticated app-shell surface for org editors; no new PII surface |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vue-tsc --build` green.
- Manual: switcher selects existing quarters; "+ Add quarter" opens the modal and creates; three setup sections collapse and remember state; grid always visible.
</verification>

<success_criteria>
- Distinct quarter switcher vs Add-quarter modal; collapsible setup sections; always-visible grid — matching the R-09 note.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-08-SUMMARY.md` when done.
</output>
