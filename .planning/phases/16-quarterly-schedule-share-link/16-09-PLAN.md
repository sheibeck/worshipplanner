---
phase: 16-quarterly-schedule-share-link
plan: 09
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/views/SettingsView.vue
  - src/stores/quarters.ts
autonomous: true
requirements: [R-02]
must_haves:
  truths:
    - "A church slug is auto-derived from the org name on first share, unique across orgs, and persisted on the org doc (R-02, D-18)"
    - "The slug is editable in Settings; a collision surfaces the 'already taken' error (R-02, D-18)"
    - "finalizeAndShare writes a quarterShares/{slug}__q{N}-{year} doc, overwritten-in-place on every finalize (R-02, Pitfall 2)"
    - "The quarterShares snapshot exposes no PII beyond pre-resolved names (D-24)"
  artifacts:
    - path: "src/views/SettingsView.vue"
      provides: "Editable Share URL slug field with collision handling"
      contains: "slug"
    - path: "src/stores/quarters.ts"
      provides: "finalizeAndShare quarterShares overwrite-in-place write"
      contains: "quarterShares"
  key_links:
    - from: "src/stores/quarters.ts::finalizeAndShare"
      to: "quarterShares/{slug}__q{N}-{year}"
      via: "setDoc overwrite reusing calendarWithNames"
      pattern: "quarterShares"
    - from: "src/views/SettingsView.vue"
      to: "src/utils/slug.ts::claimSlug"
      via: "slug save enforcing uniqueness"
      pattern: "claimSlug"
---

<objective>
Wire the memorable-URL producer side (R-02): a Settings "Share URL slug" field that claims a
unique slug (D-18), and a `finalizeAndShare` extension that writes the denormalized
`quarterShares/{slug}__q{N}-{year}` doc overwritten-in-place on every finalize (Pitfall 2).

Purpose: Every quarterly share link gets a stable, memorable, editable slug; the memorable URL
always reflects the latest finalized snapshot.
Output: Reworked `SettingsView.vue`, extended `finalizeAndShare` in `quarters.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md
@.planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
slug util (from 16-02): deriveSlug(orgName), RESERVED_SLUGS, claimSlug(baseSlug, orgId): Promise<string>.
auth store: orgId (ref<string|null>), orgName (ref<string|null>); org doc = doc(db,'organizations',orgId).
Org doc gains a `slug` field (string).

finalizeAndShare (src/stores/quarters.ts ~324-359) current shape:
- builds calendarWithNames (denormalized names) ~336-342
- writes shareTokens/{token} with quarterSnapshot { label, serviceDates, roles: rosterStore.roles.map(r=>({id,name,group})), calendar: calendarWithNames } ~344-354
- then updateQuarter(quarterId, { status:'finalized', shareToken: token })
Quarter has { label, year, quarter, serviceDates, ... }.

SettingsView.vue (grep-confirmed): "Organization Name" field + onSave (~14-37, 149-208) is the exact template for the slug field (same input/save-button/disabled/feedback pattern; updateDoc(organizations/{orgId})).
UI-SPEC copy: label "Share URL slug"; helper "yourapp.com/{slug}/quarter1-2026 — used in every quarterly share link" (fill {slug} live); collision error "That URL is already taken — try a different one."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: finalizeAndShare writes the quarterShares memorable-URL doc (overwrite-in-place)</name>
  <files>src/stores/quarters.ts</files>
  <read_first>
    - src/stores/quarters.ts (finalizeAndShare ~324-359 — calendarWithNames build, shareTokens write, snapshot shape)
    - src/stores/auth.ts (orgName/orgId + org doc access for the slug)
    - .planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md (Pattern 3 Firestore doc design; Pitfall 2 — overwrite-in-place, not accumulate)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/stores/quarters.ts" — finalizeAndShare addition)
  </read_first>
  <action>
    In `finalizeAndShare`, after the existing shareTokens setDoc (keep it as-is), resolve the org
    slug: read the org doc's `slug`; if absent, derive via `deriveSlug(orgName)` and claim it via
    `claimSlug(base, orgId)` (from src/utils/slug.ts), then persist the returned slug on the org doc
    (updateDoc(organizations/{orgId}, { slug })). Then write a SECOND doc:
    `setDoc(doc(db, 'quarterShares', `${slug}__q${quarter.quarter}-${quarter.year}`), { orgSlug: slug,
    quarterSnapshot: { label, serviceDates, roles, calendar: calendarWithNames }, token, updatedAt:
    serverTimestamp() })` — reuse the EXISTING calendarWithNames + the same roles/label/serviceDates
    snapshot fields (do NOT duplicate the name-resolution logic). The doc ID is stable per (slug,
    quarter, year), so every finalize OVERWRITES in place (Pitfall 2) — never accumulate like
    shareTokens. The snapshot carries only names (D-24) — no email/phone.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/quarters.test.ts 2>&1 | tail -20 || npx vue-tsc --build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "quarterShares" src/stores/quarters.ts` shows a setDoc with a stable `${slug}__q${...}-${...}` doc ID
    - The quarterShares snapshot reuses calendarWithNames (grep confirms it is referenced, not re-derived)
    - `grep -n "claimSlug\|deriveSlug" src/stores/quarters.ts` shows the slug is claimed/persisted when absent
    - The snapshot object contains only label/serviceDates/roles/calendar (names) — no email/phone fields
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>finalizeAndShare claims a slug when needed and writes an overwrite-in-place quarterShares doc with the names-only snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Settings "Share URL slug" field with uniqueness + collision handling</name>
  <files>src/views/SettingsView.vue</files>
  <read_first>
    - src/views/SettingsView.vue ("Organization Name" field + onSave ~14-37/149-208 — the template to copy)
    - src/utils/slug.ts (deriveSlug, claimSlug — from 16-02)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/views/SettingsView.vue" — copy the org-name field pattern, retarget to slug)
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md (Copywriting Contract — slug label/helper/collision copy)
  </read_first>
  <action>
    Add a "Share URL slug" field mirroring the Organization Name field's markup + save-button/
    disabled/feedback pattern. Initialize the input from the org doc's `slug` (or the live-derived
    value from orgName when unset), showing helper text "yourapp.com/{slug}/quarter1-2026 — used in
    every quarterly share link" with {slug} filled live. On save, sanitize via deriveSlug, then call
    claimSlug(candidate, orgId); on success persist the returned slug on the org doc
    (updateDoc(organizations/{orgId}, { slug })) and show "Saved!"; on a claim failure / collision,
    show the error "That URL is already taken — try a different one." Do NOT do a raw updateDoc that
    bypasses claimSlug — uniqueness must go through the orgSlugs claim.
  </action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | head -20 && grep -c "Share URL slug\|claimSlug\|already taken" src/views/SettingsView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Share URL slug" src/views/SettingsView.vue` >= 1
    - `grep -n "claimSlug" src/views/SettingsView.vue` shows the save path enforces uniqueness via the claim (not a raw updateDoc alone)
    - Collision error copy "That URL is already taken — try a different one." present verbatim
    - Helper text includes "used in every quarterly share link"
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>Settings has an editable, unique, persisted Share URL slug field with collision feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated editor → orgSlugs / quarterShares / org doc | Signed-in editors write; public reads the resulting quarterShares |
| org name (user-controlled) → doc ID | Slug segment of the quarterShares doc ID must be sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09-01 | Information Disclosure | quarterShares snapshot leaking email/phone | mitigate | Snapshot reuses the names-only calendarWithNames (D-24); grep-verified no email/phone fields written |
| T-16-09-02 | Tampering | slug doc-ID injection via unsanitized org name | mitigate | Slug always passes through deriveSlug ([a-z0-9-]+) before the quarterShares doc() path is built |
| T-16-09-03 | Spoofing | two orgs claiming the same slug | mitigate | claimSlug (create-only orgSlugs claim, first-writer-wins) with numeric-suffix retry; enforced by deployed rules (16-02) |
| T-16-09-04 | Tampering | stale memorable-URL data after re-finalize | mitigate | quarterShares doc overwritten-in-place on every finalize (stable doc ID) — memorable URL always shows latest (Pitfall 2) |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vue-tsc --build` green; quarters store test (if present) green.
- Manual: finalize a quarter → a quarterShares/{slug}__qN-YYYY doc appears with names-only snapshot; edit the slug in Settings → persists / collision shows the error.
- Requires deployed rules from 16-02 for the claim/write to succeed at runtime.
</verification>

<success_criteria>
- Unique, editable, persisted church slug; finalizeAndShare writes an overwrite-in-place names-only quarterShares doc.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-09-SUMMARY.md` when done.
</output>
