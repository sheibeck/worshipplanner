---
phase: 15-per-role-frequency-role-categories
plan: 06
type: execute
wave: 4
depends_on: [15-01, 15-02, 15-04]
files_modified:
  - src/components/AvailabilityDrawer.vue
  - src/components/__tests__/AvailabilityDrawer.test.ts
  - src/components/QuarterGrid.vue
  - src/components/__tests__/QuarterGrid.test.ts
autonomous: false
requirements: [D-05, D-06, D-11]
must_haves:
  truths:
    - "The availability drawer shows one tier control (Every week / Twice a month / Monthly / As-needed / Out) per role the person holds for the active quarter (D-06), writing a per-role roleTiers map via setPersonAvailability (D-05)"
    - "The manual assignment grid renders a visible, non-blocking warning badge on any cell whose assignment violates group rules — a person on TECH + non-TECH, or holding 2 BAND / 2 VOCALS the same date (D-11) — computed live from the current calendar, working for pre-existing calendars not just fresh proposals"
    - "The warning never blocks the edit — the leader stays in control (D-11 warn-don't-block)"
  artifacts:
    - path: "src/components/AvailabilityDrawer.vue"
      provides: "per-role tier controls + roleTiers in the save payload"
      contains: "roleTiers"
    - path: "src/components/QuarterGrid.vue"
      provides: "live group-violation warning badge reusing the exported evaluateGroupCombo helper"
      contains: "evaluateGroupCombo"
  key_links:
    - from: "AvailabilityDrawer onSave"
      to: "quarters store setPersonAvailability"
      via: "roleTiers included in payload"
      pattern: "roleTiers"
    - from: "QuarterGrid warning computed"
      to: "scheduler.ts evaluateGroupCombo"
      via: "import shared pure helper"
      pattern: "evaluateGroupCombo"
---

<objective>
Deliver the two quarter-view surfaces of the per-role model: per-role tier controls in the availability drawer (D-05/D-06) and a manual-grid co-occurrence warning badge (D-11).

Purpose: D-06 mirrors the D-01 roster layout for the quarter-scoped tier — a person can be `out` for Tech but `regular` for Vocals this quarter, persisted via the roleTiers write landed in 15-04. D-11 completes the co-occurrence story: auto-propose strictly obeys the rules (15-02), while manual grid edits that violate them are ALLOWED but flagged — the leader keeps control over legitimate exceptions. QuarterGrid computes the warning live from `quarter.calendar` + `roles` (works for historical calendars, not just fresh proposals) by reusing the pure `evaluateGroupCombo` helper exported from scheduler.ts (QuarterGrid cannot import scheduler's internal closures, so the shared exported helper is the single source of the rule).

Output: per-role tier controls + roleTiers save in AvailabilityDrawer.vue; a third live warning badge in QuarterGrid.vue reusing evaluateGroupCombo; extended component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-per-role-frequency-role-categories/15-CONTEXT.md
@.planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md

<interfaces>
<!-- Drawer + grid insertion points. From 15-PATTERNS.md. -->
AvailabilityDrawer.vue:
- `FREQ_PRESETS` (~lines 53-86) — the preset-button block to wrap in a per-role v-for.
- `loadDraft` (~lines 322-339): sets `draft.frequencyTier = pqd?.frequencyTier ?? 'regular'` — add `draft.roleTiers` populated per held role from `pqd?.roleTiers?.[id] ?? pqd?.frequencyTier ?? 'regular'`.
- `onSave` (~lines 492-510): the `setPersonAvailability` call — add `roleTiers: draft.roleTiers`.
- Standing-vs-quarter split already coded here (frequencyTargetN via rosterStore.updatePerson; tier via quarters store) — respect it (D-07).
QuarterGrid.vue:
- `cellIsUnfilled`/`cellHasConflict` (~lines 228-241) + badge `<span>`s (~lines 54-66) — the report-don't-force pattern to mirror for a third badge `cellHasGroupViolation`.
- `props.quarter.calendar` + `props.roles` are available directly (no ProposeResult dependency).
Shared helper (from 15-02): `import { evaluateGroupCombo } from '@/utils/scheduler'` — `evaluateGroupCombo(roleIds: string[], roleGroupOf) => { ok, reason? }`. Build `roleGroupOf` from `props.roles` (Map lookup, default 'other').
Types (from 15-01): `PersonQuarterData.roleTiers?`; RoleGroup includes 'vocals'.
setPersonAvailability (from 15-04) accepts roleTiers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-role tier controls in the availability drawer (D-05/D-06)</name>
  <files>src/components/AvailabilityDrawer.vue, src/components/__tests__/AvailabilityDrawer.test.ts</files>
  <read_first>
    - src/components/AvailabilityDrawer.vue (FREQ_PRESETS, the tier control section, loadDraft, onSave, draft reactive)
    - src/components/__tests__/AvailabilityDrawer.test.ts (existing mount harness + tier-control assertions)
    - .planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md (AvailabilityDrawer.vue section — exact edits)
  </read_first>
  <behavior>
    - Opening the drawer for a person holding 2+ roles renders one tier control per held role for the active quarter.
    - A person with `roleTiers: { sound: 'out' }` loads Sound at 'out' and other held roles at the legacy `frequencyTier` fallback, else 'regular'.
    - Changing Vocals to 'Monthly' and Tech to 'Out' then saving calls setPersonAvailability with `roleTiers` carrying both.
    - Blackout/pairing/note controls are unchanged (D-07).
  </behavior>
  <action>
    Add a reactive `draft.roleTiers: Record<string, FrequencyTier>`. Wrap the existing FREQ_PRESETS button block in a `v-for` over the person's held roles (role name heading + the same preset buttons), each bound to `draft.roleTiers[roleId]` (default 'regular', D-05). In `loadDraft`, populate `draft.roleTiers` per held role via `pqd?.roleTiers?.[id] ?? pqd?.frequencyTier ?? 'regular'`. In `onSave`, add `roleTiers: draft.roleTiers` to the setPersonAvailability payload. Leave the standing `frequencyTargetN` → rosterStore.updatePerson path and blackout/pairing/note handling untouched (D-07). Extend AvailabilityDrawer.test.ts to assert the <behavior>. Reuse existing static button classes (no interpolated Tailwind).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts` exits 0
    - Test asserts one tier control per held role + roleTiers present in the setPersonAvailability payload + per-role default 'regular'
    - `grep -n "roleTiers" src/components/AvailabilityDrawer.vue` matches in draft, loadDraft, and onSave
    - blackoutDates/pairedWith/note handling unchanged (D-07)
    - `npm run type-check` exits 0
    - Commit: `feat(15-06): per-role tier controls in availability drawer`
  </acceptance_criteria>
  <done>The availability drawer edits and persists an independent quarter tier per role the person holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live group-violation warning badge in the manual grid (D-11)</name>
  <files>src/components/QuarterGrid.vue, src/components/__tests__/QuarterGrid.test.ts</files>
  <read_first>
    - src/components/QuarterGrid.vue (cellIsUnfilled/cellHasConflict + badge markup + GROUP_ORDER/groupHeaderBg + cellPeople helper)
    - src/components/__tests__/QuarterGrid.test.ts (existing warning-badge test style)
    - src/utils/scheduler.ts (the exported evaluateGroupCombo signature from 15-02)
    - .planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md (QuarterGrid.vue section — third-badge guidance + shared-helper reuse note)
  </read_first>
  <behavior>
    - A cell where an assigned person also holds a role in an incompatible group that same date (TECH + non-TECH, or a 2nd BAND, or a 2nd VOCALS) shows a distinctly-colored warning badge (e.g. orange, not red/amber which are taken by Unfilled/Pairing-conflict).
    - The warning is computed live from `props.quarter.calendar` + `props.roles` (works on a loaded historical calendar, not only after a fresh propose).
    - The warning does NOT block or remove the assignment — the cell remains editable (D-11 warn-don't-block).
    - A legal "1 BAND + 1 VOCALS" combo shows NO warning.
  </behavior>
  <action>
    Add `cellHasGroupViolation(date, roleId)`: build `roleGroupOf` from `props.roles` (Map, default 'other'); for each personId in that cell, gather ALL roleIds that person is assigned across the date's calendar cells, call `evaluateGroupCombo(thatPersonsRoleIdsThisDate, roleGroupOf)`, and return true if any assigned person's combo is not ok. Import `evaluateGroupCombo` from '@/utils/scheduler' (the single shared rule source — do NOT re-implement the group logic in the component). Render a third `<span>` badge alongside the existing Unfilled/Pairing-conflict badges using a distinct static Tailwind color (e.g. `bg-orange-900/40 border border-orange-700/50 text-orange-300`, label "Group conflict"). Do not gate/disable the edit on the warning. Extend QuarterGrid.test.ts to assert: warning shows on TECH+BAND same person/date, warning shows on 2nd BAND, NO warning on 1 BAND + 1 VOCALS, and that the assignment is not removed.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/QuarterGrid.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/QuarterGrid.test.ts` exits 0
    - Test asserts a warning badge on an illegal combo (TECH+BAND and 2nd-BAND) and NO badge on the legal 1-BAND+1-VOCALS combo, and that the illegal assignment is still present (not blocked)
    - `grep -n "evaluateGroupCombo" src/components/QuarterGrid.vue` matches (helper imported/reused, not re-implemented)
    - `grep -n "cellHasGroupViolation" src/components/QuarterGrid.vue` matches in script and template
    - warning computed from props.quarter.calendar + props.roles (not from lastProposeResult)
    - `npx vitest run` full suite exits 0; `npm run type-check` exits 0
    - Commit: `feat(15-06): live group co-occurrence warning in manual grid`
  </acceptance_criteria>
  <done>The manual grid visibly flags illegal group combos on any calendar without blocking the leader's edit, reusing the one shared rule helper.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify per-role tier controls and manual-grid group-conflict warning</name>
  <files>n/a</files>
  <action>Human verifies per-role tier controls in the availability drawer and the non-blocking group-conflict warning badge in the manual grid.</action>
  <verify>Human confirmation that per-role tiers persist and the group-conflict warning shows without blocking</verify>
  <done>User confirms: one tier control per held role with independent persistence; "Group conflict" badge appears on TECH+BAND without removing the assignment; no warning on 1 BAND + 1 VOCALS; auto-propose produces no conflict badges</done>
  <what-built>Per-role tier controls in the availability drawer (D-06) and a non-blocking group-conflict warning badge in the manual assignment grid (D-11).</what-built>
  <how-to-verify>
    1. Run `npm run dev`, open a quarter, and open the availability drawer for a person holding both a Tech and a Band/Vocals role.
    2. Confirm one tier control (Every week / Twice a month / Monthly / As-needed / Out) appears per held role; set the Tech role to "Out" and a Band role to "Monthly", Save, reopen → confirm both persisted independently (D-06).
    3. In the manual grid, assign one person to a TECH role AND a BAND role on the same service date → confirm a visible "Group conflict" warning badge appears and the assignment is NOT removed (D-11 warn-don't-block).
    4. Assign the same person to 1 instrument (BAND) + Vocals on one date → confirm NO warning (the canonical allowed combo).
    5. Regenerate a proposal → confirm auto-propose produces no group-conflict badges (15-02 strict obey).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drawer input → store write | User-entered per-role tiers cross into a scoped Firestore write |
| Manual grid state → warning compute | Client-side calendar data drives a non-persisted advisory badge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Tampering | roleTiers write clobbering sibling quarter data | mitigate | Persistence goes through 15-04's scoped `personQuarterData.<personId>` write; the drawer only supplies the roleTiers value, never a whole-collection write. |
| T-15-06-02 | Integrity | Warning logic diverging from scheduler rules | mitigate | QuarterGrid reuses the SAME exported `evaluateGroupCombo` helper as the scheduler — one rule source, no drift. A test asserts legal vs illegal combos match. |
| T-15-06-03 | Denial of Service | Warning check over a large calendar per render | accept | Per-cell check is O(roles×people-in-date), small (quarter grids are bounded); computed reactively like existing cellIsUnfilled/cellHasConflict — no new perf class. |
| T-15-06-SC | Tampering | package installs | accept | No packages installed this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts src/components/__tests__/QuarterGrid.test.ts` green
- Human verify: per-role tiers persist (D-06); group-conflict warning shows without blocking (D-11)
- `npm run type-check` green; full suite green
</verification>

<success_criteria>
A leader sets independent per-role quarter tiers, and manual edits that break the group rules are visibly flagged (never blocked) using the one shared rule helper — completing the auto-obey / manual-warn split (D-11).
</success_criteria>

<output>
Create `.planning/phases/15-per-role-frequency-role-categories/15-06-SUMMARY.md` when done.
</output>
