---
phase: 15-per-role-frequency-role-categories
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/components/QuarterGrid.vue
  - src/components/AvailabilityRosterTable.vue
  - src/stores/quarters.ts
  - src/components/__tests__/QuarterGrid.test.ts
  - src/components/__tests__/AvailabilityRosterTable.test.ts
  - src/stores/__tests__/quarters.test.ts
autonomous: true
requirements: [D-05]

must_haves:
  truths:
    - "A person marked 'out' for role X via the per-role availability UI is excluded from role X's quick-assign candidate list in the manual grid (QuarterGrid)"
    - "A person marked 'out'/'fill-in' for one or more roles via the per-role UI shows a non-Regular status in AvailabilityRosterTable and appears in the 'Out this quarter' filter"
    - "Adding a person as a must-serve-with partner does NOT erase that partner's already-tuned roleTiers on their quarter-data entry"
  artifacts:
    - path: "src/components/QuarterGrid.vue"
      provides: "Per-(person,role) tier read consulting roleTiers, applied in availableUnassigned's roleId-scoped filter"
      contains: "roleTiers?.["
    - path: "src/components/AvailabilityRosterTable.vue"
      provides: "Status/badge/filter computed from roleTiers with legacy frequencyTier fallback"
      contains: "roleTiers"
    - path: "src/stores/quarters.ts"
      provides: "Reciprocal partner write that preserves the partner's existing roleTiers"
      contains: "roleTiers"
  key_links:
    - from: "src/components/QuarterGrid.vue availableUnassigned"
      to: "PersonQuarterData.roleTiers"
      via: "per-role tierOf(personId, roleId)"
      pattern: "roleTiers\\?\\.\\["
    - from: "src/components/AvailabilityRosterTable.vue quarterDataFor / activeFilter 'out'"
      to: "PersonQuarterData.roleTiers"
      via: "aggregate across held roles with frequencyTier fallback"
      pattern: "roleTiers"
    - from: "src/stores/quarters.ts setPersonAvailability reciprocal write"
      to: "PersonQuarterData.roleTiers"
      via: "scoped pairedWith sub-path OR carry-forward of existing roleTiers"
      pattern: "roleTiers"
---

<objective>
Close the D-05 BLOCKER gap from 15-VERIFICATION.md: the per-quarter tier "becomes per-role" was
only reconciled in `scheduler.ts` (`tierOf`) and `AvailabilityDrawer.vue`. Three other production
surfaces still read/write only the legacy per-person `frequencyTier`, so per-role availability set
through the new drawer UI is silently ignored by the manual grid's quick-assign, invisible in the
admin status table / "Out this quarter" filter, and can be erased on an unrelated reciprocal-pairing
save.

This plan reconciles all three remaining sites with `roleTiers`, mirroring `scheduler.ts`'s canonical
`tierOf` fallback chain `roleTiers?.[roleId] ?? frequencyTier ?? 'regular'`.

Purpose: D-05 states the per-quarter tier "becomes per-role" without qualifying which surfaces —
reconciliation that only holds in 2 of the app's read/write sites is not "reconciled".
Output: 3 reconciled files + test coverage proving per-role tiers flow through each surface.

Scope fence: the fourth VERIFICATION gap (`applyCsvToQuarter` dropping fields on CSV re-import) is
INTENTIONALLY EXCLUDED — the CSV import workflow is being retired. Do NOT touch `applyCsvToQuarter`,
`volunteerCsv.ts`, or any CSV import path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-role-frequency-role-categories/15-CONTEXT.md
@.planning/phases/15-per-role-frequency-role-categories/15-VERIFICATION.md

<interfaces>
<!-- Canonical per-role tier read to mirror. From src/utils/scheduler.ts (tierOf, lines 91-92): -->
<!--   roleTiers?.[roleId] ?? frequencyTier ?? 'regular'  -->
<!-- From src/types/roster.ts: -->
<!--   FrequencyTier = 'regular' | 'fillin' | 'out'  -->
<!--   PersonQuarterData { personId; blackoutDates: string[]; pairedWith: string[]; -->
<!--     frequencyTier?: FrequencyTier; roleTiers?: Record<string, FrequencyTier>; note?: string } -->
<!--   Person.roles: string[]  (roleId[] the person holds) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuarterGrid quick-assign reads per-role tier</name>
  <files>src/components/QuarterGrid.vue, src/components/__tests__/QuarterGrid.test.ts</files>
  <read_first>
    - src/components/QuarterGrid.vue (frequencyTierOf ≈304-306, availableUnassigned ≈310-319)
    - src/utils/scheduler.ts (canonical tierOf, lines 91-92 — the fallback chain to mirror)
    - src/types/roster.ts (FrequencyTier, PersonQuarterData shapes)
    - src/components/__tests__/QuarterGrid.test.ts (existing person-out fixture + mount pattern to extend)
  </read_first>
  <behavior>
    - A person with roleTiers['role-guitar'] === 'out' is EXCLUDED from availableUnassigned(date, 'role-guitar')
    - The SAME person, if roleTiers['role-drums'] === 'regular', is still INCLUDED in availableUnassigned(date, 'role-drums') (per-role, not per-person)
    - A person with no roleTiers but legacy frequencyTier === 'out' remains excluded for every role (back-compat fallback)
    - A person with neither field defaults to 'regular' and is included
  </behavior>
  <action>
    Replace the person-only `frequencyTierOf(personId)` helper with a per-(person, role)
    `tierOf(personId, roleId)` that mirrors `scheduler.ts`'s canonical read using the exact fallback
    chain `roleTiers?.[roleId] ?? frequencyTier ?? 'regular'` (read from
    `props.quarter.personQuarterData[personId]`). Update `availableUnassigned(date, roleId)` so its
    tier exclusion calls `tierOf(p.id, roleId) !== 'out'` (roleId is already in scope in that
    function), so a person marked "out" for role X via the drawer's per-role control is excluded as a
    role-X candidate but stays eligible for their other roles. Keep the existing doc comment's intent
    ("mirrors the auto-proposal exclusion") accurate. Extend the existing QuarterGrid test with a
    fixture person carrying per-role roleTiers proving per-role exclusion (out for one role, eligible
    for another) and a legacy-frequencyTier-only person proving the fallback.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/QuarterGrid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleTiers?\.\[" src/components/QuarterGrid.vue` matches (new per-role read consults roleTiers)
    - `availableUnassigned` passes `roleId` into the tier check (per-role exclusion, not per-person)
    - Test proves: out-for-role-X person absent from role-X candidates, present for role-Y candidates; legacy frequencyTier='out' person excluded via fallback
    - `npx vitest run` exits 0
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>QuarterGrid quick-assign excludes per-role 'out' candidates; per-role differentiation holds; back-compat fallback preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AvailabilityRosterTable status/filter reads per-role tier</name>
  <files>src/components/AvailabilityRosterTable.vue, src/components/__tests__/AvailabilityRosterTable.test.ts</files>
  <read_first>
    - src/components/AvailabilityRosterTable.vue (quarterDataFor ≈135-148; activeFilter 'out' predicate ≈167; freqBadge/statusLabel/statusPillClass/blackoutSummary consumers)
    - src/utils/scheduler.ts (canonical tierOf, lines 91-92)
    - src/types/roster.ts (FrequencyTier, PersonQuarterData.roleTiers, Person.roles)
    - src/components/__tests__/AvailabilityDrawer.test.ts (mount/mock pattern for a sibling component — no roster-table test exists yet, so create one following this style)
  </read_first>
  <behavior>
    - A person with roleTiers = { 'role-guitar': 'out' } (and any other roles regular) yields an aggregate status of 'out' → appears in the 'Out this quarter' filter and shows the 'Out this quarter' pill/label
    - A person with roleTiers = { 'role-guitar': 'fillin' } and no 'out' yields aggregate 'fillin' → non-Regular badge, NOT in the 'out' filter
    - A person with all held roles 'regular' (or empty roleTiers) yields 'regular'
    - A person with no roleTiers but legacy frequencyTier === 'out' still yields 'out' (fallback)
  </behavior>
  <action>
    Change `quarterDataFor(personId)`'s `frequencyTier` derivation (and the `activeFilter === 'out'`
    predicate that consumes it, plus `freqBadge`/`statusLabel`/`statusPillClass`) to compute an
    aggregate tier from `roleTiers` across the person's held roles, falling back to the legacy
    `frequencyTier` only when `roleTiers` is absent/empty, then to 'regular'. Aggregate by
    most-restrictive precedence `out` > `fillin` > `regular` (so a person out for ANY held role
    surfaces as 'out' in the status table and the "Out this quarter" filter — the primary admin audit
    surface for this phase's headline feature). Use `scheduler.ts`'s `roleTiers?.[roleId] ?? frequencyTier ?? 'regular'`
    per-role read as the building block for each held role, then reduce to the aggregate. Keep the
    returned `blackoutDates`/`pairedWith`/`note` fields untouched. Create
    `src/components/__tests__/AvailabilityRosterTable.test.ts` (following the AvailabilityDrawer test
    mount/mock style) with fixtures proving per-role aggregation and the legacy fallback.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AvailabilityRosterTable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleTiers" src/components/AvailabilityRosterTable.vue` matches (status/badge/filter now consult roleTiers)
    - Aggregate precedence out > fillin > regular implemented; legacy frequencyTier used only when roleTiers absent
    - Test proves: out-for-one-role person shows 'Out this quarter' status AND appears in the 'out' filter; fillin-for-one-role shows non-Regular; legacy frequencyTier='out' still resolves 'out'
    - `npx vitest run` exits 0
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Roster status table and "Out this quarter" filter reflect per-role tiers with most-restrictive aggregation; back-compat fallback preserved; new test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: setPersonAvailability reciprocal write preserves partner roleTiers</name>
  <files>src/stores/quarters.ts, src/stores/__tests__/quarters.test.ts</files>
  <read_first>
    - src/stores/quarters.ts (setPersonAvailability, reciprocal 'added' write ≈193-204; the 'removed' loop ≈205-212 that uses a scoped .pairedWith sub-path GATED by `if (partnerData)` entry-existence — note that gate, it is the difference that matters here)
    - src/types/roster.ts (PersonQuarterData.roleTiers)
    - src/stores/__tests__/quarters.test.ts (existing setPersonAvailability / updateDoc-assertion patterns; the brand-new-partner fixture `dean` with ZERO prior personQuarterData entry at ≈407-429; the `seedJuliaPairedWithLisa` helper where `dean` is absent from personQuarterData at ≈360-378)
  </read_first>
  <behavior>
    - Given partner P already HAS a personQuarterData[P] entry with roleTiers = { 'role-guitar': 'out' }, when another person adds P as a must-serve-with partner via setPersonAvailability, P's roleTiers is NOT erased — the reciprocal write uses the scoped `personQuarterData.${P}.pairedWith` sub-path, touching only the pairing array
    - Given partner P has NO prior personQuarterData entry at all (brand-new, first-time volunteer being paired in), the reciprocal write emits a COMPLETE, well-formed PersonQuarterData object with defaults (personId, blackoutDates: [], pairedWith: [personId], frequencyTier: 'regular', note: '') — NOT a `pairedWith`-only partial that would leave `blackoutDates` undefined
    - The reciprocal pairing link is still added in both cases (P's pairedWith gains the new personId)
    - The non-clobber guarantee (D-03 precedent) holds: unrelated fields on P's existing entry survive
  </behavior>
  <action>
    Fix the reciprocal must-serve-with partner write in `setPersonAvailability` (the `for (const partnerId of added)`
    block ≈193-204) that currently ALWAYS reconstructs a whole new `PersonQuarterData` for the partner
    carrying only personId/blackoutDates/pairedWith/frequencyTier/note — silently erasing the
    partner's existing `roleTiers` because this is a Firestore dot-path field replacement, not a
    merge.

    Branch the write on partner ENTRY EXISTENCE (`quarter.personQuarterData[partnerId]`). This is
    REQUIRED — the `added` loop, unlike the `removed` loop below it, has no natural entry-existence
    gate, so a partner can be added who has never had ANY prior personQuarterData entry. A uniform
    scoped `.pairedWith`-only write on such a partner makes `updateDoc` create a partial nested doc
    containing ONLY `pairedWith` (no `personId`, no `blackoutDates`), which then crashes the
    UNGUARDED downstream reads at `QuarterGrid.vue:294` (`personQuarterData[personId]?.blackoutDates.includes(date)`
    — the `?.` guards entry-absent, NOT `.blackoutDates`-undefined) and `scheduler.ts:85-86`
    (identical unguarded `.blackoutDates.includes(date)`). That partial-entry regression is worse than
    the data-loss bug being fixed here, so do NOT scope uniformly:

    - If `quarter.personQuarterData[partnerId]` ALREADY EXISTS → write ONLY the scoped
      `personQuarterData.${partnerId}.pairedWith` dot-path, appending `personId` to the partner's
      existing `pairedWith` (mirroring the `removed` loop's scoped sub-path). This leaves the
      partner's existing `roleTiers` and every other field intact — the whole point of the fix.
    - If it DOES NOT exist (brand-new partner) → keep writing a full, well-formed `PersonQuarterData`
      object under the `personQuarterData.${partnerId}` path with defaults exactly as today's code
      already does for that case: `personId: partnerId`, `blackoutDates: []`,
      `pairedWith: [personId]`, `frequencyTier: 'regular'`, `note: ''`. There is no `roleTiers` to
      preserve since none existed.

    Preserve the D-03-style non-clobber guarantee. Do NOT touch `applyCsvToQuarter` (out of scope).
    Extend quarters.test.ts per the acceptance criteria below (both branches).
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/quarters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existing-entry path: adding a partner who ALREADY had `personQuarterData[partnerId].roleTiers` emits ONLY a scoped `personQuarterData.${partnerId}.pairedWith` dot-path (no `personQuarterData.${partnerId}` whole-object key present in the updateDoc payload), leaving the partner's tuned roleTiers intact, and the reciprocal pairedWith link is added (test asserts on the emitted updateDoc updates payload)
    - Brand-new-partner path: adding a partner with ZERO prior `personQuarterData` entry (the `dean`-style fixture, see `src/stores/__tests__/quarters.test.ts:407-429` / `seedJuliaPairedWithLisa` ≈360-378 where `dean` is absent) emits a COMPLETE entry under `personQuarterData.${partnerId}` with `blackoutDates: []` initialized (NOT a `pairedWith`-only partial) plus `personId`, `pairedWith: [personId]`, `frequencyTier: 'regular'`, `note: ''` — so `isBlackedOut()` in `QuarterGrid.vue` and `scheduler.ts` cannot throw on that partner in a later read
    - `grep -n "roleTiers" src/stores/quarters.ts` shows the existing-entry reciprocal path avoids overwriting roleTiers (scoped sub-path)
    - `applyCsvToQuarter` is UNCHANGED (scope fence — CSV / applyCsvToQuarter paths remain untouched)
    - `npx vitest run` exits 0
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Reciprocal pairing save preserves an existing partner's tuned roleTiers via scoped sub-path (no silent data loss) AND seeds a brand-new partner with a complete blackoutDates-initialized entry (no partial-doc crash regression); non-clobber guarantee upheld; CSV path untouched; tests green.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 (full suite, no regressions from the 648-test baseline)
- `npm run type-check` exits 0
- `grep -n "roleTiers?\.\[" src/components/QuarterGrid.vue` matches
- `grep -n "roleTiers" src/components/AvailabilityRosterTable.vue` matches
- `grep -n "roleTiers" src/stores/quarters.ts` shows the reciprocal write preserves partner roleTiers
- `applyCsvToQuarter` in src/stores/quarters.ts is unchanged (CSV path out of scope)
</verification>

<success_criteria>
D-05's "per-quarter tier becomes per-role, consistently across the app" holds beyond the scheduler
and drawer:
- Manual-grid quick-assign excludes per-role 'out' candidates (per (person, role), not per person)
- The admin roster status table + "Out this quarter" filter reflect per-role tiers
- The reciprocal must-serve-with write never erases an existing partner's tuned roleTiers, and never
  emits a partial (blackoutDates-less) entry for a brand-new partner
All three surfaces mirror `scheduler.ts`'s canonical `roleTiers?.[roleId] ?? frequencyTier ?? 'regular'`
fallback chain, with test coverage proving per-role reconciliation and legacy back-compat.
</success_criteria>

<output>
Create `.planning/phases/15-per-role-frequency-role-categories/15-07-SUMMARY.md` when done
</output>
</output>
