---
phase: 51-service-order-editing-reliability
plan: 04
type: execute
wave: 2
depends_on: ["51-03"]
files_modified:
  - src/components/ServiceCard.vue
  - src/components/__tests__/ServiceCard.test.ts
  - src/stores/services.ts
  - src/stores/__tests__/services.test.ts
autonomous: true
requirements: [R112]
must_haves:
  truths:
    - "The Services listing card and the public share snapshot render slots in the SAME section-major order as the editor, including empty-bodied items (e.g. blank Miscellaneous), with no edit and no refresh."
    - "An empty-bodied item no longer sinks to the bottom until text is typed — order is deterministic regardless of whether a normalizing save has fired."
    - "No data migration; the Phase 41 share-refresh cadence (maybeRefreshShareLink/ensureShareLink) and the buildServiceSnapshot PII name-only resolution are unchanged."
  artifacts:
    - "src/components/__tests__/ServiceCard.test.ts — NEW file; asserts the listing renders section-major including an empty MISC item (Wave 0 gap)."
    - "src/components/ServiceCard.vue — opening/sending slices route through orderSlotsBySection."
    - "src/stores/services.ts — buildServiceSnapshot orders slots via orderSlotsBySection before mapping."
  key_links:
    - "Both read surfaces (listing card + share snapshot) route through orderSlotsBySection — the editor's single ordering contract from slotTypes.ts — so all three surfaces agree without a save."
---

<objective>
Fix R112: the Services listing and the public share link show items in a different (non-section-major) order than the edit screen, sinking empty-bodied items to the bottom until text is typed. Repro-test-first — commit FAILING tests proving each read surface renders raw persisted order, then route both through the editor's `orderSlotsBySection` contract.

Purpose: Confirmed root cause (51-RESEARCH.md): the editor regroups by section at render time, but `ServiceCard.vue:125-135` (listing) and `buildServiceSnapshot` (share) render the raw persisted array; new services persist in template/insertion order because `buildSlotsFromTemplate`/`createService` never call `orderSlotsBySection`. "Typing fixes it" because any edit triggers `onSave` -> `orderSlotsBySection` -> re-persist. Routing the read surfaces through the same contract makes them section-major deterministically, with NO data migration.
Output: One NEW listing test + one extended store test (both RED first) + the two read-surface fixes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-service-order-editing-reliability/51-RESEARCH.md
@.planning/phases/51-service-order-editing-reliability/51-CONTEXT.md
@CLAUDE.md
@src/components/ServiceCard.vue
@src/stores/services.ts
@src/utils/slotTypes.ts
@src/stores/__tests__/services.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — listing + share snapshot render raw order (new ServiceCard.test.ts + services.test.ts extension)</name>
  <read_first>
    - src/components/ServiceCard.vue:25-38 (opening/sending render templates) and 125-135 (`messageIndex`/`openingSlots`/`sendingSlots` slicing raw `props.service.slots`).
    - src/stores/services.ts:103-145 (`buildServiceSnapshot` — maps `service.slots` in raw order at line 106).
    - src/utils/slotTypes.ts:214-220 (`orderSlotsBySection` — identity-preserving; the target order) and `groupBySection`/`flattenBySection`.
    - src/stores/__tests__/services.test.ts (existing mocks + how store actions are exercised) for the `buildServiceSnapshot` case.
    - 51-RESEARCH.md § "R112 RED test".
  </read_first>
  <files>src/components/__tests__/ServiceCard.test.ts, src/stores/__tests__/services.test.ts</files>
  <action>
    Two RED tests using a shared fixture: a service whose `slots` array is DELIBERATELY NOT section-major — e.g. build slots in an insertion order where an empty-bodied MISC that belongs to the worship section appears LATER in the raw array than items from a trailing section (and include a MESSAGE so the listing's opening/sending split is exercised). The correct expected order is `orderSlotsBySection(fixture.slots)`.

    (a) NEW file `src/components/__tests__/ServiceCard.test.ts` (no test exists for ServiceCard — Wave 0 gap): mount `ServiceCard` with the fixture (mock the song/roster/auth stores it imports as the existing component tests do; ensure `.env.local` is present per CLAUDE.md or the Firebase-config import fails to load). Assert the rendered opening/sending slot order matches `orderSlotsBySection` — the empty MISC appears in its worship band, NOT at the bottom. RED today (renders raw order).

    (b) Extend `services.test.ts`: unit-test `buildServiceSnapshot(fixture)` and assert the returned `slots` are in `orderSlotsBySection` order. RED today (maps raw order). Assert ordering only — do NOT change assertions about `roleAssignments`/BPM.

    Per CONTEXT locked decision "Write a failing reproduction test FIRST".
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ServiceCard.test.ts src/stores/__tests__/services.test.ts</automated>
    Run the scoped command; BOTH new/extended tests must FAIL (RED) — listing and snapshot render raw, non-section-major order. Every pre-existing test in services.test.ts must still pass.
  </verify>
  <done>The new `ServiceCard.test.ts` and the extended `buildServiceSnapshot` test both exist and FAIL for the documented reason (raw persisted order sinks the empty item). No pre-existing test regresses. Committed as the RED baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — route listing + share snapshot through orderSlotsBySection</name>
  <behavior>
    - ServiceCard renders opening/sending slots in orderSlotsBySection order; the empty MISC sits in its worship band, not last.
    - buildServiceSnapshot returns slots in orderSlotsBySection order.
    - Already-section-major services are unchanged (orderSlotsBySection is identity-preserving — returns the same array reference).
    - roleAssignments PII name-only resolution and the share-refresh cadence are byte-unchanged.
  </behavior>
  <read_first>
    - src/components/ServiceCard.vue:62-70 (imports) and 125-135 (the computeds to change); note rows are keyed `:key="slot.position"` at 27/33 — positions are preserved by orderSlotsBySection and stay unique, so keys need no change.
    - src/stores/services.ts:26-31 (imports — `buildSlotsFromTemplate` already imported from `@/utils/slotTypes`) and 103-145 (`buildServiceSnapshot`).
    - src/utils/slotTypes.ts:214-220 (`orderSlotsBySection` identity-preservation guarantee).
    - 51-RESEARCH.md § "R112 fix" and § "Common Pitfalls / Pitfall 3 & Pitfall 4".
  </read_first>
  <files>src/components/ServiceCard.vue, src/stores/services.ts</files>
  <action>
    Listing — in `ServiceCard.vue`: add `import { orderSlotsBySection } from '@/utils/slotTypes'`. Introduce `const orderedSlots = computed(() => orderSlotsBySection(props.service.slots))`. Change `messageIndex` to find MESSAGE within `orderedSlots.value`, and `openingSlots`/`sendingSlots` (125-135) to slice from `orderedSlots.value` instead of `props.service.slots`. Do not change the `:key="slot.position"` bindings.

    Share — in `services.ts::buildServiceSnapshot` (line 103): add `orderSlotsBySection` to the existing `@/utils/slotTypes` import at line 28. At the top of the function body, compute `const orderedSlots = orderSlotsBySection(service.slots)` and map BPM over `orderedSlots` instead of `service.slots` (line 106). This is a pure transform of WHAT is serialized, inside the snapshot build only — it does NOT change WHEN/WHETHER the snapshot is written, so the Phase 41 `maybeRefreshShareLink`/`ensureShareLink` cadence is untouched (Pitfall 4). Do NOT touch the `roleAssignments` name-only PII resolution block (120-132).

    Do NOT add ordering into the editor's load/remote-merge watcher (Pitfall 3 — a fresh array reference there manufactures a false isDirty / non-converging merge). The read-surface fix alone satisfies R112 with no data migration; `orderSlotsBySection` being identity-preserving means already-ordered services incur no churn.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ServiceCard.test.ts src/stores/__tests__/services.test.ts && npm run type-check</automated>
    Both Task 1 RED tests now PASS (GREEN); all pre-existing services.test.ts tests still pass; `vue-tsc --build` is clean.
  </verify>
  <done>Listing and share snapshot render section-major including empty-bodied items, matching the editor, with no edit and no refresh; already-ordered services incur no churn; roleAssignments PII resolution and the Phase 41 refresh cadence are unchanged. `npm run type-check` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public share link (unauthenticated reader) ← share snapshot doc | The snapshot is world-readable; it must expose slot data + names only, never raw Person PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-04-01 | Information Disclosure | `buildServiceSnapshot` PII guard | medium | mitigate | The change reorders `service.slots` only and maps BPM over the ordered array; the `roleAssignments` name-only resolution (nameById map, 120-132) is not touched. Existing PII test remains green. |
| T-51-04-02 | Tampering | autosave/isDirty stability | medium | mitigate | Ordering is applied in the read surfaces only; `orderSlotsBySection` is identity-preserving, and the editor load/merge watcher is deliberately NOT touched (Pitfall 3). |
| T-51-04-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/ServiceCard.test.ts src/stores/__tests__/services.test.ts` — green (RED repros now passing).
- Phase gate before /gsd-verify-work: full app suite `npx vitest run --dir src --exclude '**/rules.test.ts'` (or bare `npx vitest run`) with ONLY the 2 known-baseline files red (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`), plus `npm run type-check` clean.
- Manual confirmation deferred to /gsd-verify-work: create a service with two blank Miscellaneous items; view the Services listing and open the share link; confirm order matches the edit screen without editing.
</verification>

<success_criteria>
- The Services listing and the public share link show every item in the same order as the edit screen, including empty-bodied items, without a refresh (R112, ROADMAP success criteria 3 & 4).
- No data migration; the Phase 41 share-refresh path and the snapshot PII guard are preserved.
- The RED repros were committed before the fix.
</success_criteria>

<output>
Create `.planning/phases/51-service-order-editing-reliability/51-04-SUMMARY.md` when done.
</output>
