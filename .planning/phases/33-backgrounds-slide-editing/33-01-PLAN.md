---
phase: 33-backgrounds-slide-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/slideGroup.ts
  - src/types/songLyrics.ts
  - src/types/slide.ts
  - src/utils/slideshowAssembler.ts
  - src/utils/__tests__/slideshowAssembler.test.ts
autonomous: true
requirements: [R055, R056, R057]

must_haves:
  truths:
    - "Setting a group background never overwrites or clears any slide's own backgroundImageUrl — the slide keeps winning."
    - "A group with zero slides accepts and stores a background; nothing renders until a slide exists."
    - "A group background is a single scalar, not a list — there is no ordering to specify."
    - "When a slide and its group both define a background, the slide's wins, deterministically."
    - "With no background defined at any level, resolution yields undefined — no chip, no render, no placeholder."
    - "The cascade is evaluated in the fixed order slide then group then song; the first defined value wins."
    - "A service-specific group background beats the song's, so a song background never overrides a deliberate per-service choice."
    - "★ Resolution for a group with no owning song (PRAYER / SCRIPTURE / IMPORTED) skips the song tier without throwing."
    - "★ A video entry resolves its background through the ordinary cascade — the audio carve-out is NOT copied to background."
    - statement: "A song background change propagates to every service using that song without a per-service write."
      verification: backstop
  artifacts:
    - "src/types/slideGroup.ts — GroupSlideEntry.backgroundImageUrl?: string and SlideGroup.backgroundImageUrl?: string"
    - "src/types/songLyrics.ts — SongLyrics.backgroundImageUrl?: string"
    - "src/types/slide.ts — SlideBase.backgroundImageUrl?: string and SlideBase.backgroundSource?: 'slide' | 'group' | 'song'"
    - "src/utils/slideshowAssembler.ts — resolveEntryMedia gains a song parameter and a background cascade; emitFromGroup supplies the song"
    - "src/utils/__tests__/slideshowAssembler.test.ts — a background describe block covering the cascade, the absent-song tier, and the video divergence"
  key_links:
    - "emitFromGroup looks up inputs.songLyricsById ONLY for lyric/copyright source kinds and passes undefined for every other kind"
    - "AssembledSlide.slide.backgroundImageUrl / .backgroundSource are the single source every UI surface in this phase reads — no component re-derives the cascade"
---

<objective>
Add the three stored `backgroundImageUrl` fields and the resolved background cascade to the
assembler, so every later plan in this phase reads one already-resolved value instead of
re-deriving precedence.

Purpose: R055/R056/R057's "most specific wins" behaviour is a data question, not a UI question.
Settling it in `resolveEntryMedia` first means the drawer, the card chip, the group control and the
song control are all pure consumers.
Output: three type files carrying the new fields, an extended `resolveEntryMedia`, and an assembler
test block that proves the cascade including the two paths this phase is most likely to get wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-backgrounds-slide-editing/33-CONTEXT.md
@.planning/phases/33-backgrounds-slide-editing/33-RESEARCH.md
@.planning/phases/33-backgrounds-slide-editing/33-PATTERNS.md
@.planning/phases/33-backgrounds-slide-editing/33-UI-SPEC.md
@.planning/phases/33-backgrounds-slide-editing/33-VALIDATION.md
</context>

## Premise corrections carried into this phase

1. **The Claude Design wireframes do not cover this phase.** `docs/design/slides-tab.dc.html` (pulled
   2026-07-25) contains no 3-dot menu, no "Edit details"/"Edit lyrics", and no background feature —
   all 106 `background` matches are CSS declarations. The ROADMAP's instruction to "confirm against
   the Claude Design wireframes at plan time which drawer a given slide's 3-dot menu opens" is a
   **false premise**. Everything new in this phase is **original design work**, settled in
   `33-UI-SPEC.md`. Do not go looking for wireframes that do not exist.
2. **R052's premise is inaccurate and its intent is binding.** `EditSlideDrawer.vue` as shipped has
   sections, not tabs. Do not build tabs in order to remove them — split the existing sections across
   two modes.
3. **`33-UI-SPEC.md`'s `## UI Considerations` header arithmetic is off by one against its own
   tables.** The header says "23 covered, 8 backstop, 1 unresolved"; counting the four tables gives
   **24 covered, 7 backstop, 1 unresolved = 32**. This plan set authors all 32 to the table counts
   (E1 6/2, E2 6/2, E3 5/2/1, E4 7/1). Recorded so the difference reads as an arithmetic correction,
   never as a dropped item.

## Multi-source coverage audit (phase 33)

| Source | Item | Covered by |
|---|---|---|
| GOAL | Background settable at group, slide and song level, most specific wins | 33-01, 33-03, 33-06, 33-07, 33-08 |
| GOAL | Slide editing moves to an explicit 3-dot menu with type-appropriate options | 33-02, 33-05, 33-08, 33-09 |
| REQ | R051 slide enters edit mode only via the 3-dot menu | 33-02, 33-05, 33-09 |
| REQ | R052 "Edit details" and "Edit lyrics" as separate drawers | 33-02, 33-07, 33-09 |
| REQ | R055 group background | 33-01, 33-03, 33-08 |
| REQ | R056 slide background, most specific wins | 33-01, 33-05, 33-07 |
| REQ | R057 song background from the Song Lyrics editor | 33-01, 33-03, 33-06 |
| REQ | R058 per-slide audio loses its whole-group scope | 33-04 |
| REQ | R063 options vary by service-item type | 33-02, 33-05, 33-08 |
| RESEARCH | Extend `resolveEntryMedia`, never a second resolver | 33-01 |
| RESEARCH | Pitfall 1 — background is NOT carved out for video | 33-01 |
| RESEARCH | Pitfall 3 — non-SONG groups have no `SongLyrics` | 33-01 |
| RESEARCH | Pitfall 2 — the drawer test file is not split into two files | 33-04, 33-07 |
| RESEARCH | Pitfall 4 — no `firestore.rules` change | all (explicitly out of scope) |
| RESEARCH | RQ1 — `orgs/{orgId}/backgrounds/**`, no `storage.rules` change | 33-03 |
| RESEARCH | RQ3 — the removed field has exactly one write surface and no executable reader | 33-04 |
| RESEARCH | RQ5 — `SlidesTab.vue:255-258` is the single coupling point | 33-09 |
| RESEARCH | `SlideActionMenu` is the codebase's first real ARIA menu | 33-02 |
| CONTEXT | Clicking a slide still SELECTS | 33-05, 33-09 |
| CONTEXT | One shared `SlideActionMenu.vue`, props-driven | 33-02 |
| CONTEXT | Song groups get a menu with a reduced item set | 33-02, 33-08 |
| CONTEXT | One component with a `mode` prop, not two components | 33-07 |
| CONTEXT | Only one drawer open at a time | 33-07, 33-09 |
| CONTEXT | Per-type item list is a pure function | 33-02 |
| CONTEXT | The removed audio-scope field goes outright, no deprecation | 33-04 |
| CONTEXT | Provenance is tracked so an override is never invisible | 33-01, 33-05, 33-07, 33-08 |
| CONTEXT | Upload mechanics are Claude's discretion | 33-03 |

**Deferred (excluded, not gaps):** R053 (Phase 36), keyboard reordering of slides, background
rendering fidelity when presented (Phase 35), backgrounds at service/section level.

## Artifacts this phase produces

**New files (6)**

| Path | Owner plan |
|---|---|
| `src/components/slides/SlideActionMenu.vue` | 33-02 |
| `src/components/slides/__tests__/SlideActionMenu.test.ts` | 33-02 |
| `src/composables/useBackgroundUpload.ts` | 33-03 |
| `src/composables/__tests__/useBackgroundUpload.test.ts` | 33-03 |
| `src/components/slides/BackgroundControl.vue` | 33-03 |
| `src/components/slides/__tests__/BackgroundControl.test.ts` | 33-03 |

**New exported symbols**

| Symbol | File | Owner |
|---|---|---|
| `MenuItemKey`, `MenuItem`, `slideActionMenuItems()`, `backgroundImageLabel()` | `src/components/slides/slideDisplay.ts` | 33-02 |
| `BACKGROUND_MAX_BYTES`, `UseBackgroundUploadReturn`, `useBackgroundUpload()` | `src/composables/useBackgroundUpload.ts` | 33-03 |
| `setSongBackground()` | `src/stores/songLyrics.ts` | 33-06 |
| `GroupBackgroundPatch`, `setGroupBackground()` | `src/stores/slideGroups.ts` | 33-08 |

**Type fields ADDED**

| Field | File | Owner |
|---|---|---|
| `GroupSlideEntry.backgroundImageUrl?: string` | `src/types/slideGroup.ts` | 33-01 |
| `SlideGroup.backgroundImageUrl?: string` | `src/types/slideGroup.ts` | 33-01 |
| `SongLyrics.backgroundImageUrl?: string` | `src/types/songLyrics.ts` | 33-01 |
| `SlideBase.backgroundImageUrl?: string` | `src/types/slide.ts` | 33-01 |
| `SlideBase.backgroundSource?: 'slide' \| 'group' \| 'song'` | `src/types/slide.ts` | 33-01 |

**Type field REMOVED**

| Field | File | Owner |
|---|---|---|
| the per-slide audio scope field on `GroupSlideEntry` | `src/types/slideGroup.ts` | 33-04 |

**New props / emits**

| Component | Props added | Emits added | Owner |
|---|---|---|---|
| `SlideActionMenu.vue` | `entryId`, `items`, `open` | `toggle`, `select` | 33-02 |
| `BackgroundControl.vue` | `imageUrl?`, `caption`, `inheritedFrom?`, `isEditor`, `orgId` | `attach`, `remove` | 33-03 |
| `SlideCard.vue` | `menuItems?`, `menuOpen?` | `menu-toggle`, `menu-select` | 33-05 |
| `SlideGrid.vue` | — | `menu-action` | 33-08 |
| `EditSlideDrawer.vue` | `mode`, `pendingAction` | `pending-action-consumed` | 33-07 |
| `EditSlideDrawer.vue` | — | `edit-in-scripture` **removed** | 33-09 |

**New `data-testid` values**

| Value | Owner |
|---|---|
| `slide-action-menu`, `slide-action-trigger-{entryId}`, `slide-action-panel-{entryId}`, `slide-action-item-{key}` | 33-02 |
| `background-control`, `background-control-image`, `background-control-filename`, `background-control-caption`, `background-control-inherited`, `background-control-add`, `background-control-input`, `background-control-remove`, `background-control-upload-progress`, `background-control-upload-error` | 33-03 |
| `audio-scope-hint` | 33-04 |
| `slide-card-background-chip` | 33-05 |
| `song-background-row` | 33-06 |
| `drawer-background-section`, `background-attach`, `background-attach-input`, `background-provenance`, `background-set-override`, `background-thumbnail`, `background-filename`, `background-remove`, `background-remove-caption`, `background-upload-progress`, `background-upload-error` | 33-07 |
| `slide-grid-group-background` | 33-08 |

## Deliberate deletions (phase-wide — none of these is breakage)

| Deletion | Owner | Why it is intentional |
|---|---|---|
| The per-slide audio scope field on `GroupSlideEntry` | 33-04 | R058 removes the feature; D-19 forbids deprecation shims in this area |
| `EditSlideDrawer.vue`'s scope pill block, its two class constants, its whole-group attach function, its scope branch, and its reset line | 33-04 | Same — the group music control above the grid is the one remaining entry point |
| `src/stores/slideGroups.ts`'s "Open Question 1 resolved" doc-comment paragraph | 33-04 | ★ It is a COMMENT, not an executable branch — see 33-04's correction |
| `EditSlideDrawer.test.ts`'s `:813-957` describe block, wholesale | 33-04 | Its entire subject is gone; adapting it would test nothing |
| `SlidesTab.vue`'s `drawerOpen.value = true` line inside `onSelectSlide` — one line only | 33-09 | R051's whole change. Selection itself is load-bearing and stays |
| The drawer's in-body "Edit in song" / "Edit in scripture" link buttons, their handlers, and the `edit-in-scripture` emit | 33-09 | Superseded by the menu items; keeping both duplicates one action in two places |
| `SlideCard.vue`'s root `<button>` element | 33-05 | Replaced by `<div role="button" tabindex="0">` so the menu trigger can legally nest |

<tasks>

<task type="auto">
  <name>Task 1: Add the three stored background fields and the two resolved fields</name>
  <read_first>
    - src/types/slideGroup.ts (the `SlideGroup` and `GroupSlideEntry` interfaces, lines 34-73)
    - src/types/songLyrics.ts (the `SongLyrics` interface, lines 36-59)
    - src/types/slide.ts (the `SlideBase` interface, lines 12-35; `AssembledSlide`, lines 120-146)
    - .planning/phases/33-backgrounds-slide-editing/33-UI-SPEC.md § Design System — "The resolution/provenance shape"
    - .planning/phases/33-backgrounds-slide-editing/33-PATTERNS.md § `src/types/*` (modified)
  </read_first>
  <files>src/types/slideGroup.ts, src/types/songLyrics.ts, src/types/slide.ts</files>
  <action>
Add `backgroundImageUrl?: string` to `GroupSlideEntry` (sibling to `audioUrl`) and to `SlideGroup`
(sibling to `bedAudioUrl`) in `src/types/slideGroup.ts`. Add `backgroundImageUrl?: string` to
`SongLyrics` in `src/types/songLyrics.ts` — greenfield, no migration, per D-19 as extended to the
song-lyrics structures.

Add BOTH `backgroundImageUrl?: string` and `backgroundSource?: 'slide' | 'group' | 'song'` to
`SlideBase` in `src/types/slide.ts`, NOT to `AssembledSlide`.

★ Planner decision resolving a notation difference between artifacts: `33-UI-SPEC.md`'s Design
System table names these `AssembledSlide.backgroundImageUrl` / `AssembledSlide.backgroundSource`,
while `33-PATTERNS.md`'s authoritative `SlideCard.vue` excerpt reads
`props.assembledSlide.slide.backgroundSource`. Both fields go on `SlideBase`, so every consumer
reaches them as `assembledSlide.slide.backgroundImageUrl` / `assembledSlide.slide.backgroundSource`.
Rationale: the two fields are one logical pair and must not be split across two objects the way
`audioUrl` (on `SlideBase`) and `audioFromBed` (on `AssembledSlide`) historically were. Do not also
mirror them onto `AssembledSlide`.

`backgroundSource` is a single tri-state field, never two booleans — a 3-level cascade needs three
mutually exclusive states, and two independent booleans can encode an impossible one. Document
`undefined` as "nothing resolved at any level".

Doc-comment each new field the way its audio sibling is documented in the same file: name the level
it belongs to, name the requirement (R055/R056/R057), and for `SlideBase` state that the value is
resolved by `resolveEntryMedia` and never persisted standalone.
  </action>
  <acceptance_criteria>
    - `grep -c 'backgroundImageUrl' src/types/slideGroup.ts` returns 2
    - `grep -c 'backgroundImageUrl' src/types/songLyrics.ts` returns 1
    - `grep -c "backgroundSource" src/types/slide.ts` returns at least 1, and the declaration reads exactly `backgroundSource?: 'slide' | 'group' | 'song'`
    - `grep -c 'backgroundImageUrl\|backgroundSource' src/types/slide.ts` shows both fields inside the `SlideBase` interface body, not inside `AssembledSlide`
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>All five fields exist with the exact names and the exact `backgroundSource` union above; `vue-tsc --build` is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend resolveEntryMedia with the slide/group/song cascade</name>
  <read_first>
    - src/utils/slideshowAssembler.ts lines 195-235 (`ResolvedGroupMedia`, `resolveEntryMedia`, and the video carve-out's doc comment)
    - src/utils/slideshowAssembler.ts lines 265-300 (`emitFromGroup`, the single call site at :279)
    - src/utils/slideshowAssembler.ts lines 39-45 (`AssemblyInputs.songLyricsById`)
    - src/utils/__tests__/slideshowAssembler.test.ts (existing fixture helpers and describe-block style)
    - .planning/phases/33-backgrounds-slide-editing/33-RESEARCH.md § Pitfall 1 and § Pitfall 3
    - .planning/phases/33-backgrounds-slide-editing/33-UI-SPEC.md § Phase-Specific Component Contracts §9
  </read_first>
  <files>src/utils/slideshowAssembler.ts, src/utils/__tests__/slideshowAssembler.test.ts</files>
  <behavior>
    - Entry has its own background, group and song also have one → resolved url is the entry's, `backgroundSource` is `'slide'`.
    - Entry has none, group has one, song has one → resolved url is the group's, `backgroundSource` is `'group'`.
    - Entry and group have none, song has one → resolved url is the song's, `backgroundSource` is `'song'`.
    - Nothing set at any level → both fields absent from the emitted slide (never present-and-undefined).
    - ★ A group with NO owning song (a PRAYER / SCRIPTURE / IMPORTED group, where the song argument is `undefined`) resolves group-then-nothing and does not throw.
    - ★ A `video`-kind entry with no background of its own, inside a group that has one, resolves `backgroundSource: 'group'` — while the SAME entry still resolves no bed audio (`audioFromBed` false, no `audioUrl`). Both assertions live in one test so the asymmetry is impossible to miss.
    - A group with `backgroundImageUrl` set and an empty `slides` array emits no slides and throws nothing.
    - An entry carrying a stale unknown extra property alongside no `audioUrl`, in a group with `bedAudioUrl`, still resolves `audioUrl` from the bed with `audioFromBed` true.
  </behavior>
  <action>
Change `resolveEntryMedia`'s signature to `resolveEntryMedia(group: SlideGroup, entry: GroupSlideEntry,
song: SongLyrics | undefined): ResolvedGroupMedia` and add `backgroundImageUrl?: string` plus
`backgroundSource?: 'slide' | 'group' | 'song'` to the module-private `ResolvedGroupMedia` interface.

★ Pitfall 1 — compute the background BEFORE the `sourceRef.kind === 'video'` early return, and return
the computed background from that early-return branch too. A video entry must still receive a
resolved background. Add a short comment at the early return recording that the audio suppression is
deliberately not extended to background, and cite `33-UI-SPEC.md` §9 as the reasoning so a future
reviewer finds it before "fixing" the asymmetry into false consistency. The audio half of that branch
is unchanged: a video entry still resolves no bed audio.

★ Pitfall 3 — use optional chaining on the song tier. Non-SONG groups (PRAYER / SCRIPTURE / MESSAGE /
HYMN / IMPORTED) have no `SongLyrics` document at all, and the song argument is legitimately
`undefined` for them.

Resolution order is slide, then group, then song; the first defined value wins. Set
`backgroundSource` to the tier that actually supplied the value, and leave both fields unset when no
tier did. Follow the existing conditional-assignment idiom in this function (`if (resolvedAudioUrl)
media.audioUrl = ...`) so an unset field is genuinely absent rather than present-and-undefined —
`stripUndefined` is not in this path.

At the `emitFromGroup` call site (`:279`), look the song up only for source kinds that carry a
`songId` — `lyric` and `copyright` — via `inputs.songLyricsById.get(entry.sourceRef.songId)`, and
pass `undefined` for every other kind. Spread the two resolved background fields onto the emitted
slide object using the same conditional-spread idiom the audio fields already use immediately above.

Then add a `background` describe block to `src/utils/__tests__/slideshowAssembler.test.ts` covering
every case in this task's behavior list. Follow the file's existing fixture-builder style; do not
introduce a new mocking approach.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts -t "background"` passes with at least 8 assertions and 0 failures
    - The non-SONG-group case is its own named test whose title contains `PRAYER` or `no owning song`, and it passes without a thrown error — this criterion is satisfied only by a test that constructs a group whose entries' `sourceRef.kind` is never `lyric` or `copyright`
    - The video case is a single test asserting BOTH `slide.backgroundSource === 'group'` AND that the same emitted slide has no `audioUrl` and `audioFromBed` is falsy
    - `grep -n "sourceRef.kind === 'video'" src/utils/slideshowAssembler.ts` shows the early return, and the returned object literal on that line's branch includes a background field
    - `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts` passes in full (no pre-existing case regressed)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideshowAssembler.test.ts && npm run type-check</automated>
  </verify>
  <done>The cascade resolves slide → group → song with a tri-state provenance field; a video entry gets a background but no bed audio; a group with no song resolves without throwing; the whole assembler suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore document → client resolver | Stored `backgroundImageUrl` strings at three levels are attacker-influenceable only by an authenticated org member; they flow into this pure resolver and out to UI surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Denial of Service | `resolveEntryMedia` | medium | mitigate | The absent-song tier is dereferenced with optional chaining; a dedicated non-SONG-group test proves assembly of a PRAYER/SCRIPTURE group does not throw, so one malformed group cannot blank the whole slideshow |
| T-33-02 | Information Disclosure | `SlideBase.backgroundImageUrl` | low | accept | The resolved value is a Firebase Storage download URL already readable by any org member under the existing `orgs/{orgId}/{allPaths=**}` rule; this phase adds no new reader class. Cross-org access to uploaded background images is canon access-control/IDOR territory owned by `storage.rules` and code review, not by this phase |
| T-33-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — this phase installs zero new dependencies (`33-RESEARCH.md` § Package Legitimacy Audit records N/A). No install task exists in any plan of this phase |
</threat_model>

<verification>
- `npm run type-check` (the `vue-tsc --build` form — the `-p tsconfig.app.json` form silently skips
  test files and is NOT sufficient evidence) exits 0.
- `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts` is fully green.
- `npx vitest run src/components/slides` is unchanged from its pre-plan result (this plan touches no
  component).
</verification>

<success_criteria>
- All five new type fields exist with the exact names in this plan's artifact table.
- `resolveEntryMedia` is the ONLY function that resolves a background — no second resolver exists
  anywhere under `src/`.
- The video divergence and the absent-song tier each have their own passing test.
- The full assembler suite passes with no regression to existing audio cases.
</success_criteria>

<output>
Create `.planning/phases/33-backgrounds-slide-editing/33-01-SUMMARY.md` when done.
</output>
