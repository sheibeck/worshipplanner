---
phase: 102-gated-scripture-fetch-dispatcher
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/index.ts
  - functions/src/index.test.ts
autonomous: true
requirements: [R297]
user_setup:
  - service: firebase-functions
    why: "This plan tightens the api proxy so esv/nlt deny disabled orgs. Deploy is DEFERRED to owner-gated milestone-end batch — build/test/commit only this phase."
    dashboard_config:
      - task: "DO NOT deploy in this phase. When the owner approves the milestone-end batch, deploy with `firebase deploy --only functions:api`. Because bibleApiEnabled defaults OFF, deploying this gate denies esv/nlt for every org not yet explicitly enabled — this is intended and must be sequenced with enabling Berean (and any other org) via setOrgBibleEnabled."
        location: "owner-gated, milestone end"

must_haves:
  truths:
    - "The api proxy's /api/esv and /api/nlt branches reject a fetch when the caller's org has bibleApiEnabled !== true, independent of the client dispatcher (defense-in-depth, R297)"
    - "An authenticated caller with no orgId claim is rejected on esv/nlt (mirrors the anthropic org-required 403), never falling through to the upstream fetch (R297)"
    - "A missing org doc / missing field defaults to DENY, and a Firestore read error FAILS CLOSED — no disabled org can spend ESV/NLT quota"
    - "The anthropic branch, rate limiter, ledger, and quota logic are byte-unchanged"
  artifacts:
    - functions/src/index.ts
    - functions/src/index.test.ts
  key_links:
    - "esv/nlt branch → resolveOrgId(decodedCaller) → checkOrgBibleEnablement(getFirestore(), orgId) → 403/503 reject BEFORE the upstream fetch"
    - "checkOrgBibleEnablement live-reads organizations/{orgId}.bibleApiEnabled (default false), fail-closed on read error — mirrors checkOrgAiEnablement 1:1"
---

<objective>
Add the server-side half of R297 (defense-in-depth): the `api` proxy currently gates ONLY the `anthropic` branch per-org (`checkOrgAiEnablement`, ~line 634); the `esv` and `nlt` branches fall straight through, per-org-ungated. Introduce `checkOrgBibleEnablement(db, orgId)` mirroring `checkOrgAiEnablement` and apply it to the esv/nlt branches so a disabled org's ESV/NLT proxy request is refused even if a client bypasses the Plan-01 dispatcher.

Purpose: R297 requires the gate enforced "independently at the server esv/nlt proxy branches." The client gate (Plan 01) is a cost/UX guard; this is the real security control the owner asked to be enforced, not just UI-hidden.

Output: `checkOrgBibleEnablement` + its unit tests (mirroring the checkOrgAiEnablement describe block), and the esv/nlt branches of the `api` onRequest handler wired to reject a disabled/org-less caller before the upstream fetch. NO deploy — build/test/commit only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-gated-scripture-fetch-dispatcher/102-CONTEXT.md
@.planning/ROADMAP.md
@functions/src/index.ts
@functions/src/index.test.ts

# CRITICAL project rules — read before running any gate (functions test/build commands)
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checkOrgBibleEnablement + unit tests (mirror checkOrgAiEnablement)</name>
  <files>functions/src/index.ts, functions/src/index.test.ts</files>
  <behavior>
    - ALLOW: an org with bibleApiEnabled:true returns { ok: true }.
    - DENY (403): bibleApiEnabled:false → { ok: false, status: 403, error: { error: <Bible-specific message> } }.
    - DENY (403): field entirely ABSENT (every pre-Phase-101 org, default OFF) → same 403.
    - DENY (403): nonexistent org doc → same 403, never throws.
    - FAIL-CLOSED (503): a Firestore read error → { ok: false, status: 503, error: { error: <Bible-specific retry message> } } (deliberate departure from the rate limiter's fail-open, exactly like checkOrgAiEnablement).
    - reads exactly organizations/{orgId} — the exact orgId argument, no other collection.
  </behavior>
  <action>
In `functions/src/index.ts`, add `checkOrgBibleEnablement(db: Firestore, orgId: string)` mirroring `checkOrgAiEnablement` (~364) 1:1 — same structure, same live `db.collection("organizations").doc(orgId).get()`, same fail-closed try/catch. Reuse the existing result shape: add `export type OrgBibleEnablementResult = OrgAiEnablementResult;` (the `{ ok: true } | { ok: false, status, error }` union at ~335-343) so no redundant type is introduced, and annotate the function's return with it. Read `bibleApiEnabled` (default `?? false`); deny with status 403 and a Bible-specific message (e.g. "Bible API features are disabled for your organization.") unless `bibleApiEnabled === true`; on a caught read error return status 503 with a Bible-specific retry message (e.g. "Could not verify Bible availability. Try again shortly."). `export` the function so the test can import it. Do NOT touch checkOrgAiEnablement.

In `functions/src/index.test.ts`, add `checkOrgBibleEnablement` to the import list (~53) and add a `describe("checkOrgBibleEnablement ...")` block that copies the `checkOrgAiEnablement` block's `mockOrgDb` harness (~4048-4113) and asserts every case in the behavior block, reading `bibleApiEnabled` instead of `aiMasterEnabled`. This unit test IS the "esv/nlt proxy branch denies when disabled / allows when enabled" coverage — the `api` onRequest handler has no existing test harness (Assumption A2), so the extracted gate function is the testable seam, exactly as the anthropic gate is tested via checkOrgAiEnablement rather than the handler.
  </action>
  <verify>
    <automated>cd functions && npm run build && npx vitest run src/index.test.ts -t "checkOrgBibleEnablement"</automated>
  </verify>
  <done>checkOrgBibleEnablement is exported and typed; its describe block passes all six cases (ALLOW / DENY-false / DENY-absent / DENY-nonexistent / FAIL-CLOSED-503 / reads-exact-org); functions build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the esv/nlt proxy branches with checkOrgBibleEnablement</name>
  <files>functions/src/index.ts</files>
  <action>
In the `api` onRequest handler, after the `anthropic` cost-control block closes (~691) and BEFORE the `try { const upstream = await fetch(...) }` (~693), add a new block guarded by `if (service === "esv" || service === "nlt")`. Inside it, mirror the anthropic branch's org-resolution + enablement reject (~629-638):

- `const callerOrgId = resolveOrgId(decodedCaller!);` — `decodedCaller` is non-null here because esv and nlt are both in SECRET_INJECTED, so the auth gate at ~564-571 already returned 401 for a null caller (same invariant the anthropic branch relies on).
- If `!callerOrgId`, `res.status(403).json({ error: "Bible API features require an organization." }); return;` (mirrors the anthropic org-required 403 at ~630-632 — an org-less token must never reach the upstream fetch un-gated).
- `const bibleVerdict = await checkOrgBibleEnablement(getFirestore(), callerOrgId);` then `if (!bibleVerdict.ok) { res.status(bibleVerdict.status).json(bibleVerdict.error); return; }`.

Do NOT modify the `anthropic` branch, the rate limiter, the usage ledger, the appConfig read, or the quota logic. planningcenter (not a Bible service, not in this condition) continues to fall through ungated exactly as before. This block runs before the shared `fetch(upstreamUrl, ...)`, so a denied org produces zero ESV/NLT proxy requests.
  </action>
  <verify>
    <automated>cd functions && npm run build && npm test</automated>
  </verify>
  <done>functions build clean; full functions suite green (baseline is all-pass per Phase 101 — 627/627); the esv/nlt branches now call checkOrgBibleEnablement and reject a disabled/org-less caller before the upstream fetch; anthropic/rate-limiter/ledger/quota untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/esv, /api/nlt (onRequest) | untrusted caller crosses into a secret-injected, org-metered proxy |
| proxy → organizations/{orgId} | live enablement read is the enforcement source, not the (staleable) orgId claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-102-02 | Elevation / Tampering | a caller bypasses the Plan-01 client dispatcher and hits /api/esv or /api/nlt directly for a disabled org, spending ESV/NLT quota | high | mitigate | checkOrgBibleEnablement live-reads organizations/{orgId}.bibleApiEnabled and rejects (403) before the upstream fetch; independent of any client gate |
| T-102-03 | Spoofing | an authenticated but org-less token reaches the paid esv/nlt proxy | medium | mitigate | resolveOrgId null → 403 "Bible API features require an organization." before the fetch (mirrors the anthropic org-required reject) |
| T-102-04 | Denial of Service | a Firestore hiccup on the enablement read silently lets a disabled org through | medium | mitigate | fail-closed (503) on read error — deliberate departure from the rate limiter's fail-open, matching checkOrgAiEnablement |
| T-102-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan (adds one function + wiring to existing functions/src/index.ts) — no legitimacy gate required |
</threat_model>

<verification>
Run after both tasks:

- Functions build + full suite (CLAUDE.md — `cd functions && npm run build && npm test`; baseline is all-pass, 627/627 from Phase 101, plus this plan's new checkOrgBibleEnablement cases): clean build, no failures.
- Root type gate (CLAUDE.md — the `--build` form; functions changes must not regress the root typecheck): `npm run type-check` clean.
- Wiring proof: `grep -n "checkOrgBibleEnablement" functions/src/index.ts` shows the definition plus a call inside the esv/nlt branch of the `api` handler.

DEPLOY IS DEFERRED (do NOT run `firebase deploy` this phase). Hand-over note for the owner-gated milestone-end batch: `firebase deploy --only functions:api`. Because bibleApiEnabled defaults OFF, deploying this gate denies esv/nlt for every not-yet-enabled org — intended, and exactly why it is sequenced with enabling Berean via setOrgBibleEnabled.
</verification>

<success_criteria>
- R297 (server defense-in-depth): the `api` proxy's esv and nlt branches independently enforce the per-org gate — a disabled org (or org-less caller) is rejected with an appropriate HTTP status before any upstream ESV/NLT request.
- checkOrgBibleEnablement mirrors checkOrgAiEnablement: default-OFF deny, fail-closed 503, reads exactly organizations/{orgId} — proven by its unit-test describe block.
- The anthropic branch, rate limiter, usage ledger, and quota are unchanged; full functions suite stays green.
- No `firebase deploy` executed; the exact deferred deploy command is documented for owner-gated batch.
</success_criteria>

<output>
Create `.planning/phases/102-gated-scripture-fetch-dispatcher/102-02-SUMMARY.md` when done.
</output>
