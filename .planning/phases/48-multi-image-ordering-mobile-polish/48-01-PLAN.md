---
phase: 48-multi-image-ordering-mobile-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/slides/dropRouting.ts
  - src/components/slides/__tests__/dropRouting.test.ts
  - src/components/GettingStarted.vue
  - src/components/__tests__/GettingStarted.test.ts
autonomous: true
requirements: [R098, R103]

must_haves:
  truths:
    - "Dropping slide2.png, slide10.png, slide1.png through classifyFiles returns images in natural order [slide1, slide2, slide10] (numeric collation, not lexicographic slide1/slide10/slide2) — R098"
    - "resolveDrop surfaces the SAME natural order via its .images field — R098"
    - "Only the images bucket is sorted; decks, videos and audioFiles preserve their drop order unchanged — R098"
    - "The Getting Started panel shows a dismiss × control that hides the panel and persists the dismissal across reloads — R103"
    - "Dismissing before finishing steps hides the panel even though allDone is false; the two conditions are independent — R103 (UI Considerations: present/absent)"
    - "An already-dismissed panel does not flash-then-hide on first paint — the localStorage read is synchronous at setup() — R103 (UI Considerations: loading/race)"
  artifacts:
    - "src/components/slides/dropRouting.ts — Intl.Collator natural sort on the images bucket inside classifyFiles"
    - "src/components/slides/__tests__/dropRouting.test.ts — new numeric-collation cases for classifyFiles AND resolveDrop"
    - "src/components/GettingStarted.vue — dismiss control + localStorage-seeded dismissed ref"
    - "src/components/__tests__/GettingStarted.test.ts — NEW test file (Wave 0 gap)"
  key_links:
    - "classifyFiles is the single sort point every consumer (resolveDrop and any direct caller) reads from — sort in-place before the return"
    - "Root v-if becomes !allDone && !dismissed — composes Firestore-driven allDone with localStorage-driven dismissed"
    - "localStorage key wp:gettingStartedDismissed (flat, unscoped, matching CollapsibleSection.vue's precedent)"
---

<objective>
Deliver the two independent, self-contained fixes of Phase 48: deterministic multi-image import
ordering (R098) and a dismissible Getting Started onboarding panel (R103). Neither shares any file
or type with the other work in this phase, so they land together in one Wave 1 plan.

Purpose: R098 removes a real ordering defect (multi-image drops currently land in the browser's raw
DataTransfer/filesystem order, so slide10 sorts before slide2). R103 lets an editor dismiss the
dashboard onboarding panel permanently on their device.

Output (artifacts this plan produces): the `Intl.Collator` sort inside `classifyFiles`; new
natural-order tests for both `classifyFiles` and `resolveDrop`; the GettingStarted dismiss control +
`localStorage` key; and a brand-new `GettingStarted.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-multi-image-ordering-mobile-polish/48-CONTEXT.md
@.planning/phases/48-multi-image-ordering-mobile-polish/48-RESEARCH.md
@.planning/phases/48-multi-image-ordering-mobile-polish/48-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R098 — natural-order sort on the images bucket in classifyFiles</name>
  <files>src/components/slides/dropRouting.ts, src/components/slides/__tests__/dropRouting.test.ts</files>
  <read_first>
    - src/components/slides/dropRouting.ts (whole file, 116 lines — classifyFiles is lines 41-63, resolveDrop 96-115)
    - src/components/slides/__tests__/dropRouting.test.ts (its `file(name, type)` helper and describe/it structure — reuse them, do not invent a new helper)
    - .planning/phases/48-multi-image-ordering-mobile-polish/48-RESEARCH.md § "R098 — the exact one-line fix" and § Pitfall 1
  </read_first>
  <behavior>
    - classifyFiles([slide2.png, slide10.png, slide1.png]) → images === [slide1, slide2, slide10]
    - A naive lexicographic sort would give [slide1, slide10, slide2]; assert the FULL array order to prove numeric collation
    - resolveDrop([slide2.png, slide10.png, slide1.png]).images === [slide1, slide2, slide10] (same order surfaces through the consumer)
    - decks/videos/audioFiles order is unchanged by the sort (drop-order preserved for a multi-file drop of those kinds)
  </behavior>
  <action>
    In classifyFiles, immediately before the `return { decks, images, videos, audioFiles, rejected }`
    statement, construct one collator `new Intl.Collator(undefined, { numeric: true, sensitivity: 'base' })`
    and sort the images array in place: `images.sort((a, b) => collator.compare(a.name, b.name))`.
    Sort ONLY the images array (per D-098 locked decision — images bucket only; decks/videos/audio
    stay in drop order because a multi-PPTX drop already routes to a single deck and the others are
    unaffected by R098). Do NOT add the sort to resolveDrop, and do NOT sort a copy/slice — mutate the
    same `images` array classifyFiles returns, since resolveDrop reads `classified.images` from that
    exact object (Pitfall 1). Add a one-line comment tagging it R098. In the test file, add two `it`
    cases using the existing `file()` helper: one asserting `classifyFiles(...).images` equals the full
    natural-ordered array, and one asserting `resolveDrop(...).images` equals the same — deliberately
    passing the files in scrambled drop order [slide2, slide10, slide1].
  </action>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/dropRouting.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - classifyFiles sorts the images bucket via Intl.Collator({ numeric: true, sensitivity: 'base' }) in place before its return
    - Both new tests pass: classifyFiles AND resolveDrop return [slide1, slide2, slide10] for the scrambled input
    - No change to the decks/videos/audioFiles ordering (existing dropRouting tests still green)
  </acceptance_criteria>
  <done>Multi-image drops classify to filename natural order; the slide2-vs-slide10 trap is proven closed by test at both call sites.</done>
</task>

<task type="auto">
  <name>Task 2: R103 — dismissible Getting Started panel with localStorage persistence</name>
  <files>src/components/GettingStarted.vue, src/components/__tests__/GettingStarted.test.ts</files>
  <read_first>
    - src/components/GettingStarted.vue (whole file, 120 lines — header at lines 2-6, root v-if at line 2, script setup 62-119; note steps/allDone are computed from songStore/serviceStore/memberCount)
    - src/components/CollapsibleSection.vue lines 40-45 (the synchronous localStorage.getItem precedent to mirror)
    - .planning/phases/48-multi-image-ordering-mobile-polish/48-UI-SPEC.md § 5 (the exact dismiss button markup, aria-label, data-testid, key name) and § UI Considerations rows for present/absent and loading/race
  </read_first>
  <action>
    In GettingStarted.vue: (1) change the root element from `v-if="!allDone"` to
    `v-if="!allDone && !dismissed"`. (2) Change the header wrapper `div` at line 3 from
    `class="px-6 py-4 border-b border-gray-800"` to
    `class="px-6 py-4 border-b border-gray-800 flex items-start justify-between gap-3"`, wrap the
    existing `<h2>`/`<p>` in a `<div>`, and add the dismiss `<button>` after that div exactly as
    specified in 48-UI-SPEC § 5: `type="button"`, `aria-label="Dismiss Getting Started"`,
    `data-testid="getting-started-dismiss"`, class
    `-m-1.5 p-1.5 rounded-md text-gray-500 hover:text-gray-300 hover:bg-gray-800 transition-colors shrink-0`,
    containing the `h-4 w-4` × SVG (the M6 18L18 6M6 6l12 12 path), `@click="onDismiss"`. (3) In
    `<script setup>` add `const DISMISS_KEY = 'wp:gettingStartedDismissed'`, a
    `const dismissed = ref(localStorage.getItem(DISMISS_KEY) !== null)` seeded synchronously at setup
    (no onMounted, no watcher — avoids the first-paint flash), and
    `function onDismiss() { localStorage.setItem(DISMISS_KEY, 'true'); dismissed.value = true }`. Use
    the flat, unscoped key exactly — do NOT namespace by org/uid (locked decision; CollapsibleSection
    precedent). No un-dismiss UI (out of scope, Deferred Ideas). Create
    src/components/__tests__/GettingStarted.test.ts (none exists): mock the three stores so at least one
    step is incomplete (allDone=false, panel visible); clear localStorage in beforeEach AND afterEach to
    prevent cross-test pollution; assert (a) clicking [data-testid="getting-started-dismiss"] sets the
    key and hides the root, (b) seeding the key before mount hides the panel on first render with no
    flash, (c) an allDone=true state hides the panel regardless of the key, and (d) dismissed-but-not-
    allDone hides it.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/GettingStarted.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Dismiss × button renders in the header with the exact aria-label and data-testid from 48-UI-SPEC § 5
    - Clicking it writes wp:gettingStartedDismissed to localStorage and hides the panel
    - `dismissed` is seeded synchronously at setup() (ref initializer reads localStorage), not in onMounted — no flash
    - Root v-if is `!allDone && !dismissed`; the two conditions are independent (either alone hides)
    - GettingStarted.test.ts exists and clears localStorage around each test
  </acceptance_criteria>
  <done>An editor can dismiss the Getting Started panel; it stays dismissed across reloads on that device, independent of step completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS drag → client (classifyFiles) | Untrusted `File[]` from a native drop; already filtered by this module (unchanged) |
| client → browser localStorage | Per-device UI-state write (dismiss flag); no server, no other user, no tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01-01 | Tampering | localStorage wp:gettingStartedDismissed | low | accept | Client-only cosmetic onboarding flag; a tampered value at worst re-shows/hides a panel for that one browser profile — no church data, no auth, no cross-tenant effect |
| T-48-01-02 | Denial of Service | classifyFiles sort | low | accept | `Intl.Collator.compare` over a drop's file list is O(n log n) on a user-bounded, tiny array; no new input path, no amplification |

No new packages installed (RESEARCH.md Package Legitimacy Audit is empty by design) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/components/slides/__tests__/dropRouting.test.ts` green (R098)
- `npx vitest run src/components/__tests__/GettingStarted.test.ts` green (R103)
- Wave merge: `npx vitest run` at the documented 2-file baseline and `npm run type-check` clean
</verification>

<success_criteria>
- ROADMAP criterion 1 (natural-order multi-image via Intl.Collator in classifyFiles) proven by test
- ROADMAP criterion 4 clause C (Getting Started panel dismissible) delivered with per-device persistence
</success_criteria>

<output>
Create `.planning/phases/48-multi-image-ordering-mobile-polish/48-01-SUMMARY.md` when done
</output>
