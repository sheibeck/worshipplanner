---
phase: 48-multi-image-ordering-mobile-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/actionBarItems.ts
  - src/views/serviceEditorActionBar.ts
  - src/views/__tests__/serviceEditorActionBar.test.ts
  - src/components/ContextualActionBar.vue
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true

requirements: [R100, R101, R102]

must_haves:
  truths:
    - "buildServiceOrderItems returns Suggest All Songs -> Export -> Save -> Print -> Share (Print/Share appended after Save) — R101"
    - "buildPrintItem is unconditional; buildShareItem returns undefined when !ctx.isEditor — R101"
    - "ContextualActionBar renders a print icon and a share icon (SVG paths reused verbatim) and its root wraps (flex-wrap) so up to 5 items wrap at 375px instead of overflowing — R101 (UI Considerations: populated/narrow-viewport)"
    - "Print and Share only appear on the Service Order tab; the Roles branch still returns [] — R101 (UI Considerations: empty)"
    - "The page-bottom row no longer renders Print or Share; only Delete remains, right-aligned, with no orphaned flex-1 spacer — R101"
    - "Print click still calls window.print(); Share still shows Sharing.../Link Copied!/error states — R101"
    - "Undo is a text link beside SaveStatusIndicator in the save-status bar, NOT a button in the header save area; Ctrl+Z still triggers onUndo and the link is gated on previousService exactly as before — R102"
    - "The save-status wrapper carries flex items-center gap-2 UNCONDITIONALLY (border/background/padding/sticky stay conditional) so the Undo link lays out at idle — R102 (UI Considerations: present/absent)"
    - "The header save-area row and the bottom row stack vertically below sm using QuarterView's recipe — R100"
    - "The autosave-failure / lifecycle-error line stays on its own flex justify-end -mt-1 mb-3 line below the header (out-of-band 136fd0a) — not reflowed inline — R100 (UI Considerations: long-text)"
    - "The congregational-editor modal's SaveStatusIndicator gains no Undo affordance — R102"
  artifacts:
    - "src/components/actionBarItems.ts — ActionBarIcon union += 'print' | 'share'"
    - "src/views/serviceEditorActionBar.ts — buildPrintItem, buildShareItem, ActionBarHandlers.onPrint/onShare, ActionBarContext.isEditor/isSharing/shareCopied/shareError, wired into buildServiceOrderItems"
    - "src/components/ContextualActionBar.vue — print/share icon branches + flex-wrap root"
    - "src/views/ServiceEditorView.vue — bottom-row Print/Share removed, ctx threaded, Undo relocated to a link, QuarterView stacking recipe applied"
    - "Updated serviceEditorActionBar.test.ts and ServiceEditorView.test.ts (stale assertions corrected)"
  key_links:
    - "An ActionBarIcon union member AND a matching ContextualActionBar template branch are both required — an icon renders only if both exist (Pitfall 3; type-check will NOT catch a missing template branch)"
    - "activeActionItems (ServiceEditorView.vue ~2055) is the one call site that must thread onPrint/onShare/isEditor/isSharing/shareCopied/shareError — the new required ctx/handler fields force it"
    - "The type contract (serviceEditorActionBar.ts/actionBarItems.ts) and its consumer (ServiceEditorView.vue) change in THIS plan together, so type-check is green after the plan — never split them across waves"
    - "Save-status idle-class tests (ServiceEditorView.test.ts ~2371 and ~6027) currently assert toEqual([]) — they MUST become toEqual(['flex','items-center','gap-2']) after the unconditional-flex change"
---

<objective>
Relocate Print and Share into the top contextual action bar, demote Undo to a link beside the
save-status text, and stack the service-edit action rows on a phone (R101, R102, R100). These three
requirements all rewrite the SAME `ServiceEditorView.vue` header block and share one type contract,
so they land as one plan in the mandated order: Print/Share leave the bottom first, Undo relocates
second, the remaining rows get the stacking recipe third — avoiding three passes over the same block
(RESEARCH.md Summary).

Purpose: consolidate primary service actions into the one declarative R068 action bar (extending, not
reinventing, that pattern), move a destructive-adjacent low-frequency control (Undo) to link weight,
and make the screen usable at 375px — while keeping Delete deliberately at the bottom and never
reflowing the out-of-band 136fd0a autosave-failure line back inline.

Output (artifacts this plan produces): the `print`/`share` action keys and their icon branches; the
threaded action-bar context; the Undo link relocation with an unconditional save-status flex wrapper;
the QuarterView stacking recipe applied to two rows; and corrected tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-multi-image-ordering-mobile-polish/48-CONTEXT.md
@.planning/phases/48-multi-image-ordering-mobile-polish/48-RESEARCH.md
@.planning/phases/48-multi-image-ordering-mobile-polish/48-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: R101 — extend the declarative action-bar model (types, builders, icon branches)</name>
  <files>src/components/actionBarItems.ts, src/views/serviceEditorActionBar.ts, src/components/ContextualActionBar.vue, src/views/__tests__/serviceEditorActionBar.test.ts</files>
  <read_first>
    - src/components/actionBarItems.ts (whole file, 53 lines — the ActionBarIcon union at 35-41)
    - src/views/serviceEditorActionBar.ts (whole file, 213 lines — ActionBarHandlers 57-62, ActionBarContext 64-90, buildServiceOrderItems 169-189)
    - src/components/ContextualActionBar.vue (whole file, 114 lines — icon `v-if`/`v-else-if` branches 12-67, root class line 2, BASE_CLASS 102-103)
    - src/views/ServiceEditorView.vue lines 1300-1333 (the printer SVG path at 1310-1312 and the share SVG path at 1326-1328 to copy verbatim)
    - src/views/__tests__/serviceEditorActionBar.test.ts (whole file, 367 lines — existing describe/it and ctx-builder helpers to reuse)
    - .planning/phases/48-multi-image-ordering-mobile-polish/48-UI-SPEC.md § 3 (buildPrintItem/buildShareItem contracts, ordering, flex-wrap) and .../48-RESEARCH.md § Pattern 3 and Pitfall 3
  </read_first>
  <action>
    (1) In actionBarItems.ts add `'print'` and `'share'` to the ActionBarIcon union. (2) In
    serviceEditorActionBar.ts add `onPrint: () => void` and `onShare: () => void` to ActionBarHandlers,
    and add `isEditor: boolean`, `isSharing: boolean`, `shareCopied: boolean`,
    `shareError: string | null` to ActionBarContext (required fields, matching the file's established
    aiEnabled/pcEnabled pattern so the compiler forces the one call site to supply them). Add
    `buildPrintItem(ctx)` returning key `'print'`, label `'Print'`, icon `'print'`,
    `testId: 'print-btn'` (preserves the existing print-btn selector once the bottom button is deleted),
    onClick `ctx.handlers.onPrint`. Add `buildShareItem(ctx)` returning `undefined` when
    `!ctx.isEditor`, else key `'share'`, icon `'share'`, onClick `ctx.handlers.onShare`, and label
    `ctx.isSharing ? 'Sharing...' : ctx.shareCopied ? 'Link Copied!' : ctx.shareError ? ctx.shareError : 'Share'`.
    In buildServiceOrderItems, after the Save push, push buildPrintItem(ctx) then, if buildShareItem(ctx)
    is defined, push it — final order Suggest -> Export -> Save -> Print -> Share. Leave buildSlidesItems
    and the Roles branch ([]) untouched. (3) In ContextualActionBar.vue add two new icon branches after
    the existing ones (before the label `{{ item.label }}`): `v-else-if="item.icon === 'print'"`
    rendering the printer SVG copied verbatim from ServiceEditorView.vue:1310-1312, and
    `v-else-if="item.icon === 'share'"` rendering the share SVG copied verbatim from
    ServiceEditorView.vue:1326-1328 (single share glyph; the copied state is conveyed by the label, not
    a swapped icon). Change the root class (line 2) from `flex items-center gap-3` to
    `flex flex-wrap items-center gap-3` so up to 5 items wrap at 375px. (4) In serviceEditorActionBar.test.ts
    extend the ctx-builder helper with the four new required fields, and add tests: buildServiceOrderItems
    yields keys ending in [..., 'save', 'print', 'share'] when isEditor and canEditService; Share is
    absent (no 'share' key) when isEditor is false; Print is present regardless of isEditor; the Roles
    tab still returns [].
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/serviceEditorActionBar.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ActionBarIcon union includes 'print' and 'share'; ContextualActionBar has matching template branches (both halves — Pitfall 3)
    - buildServiceOrderItems order is Suggest -> Export -> Save -> Print -> Share; Roles still []
    - buildShareItem returns undefined when !isEditor; buildPrintItem is unconditional with testId 'print-btn'
    - ContextualActionBar root is flex flex-wrap items-center gap-3
    - serviceEditorActionBar.test.ts passes with the new cases (note: full type-check runs in Task 2 after the call site is updated)
  </acceptance_criteria>
  <done>The shared action-bar model exposes Print and Share for the Service Order tab, renders their icons, and wraps at phone width — with no consumer wired yet.</done>
</task>

<task type="auto">
  <name>Task 2: R101 + R102 — apply to ServiceEditorView (remove bottom Print/Share, thread ctx, relocate Undo)</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <read_first>
    - src/views/ServiceEditorView.vue lines 96-147 (the header Save area: Undo button 101-112, Mark as Planned 125-134, ContextualActionBar 145), lines 250-263 (save-status wrapper), lines 1300-1349 (bottom Print/Share/Delete row incl. flex-1 spacer at 1335), lines 2055-2075 (activeActionItems ctx), and the onPrint (~3058) / onShare (~3511) / isSharing/shareCopied/shareError refs (~1479-1481) / previousService / onUndo / Ctrl+Z handler
    - src/views/__tests__/ServiceEditorView.test.ts lines 582-588 (print-btn click), 851-871 (Roles tab Undo), 2365-2372 (save-status idle classes toEqual([])), 5185-5208 (Print/Share/Delete carve-out loop), 6017-6028 (idle strips chrome toEqual([])), 6054-6070 (the STALE title at 6060)
    - .planning/phases/48-multi-image-ordering-mobile-polish/48-UI-SPEC.md § 3, § 4 and § Anti-Patterns; .../48-RESEARCH.md Pitfalls 3,4,5
  </read_first>
  <action>
    R101 first: delete the two bottom-row `<button>` blocks — Print (1302-1314) and Share (1319-1333) —
    and delete the now-orphaned `<div class="flex-1" />` spacer (1335). Keep the Delete button
    (1337-1348) exactly as-is (same authStore.isEditor gate, same confirm flow, same position). Change
    the bottom row's own class (1301) so Delete stays right-aligned by the row's own flex, e.g.
    `mt-6 pt-4 border-t border-gray-800 flex flex-wrap items-center justify-end gap-2 print:hidden`.
    Thread the new context into activeActionItems (2055-2075): add `isEditor: authStore.isEditor`,
    `isSharing: isSharing.value`, `shareCopied: shareCopied.value`, `shareError: shareError.value` to
    the ctx object, and add `onPrint` and `onShare` to the handlers object (reference the existing
    onPrint/onShare functions). Do NOT reflow or move the lifecycle-error line (169-177) — it stays on
    its own `flex justify-end -mt-1 mb-3` line (out-of-band 136fd0a).
    R102 second: remove the Undo `<button>` from the header Save area (101-112). Add an Undo link inside
    the save-status wrapper (255-263) immediately after `<SaveStatusIndicator>`: a `<button type="button"
    data-testid="undo-link" title="Undo last save (Ctrl+Z)" v-if="previousService" @click="onUndo"` with
    class `text-xs text-indigo-400 hover:text-indigo-300 underline transition-colors` and text `Undo`.
    Change the wrapper's `:class` (256-259) so `flex items-center gap-2` is UNCONDITIONAL and only
    `sticky top-0 z-10 mb-3 rounded-md border border-gray-800 bg-gray-900 px-4 py-2` stays inside the
    serviceSaveStatusVisible ternary — i.e. `:class="['flex items-center gap-2', serviceSaveStatusVisible ? 'sticky top-0 z-10 mb-3 rounded-md border border-gray-800 bg-gray-900 px-4 py-2' : '']"`.
    Keep the Ctrl+Z keybinding and onUndo handler unchanged. Do NOT add an Undo affordance to the
    congregational-editor modal's second SaveStatusIndicator (~519).
    Then update ServiceEditorView.test.ts: (a) 2371 and 6027 — change the idle assertion from
    `.toEqual([])` to `.toEqual(['flex', 'items-center', 'gap-2'])` (the flex layout is now
    unconditional); (b) 6060 — the stale title still claims the header Save area "keeps Undo"; rewrite
    the title to describe the new placement and add assertions that there is NO undo control inside the
    header Save area and that `[data-testid="undo-link"]` exists inside `[data-testid="service-save-status-bar"]`;
    (c) 851 — confirm the Undo text still resolves (now via the undo-link in the save-status bar, which
    still renders on the Roles tab); adjust the assertion to look for `[data-testid="undo-link"]` if the
    text search is brittle; (d) 5185-5208 — the carve-out loop still expects Print/Share/Delete to
    render; update it so Print (`[data-testid="print-btn"]`) and Share (by text) are found in the top
    contextual action bar and Delete remains at the bottom (Print is unconditional; Share needs isEditor);
    (e) 582-588 — the print-btn click test now finds the single top-bar print-btn; confirm it still fires
    window.print(). Run type-check as part of this task's verify since the call site now satisfies the
    new required ctx/handler fields.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - Bottom row renders only Delete (right-aligned), no Print/Share, no flex-1 spacer
    - Print and Share render in the top action bar on the Service Order tab; print-btn click fires window.print(); Share shows its state labels
    - Undo is an undo-link inside service-save-status-bar, gated on previousService; NO undo control in the header Save area; congregational modal untouched
    - save-status wrapper flex classes are unconditional; idle-class tests assert ['flex','items-center','gap-2']
    - The stale test at 6060 is rewritten and asserts the new Undo placement
    - The 136fd0a lifecycle-error line is unchanged (own line, not inline)
    - `npm run type-check` is clean (activeActionItems supplies all new required fields)
  </acceptance_criteria>
  <done>Print/Share live in the top bar, Undo is a link beside the save status, Delete stays at the bottom, and the type contract from Task 1 is fully consumed with a green type-check.</done>
</task>

<task type="auto">
  <name>Task 3: R100 — stack the service-edit action rows on a phone (QuarterView recipe)</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <read_first>
    - src/views/ServiceEditorView.vue line 97 (the header Save-area row `flex items-center gap-3`, now holding Mark as Planned + ContextualActionBar after Undo left), line 38 (the header row already using flex-col sm:flex-row — no change), lines 1300-1301 (the bottom row after Task 2)
    - src/views/QuarterView.vue lines 6 and 13 (the two-part recipe to copy verbatim)
    - src/views/__tests__/ServiceEditorView.test.ts (add a stacking class assertion; reuse mountView)
    - .planning/phases/48-multi-image-ordering-mobile-polish/48-UI-SPEC.md § 2
  </read_first>
  <action>
    Apply QuarterView's button-cluster recipe (QuarterView.vue:13) to the header Save-area row
    (ServiceEditorView.vue:97). Change its class from `flex items-center gap-3` to
    `flex flex-col sm:flex-row sm:flex-wrap sm:items-center sm:justify-end gap-2 w-full sm:w-auto [&>*]:w-full sm:[&>*]:w-auto [&>*]:justify-center sm:[&>*]:justify-start`
    so Mark as Planned and the action-bar items stack full-width below sm and sit inline at sm+, using
    the same sm breakpoint as the rest of the app (do not invent a new breakpoint). The header row at
    line 38 already uses the recipe's first half (flex-col sm:flex-row) — leave it. The bottom row was
    already given flex-wrap items-center justify-end in Task 2 and holds only Delete, so it needs no
    stacking recipe. Add one component test asserting the Save-area row carries `flex-col` and
    `sm:flex-row`. Do NOT touch the lifecycle-error line's own-line placement.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - The header Save-area row uses the QuarterView flex-col sm:flex-row recipe with w-full sm:w-auto child sizing
    - Buttons stack full-width below sm and sit inline at sm+
    - The bottom row (Delete only) and the header row (line 38) are unchanged from Task 2 / today
    - New stacking class assertion passes; full suite green at the documented 2-file baseline; type-check clean
  </acceptance_criteria>
  <done>On a phone the service-edit action buttons stack vertically matching the Schedule screen's existing recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → window.print() (Print) | Browser-local print dialog; no data crosses a network boundary |
| editor → share-link creation (Share) | Pre-existing onShare handler (share token write) — unchanged; only its render location moves |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-03-01 | Elevation of Privilege | Share action visibility | medium | mitigate | buildShareItem preserves the exact `isEditor` gate the bottom-row Share used (a viewer-created share would silently omit editor-only roster data) — the gate moves with the control, it is not dropped |
| T-48-03-02 | Information Disclosure | relocated Print/Share vs. unchanged onPrint/onShare handlers | low | accept | Only the render location changes; window.print() and the share-token creation logic are byte-unchanged, so no new disclosure path is introduced |
| T-48-03-03 | Tampering | duplicate print-btn selector during transition | low | mitigate | The bottom-row Print/Share buttons are DELETED (not hidden) in the same task that adds the top-bar items, so exactly one print-btn exists — no selector collision (Anti-Patterns) |

No new packages installed — no supply-chain checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/serviceEditorActionBar.test.ts` green (Task 1)
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` green (Tasks 2-3)
- `npm run type-check` clean (vue-tsc --build — includes test files; required because Task 1 adds required ctx/handler fields every call site and test-constructed ctx must satisfy)
- Wave merge: `npx vitest run` at the documented 2-file baseline
</verification>

<success_criteria>
- ROADMAP criterion 3 (buttons stack on the service edit screen matching QuarterView's recipe) delivered
- ROADMAP criterion 4 clauses A+B (Print/Share in the top contextual action bar; Undo a link beside last-saved) delivered; Delete stays at the bottom; the 136fd0a autosave-failure line stays on its own line
</success_criteria>

<output>
Create `.planning/phases/48-multi-image-ordering-mobile-polish/48-03-SUMMARY.md` when done
</output>
