---
phase: 110-architectural-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md
autonomous: true
requirements: [R320]
must_haves:
  truths:
    - "110-FINDINGS-lifecycle-isolation.md enumerates store/Firestore-listener lifecycle findings, each with a Critical/High/Medium/Low severity and a concrete src/ (or firestore.rules) location"
    - "110-FINDINGS-lifecycle-isolation.md enumerates multi-tenant (org) isolation architecture findings, each with a severity and a concrete location"
    - "The church-switch re-subscribe path (resetOrgScopedStores + per-view watch(() => authStore.orgId)) is explicitly reviewed and its finding(s) recorded"
    - "No files under src/, functions/, firestore.rules, or storage.rules are modified by this plan"
  artifacts:
    - .planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md
  key_links:
    - "resetOrgScopedStores() teardown obligations in src/stores/orgScopedStores.ts reconciled against every org-scoped store's unsubscribe method and every view's re-subscribe watcher"
    - "orgId propagation from src/stores/auth.ts into org-scoped store subscribe(orgId) calls (cross-tenant read/write vectors)"
---

<objective>
Conduct the architectural review pass for TWO of the five ROADMAP dimensions — (2) store /
Firestore-listener lifecycle (including org-scoped teardown / re-subscription) and (3) multi-tenant
(org) isolation architecture — and record the findings in a per-plan findings file. These two
dimensions are grouped because org-scoped teardown/re-subscription is simultaneously a listener-lifecycle
concern and an isolation concern; reviewing them together lets one executor read the shared surface once.

Purpose: Produce grounded, severity-ranked findings that the consolidation plan (110-03) merges into the
single 110-ARCHITECTURE-REVIEW.md that Phase 111 remediates from.

Output: `.planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md`.

This is a REVIEW-ONLY plan. It reads source and writes ONE Markdown findings file. It changes NO code,
touches no rules, and performs no deploy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-architectural-review/110-CONTEXT.md
</context>

<review_method_note>
The executor conducts this review ITSELF by directly reading the source, the `.planning/codebase/` map
docs, and the relevant `docs/adr/` ADRs. Do NOT attempt to spawn reviewer sub-agents (the executor has
no Agent/Task tool — that will fail). The `.planning/graphs/` knowledge graph is STALE — grep/read real
source, do not trust graph queries. The base `.planning/codebase/` maps predate v1.1->v2.7, so verify
every map claim against live source before recording it as a finding, and cite the real file:line you
confirmed.
</review_method_note>

<severity_rubric>
Per 110-CONTEXT.md (owner-accepted 2026-09-02), classify every finding:
- Critical — data loss, cross-tenant data leak, or auth bypass.
- High — a correctness bug or multi-tenant isolation weakness likely to bite under real use.
- Medium — maintainability/coupling risk, or a latent bug that needs specific conditions.
- Low — nits / style.
Critical + High are remediated in Phase 111; Medium + Low are triaged to backlog. Record the severity
explicitly on every finding so the consolidation plan can separate the two groups unambiguously.
</severity_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Review store / Firestore-listener lifecycle (dimension 2), incl. org-scoped teardown/re-subscription</name>
  <files>.planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md</files>
  <read_first>
    - src/stores/orgScopedStores.ts (resetOrgScopedStores — the church-switch teardown hub)
    - src/stores/auth.ts (loadOrgContext, the member onSnapshot listener ~lines 112-138, enterOrgAsSuperAdmin, church-switch entry, orgId source of truth)
    - Every org-scoped store's subscribe / unsubscribe surface: src/stores/services.ts, songs.ts, roster.ts, teams.ts, quarters.ts, slideGroups.ts, scriptureSlides.ts, importedSlides.ts, pptxRenders.ts, serviceMessages.ts, songLyrics.ts
    - The per-view re-subscribe watchers (watch(() => authStore.orgId, { immediate: true })) in: src/views/ServiceEditorView.vue, ServicesView.vue, SongsView.vue, RosterView.vue, QuarterView.vue, TeamView.vue, SettingsView.vue, DashboardView.vue
    - src/router/index.ts (unsubscribeAll / teardown wiring on logout and route change)
    - .planning/codebase/ARCHITECTURE.md sections: "Store Layer", "Data Access Layer", "Architectural Constraints", "Anti-Patterns" (Missing Unsubscribe on Route Change), "Backend Behavioral Notes"
    - .planning/codebase/CONCERNS.md sections: "Firebase listener subscription leak risk", "Firestore listener lifecycle in auth store", "Autosave race condition risk"
    - docs/adr/ — grep for the church-switch / store-lifecycle ADRs (e.g. 0066 sidebar in-place church switcher, 0097 enter-church action, 0098 enterOrgAsSuperAdmin success/failure signalling) plus quick 260901-lua context
  </read_first>
  <action>
    Review the store / Firestore-listener lifecycle dimension. Trace, for each org-scoped store, the full
    lifecycle: where onSnapshot is established, whether the unsubscribe handle is stored, whether it is
    always called, and whether a rapid re-subscribe (church switch) can leave a listener orphaned or
    fire a stale-org snapshot into the newly-selected org's UI. Reconcile resetOrgScopedStores() against
    the actual set of org-scoped stores — flag any store that owns an onSnapshot but is NOT torn down by
    resetOrgScopedStores(), and any store whose named teardown method (e.g. unsubscribeGroups,
    unsubscribeReadings) drifts from its subscribe surface. Explicitly scrutinize the church-switch
    re-subscribe hot spot (a recent real defect area, quick 260901-lua): confirm that teardown-then-
    re-subscribe ordering, the per-view watch(() => authStore.orgId) immediate re-subscription, and the
    auth member-listener setup have no race, no leak, and no stale-org bleed. Record maintainability
    concerns (e.g. the ServiceEditorView autosave/deep-watch surface) at Medium unless they can cause
    data loss. Assign each finding a severity per the rubric and a concrete file:line (or file:region)
    location confirmed against live source. Genuine SECURITY findings noticed incidentally (rules/auth)
    are out of scope for fixing here — note them briefly for handoff to Phase 112, do not deep-dive.
    Write the findings under a clearly-owned section header for this dimension in the findings file.
  </action>
  <verify>
    <automated>bash -c "F=.planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md; test -f \"$F\" && grep -qiE 'lifecycle' \"$F\" && grep -qE 'resetOrgScopedStores' \"$F\" && grep -qE 'Critical|High|Medium|Low' \"$F\""</automated>
  </verify>
  <acceptance_criteria>
    - 110-FINDINGS-lifecycle-isolation.md contains a section header owning the store/listener-lifecycle dimension.
    - The church-switch re-subscribe path is addressed by name: the file mentions resetOrgScopedStores and the per-view authStore.orgId watcher.
    - Every lifecycle finding row carries an explicit severity token and a concrete src/ (or router/rules) file:line-or-region location.
    - No files under src/ or functions/ are modified.
  </acceptance_criteria>
  <done>The store/Firestore-listener lifecycle dimension is fully reviewed and its severity-ranked, located findings are written to the findings file.</done>
</task>

<task type="auto">
  <name>Task 2: Review multi-tenant (org) isolation architecture (dimension 3)</name>
  <files>.planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md</files>
  <read_first>
    - src/stores/auth.ts (orgId / orgIds / role, custom-claim consumption, enterOrgAsSuperAdmin)
    - src/stores/orgScopedStores.ts and each org-scoped store's Firestore path construction (organizations/{orgId}/...)
    - src/firebase/index.ts (client SDK init)
    - functions/src/orgMembershipClaims.ts, functions/src/superAdminClaims.ts, functions/src/index.ts (server-side org re-authorization on the proxy)
    - .planning/codebase/ARCHITECTURE.md sections: "Architectural Constraints" (org-scoped data access, single-org-per-user, hardcoded org IDs anti-pattern), "Backend Behavioral Notes" (isOrgMember / isOrgEditor / isSuperAdmin, claim architecture)
    - .planning/codebase/INTEGRATIONS.md and CONCERNS.md (org-isolation-relevant entries)
    - docs/adr/ — grep for org-claim / multi-tenant / super-admin ADRs (e.g. 0007 org-slug scoping, claim-widening entries)
  </read_first>
  <action>
    Review the multi-tenant (org) isolation ARCHITECTURE — the structural question of whether tenant data
    can only ever be reached through the current org context, not the line-by-line security-rules audit
    (that is Phase 112). Assess: how orgId is derived and propagated (single source of truth vs.
    duplicated/hardcoded), whether every org-scoped read/write is namespaced under
    organizations/{orgId}, whether the custom-claim-based membership model and the super-admin
    cross-org entry path preserve isolation architecturally, and whether any client path can construct a
    Firestore reference for an org the user is not currently scoped to (cross-tenant read/write vector).
    Assess the isolation implications of the church-switch flow reviewed in Task 1 (does switching leave
    any store still bound to the prior org's data). Classify a proven or plausible cross-tenant data leak
    or auth bypass as Critical; an isolation weakness likely to bite under real use as High; a
    maintainability/latent isolation risk as Medium. Give every finding a concrete file:line location
    confirmed against live source. Note incidental deep security-rule concerns for Phase 112 rather than
    fixing or exhaustively auditing them here. Write the findings under a clearly-owned section header for
    this dimension in the SAME findings file, appended after Task 1's section (single writer, sequential —
    no parallel write).
  </action>
  <verify>
    <automated>bash -c "F=.planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md; test -f \"$F\" && grep -qiE 'isolation|multi-tenant' \"$F\" && grep -qiE 'orgId|organizations/' \"$F\" && grep -qE 'src/|functions/' \"$F\""</automated>
  </verify>
  <acceptance_criteria>
    - 110-FINDINGS-lifecycle-isolation.md contains a distinct section header owning the multi-tenant isolation dimension.
    - Every isolation finding row carries an explicit severity token and a concrete file:line-or-region location.
    - Incidental security-rule observations, if any, are labelled as Phase 112 handoff items, not fixed.
    - No files under src/ or functions/ are modified.
  </acceptance_criteria>
  <done>The multi-tenant isolation architecture dimension is fully reviewed and its severity-ranked, located findings are appended to the findings file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Review-only plan; reads source and writes one Markdown file under `.planning/`. No runtime code, no rules, no deploy, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-110-01 | Information Disclosure | 110-FINDINGS-lifecycle-isolation.md contents | low | accept | Internal `.planning/` review artifact listing file paths and architectural findings; contains no secrets/credentials and is never shipped or deployed. |
| T-110-02 | Tampering | git-tracked review artifact | low | accept | Committed via the normal GSD commit flow; the output is inert Markdown, not executable. |
</threat_model>

<verification>
- File exists: `.planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md`.
- Both dimension sections present (store/listener lifecycle; multi-tenant isolation).
- At least one finding carries each of the severity tokens actually used, and every finding has a location.
- Church-switch re-subscribe path addressed by name (resetOrgScopedStores + authStore.orgId watcher).
- NO code changed: `bash -c "test -z \"$(git diff --name-only -- src functions firestore.rules storage.rules)\""` succeeds.
</verification>

<success_criteria>
Two dimensions (store/listener lifecycle; multi-tenant isolation) are reviewed against live source, with
every finding carrying an explicit severity and a concrete location, and no source files modified.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces: `.planning/phases/110-architectural-review/110-FINDINGS-lifecycle-isolation.md`
(consumed by the consolidation plan 110-03). The phase's other artifacts are
`110-FINDINGS-boundaries-coupling-dataflow.md` (plan 110-02) and the final ranked
`110-ARCHITECTURE-REVIEW.md` (plan 110-03).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/110-architectural-review/110-01-SUMMARY.md` when done.
</output>
