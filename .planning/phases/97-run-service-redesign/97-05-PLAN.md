---
phase: 97-run-service-redesign
plan: 05
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - src/components/run/RunPreflightPanel.vue
  - src/components/run/RunPreviewPair.vue
  - src/components/run/RunFilmstrip.vue
  - src/components/run/__tests__/RunFilmstrip.test.ts
autonomous: true
requirements: [R276, R282]

must_haves:
  truths:
    - "PRE-FLIGHT PANEL (R276 State A): a NEW src/components/run/RunPreflightPanel.vue is PURE presentation — a centered 'Ready when you are' column with an Audience display card + a Confidence display card (each showing the assigned monitor label + a 'Not open' amber badge pre-live), an honest readiness line ('All N slides rendered' green check driven by an `allRendered`/`renderedCount`/`slideCount` prop — NOT the design's CCLI), a primary 'Go live' button, and a secondary 'Rehearse without screens' button. It EMITS @go-live and @rehearse and owns NO logic. CRITICAL: the Go-live button carries data-testid=run-go-live-btn (the identifier RunControlView.output.test.ts drives go-live through) so wiring in 97-09 keeps that suite green."
    - "PREVIEW PAIR + NEXT-UP SCALE (R276 owner fix #2): a NEW src/components/run/RunPreviewPair.vue renders the current slide (dominant, LEFT) with a GREEN live frame when the `live` prop is true (owner fix #4 — green, not the design's red) and the next slide (subordinate, RIGHT) at a SMALLER scale (wrap the next SlideCanvas in a transform: scale() container — SlideCanvas has no font-size prop). It PRESERVES the run-current-preview / run-next-preview testids and the 'End of service' empty-next copy the existing dual-preview test asserts. Uses the real SlideCanvas :interactive=false."
    - "IN-ITEM FILMSTRIP (R282): a NEW src/components/run/RunFilmstrip.vue takes `slides: AssembledSlide[]` (the active item's slides) + a parallel `indices: number[]` (each slide's ARRAY index in assembledSlideshow) + `currentIndex: number | null`, renders each as a scaled non-interactive SlideCanvas thumb (current = live frame, others = accent frame), and EMITS @jump(index) with the clicked slide's ARRAY index (from `indices`, never a local loop index). data-testid=run-filmstrip on the strip, run-filmstrip-slide on each thumb. The which-slides + array-index derivation is the parent's job (97-08); this component only renders + emits."
    - "NO run-take TESTID: RunPreviewPair's 'Take ->' hint (if rendered) is a plain visual hint with NO run-take/run-push-live testid, preserving the single-selection contract the control suite asserts."
  artifacts:
    - "src/components/run/RunPreflightPanel.vue — State-A pre-flight column (display cards + readiness + Go live + Rehearse)."
    - "src/components/run/RunPreviewPair.vue — current (green-when-live frame) + next (scaled smaller) preview pair, preserved testids."
    - "src/components/run/RunFilmstrip.vue — click-to-jump in-item filmstrip emitting the slide's array index."
    - "src/components/run/__tests__/RunFilmstrip.test.ts — clicking the Nth thumb emits indices[N] (array index), not the loop index."
  key_links:
    - "RunPreflightPanel props { serviceName, slideCount, renderedCount, allRendered, audienceLabel, confidenceLabel } -> readiness line + display cards; @go-live (run-go-live-btn) / @rehearse (run-rehearse-btn)."
    - "RunPreviewPair props { current, next, live, nextScale } -> run-current-preview (green frame iff live) + run-next-preview (scaled; 'End of service' when next null)."
    - "RunFilmstrip props { slides, indices, currentIndex } -> run-filmstrip-slide[n] @click emits @jump(indices[n])."
---

<objective>
Build the three presentational components at the heart of the redesigned control body: RunPreflightPanel
(State A 'Ready when you are', R276), RunPreviewPair (program + next-up-smaller-font, R276 owner fix #2), and
RunFilmstrip (in-item click-to-jump strip, R282). All pure presentation — the readiness derivation, the
which-slides-are-in-this-item computation, and every channel post stay in useRunControl (97-08); these take
props and emit intent. Wired into RunControlView in 97-09.

Purpose: R276 (pre-flight + preview split + smaller next-up), R282 (in-item filmstrip). Disjoint new files; no
parent edit here.

Output: RunPreflightPanel.vue + RunPreviewPair.vue + RunFilmstrip.vue + a filmstrip array-index test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-UI-SPEC.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/views/RunControlView.vue
@src/components/slides/SlideCanvas.vue
@src/types/slide.ts
@src/views/__tests__/RunControlView.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunPreflightPanel.vue — State A 'Ready when you are' (display cards + readiness + Go live + Rehearse)</name>
  <files>src/components/run/RunPreflightPanel.vue</files>
  <read_first>
    - 97-UI-SPEC.md State A (:44-63) — the centered column: heading + sub, an Audience display card + a Confidence
      display card (preview/'no signal' thumb + monitor name + a 'Not open' amber badge + a Change link), a green
      readiness check 'All N slides rendered' (from renderState, NOT CCLI), a primary 'Go live' + a secondary
      'Rehearse without screens' + an Enter key hint.
    - src/views/RunControlView.vue — the Go-live button idiom (:22-45, the play SVG) whose data-testid
      run-go-live-btn MUST be reused on this panel's primary button; the amber 'Not open' idiom (:104-111).
    - src/types/slide.ts renderState (:50-62) — a slide with renderState 'pending'/'failed' is NOT rendered; the
      parent computes allRendered = every assembled slide's renderState is undefined (97-08), and passes it here.
  </read_first>
  <action>
    Create src/components/run/RunPreflightPanel.vue as PURE presentation. defineProps&lt;{ serviceName: string;
    slideCount: number; itemCount: number; renderedCount: number; allRendered: boolean; audienceLabel: string;
    confidenceLabel: string }&gt; and defineEmits&lt;{ 'go-live': []; rehearse: [] }&gt;.

    Render (data-testid="run-preflight", Nocturne Run-scoped palette):
    - Heading 'Ready when you are' + sub 'Going live opens the audience and confidence windows and puts slide 1
      on the screens.'
    - An Audience display card (data-testid="run-preflight-audience"): the audienceLabel monitor name + a 'Not
      open' amber badge + a Change link. A Confidence display card (data-testid="run-preflight-confidence")
      mirror.
    - A readiness line (data-testid="run-readiness"): when allRendered a green check + 'All {{ slideCount }}
      slides rendered'; otherwise an amber '{{ renderedCount }} of {{ slideCount }} slides rendered' (honest, no
      CCLI).
    - Primary Go-live button: data-testid="run-go-live-btn", aria-label 'Go live — open the audience and
      confidence displays', the play-triangle SVG + 'Go live', @click emits @go-live. REUSE run-go-live-btn.
    - Secondary button: data-testid="run-rehearse-btn", 'Rehearse without screens', @click emits @rehearse.
    - An 'Enter' key hint near the primary.
    No channel, no store, no getScreenDetails. The parent supplies audienceLabel/confidenceLabel from
    loadMapping() and allRendered/renderedCount from renderState (97-08).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-preflight" src/components/run/RunPreflightPanel.vue &amp;&amp; grep -q "run-go-live-btn" src/components/run/RunPreflightPanel.vue &amp;&amp; grep -q "run-rehearse-btn" src/components/run/RunPreflightPanel.vue &amp;&amp; grep -q "run-readiness" src/components/run/RunPreflightPanel.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunPreflightPanel is pure props-in/emits-out: display cards with 'Not open' badges, an honest readiness line
      from allRendered/renderedCount/slideCount, a Go-live button (run-go-live-btn) emitting @go-live, and a
      Rehearse button (run-rehearse-btn) emitting @rehearse; no channel/store logic. Type-check clean; baseline green.
  </acceptance_criteria>
  <done>RunPreflightPanel presents the centered State-A pre-flight surface with honest readiness and the run-go-live-btn / run-rehearse-btn actions, ready to wire in 97-09.</done>
</task>

<task type="auto">
  <name>Task 2: RunPreviewPair.vue — program (green-when-live) + next-up at a smaller scale</name>
  <files>src/components/run/RunPreviewPair.vue</files>
  <read_first>
    - src/views/RunControlView.vue dual preview (:466-511) — the current run-current-preview (ring-2 ring-indigo,
      aspect-video, SlideCanvas :interactive=false) + next run-next-preview (+ 'End of service' empty copy) to
      reproduce with the redesign's frames.
    - src/views/__tests__/RunControlView.test.ts dual-preview test (:505-526) — asserts run-current-preview /
      run-next-preview show the slide id text and 'End of service' past the last slide; RunPreviewPair MUST keep
      those testids + that empty copy so the wired view (97-09) passes that assertion after go-live.
    - 97-UI-SPEC.md State B top row (:76-81) — Program 'On screen' (green ring per owner) + Next up at a SMALLER
      font scaled to the pane; OMIT Key/BPM.
  </read_first>
  <action>
    Create src/components/run/RunPreviewPair.vue importing SlideCanvas. defineProps&lt;{ current: AssembledSlide |
    null; next: AssembledSlide | null; live: boolean; nextScale?: number }&gt; (default nextScale 0.8), import type
    AssembledSlide from '@/types/slide'. No emits (previews are display-only; navigation is the transport/rail).

    Render:
    - CURRENT (dominant, LEFT): data-testid="run-current-preview", aspect-video, a live frame that is a GREEN ring
      when `live` is true and a neutral ring otherwise (owner fix #4 — green, not red); a small 'LIVE' tag shown
      only when live; `<SlideCanvas v-if="current" :slide="current" :interactive="false" />` else a 'Loading
      slideshow…' placeholder.
    - NEXT (subordinate, RIGHT): data-testid="run-next-preview"; wrap the next SlideCanvas in a container with
      transform: scale(nextScale) (transform-origin: top center) so the upcoming slide renders SMALLER (owner fix
      #2); `<SlideCanvas v-if="next" ...>` else the 'End of service' copy (preserve this exact text). An optional
      plain 'Take →' hint with NO run-take/run-push-live testid.
    Nocturne Run-scoped palette. No store/channel.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-current-preview" src/components/run/RunPreviewPair.vue &amp;&amp; grep -q "run-next-preview" src/components/run/RunPreviewPair.vue &amp;&amp; grep -q "End of service" src/components/run/RunPreviewPair.vue &amp;&amp; grep -q "scale" src/components/run/RunPreviewPair.vue</automated>
    <automated>! grep -qE "run-take|run-push-live" src/components/run/RunPreviewPair.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunPreviewPair renders run-current-preview (green ring + 'LIVE' tag only when live) and run-next-preview
      (SlideCanvas wrapped in a transform: scale container for the smaller next-up font; 'End of service' when
      next is null), imports the real SlideCanvas :interactive=false, and uses NO run-take/run-push-live testid.
      Type-check clean; baseline green.
  </acceptance_criteria>
  <done>RunPreviewPair presents the program preview (green-when-live) and a smaller-scaled next-up preview, preserving run-current-preview / run-next-preview + the 'End of service' copy for the wired view.</done>
</task>

<task type="auto">
  <name>Task 3: RunFilmstrip.vue — in-item click-to-jump strip emitting the array index (+ test)</name>
  <files>src/components/run/RunFilmstrip.vue, src/components/run/__tests__/RunFilmstrip.test.ts</files>
  <read_first>
    - 97-UI-SPEC.md in-item filmstrip (:85-88) — 'Slides in this item', horizontal strip of the current item's
      slides as small scaled SlideCanvas thumbs (current = live frame, next = accent frame), each click-to-jump,
      a trailing 'Next item →'.
    - 97-PATTERNS.md build-from-spec filmstrip (:217) — the slotIndex + array-index relationship: each thumb's
      click emits the slide's ARRAY index (its position in assembledSlideshow), which the parent passes in
      `indices`. RunFilmstrip does NOT compute which slides belong to the item — it renders the given slides.
    - src/views/RunControlView.test.ts createFakeChannel + stubbed SlideCanvas (:166-185) — stub SlideCanvas the
      same way in the filmstrip test.
  </read_first>
  <action>
    Create src/components/run/RunFilmstrip.vue importing SlideCanvas. defineProps&lt;{ slides: AssembledSlide[];
    indices: number[]; currentIndex: number | null }&gt; (slides[i] pairs with indices[i]; indices[i] is that
    slide's array index in assembledSlideshow), defineEmits&lt;{ jump: [index: number] }&gt;.

    Render (data-testid="run-filmstrip"):
    - a label 'Slides in this item';
    - v-for over slides with the loop index `i`: a button (data-testid="run-filmstrip-slide",
      :data-index="indices[i]") wrapping a scaled non-interactive `<SlideCanvas :slide="slides[i]"
      :interactive="false" />`; a live frame when `indices[i] === currentIndex`, an accent frame otherwise;
      `@click="emit('jump', indices[i])"` — emit the ARRAY index from `indices`, NEVER the loop index `i`.
    - a trailing 'Next item →' affordance is optional (a plain hint; if made clickable it emits nothing here —
      item navigation is the rail/transport's job).
    Nocturne Run-scoped palette. No store/channel.

    Create src/components/run/__tests__/RunFilmstrip.test.ts: stub SlideCanvas (mirror the control test's stub).
    Mount with slides = 3 fixtures, indices = [2,3,4], currentIndex = 3. Assert: three run-filmstrip-slide thumbs
    render; the thumb whose data-index === 3 has the live-frame class; clicking the FIRST thumb emits jump with
    2 (indices[0], NOT 0), and clicking the THIRD emits 4. This proves the array-index emit contract that keeps
    click-to-jump posting the correct global index.
  </action>
  <verify>
    <automated>npx vitest run src/components/run/__tests__/RunFilmstrip.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-filmstrip-slide" src/components/run/RunFilmstrip.vue &amp;&amp; grep -q "indices\[i\]" src/components/run/RunFilmstrip.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunFilmstrip renders one run-filmstrip-slide per given slide, marks the current one, and emits @jump with the
      slide's ARRAY index (indices[i]), never the loop index; the test proves clicking thumb 0 emits 2 and thumb 2
      emits 4 with currentIndex framing. Type-check clean; baseline green.
  </acceptance_criteria>
  <done>RunFilmstrip presents the in-item click-to-jump strip that emits each slide's global array index, proven by the array-index test — ready for the parent to wire to postIndex in 97-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filmstrip click -> @jump(arrayIndex) -> parent postIndex | The thumb emits the slide's GLOBAL array index (from the parent-supplied indices), not a local loop index; a wrong index would jump to the wrong slide, so the test pins the indices[i] contract |
| presentational children -> parent | RunPreflightPanel/RunPreviewPair/RunFilmstrip hold no channel/store logic; readiness + which-slides + posts stay in useRunControl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-05-01 | Tampering | filmstrip jumping to the wrong slide | high | mitigate | @jump emits indices[i] (the global array index), proven by the RunFilmstrip test asserting thumb 0 → 2 and thumb 2 → 4; the parent maps @jump straight to postIndex. |
| T-97-05-02 | Repudiation | dishonest readiness / pre-live green | medium | mitigate | The readiness line is driven by allRendered/renderedCount from renderState (parent, 97-08) — an honest 'N of M rendered' when not all ready; the preview live frame is green only when the live prop is true. |
| T-97-05-SC | Tampering | package installs (npm) | n/a | accept | No installs — presentational components + a test only. |
</threat_model>

<verification>
- `npm run type-check` clean; `npx vitest run` shows only the baseline; RunFilmstrip.test.ts passes. No `--dir
  src`; no rules suite.
- Manual check: run-go-live-btn preserved on RunPreflightPanel; run-current-preview/run-next-preview + 'End of
  service' preserved on RunPreviewPair; RunFilmstrip emits the array index; no run-take/run-push-live anywhere.
</verification>

<success_criteria>
- The State-A pre-flight panel (R276), the program + smaller-next-up preview pair (R276), and the in-item
  click-to-jump filmstrip (R282) exist as pure, tested components with the testids the wired control suite
  depends on preserved.
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-05-SUMMARY.md` when done.
</output>
