---
phase: 97-run-service-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/useRunControl.ts
  - src/views/RunControlView.vue
autonomous: true
requirements: [R276]

must_haves:
  truths:
    - "BEHAVIOUR-PRESERVING EXTRACTION (R276 foundation): every Phase 92-96 machinery block moves VERBATIM-IN-BEHAVIOUR from RunControlView.vue's <script setup> into a new src/composables/useRunControl.ts (mirroring how useOutputWindow.ts owns the output lifecycle). RunControlView.vue keeps its EXISTING template UNCHANGED and its script becomes `const { ... } = useRunControl({ channelFactory: props.channelFactory })` plus SlideCanvas import + the defineProps. NO redesign, NO new feature, NO testid rename, NO URL change in this plan — the sole deliverable is the seam."
    - "SINGLE-WRITER + HANDSHAKE PRESERVED (R266): useRunControl owns index/seq/handle, postIndex, resendCurrent, and registers openRunChannel(serviceId, options.channelFactory) + handle.onHello(resendCurrent) + the on-mount slide-0 post + the watch(assembledSlideshow) late-arrival post in ITS onMounted, and handle.close() in ITS onUnmounted — so the channelFactory prop threads through exactly as today and RunControlView.test.ts's post/seq/onHello assertions pass UNTOUCHED."
    - "OPEN STATE MACHINE + WR-01 + RECOVERY PRESERVED: OutputStatus/outputStatus/openOutputs/openPlaced/openUnplaced/bothOpened/openWindow, goLiveRequestId/isUnmounted, startClosedPoll/readClosed/reopenOutput/reopenReassignedOutputs/onScreensChange/stopRecoveryWatchers, liveScreenDetails/ScreenDetailsLike/resolveScreen/screenLabel, audienceClosed/confidenceClosed/monitorChanged/reassignRole, closeOutputs/confirmExit/openExitConfirm/cancelExit — ALL move into the composable with identical logic and identical teardown ordering (stopRecoveryWatchers before closeOutputs in confirmExit; stopRecoveryWatchers + handle.close + removeEventListener in onUnmounted)."
    - "KEYBOARD + NAV + RAIL PRESERVED: handleKeydown (registered on document in the composable's onMounted, removed in onUnmounted), goBySlide/goByItem/jumpToSlot, rail/firstIndexBySlot/slideCountBySlot/railRows/currentSlotIndex/itemTitle/countLabel, and the railRef/activeItemRef/captureActiveRow + watch(index) auto-scroll + watch(confirmOpen) cancel-focus all move into the composable and are RETURNED so the unchanged template binds them. The composable exports a RailRow row type for downstream child components."
    - "GREEN SUITES ARE THE GATE: `npx vitest run` shows ONLY the baseline (src/storage.rules.test.ts) and `npm run type-check` is clean AFTER the extraction, with src/views/__tests__/RunControlView.test.ts and RunControlView.output.test.ts passing WITHOUT any edit to those test files — the tests mount the REAL view and assert via data-testid + the injected channelFactory, so a correct extraction is invisible to them."
  artifacts:
    - "src/composables/useRunControl.ts — the extracted control-core composable owning the channel single-writer, nav, rail derivations, open state machine, WR-01 guard, Phase 96 recovery, exit/teardown, and keyboard, exporting a RailRow type and returning every binding the current template uses."
    - "src/views/RunControlView.vue — unchanged template; script reduced to defineProps<{channelFactory?}>, SlideCanvas import, and the single useRunControl({ channelFactory }) destructure."
  key_links:
    - "RunControlView.vue <script> -> useRunControl({ channelFactory: props.channelFactory }) -> returns { serviceHeading,index,current,next,currentSlotIndex,railRows,firstIndexBySlot,countLabel,jumpToSlot,railRef,captureActiveRow,outputStatus,readyAudienceLabel,readyConfidenceLabel,blockedRole,audienceClosed,confidenceClosed,monitorChanged,reassignRole,reopenOutput,reopenReassignedOutputs,openOutputs,confirmOpen,openExitConfirm,cancelExit,confirmExit,cancelBtnRef } bound by the UNCHANGED template."
    - "useRunControl onMounted -> openRunChannel(serviceId.value, options.channelFactory) + onHello(resendCurrent) + document.addEventListener('keydown', handleKeydown) + slide-0 post; onUnmounted -> isUnmounted=true + stopRecoveryWatchers() + handle.close() + removeEventListener — the exact lifecycle the un-extracted view registered."
---

<objective>
Extract ALL of RunControlView.vue's Phase 92-96 control machinery into a new src/composables/useRunControl.ts,
leaving the view as template + a single composable destructure — the low-regression seam every subsequent
Phase 97 plan builds on. This mirrors the Phase 94 useOutputWindow extraction: the composable owns the
single-writer channel, navigation model, rail derivations, honest open state machine, WR-01 stale guard,
Phase 96 recovery (closed-poll + screenschange reassign), exit/teardown ordering, and the document keyboard
handler; the view keeps its EXISTING markup unchanged.

Purpose: the redesign (97-08 composable features, 97-09 template) and the presentational children
(97-04/05/06) all need a clean composable seam so the correctness machinery is edited in one place and the
template becomes swappable without touching the invariants. This plan ships ZERO behaviour change.

Output: src/composables/useRunControl.ts (new) + a reduced src/views/RunControlView.vue script. The existing
RunControlView.test.ts and RunControlView.output.test.ts suites are the acceptance gate and are NOT edited.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/views/RunControlView.vue
@src/composables/useOutputWindow.ts
@src/composables/useServiceAssembly.ts
@src/utils/runChannel.ts
@src/utils/serviceSlots.ts
@src/utils/monitorConfig.ts
@src/views/__tests__/RunControlView.test.ts
@src/views/__tests__/RunControlView.output.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/composables/useRunControl.ts owning the entire control-core (verbatim-in-behaviour)</name>
  <files>src/composables/useRunControl.ts</files>
  <read_first>
    - src/views/RunControlView.vue — the WHOLE &lt;script setup&gt; (:576-1193). Every block moves: props seam
      note (:600-607); useServiceAssembly + useRouter (:613-614); the single-writer channel index/seq/handle +
      postIndex/resendCurrent (:616-633); current/next/currentSlotIndex (:635-641); rail/firstIndexBySlot/
      slideCountBySlot/itemTitle/countLabel/railRows (:643-686); goBySlide/goByItem/jumpToSlot (:688-717);
      railRef/activeItemRef/captureActiveRow + watch(index) auto-scroll (:719-728); confirmOpen/cancelBtnRef/
      handleKeydown (:730-762); OutputStatus type + refs + outputWindows (:764-790); the whole 96-01 recovery
      block ScreenDetailsLike/audienceClosed/confidenceClosed/monitorChanged/reassignRole/pollId/
      liveScreenDetails/readClosed/startClosedPoll/reopenOutput/reopenReassignedOutputs/onScreensChange/
      stopRecoveryWatchers (:792-943); goLiveRequestId/isUnmounted/openWindow/resolveScreen/screenLabel/
      bothOpened/openPlaced/openUnplaced/audienceUrl/confidenceUrl/openOutputs/closeOutputs (:945-1131);
      openExitConfirm/cancelExit/confirmExit + watch(confirmOpen) (:1133-1160); serviceHeading (:1162-1166);
      onMounted/watch(assembledSlideshow)/onUnmounted (:1168-1193).
    - src/composables/useOutputWindow.ts — the exact extraction precedent: a composable that (a) takes an
      options object with channelFactory, (b) calls useServiceAssembly() FIRST so its subscribe onMounted
      registers before the composable's own, (c) registers its OWN onMounted/onUnmounted opening/closing the
      channel, and (d) returns a plain object of refs+functions. Mirror this shape and ordering.
    - src/views/__tests__/RunControlView.test.ts (:37-262) — the harness that MUST stay green untouched:
      mountView passes props.channelFactory; createFakeChannel captures posts + emitHello(); the suite asserts
      posts/seq/onHello, rail testids, keyboard nav, exit-confirm, and previews. Confirm nothing it asserts is
      an INTERNAL of the script (it only reads data-testid + channel posts), so moving the script into a
      composable is invisible to it.
    - src/views/__tests__/RunControlView.output.test.ts (:1-45 harness) — drives go-live via run-go-live-btn,
      fakes getScreenDetails/window.open/saveMapping, and exercises the recovery/WR-01 blocks. The composable
      must reproduce openOutputs/openPlaced/openUnplaced/reopenOutput/onScreensChange/stopRecoveryWatchers
      byte-for-byte in behaviour so these pass untouched.
  </read_first>
  <action>
    Create src/composables/useRunControl.ts exporting `interface UseRunControlOptions { channelFactory?:
    BroadcastChannelFactory }` and `export function useRunControl(options: UseRunControlOptions = {})`.

    MOVE the entire RunControlView &lt;script setup&gt; body into the composable body, making ONLY these
    mechanical adjustments (no logic change):
    - Replace `props.channelFactory` reads with `options.channelFactory`.
    - Call `const { serviceId, orgIdRef, localService, assembledSlideshow } = useServiceAssembly()` FIRST
      (same as today) so its onMounted subscribe registers before this composable's onMounted (mirrors
      useOutputWindow's ordering note).
    - Keep `const router = useRouter()` inside the composable.
    - Keep ALL refs, computeds, functions, watchers, onMounted, onUnmounted EXACTLY as they are in the view
      today, including: the single-writer index/seq/handle + postIndex/resendCurrent; current/next/
      currentSlotIndex; rail/firstIndexBySlot/slideCountBySlot/itemTitle/countLabel/railRows; goBySlide/
      goByItem/jumpToSlot; railRef/activeItemRef/captureActiveRow + the watch(index) scrollIntoView; confirmOpen/
      cancelBtnRef/handleKeydown; the OutputStatus machine + outputWindows; the 96-01 recovery block in full;
      goLiveRequestId/isUnmounted/openWindow/resolveScreen/screenLabel/bothOpened/openPlaced/openUnplaced/
      audienceUrl/confidenceUrl/openOutputs/closeOutputs; openExitConfirm/cancelExit/confirmExit + watch(
      confirmOpen); serviceHeading; and the onMounted/watch(assembledSlideshow)/onUnmounted lifecycle.
    - EXPORT a row type for downstream children: `export interface RailRow { index: number; section: string;
      title: string; count: number; hasSlides: boolean; isActive: boolean }` and type `railRows` as
      `ComputedRef&lt;RailRow[]&gt;` (the shape it already produces at RunControlView :674-686).
    - Import from the same modules the view imports (vue, vue-router, useServiceAssembly, runChannel,
      serviceSlots, monitorConfig, slotTypes, types/service, types/slide). Do NOT import SlideCanvas (that is a
      template concern that stays in the view).
    - RETURN a single object with every binding the CURRENT template references (see key_links). Do NOT return
      internal-only helpers that the template never binds (postIndex/goBySlide/goByItem are used by
      handleKeydown/jumpToSlot internally — jumpToSlot IS bound by the template, goBySlide/goByItem are not;
      still, RETURN goBySlide/goByItem/postIndex too so 97-08/97-09 can wire the transport bar + rehearse +
      blackout without re-plumbing — returning extra bindings is harmless and forward-enabling).

    Do NOT change the output URLs (audienceUrl/confidenceUrl stay `/present/{role}/${serviceId}?org=...` with
    NO &role= — the output self-fullscreen in 97-03 reads its role from a composable option, not a URL param,
    so RunControlView.output.test.ts's URL assertions stay green). Do NOT touch the keyboard 'B' reserved slot
    yet (:760 stays a no-op comment — 97-08 wires it).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>grep -q "export function useRunControl" src/composables/useRunControl.ts &amp;&amp; grep -q "export interface RailRow" src/composables/useRunControl.ts</automated>
    <automated>grep -q "onHello(resendCurrent)" src/composables/useRunControl.ts &amp;&amp; grep -q "stopRecoveryWatchers" src/composables/useRunControl.ts &amp;&amp; grep -q "startClosedPoll" src/composables/useRunControl.ts &amp;&amp; grep -q "goLiveRequestId" src/composables/useRunControl.ts</automated>
  </verify>
  <acceptance_criteria>
    - useRunControl.ts compiles under `npm run type-check` and contains the full control-core (channel single-
      writer, nav, rail, open state machine, WR-01 guard, 96-01 recovery, exit/teardown, keyboard) with
      identical logic to the pre-extraction view.
    - It takes { channelFactory? }, calls useServiceAssembly() first, registers its own onMounted/onUnmounted
      channel + keyboard lifecycle, exports a RailRow type, and returns every binding the current template uses.
    - No output-URL change, no new feature, no 'B' wiring. (Task 2 swaps the view to consume it.)
  </acceptance_criteria>
  <done>src/composables/useRunControl.ts exists as a behaviour-preserving extraction of the entire RunControlView control-core, typed and exporting RailRow, ready for the view to consume in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce RunControlView.vue script to consume useRunControl (template unchanged)</name>
  <files>src/views/RunControlView.vue</files>
  <read_first>
    - src/views/RunControlView.vue — the template (:1-574) which stays UNCHANGED, and the &lt;script setup&gt;
      (:576-1193) which is replaced by the composable destructure. Note the template's exact binding names so
      the destructure returns matching identifiers (serviceHeading, outputStatus, audienceClosed, railRows,
      jumpToSlot, railRef, captureActiveRow, confirmOpen, cancelBtnRef, current, next, readyAudienceLabel,
      readyConfidenceLabel, blockedRole, reassignRole, reopenOutput, reopenReassignedOutputs, openOutputs,
      openExitConfirm, cancelExit, confirmExit, countLabel, firstIndexBySlot).
    - src/composables/useRunControl.ts (Task 1) — the returned object's exact keys.
    - src/views/__tests__/RunControlView.test.ts + RunControlView.output.test.ts — the green gate; do NOT edit.
  </read_first>
  <action>
    Replace RunControlView.vue's &lt;script setup lang="ts"&gt; body with:
    - the SlideCanvas import (template needs it): `import SlideCanvas from '@/components/slides/SlideCanvas.vue'`;
    - the run-channel type import for the prop: `import type { BroadcastChannelFactory } from '@/utils/runChannel'`;
    - the composable import: `import { useRunControl } from '@/composables/useRunControl'`;
    - `const props = defineProps<{ channelFactory?: BroadcastChannelFactory }>()`;
    - `const { ...all bindings the template uses... } = useRunControl({ channelFactory: props.channelFactory })`.

    Leave the &lt;template&gt; and the top file comment EXACTLY as they are. Delete every now-migrated import
    (vue refs, vue-router, useServiceAssembly, openRunChannel, serviceSlots, monitorConfig, slotTypes, type
    imports) that the template no longer needs directly — keep ONLY SlideCanvas + BroadcastChannelFactory +
    useRunControl. Confirm the destructure lists EVERY identifier the template binds (a missing one is a
    type-check/runtime error the existing suite will catch).

    Do NOT add features or change markup. This is purely swapping the script's guts for the composable.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/views/__tests__/RunControlView.test.ts src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "useRunControl({ channelFactory: props.channelFactory })" src/views/RunControlView.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunControlView.vue's script is reduced to defineProps + the useRunControl destructure + SlideCanvas import;
      the template is byte-for-byte unchanged.
    - `npx vitest run src/views/__tests__/RunControlView.test.ts src/views/__tests__/RunControlView.output.test.ts`
      passes with NO edits to those test files (the extraction is invisible to them).
    - `npm run type-check` is clean (vue-tsc --build; NODE_OPTIONS=--max-old-space-size=8192 only if it OOMs; NO
      Array.prototype.at) and `npx vitest run` shows only the baseline (src/storage.rules.test.ts); no `--dir src`.
  </acceptance_criteria>
  <done>RunControlView.vue now consumes useRunControl for its entire behaviour with an unchanged template, and both control suites pass untouched — the clean seam every later Phase 97 plan depends on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suites -> real view -> composable | The extraction is validated by the SAME channelFactory injection + data-testid assertions the pre-97 suites use; a behaviour drift in the move fails RunControlView.test.ts/output.test.ts rather than shipping |
| composable onMounted/onUnmounted ordering | useServiceAssembly() must be called first so its subscribe registers before the channel opens, and teardown (stopRecoveryWatchers before closeOutputs; handle.close + keydown removal on unmount) must match the un-extracted view exactly, or the no-leak/exit assertions regress |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-01-01 | Tampering | behaviour drift during extraction | high | mitigate | Move every block verbatim-in-behaviour; the pre-97 RunControlView.test.ts + output.test.ts (single-writer seq, onHello resend, rail, keyboard, exit-confirm, closed-poll/reopen, reassign precedence, WR-01, no-leak) are the untouched gate and MUST stay green. |
| T-97-01-02 | Denial of Service | lost teardown (poll/listener/keydown leak) | high | mitigate | Preserve stopRecoveryWatchers in confirmExit (before closeOutputs) AND onUnmounted, plus handle.close + document keydown removal in onUnmounted, exactly as today; the output suite's no-leak block asserts this. |
| T-97-01-SC | Tampering | package installs (npm) | n/a | accept | No package-manager installs — a same-repo composable extraction only; the supply-chain/legitimacy checkpoint is not applicable. |
</threat_model>

<verification>
- `npm run type-check` (vue-tsc --build — typechecks the test files too; NODE_OPTIONS=--max-old-space-size=8192
  only on OOM; NO Array.prototype.at) is clean.
- `npx vitest run` shows only the baseline (src/storage.rules.test.ts). RunControlView.test.ts and
  RunControlView.output.test.ts pass WITHOUT edits. No `--dir src`; no `npm run test:rules` (client-only, no
  Firestore/rules change).
- Manual check: the composable owns the channel single-writer + onHello + keyboard + open state machine + WR-01
  + 96-01 recovery + exit/teardown; the view script is just defineProps + the destructure + SlideCanvas import;
  the template is unchanged; output URLs are unchanged (no &role=).
</verification>

<success_criteria>
- The entire Phase 92-96 control-core lives in src/composables/useRunControl.ts; RunControlView.vue is template
  + a single useRunControl destructure; both control suites stay green untouched; type-check clean; no baseline
  regression. This is a pure seam with zero behaviour change (R276 foundation).
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-01-SUMMARY.md` when done.
</output>
