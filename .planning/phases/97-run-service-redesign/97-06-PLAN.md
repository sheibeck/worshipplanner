---
phase: 97-run-service-redesign
plan: 06
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - src/components/run/RunRail.vue
  - src/components/run/RunDisplaysPanel.vue
  - src/components/run/__tests__/RunRail.test.ts
autonomous: true
requirements: [R276]

must_haves:
  truths:
    - "ORDER-OF-SERVICE RAIL (R276): a NEW src/components/run/RunRail.vue is PURE presentation importing the RailRow type from useRunControl. It reproduces the existing rail markup EXACTLY enough to keep the control suite green: data-testid=rail-item on a has-slides row (with :data-active from an activeIndex prop and the item title/section/count), data-testid=rail-item-empty on a no-slides row (inert), and data-testid=run-rail-empty on the zero-slides empty state ('Nothing to present yet'). It EMITS @jump(slotIndex) on a has-slides row click; an empty-slot row is inert (no emit). Active item EXPANDS to its slide list from an `expandedSlides` prop, each slide row emitting @jump-slide(arrayIndex)."
    - "SELF-SCROLL (R276): RunRail owns its OWN active-row auto-scroll — it captures the active row's element and watches `activeIndex`, calling scrollIntoView({ block: 'nearest' }) on change (moving the Phase 95 captureActiveRow/watch(index) behaviour INTO the child, per 97-PATTERNS decomposition), so the parent no longer needs the rail ref plumbing."
    - "DISPLAYS PANEL (R276 State C, ADDITIVE): a NEW src/components/run/RunDisplaysPanel.vue is PURE presentation rendering the header displays cluster + State-C per-output cards: an Audience card + a Confidence card (name/resolution + a live/'Not open' status) with a Reopen action, a DISABLED 'Off' Stage placeholder (no 3rd-output build), and a Manage link. It EMITS @reopen(role) and @manage. It does NOT own the Phase 96 recovery banners/rows (run-output-closed-*, run-reassign-banner) — those stay INLINE in the redesigned parent (97-09) with their exact testids to keep RunControlView.output.test.ts green."
    - "PURE PRESENTATION: neither component imports a store, the run channel, getScreenDetails, or monitorConfig side-effects; all state (railRows, activeIndex, expandedSlides, per-output open/label, reopen) is supplied by the parent and every action is emitted."
  artifacts:
    - "src/components/run/RunRail.vue — the order-of-service rail (rail-item/rail-item-empty/run-rail-empty preserved), self-scrolling, emitting @jump / @jump-slide."
    - "src/components/run/RunDisplaysPanel.vue — the additive displays cluster + State-C cards + disabled Stage-off, emitting @reopen / @manage."
    - "src/components/run/__tests__/RunRail.test.ts — a has-slides row click emits @jump(slotIndex); an empty-slot row emits nothing; the zero-slides empty state renders run-rail-empty."
  key_links:
    - "RunRail props { rows: RailRow[], activeIndex, expandedSlides } -> rail-item (:data-active=row.index===activeIndex) / rail-item-empty / run-rail-empty; @jump(row.index) on a has-slides click; @jump-slide(arrayIndex) on an expanded slide row; watch(activeIndex) -> active row scrollIntoView."
    - "RunDisplaysPanel props { audience:{open,label}, confidence:{open,label}, live } -> per-output cards + disabled Stage-off; @reopen(role) / @manage."
---

<objective>
Build the last two presentational children: RunRail (the order-of-service rail, self-scrolling, preserving the
rail testids the control suite asserts) and RunDisplaysPanel (the additive displays cluster + State-C per-output
cards + a disabled Stage-off placeholder). Both pure presentation — railRows, active/expanded state, per-output
status, and every reopen/jump post stay in useRunControl; these render props and emit intent. Wired into
RunControlView in 97-09, where the Phase 96 recovery banners stay INLINE with their exact testids.

Purpose: R276 (the redesigned rail + displays surfaces). Disjoint new files; no parent edit here.

Output: RunRail.vue + RunDisplaysPanel.vue + a rail jump/empty test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-UI-SPEC.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/views/RunControlView.vue
@src/composables/useRunControl.ts
@src/views/__tests__/RunControlView.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunRail.vue — self-scrolling order-of-service rail (preserved testids) + test</name>
  <files>src/components/run/RunRail.vue, src/components/run/__tests__/RunRail.test.ts</files>
  <read_first>
    - src/views/RunControlView.vue rail (:388-463) — the exact markup to reproduce: the aside/overflow-y-auto
      scroll container, the run-rail-empty empty state (:402-412, 'Nothing to present yet'), the rail-item button
      (:417-446, :data-active, section/title/count, active indigo classes), the rail-item-empty inert row
      (:449-460), and captureActiveRow + watch(index) scrollIntoView (:719-728) to MOVE into this child.
    - src/composables/useRunControl.ts (97-01) — the exported RailRow interface { index, section, title, count,
      hasSlides, isActive } to import; here isActive is derived from an activeIndex prop instead.
    - src/views/__tests__/RunControlView.test.ts rail tests (:353-411) — assert rail-item data-active + title
      text, empty-slot row inert (posts nothing), run-rail-empty on zero slides. RunRail MUST keep those testids
      and the has-slides-vs-empty branching so the wired view passes untouched.
  </read_first>
  <action>
    Create src/components/run/RunRail.vue importing `type RailRow` from '@/composables/useRunControl'. defineProps
    &lt;{ rows: RailRow[]; activeIndex: number | null; expandedSlides?: { arrayIndex: number; label: string;
    isCurrent: boolean }[] }&gt; and defineEmits&lt;{ jump: [slotIndex: number]; 'jump-slide': [arrayIndex: number]
    }&gt;.

    Reproduce the rail markup:
    - the aside scroll container (self-owned ref) + the 'Order of Service' sticky header;
    - run-rail-empty when rows is empty / no has-slides rows (mirror the current condition using a computed
      `hasAnySlides = rows.some(r => r.hasSlides)`), text 'Nothing to present yet';
    - v-for rows: a has-slides row → a button data-testid="rail-item" :data-active="String(row.index ===
      activeIndex)" with the section/title/count and the active indigo classes when row.index === activeIndex,
      @click="emit('jump', row.index)"; when active AND expandedSlides given, render the expanded slide list
      below it, each row data-testid="run-rail-slide" @click="emit('jump-slide', slide.arrayIndex)" with an
      is-current highlight. A no-slides row → data-testid="rail-item-empty" inert (no @click).
    - SELF-SCROLL: capture the active row's element via a :ref callback into a local ref, and `watch(() =>
      props.activeIndex, async () => { await nextTick(); activeRowEl.value?.scrollIntoView({ block: 'nearest' })
      })` — the Phase 95 behaviour, now owned by the child.
    Use the Nocturne Run-scoped rail palette (bg #141624 per 97-UI-SPEC) via local classes; keep the existing
    active-indigo semantics recognisable. No store/channel.

    Create src/components/run/__tests__/RunRail.test.ts (jsdom; stub Element.prototype.scrollIntoView = vi.fn()):
    mount with 3 rows (index 0 has-slides active, index 1 no-slides, index 2 has-slides), activeIndex 0. Assert:
    the active row has data-active 'true' and its title; clicking the index-2 has-slides row emits jump with 2;
    clicking the rail-item-empty row emits nothing; a zero-rows mount renders run-rail-empty.
  </action>
  <verify>
    <automated>npx vitest run src/components/run/__tests__/RunRail.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "rail-item" src/components/run/RunRail.vue &amp;&amp; grep -q "rail-item-empty" src/components/run/RunRail.vue &amp;&amp; grep -q "run-rail-empty" src/components/run/RunRail.vue &amp;&amp; grep -q "scrollIntoView" src/components/run/RunRail.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunRail imports RailRow, renders rail-item (:data-active from activeIndex) / rail-item-empty / run-rail-empty
      preserving those testids and the has-slides-vs-empty branching, expands the active item to its slide list
      (run-rail-slide), self-scrolls the active row on activeIndex change, and emits @jump(slotIndex) /
      @jump-slide(arrayIndex) — empty rows inert. The test proves the jump emit + empty-row inertness + empty state.
  </acceptance_criteria>
  <done>RunRail presents the self-scrolling order-of-service rail with the preserved rail testids and the active-item slide expansion, emitting @jump / @jump-slide.</done>
</task>

<task type="auto">
  <name>Task 2: RunDisplaysPanel.vue — additive displays cluster + State-C cards + disabled Stage-off</name>
  <files>src/components/run/RunDisplaysPanel.vue</files>
  <read_first>
    - 97-UI-SPEC.md State C (:92-100) — the Displays panel: per-screen cards (Audience/Confidence/Stage-off) with
      a live thumbnail + name/resolution/screen + Reopen / Move actions; Stage = a disabled/'Off' placeholder
      only (no 3rd-output build).
    - 97-CONTEXT.md OUT OF SCOPE (:41-44) — the Stage/3rd output may appear ONLY as a disabled 'Off' placeholder.
    - src/views/RunControlView.vue placed-cluster dot+word idiom (:66-149) for the per-output status dots.
    - NOTE (do NOT duplicate): the Phase 96 recovery banners/rows (run-output-closed-audience/confidence,
      run-reopen-*, run-reassign-banner, run-fallback-banner, run-blocked-banner, run-partial-banner) are NOT this
      component's job — they stay inline in the parent (97-09) with their exact testids. RunDisplaysPanel is the
      NEW, additive displays surface only.
  </read_first>
  <action>
    Create src/components/run/RunDisplaysPanel.vue as PURE presentation. defineProps&lt;{ audience: { open:
    boolean; label: string }; confidence: { open: boolean; label: string }; live: boolean }&gt; and defineEmits
    &lt;{ reopen: [role: 'audience' | 'confidence']; manage: [] }&gt;.

    Render (data-testid="run-displays-panel", Nocturne Run-scoped palette):
    - an Audience card (data-testid="run-display-audience"): a dot GREEN when audience.open else amber/muted, the
      audience.label, and a Reopen button (data-testid="run-display-reopen-audience") emitting @reopen('audience');
    - a Confidence card (data-testid="run-display-confidence") mirror with run-display-reopen-confidence;
    - a DISABLED Stage placeholder (data-testid="run-display-stage-off"): a muted 'Stage — Off' card, aria-
      disabled, no actions (no 3rd output built);
    - a Manage link (data-testid="run-displays-manage") emitting @manage.
    Do NOT render any run-output-closed-* / run-reassign-* / run-fallback-* testid here (those live in the parent).
    No store/channel/getScreenDetails.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-displays-panel" src/components/run/RunDisplaysPanel.vue &amp;&amp; grep -q "run-display-stage-off" src/components/run/RunDisplaysPanel.vue &amp;&amp; grep -q "run-display-reopen-audience" src/components/run/RunDisplaysPanel.vue</automated>
    <automated>! grep -qE "run-reassign-banner|run-output-closed" src/components/run/RunDisplaysPanel.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunDisplaysPanel is pure props-in/emits-out: Audience + Confidence cards (green when open, Reopen emitting
      @reopen(role)), a disabled 'Off' Stage placeholder, and a Manage link (@manage); it renders NONE of the
      Phase 96 recovery testids (those stay in the parent). Type-check clean; baseline green.
  </acceptance_criteria>
  <done>RunDisplaysPanel presents the additive displays cluster + State-C cards with a disabled Stage-off, emitting @reopen / @manage, without duplicating the tested recovery machinery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rail click -> @jump(slotIndex) / @jump-slide(arrayIndex) | The rail emits a slot index (has-slides only) or a slide's array index; empty rows are inert — the parent maps these to jumpToSlot/postIndex, so a wrong/absent emit would mis-navigate and is pinned by the RunRail test |
| RunDisplaysPanel vs Phase 96 recovery | The tested recovery banners/rows stay in the parent; RunDisplaysPanel is additive, so it cannot regress the output.test.ts recovery assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-06-01 | Tampering | rail testid drift breaking the control suite | high | mitigate | RunRail reproduces rail-item/rail-item-empty/run-rail-empty + the has-slides branching; the RunRail test + the wired view's existing rail tests are the gate. |
| T-97-06-02 | Repudiation | claiming a 3rd (Stage) output exists | low | mitigate | Stage is a DISABLED 'Off' placeholder only (run-display-stage-off), no actions — no fake 3rd-output UI. |
| T-97-06-SC | Tampering | package installs (npm) | n/a | accept | No installs — presentational components + a test only. |
</threat_model>

<verification>
- `npm run type-check` clean; `npx vitest run` shows only the baseline; RunRail.test.ts passes. No `--dir src`;
  no rules suite.
- Manual check: RunRail preserves the rail testids + self-scrolls; RunDisplaysPanel is additive (no recovery
  testids) with a disabled Stage-off; both are pure presentation.
</verification>

<success_criteria>
- The redesigned order-of-service rail (self-scrolling, testids preserved) and the additive displays panel
  (per-output cards + disabled Stage-off) exist as pure, tested components ready to wire in 97-09 (R276).
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-06-SUMMARY.md` when done.
</output>
