---
phase: 19-scripture-and-congregational-reading-slides
plan: 02
type: tdd
wave: 2
depends_on: [19-01]
requirements: [R008, R017]
files_modified:
  - src/stores/scriptureSlides.ts
  - src/stores/__tests__/scriptureSlides.test.ts
autonomous: true
---

<objective>
Provide the persistence layer for scripture readings: a `useScriptureSlides` Pinia store with Firestore CRUD and a real-time subscription, following the proven songLyrics store pattern.

Purpose: The store is the persistence layer for scripture readings. The editor components in plans 19-03 and 19-04 depend on it for CRUD operations and auto-save.
Output: `src/stores/scriptureSlides.ts` + a Vitest suite with mocked Firestore.
</objective>

<context>
@.planning/phases/19-scripture-and-congregational-reading-slides/19-RESEARCH.md
@src/types/scriptureReading.ts
@src/stores/songLyrics.ts
</context>

## Must-Haves

- `useScriptureSlides` Pinia store provides CRUD with a real-time Firestore subscription following the songLyrics pattern — unit tests pass.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: useScriptureSlides Pinia store with Firestore CRUD</name>
  <files>src/stores/scriptureSlides.ts, src/stores/__tests__/scriptureSlides.test.ts</files>
  <behavior>
    - `useScriptureSlides` exposes `readings`, `isLoading`, a `currentReading` computed, `subscribeReadings(orgId)` / `unsubscribeReadings()` (`onSnapshot` on `organizations/{orgId}/scriptureReadings` ordered by `updatedAt desc`), `createReading(orgId, data)` (adds `createdAt`/`updatedAt` server timestamps, returns the new doc id), `updateReading(orgId, readingId, data)` (patches with a fresh `updatedAt`), and `getReading(orgId, readingId)`.
    - Mirrors the songLyrics store's subscription lifecycle and server-timestamp conventions.
  </behavior>
  <action>
    RED first: author `src/stores/__tests__/scriptureSlides.test.ts` with mocked Firestore covering subscribe/unsubscribe, create (returns id, sets timestamps), update (patches + updatedAt), get (returns null when absent), and the currentReading computed. Confirm failure, then implement `src/stores/scriptureSlides.ts` to green.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/scriptureSlides.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/__tests__/scriptureSlides.test.ts` passes.
    - Store exports `subscribeReadings`, `unsubscribeReadings`, `createReading`, `updateReading`, `getReading`, `currentReading`.
    - `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>The store persists scripture readings under `organizations/{orgId}/scriptureReadings` with a real-time subscription; its suite is green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/stores/__tests__/scriptureSlides.test.ts` green.
- `npx vue-tsc --build` green.
</verification>

<output>
Create `.planning/phases/19-scripture-and-congregational-reading-slides/19-02-SUMMARY.md` when done.
</output>
