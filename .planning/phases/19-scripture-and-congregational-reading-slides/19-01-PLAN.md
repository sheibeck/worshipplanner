---
phase: 19-scripture-and-congregational-reading-slides
plan: 01
type: tdd
wave: 1
depends_on: []
requirements: [R008]
files_modified:
  - src/types/slide.ts
  - src/types/scriptureReading.ts
  - src/utils/scriptureSplitter.ts
  - src/utils/__tests__/scriptureSplitter.test.ts
autonomous: true
---

<objective>
Establish the type + splitting foundation for Phase 19: add the `ScriptureSlide` type to the `Slide` union, define the `ScriptureReading` Firestore document type, and implement the `splitPassage` utility that breaks ESV passage text into slide-sized chunks at verse boundaries.

Purpose: The `ScriptureSlide` type and the passage splitter are the foundation for all Phase 19 work. The splitter is the highest-risk algorithm (novel code, not a pattern copy), so it is front-loaded with thorough tests. Plans 19-02 through 19-05 consume these types and this utility.
Output: `ScriptureSlide` on the `Slide` union, `src/types/scriptureReading.ts`, `src/utils/scriptureSplitter.ts`, and a full Vitest suite.
</objective>

<context>
@.planning/phases/19-scripture-and-congregational-reading-slides/19-RESEARCH.md
@src/types/slide.ts
@src/types/service.ts
@src/utils/esvApi.ts
</context>

## Must-Haves

- `ScriptureSlide` type added to the `Slide` union in `src/types/slide.ts` with `contentKind: 'scripture'`.
- `ScriptureReading` Firestore document type defined in `src/types/scriptureReading.ts`.
- `splitPassage` utility correctly splits ESV text at verse boundaries with a configurable words-per-slide threshold — 8+ unit tests covering short / long / edge cases.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: ScriptureSlide + ScriptureReading types and the passage splitter</name>
  <files>src/types/slide.ts, src/types/scriptureReading.ts, src/utils/scriptureSplitter.ts, src/utils/__tests__/scriptureSplitter.test.ts</files>
  <behavior>
    - `src/types/slide.ts` gains a `ScriptureSlide` interface (`contentKind: 'scripture'`, `reference`, `bookRef: ScriptureRef`, `text`, `verseRange`, `readingMode: 'normal' | 'congregational'`, optional `sections`) plus a `CongregationalSection` type (`speaker: 'LEADER' | 'CONGREGATION'`, `text`, optional `verseRange`); `ScriptureSlide` is added to the `Slide` union.
    - `src/types/scriptureReading.ts` defines the `ScriptureReading` document type (`id`, `reference: ScriptureRef`, `displayReference`, `rawText`, `readingMode`, `slides: ScriptureSlide[]`, optional `congregationalSections`, `createdAt`/`updatedAt` timestamps).
    - `splitPassage(text, ref, opts?)` parses ESV bracketed verse numbers, accumulates verses until the words-per-slide threshold is exceeded, and emits slides at verse boundaries; falls back to sentence-boundary splitting when no verse numbers are present; returns a single slide for short passages.
  </behavior>
  <action>
    RED first: author `src/utils/__tests__/scriptureSplitter.test.ts` with cases covering short passage (< threshold → 1 slide), long passage (> threshold → multiple slides), verse-boundary detection from ESV `[n]` markers, no-verse-number fallback to sentence splitting, very long chapter, and verse-range labeling. Confirm failure, then implement `scriptureSplitter.ts` to green. `DEFAULT_WORDS_PER_SLIDE = 50`.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/scriptureSplitter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/scriptureSplitter.test.ts` passes (8+ tests green).
    - `ScriptureSlide` is present in the `Slide` union in `src/types/slide.ts`.
    - `ScriptureReading` type exists in `src/types/scriptureReading.ts`.
    - `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>The scripture types exist and the splitter's suite (short / long / verse-boundary / fallback / edge) is green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/utils/__tests__/scriptureSplitter.test.ts` green.
- `npx vue-tsc --build` green.
</verification>

<output>
Create `.planning/phases/19-scripture-and-congregational-reading-slides/19-01-SUMMARY.md` when done.
</output>
