---
phase: 58-messaging-infrastructure-settings-recipient-resolution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - src/rules.test.ts
autonomous: true
requirements: [R130, R132]
user_setup:
  - service: firebase
    why: "The firestore.rules additions ship built/tested but UNDEPLOYED (deploy-gated per the v1.7 standing autonomy grant); they only take effect in production after the owner deploys."
    dashboard_config:
      - task: "Run `firebase deploy --only firestore:rules` to publish the new messages/recipients/lockSnapshots rules to production"
        location: "Owner's terminal in the main checkout (not run by Claude)"

must_haves:
  truths:
    - "The new nested messages/recipients/lockSnapshots collections are denied by default in firestore.rules and only opened by explicit isOrgMember/isOrgEditor allow blocks (R130/R132)"
    - "The emulator suite includes a GENUINE ALLOW-case that passes (org editor creates a messages doc; org member reads it; org editor writes a lockSnapshots doc), not only deny-cases"
    - "Any client write to a recipients subdoc or a messages status update/delete is denied (Admin-SDK-only)"
    - "A viewer creating messages, and any cross-org access, are denied"
  artifacts:
    - firestore.rules
    - src/rules.test.ts
  key_links:
    - "messages + recipients + lockSnapshots blocks nest INSIDE match /services/{docId} (a sibling placement would protect the wrong path)"
    - "the emulator evaluates the real firestore.rules text via readFileSync — a syntax/logic error is caught before the owner's manual deploy"
---

<objective>
Add deny-by-default `firestore.rules` scaffolding for the three new nested collections
(`services/{id}/messages`, `.../messages/{id}/recipients`, `services/{id}/lockSnapshots`) and prove them
with an emulator test suite that includes a genuine ALLOW-case — not only deny-cases. No client code
writes these collections this phase; the rules are exercised solely by the emulator test's own seeded
writes. This is the ONLY server-side enforcement layer Phase 58 adds.

Purpose: ROADMAP success criterion 5 and the rules-first discipline carried from v1.5 (CLAUDE.md's
documented `storage.rules` all-deny-suite incident, where a deny-everyone rule shipped behind a deny-only
test for a whole milestone). R130 (the messages surface the kill-switch governs) and R132 (the
lockSnapshots the per-service defaults feed) both depend on these collections being locked down from
commit one.
Output: new nested rules blocks in `firestore.rules`, new `describe` blocks in `src/rules.test.ts`.

DEPLOY-GATED (v1.7 grant): ships built/tested/UNDEPLOYED. Hand the owner the exact command
`firebase deploy --only firestore:rules`. Do NOT deploy. Undeployed rules block nothing in Phase 58
because no client writes these collections yet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — rules design + rules-testing discipline
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-CONTEXT.md
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-RESEARCH.md
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-PATTERNS.md

# Exact analogs — nesting precedent, Admin-only precedent, test-harness helpers
@firestore.rules
@src/rules.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add emulator ALLOW + deny test cases (test-first, RED)</name>
  <files>src/rules.test.ts</files>
  <action>
    Add new `describe` block(s) to src/rules.test.ts for the three collections, modeled on the existing
    `pptxRenders` describe block (~1496-1600) and reusing the file's existing helpers `seedMembershipDoc`
    (line 33), `seedDoc` (line 44), `assertSucceeds`/`assertFails`, and
    `testEnv.authenticatedContext(uid)`. Write EVERY test against the FULL nested path
    `organizations/{orgId}/services/{serviceId}/messages/{id}` (and `.../recipients/{id}`,
    `.../lockSnapshots/{id}`) — never a sibling `organizations/{orgId}/messages/{id}` path — so a
    misplaced (sibling) rule block would be caught (58-RESEARCH.md Pitfall 3).

    Required cases (per 58-CONTEXT.md's non-negotiable ALLOW-case discipline and the Admin-SDK-only design):
    - messages: ALLOW an org editor to create a messages doc under their own service (genuine
      assertSucceeds); ALLOW an org member/viewer to read a messages doc; DENY a viewer creating a
      messages doc; DENY an org editor updating or deleting a messages doc (status transitions are
      Admin-SDK-only).
    - recipients: ALLOW an org member to read a recipients subdoc; DENY ANY client write (even editor) to
      a recipients subdoc (Admin-SDK-only).
    - lockSnapshots: ALLOW an org editor to write a lockSnapshots doc (genuine assertSucceeds — this is
      the one new collection the client MAY write); ALLOW an org member to read it; DENY a viewer writing
      it; DENY cross-org access (a member of orgB touching orgA's lockSnapshots).

    Seed fixtures with `seedMembershipDoc(orgId, uid, role)` and `seedDoc(...)` exactly as the pptxRenders
    tests do. This task is expected to run RED for the ALLOW cases (they fail against the current rules,
    which default-deny these paths) and GREEN for the deny cases — Task 2 turns the ALLOW cases green.
  </action>
  <verify>
    <automated>npm run test:rules</automated>
  </verify>
  <done>New describe blocks exist covering ALLOW + deny for messages/recipients/lockSnapshots at their full nested paths; the deny cases pass and the ALLOW cases are present (red until Task 2). If an emulator is already running and `npm run test:rules` reports port-taken, use `npx vitest run --config vitest.rules.config.ts` instead (CLAUDE.md).</done>
</task>

<task type="auto">
  <name>Task 2: Add nested deny-by-default rules blocks (GREEN)</name>
  <files>firestore.rules</files>
  <action>
    Inside the existing `match /services/{docId} { ... }` block in firestore.rules (opens ~line 100,
    closes ~line 128), before its closing brace, add the three nested blocks per 58-RESEARCH.md's
    "Rules block" code example and 58-PATTERNS.md's firestore.rules section — copying the two-segment
    nesting shape of the existing `songs/{songId}` → `match /lyrics/{lyricsId}` precedent (~189-200) and
    the member-read / Admin-only-write shape of `pptxRenders` (~202-217):

    - `match /messages/{messageId}`: `allow read: if isOrgMember(orgId);` `allow create: if
      isOrgEditor(orgId);` `allow update, delete: if false;` (status transitions are Admin-SDK-only,
      Phase 59+). Nested one level deeper inside it: `match /recipients/{recipientId}` with `allow read:
      if isOrgMember(orgId);` and `allow write: if false;` (Admin-SDK-only).
    - `match /lockSnapshots/{snapshotId}`: `allow read: if isOrgMember(orgId);` `allow write: if
      isOrgEditor(orgId);` (the client DOES write lock snapshots at lock time, Phase 61 — unlike
      pptxRenders' pure read-only).

    Confirm placement is INSIDE `match /services/{docId}` (so the resolved path is
    `organizations/{orgId}/services/{serviceId}/messages/...`), NOT a sibling under
    `organizations/{orgId}`. Do NOT touch the generic-wildcard exclusion list (the `collection != 'X'`
    comment block ~219-266): two-segment-deep paths fall through to default-deny automatically and need no
    exclusion entry, exactly as `songs/{id}/lyrics/{id}` does not appear there (58-PATTERNS.md). Do NOT
    deploy — leave the change built and undeployed.
  </action>
  <verify>
    <automated>npm run test:rules</automated>
  </verify>
  <done>All messages/recipients/lockSnapshots ALLOW and deny cases from Task 1 are green against the real firestore.rules text; the three blocks nest inside match /services/{docId}; the wildcard exclusion list is untouched; rules remain undeployed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client (Firestore SDK) → Firestore `services/{id}/messages`, `.../recipients`, `.../lockSnapshots` | Untrusted client reads/writes cross here; `firestore.rules` is the sole enforcement (no Cloud Function exists yet this phase) |
| Org A member → Org B's nested collections | Cross-org access attempt via a guessed/enumerated serviceId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-03a | Elevation of Privilege | viewer creating a `messages`/`lockSnapshots` doc directly via the client SDK | high | mitigate | `isOrgEditor(orgId)` create/write checks on the new blocks; exercised by the emulator DENY-cases. Client-side gating is UX only — the rules are the boundary. |
| T-58-03b | Information Disclosure / Tampering | Org A member reading/writing Org B's nested collections by enumerating a serviceId | high | mitigate | `isOrgMember/isOrgEditor(orgId)` resolve `orgId` from the `organizations/{orgId}/...` path segment, not a client field; cross-org DENY-case test proves it. No collectionGroup query in scope. |
| T-58-03c | Tampering | client forging a `messages.status`/`recipients.status` "sent" flag without a real send | high | mitigate | `allow update, delete: if false` on messages and `allow write: if false` on recipients — no client, editor or not, can write these; DENY-case tests assert it. Admin SDK (Phase 59+) is the only writer. |
| T-58-03d | Tampering / Repudiation | deploying an untested rules typo (silent fail-open/closed) | high | mitigate | `npm run test:rules` evaluates the ACTUAL `firestore.rules` text (readFileSync in beforeAll) in the emulator; a syntax/logic error fails the suite before the owner's manual deploy. |
| T-58-03e | Tampering | rule block placed as a sibling (wrong path) instead of nested | medium | mitigate | Every emulator test targets the full nested path, so a sibling misplacement fails the ALLOW cases (Pitfall 3). |
| T-58-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `npm run test:rules` green for all new messages/recipients/lockSnapshots ALLOW + deny cases (the rules
  suite starts its own emulator; if one is already up and it reports port-taken, run
  `npx vitest run --config vitest.rules.config.ts` — CLAUDE.md).
- The genuine ALLOW-cases (editor-creates-messages, member-reads-messages, editor-writes-lockSnapshots)
  PASS — not merely present. A deny-only pass is treated as a failure per CLAUDE.md's incident.
- `npm run type-check` clean (`vue-tsc --build`) — the rules test file is a `.ts` file typechecked by the
  build.
- `npx vitest run` (app suite) unchanged: `src/rules.test.ts` is excluded from it by `vite.config.ts`, so
  the app-suite baseline is unaffected. Do NOT use `vitest run --dir src` (pulls in rules.test.ts, fails
  with no emulator — a tooling artifact).
- HANDOVER: record the exact `firebase deploy --only firestore:rules` command in the SUMMARY and in
  `.planning/PENDING-VERIFICATION.md` as the owner's deploy step. Do NOT deploy.
</verification>

<success_criteria>
- messages/recipients/lockSnapshots are deny-by-default with explicit isOrgMember/isOrgEditor allow
  blocks nested inside `match /services/{docId}`.
- The emulator suite proves a genuine ALLOW-case per new collection AND the Admin-SDK-only + cross-org
  deny-cases, all against the real rules text.
- Rules ship undeployed; the exact deploy command is handed to the owner.
</success_criteria>

<output>
Create `.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-03-SUMMARY.md` when done.
</output>
