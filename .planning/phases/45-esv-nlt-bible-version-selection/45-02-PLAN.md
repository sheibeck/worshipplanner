---
phase: 45-esv-nlt-bible-version-selection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/organization.ts
  - src/stores/__tests__/auth.test.ts
  - src/views/SettingsView.vue
  - src/views/__tests__/SettingsView.test.ts
autonomous: true
requirements: [R090]

must_haves:
  truths:
    - "In Settings, an org editor can choose ESV or NLT for scripture passages; the choice persists to settings.bibleVersion"
    - "A church that never opens the setting resolves to NLT (owner override default) through the single loadOrgContext merge — no second defaults-merge point"
    - "Only 'ESV' or 'NLT' is ever written to settings.bibleVersion; a non-editor cannot save"
  artifacts:
    - src/types/organization.ts
    - src/views/SettingsView.vue
  key_links:
    - "DEFAULT_ORG_SETTINGS.bibleVersion='NLT' -> loadOrgContext spread merge (auth.ts:201-204) -> authStore.settings.bibleVersion"
    - "radio @change -> updateDoc('settings.bibleVersion') + authStore.settings.bibleVersion mirror-write (mirrors onToggleAiEnabled)"
---

<objective>
Add the church-level Bible version choice: the `bibleVersion: 'ESV' | 'NLT'` field on `OrgSettings` with a default of `'NLT'` (owner's locked override, D-Area1), and the "Bible Translation" card in `SettingsView.vue` that reads and writes it. Implements R090's storage + settings-UI half.

Per D-Area1 (locked): ONE field + ONE `DEFAULT_ORG_SETTINGS` entry, merged through the SINGLE existing `auth.ts::loadOrgContext` merge point (which already spreads `...DEFAULT_ORG_SETTINGS` at lines 201-204, so the field flows through with no second merge). The Settings card mirrors the existing AI Features / Planning Center / Vertical Worship toggle cards exactly — no new visual language (45-UI-SPEC.md).

Purpose: Give a church the R090 choice control and a sensible house default, on the same infrastructure Phases 39/44 established.
Output: the field + default, the Settings card, and tests proving the default resolution and the save/revert behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-esv-nlt-bible-version-selection/45-CONTEXT.md
@.planning/phases/45-esv-nlt-bible-version-selection/45-UI-SPEC.md
@.planning/phases/45-esv-nlt-bible-version-selection/45-RESEARCH.md

# Patterns to mirror (read before writing)
@src/types/organization.ts
@src/stores/auth.ts
@src/views/SettingsView.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrgSettings.bibleVersion field + DEFAULT_ORG_SETTINGS default ('NLT') + default-resolution test</name>
  <files>src/types/organization.ts, src/stores/__tests__/auth.test.ts</files>
  <behavior>
    - DEFAULT_ORG_SETTINGS.bibleVersion === 'NLT'.
    - After loadOrgContext for an org whose stored settings omit bibleVersion, authStore.settings.bibleVersion resolves to 'NLT' (the existing `...DEFAULT_ORG_SETTINGS` spread covers it — no new merge line needed).
    - After loadOrgContext for an org whose stored settings has bibleVersion: 'ESV', authStore.settings.bibleVersion resolves to 'ESV' (stored value wins over default via the existing `...orgSettings` spread order).
  </behavior>
  <action>Add `bibleVersion: 'ESV' | 'NLT'` to the `OrgSettings` interface in `src/types/organization.ts`, and add `bibleVersion: 'NLT'` to `DEFAULT_ORG_SETTINGS` (per 45-RESEARCH.md § Code Examples — the owner's locked override, NOT the "preserve current behavior" ESV default; add a short comment saying so per D-Area1). Do NOT add a second defaults-merge anywhere — verify by reading `src/stores/auth.ts::loadOrgContext` (lines 201-204) that the existing `{ ...DEFAULT_ORG_SETTINGS, ...orgSettings, ... }` spread already carries the new field; do not touch auth.ts. Add test cases to `src/stores/__tests__/auth.test.ts` asserting the three behaviors above (default resolution when absent, stored-value-wins, and DEFAULT constant value). This is the R090/R092-adjacent guarantee that the NLT default governs only churches that have never chosen — existing stored ESV choices are preserved.</action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/auth.test.ts</automated>
  </verify>
  <done>OrgSettings has a typed `bibleVersion`; DEFAULT is 'NLT'; loadOrgContext resolves absent->'NLT' and stored->stored through the single existing merge; no second merge point added; type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettingsView.vue "Bible Translation" card (ESV/NLT radio, save-on-change, editor-gated)</name>
  <files>src/views/SettingsView.vue, src/views/__tests__/SettingsView.test.ts</files>
  <behavior>
    - The card renders both options: "ESV (English Standard Version)" and "NLT (New Living Translation)", reflecting authStore.settings.bibleVersion as the checked value.
    - Selecting an option calls updateDoc with { 'settings.bibleVersion': <value> } and mirror-writes authStore.settings.bibleVersion, then shows "Saved!" for ~2000ms (mirrors onToggleAiEnabled).
    - On updateDoc rejection, the selection reverts to its prior value and "Failed to save. Please try again." appears.
    - A non-editor (authStore.isEditor false) cannot trigger a save (handler early-returns), matching the sibling toggles' guard.
    - Only 'ESV' or 'NLT' can be written — the control is a two-option exclusive radio, no free text (V5 input validation, T-45-21).
  </behavior>
  <action>Add a "Bible Translation" card to `src/views/SettingsView.vue` following the exact structure/handlers of the existing AI Features card (read `onToggleAiEnabled` at SettingsView.vue:656-676 and its markup; 45-RESEARCH.md § Code Examples reproduces it). Use the card shell `rounded-lg bg-gray-900 border border-gray-800 p-4 mt-6`, `<h2 class="text-sm font-semibold text-gray-300">Bible Translation</h2>`, the explanatory caption and both option labels verbatim from 45-UI-SPEC.md § Copywriting Contract, and a radio group (`text-indigo-600 focus:ring-indigo-500`, `text-sm text-gray-200` labels) bound to a local `bibleVersionInput` ref seeded from `authStore.settings.bibleVersion`. Write an `onChangeBibleVersion` handler shaped exactly like `onToggleAiEnabled`: guard on `authStore.orgId && authStore.isEditor`, `updateDoc(doc(db,'organizations',authStore.orgId), { 'settings.bibleVersion': newValue })`, mirror `authStore.settings.bibleVersion = newValue`, `savedFeedback` true then false after 2000ms, and on catch set the error message and revert `bibleVersionInput.value` to the prior value. No explicit Save button (save-on-`@change`), no loading spinner — consistency with siblings is the resolution (45-UI-SPEC.md § UI Considerations). Add data-testids consistent with the sibling cards' convention so the test can assert selection, save call, feedback, and revert-on-error. Add the test cases to `src/views/__tests__/SettingsView.test.ts`.</action>
  <verify>
    <automated>npx vitest run src/views/__tests__/SettingsView.test.ts</automated>
  </verify>
  <done>The card renders both options gated to editors, saves to settings.bibleVersion on change with mirror-write + "Saved!" feedback, reverts + shows the error on failure, and matches the sibling cards' visual contract; tests pass; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Firestore (organizations/{orgId}) | The Settings write of settings.bibleVersion crosses into persisted org config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-21 | Tampering (invalid value) | SettingsView.vue bibleVersion write | low | mitigate | The control is a two-option exclusive radio; only 'ESV'/'NLT' can be selected. The typed `OrgSettings.bibleVersion` union backs it; no free-text path exists. |
| T-45-22 | Elevation of Privilege | settings.bibleVersion write | medium | mitigate | Handler guards on `authStore.isEditor` (mirrors sibling toggles); Firestore rule `allow write: if isOrgEditor(orgId)` (firestore.rules:27-31) already governs the whole settings map — no rules change needed (45-RESEARCH.md § Security Domain). |
| T-45-23 | Repudiation/consistency | loadOrgContext merge | low | accept | Single-merge contract preserved; no second defaults path can diverge. Accepted as the established Phase 39/44 pattern. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies (45-RESEARCH.md § Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/auth.test.ts src/views/__tests__/SettingsView.test.ts` passes.
- `npm run type-check` (vue-tsc --build) clean.
- Full app suite baseline unchanged (`npx vitest run --dir src --exclude '**/rules.test.ts'` fails only `storage.rules.test.ts` + `RosterView.test.ts`).
- Deferred owner human-verify (record to PENDING-VERIFICATION.md § Phase 45, do not block per standing grant): visually confirm the Bible Translation card matches its sibling cards and the choice persists across reload.
</verification>

<success_criteria>
- R090 storage + settings-UI half complete: a church can choose ESV/NLT in Settings.
- Default resolves to NLT via the single existing merge; stored ESV choices preserved.
- Save/revert/feedback and editor-gating match the sibling toggle pattern; no new visual language.
</success_criteria>

<output>
Create `.planning/phases/45-esv-nlt-bible-version-selection/45-02-SUMMARY.md` when done.
</output>
