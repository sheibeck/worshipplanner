---
phase: 123-files-tab-songs-list-column-upload-external-link-attach
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SongTable.vue
  - src/components/__tests__/SongTable.test.ts
  - src/stores/songs.ts
autonomous: true
requirements: [R362]

must_haves:
  truths:
    - "The Songs list shows a Files column with a paperclip icon and none / 1 file / N files for each song, derived from song.attachments?.length ?? 0 (R362)."
    - "The Files column sits last, immediately before the trailing chevron, and is hidden when songStore.columnVisibility.files is false (R362)."
    - "The column cog menu lists a Files toggle that flips its visibility, and Reset restores it to visible."
    - "A legacy song with no attachments field renders 'none' without throwing."
  artifacts:
    - src/components/SongTable.vue
    - src/components/__tests__/SongTable.test.ts
  key_links:
    - "The cell reads song.attachments?.length off the already-subscribed Song doc (no fetch); DEFAULT_COLUMN_VISIBILITY.files=true plus the hydrate merge means a saved payload missing the key still defaults the column visible."
---

<objective>
Add the toggleable Songs-list **Files** column (R362): a paperclip icon plus plain-text
`none` / `1 file` / `N files`, positioned last before the trailing chevron, wired into the existing
column-visibility cog + store preference. Read-only display off the already-subscribed Song doc — no
upload/link logic here.

Purpose: Deliver R362 so an editor sees, at a glance in the library, which songs carry attachments.
Output: A new column in `SongTable.vue`, a `files` entry in the store's column-visibility map + cog
toggle list, and component-test coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Column pattern + store preference to extend
@src/components/SongTable.vue
@src/stores/songs.ts
@src/types/song.ts

# Authoritative column copy/classes (paperclip glyph, plain text not a pill)
@.planning/phases/121-song-files-ui-design-spec/121-UI-SPEC.md
@.planning/phases/123-files-tab-songs-list-column-upload-external-link-attach/123-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add files column key to the store preference (R362)</name>
  <files>src/stores/songs.ts</files>
  <action>
    In `src/stores/songs.ts`, add `files: true` to the `DEFAULT_COLUMN_VISIBILITY` map (alongside
    category/key/ccli/lastUsed/tags/themes, ~:51-58). No other store change is needed: `toggleColumn`,
    `resetColumns`, `persistColumnVisibility`, and `hydrateColumnVisibility` already operate generically
    over the map, and the hydrate path already merges saved payloads over the defaults
    (`{ ...DEFAULT_COLUMN_VISIBILITY, ...parsed }`), so an older saved preference that predates this key
    still defaults the Files column visible. Do not add a bespoke method.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>DEFAULT_COLUMN_VISIBILITY includes files:true; type-check clean; no new store method added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Files column th/td + cog toggle in SongTable (R362)</name>
  <files>src/components/SongTable.vue, src/components/__tests__/SongTable.test.ts</files>
  <behavior>
    - With columnVisibility.files true, the table renders a "Files" header and, per row, a paperclip
      icon plus text: 'none' for a song with no/empty attachments, '1 file' for one, 'N files' for N>=2.
    - A song whose attachments field is absent (legacy) renders 'none' and does not throw.
    - With columnVisibility.files false, neither the Files header nor its cells render.
    - The cog menu includes a "Files" checkbox that calls toggleColumn('files').
  </behavior>
  <action>
    In `src/components/SongTable.vue`, add the Files column as the LAST data column, immediately before
    the trailing chevron/cog `th` (~:157-158) and its matching `td` before the chevron `td` (~:283-284),
    matching the existing toggleable-column idiom exactly. Header:
    `<th v-if="songStore.columnVisibility.files" scope="col" class="px-4 py-3 text-left text-xs
    font-medium text-gray-400 uppercase tracking-wider">Files</th>`. Cell:
    `<td v-if="songStore.columnVisibility.files" class="px-4 py-3 text-gray-300">` containing a
    hand-inlined Heroicons-outline `paperclip` SVG (`h-4 w-4 text-gray-500`, the standard
    `fill="none" stroke="currentColor" stroke-width="2" stroke-linecap/linejoin="round"` icon attrs —
    source the exact glyph `d` path from Heroicons v2 outline) beside plain text (`text-sm`), laid out
    with a small flex/gap. This is PLAIN TEXT, not a pill/badge (the pill is reserved for the Files-tab
    count badge in plan 123-03). Compute the copy from `song.attachments?.length ?? 0`: 0 -> `none`,
    1 -> `1 file`, otherwise `${n} files` — add a small helper (e.g. a `filesLabel(song)` function or a
    computed-per-row expression) rather than inlining the ternary three ways. Add
    `{ key: 'files', label: 'Files' }` as the last entry of the `toggleableColumns` array (~:330-337) so
    the cog menu renders its checkbox via the existing v-for (no other cog change needed).

    Extend `src/components/__tests__/SongTable.test.ts` with cases covering every behavior above (mount
    with a Pinia store, toggle `columnVisibility.files`, and assert header/cell presence and the
    none/1 file/N files copy including the legacy no-attachments song).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongTable.test.ts && npm run type-check</automated>
  </verify>
  <done>SongTable tests pass; type-check clean; the Files column renders last before the chevron with paperclip + none/1 file/N files, toggles via the cog, and legacy songs render 'none' without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore Song doc -> rendered table cell | The column renders a derived count off the already-subscribed Song document; no new input crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-05 | Information disclosure | Files column count | low | accept | The column exposes only an attachment count off the subscribed doc — no new data or trust boundary. Per UI-SPEC Access Control, visibility of *that there are files* is intentionally unrestricted; `/songs` is `requiresEditor` regardless. |

No package-manager installs occur in this phase; the package-legitimacy gate is not applicable.
</threat_model>

<verification>
- `npm run type-check` clean (vue-tsc --build).
- `npx vitest run` at baseline: only `src/storage.rules.test.ts` fails; the extended SongTable tests
  green.
</verification>

<success_criteria>
The Songs list shows a toggleable Files column (paperclip + none/1 file/N files) last before the
chevron, gated on the store preference, defaulting visible, and safe for legacy songs with no
attachments field.
</success_criteria>

<output>
Create `.planning/phases/123-files-tab-songs-list-column-upload-external-link-attach/123-02-SUMMARY.md` when done
</output>
