---
phase: 88-editing-ux-polish-roles-teams-slideout-song-key-typeahead
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/keys.ts
  - src/constants/__tests__/keys.test.ts
  - src/components/ArrangementAccordion.vue
  - src/components/__tests__/ArrangementAccordion.test.ts
  - src/components/SongSlideOver.vue
  - src/components/__tests__/SongSlideOver.test.ts
autonomous: true
requirements: [R258]
must_haves:
  truths:
    - "In the song edit slideout, the Key field is a native typeahead: typing filters suggestions drawn from the 14 major keys, and a value can be picked from the list (R258)."
    - "A free-typed key not on the list (e.g. 'Am' from a CSV/Planning Center import) still saves and displays on the primary/first arrangement — the list does not restrict entry (R258)."
    - "ArrangementAccordion's Key dropdown renders the same 14 major keys, now sourced from the shared constant rather than an inline literal."
  artifacts:
    - "src/constants/keys.ts exporting MAJOR_KEYS (14 major roots) and MINOR_KEYS"
    - "SongSlideOver Key field rendered as <input list=…> paired with a <datalist> of MAJOR_KEYS"
  key_links:
    - "SongSlideOver Key input `list` attribute ↔ `<datalist id>` ↔ MAJOR_KEYS constant"
    - "ArrangementAccordion imports MAJOR_KEYS/MINOR_KEYS from src/constants/keys.ts (single source, no inline duplicate)"
    - "Key value still binds to the primary/first arrangement via the existing computed + onSave path (persistence unchanged from Phase 87 / R249)"
---

<objective>
Replace the Phase-87 free-text song **Key** input in `SongSlideOver.vue` with a native
`<input list=…>` + `<datalist>` type-ahead (R258), sourcing the option list from a new shared
`src/constants/keys.ts` constant that `ArrangementAccordion.vue` also consumes — so both surfaces
draw the 14 major keys from one place.

Purpose: The owner asked (v2.3 UAT) that Key be "a dropdown of available keys with type ahead"
instead of raw free text, while still accepting imported/unusual keys.
Output: `src/constants/keys.ts` (shared), an `<input list>`+`<datalist>` Key control in
SongSlideOver, ArrangementAccordion refactored onto the shared constant, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-editing-ux-polish-roles-teams-slideout-song-key-typeahead/88-CONTEXT.md

# The Key control being replaced + the datalist idiom to mirror (tags at list="ss-existing-user-tags")
@src/components/SongSlideOver.vue
# The inline majorKeys/minorKeys literals to extract into the shared constant (script @ ~line 179)
@src/components/ArrangementAccordion.vue
# CLAUDE.md test discipline: app tests via bare `npx vitest run` or a single-file path — never `--dir src`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared key constant and move ArrangementAccordion onto it</name>
  <files>src/constants/keys.ts, src/constants/__tests__/keys.test.ts, src/components/ArrangementAccordion.vue, src/components/__tests__/ArrangementAccordion.test.ts</files>
  <behavior>
    - keys.test.ts: MAJOR_KEYS deep-equals ['C','C#','Db','D','Eb','E','F','F#','Gb','G','Ab','A','Bb','B'] in that exact order (14 entries); MINOR_KEYS deep-equals ['Cm','C#m','Dm','Ebm','Em','Fm','F#m','Gm','Abm','Am','Bbm','Bm'].
    - ArrangementAccordion.test.ts: mounting the component with a minimal arrangement renders the Major optgroup with exactly the MAJOR_KEYS options (same count + values), proving it reads the shared constant.
  </behavior>
  <action>
    Create `src/constants/keys.ts` exporting two `as const` string arrays: `MAJOR_KEYS` (the 14 major
    roots currently inline at ArrangementAccordion's script `majorKeys`) and `MINOR_KEYS` (the current
    inline `minorKeys`). Keep both value lists byte-identical to today's literals and in the same order —
    this is a pure extraction, not a content change.

    Refactor `ArrangementAccordion.vue`: delete the two local `const majorKeys`/`const minorKeys`
    declarations and instead `import { MAJOR_KEYS, MINOR_KEYS } from '@/constants/keys'`. Update the two
    template `v-for` loops (the Major and Minor optgroups) to iterate the imported constants. No template
    markup or behavior changes beyond the binding source.

    Add `src/constants/__tests__/keys.test.ts` pinning the constant contents (per Behavior). Add
    `src/components/__tests__/ArrangementAccordion.test.ts` that mounts ArrangementAccordion with a minimal
    `arrangement` prop (shape from `@/types/song` Arrangement — read the component's `defineProps` for the
    exact fields: `arrangement: Arrangement`, `availableTags: string[]`) and asserts the Major optgroup's
    option values equal MAJOR_KEYS. Stub Teleport if needed; the component takes no store, so no pinia mock
    is required.
  </action>
  <verify>
    <automated>npx vitest run src/constants/__tests__/keys.test.ts src/components/__tests__/ArrangementAccordion.test.ts</automated>
  </verify>
  <done>keys.ts exports MAJOR_KEYS (14) + MINOR_KEYS; ArrangementAccordion imports them (no inline literals remain); both new test files pass; `npm run type-check` is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert the SongSlideOver Key field to an input+datalist typeahead</name>
  <files>src/components/SongSlideOver.vue, src/components/__tests__/SongSlideOver.test.ts</files>
  <behavior>
    - Typing in the Key field and picking a listed value (e.g. 'D') persists that key onto the primary/first arrangement via updateSong (existing round-trip still green).
    - A free-typed value NOT in MAJOR_KEYS (e.g. 'Am') is accepted and persists onto the primary/first arrangement — the datalist suggests but does not constrain.
    - The rendered Key control exposes a <datalist> whose <option> values equal MAJOR_KEYS (the 14 suggestions).
  </behavior>
  <action>
    In `SongSlideOver.vue`, locate the always-visible Key `<input>` bound to `primaryArrangementKey`
    (the one carrying `data-testid="song-key-input"`). Keep that same input, its `v-model`, and its
    `data-testid` unchanged — only add a `list="ss-key-options"` attribute and render a sibling
    `<datalist id="ss-key-options">` whose options iterate `MAJOR_KEYS` (mirror the existing tag datalist
    idiom already in this file: `list="ss-existing-user-tags"` + `<datalist id="ss-existing-user-tags">`).
    Import `MAJOR_KEYS` from `@/constants/keys`. Do not change the `primaryArrangementKey` computed, the
    save path, or the "Primary key" select below it — persistence stays exactly as R249 shipped, and free
    entry must remain allowed (datalist gives that for free; do NOT add any list-membership validation).

    Update `src/components/__tests__/SongSlideOver.test.ts`: the existing "key (R249)" describe block uses
    `data-testid="song-key-input"` + `setValue` and continues to pass unchanged. Add two focused tests to
    that block: (a) the Key control's datalist renders exactly the MAJOR_KEYS option values; (b) setting the
    Key input to a value absent from MAJOR_KEYS (e.g. 'Am') and clicking Save still calls updateSong with
    that free-typed key on the primary/first arrangement (assert on the arrangements payload, mirroring the
    existing R249 assertions).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongSlideOver.test.ts</automated>
  </verify>
  <done>Key field renders `<input list="ss-key-options">` + a `<datalist>` of the 14 major keys; free-typed non-list keys still save; all existing SongSlideOver key tests plus the two new ones pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (songs) | Key edits are written through the existing `songStore.updateSong`; no new write path or field is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-01 | Tampering | SongSlideOver Key input → arrangement.key | low | accept | Free-text key was already accepted (R249); a datalist only suggests values and adds no new trust surface. Existing org-scoped `firestore.rules` on the songs collection are unchanged. |
| T-88-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies are installed — the datalist uses native HTML and the constant is local. No package legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run src/constants/__tests__/keys.test.ts src/components/__tests__/ArrangementAccordion.test.ts src/components/__tests__/SongSlideOver.test.ts` — all green.
- `npm run type-check` (vue-tsc --build) — clean (per CLAUDE.md, this gate typechecks test files too; do NOT rely on `-p tsconfig.app.json`).
- Grep confirms no `majorKeys`/`minorKeys` inline literal remains in ArrangementAccordion.vue.
</verification>

<success_criteria>
The song Key control is a searchable type-ahead of the 14 major keys that still accepts free entry and
persists to the primary/first arrangement; ArrangementAccordion and the Key datalist share one constant
source; targeted suites and type-check pass.
</success_criteria>

<output>
Create `.planning/phases/88-editing-ux-polish-roles-teams-slideout-song-key-typeahead/88-01-SUMMARY.md` when done.
</output>
