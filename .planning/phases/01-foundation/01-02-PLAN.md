---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/views/LoginView.vue
  - src/views/DashboardView.vue
  - src/components/AppShell.vue
  - src/components/AppSidebar.vue
  - src/components/GettingStarted.vue
  - src/App.vue
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02

user_setup:
  - service: firebase
    why: "Authentication and Firestore database"
    env_vars:
      - name: VITE_FIREBASE_API_KEY
        source: "Firebase Console -> Project Settings -> General -> Your apps -> Web app -> Config"
      - name: VITE_FIREBASE_AUTH_DOMAIN
        source: "Same location as above"
      - name: VITE_FIREBASE_PROJECT_ID
        source: "Same location as above"
      - name: VITE_FIREBASE_STORAGE_BUCKET
        source: "Same location as above"
      - name: VITE_FIREBASE_MESSAGING_SENDER_ID
        source: "Same location as above"
      - name: VITE_FIREBASE_APP_ID
        source: "Same location as above"
    dashboard_config:
      - task: "Create Firebase project (if not already done)"
        location: "https://console.firebase.google.com -> Add project"
      - task: "Enable Google sign-in provider"
        location: "Firebase Console -> Authentication -> Sign-in method -> Google -> Enable"
      - task: "Enable Email/Password sign-in provider"
        location: "Firebase Console -> Authentication -> Sign-in method -> Email/Password -> Enable"
      - task: "Create a Firestore database"
        location: "Firebase Console -> Firestore Database -> Create database (start in test mode, rules will be overwritten on deploy)"
      - task: "Add a web app to the project"
        location: "Firebase Console -> Project Settings -> General -> Your apps -> Add app -> Web"

must_haves:
  truths:
    - "User sees a clean login page with a prominent Google sign-in button and a smaller email/password form"
    - "User can click Google sign-in and authenticate via popup"
    - "User can enter email and password to sign in or auto-create an account"
    - "User can click Forgot password and receive a reset email"
    - "After sign-in, user sees a dashboard with a sidebar and getting started checklist"
    - "Sidebar collapses to hamburger menu on mobile viewports"
    - "Signed-in user stays signed in across page refresh without flash to login"
    - "Unauthenticated user accessing / is redirected to /login"
    - "User can sign out from the sidebar and is returned to /login"
  artifacts:
    - path: "src/views/LoginView.vue"
      provides: "Sign-in page with Google and email/password options"
      min_lines: 80
    - path: "src/views/DashboardView.vue"
      provides: "Protected dashboard with getting started checklist"
      min_lines: 30
    - path: "src/components/AppShell.vue"
      provides: "Layout wrapper with sidebar and main content area"
      min_lines: 20
    - path: "src/components/AppSidebar.vue"
      provides: "Persistent sidebar with nav links and mobile collapse"
      min_lines: 50
    - path: "src/components/GettingStarted.vue"
      provides: "Onboarding checklist for empty dashboard state"
      min_lines: 40
  key_links:
    - from: "src/views/LoginView.vue"
      to: "src/stores/auth.ts"
      via: "useAuthStore() for loginWithGoogle, loginWithEmail, resetPassword"
      pattern: "useAuthStore"
    - from: "src/components/AppSidebar.vue"
      to: "src/stores/auth.ts"
      via: "useAuthStore() for logout and user display"
      pattern: "useAuthStore"
    - from: "src/App.vue"
      to: "src/components/AppShell.vue"
      via: "AppShell wraps router-view for authenticated routes"
      pattern: "AppShell"
    - from: "src/views/LoginView.vue"
      to: "vue-router"
      via: "router.push('/') after successful sign-in"
      pattern: "router\\.push"
---

<objective>
Build all user-facing UI for Phase 1: the login page, app shell with sidebar navigation, and dashboard with getting started checklist.

Purpose: Delivers the visual experience users interact with — the sign-in flow, the authenticated app layout, and the onboarding guidance. Consumes the auth store and router from Plan 01.

Output: A fully functional authentication UX where users can sign in with Google or email, see their dashboard with a sidebar, and follow a getting started checklist.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes. -->

From src/stores/auth.ts (created in Plan 01):
```typescript
export const useAuthStore = defineStore('auth', () => {
  const user: Ref<User | null>
  const isReady: Ref<boolean>
  const isAuthenticated: ComputedRef<boolean>

  async function loginWithGoogle(): Promise<User | null>
  async function loginWithEmail(email: string, password: string): Promise<UserCredential>
  async function registerWithEmail(email: string, password: string): Promise<UserCredential>
  async function resetPassword(email: string): Promise<void>
  async function logout(): Promise<void>
})
```

From src/router/index.ts (created in Plan 01):
```typescript
// Routes:
// /login -> LoginView.vue (public)
// /     -> DashboardView.vue (meta: { requiresAuth: true })
```

From src/firebase/index.ts (created in Plan 01):
```typescript
export const app: FirebaseApp
export const auth: Auth
export const db: Firestore
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LoginView, AppShell, AppSidebar, and DashboardView with GettingStarted checklist</name>
  <files>
    src/views/LoginView.vue, src/views/DashboardView.vue,
    src/components/AppShell.vue, src/components/AppSidebar.vue,
    src/components/GettingStarted.vue, src/App.vue
  </files>
  <action>
    **All components use `<script setup lang="ts">` and Tailwind CSS utility classes.**

    **Visual tone:** Clean and professional. Minimal, muted colors, whitespace-heavy, tool-focused (think Linear/Notion). Light theme only. Use a neutral color palette — slate/gray tones for text and borders, a single accent color (indigo or blue) for primary actions. Inter or system font stack.

    1. **`src/views/LoginView.vue`** — Full-page login centered vertically and horizontally:
       - "WorshipPlanner" title text (no logo/hero imagery — locked decision)
       - Large "Sign in with Google" button — prominent, primary styling, full-width within the card. Use a Google "G" SVG icon inline (do NOT import an icon library for one icon).
       - Horizontal divider with "or" text
       - Email + password form below (smaller/secondary feel):
         - Email input, password input, submit button ("Sign in")
         - "Forgot password?" link below the form
       - When "Forgot password?" is clicked: show inline email-only form with "Send reset link" button. On success, show confirmation message. On error, show error message.
       - Loading states: disable buttons and show spinner text ("Signing in...") during auth calls
       - Error handling: display Firebase error messages in a red text block below the form. Map common error codes to friendly messages:
         - `auth/wrong-password` -> "Incorrect password. Try again or reset your password."
         - `auth/too-many-requests` -> "Too many attempts. Please try again later."
         - `auth/popup-closed-by-user` -> (no error, just return to idle state)
         - `auth/invalid-email` -> "Please enter a valid email address."
         - Default: show the error message text
       - After successful sign-in (any method), call `router.push('/')`
       - Uses `useAuthStore()` for `loginWithGoogle`, `loginWithEmail`, `resetPassword`

    2. **`src/components/AppShell.vue`** — Layout wrapper:
       - Flex container: AppSidebar on the left, `<slot />` (main content) on the right taking remaining width
       - On mobile (below `lg` breakpoint): sidebar is hidden by default, revealed by hamburger toggle
       - The shell manages the `sidebarOpen` state and passes it to AppSidebar

    3. **`src/components/AppSidebar.vue`** — Persistent left sidebar:
       - Width: ~64px collapsed (icon-only) on medium screens, ~240px expanded on large screens. On mobile: full overlay when open, hidden when closed.
       - Top: "WorshipPlanner" text/brand
       - Nav items (placeholder for future phases, but scaffold them now):
         - Dashboard (home icon) — links to `/`
         - Songs (music note icon) — links to `/songs` (not yet routed, just the link)
         - Services (calendar icon) — links to `/services` (not yet routed)
         - Tasks (checklist icon) — links to `/tasks` (not yet routed)
       - Use simple inline SVG icons (heroicons-style, ~20x20). Do NOT install an icon library.
       - Bottom: User info display (avatar circle with initials from `user.displayName` or `user.email`, truncated email), and "Sign out" button
       - Sign out: calls `authStore.logout()` then `router.push('/login')`
       - Active nav item highlighted with accent color background
       - Mobile: hamburger button (three horizontal lines) in top-left triggers sidebar overlay. Clicking outside or on a nav item closes it.

    4. **`src/views/DashboardView.vue`** — Protected dashboard page:
       - Wrapped in AppShell
       - Page header: "Dashboard" title, greeting "Welcome, {displayName}" (or "Welcome" if no displayName)
       - Main content: GettingStarted component (this IS the dashboard content for Phase 1 — the empty state)

    5. **`src/components/GettingStarted.vue`** — Onboarding checklist:
       - Card-style container with "Getting Started" heading
       - Steps (hardcoded for now, dynamic completion tracking deferred to when features exist):
         1. "Sign in to your account" — auto-complete (always checked since user is viewing this)
         2. "Import your song library" — unchecked, links to `/songs` with descriptive text "Import songs from Planning Center CSV or add them manually"
         3. "Plan your first service" — unchecked, links to `/services` with descriptive text "Create a service plan with the Vertical Worship methodology"
         4. "Share with your team" — unchecked, descriptive text "Invite team members to collaborate" (Phase 5)
       - Each step: checkbox (visual only for now), step title, description text, and an arrow/link indicator for actionable steps
       - Checked steps get a green checkmark and muted text styling
       - Clean card design consistent with the Linear/Notion aesthetic — subtle borders, rounded corners, good spacing

    6. **Update `src/App.vue`**:
       - Show a full-screen loading spinner/skeleton while `authStore.isReady` is `false` (prevents flash-of-login-page on refresh)
       - Once `isReady` is true, render `<router-view />`
       - The loading state should be minimal — centered spinner or "Loading..." text with the WorshipPlanner name

    **Important anti-patterns to avoid:**
    - Do NOT use any component libraries (Vuetify, PrimeVue, etc.) — pure Tailwind
    - Do NOT install icon libraries — use inline SVG
    - Do NOT add dark mode classes or toggle
    - Do NOT create a separate registration page — email sign-in auto-creates (locked decision)
    - Do NOT use `signInWithRedirect` anywhere — only `signInWithPopup` (locked decision)
  </action>
  <verify>
    <automated>cd C:/projects/worshipplanner && npm run build 2>&1 | tail -5 && npx vue-tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - LoginView renders with prominent Google button and secondary email/password form
    - LoginView handles forgot password flow inline
    - LoginView shows loading states and friendly error messages
    - AppShell renders sidebar + content layout
    - AppSidebar shows nav items, user info, sign out button
    - AppSidebar collapses to hamburger on mobile
    - DashboardView shows welcome greeting and GettingStarted checklist
    - GettingStarted shows 4-step onboarding checklist with step 1 auto-checked
    - App.vue shows loading state until auth isReady resolves
    - Build succeeds with zero errors
    - No component libraries or icon libraries installed — pure Tailwind + inline SVG
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 authentication flow and app shell</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 1 authentication flow and app shell — sign-in page (Google + email/password), protected dashboard with sidebar navigation, getting started checklist, and Firestore security rules.

    Prerequisites:
    - Copy `.env.local.example` to `.env.local` and fill in your Firebase project credentials
    - Ensure Firebase project has Google and Email/Password sign-in providers enabled
    - Install firebase-tools globally if not installed: `npm install -g firebase-tools`

    Test with emulators (no Firebase credentials needed for basic test):
    1. Run `firebase emulators:start` in one terminal
    2. Run `npm run dev` in another terminal
    3. Visit the dev server URL (likely http://localhost:5173)

    Verify these behaviors:
    1. **Unauthenticated redirect:** Opening http://localhost:5173/ redirects to /login
    2. **Login page appearance:** Clean, minimal page with "WorshipPlanner" title, large Google sign-in button on top, email/password form below
    3. **Google sign-in:** Click the Google button — popup appears, sign in succeeds, redirected to dashboard
    4. **Email sign-in:** Enter an email + password — if new account, auto-creates; if existing, signs in. Redirected to dashboard
    5. **Forgot password:** Click "Forgot password?" — email-only form appears, submitting shows confirmation
    6. **Dashboard:** After sign-in, see sidebar on left with nav items (Dashboard, Songs, Services, Tasks) and main area with "Getting Started" checklist
    7. **Getting started checklist:** Step 1 ("Sign in") is checked, steps 2-4 are unchecked with descriptions
    8. **Sidebar mobile:** Resize browser below ~1024px — sidebar becomes hamburger menu
    9. **Sign out:** Click "Sign out" in sidebar — returns to login page
    10. **Persistence:** After sign-in, refresh the page — user stays signed in (no flash to login)
    11. **Visual quality:** Overall look is clean, professional, tool-focused (Linear/Notion feel)
  </action>
  <verify>Human confirms all 11 verification items pass. Type "approved" or describe issues.</verify>
  <done>All authentication flows work correctly, UI matches design requirements, app is ready for Phase 2.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run` — all tests from Plan 01 still pass
- `npx vue-tsc --noEmit` — no TypeScript errors
- Manual browser verification of all 11 items in the checkpoint
</verification>

<success_criteria>
- Login page matches locked decisions: Google-first, no separate registration, minimal branding, forgot password flow
- App shell matches locked decisions: sidebar navigation, mobile hamburger collapse, clean professional tone
- Dashboard shows getting started checklist as empty state
- Light theme only (no dark mode)
- Auth flow works end-to-end: sign in -> dashboard -> sign out -> login
- Page refresh maintains auth state without flash
- Unauthenticated access redirected to login
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
