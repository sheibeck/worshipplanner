---
phase: 109-behavioral-architectural-extraction-comment-convention
plan: 05
type: execute
wave: 4
depends_on: ['109-04']
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/STACK.md
  - src/stores/appConfig.ts
  - src/stores/auth.ts
  - src/stores/quarters.ts
  - src/stores/services.ts
  - src/stores/songLyrics.ts
  - src/stores/toasts.ts
  - src/stores/orgScopedStores.ts
  - src/stores/pptxRenders.ts
  - src/stores/slideGroups.ts
  - src/types/organization.ts
  - src/types/pptxRender.ts
  - src/types/service.ts
  - src/types/slide.ts
  - src/types/slideGroup.ts
  - src/types/songLyrics.ts
  - src/types/importedDeck.ts
  - src/config/appConfigDefaults.ts
  - src/config/slideFonts.ts
  - src/firebase/index.ts
  - src/main.ts
  - src/views/ServiceEditorView.vue
  - src/views/serviceEditorActionBar.ts
autonomous: true
requirements: [R318]

must_haves:
  truths:
    - "Every Bucket B handoff entry whose source path is src/stores/**, src/types/**, src/config/**, src/firebase/**, src/main.ts, or src/views/** has its 'how it works' narration relocated into the target .planning/codebase/ map doc named for it in the 108 handoff, OR is explicitly kept inline with a one-line justification — no silent drops (R318)."
    - "Each relocated store/type/view comment is reduced to a short map-doc pointer per the convention (R318)."
    - "The comment-only edits change no behavior: type-check passes and the app test suite shows no NEW failures versus the pre-edit baseline (R318)."
    - "Across all five R318 plans (109-02..109-05) the relocated + kept-inline entries reconcile to the 108 handoff's stated 309 total — no drops phase-wide (R318)."
  artifacts:
    - ".planning/codebase/ARCHITECTURE.md / INTEGRATIONS.md / CONCERNS.md / STACK.md (store/type/view behavioral notes relocated in)"
    - "src/stores/**, src/types/**, src/config/**, src/firebase/index.ts, src/main.ts, src/views/** source comments shrunk to pointers"
  key_links:
    - "each shrunk store/type/view pointer -> the exact map-doc section that now holds its narration (self-consistent within this plan)"
    - "services.ts R036 draft-only write-guard + stageLayout public-projection notes -> ARCHITECTURE.md, so the defense-in-depth and field-allowlist contracts survive the shrink"
    - "phase-wide entry count -> the handoff's 309 total (final reconciliation happens here, the last R318 plan)"
---

<objective>
Relocate the behavioral/architectural narration out of the store / type / config / entry-point / view
source comments into the `.planning/codebase/` map docs and shrink each source comment to a pointer — the
final R318 sweep, covering `src/stores/**`, `src/types/**`, `src/config/**`, `src/firebase/index.ts`,
`src/main.ts`, and `src/views/**` (~66 entries; ServiceEditorView.vue alone ~16).

This plan owns every Bucket B handoff entry whose source path is in those trees, and — as the LAST R318
plan — performs the phase-wide completeness reconciliation against the 108 handoff's stated 309 total.

Purpose: the stores hold the org-scoped-store teardown and lifecycle-lock contracts, the types hold the
additive-field/no-migration conventions, and ServiceEditorView holds the multi-instance-Sortable and
lifecycle-seam narration — all "how it works" that belongs in the map docs per 109-CONTEXT.md.
Output: updated map docs + shrunk source comments + a phase-wide no-drops reconciliation.

Dependency note: this plan appends to the same four map docs as 109-02/03/04; it is serialized to Wave 4
(after 109-04) so no two plans write the shared docs concurrently. The dependency is doc-write
serialization, not content.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-behavioral-architectural-extraction-comment-convention/109-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@CLAUDE.md

Worklist (do NOT @-ref — ~488KB; read the named line range only):
`.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md` lines
~6951-7272 ("Phase 109 Handoff"). This plan's scope = every entry whose bolded source path begins
`src/stores/`, `src/types/`, `src/config/`, `src/firebase/`, `src/main.ts`, or `src/views/`.

Follow the RELOCATION PROTOCOL defined in 109-02-PLAN.md Task 1. Heed the 108-02-SUMMARY.md lesson about
`src/stores/slideGroups.ts`: it has one large JSDoc that was split across two hits — hand-edit it, do not
run automated boundary-recovery across it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate + shrink src/stores/**, src/config/**, src/firebase/index.ts, src/main.ts</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/STACK.md, src/stores/appConfig.ts, src/stores/auth.ts, src/stores/quarters.ts, src/stores/services.ts, src/stores/songLyrics.ts, src/stores/toasts.ts, src/stores/orgScopedStores.ts, src/stores/pptxRenders.ts, src/stores/slideGroups.ts, src/config/appConfigDefaults.ts, src/config/slideFonts.ts, src/firebase/index.ts, src/main.ts</files>
  <read_first>
    - 108-COMMENT-INVENTORY.md lines ~6951-7272: entries whose path begins `src/stores/`, `src/config/`,
      `src/firebase/`, or is `src/main.ts` (this task's sub-scope; services.ts ~9 entries, auth.ts ~5).
    - The four map docs' section layout (relevant homes: ARCHITECTURE.md for the services.ts write-guard/
      lifecycle + stageLayout projection; STACK.md for orgScopedStores/pptxRenders/slideGroups/firebase/
      main entry-point notes).
    - The full source comment for each entry (handoff truncates at "...").
  </read_first>
  <action>
    Apply the RELOCATION PROTOCOL to every in-scope entry in these files. Relocate the services.ts
    draft-only write-guard narration (defense-in-depth layer 2 of 3) and the stageLayout public-projection
    field-allowlist narration to ARCHITECTURE.md, keeping a one-line invariant inline. Note firebase/index.ts
    "import.meta.env.DEV is load-bearing, do not simplify" and main.ts "deliberately NO module-load
    requestFullscreen" — these are gotchas that stay inline as one-liners with a pointer. Hand-edit
    slideGroups.ts (shared-JSDoc hazard). Commit atomically.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['src/stores/appConfig.ts','src/stores/auth.ts','src/stores/quarters.ts','src/stores/services.ts','src/stores/songLyrics.ts','src/stores/toasts.ts','src/stores/orgScopedStores.ts','src/stores/pptxRenders.ts','src/stores/slideGroups.ts','src/config/appConfigDefaults.ts','src/config/slideFonts.ts','src/firebase/index.ts','src/main.ts'];let c=0;for(const f of files){c+=(fs.readFileSync(f,'utf8').match(/See \.planning\/codebase\//g)||[]).length;}if(c<15){console.error('too few pointers:',c);process.exit(1);}console.log('pointers:',c)"</automated>
  </verify>
  <acceptance_criteria>
    - Every in-scope stores/config/firebase/main entry is relocated+shrunk or recorded as kept-inline — none dropped.
    - The listed files collectively contain at least ~15 `See .planning/codebase/` pointers.
    - Narration each pointer references exists in the named map doc + section.
    - Comment-only diffs (no executable line changed).
  </acceptance_criteria>
  <done>Stores/config/firebase/main relocated, shrunk, committed; pointers resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate + shrink src/types/** and src/views/**</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/STACK.md, src/types/organization.ts, src/types/pptxRender.ts, src/types/service.ts, src/types/slide.ts, src/types/slideGroup.ts, src/types/songLyrics.ts, src/types/importedDeck.ts, src/views/ServiceEditorView.vue, src/views/serviceEditorActionBar.ts</files>
  <read_first>
    - 108-COMMENT-INVENTORY.md lines ~6951-7272: entries whose path begins `src/types/` or `src/views/`
      (this task's sub-scope; ServiceEditorView.vue ~16 entries across all four docs).
    - The four map docs after Task 1's additions, to append consistently.
  </read_first>
  <action>
    Apply the RELOCATION PROTOCOL to every in-scope entry under `src/types/**` and `src/views/**`. The type
    files carry the "additive, optional, no-migration — absent on every doc written before this field"
    convention repeated across service/slide/organization/stageLayout — relocate the fuller explanation to
    the map doc and leave a one-line "additive, no migration" residue. ServiceEditorView carries the
    multi-instance Sortable + lifecycle-seam narration for ARCHITECTURE.md/STACK.md; relocate and point.
    Preserve Vue template vs script comment styles. Commit atomically.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['src/types/organization.ts','src/types/pptxRender.ts','src/types/service.ts','src/types/slide.ts','src/types/slideGroup.ts','src/types/songLyrics.ts','src/types/importedDeck.ts','src/views/ServiceEditorView.vue','src/views/serviceEditorActionBar.ts'];let c=0;for(const f of files){c+=(fs.readFileSync(f,'utf8').match(/See \.planning\/codebase\//g)||[]).length;}if(c<18){console.error('too few pointers:',c);process.exit(1);}console.log('pointers:',c)"</automated>
  </verify>
  <acceptance_criteria>
    - Every in-scope types/views entry is relocated+shrunk or recorded as kept-inline — none dropped.
    - The listed files collectively contain at least ~18 `See .planning/codebase/` pointers.
    - Comment-only diffs (no executable/template logic changed).
  </acceptance_criteria>
  <done>Types + views relocated, shrunk, committed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove no behavior change + PHASE-WIDE completeness reconciliation (309)</name>
  <files>src/views/ServiceEditorView.vue, src/stores/services.ts</files>
  <read_first>
    - CLAUDE.md: "Type-checking" (`npm run type-check`), "Testing" (bare `npx vitest run`; baseline = only
      `src/storage.rules.test.ts`; render-service via `cd render-service && npm test`; do NOT use `--dir src`).
    - 108's deferred-items.md: the pre-existing `appConfig.test.ts` failure is not a regression.
    - The prior SUMMARYs 109-02/03/04 (their recorded relocated + kept-inline counts) for the phase-wide sum.
  </read_first>
  <action>
    Prove zero behavior change for THIS plan's edits (type-check + app suite; render-service is untouched
    across the whole phase — zero Bucket B entries — but run it once to confirm the 39/39 baseline is
    intact). Then do the PHASE-WIDE reconciliation: sum the relocated + kept-inline entry counts recorded in
    109-02, 109-03, 109-04 SUMMARYs plus this plan's, and confirm the total equals the 108 handoff's stated
    309 (the handoff's "Completeness guarantee"). Also confirm every `See .planning/codebase/` pointer added
    across the phase resolves to a map doc that exists. Any shortfall against 309 means an entry was dropped
    — locate and relocate it before finishing.
  </action>
  <verify>
    <automated>npm run type-check && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0.
    - `npx vitest run` shows no NEW failing files versus the pre-edit baseline (known baseline only).
    - `cd render-service && npm test` still 39/39 (untouched).
    - The phase-wide sum of relocated + kept-inline entries (109-02 + 109-03 + 109-04 + 109-05) equals the
      handoff's 309 — recorded in this SUMMARY; no drops.
    - Every `See .planning/codebase/` pointer added in the phase names a map doc file that exists.
  </acceptance_criteria>
  <done>Type-check clean, no new test failures, and the phase-wide entry count reconciles to 309 with zero drops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment edit -> compiled app | A malformed comment/type-doc edit could drop a delimiter and change executable TS in a store or the largest view. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-10 | Tampering | Shared-JSDoc boundary-recovery corrupting slideGroups.ts (repeat of Phase 108 near-miss) | high | mitigate | Hand-edit slideGroups.ts; comment-only diffs; Task 3 type-check + app suite must show no new failures. |
| T-109-11 | Repudiation | An entry silently dropped between the 309-count handoff and the finished sweep | high | mitigate | Task 3 phase-wide reconciliation to 309 across all four R318 SUMMARYs; any shortfall is located and relocated before finishing. |

No package-manager installs occur in this phase, so the supply-chain threat (T-{phase}-SC) does not apply.
</threat_model>

<verification>
- Every in-scope stores/types/config/firebase/main/views handoff entry relocated+shrunk or explicitly kept-inline.
- Phase-wide relocated + kept-inline total equals the handoff's 309 (no drops).
- Source files carry `See .planning/codebase/` pointers resolving to real map-doc sections.
- `npm run type-check` exits 0; `npx vitest run` shows no NEW failures versus baseline; render-service 39/39.
- Comment-only diffs.
</verification>

<success_criteria>
- R318 fully satisfied phase-wide: all 309 Bucket B "how it works" narrations relocated into the map docs
  (or explicitly kept inline with reason), source comments shrunk to pointers, nothing dropped, zero
  behavior change.
</success_criteria>

## Artifacts this phase produces

- `.planning/codebase/ARCHITECTURE.md`, `INTEGRATIONS.md`, `CONCERNS.md`, `STACK.md` — store/type/view
  behavioral notes relocated in (R318).
- `src/stores/**` (9), `src/types/**` (7), `src/config/**` (2), `src/firebase/index.ts`, `src/main.ts`,
  `src/views/**` (2) — source comments shrunk to `See .planning/codebase/<DOC>.md (<section>)` pointers (R318).
- Phase-wide reconciliation record proving all 309 Bucket B entries accounted for.

<output>
Create `.planning/phases/109-behavioral-architectural-extraction-comment-convention/109-05-SUMMARY.md` when done.
Record the in-scope entry count, the relocated count, every explicit "kept inline" decision with its reason,
and the phase-wide 309 reconciliation.
</output>
