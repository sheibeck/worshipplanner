---
phase: 109-behavioral-architectural-extraction-comment-convention
plan: 04
type: execute
wave: 3
depends_on: ['109-03']
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/STACK.md
  - src/components/ContextualActionBar.vue
  - src/components/MiscLabelBadge.vue
  - src/components/PptxImportModal.vue
  - src/components/SongSlotPicker.vue
  - src/components/SongTable.vue
  - src/components/actionBarItems.ts
  - src/components/admin/CleanupEnableConfirmDialog.vue
  - src/components/admin/DeactivateOrgConfirmDialog.vue
  - src/components/run/RunFilmstrip.vue
  - src/components/run/RunHeader.vue
  - src/components/run/RunPreviewPair.vue
  - src/components/run/RunRail.vue
  - src/components/run/RunTransportBar.vue
  - src/components/run/RunDisplaysPanel.vue
  - src/components/settings/ServiceTemplateEditor.vue
  - src/components/slides/BackgroundControl.vue
  - src/components/slides/EditSlideDrawer.vue
  - src/components/slides/SlideCanvas.vue
  - src/components/slides/SlideGrid.vue
  - src/components/slides/SlideGroupMusicControl.vue
  - src/components/slides/SlidesTab.vue
  - src/components/slides/SlotLoopControl.vue
  - src/components/slides/dropRouting.ts
  - src/components/slides/slideDisplay.ts
  - src/components/slides/SlideCard.vue
  - src/components/slides/SlideDropTarget.vue
  - src/components/stage/StageKindIcon.vue
  - src/components/stage/StageLayoutEditor.vue
  - src/components/stage/StageLayoutPrintDocument.vue
  - src/components/stage/StageMarkerChip.vue
  - src/components/stage/StageRoom.vue
  - src/components/stage/StageLayoutView.vue
  - src/composables/useAutoSave.ts
  - src/composables/useBackgroundUpload.ts
  - src/composables/useLoopTimer.ts
  - src/composables/useOutputWindow.ts
  - src/composables/useRunControl.ts
  - src/composables/useRunTimers.ts
  - src/composables/useSlideshowAssembly.ts
  - src/composables/useUnsavedGuard.ts
  - src/composables/useMediaUpload.ts
autonomous: true
requirements: [R318]

must_haves:
  truths:
    - "Every Bucket B handoff entry whose source path is src/components/** or src/composables/** has its 'how it works' narration relocated into the target .planning/codebase/ map doc named for it in the 108 handoff, OR is explicitly kept inline with a one-line justification — no silent drops (R318)."
    - "Each relocated component/composable comment is reduced to a short map-doc pointer per the convention (R318)."
    - "The comment-only edits change no behavior: type-check passes and the app test suite shows no NEW failures versus the pre-edit baseline (R318)."
  artifacts:
    - ".planning/codebase/ARCHITECTURE.md / INTEGRATIONS.md / CONCERNS.md / STACK.md (component & composable behavioral notes relocated in)"
    - "src/components/** and src/composables/** source comments shrunk to pointers"
  key_links:
    - "each shrunk component/composable pointer -> the exact map-doc section that now holds its narration (self-consistent within this plan)"
    - "useRunControl.ts single-loop-timer / output-orchestration notes -> ARCHITECTURE.md, so the 'timer lives in control, outputs are receive-only' anti-pattern survives the shrink"
---

<objective>
Relocate the behavioral/architectural narration out of the `src/components/**` and `src/composables/**`
source comments into the `.planning/codebase/` map docs and shrink each source comment to a pointer — the
R318 sweep for the component + composable trees (~66 entries).

This plan owns every Bucket B handoff entry whose source path is `src/components/**` or `src/composables/**`.
It works the "Phase 109 Handoff" worklist in `108-COMMENT-INVENTORY.md` (lines ~6951-7272).

Purpose: the Run-flow composables (useRunControl, useOutputWindow, useLoopTimer) and the slides/stage
components carry long "how the whole feature works" narration and hard-won anti-patterns (single loop
timer, receive-only outputs, fullscreen delegation) — exactly the behavior the map docs should hold per
109-CONTEXT.md.
Output: updated map docs + shrunk component/composable source comments.

Dependency note: this plan appends to the same four map docs as 109-02/03/05; it is serialized to Wave 3
(after 109-03) so no two plans write the shared docs concurrently. The dependency is doc-write
serialization, not content.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-behavioral-architectural-extraction-comment-convention/109-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@CLAUDE.md

Worklist (do NOT @-ref — ~488KB; read the named line range only):
`.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md` lines
~6951-7272 ("Phase 109 Handoff"). This plan's scope = every entry whose bolded source path begins
`src/components/` or `src/composables/`.

Follow the RELOCATION PROTOCOL defined in 109-02-PLAN.md Task 1. Heed the 108-02-SUMMARY.md lesson about
`useSlideshowAssembly.ts`: line ~870 there is a `console.warn()` STRING LITERAL (not a comment) that
mentions a review tag — never treat a string-literal line as a comment to shrink. Vue SFC comments use
`<!-- -->` in `<template>` and `//` / `/* */` in `<script>` — preserve each style.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate + shrink src/components/**</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/STACK.md, src/components/ContextualActionBar.vue, src/components/MiscLabelBadge.vue, src/components/PptxImportModal.vue, src/components/SongSlotPicker.vue, src/components/SongTable.vue, src/components/actionBarItems.ts, src/components/admin/CleanupEnableConfirmDialog.vue, src/components/admin/DeactivateOrgConfirmDialog.vue, src/components/run/RunFilmstrip.vue, src/components/run/RunHeader.vue, src/components/run/RunPreviewPair.vue, src/components/run/RunRail.vue, src/components/run/RunTransportBar.vue, src/components/run/RunDisplaysPanel.vue, src/components/settings/ServiceTemplateEditor.vue, src/components/slides/BackgroundControl.vue, src/components/slides/EditSlideDrawer.vue, src/components/slides/SlideCanvas.vue, src/components/slides/SlideGrid.vue, src/components/slides/SlideGroupMusicControl.vue, src/components/slides/SlidesTab.vue, src/components/slides/SlotLoopControl.vue, src/components/slides/dropRouting.ts, src/components/slides/slideDisplay.ts, src/components/slides/SlideCard.vue, src/components/slides/SlideDropTarget.vue, src/components/stage/StageKindIcon.vue, src/components/stage/StageLayoutEditor.vue, src/components/stage/StageLayoutPrintDocument.vue, src/components/stage/StageMarkerChip.vue, src/components/stage/StageRoom.vue, src/components/stage/StageLayoutView.vue</files>
  <read_first>
    - 108-COMMENT-INVENTORY.md lines ~6951-7272: entries whose path begins `src/components/` (this task's
      sub-scope; EditSlideDrawer.vue ~7 entries, slideDisplay.ts ~7 entries).
    - The four map docs' section layout (relevant homes: ARCHITECTURE.md for the drawer/grid gating +
      slideDisplay pure-helper contracts; STACK.md for SlideCanvas/SlideDropTarget/SlideGrid drag stack;
      CONCERNS.md for SlideCard deferred-render + RunDisplaysPanel recovery notes).
    - The full source comment for each entry (handoff truncates at "...").
  </read_first>
  <action>
    Apply the RELOCATION PROTOCOL to every in-scope entry under `src/components/**`. Relocate the fuller
    narration to the handoff-named map doc; keep inline the residue code cannot convey (e.g. the "★ R036 —
    lifecycle lock kept DISTINCT from isEditor" gate rationale can shrink to a one-line pointer, but a
    load-bearing invariant like "resource == null MUST be first" stays inline as a one-liner). Preserve Vue
    template vs script comment styles. Commit atomically (may split the commit across sub-groups —
    slides/, stage/, run/, top-level — for reviewability).
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process');const out=cp.execSync('git ls-files src/components',{encoding:'utf8'}).trim().split(/\r?\n/);const fs=require('fs');let c=0;for(const f of out){c+=(fs.readFileSync(f,'utf8').match(/See \.planning\/codebase\//g)||[]).length;}if(c<25){console.error('too few pointers:',c);process.exit(1);}console.log('pointers:',c)"</automated>
  </verify>
  <acceptance_criteria>
    - Every in-scope `src/components/**` entry is relocated+shrunk or recorded as kept-inline — none dropped.
    - `src/components/**` collectively contains at least ~25 `See .planning/codebase/` pointers.
    - Narration each pointer references exists in the named map doc + section.
    - Comment-only diffs (no executable/template logic changed).
  </acceptance_criteria>
  <done>Components relocated, shrunk, committed; pointers resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate + shrink src/composables/**</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/STACK.md, src/composables/useAutoSave.ts, src/composables/useBackgroundUpload.ts, src/composables/useLoopTimer.ts, src/composables/useOutputWindow.ts, src/composables/useRunControl.ts, src/composables/useRunTimers.ts, src/composables/useSlideshowAssembly.ts, src/composables/useUnsavedGuard.ts, src/composables/useMediaUpload.ts</files>
  <read_first>
    - 108-COMMENT-INVENTORY.md lines ~6951-7272: entries whose path begins `src/composables/` (this task's
      sub-scope; useRunControl.ts ~7 entries, useSlideshowAssembly.ts ~11 entries).
    - The four map docs after Task 1's additions, to append consistently.
    - The reminder in this plan's context about `useSlideshowAssembly.ts` line ~870 (string literal, NOT a
      comment).
  </read_first>
  <action>
    Apply the RELOCATION PROTOCOL to every in-scope entry under `src/composables/**`. The Run-flow
    composables carry the milestone's key anti-patterns — "the single loop timer lives in the control, never
    in an output window", "outputs are receive-only", fullscreen capability delegation — relocate these to
    ARCHITECTURE.md's anti-patterns/abstractions and leave a one-line pointer. Do NOT touch the
    useSlideshowAssembly console.warn string literal. Commit atomically.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['useAutoSave','useBackgroundUpload','useLoopTimer','useOutputWindow','useRunControl','useRunTimers','useSlideshowAssembly','useUnsavedGuard','useMediaUpload'].map(n=>'src/composables/'+n+'.ts');let c=0;for(const f of files){c+=(fs.readFileSync(f,'utf8').match(/See \.planning\/codebase\//g)||[]).length;}if(c<12){console.error('too few pointers:',c);process.exit(1);}console.log('pointers:',c)"</automated>
  </verify>
  <acceptance_criteria>
    - Every in-scope `src/composables/**` entry is relocated+shrunk or recorded as kept-inline — none dropped.
    - `src/composables/**` collectively contains at least ~12 `See .planning/codebase/` pointers.
    - The `useSlideshowAssembly.ts` console.warn string literal is unchanged.
    - Comment-only diffs.
  </acceptance_criteria>
  <done>Composables relocated, shrunk, committed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove no behavior change + component/composable completeness count</name>
  <files>src/composables/useRunControl.ts, src/components/slides/EditSlideDrawer.vue</files>
  <read_first>
    - CLAUDE.md: "Type-checking" (`npm run type-check`) and "Testing" (bare `npx vitest run`; baseline =
      only `src/storage.rules.test.ts`; do NOT use `--dir src`).
    - 108's deferred-items.md: the pre-existing `appConfig.test.ts` failure is not a regression.
  </read_first>
  <action>
    Count this plan's in-scope handoff entries (grep the handoff for bolded paths beginning
    `src/components/` and `src/composables/`) and confirm relocated + kept-inline equals that count — no
    drops. Run the type-check gate and the app suite; compare failing files to the pre-edit baseline — the
    only allowed failures are the known baseline (`src/storage.rules.test.ts`, plus the pre-existing
    `appConfig.test.ts`). Any NEW failing file is a regression to fix before finishing.
  </action>
  <verify>
    <automated>npm run type-check && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0.
    - `npx vitest run` shows no NEW failing files versus the pre-edit baseline.
    - Relocated + kept-inline count equals the in-scope components+composables handoff entry count —
      recorded in the SUMMARY; no drops.
  </acceptance_criteria>
  <done>Type-check clean, no new test failures, entry count reconciles to the handoff with zero drops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment edit -> compiled Vue SFC | A malformed comment/template-comment edit could change rendered markup or drop a delimiter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-08 | Tampering | Vue template `<!-- -->` vs script `//` comment edit changing render or breaking parse | high | mitigate | Preserve each comment style; comment-only diffs; Task 3 type-check + app suite must show no new failures. |
| T-109-09 | Tampering | Editing the useSlideshowAssembly console.warn string literal as if it were a comment | medium | mitigate | Explicitly excluded in Task 2; left unchanged. |

No package-manager installs occur in this phase, so the supply-chain threat (T-{phase}-SC) does not apply.
</threat_model>

<verification>
- Every in-scope components/composables handoff entry relocated+shrunk or explicitly kept-inline (count reconciles, no drops).
- Component/composable source files carry `See .planning/codebase/` pointers resolving to real map-doc sections.
- `npm run type-check` exits 0; `npx vitest run` shows no NEW failures versus baseline.
- Comment-only diffs.
</verification>

<success_criteria>
- R318 (components + composables subset) satisfied: narration relocated into the map docs, source comments
  shrunk, nothing dropped, zero behavior change.
</success_criteria>

## Artifacts this phase produces

- `.planning/codebase/ARCHITECTURE.md`, `INTEGRATIONS.md`, `CONCERNS.md`, `STACK.md` — component & composable
  behavioral notes relocated in (R318).
- `src/components/**` (~32 files) + `src/composables/**` (9 files) — source comments shrunk to
  `See .planning/codebase/<DOC>.md (<section>)` pointers (R318).

<output>
Create `.planning/phases/109-behavioral-architectural-extraction-comment-convention/109-04-SUMMARY.md` when done.
Record the in-scope entry count, the relocated count, and every explicit "kept inline" decision with its reason.
</output>
