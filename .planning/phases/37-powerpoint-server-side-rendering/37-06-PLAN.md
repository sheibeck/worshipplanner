---
phase: 37-powerpoint-server-side-rendering
plan: 06
type: execute
wave: 4
depends_on: [37-02, 37-05]
files_modified:
  - render-service/DEPLOY.md
  - .planning/PENDING-VERIFICATION.md
autonomous: true
requirements: [R062]

user_setup:
  - service: google-cloud-run
    why: "R062 requires a standalone Cloud Run service (LibreOffice + Poppler, custom Dockerfile) because Firebase Functions buildpacks cannot install those system packages. Deploying it provisions billable infrastructure, which STATE.md reserves as the owner's decision."
    env_vars:
      - name: PPTX_RENDER_SERVICE_URL
        source: "Printed by `gcloud run deploy pptx-render` on success; set as a firebase-functions/params defineString value for the functions codebase (params are sourced from functions/.env* or the deploy-time prompt)."
      - name: PPTX_RENDER_CLEANUP_ENABLED
        source: "Leave UNSET. The orphan sweep is dry-run-by-default by design; set it to the exact string \"true\" only after reviewing a dry-run log."
    dashboard_config:
      - task: "Create the pptx-render-sa service account and grant it roles/storage.objectAdmin scoped to the project's default bucket"
        location: "gcloud iam service-accounts create / gcloud storage buckets add-iam-policy-binding — full commands in render-service/DEPLOY.md"
      - task: "Enable run, artifactregistry, cloudbuild and eventarc APIs"
        location: "gcloud services enable — full command in render-service/DEPLOY.md"
      - task: "Deploy the service with --no-allow-unauthenticated, then grant the functions service account roles/run.invoker on it"
        location: "gcloud run deploy / gcloud run services add-iam-policy-binding — full commands in render-service/DEPLOY.md"

must_haves:
  truths:
    - "render-service/DEPLOY.md contains the exact gcloud run deploy command with every flag explained and every prerequisite listed — service account, both IAM directions, Artifact Registry, region, memory/CPU, timeout, concurrency and env vars — in a durable file the owner will find, not buried in a SUMMARY."
    - "DEPLOY.md states plainly that nothing in this repository runs these commands, and that cost, latency and visual fidelity cannot be validated until the owner does."
    - ".planning/PENDING-VERIFICATION.md gains a new `## Phase 37` section covering real visual fidelity against a multi-font multi-slide deck, that font substitution actually happened, cost and latency, the deploy itself, owner sign-off on google-auth-library, and reviewing a cleanup dry-run before enabling deletion. The existing Phase 31-35 sections are untouched."
    - "No Phase 37 verification item is ever recorded as passed or self-approved — each is written as an open owner to-do, per STATE.md's standing autonomy grant."
    - "All three test suites are green: render-service/, functions/, and the app's src/ suite at its documented two-file baseline; npm run type-check (vue-tsc --build) is clean."
  prohibitions:
    - statement: "Nothing in this phase may deploy or provision. No gcloud, no firebase deploy, no docker build/push, no GCP resource, service account, IAM binding, or Artifact Registry repo. Deploying provisions billable infrastructure and is the owner's decision, not this run's."
      status: kept
      verification: "This plan writes the deploy command as documentation and never executes it. Task 3 includes an explicit audit: `git log --patch` for the phase and every 37-*-SUMMARY.md are searched for an executed gcloud, firebase deploy, docker build or docker push invocation, and the audit result is recorded."
    - statement: "No Microsoft font may be bundled into the image, directly or via a transitive package such as ttf-mscorefonts-installer. Only metric-compatible open substitutes (Carlito / Caladea / Liberation). This is a licensing constraint, not a preference, and needs a build-time assertion rather than an intention."
      status: kept
      verification: "Task 3's gate runs `cd render-service && npx vitest run`, which includes the Dockerfile font-policy test authored in 37-01. DEPLOY.md additionally tells the owner that the image build will FAIL if a Microsoft core-font package is ever present, so the assertion is visible at the moment it matters."
    - statement: "Any deletion path introduced here defaults to dry-run and requires an explicit opt-in env var, mirroring cleanupExpiredMedia's post-incident shape. No deletion path may delete by default, and no doc comment may describe a default the code does not implement — that exact mismatch was the 2026-07-28 incident."
      status: kept
      verification: "DEPLOY.md documents PPTX_RENDER_CLEANUP_ENABLED as leave-unset with an explicit instruction to review a dry-run log first, and PENDING-VERIFICATION item 37.6 makes that review an owner to-do rather than an assumption. Task 3's gate re-runs the fail-safe suite from 37-05."

  artifacts:
    - path: "render-service/DEPLOY.md"
      provides: "The complete owner-run deploy handoff: prerequisites, the deploy command, every flag explained, both IAM directions, post-deploy configuration and post-deploy verification"
      min_lines: 90
      contains: "gcloud run deploy pptx-render"
    - path: ".planning/PENDING-VERIFICATION.md"
      provides: "A new Phase 37 section with six open owner to-dos, added without disturbing Phases 31-35"
      contains: "Phase 37"

  key_links:
    - from: "render-service/DEPLOY.md"
      to: "functions/src/index.ts PPTX_RENDER_SERVICE_URL"
      via: "the post-deploy step that records the printed service URL as the functions param"
      pattern: "PPTX_RENDER_SERVICE_URL"
    - from: ".planning/PENDING-VERIFICATION.md"
      to: "render-service/DEPLOY.md"
      via: "item 37.4 points the owner at the deploy handoff file"
      pattern: "DEPLOY.md"
---

<objective>
Hand the phase over: write the deploy command somewhere the owner will actually find it, record every
question this run genuinely could not answer as an open item, and prove all three test suites are green.

Purpose: STATE.md's instruction is *"Write the Cloud Run service, Dockerfile, bridging Cloud Function
and tests, then STOP and hand the owner the exact `gcloud run deploy` command."* This plan is the STOP
and the hand-over. It is also where the phase is honest about its limits: fidelity, font substitution,
cost and latency are unknowable without a deployed service, and 37-CONTEXT.md asks that this be said
plainly rather than estimated and called validated.

Output: `render-service/DEPLOY.md` and a new `## Phase 37` section in `.planning/PENDING-VERIFICATION.md`.

**This plan runs no command it documents.**
</objective>

## Artifacts this phase produces

| Artifact | Kind | Owning plan |
|---|---|---|
| `render-service/` (standalone project, NOT inside `functions/`) | directory | 37-01 |
| `render-service/package.json`, `tsconfig.json`, `vitest.config.ts`, `.dockerignore`, `.gitignore` | config | 37-01 |
| `render-service/Dockerfile` | container definition | 37-01 |
| `render-service/fontconfig/60-metric-compat-aliases.conf` | fontconfig aliases | 37-01 |
| `render-service/src/dockerfile.test.ts` | font-policy gate | 37-01 |
| `render-service/src/render.ts` — `renderPptxToImages`, `RenderRequest`, `RenderResult`, `PPTX_IMPORT_PATH_GUARD`, `renderedPrefix`, `renderedObjectName`, `pageNumberFromOutputName`, `RENDER_DPI`, `SOFFICE_TIMEOUT_MS`, `PDFTOPPM_TIMEOUT_MS`, `RENDERED_PAGE_PAD` | module | 37-02 |
| `render-service/src/server.ts` — `validateRenderBody`, `handleRenderRequest`, `createApp`, `BadRequestError` | module | 37-02 |
| `render-service/src/main.ts` — container entrypoint (`node lib/main.js`) | module | 37-02 |
| `render-service/src/render.test.ts`, `render-service/src/server.test.ts` | tests | 37-02 |
| `functions/src/renderInvoker.ts` — `invokeRenderService`, `InvokeRenderServiceArgs`, `RenderServiceResponse`, `RENDER_REQUEST_TIMEOUT_MS` | module | 37-03 |
| `functions/src/renderInvoker.test.ts` | tests | 37-03 |
| `functions/src/index.ts` — `PptxRenderStatus`, `PptxRenderDoc`, `pptxRenderDocRef` (37-03); `PPTX_RENDER_SERVICE_URL`, `renderedPrefixFor`, `RENDERED_OBJECT_NAME`, `RenderOutcome`, `requestPptxRenderHandler`, `requestPptxRender` (37-04) | exports | 37-03 / 37-04 |
| `functions/src/index.ts` — `cleanupOrphanRendersHandler`, `cleanupOrphanRenders`, `ORPHAN_RENDER_STALE_HOURS`, `RENDERED_OBJECT_GUARD`, `OrphanCleanupSummary` | exports | 37-05 |
| Firestore collection `organizations/{orgId}/pptxRenders/{importId}` | data | 37-03 |
| Storage prefix `orgs/{orgId}/pptx-imports/{importId}/rendered/page-NNNN.png` (4-digit zero-padded) | data | 37-02 / 37-04 |
| Env var `PPTX_RENDER_SERVICE_URL` (functions param, empty default) | config | 37-04 |
| Env var `PPTX_RENDER_CLEANUP_ENABLED` (functions, opt-in for real deletion) | config | 37-05 |
| Container env `HOME=/tmp`, `NODE_ENV=production`, `PORT` | config | 37-01 / 37-02 |
| `ImportedDeck.renderImportId?: string` in `src/types/importedDeck.ts` | type | 37-05 |
| npm deps — `express`, `@google-cloud/storage`, `@types/express`, `@types/node` (render-service); `google-auth-library` (functions) | dependencies | 37-01 / 37-03 |
| `render-service/DEPLOY.md` | doc | 37-06 |
| `.planning/PENDING-VERIFICATION.md` → `## Phase 37` section, items 37.1–37.6 | doc | 37-06 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PENDING-VERIFICATION.md
@.planning/phases/37-powerpoint-server-side-rendering/37-CONTEXT.md
@.planning/phases/37-powerpoint-server-side-rendering/37-RESEARCH.md
@.planning/phases/37-powerpoint-server-side-rendering/37-VALIDATION.md
@.planning/phases/37-powerpoint-server-side-rendering/37-01-SUMMARY.md
@.planning/phases/37-powerpoint-server-side-rendering/37-02-SUMMARY.md
@.planning/phases/37-powerpoint-server-side-rendering/37-03-SUMMARY.md
@.planning/phases/37-powerpoint-server-side-rendering/37-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: render-service/DEPLOY.md — the exact owner-run handoff</name>
  <files>render-service/DEPLOY.md</files>
  <read_first>
    - .planning/phases/37-powerpoint-server-side-rendering/37-RESEARCH.md — the final section, `## The exact gcloud run deploy handoff command (for the owner, NOT to be run this session)`, including the three prerequisite commands, the deploy command, the full flag-explanation table, the post-deploy `add-iam-policy-binding` command and the closing paragraph about recording the service URL
    - .planning/STATE.md — the v1.4 standing decision "BUILD BUT DO NOT DEPLOY" and the standing autonomy grant's "No deploys" line
    - render-service/Dockerfile — the memory/CPU/concurrency assumptions the flags must match, and the build-time font assertion the owner should know about
    - functions/src/index.ts — the `PPTX_RENDER_SERVICE_URL` param name and the `PPTX_RENDER_CLEANUP_ENABLED` env var name, so DEPLOY.md names them exactly
  </read_first>
  <action>
    Write `render-service/DEPLOY.md` — a durable, findable file next to the Dockerfile it deploys, not a
    note inside a SUMMARY.

    Open with a banner stating that **nothing in this repository runs any command in this file**. Phase
    37 was built under an explicit owner instruction to build but not deploy, because every command here
    provisions billable infrastructure. Say which categories are involved: a Cloud Run service, an
    Artifact Registry repository created implicitly by `--source` builds, Cloud Build minutes, a service
    account, and two IAM bindings.

    Then, in order:

    **Prerequisites.** Transcribe the three commands from 37-RESEARCH.md: creating the `pptx-render-sa`
    service account; granting it `roles/storage.objectAdmin` scoped to the project's default bucket
    (explaining that this is bucket-scoped rather than project-wide on purpose, and that the service
    needs to read `source.pptx` and write `rendered/*.png` and nothing else); and enabling the `run`,
    `artifactregistry`, `cloudbuild` and `eventarc` APIs. Flag every `PROJECT_ID` placeholder the owner
    must substitute.

    **The deploy command.** Transcribe `gcloud run deploy pptx-render` with `--source=./render-service`,
    `--region=us-central1`, `--no-allow-unauthenticated`, `--service-account=pptx-render-sa@...`,
    `--memory=2Gi`, `--cpu=2`, `--timeout=300`, `--concurrency=1`, `--min-instances=0`,
    `--max-instances=5` and `--project=PROJECT_ID`.

    **Every flag explained**, as a table, reproducing 37-RESEARCH.md's rationale column and keeping its
    honesty: `--memory=2Gi` and `--cpu=2` are starting points that were **not** empirically validated
    because that requires deploying; `--region` must be confirmed against the project's actual
    Firestore/Functions region before running; `--concurrency=1` is deliberate because LibreOffice's
    shared-profile lock makes concurrent conversions on one instance unreliable, so parallelism comes
    from scaling out; `--timeout=300` sits above the render code's own 180s + 120s internal timeouts and
    well under Cloud Run's platform maximum; `--no-allow-unauthenticated` is the core security
    requirement and must never be relaxed to `allUsers` or `allAuthenticatedUsers`.

    **The second IAM direction.** Transcribe `gcloud run services add-iam-policy-binding pptx-render`
    granting `roles/run.invoker` to the service account the `requestPptxRender` function runs as,
    including the caveat that gen2 functions default to `PROJECT_ID@appspot.gserviceaccount.com` unless a
    dedicated SA was configured, and that the actual value must be confirmed first. Explain why both
    directions are needed: the render service's own SA governs what the service may touch in Storage,
    while the invoker binding governs who may call the service at all.

    **Post-deploy configuration.** Record the printed service URL as the `PPTX_RENDER_SERVICE_URL`
    param for the functions codebase — a `firebase-functions/params` `defineString` value, sourced from
    `functions/.env*` or answered at the deploy prompt, following the same params pattern already used
    for `CLAUDE_API_KEY`/`ESV_API_KEY` in `functions/src/index.ts`. State that until this is set,
    `requestPptxRenderHandler` marks every render `failed` with
    `failureReason: "render-service-not-configured"` — that is deliberate and tested, not a bug. Then
    note that `functions/` must be redeployed for the new handlers to exist at all, and that
    `firebase deploy --only firestore:rules` is a **separate** deferred item (backlog 999.3) which this
    phase does not need — no rules change was made, because `firestore.rules`' catch-all already denies
    client access to `pptxRenders`.

    **The cleanup toggle.** `PPTX_RENDER_CLEANUP_ENABLED` must be left **unset**. Explain that
    `cleanupOrphanRenders` runs daily at 03:00 UTC as a dry run, logging what it would delete, and that
    the owner should read at least one dry-run log before ever setting the value to the exact string
    `"true"`. Name the 2026-07-28 incident as the reason this is opt-in rather than opt-out.

    **What the build will refuse to do.** Note that the image build fails outright if a Microsoft
    core-font package is ever present, by design — so a failed build with that message is the licensing
    gate working, not a defect.

    **Post-deploy verification, for the owner.** List what only a deployed service can answer, matching
    37-VALIDATION.md's Manual-Only table: real visual fidelity against a **real multi-font, multi-slide
    deck** (the ROADMAP is explicit that a 2-slide fixture proves nothing); whether font *substitution*
    actually happened, using a deck authored in Calibri and Cambria and checking the result is metrically
    Carlito/Caladea rather than a generic fallback; and real cost and latency across several decks, with
    cold starts likely dominating.

    Do not execute any command in this file. Do not create any GCP resource.
  </action>
  <verify>
    <automated>test -f render-service/DEPLOY.md && grep -c 'gcloud run deploy pptx-render' render-service/DEPLOY.md && grep -c 'PPTX_RENDER_SERVICE_URL' render-service/DEPLOY.md && grep -c 'roles/run.invoker' render-service/DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `render-service/DEPLOY.md` exists and contains the literal `gcloud run deploy pptx-render`, the `--no-allow-unauthenticated` flag, `roles/run.invoker`, `roles/storage.objectAdmin`, `PPTX_RENDER_SERVICE_URL` and `PPTX_RENDER_CLEANUP_ENABLED`.
    - Every flag in the deploy command has a row in the explanation table, and the `--memory`/`--cpu` rows state explicitly that the values are unvalidated starting points.
    - Both IAM directions are documented, with the reason each is needed.
    - The file opens with a statement that nothing in the repository runs these commands.
    - No command from the file was executed: `gcloud`, `docker` and `firebase deploy` appear only as documented text.
  </acceptance_criteria>
  <done>The owner has one file, next to the Dockerfile, containing everything needed to deploy — and nothing was deployed to produce it.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 37 section to PENDING-VERIFICATION.md without disturbing Phases 31-35</name>
  <files>.planning/PENDING-VERIFICATION.md</files>
  <read_first>
    - .planning/PENDING-VERIFICATION.md — the whole file. Note the heading order (`## Phase 31` … `## Phase 35`, then a `---` separator and `## Notes and failures` at the end) and the established item style: a `☐ **NN.N** **Title.**` bullet with a rationale sentence and a bolded **Instructions:** clause.
    - .planning/STATE.md — the standing autonomy grant's "Never record a deferred check as passed" and "Where deferred items go"
    - .planning/phases/37-powerpoint-server-side-rendering/37-VALIDATION.md — the `## Manual-Only Verifications` table, which is the source list for these items
    - .planning/phases/37-powerpoint-server-side-rendering/37-01-SUMMARY.md and 37-03-SUMMARY.md — the two package-legitimacy checkpoint dispositions to transcribe
  </read_first>
  <action>
    Insert a new `## Phase 37 — PowerPoint Server-Side Rendering` section into
    `.planning/PENDING-VERIFICATION.md`, positioned **after the Phase 35 section and before the `---`
    that precedes `## Notes and failures`**. Use `Edit` with a scoped replacement anchored on the
    existing separator — **do not rewrite the file** and do not alter a single character of the Phase
    31, 32, 33, 34 or 35 sections.

    Open the section with a short paragraph stating what this phase did and did not prove: the render
    service, its Dockerfile, the bridging function, the completeness check, the IAM contract shape, the
    font policy and the dry-run default are all automated-tested; nothing was deployed, no container was
    built, and no GCP resource was created, by explicit owner instruction. Every item below is open.

    Then six unchecked items, in the file's existing style:

    - **37.1 Real visual fidelity (R062 criterion 1).** Only a deployed service can render. Instructions:
      run `render-service/DEPLOY.md`'s deploy command, then import a **real multi-font, multi-slide
      deck** — the ROADMAP is explicit that a 2-slide fixture proves nothing about fidelity or cost.
      Compare backgrounds, fonts, layout and effects against PowerPoint's own rendering. Note that
      static-frame export means transitions and animations are not rendered, which is expected: R062 asks
      for a true visual representation, not motion.
    - **37.2 Font substitution actually happened (R062 criterion 3's *effect*).** The Dockerfile test
      proves the right packages are installed and no Microsoft font is bundled; it cannot prove
      LibreOffice actually maps Calibri to Carlito and Cambria to Caladea. A fontconfig alias file was
      shipped for exactly this reason, but 37-RESEARCH.md records it as an assumption until seen.
      Instructions: import a deck authored in Calibri and Cambria and confirm the rendering is metrically
      correct rather than falling back to Liberation Sans.
    - **37.3 Cost and latency.** Cannot be estimated credibly without running it, and this run refused to
      estimate and call it validated. Instructions: render several real decks; record CPU-seconds and
      wall time. Cold starts likely dominate. Revisit `--memory=2Gi`, `--cpu=2` and `--max-instances=5`
      against the observed numbers.
    - **37.4 The deploy itself.** Every command in `render-service/DEPLOY.md` provisions billable
      infrastructure and is the owner's call. Instructions: review `render-service/DEPLOY.md`, confirm
      the region against the project's actual Firestore/Functions region, then run the prerequisites,
      the deploy, and the `roles/run.invoker` binding. Afterwards set `PPTX_RENDER_SERVICE_URL` and
      redeploy `functions/`.
    - **37.5 Sign-off on the new dependencies.** `google-auth-library` was flagged `[SUS]` by the package
      checker on a `too-new` heuristic that fires identically against this repo's already-shipping
      `firebase-admin` and `firebase-functions` — it reads as a false positive from Google's release
      cadence, and it is recorded rather than hidden. Transcribe the actual dispositions and resolved
      source repositories from `37-01-SUMMARY.md` and `37-03-SUMMARY.md`. Instructions: confirm you are
      comfortable with `google-auth-library` in `functions/`, and with `express`, `@google-cloud/storage`,
      `@types/express` and `@types/node` in `render-service/`.
    - **37.6 Review a cleanup dry-run before enabling deletion.** `cleanupOrphanRenders` runs daily at
      03:00 UTC and is dry-run-by-default; `PPTX_RENDER_CLEANUP_ENABLED` must stay unset until a real log
      has been read. Instructions: after the service has run for a while, read a dry-run log and confirm
      the would-delete list contains only stale `pending`/`failed` renders and their `rendered/` objects
      — never a `source.pptx`, never anything under `images/`, never a `ready` render.

    ★ **Every item stays unchecked.** Under STATE.md's standing autonomy grant, deferred means deferred
    and disclosed — never self-approved, never recorded as passed.
  </action>
  <verify>
    <automated>grep -c '^## Phase 3[1-5]' .planning/PENDING-VERIFICATION.md && grep -c '^## Phase 37' .planning/PENDING-VERIFICATION.md && grep -c '☐ \*\*37\.' .planning/PENDING-VERIFICATION.md</automated>
    <human-check>
      Open `.planning/PENDING-VERIFICATION.md` and read the new `## Phase 37 — PowerPoint Server-Side
      Rendering` section.

      1. Confirm the Phase 31, 32, 33, 34 and 35 sections are all still present and unchanged, and that
         `## Notes and failures` is still the final heading.
      2. Confirm all six Phase 37 items (37.1–37.6) are unchecked, and that none of them is worded as
         though it had already been verified.
      3. Confirm 37.4 points at `render-service/DEPLOY.md`, and that reading that file gives you
         everything needed to deploy without returning to the planning documents.
      4. Confirm 37.5 records the actual disposition of both package-legitimacy checkpoints rather than
         claiming approval.

      **Do not self-approve this check.** Under STATE.md's standing autonomy grant, record it as
      deferred and continue.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Phase 3[1-5]' .planning/PENDING-VERIFICATION.md` returns 5 — every prior phase section survives.
    - `grep -c '^## Phase 37' .planning/PENDING-VERIFICATION.md` returns 1.
    - Six items numbered 37.1 through 37.6 exist, all unchecked (`☐`), none containing the word "passed", "verified" or "approved" as a claim about itself.
    - `## Notes and failures` remains the last heading in the file.
    - `git diff .planning/PENDING-VERIFICATION.md` shows only insertions — zero deletions from the Phase 31-35 regions.
  </acceptance_criteria>
  <done>Everything this run genuinely could not answer is written down as an open owner to-do, and nothing prior was clobbered.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — all three suites, both type checks, and the no-deploy audit</name>
  <files>.planning/PENDING-VERIFICATION.md</files>
  <read_first>
    - CLAUDE.md — the two testing sections: `npx vitest run` excludes `src/rules.test.ts`; the documented app-suite baseline failures are `src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts`; and the type-check gate must be `npm run type-check` (`vue-tsc --build`), never `vue-tsc --noEmit -p tsconfig.app.json`
    - .planning/phases/37-powerpoint-server-side-rendering/37-VALIDATION.md — `## Test Infrastructure`, especially the note that `functions/` is a separate suite the app's `npx vitest run src/` does not cover, and `render-service/` is a third
  </read_first>
  <action>
    Run the phase gate. `functions/` is a separate suite the app's `npx vitest run src/` does not cover,
    and `render-service/` is a third — the gate is **all applicable suites green**, not just the app's.

    1. `cd render-service && npx vitest run` — every test passes.
    2. `cd render-service && npx tsc --noEmit` — exits 0.
    3. `cd functions && npx vitest run` — every test passes.
    4. `cd functions && npx tsc --noEmit` — exits 0.
    5. `npx vitest run src/` — passing, with the failing FILE SET matching the documented baseline
       exactly: `src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts`, and no other.
       A third failing file is a regression, not baseline.
    6. `npm run type-check` — the `vue-tsc --build` form CLAUDE.md requires. Do NOT substitute
       `vue-tsc --noEmit -p tsconfig.app.json`: it silently skips the test files, and that exact
       substitution let five `TS2339` errors survive two full phases.
    7. `npm run build` — succeeds.

    Then the **no-deploy audit**. Search this phase's commits and every `37-*-SUMMARY.md` for evidence
    that a prohibited command was executed rather than merely documented: `gcloud`, `firebase deploy`,
    `docker build`, `docker push`. Occurrences inside `render-service/DEPLOY.md` and inside plan or
    summary prose are expected — the audit is looking for an *executed* invocation, a recorded image
    tag, a service URL, or a created resource. Record the audit's result explicitly in this plan's
    SUMMARY.

    If any suite fails, fix it before completing the plan. If a failure cannot be fixed within this
    plan's scope, record it honestly as an open item rather than adjusting the baseline to match —
    the ROADMAP scheduled Phase 37 last precisely so an overrun or cut disturbs nothing else, and an
    honest partial with a clear handoff beats a speculative whole.
  </action>
  <verify>
    <automated>cd render-service && npx vitest run && npx tsc --noEmit && cd ../functions && npx vitest run && npx tsc --noEmit && cd .. && npm run type-check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd render-service && npx vitest run` passes; `cd render-service && npx tsc --noEmit` exits 0.
    - `cd functions && npx vitest run` passes; `cd functions && npx tsc --noEmit` exits 0.
    - `npx vitest run src/` fails in exactly two files — `src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts` — and no others.
    - `npm run type-check` (`vue-tsc --build`) exits 0, and the SUMMARY records that this form was used rather than the narrower `-p tsconfig.app.json` form.
    - `npm run build` succeeds.
    - The no-deploy audit result is recorded in the SUMMARY: no `gcloud`, `firebase deploy`, `docker build` or `docker push` was executed at any point in the phase, and no GCP resource, service account, IAM binding, Artifact Registry repo or image tag exists as a result of this run.
  </acceptance_criteria>
  <done>All three suites are green, both type gates are clean in the form CLAUDE.md requires, and the deploy prohibition is audited rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → owner action | A command written here will later be run with project-owner credentials |
| Planning record → future decisions | An item recorded as passed would be trusted later without re-checking |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-23 | Elevation of Privilege | The documented deploy command | critical | mitigate | `DEPLOY.md` specifies `--no-allow-unauthenticated` with an explicit instruction never to relax it to `allUsers`/`allAuthenticatedUsers`, a dedicated least-privilege `pptx-render-sa`, and `roles/storage.objectAdmin` scoped to one bucket rather than the project. Both IAM directions are stated with the reason each exists. |
| T-37-24 | Repudiation | A deferred check recorded as passed | high | mitigate | All six Phase 37 items are written unchecked, with an explicit instruction not to self-approve; Task 2's acceptance criteria assert no item claims verification of itself, and the human-check block repeats the constraint. |
| T-37-25 | Tampering | A whole-file rewrite clobbering Phases 31-35 | high | mitigate | Task 2 mandates a scoped `Edit` anchored on the existing separator, and the acceptance criteria require `git diff` to show insertions only, with all five prior phase headings still present. |
| T-37-SC | Tampering | Unreviewed new dependencies reaching production | high | mitigate | Item 37.5 carries both package-legitimacy checkpoint dispositions forward as an open owner to-do rather than closing them; neither was self-approved. |
| T-37-26 | Tampering | A prohibited command executed and unnoticed | critical | mitigate | Task 3's no-deploy audit searches the phase's commits and summaries for an *executed* `gcloud`/`firebase deploy`/`docker build`/`docker push` and records the result explicitly, so the prohibition is evidenced rather than assumed. |
</threat_model>

<verification>
- `cd render-service && npx vitest run && npx tsc --noEmit` — green
- `cd functions && npx vitest run && npx tsc --noEmit` — green
- `npx vitest run src/` — documented two-file baseline only
- `npm run type-check` (`vue-tsc --build`) and `npm run build` — green
- `.planning/PENDING-VERIFICATION.md` — Phase 31-35 sections byte-identical, Phase 37 section added, all six items unchecked
- No `gcloud`, `firebase deploy`, `docker build` or `docker push` was executed anywhere in the phase
</verification>

<success_criteria>
The owner has a complete, self-contained deploy handoff in `render-service/DEPLOY.md`; every question
that genuinely required a deployed service is recorded as an open, unchecked item under a Phase 37
heading that leaves Phases 31-35 untouched; all three test suites and both type gates are green; and the
build-but-do-not-deploy instruction is audited, not merely asserted.
</success_criteria>

<output>
Create `.planning/phases/37-powerpoint-server-side-rendering/37-06-SUMMARY.md` when done.
Record the no-deploy audit result verbatim, the exact failing-file set from `npx vitest run src/`, and
the fact that `npm run type-check` (`vue-tsc --build`) was the form used.
</output>
