---
phase: 27-service-order-tab-rename-and-strip-slide-editing-risk-medium
plan: 05
type: execute
wave: 4
depends_on: ["27-01", "27-04"]
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
  - src/components/SlideshowPreview.vue
  - src/components/__tests__/SlideshowPreview.test.ts
  - src/components/slides/slideDisplay.ts
autonomous: true
requirements: [R034, R018]

must_haves:
  truths:
    - "The Service Order tab no longer renders assembled slide content — the inline slideshow preview is gone (R034)."
    - "Whatever 27-01 decided about starting a presentation is what the code now does, exactly, with no planner substitution."
    - "Every panel-visibility assertion in the view's test suite probes the tab panel's own stable seam rather than a slide component, so the suite no longer depends on what is mounted inside the panel."
    - "Phase 26's 'Edit in scripture' relay still switches to the Service Order tab and opens the requested passage editor."
    - "The full unit run fails in exactly the same ten baseline FILES as before the phase — no eleventh."
  artifacts:
    - "src/views/ServiceEditorView.vue reflecting the recorded decision"
    - "src/views/__tests__/ServiceEditorView.test.ts with seam-based panel probes and preview/present tests matching the decision"
    - ".planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-05-SUMMARY.md recording the phase-wide gate results"
  key_links:
    - "Five assertions across three describe blocks currently use the slideshow-preview component as a proxy for 'the first tab's panel is visible'. They must move to the panel seam BEFORE the preview can leave, or the suite goes red for a reason unrelated to the change under test."
    - "The presentation viewer is mounted behind a local flag that only the preview panel's present event sets. The flag, the viewer mount and the assembly entries feeding it stand or fall together with whichever option was chosen."
    - "The assembled-sections entry of the assembly destructure exists solely to feed the preview panel. It is a view-level destructure, not the composable's export — the composable keeps exporting and testing it either way."
  prohibitions:
    - "MUST NOT delete src/components/PresentationViewer.vue or src/components/__tests__/PresentationViewer.test.ts under any option. Phase 23 is code-complete with an outstanding human-verify checkpoint; do not disturb its assertions beyond what the removal strictly forces."
    - "MUST NOT remove the assembled-sections computed, its export or its tests from src/composables/useSlideshowAssembly.ts. Only the view's destructure of it is in scope; re-shaping the composable is explicitly deferred by the phase context."
    - "MUST NOT substitute the planner's recommendation for the recorded decision. Read 27-01-SUMMARY.md and implement what it says; if it is missing or ambiguous, stop and report."
    - "MUST NOT build a present affordance on the Slides tab. That is new feature work and out of scope for this phase under any option."
    - "MUST NOT touch the slot id backfill, autosave, the group delete cascade and its warning, the scripture editor, or the section-assignment select."
  <!-- planner-discipline-allow: SlideshowPreview -->
---

<objective>
Close the phase: move the test suite's panel probes onto the tab's own seam, apply 27-01's recorded
decision about the inline slideshow preview and the presentation entry point, then run the phase-wide
gate.

Purpose: this is the last slide surface named in the phase context's fate table, and the one whose
removal has a consequence the fate table did not anticipate — which is why its disposition was decided
by a human in 27-01 rather than by this plan.
Output: A Service Order tab that renders no slide content, a test suite that no longer probes slide
components to answer questions about tabs, and a recorded phase-wide verification result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-CONTEXT.md
@.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-01-SUMMARY.md
@.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-04-SUMMARY.md
</context>

<interfaces>
Sites in `src/views/__tests__/ServiceEditorView.test.ts` that use the preview component as a
panel-visibility proxy (line numbers approximate; the file has been edited by 27-02, 27-03 and 27-04):

- ~1296, ~1304, ~1310 — inside `describe('ServiceEditorView - Slides tab (Phase 25-03)')`.
- ~1417, ~1424 — inside `describe('ServiceEditorView - Edit in scripture plumbing (Phase 26-03)')`.

Each calls the file's `isVShowHidden` helper on `wrapper.findComponent(SlideshowPreview)`. The helper
accepts anything exposing `.element`, so `wrapper.find('[data-testid="service-order-panel"]')` — the seam
27-02 added — is a drop-in replacement.

Tests bound to the preview and the presentation viewer, all inside
`describe('ServiceEditorView - Section headers and slideshow preview (Phase 20-04)')`:

- ~651 — asserts the preview panel mounts bound to the live assembled sections.
- ~660 — asserts the presentation viewer is NOT mounted on initial render.
- ~667 — asserts the viewer mounts when the preview emits present and unmounts when the viewer emits exit.
- ~680 — asserts the same assembled-slideshow array instance reaches the viewer across re-renders.
- ~618-621 — the describe's own presentation-viewer stub declaration, which exists only for those tests.

The section-header tests (~633, ~644) and the section-select tests (~700, ~713) in that same block are
service structure and stay untouched under every option.

Sites in `src/views/ServiceEditorView.vue` (approximate): the preview usage and the viewer mount around
~1009-1018 inside a wrapper div; the two component import statements around ~1193-1194; the assembly
destructure around ~1517-1525 (the assembled-sections and loading entries); and the presenting flag
declaration just below it.

`src/components/slides/slideDisplay.ts` around ~93 names the preview component in prose only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Probe the tab panel by its own seam, not by what is inside it</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
  Replace all five panel-visibility probes listed in the interfaces block so they call the visibility
  helper on the Service Order panel's own seam element rather than on the preview component. Keep every
  assertion's expected boolean and every surrounding step exactly as it is — this is a change of probe,
  not of expectation. The Slides-tab probe in the same tests continues to use the Slides tab component.

  Leave the preview component's import in the test file for now; the Phase 20-04 block still uses it and
  Task 2 decides its fate.

  This task must leave the whole file green, because the preview is still mounted at this point. If any
  probe flips result when swapped, stop and report — that would mean the seam and the preview do not
  actually share a visibility ancestor.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>The whole file passes, and no remaining visibility probe in it resolves through the preview component.</done>
</task>

<task type="auto">
  <name>Task 2: Apply 27-01's recorded decision</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts, src/components/SlideshowPreview.vue, src/components/__tests__/SlideshowPreview.test.ts, src/components/slides/slideDisplay.ts</files>
  <action>
  Read `27-01-SUMMARY.md` and implement the option it names. If it names none, or names more than one,
  stop and report rather than choosing.

  If option-a (keep a bare present button on the Service Order tab): remove the preview usage and its
  wrapper comment, keep the presentation viewer mount and the presenting flag, and in the preview's place
  put a single button that sets the presenting flag. Give it the same accessible label the preview's own
  present control used, the same disabled-when-there-is-nothing-to-present guard driven by the assembled
  slideshow being empty, the same explanatory title text on the disabled state, and a stable test id.
  Drop the assembled-sections entry from the view's destructure and keep the slideshow and loading
  entries. Delete the preview component and its test file only after confirming no importer remains.
  Update the Phase 20-04 tests so the present event now originates from the new button rather than the
  preview: delete the mounts-the-preview test, keep the not-mounted-on-initial-render test, and rewrite
  the mount-on-present and array-identity tests to click the button instead of emitting from the preview.

  If option-b (keep the preview panel unchanged): change no source file. Record in the SUMMARY that the
  phase context's fate table is superseded by the recorded decision and why, and leave the Phase 20-04
  tests exactly as they are. Skip the deletion and the prose fix.

  If option-c (remove the preview and the viewer mount): remove the preview usage, the viewer mount, its
  wrapper div, the presenting flag, both component import statements, and the assembled-sections and
  loading entries from the view's destructure — keeping the slideshow entry, which the Slides tab still
  receives as a prop. Delete the preview component and its test file after confirming no importer remains.
  Delete the four preview-and-viewer tests and the presentation-viewer stub declaration from the Phase
  20-04 describe block, keeping its section-header and section-select tests. Do NOT delete the
  presentation viewer component or its own test file. Record in the SUMMARY, as explicit v1.3 debt, that
  Phase 23's outstanding batch human-verify for presentation is now unperformable until a present
  affordance exists.

  Under option-a and option-c only: update the prose in `src/components/slides/slideDisplay.ts` that names
  the deleted preview component, without changing any code there.

  Under every option: leave the presentation viewer component file and its test file alone, and leave the
  assembly composable's assembled-sections export and its own tests alone.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts src/composables/__tests__/useSlideshowAssembly.test.ts</automated>
    <automated>test -f src/components/PresentationViewer.vue && test -f src/components/__tests__/PresentationViewer.test.ts && echo VIEWER-INTACT</automated>
    <automated>npm run type-check</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
  The implemented behaviour matches the option named in 27-01-SUMMARY.md; the view's suite and the
  assembly composable's suite both pass; the presentation viewer component and its test are untouched;
  type-check reports 0 errors and the production build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide gate against the recorded baseline</name>
  <files>.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-05-SUMMARY.md</files>
  <action>
  Run the full unit suite and compare the FAILING FILE SET — not the test count — against the recorded
  baseline of exactly ten files: eight stale duplicates under the quarantined worktrees directory, the
  storage rules suite that needs an emulator this run must not start, and the roster view suite with its
  known stale assertion. The passing test count will legitimately DROP this phase because deleted
  components took their suites with them; that is expected. An eleventh failing file is a regression.

  Never run the rules test script and never restart or touch the emulator — a live user session may hold
  its ports.

  Then confirm the phase's own goal end to end at the code level: the first tab reads Service Order; no
  deck editor, deck import, media attachment or slide preview surface remains on it; the passage editor
  and the section select still do; and the Phase 26 relay still opens the passage editor on that tab.
  Record each check and its result in the SUMMARY, together with the final disposition of the three
  components the phase considered for deletion and the one it was forbidden from deleting.
  </action>
  <verify>
    <automated>npx vitest run src/ 2>&1 | tail -40</automated>
    <automated>npm run type-check</automated>
    <automated>npm run build</automated>
    <human-check>
    Deferred to the milestone's batch human-verify, alongside the outstanding Phase 20-23, 25 and 26 items:
    open a service, confirm the first tab reads "Service Order" and shows the order of service with no
    slide-editing controls on any plan item, confirm the passage editor and section select still work
    there, confirm the Slides tab still imports a deck and attaches group music, follow the drawer's
    "Edit in scripture" link and confirm it lands on the Service Order tab with the right passage open,
    and confirm the presentation entry point behaves as 27-01 decided.
    </human-check>
  </verify>
  <done>
  The full run fails in exactly the ten baseline files and no more; type-check reports 0 errors; the
  production build succeeds; and the SUMMARY records every goal check plus the per-component disposition.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → presentation output | The present entry point drives a fullscreen surface shown to a congregation; losing or mis-gating it is an availability concern, not a confidentiality one. |
| viewer → service editor | The preview panel was visible to viewers as well as editors; its removal must not change what a viewer can otherwise reach. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05-01 | Denial of Service | presentation entry point | high | mitigate | Disposition is decided by a human in 27-01 and implemented verbatim here; under option-c the resulting loss is recorded as explicit v1.3 debt rather than discovered later. |
| T-27-05-02 | Tampering | Phase 23 presentation viewer | high | mitigate | The viewer component and its test file are prohibited from deletion under every option, and Task 2 gates on both files still existing. |
| T-27-05-03 | Denial of Service | assembly composable | medium | mitigate | Only the view's destructure narrows; the composable's exports and tests are prohibited from change and its suite runs in Task 2's gate. |
| T-27-05-04 | Denial of Service | Phase 26 scripture relay | medium | mitigate | The relay's own describe block is re-probed in Task 1 and re-run in every subsequent gate. |
| T-27-05-05 | Repudiation | phase outcome | medium | mitigate | Task 3 records the baseline comparison, each goal check and the per-component disposition in the SUMMARY, so the phase's removals are auditable. |
| T-27-05-06 | Denial of Service | Firestore/Storage emulator | medium | mitigate | Task 3 forbids running the rules script or restarting the emulator; a live user session may hold its ports. |

No package-manager installs occur in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `npx vitest run src/` fails in exactly the ten recorded baseline files, no eleventh.
- `npm run type-check` reports 0 errors and `npm run build` succeeds.
- `src/components/PresentationViewer.vue` and its test file are present and unmodified beyond what the removal strictly forced.
- The Phase 26 Edit-in-scripture describe block passes.
</verification>

<success_criteria>
The Service Order tab renders no slide content, 27-01's decision is implemented exactly as recorded, the
test suite probes tabs by their own seam, and the full-suite failing FILE SET has not grown past its
ten-file baseline.
</success_criteria>

<output>
Create `.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-05-SUMMARY.md` when done
</output>
