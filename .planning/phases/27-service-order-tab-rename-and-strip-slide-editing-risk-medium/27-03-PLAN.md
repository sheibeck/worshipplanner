---
phase: 27-service-order-tab-rename-and-strip-slide-editing-risk-medium
plan: 03
type: execute
wave: 2
depends_on: ["27-02"]
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
  - src/components/ImportedSlideEditor.vue
  - src/components/__tests__/ImportedSlideEditor.test.ts
  - src/stores/importedSlides.ts
autonomous: true
requirements: [R034]

must_haves:
  truths:
    - "The Service Order tab offers no way to edit an imported deck's slides — the expand/collapse control and the editor panel behind it are both gone (R034)."
    - "The Service Order tab offers no PowerPoint or image import action — neither of the two section-scoped menu entries, nor the import modal they opened, is mounted there any more (R034)."
    - "An existing imported plan item still renders as a plan item on the Service Order tab, with its heading and its empty-state wording, because it is service structure and only its slide EDITING left."
    - "Deck import still works from the Slides tab, which mounts its own import modal instance with its own confirm handler (Phase 25-07)."
    - "The imported-deck editor component and its test file no longer exist on disk, and no file in src refers to them (D-02, D-19)."
  artifacts:
    - "src/views/ServiceEditorView.vue with the imported-deck editor and both import surfaces removed, along with their now-unreferenced script state"
    - "src/views/__tests__/ServiceEditorView.test.ts carrying an absence regression block"
  key_links:
    - "The import modal component is imported by TWO files — this view and the Slides tab's grid, which reuses it under Phase 25 D-15. Only this view's usage and its import statement are removed; the component file itself must survive or the Slides tab's import breaks."
    - "The imported-deck editor component itself imports the scripture editor component. Deleting the former does not endanger the latter, which D-01 keeps mounted on this tab."
    - "The imported plan item's template branch has two halves: a heading plus empty-state that are service structure, and an editor toggle plus panel that are slide editing. Only the second half leaves; deleting the whole branch would make existing imported plan items render as nothing."
    - "The import-confirm handler is the only thing that creates a new imported plan item. Under the v1.3 model (R032) a user adds an ordinary plan item and appends a deck to its group from the Slides tab instead, so removing it is a model change, not a capability loss."
  prohibitions:
    - "MUST NOT delete src/components/PptxImportModal.vue, its test file, or its section prop. The Slides tab's grid mounts it and asserts that prop."
    - "MUST NOT delete or unmount src/components/ScriptureSlideEditor.vue — D-01 keeps scripture editing on this tab."
    - "MUST NOT remove the imported plan item's own row, heading or empty-state text, and MUST NOT remove the imported slot type, its type import, or the slot-factory call that still builds other slot kinds."
    - "MUST NOT touch the group delete cascade, its warning, the slot id backfill, autosave, or the section-assignment select."
    - "MUST NOT add a replacement import affordance anywhere. The Slides tab already has one."
    - "MUST NOT add a new component import to the view's test file to assert absence — assert on rendered text and test ids instead, so the test's mount surface does not grow."
  <!-- planner-discipline-allow: ImportedSlideEditor -->
---

<objective>
Remove the Phase 21 slide-editing surfaces from the Service Order tab: the imported-deck slide editor
and the two section-scoped PowerPoint/image import actions with the modal they opened. Then delete the
editor component, which those removals orphan.

Purpose: the first half of R034's "carries no slide editing". Editing an imported deck's slides is
unambiguously slide editing, and importing a deck now belongs to the Slides tab, where Phase 25-07
already put it.
Output: A Service Order tab with no deck-editing or deck-import surface, and one fewer dead component
on disk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-CONTEXT.md
@.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-02-SUMMARY.md
</context>

<interfaces>
Sites in `src/views/ServiceEditorView.vue` (line numbers approximate — the file is ~2847 lines and
27-02 has already edited it; re-locate everything before editing):

- ~256-263 — the import modal usage, bound to a boolean open flag, an org id, a section ref, a confirm
  handler and a cancel assignment.
- ~888-926 — the imported plan item's template branch. KEEP the heading and the empty-state paragraph.
  REMOVE the editor toggle button (test id `edit-imported-slides-btn`, whose label text flips between
  two strings) and the editor panel below it (test id `imported-editor-panel`).
- ~1004-1005 — the two Add-Element menu entries whose test ids are `add-import-announcements` and
  `add-import-sermon`, labelled "Import PowerPoint / Images (Announcements)" and "Import PowerPoint
  (Sermon)".
- ~1190 and ~1192 — the import statements for the imported-deck editor and the import modal.
- ~1398-1429 — the whole "Imported (PPTX/image) slot state" script section: the modal open flag, the
  modal section ref, the expanded-items set, the open-modal function, the toggle function, and the
  import-confirm handler that appends a new imported plan item. All six become unreferenced.

What must remain referenced after the edit: the imported slot TYPE (still used by the surviving heading
branch and by the has-content predicate near line 1920), the slot-factory helper (still used by the
generic add-slot path near line 1896), and the add-menu's other five entries.

`src/stores/importedSlides.ts` line ~79 carries a comment naming the deleted editor as the guarded edit
path. That is prose, not an import.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Assert the deck-editing and deck-import surfaces are absent</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
  Add one new describe block, titled for Phase 27, that mounts the view the same way the neighbouring
  Slides-tab block does (shallowMount with the same stub set, editor auth state, org id set).

  Assert absence: no element matching `[data-testid="edit-imported-slides-btn"]`, none matching
  `[data-testid="imported-editor-panel"]`, none matching `[data-testid="add-import-announcements"]`,
  none matching `[data-testid="add-import-sermon"]`, and the rendered page text does not contain the
  phrase "Import PowerPoint".

  Assert survival in the same block, using a locally built service fixture containing one imported plan
  item that carries an import id plus one that does not: the imported plan item's heading still renders,
  and the id-less one still renders its empty-state wording. This is the guard against deleting the whole
  branch instead of just its editing half.

  Also assert the Add Element menu still offers its five non-import entries after opening it, so the
  removal cannot silently take a neighbour with it.

  Do not import any additional component into this test file. Do not modify the view yet — this task's
  commit is the test half of a RED then GREEN pair and the new block is expected to fail.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>The new describe block FAILS on its absence assertions while every pre-existing test in the file still passes.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Strip the deck editor and both import surfaces from the view</name>
  <files>src/views/ServiceEditorView.vue</files>
  <action>
  In `src/views/ServiceEditorView.vue` only, remove:

  1. The import modal usage near line 256 together with its surrounding comment.
  2. Inside the imported plan item's branch, the editor toggle button and the editor panel it reveals —
     keeping the branch's heading and empty-state paragraph exactly as they are.
  3. Both section-scoped import entries in the Add Element menu, leaving the other five entries and the
     menu's click-away backdrop untouched.
  4. The two now-unused component import statements: the imported-deck editor and the import modal.
  5. The entire imported-slot script section listed in the interfaces block above — modal open flag,
     modal section ref, expanded-items set, open-modal function, toggle function and import-confirm
     handler — plus its section header comment.

  Leave the imported slot TYPE import, the slot-factory import and every other slot kind alone. If
  removing the import-confirm handler leaves a type import or helper unreferenced, check whether another
  call site still needs it before removing anything extra; the interfaces block names the two that stay.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>test -f src/components/PptxImportModal.vue && test -f src/components/ScriptureSlideEditor.vue && echo SURVIVORS-INTACT</automated>
    <automated>npx vitest run src/components/slides/__tests__/SlideGrid.test.ts</automated>
  </verify>
  <done>
  The view's suite passes including the new absence block, type-check reports 0 errors, the import modal
  and scripture editor component files still exist, and the Slides-tab grid suite — which mounts the
  import modal for real — still passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete the orphaned deck editor and clear its last prose reference</name>
  <files>src/components/ImportedSlideEditor.vue, src/components/__tests__/ImportedSlideEditor.test.ts, src/stores/importedSlides.ts</files>
  <action>
  Re-run the import check against the CURRENT tree before deleting anything — the graph has shifted since
  the phase context was written. Search src for real import statements naming the imported-deck editor
  component. Expect zero after Task 2. If any remain, stop and report rather than deleting.

  With zero importers confirmed, delete `src/components/ImportedSlideEditor.vue` and its counterpart
  `src/components/__tests__/ImportedSlideEditor.test.ts` (D-02, D-19: delete, do not deprecate; a test
  file for a deleted component is dead weight).

  Then update the stale comment in `src/stores/importedSlides.ts` near line 79 that names the deleted
  component as the guarded edit path — reword it to describe the guard by what it does rather than by the
  component that used to call it. Do not change any code in that store.

  Confirm the scripture editor component is still on disk and still imported by the view; the deleted
  component imported it, so this is the edge D-02 flags.
  </action>
  <verify>
    <automated>test ! -f src/components/ImportedSlideEditor.vue && test ! -f src/components/__tests__/ImportedSlideEditor.test.ts && echo DELETED</automated>
    <automated>test -z "$(grep -rl 'ImportedSlideEditor' src)" && echo NO-REMAINING-REFERENCES</automated>
    <automated>npm run type-check</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
  Both files are gone, nothing in src names the deleted component any more, type-check reports 0 errors
  and the production build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → Firestore/Storage | The removed import path wrote decks to Storage and plan items to Firestore; the surviving Slides-tab path writes the same data through its own handler. |
| viewer → service editor | The removed controls were all editor-gated; their absence must not change what a viewer could already see. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03-01 | Denial of Service | Slides-tab deck import | high | mitigate | The import modal is shared with the Slides tab's grid. Task 2 forbids deleting it and runs the grid's own suite, which mounts the modal for real, as a gate. |
| T-27-03-02 | Denial of Service | existing imported plan items | high | mitigate | Only the editing half of the imported plan item branch is removed; Task 1 asserts the heading and empty state still render for a fixture carrying imported items. |
| T-27-03-03 | Tampering | Storage upload rules | low | accept | No rule changes. The surviving Slides-tab import uses the same org-scoped Storage path the removed one did, so the reachable write surface is unchanged. |
| T-27-03-04 | Elevation of Privilege | editor-gated controls | medium | mitigate | Removals only take editor-gated controls away; no guard is loosened. The viewer-visibility test in the neighbouring block stays in place. |

No package-manager installs occur in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts src/components/slides/__tests__/SlideGrid.test.ts` passes.
- `npm run type-check` reports 0 errors and `npm run build` succeeds.
- `src/components/PptxImportModal.vue` and `src/components/ScriptureSlideEditor.vue` are both still present.
</verification>

<success_criteria>
No deck-editing or deck-import surface remains on the Service Order tab, existing imported plan items
still render there, the Slides tab's import still works, and the orphaned editor component plus its test
are gone with no dangling references.
</success_criteria>

<output>
Create `.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-03-SUMMARY.md` when done
</output>
