---
phase: 12-advanced-song-search-and-multi-select-persistent-tag-filteri
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
closes_gap: "UAT test 8 — delete-confirmation modal copy matches documented behavior (doc_update_only, code_fix: false)"
files_modified:
  - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md
  - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-05-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-16]
must_haves:
  truths:
    - "D-16 documents the delete-confirmation modal's GENERIC wording as the intended behavior (no element-type-aware copy claim)"
    - "12-05-SUMMARY.md no longer claims the shipped modal produces element-type-aware copy"
    - "ROADMAP's Phase 12 plan-05 line describes generic (not element-type-aware) delete-confirmation copy"
    - "NO runtime/code files are modified — this is documentation only"
  artifacts:
    - path: ".planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md"
      provides: "D-16 updated to describe generic wording as intended"
    - path: ".planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-05-SUMMARY.md"
      provides: "Corrected description of shipped generic delete-confirmation copy"
  key_links: []
---

<objective>
Close the UAT test-8 gap, which is documentation-only (`code_fix: false`). The shipped delete-confirmation modal always uses generic wording ("this item" / "this element") rather than element-type-aware copy ("this song" / "this scripture"). The user ACCEPTS the generic wording and wants the functionality kept — so the docs must be corrected to describe the generic wording as the INTENDED behavior. Update D-16 (in 12-CONTEXT.md), the 12-05-SUMMARY.md claims, and the ROADMAP plan-05 line.

Purpose: Make the spec/docs match the accepted shipped behavior so there is no lingering "documented-but-not-implemented" element-type-aware copy claim.
Output: Edited `12-CONTEXT.md` (D-16), `12-05-SUMMARY.md`, and `ROADMAP.md`.

Scope guard: DOCUMENTATION ONLY. Do NOT touch `src/views/ServiceEditorView.vue` or any other code file. Do NOT change the modal's runtime behavior — the confirmation gate on every element removal (D-15) stays exactly as shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UAT.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md
@.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-05-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
UAT gap (12-UAT.md, test 8): "User ACCEPTS the generic wording and wants to KEEP the functionality; no code fix. Action: update the spec/docs (D-16 and 12-05-SUMMARY.md claim of element-type-aware copy) to describe the generic wording as the intended behavior."

D-16 CURRENT text (12-CONTEXT.md line 46) — the clause to correct is "naming the element type when known":
> **D-16:** Reuse the existing D-14 Teleport modal ... Use **generic wording** — e.g. "Remove this element from the plan?" — naming the element type when known. ...

12-05-SUMMARY.md CURRENT element-type-aware claims to correct:
- `provides:` bullet (line ~13): "Element-type-aware delete-confirmation copy (elementLabel helper + deleteConfirmHeading/deleteConfirmBody computeds)"
- title/summary line (~43): "made the modal heading/body element-type-aware"
- Accomplishments bullet (~55-56): "Added `elementLabel(kind)` helper mapping ... to human-readable labels" and "generic, element-type-aware wording per D-16"

ROADMAP.md CURRENT (line 142): "12-05-PLAN.md — Delete-confirmation: widen removeSlot gate to all removals (incl. empty slots) + element-type-aware copy, reusing D-14 modal"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct D-16, 12-05-SUMMARY, and ROADMAP to describe generic delete-confirmation wording as intended</name>
  <files>.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md, .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-05-SUMMARY.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-UAT.md (test 8 note + the doc_update_only gap at the bottom)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-CONTEXT.md (D-16 line 46)
    - .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-05-SUMMARY.md (lines 13, 43, 54-56, 70)
    - .planning/ROADMAP.md (Phase 12 plan list, line 142)
  </read_first>
  <action>
    Make DOCUMENTATION-ONLY edits so the docs describe the shipped GENERIC wording as intended. Do NOT edit any file under `src/`.

    A) 12-CONTEXT.md — D-16 (line 46): reword to state the intended/accepted behavior is a single GENERIC confirmation wording that is NOT element-type-aware. Replace the "naming the element type when known" clause with a note that the shipped and accepted copy is generic ("Remove this element from the plan?" / "this item"), the same for every element type. Add a short parenthetical: "(Accepted per 12-UAT test 8, 2026-07-02: generic wording is the intended behavior; element-type-aware copy was NOT required.)". Keep the rest of D-16 (reuse D-14 modal, Cancel + red confirm, onClearSong distinct) intact.

    B) 12-05-SUMMARY.md — correct every "element-type-aware" claim to describe the actual shipped generic wording:
    - `provides:` bullet (~line 13): change "Element-type-aware delete-confirmation copy (elementLabel helper + deleteConfirmHeading/deleteConfirmBody computeds)" to describe GENERIC delete-confirmation copy driven by the heading/body computeds (the `elementLabel` helper exists in code but the shipped copy reads generic).
    - Title/summary sentence (~line 43): replace "made the modal heading/body element-type-aware" with "kept the modal heading/body generic (single wording for all element types)".
    - Accomplishments bullets (~lines 55-56): reword so they state the modal uses generic wording per D-16 (drop the "element-type-aware" phrasing). You MAY keep a factual note that an `elementLabel()` helper exists in the code but its output is not surfaced as distinct per-type copy in the shipped modal.
    - Add a brief clarifying line under "Deviations from Plan" (or a new "Post-UAT Correction" note): "Per 12-UAT test 8 (2026-07-02), the shipped modal copy is generic, not element-type-aware; the user accepted this and D-16 was updated to make generic wording the intended behavior. No code change."
    - Do NOT alter the commit hashes, metrics, or the D-15 gate-widening description (that behavior shipped correctly).

    C) ROADMAP.md (line 142): change "+ element-type-aware copy" to "+ generic confirmation copy" in the 12-05 plan bullet.

    Commit `docs(12-08): correct D-16/12-05-SUMMARY/ROADMAP to describe generic delete-confirmation wording as intended`.
  </action>
  <verify>
    <automated>! grep -rn "element-type-aware" .planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-05-SUMMARY.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 12-CONTEXT.md D-16 states generic wording is the intended behavior and references the 12-UAT test-8 acceptance
    - 12-05-SUMMARY.md no longer asserts the shipped modal produces element-type-aware copy; a post-UAT correction note is present
    - ROADMAP.md line 142 reads "generic confirmation copy" (not "element-type-aware copy")
    - `git status` shows NO modified files under `src/` (documentation-only)
    - `grep -rn "element-type-aware" .planning/.../12-05-SUMMARY.md .planning/ROADMAP.md` returns no matches
  </acceptance_criteria>
  <done>Docs describe the shipped generic delete-confirmation wording as intended; no code touched.</done>
</task>

</tasks>

<threat_model>
Not applicable — documentation-only change. No trust boundaries, inputs, or code paths are affected.
</threat_model>

<verification>
- `grep -rn "element-type-aware" .planning/phases/12-.../12-05-SUMMARY.md .planning/ROADMAP.md` — no matches
- D-16 in 12-CONTEXT.md describes generic wording as intended
- `git diff --name-only` shows only `.planning/` files changed (no `src/` files)
</verification>

<success_criteria>
- D-16, 12-05-SUMMARY.md, and ROADMAP describe the shipped generic delete-confirmation wording as the intended behavior (D-16)
- Delete-confirmation runtime behavior (the D-15 gate on every removal) is unchanged
- No code files modified
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-song-search-and-multi-select-persistent-tag-filteri/12-08-SUMMARY.md`
</output>
