---
phase: 98-fullscreen-setup-helper
plan: 02
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - src/components/FullscreenSetupPanel.vue
  - src/components/__tests__/FullscreenSetupPanel.test.ts
  - src/views/MonitorSetupView.vue
  - src/views/__tests__/MonitorSetupView.test.ts
autonomous: true
requirements: [R285, R286, R287]

must_haves:
  truths:
    - "FullscreenSetupPanel renders four mutually-exclusive states (checking / ready / not-ready / unsupported) with the UI-SPEC data-testids and copy (R285)."
    - "In not-ready state, the primary button label names the DETECTED browser+OS and, on click, generates the origin-baked artifact for that OS and triggers a download; Windows additionally shows the HKLM admin fallback link (R286)."
    - "Clicking 'Confirm fullscreen support' re-runs the readiness check and, when it now resolves ready, flips to the ready state WITHOUT a remount; a confirm that is still not-ready reveals the troubleshooting block (R287)."
    - "MonitorSetupView mounts FullscreenSetupPanel exactly once, below the existing phase v-if/else chain, in EVERY phase (R287 embedded, additive)."
    - "The origin baked into every generated file comes from window.location.origin (never a hardcoded string or URL param) (R286, CONTEXT Decision 2)."
  artifacts:
    - src/components/FullscreenSetupPanel.vue
    - src/components/__tests__/FullscreenSetupPanel.test.ts
    - src/views/MonitorSetupView.vue
    - src/views/__tests__/MonitorSetupView.test.ts
  key_links:
    - "Panel consumes useFullscreenReadiness (status + recheck) and buildPolicyArtifact + downloadTextFile from 98-01."
    - "os/browser come from detectOS()/detectBrowser() (98-01); labels drive the CTA copy 'Download setup file for {browserLabel} on {osLabel}'."
    - "The existing gesture fallbacks (useRunControl delegateFullscreenToAll / 'Fullscreen displays', useOutputWindow tap-to-fullscreen) are NOT touched — the panel is purely additive on MonitorSetupView (Preserve constraint)."
---

<objective>
Build the `FullscreenSetupPanel.vue` UI to the 98-UI-SPEC design contract (four states, per-OS
download + numbered steps + honest-friction caveats + troubleshooting) and mount it, unconditionally,
at the bottom of `MonitorSetupView.vue`. This is the thin rendering layer over the Wave-1 logic.

Purpose: delivers the operator-facing surface of R285 (honest state + Confirm re-check), R286
(one-click, origin-correct, per-OS download with honest steps), and R287 (embedded, self-correcting,
actionable troubleshooting) — while leaving the Phase 92 monitor-assignment machine and all existing
gesture fallbacks untouched.

Output: `src/components/FullscreenSetupPanel.vue` (+ test), and an additive edit to
`src/views/MonitorSetupView.vue` (+ its test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase inputs (authoritative — the panel is built to the UI-SPEC)
@.planning/phases/98-fullscreen-setup-helper/98-CONTEXT.md
@.planning/phases/98-fullscreen-setup-helper/98-UI-SPEC.md
@.planning/phases/98-fullscreen-setup-helper/98-PATTERNS.md

# Wave-1 modules this plan consumes (from 98-01)
@src/utils/osDetect.ts
@src/utils/fullscreenPolicyFiles.ts
@src/utils/downloadTextFile.ts
@src/composables/useFullscreenReadiness.ts

# Exact visual + test analogs — read before writing
@src/components/MonitorFallbackPanel.vue
@src/components/MonitorCard.vue
@src/components/__tests__/MonitorCard.test.ts
@src/views/MonitorSetupView.vue
@src/views/__tests__/MonitorSetupView.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FullscreenSetupPanel.vue component</name>
  <files>src/components/FullscreenSetupPanel.vue, src/components/__tests__/FullscreenSetupPanel.test.ts</files>
  <behavior>
    - Renders a constant heading "Automatic fullscreen" + the constant subhead in all states, inside the shell with data-testid fullscreen-setup-panel.
    - status='checking' -> shows fullscreen-setup-status-checking (spinner + "Checking this computer's setup…"), no buttons.
    - status='ready' -> shows fullscreen-setup-status-ready (green check + "This computer is set up for automatic fullscreen."), no download/confirm/troubleshooting.
    - status='not-ready' -> shows fullscreen-setup-status-not-ready, the fullscreen-setup-download-button labeled "Download setup file for {browserLabel} on {osLabel}", the fullscreen-setup-instructions ordered list, the fullscreen-setup-caveat, and the fullscreen-setup-confirm-button. On Windows only, also fullscreen-setup-admin-download-link.
    - status='unsupported' -> shows fullscreen-setup-status-unsupported with the fallback copy and a demoted (text-link) fullscreen-setup-confirm-button "Check again".
    - Clicking the download button calls buildPolicyArtifact(os, window.location.origin, 'HKCU') then downloadTextFile(filename, contents, mimeType); the Windows admin link uses scope 'HKLM'.
    - Clicking confirm calls recheck(); when recheck flips to 'ready' the not-ready block unmounts and the ready block renders with NO wrapper remount.
    - The fullscreen-setup-troubleshooting block appears ONLY after a confirm click has resolved and status is still not-ready (not on first paint).
    - If downloadTextFile throws, a text-red-400 error line appears under the download button.
  </behavior>
  <action>
    Create `src/components/FullscreenSetupPanel.vue` (FLAT under src/components/, matching
    MonitorCard.vue / MonitorFallbackPanel.vue — NOT a monitor/ subfolder). Build it exactly to
    98-UI-SPEC: the panel shell markup, State 1 (checking), State 2 (ready), State 3 (not-ready) with
    per-OS numbered steps + caveat + admin link, State 3b (troubleshooting), and State 4 (unsupported) —
    reusing every data-testid and every string in the UI-SPEC Copywriting Contract and Data-testid Hooks
    table verbatim. Use the dark-theme Tailwind vocabulary from PATTERNS.md Shared Patterns
    (bg-gray-900/border-gray-800 card, indigo-600 primary buttons, green-400 ready, amber not-ready,
    gray unsupported, quiet underline links). Reuse the inline check-circle SVG from MonitorSetupView.vue
    lines 54-57 for ready and the amber warning-triangle for not-ready.

    Script setup:
    - `const { status, recheck } = useFullscreenReadiness()` (98-01) drives the state machine.
    - `const os = detectOS()` / `const browser = detectBrowser()` (98-01) computed once; expose
      `osLabel(os)` / `browserLabel(browser)` for the CTA label and per-OS branches.
    - Read the origin from `window.location.origin` at download time (NEVER hardcode, NEVER a URL param —
      CONTEXT Decision 2 / T-98-02). Do not thread any query param into the generator.
    - `onDownload()` -> buildPolicyArtifact(os, window.location.origin, 'HKCU') then downloadTextFile(...);
      wrap in try/catch and set a `downloadError` ref to show the red failure line (UI-SPEC "Download
      failure" row — the one place red is used).
    - `onDownloadAdmin()` (Windows) -> same but scope 'HKLM'.
    - `onConfirm()` -> `await recheck()`; set an `attempted` ref true so troubleshooting can show only
      after a resolved-still-not-ready confirm.
    - Per-OS instruction step-2 verb + caveat text come from the UI-SPEC Copywriting Contract, selected
      by `os`. macOS/Linux copy MUST stay honest about admin/sudo friction and MUST NOT over-promise;
      the macOS steps note the two-step System Settings -> Profiles approval and the unsigned-profile
      warning (RESEARCH Pitfall 5), and mention the `defaults write` alternative; Linux notes the
      sudo + path-may-vary caveat (RESEARCH Pitfall 6 / OQ2). Windows step-2 states the "may be
      dangerous — click Keep" friction (RESEARCH Pitfall 4). All three tell the operator to FULLY quit
      and reopen the browser (not just the tab) before clicking Confirm (RESEARCH Pitfall 3).

    Write `src/components/__tests__/FullscreenSetupPanel.test.ts` FIRST (RED), structured like
    MonitorCard.test.ts (@vue/test-utils mount, data-testid lookups). Mock the readiness composable OR
    the underlying navigator.permissions (useOutputWindow.test.ts idiom) to drive each state
    deterministically. Cover: each state renders its own testid and correct copy; the download button
    label interpolates browserLabel+osLabel; clicking download invokes downloadTextFile with a filename
    ending in the OS-correct extension and the origin baked into the contents (spy on downloadTextFile
    via vi.mock of src/utils/downloadTextFile, and/or assert the generated contents contain
    window.location.origin); the Windows admin link is present only when os==='windows'; a confirm that
    flips not-ready->ready re-renders to the ready testid on the SAME wrapper (no unmount) — proving R287
    "no reload"; troubleshooting is absent on first paint and present only after a still-not-ready
    confirm; a thrown download surfaces the red error line. Also assert the panel NEVER calls
    requestFullscreen (spy).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/FullscreenSetupPanel.test.ts</automated>
  </verify>
  <done>The panel renders all four UI-SPEC states with the exact testids/copy, downloads the origin-baked OS-correct artifact (HKCU default + HKLM admin link on Windows), self-corrects not-ready->ready without remount, and reveals troubleshooting only after a failed confirm; all component tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the panel in MonitorSetupView (additive)</name>
  <files>src/views/MonitorSetupView.vue, src/views/__tests__/MonitorSetupView.test.ts</files>
  <behavior>
    - MonitorSetupView renders exactly one FullscreenSetupPanel, as the last child of the outer `div.px-6.py-8.max-w-4xl`, OUTSIDE the existing phase v-if/else chain (so it is present in prompt/detecting/denied/manual/unavailable/granted alike).
    - No existing phase branch, testid, or handler in MonitorSetupView is altered or removed.
    - The existing MonitorSetupView test assertions all still pass.
  </behavior>
  <action>
    Edit `src/views/MonitorSetupView.vue`: import FullscreenSetupPanel from `@/components/FullscreenSetupPanel.vue`
    (alongside the existing MonitorCard / MonitorFallbackPanel imports at lines 155-157), and add
    `<FullscreenSetupPanel class="mt-8" />` as the LAST child inside the `<div class="px-6 py-8 max-w-4xl">`
    container (after the closing tag of the phase v-if/else chain, before the div closes at line 149).
    Render it UNCONDITIONALLY — it is NOT one of the mutually-exclusive phase branches (UI-SPEC Placement
    rationale: auto-fullscreen readiness is gated by the `fullscreen` permission, a different permission
    than `window-management`, and is relevant even to an operator on the manual/denied/unavailable path).
    Do NOT modify any existing branch, ref, computed, or handler — this is purely additive (Preserve
    constraint). The panel sets its own `data-testid="fullscreen-setup-panel"` root, so no wrapper testid
    is needed in the view.

    Update `src/views/__tests__/MonitorSetupView.test.ts`: stub FullscreenSetupPanel via mount `global.stubs`
    (a lightweight stub exposing `data-testid="fullscreen-setup-panel"`) so the readiness composable does
    not couple these view tests, then add an assertion that the panel is present in at least two different
    phases (e.g. the default/prompt phase and one other reachable phase such as `unavailable`), proving it
    renders outside the phase chain. Keep every pre-existing assertion intact and passing. If the file
    already stubs child components, follow that same stubbing convention.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/MonitorSetupView.test.ts</automated>
  </verify>
  <done>MonitorSetupView mounts FullscreenSetupPanel once, unconditionally, below the phase chain; all existing view tests plus the new presence assertions pass; no existing branch/handler changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input -> generated file | The only value flowing into the downloaded artifact is the app's own computed origin. |
| panel -> existing Monitor Setup machine | The panel is additive; it must not read or mutate the phase state machine or gesture-fallback call sites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-04 | Tampering | FullscreenSetupPanel download handlers | medium | mitigate | Origin is read directly from `window.location.origin` at download time — never from a route/query param, prop, or user text field. Component test asserts the downloaded contents contain the live origin and no injection path threads a URL param into the generator (RESEARCH V5/Tampering). |
| T-98-05 | Information Disclosure | panel copy (honest friction) | low | accept | Copy intentionally discloses platform friction (Windows "may be dangerous", macOS admin/unsigned, Linux sudo) rather than hiding it — this is a UX requirement (CONTEXT Decision 5), not a leak. |
| T-98-06 | Elevation of Privilege | additive mount in MonitorSetupView | low | mitigate | The panel is rendered additively and does not touch useRunControl/useOutputWindow fallbacks or the Phase 92 phase machine; the readiness composable it uses is read-only (never calls requestFullscreen). Regression guard: existing MonitorSetupView + useOutputWindow suites stay green. |

No package-install threat row (T-98-SC): this plan installs no packages.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/FullscreenSetupPanel.test.ts src/views/__tests__/MonitorSetupView.test.ts` — all green.
- `npm run type-check` (vue-tsc --build, per CLAUDE.md) is clean.
- `npx vitest run` (bare) shows no NEW failures beyond the known `src/storage.rules.test.ts` baseline.
- Regression guard: `npx vitest run src/composables/__tests__/useOutputWindow.test.ts` stays green; confirm via Glob whether a `useRunControl` test file exists and, if so, include it — neither useOutputWindow.ts nor useRunControl.ts is modified by this plan.
- Manual/UAT (owner, at /gsd-verify-work — NOT an in-plan gate): on real hardware, download the file, run it, fully restart the browser, click "Confirm fullscreen support", and confirm the panel flips to ready ✓.
</verification>

<success_criteria>
- R285: the panel shows honest ready ✓ / not-ready / unsupported / checking states with a working "Confirm fullscreen support" re-check.
- R286: not-ready state offers a one-click, origin-baked, OS-correct download (Windows HKCU default + HKLM admin link, macOS .mobileconfig, Linux JSON) with honest per-OS steps and caveats.
- R287: the helper is embedded in MonitorSetupView, self-corrects to ready ✓ without a reload, and shows actionable troubleshooting while not ready; gesture fallbacks are untouched.
- All new component + view tests green; type-check clean; no new full-suite failures.
</success_criteria>

<output>
Create `.planning/phases/98-fullscreen-setup-helper/98-02-SUMMARY.md` when done.
</output>
