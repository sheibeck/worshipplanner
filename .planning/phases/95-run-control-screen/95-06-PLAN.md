---
phase: 95-run-control-screen
plan: 06
type: execute
wave: 4
depends_on: ["95-04", "95-02"]
files_modified:
  - src/views/__tests__/RunControlView.output.test.ts
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R261, R266, R275]

must_haves:
  truths:
    - "src/views/__tests__/RunControlView.output.test.ts mounts the REAL RunControlView with the same harness as RunControlView.test.ts (injected fake channelFactory, mocked router/stores/assembly, stubbed SlideCanvas) but drives the ON-MOUNT output orchestration: a per-test installed window.getScreenDetails returning fake screens, a real saved monitor mapping seeded into localStorage via computeFingerprint, and a window.open spy returning a fake window (moveTo + document.documentElement.requestFullscreen spies) — never a real window (R261)."
    - "MATCHED (R261/R266): with a saved mapping whose fingerprints equal computeFingerprint of the two fake screens, mounting calls window.open TWICE with the audience URL + name 'wp-audience' and the confidence URL + name 'wp-confidence', and calls requestFullscreen with a { screen } option per opened window; the top-bar 'Displays ready' status renders (run-status-ready) and NO fallback banner shows."
    - "FALLBACK (R261): when matchMapping is needs-reprompt (fake screens that do NOT match the saved mapping), OR getScreenDetails is absent (deleted), OR getScreenDetails rejects (denied), OR no mapping is saved — the view still calls window.open for both output windows UN-POSITIONED (no placement features), shows the amber run-fallback-banner with the 'Open monitor setup' link to /monitor-setup, calls NO requestFullscreen, and NEVER throws."
    - "GUARDS (R261): when the window.open spy returns null (blocked/jsdom), mounting does not throw and no requestFullscreen is attempted; and clicking through the Escape → run-exit-confirm path calls close() on each previously-opened fake window before navigating."
    - "src/views/__tests__/ServiceEditorView.test.ts gains a describe block proving the Run button (R261/R275): ABSENT on a draft service; PRESENT (data-testid=run-service-btn, aria-label='Run this service live') on a LOCKED service for an EDITOR; PRESENT on a LOCKED service for a VIEWER (isEditor=false, orgId set) — the R275 proof that running is not editor-gated; and clicking it router.push-es '/run/service-1?org=org-1'."
  artifacts:
    - src/views/__tests__/RunControlView.output.test.ts
    - "src/views/__tests__/ServiceEditorView.test.ts (a new 'Run entry button (R261/R275)' describe block)"
  key_links:
    - "The output test seeds localStorage with saveMapping({ assignments:[{fingerprint:computeFingerprint(screenA),role:'audience'},{fingerprint:computeFingerprint(screenB),role:'confidence'}], savedAt:Date.now() }) and installs window.getScreenDetails = () => Promise.resolve({ screens:[screenA,screenB] }); window.open is vi.spyOn'd to return a fresh fake window per call so per-window moveTo/requestFullscreen are inspectable."
    - "The button describe block reuses ServiceEditorView.test.ts's existing mount harness + mockAuthState (isEditor/orgId) + mockService(status), flipping status to 'planned'/'exported' and isEditor false to prove the viewer case; a shared useRouter push spy (stabilized in the file's vue-router mock) asserts the /run navigation."
---

<objective>
Author the two remaining test surfaces: the RunControlView output-window orchestration suite (the #2 phase
risk — the build-from-spec window.open + getScreenDetails + placement + fallback), and the ServiceEditorView
Run-button suite (R261 presence/absence + the R275 viewer-can-Run proof). Together with 95-05 these close
the phase's Nyquist coverage.

Purpose: R261 (the control screen opens/places the output windows, with a first-class fallback), R266 (the
opened windows are the outputs the single-selection state drives), R275 (a viewer — not just an editor — can
Run a locked service). Every window and screen is faked; no real window is ever opened and no real
getScreenDetails is called.

Output: src/views/__tests__/RunControlView.output.test.ts (new) and a Run-button describe block added to
src/views/__tests__/ServiceEditorView.test.ts (reusing its proven mount harness).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-run-control-screen/95-CONTEXT.md
@.planning/phases/95-run-control-screen/95-PATTERNS.md
@src/views/RunControlView.vue
@src/views/ServiceEditorView.vue
@src/views/__tests__/AudienceOutputView.test.ts
@src/views/__tests__/MonitorSetupView.test.ts
@src/views/__tests__/ServiceEditorView.test.ts
@src/utils/monitorConfig.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author RunControlView.output.test.ts — getScreenDetails/matchMapping/window.open/placement/fallback</name>
  <files>src/views/__tests__/RunControlView.output.test.ts</files>
  <read_first>
    - src/views/RunControlView.vue — the openOutputs/openPlaced/openUnplaced/closeOutputs flow and the exact data-testids (run-status-ready, run-status-opening, run-fallback-banner) + the "Open monitor setup" link + the 'wp-audience'/'wp-confidence' window names + the audience/confidence URL shape.
    - src/views/__tests__/MonitorSetupView.test.ts — the idiom for installing/deleting a window capability per test (how it stubs window.getScreenDetails / navigator.* and asserts matched vs fallback branches); mirror its install/delete + fake ScreenDetails shape.
    - src/views/__tests__/AudienceOutputView.test.ts — the base harness (reactive router mock, mocked stores + useSlideshowAssembly, SlideCanvas stub, injected channelFactory, enableAutoUnmount) shared with RunControlView.test.ts.
    - src/utils/monitorConfig.ts — computeFingerprint (:69) + saveMapping (:108) so you can seed a REAL matching mapping into localStorage from your fake screens (exercising the real loadMapping/matchMapping rather than mocking them), plus ScreenLike (:28-35).
  </read_first>
  <action>
    Create src/views/__tests__/RunControlView.output.test.ts reusing the RunControlView.test.ts harness
    (import/duplicate the same router/store/assembly/SlideCanvas mocks + fake channel + enableAutoUnmount).
    Add the output-orchestration machinery:
    - Two fake screens screenA/screenB (ScreenLike: label/width/height/left/top/isPrimary) with distinct
      positions.
    - A helper to install window.getScreenDetails = () =&gt; Promise.resolve({ screens:[screenA,screenB] })
      (assign in beforeEach or per-test; delete it in afterEach), mirroring MonitorSetupView.test.ts's
      capability install.
    - A window.open spy: vi.spyOn(window,'open').mockImplementation(() =&gt; makeFakeWin()) where makeFakeWin
      returns { moveTo: vi.fn(), close: vi.fn(), document: { documentElement: { requestFullscreen: vi.fn().
      mockResolvedValue(undefined) } } } (cast to Window as needed); keep references to the returned fakes
      so per-window calls are inspectable. Provide a variant test where window.open returns null.
    - localStorage seeding: import { computeFingerprint, saveMapping } from '@/utils/monitorConfig' and, in
      the MATCHED tests, saveMapping({ assignments:[{fingerprint:computeFingerprint(screenA),role:'audience'},
      {fingerprint:computeFingerprint(screenB),role:'confidence'}], savedAt:Date.now() }) before mount; clear
      localStorage in beforeEach so the FALLBACK tests start with no mapping.

    WRITE these describe blocks:
    1. MATCHED (R261/R266): seed the matching mapping + install getScreenDetails; mount + flushPromises;
       assert window.open was called with the audience URL ('/present/audience/service-1?org=org-1') and name
       'wp-audience', and again with the confidence URL and name 'wp-confidence'; assert each opened fake
       window's document.documentElement.requestFullscreen was called with an object carrying a `screen`;
       assert run-status-ready renders and run-fallback-banner does NOT.
    2. NEEDS-REPROMPT fallback (R261): seed a mapping that does NOT match the fake screens (e.g. save
       fingerprints for different screens) + install getScreenDetails; mount; assert window.open was still
       called twice but WITHOUT placement features (un-positioned), requestFullscreen was NOT called,
       run-fallback-banner renders with the "Open monitor setup" link to /monitor-setup, and nothing threw.
    3. UNAVAILABLE fallback (R261): DELETE window.getScreenDetails; mount; assert both windows opened
       un-positioned, run-fallback-banner shows, no throw.
    4. DENIED fallback (R261): install getScreenDetails = () =&gt; Promise.reject(new Error('denied')); mount +
       flushPromises; assert the .catch path opened un-positioned pop-outs + the banner, no throw.
    5. NULL window guard (R261): matched setup but window.open mockReturnValue(null); mount; assert no throw
       and no requestFullscreen attempted.
    6. CLOSE ON EXIT (R266): open (un-positioned) then dispatch Escape + click run-exit-confirm; assert each
       previously-returned fake window's close() spy fired before navigation.

    Never call a real getScreenDetails and never open a real window — every screen + window is a fake.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - The suite covers matched → window.open (both URLs + stable names) + requestFullscreen({screen}) per
      window + run-status-ready; needs-reprompt/unavailable/denied/no-mapping → un-positioned window.open +
      run-fallback-banner + monitor-setup link + no requestFullscreen + no throw; null window.open → no throw;
      exit → each opened window closed.
    - All screens/windows are faked; `npx vitest run src/views/__tests__/RunControlView.output.test.ts`
      passes; `npx vitest run` shows only the baseline; `npm run type-check` is clean; no `--dir src`.
  </acceptance_criteria>
  <done>The output orchestration is proven: matched placement opens + positions + fullscreens both windows, every not-matched/blocked path degrades to un-positioned pop-outs + the amber banner without throwing, and exit closes the windows — green with no baseline regression.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Run-button describe block to ServiceEditorView.test.ts (R261/R275)</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <read_first>
    - src/views/__tests__/ServiceEditorView.test.ts — the full existing harness: the vue-router mock (:41-46; note useRouter returns a fresh { push: vi.fn() } — you must stabilize this into a shared push spy to assert the /run navigation, a low-risk change no existing test asserts against), mockAuthState (reactive, isEditor/orgId, :489-554) which tests flip (e.g. :883 mockAuthState.isEditor = true), mockService (status:'draft' default, :286-292) + mockServicesList (:313), the shallowMount mount helper the suite uses, and enableAutoUnmount (:33). Find how an existing test mounts with a chosen status + auth role and reuse that exact approach.
    - src/views/ServiceEditorView.vue — the run-service-btn markup + canRunService gate added in 95-02 (data-testid, aria-label, the /run push in onRun).
  </read_first>
  <action>
    Add a new describe('Run entry button (R261/R275)') block to ServiceEditorView.test.ts, using the file's
    existing mount harness (do NOT build a new one). If needed for the navigation assertion, stabilize the
    top-level vue-router mock so useRouter returns a single shared push spy (hoist mockRouterPush = vi.fn()
    and reference it from the mock factory) — reset it in beforeEach; this is additive and no existing test
    reads push.

    Cases (mount the real ServiceEditorView via the existing helper, set the service status + auth role per
    the harness's established pattern, flushPromises, then assert on wrapper.find('[data-testid=
    "run-service-btn"]')):
    1. DRAFT → ABSENT: default status 'draft' (any role) → run-service-btn does NOT exist.
    2. LOCKED + EDITOR → PRESENT: status 'planned' (or 'exported'), mockAuthState.isEditor=true, orgId set →
       run-service-btn exists with aria-label 'Run this service live'.
    3. LOCKED + VIEWER → PRESENT (the R275 proof): status 'planned', mockAuthState.isEditor=FALSE, orgId set →
       run-service-btn STILL exists (running is not editor-gated). This is the load-bearing R275 assertion.
    4. CLICK → NAVIGATE: on a locked service, trigger the run-service-btn click and assert the shared
       mockRouterPush was called with '/run/service-1?org=org-1' (matching the mocked route id + orgId). If
       stabilizing the push spy proves impractical within the harness, instead assert the button's presence +
       that onRun is bound (@click) and record the navigation assertion as covered by the presence proof —
       but PREFER the real push assertion.

    Keep the block self-contained (reset mockAuthState/status in its own beforeEach or per-test so it does not
    perturb the rest of the file), and rely on enableAutoUnmount for cleanup.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - The new describe block proves: run-service-btn absent on a draft; present on a locked service for an
      editor; present on a locked service for a VIEWER (isEditor=false, orgId set) — the R275 proof; and (best
      effort, preferred) clicking it router.push-es '/run/service-1?org=org-1'.
    - The rest of ServiceEditorView.test.ts stays green (the block is additive and resets its own state).
    - `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` passes; `npx vitest run` shows only the
      baseline (src/storage.rules.test.ts); `npm run type-check` is clean; no `--dir src`.
  </acceptance_criteria>
  <done>The Run button is proven absent on a draft and present on a locked service for BOTH an editor and a viewer (the R275 non-editor-gating proof), navigating to /run/:serviceId?org= on click — with the rest of the ServiceEditorView suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> real monitorConfig | The matched-placement test seeds a REAL localStorage mapping via computeFingerprint and exercises the real loadMapping/matchMapping, so a placement regression is caught against production matching logic, not a mock |
| test harness -> faked window/screen | window.open + getScreenDetails are faked so no real window/permission is touched; the null-return and reject branches prove the guards |
| Run-button describe block -> shared ServiceEditorView harness | The block reuses the existing mount + auth/status mocks additively and resets its own state so it cannot pollute sibling tests' timers/spies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-16 | Denial of Service | window.open null / throw regression | high | mitigate | The suite mounts with window.open returning null and with getScreenDetails rejecting/absent and asserts no throw + calm fallback, so a regression that let a blocked pop-out or a denied permission crash the control screen fails here. |
| T-95-17 | Elevation of Privilege | R275 viewer-can-Run regression | high | mitigate | The button block asserts the Run button PRESENT for a viewer (isEditor=false) on a locked service; a regression that re-gated Run on canEditService/isEditor (locking viewers out) fails here. |
| T-95-18 | Information Disclosure | wrong output URL / org carry | low | mitigate | The matched test asserts the exact audience/confidence URLs incl. ?org=, so a regression that dropped or mis-scoped the org in the opened window URL is caught. |
| T-95-SC | Tampering | package installs (npm) | n/a | accept | No package-manager installs — test files against existing in-repo modules; supply-chain checkpoint not applicable. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/RunControlView.output.test.ts` and `npx vitest run
  src/views/__tests__/ServiceEditorView.test.ts` are green; `npx vitest run` shows only the
  src/storage.rules.test.ts baseline; `npm run type-check` is clean; no `--dir src`.
- Matched placement (window.open + requestFullscreen({screen}) per role), every fallback path (un-positioned
  pop-outs + amber banner + monitor-setup link, no throw), the null-window guard, close-on-exit, and the
  Run-button presence/absence incl. the viewer case are all asserted.
- HUMAN-UAT: none introduced by these test-only tasks (the real two-monitor placement + a real viewer
  account reaching the control screen remain the deferred milestone-end UAT items from 95-04 / 95-02).
</verification>

<success_criteria>
- The output-window orchestration (R261/R266) and the Run-button gating incl. R275's viewer-can-Run are
  covered by passing behavioral suites, with every window/screen faked and no baseline regression.
</success_criteria>

<output>
Create `.planning/phases/95-run-control-screen/95-06-SUMMARY.md` when done.
</output>
</content>
