---
phase: 95-run-control-screen
plan: 05
type: execute
wave: 4
depends_on: ["95-04"]
files_modified:
  - src/views/__tests__/RunControlView.test.ts
autonomous: true
requirements: [R262, R263, R264, R265, R266]

must_haves:
  truths:
    - "src/views/__tests__/RunControlView.test.ts mounts the REAL RunControlView with an injected in-memory channelFactory (BroadcastChannelLike), a mocked vue-router (params.serviceId + query.org + a captured useRouter push spy), inert @/firebase, mocked @/stores/auth + @/stores/services, a mocked useSlideshowAssembly returning a fixed assembledSlideshow whose slides carry slotIndex, a stubbed SlideCanvas, and window.open stubbed to return null + getScreenDetails deleted so the on-mount output-open takes the harmless un-positioned path — the suite is about THIS view's control behavior, never Firestore and never a real window (R262-R266)."
    - "SINGLE-WRITER + seq (R266): the suite proves the view posts a `state` on mount (slide 0), posts on EVERY navigation with a STRICTLY INCREASING seq, and that onHello (a hello pushed into the fake channel) triggers a re-post of the current index with a higher seq — asserted against the fake channel's recorded messages exercising the REAL openRunChannel."
    - "RAIL (R262/R263): the active rail item (data-active) is the one whose slot index === current.slotIndex; clicking a has-slides rail item posts a `state` with the index from firstAssembledIndexBySlot; clicking an empty-slot row (rail-item-empty) posts nothing; and a service with zero assembled slides renders the 'Nothing to present yet' empty state."
    - "KEYBOARD (R265): a document keydown of ArrowRight and of Space each advance +1 slide; ArrowLeft goes -1; ArrowDown/ArrowUp move to the next/previous order item's first assembled slide (skipping an empty slot); each posts a `state`. Escape OPENS the exit-confirm dialog (run-exit-dialog appears) and does NOT close the channel or unmount; while the dialog is open a further ArrowRight posts NOTHING (keys inert); clicking the red run-exit-confirm closes the channel and calls router.push."
    - "PREVIEW (R264/R266): the current preview shows the slide at index and the next preview shows index+1; at the last slide the next pane shows 'End of service'; there is no push-to-live control (single-selection). The suite passes under bare `npx vitest run` with only the src/storage.rules.test.ts baseline failing."
  artifacts:
    - src/views/__tests__/RunControlView.test.ts
  key_links:
    - "The fake channel (createFakeChannel from the AudienceOutputView.test.ts idiom) records posted messages and can push { type:'hello' } / { type:'state' } into the view's real openRunChannel listener; assertions read posted state seqs for strict monotonicity."
    - "serviceStore.services is seeded with a service whose slots span indices 0/1/2 (slot 1 has no assembled slide) so sortedSlotsWithIndex + firstAssembledIndexBySlot exercise the has-slides / active / empty rail states; assembledSlideshow carries slides with slotIndex 0 and 2."
    - "useRouter is mocked with a captured push spy so the exit-confirm navigation is assertable; keydown is dispatched on document (the view binds its listener there)."
---

<objective>
Author the behavioral suite that proves RunControlView's operator-facing control contract: the single-
writer channel with a strictly increasing seq (the #1 phase risk), the you-are-here rail with click-to-
jump and empty-slot no-op, full keyboard navigation, and the Escape-opens-confirm (never teardown)
guarantee. This is the Nyquist coverage 95-03 deferred: the impl plans gated only on type-check + the
baseline suite; this plan is where the control behaviors are actually asserted against the real view.

Purpose: R262 (rail highlight), R263 (click-to-jump), R264 (current/next preview), R265 (keyboard nav +
Escape-with-confirmation), R266 (single-selection: every nav posts with a monotonic seq; no push step). The
harness reuses the proven AudienceOutputView.test.ts lineage (reactive router mock, injected in-memory
channel, mocked stores + assembly, stubbed SlideCanvas, enableAutoUnmount so onUnmounted channel-close
fires) and additionally neutralizes the on-mount output-open (stub window.open → null, delete
getScreenDetails) so the control assertions are isolated from the placement orchestration (which 95-06
covers).

Output: src/views/__tests__/RunControlView.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-run-control-screen/95-CONTEXT.md
@.planning/phases/95-run-control-screen/95-PATTERNS.md
@src/views/RunControlView.vue
@src/views/__tests__/AudienceOutputView.test.ts
@src/utils/runChannel.ts
@src/utils/serviceSlots.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author RunControlView.test.ts — channel/seq + rail + keyboard + preview + escape-confirm</name>
  <files>src/views/__tests__/RunControlView.test.ts</files>
  <read_first>
    - src/views/__tests__/AudienceOutputView.test.ts END TO END — the harness to clone: the reactive vue-router mock (:40-44), the vi.hoisted store/slide fixtures (:49-79), inert @/firebase (:81), mocked @/stores/auth + @/stores/services + useSlideshowAssembly (:83-103), the SlideCanvas stub (:108-130), createFakeChannel (:138-160), enableAutoUnmount (:33), and the beforeEach/afterEach capability install/reset (:176-209). RunControlView is the WRITER, so you assert what it POSTS (the inverse of the audience test's never-posts assertion), and you push hello/state INTO it.
    - src/views/RunControlView.vue — the exact data-testids and behavior to assert: rail-item (+ data-active), rail-item-empty, run-current-preview, run-next-preview, run-exit-btn, run-exit-dialog, run-exit-cancel, run-exit-confirm; the LIVE pill; the "End of service" copy; the "Nothing to present yet" empty state; postIndex/seq; onHello resend; the keydown map; the confirmExit path.
    - src/utils/runChannel.ts — the real openRunChannel the fake channel drives (postState verbatim, onState stale-drop by seq, onHello). Your fake channel's recorded `posted` array is the source of truth for seq monotonicity; pushing { type:'hello' } through the stored listener must reach the view's onHello.
    - src/utils/serviceSlots.ts — sortedSlotsWithIndex + firstAssembledIndexBySlot, so your fixtures produce a rail with a has-slides item, the active item, and an empty-slot item.
  </read_first>
  <action>
    Create src/views/__tests__/RunControlView.test.ts cloning the AudienceOutputView.test.ts harness, with
    these adaptations:
    - vue-router mock: reactive route with params.serviceId='service-1' + query.org='org-1'; useRouter
      returns a captured push spy (hoist a mockRouterPush = vi.fn() so the exit-confirm test can assert it);
      include RouterLink as a passthrough stub if RunControlView uses it.
    - @/stores/services mock: services seeded with ONE service { id:'service-1', slots:[{position:0,...},
      {position:1,...},{position:2,...}] } (three slots; give each whatever minimal ServiceSlot shape the
      type needs), plus orgId + subscribe + unsubscribeAll spies. useServiceAssembly is REAL (not mocked) —
      it reads these mocks and the mocked useSlideshowAssembly.
    - useSlideshowAssembly mock: returns { assembledSlideshow: ref([...]) } where the slides carry slotIndex
      — e.g. three slides with slotIndex 0, 0, 2 (so slot index 0 has assembled slides, slot 1 is EMPTY /
      absent from firstAssembledIndexBySlot, slot 2 has an assembled slide). Each slide's inner shape mirrors
      the audience test's fakeSlide (an id so the SlideCanvas stub can render which slide shows).
    - SlideCanvas stub: same lightweight stub rendering the slide id.
    - beforeEach: stub window.open via vi.spyOn(window,'open').mockReturnValue(null) and DELETE
      window.getScreenDetails (so the view's on-mount openOutputs takes the un-positioned/fallback path
      harmlessly and never throws); stub document.fonts if the view path needs it (it does not gate on
      fontReady, so likely unnecessary — include only if a mount warns).
    - A createFakeChannel identical to the audience test, exposing posted[], close, and helpers emitState(
      index,seq) and emitHello() (emitHello pushes { data:{ type:'hello' } } through the stored listener).

    WRITE these describe blocks / assertions:
    1. Single-writer + seq (R266): mount, flushPromises; assert posted contains a `state` with index 0 (the
       initial go-live). Dispatch ArrowRight (document keydown); assert a new `state` with index 1 and a seq
       strictly greater than the previous. Collect all posted state seqs across several navigations and
       assert they are strictly increasing.
    2. onHello resend (R266): after mounting and navigating to some index, call emitHello(); assert the view
       posts the CURRENT index again with a higher seq (so a late-joining output resyncs and the stale-drop
       accepts it).
    3. Rail highlight (R262): after the state settles on a slide with slotIndex 2, the rail item with
       data-active="true" corresponds to slot index 2; navigating back to slotIndex 0 moves the active flag.
    4. Click-to-jump (R263): clicking the has-slides rail item for slot index 2 posts a `state` whose index
       equals firstAssembledIndexBySlot(...).get(2); clicking rail-item-empty (slot index 1) posts NOTHING
       new.
    5. Keyboard (R265): ArrowRight and Space each post +1; ArrowLeft posts -1 (clamped at 0); ArrowDown moves
       to the next order item's first assembled slide (skipping the empty slot 1 → lands on slot 2's first
       index); ArrowUp moves back.
    6. Escape-confirm (R265): dispatch Escape → run-exit-dialog exists AND the channel is NOT closed and the
       component is still mounted (no teardown); while the dialog is open, dispatch ArrowRight → posted gains
       NO new state (keys inert); click run-exit-confirm → the fake channel close spy fired AND mockRouterPush
       was called (navigated back to the service editor).
    7. Preview (R264): the current preview shows the slide at the current index; the next preview shows
       index+1; drive to the last slide and assert the next pane shows "End of service"; assert there is no
       push-to-live control (no button/testid implying a separate go-live step).
    8. Empty state (R262): remount with useSlideshowAssembly returning an EMPTY assembledSlideshow and assert
       the rail shows the "Nothing to present yet" heading.

    Keep every assertion driven by the fake channel's recorded messages + the rendered DOM, never by a real
    BroadcastChannel or a real window. Use enableAutoUnmount(afterEach) so the view's onUnmounted channel
    close runs.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RunControlView.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - The suite mounts the real RunControlView with the injected fake channel + mocked stores/assembly +
      stubbed SlideCanvas + neutralized window.open/getScreenDetails, and covers: initial go-live post,
      strictly-increasing seq on every navigation, onHello resend, rail active-by-slotIndex + click-to-jump +
      empty-slot no-op + empty state, the full keyboard map, Escape-opens-confirm-not-teardown + keys-inert-
      while-open + confirm closes channel and navigates, and current/next preview incl. "End of service".
    - `npx vitest run src/views/__tests__/RunControlView.test.ts` passes; `npx vitest run` shows only the
      baseline (src/storage.rules.test.ts); `npm run type-check` is clean; no `--dir src`.
  </acceptance_criteria>
  <done>RunControlView's control contract is proven: monotonic single-writer seq, you-are-here rail with click-to-jump, full keyboard nav, and an Escape-guarded exit that never tears down on a stray key — the suite is green with no baseline regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> real openRunChannel | The suite drives the REAL channel through an injected in-memory BroadcastChannelLike, so seq monotonicity + onHello resend are proven against production stale-drop logic, not a bypass |
| on-mount output-open -> jsdom | window.open is stubbed to null and getScreenDetails deleted so the placement orchestration cannot open a real window or throw during control-behavior assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-14 | Tampering | seq regression escaping tests | high | mitigate | The suite asserts strict monotonicity across mount + every navigation + onHello resend against the fake channel's recorded messages, so any future edit that resets or fails to advance seq (which would let a reopened output be driven backward) fails here. |
| T-95-15 | Denial of Service | stray-Escape teardown regression | high | mitigate | The suite asserts Escape only opens the dialog (channel open, component mounted) and that nav keys are inert while it is open, so a regression that made Escape (or a key while open) tear down a live service fails here. |
| T-95-SC | Tampering | package installs (npm) | n/a | accept | No package-manager installs — a test file authored against existing in-repo modules; supply-chain checkpoint not applicable. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/RunControlView.test.ts` is green; `npx vitest run` shows only the
  src/storage.rules.test.ts baseline; `npm run type-check` (vue-tsc --build) is clean; no `--dir src`.
- The single-writer seq monotonicity + onHello resend, the rail highlight/click-jump/empty behaviors, the
  keyboard map, and the Escape-confirm-not-teardown are all asserted against the real view.
- HUMAN-UAT: none introduced by this test-only plan.
</verification>

<success_criteria>
- The RunControlView control contract (R262-R266) is covered by a passing behavioral suite that drives the
  real single-writer channel and asserts monotonic seq, rail navigation, keyboard nav, and the Escape-guarded
  exit — with no baseline regression.
</success_criteria>

<output>
Create `.planning/phases/95-run-control-screen/95-05-SUMMARY.md` when done.
</output>
</content>
