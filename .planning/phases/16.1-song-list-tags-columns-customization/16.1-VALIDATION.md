---
phase: 16.1
slug: song-list-tags-columns-customization
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-12
---

# Phase 16.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Requirement/traceability set is D-01..D-18 (see 16.1-CONTEXT.md — no formal REQ-IDs).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest ^4.0.18 + @vue/test-utils ^2.4.6 |
| **Config file** | `vitest.config.ts` (unit) |
| **Quick run command** | `npx vitest run <path-to-file>` |
| **Full suite command** | `npm run test:unit` |
| **Type gate** | `npm run type-check` (vue-tsc — surfaces every remaining `teamTags` ref as a compile error) |
| **Estimated runtime** | ~15–30 seconds (unit); type-check adds ~10–20s |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run <changed-test-file>`
- **After every plan wave:** Run `npm run test:unit` (full suite) + `npm run type-check`
- **Before `/gsd:verify-work`:** Full suite green AND `vue-tsc --build` clean
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| D-ID | Behavior | Test Type | Automated Command | File Exists | Status |
|------|----------|-----------|-------------------|-------------|--------|
| D-01 | `teamTags` values migrate into `tags`, de-duplicated; field removed from `Song` | unit | `npx vitest run src/stores/__tests__/songs.test.ts` | ✅ existing — add migration cases | ⬜ pending |
| D-03/D-04 | `rankSongsForSlot`: no hard team filter; team-name↔tag soft nudge | unit (TDD) | `npx vitest run src/utils/__tests__/suggestions.test.ts` | ✅ existing — ~10 hard-filter tests rewrite | ⬜ pending |
| D-05 | PC import maps team-style tags into `tags`, preserving user tags on re-import | unit (TDD) | `npx vitest run src/utils/__tests__/pcSongImport.test.ts` | ✅ existing — repoint `teamTags` blocks | ⬜ pending |
| D-06 | `team:` search prefix removed or aliased to plain tag match | unit | `npx vitest run src/utils/__tests__/songSearch.test.ts` | ✅ existing — direct test ~line 104 | ⬜ pending |
| D-08/D-09/D-10 | Column-visibility: default all-visible, persists per-user/org localStorage | unit | `npx vitest run src/stores/__tests__/songs.test.ts` | ⚠️ store logic ok; component test now created Wave 0 (plan 05 Task 1) | ⬜ pending |
| D-13 | Themes column inline-editable, same UX as Tags | component | `npx vitest run src/components/__tests__/SongTable.test.ts` | ✅ created Wave 0 — plan 05 Task 1 (TDD RED→GREEN) | ⬜ pending |
| D-14 | Import-preservation for edited themes (chosen removal-tracking option) | unit | `npx vitest run src/stores/__tests__/songs.test.ts` | ✅ existing `upsertSongs` block to extend | ⬜ pending |
| D-15/D-16 | `vwModeEnabled` church-level flag; VW indicators hidden app-wide when off (incl. the `type:` search prefix, gated in songSearch.ts + threaded from songs.ts/SongSlotPicker callers) | unit + component | `npx vitest run src/stores/__tests__/auth.test.ts` + `src/utils/__tests__/songSearch.test.ts` + `src/stores/__tests__/songs.test.ts` + surface component tests | ✅ auth.test.ts loadOrgContext coverage added Wave 0 (plan 02 Task 1); songSearch type: gate test (plan 03 Task 2); songs.ts/SongSlotPicker filteredSongs gate wiring (plan 07 Task 1 — type-check + plan 08 checkpoint; gate logic unit-tested in plan 03) | ⬜ pending |
| D-17/D-18 | Explainer content correct (1-2-3 only, not 1-5) | component/manual | manual UAT + SongTable.test.ts mount checks | ❌ static copy — primarily manual-only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `src/components/__tests__/SongTable.test.ts` — new component test covering column-visibility toggling, VW-off Category hiding, and the Tags/Themes pill split. **Created first in plan 05 Task 1 (TDD RED→GREEN), so Tasks 2-3 of plan 05 run against a real file** (resolves the prior ordering defect where Tasks 1-2 sampled a not-yet-created file). Closest precedent: `SongBadge.test.ts` prop-driven mount pattern.
- [x] `src/stores/__tests__/auth.test.ts` — coverage of `loadOrgContext` / org-doc field reads, specifically `vwModeEnabled` default-true-on-missing-field and the logout/no-org reset path. Added in plan 02 Task 1.
- [x] Run `vue-tsc --build` (`npm run type-check`) immediately after removing `Song.teamTags` from the type (plan 08 Task 1, before touching consumers) to get a compiler-generated exhaustive blast-radius list.

*Existing infrastructure (Vitest + @vue/test-utils) covers the remaining phase requirements — no framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| VW explainer copy reads correctly and teaches 1-2-3 (not 1-5) | D-17/D-18 | Static prose; correctness is editorial, not asserted logic | Open the explainer affordance in-app; confirm copy names 1 = Call to Worship, 2 = Intimate, 3 = Ascription and does NOT introduce a 1–5 scale |
| VW-off hides all indicators app-wide with no data loss | D-16 | Cross-surface visual gating (list, picker, editor, search, filters) | Toggle VW off in Settings; confirm Category column, VW badges, 1-2-3 slot typing, the VW-type filter dropdown, and rotation hints all hidden, AND a `type:1` search (Songs list + picker) returns no songs while off; toggle on → everything restored and `type:1` matches again |
| Settings-cog column show/hide affordance | D-08 | Visual/interaction affordance | Open cog on Songs panel; toggle each non-Title column; confirm Title never hideable and choices persist across reload |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (SongTable.test.ts created first in plan 05 Task 1; auth.test.ts coverage in plan 02 Task 1)
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-12
</content>
