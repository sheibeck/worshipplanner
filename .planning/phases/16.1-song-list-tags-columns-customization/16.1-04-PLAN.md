---
phase: 16.1-song-list-tags-columns-customization
plan: 04
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - src/stores/songs.ts
  - src/stores/__tests__/songs.test.ts
autonomous: true
requirements: [D-08, D-09, D-10]

must_haves:
  truths:
    - "Column visibility defaults to all columns visible (D-09)"
    - "Column-visibility choices persist per-user, per-org in localStorage and rehydrate on subscribe (D-10)"
    - "Title is not a toggleable column — only Category, Key, CCLI, Last Used, Tags, Themes are (D-08)"
  artifacts:
    - path: "src/stores/songs.ts"
      provides: "columnVisibility state, toggleColumn/resetColumns actions, per-user/org persist+hydrate mirroring the tagFilter trio"
      contains: "songTableColumns"
  key_links:
    - from: "src/stores/songs.ts columnStorageKey()"
      to: "localStorage"
      via: "wp:songTableColumns:v1:${org}:${uid} namespaced key"
      pattern: "songTableColumns"
    - from: "src/stores/songs.ts subscribe()"
      to: "hydrateColumnVisibility()"
      via: "hydrate call alongside hydrateTagFilter"
      pattern: "hydrateColumn"
---

<objective>
Add per-user, per-org column-visibility state to the songs store so the Songs panel cog (plan 05) has a persisted source of truth to bind to.

Purpose: D-08/D-09/D-10 introduce a personal (localStorage, NOT church-level) column-visibility preference. The exact per-user/org namespacing, persist, and hydrate pattern already exists in `songs.ts` as the `tagFilterStorageKey`/`persistTagFilter`/`hydrateTagFilter` trio (Phase 12 T-12-03) — copy it verbatim for a `wp:songTableColumns:v1:${org}:${uid}` key. Title is always visible and is therefore not part of the toggleable set.

Output: `columnVisibility` store state + persist/hydrate + toggle/reset actions with test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-CONTEXT.md
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-RESEARCH.md

<interfaces>
From src/stores/songs.ts (the template to copy):
  - tagFilterStorageKey() lines 110-116: builds `wp:tagFilter:v2:${org}:${uid}`, returns null when
    uid/org missing (never read/write under a shared key — T-12-03).
  - persistTagFilter()/hydrateTagFilter() lines 118-156: try/catch localStorage, reset-to-default on
    missing key / corrupt / private-mode.
  - watch([...], persistTagFilter, { deep: true }) line 158.
  - subscribe() calls hydrateTagFilter() at line 186 after org+uid resolve.
  - return object lines 319-341 exports store surface.
Note: the Songs-list `type:` search VW-gate (D-16) lives with the search callers in plan 07, not here —
  this plan is purely the column-visibility store surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add columnVisibility state + toggle/reset actions</name>
  <read_first>
    - src/stores/songs.ts (state refs lines 22-37; return block 319-341)
    - src/stores/__tests__/songs.test.ts (store setup/mount helper)
  </read_first>
  <behavior>
    - A fresh store exposes columnVisibility with keys category, key, ccli, lastUsed, tags, themes all === true (D-09 default all visible).
    - toggleColumn('tags') flips only that key.
    - resetColumns() sets every key back to true.
    - There is no 'title' key (Title is never toggleable, D-08).
  </behavior>
  <action>
    Add `const columnVisibility = ref<Record<string, boolean>>({ category: true, key: true, ccli: true, lastUsed: true, tags: true, themes: true })`. Add `toggleColumn(col: string)` (flips columnVisibility.value[col] via a new object assignment to keep reactivity) and `resetColumns()` (reassigns all-true). Export `columnVisibility`, `toggleColumn`, `resetColumns` from the return object. Do not include a 'title' key.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/songs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test asserts default columnVisibility has all six keys true and no 'title' key.
    - New test asserts toggleColumn flips a single key and resetColumns restores all-true.
    - `npx vitest run src/stores/__tests__/songs.test.ts` exits 0.
  </acceptance_criteria>
  <done>columnVisibility state + actions exist with default-all-visible coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist + hydrate column visibility per user/org</name>
  <read_first>
    - src/stores/songs.ts (tagFilterStorageKey/persistTagFilter/hydrateTagFilter trio lines 110-158; subscribe hydrate call line 186)
    - src/stores/__tests__/songs.test.ts (any existing localStorage persistence test for tagFilter to mirror)
  </read_first>
  <behavior>
    - Setting columnVisibility writes JSON under wp:songTableColumns:v1:${org}:${uid}.
    - hydrate reads that key and restores the saved map; missing/corrupt/no-key resets to all-true default.
    - No read/write occurs when uid or org is unavailable (no shared-key leakage, T-12-03).
  </behavior>
  <action>
    Add `columnStorageKey()` mirroring tagFilterStorageKey but returning `wp:songTableColumns:v1:${org}:${uid}` (null when uid/org missing). Add `persistColumnVisibility()` and `hydrateColumnVisibility()` mirroring the tagFilter persist/hydrate try/catch + reset-to-default semantics (default here is all-true, not empty sets). Add a `watch(columnVisibility, persistColumnVisibility, { deep: true })`. Call `hydrateColumnVisibility()` inside subscribe() right after the existing hydrateTagFilter() call. Merge hydrated keys over the default map so a newly-added column key defaults visible even if an older saved payload omits it.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/songs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test asserts persist writes the namespaced key and hydrate restores a saved map.
    - New test asserts corrupt/missing payload resets to all-true.
    - subscribe() calls hydrateColumnVisibility().
    - `npx vitest run src/stores/__tests__/songs.test.ts` exits 0 and `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Column visibility persists per user/org and rehydrates on subscribe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store ↔ browser localStorage | per-user/org view preference, client-owned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-07 | Tampering | localStorage column prefs | accept | Tampering only changes which columns THIS user sees; cosmetic, no data or auth impact. |
| T-16.1-08 | Information Disclosure | shared-browser key leakage | mitigate | Key is namespaced per uid+org and returns null when either is missing (reuses T-12-03 guard) — no cross-account bleed. |

Severity LOW: personal view preference in localStorage; never touches Firestore.
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/songs.test.ts` exits 0.
- `npm run type-check` exits 0.
</verification>

<success_criteria>
- Column visibility defaults all-visible, persists per user/org, and rehydrates — ready for the cog UI in plan 05.
</success_criteria>

<output>
Create `.planning/phases/16.1-song-list-tags-columns-customization/16.1-04-SUMMARY.md` when done.
</output>
</content>
