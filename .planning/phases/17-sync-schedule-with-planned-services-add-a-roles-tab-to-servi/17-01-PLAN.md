---
phase: 17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/service.ts
  - src/utils/serviceRoles.ts
  - src/utils/__tests__/serviceRoles.test.ts
autonomous: true
requirements: [CR-02, CR-03]
must_haves:
  truths:
    - "resolveServiceRoleAssignments returns, for each role sorted by order, the quarter-scheduled people for the service date when no override is present"
    - "an un-overridden role's effectivePersonIds equals quarter.calendar[date][roleId] (round-trip invariant)"
    - "a role with an override present resolves effectivePersonIds to the override, ignoring the schedule"
    - "an override set to an empty array resolves to nobody serving (explicit clear), distinct from an absent override which inherits the schedule"
    - "when no quarter covers the service date, every role resolves with empty scheduledPersonIds and no thrown error"
  artifacts:
    - src/types/service.ts
    - src/utils/serviceRoles.ts
    - src/utils/__tests__/serviceRoles.test.ts
  key_links:
    - "resolveServiceRoleAssignments reads quarter.calendar[service.date][roleId] via findQuarterForDate(quarters, service.date)"
---

<objective>
Establish the pure seeding-join foundation for Phase 17: promote the Service document to carry people-per-role via one optional sparse override field, and add the pure resolver that computes each role's effective assignment (override ?? quarter-scheduled) for a service date.

Purpose: Every other plan in this phase (store writes, share snapshot, Roles tab UI, public share view) consumes `resolveServiceRoleAssignments`. Building it first, test-driven and store-free, gives every downstream plan a stable, verified contract.
Output: `Service.roleAssignmentOverrides` field, `src/utils/serviceRoles.ts` (pure functions + `ResolvedRoleAssignment` type), and a full Vitest suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-RESEARCH.md
@.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md
@src/types/service.ts
@src/types/roster.ts
</context>

## Artifacts this phase produces (Phase 17 cumulative — this plan builds the first three)

- **This plan:** `Service.roleAssignmentOverrides?: Record<string, string[]>` (type field); `src/utils/serviceRoles.ts` exporting `ResolvedRoleAssignment`, `findQuarterForDate`, `resolveServiceRoleAssignments`; `src/utils/__tests__/serviceRoles.test.ts`.
- Later plans: `services.ts` `setRoleOverride`/`clearRoleOverride` + extended `createShareToken`; `serviceShares` collection + Firestore rule; `/:slug/service-:date` route; `ServiceEditorView.vue` Roles tab; `ShareView.vue` Who's Serving section; `RESERVED_SLUGS += 'service-share'`; `src/rules.test.ts` serviceShares block.

<assumption_delta_decision>
Primary noun: the planned Service (`organizations/{orgId}/services/{id}`).
Transition: singular→plural — the Service promotes from "music only" to "music + people-per-role".
Decision: PROMOTE — add exactly one optional sparse field `roleAssignmentOverrides?: Record<roleId, personId[]>` onto the Service document. Per-role people are resolved LIVE at read/share time as `override ?? quarterScheduled ?? []`, NEVER snapshotted onto the Service.
Rationale: mirrors the existing `Quarter.roleOverridesByDate` sparse-override precedent; zero migration (new field is optional, absent on every existing doc); the Quarter/schedule remains the unmerged source of truth (ADD-ALONGSIDE at the collection level — Service and Quarter documents are NOT merged, per 17-RESEARCH.md §4b).
Invariant test (authored in this plan): an un-overridden role always round-trips to the quarter-scheduled person for that date — `resolveServiceRoleAssignments(...).effectivePersonIds === quarter.calendar[date][roleId]` when no override key is present.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the roleAssignmentOverrides sparse field to the Service type</name>
  <read_first>
    - src/types/service.ts (the Service interface, lines ~50-67, and ServiceInput at ~67)
    - src/types/roster.ts (Quarter.roleOverridesByDate — the sparse-override precedent whose SHAPE this mirrors)
    - .planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md (section "src/types/service.ts (model)")
  </read_first>
  <files>src/types/service.ts</files>
  <behavior>
    - The Service interface gains an optional field `roleAssignmentOverrides?: Record<string, string[]>` (roleId -> personId[]).
    - Absent key/field = inherit from schedule; present key = override for that role.
    - No other Service field changes; `ServiceInput` (Omit of id/createdAt/updatedAt) needs no edit since the field is optional.
  </behavior>
  <action>
    Add a single optional field to the `Service` interface in src/types/service.ts: `roleAssignmentOverrides?: Record<string, string[]>` keyed roleId -> personId[]. Add a short doc comment describing the semantics by CONCEPT (absent key inherits the schedule; a present key overrides that one role), and note it mirrors the sparse-override-map precedent on the Quarter type. Do NOT restate any grep-target literal from acceptance criteria in the comment. Leave `ServiceInput` unchanged (optional field is compatible). This implements CR-03 (per-service override storage) at the type layer per D-derived research decision (sparse override on the Service doc, resolved live).
  </action>
  <verify>
    <automated>npx vue-tsc --build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleAssignmentOverrides" src/types/service.ts` shows the field declared as optional (`?:`) with type `Record<string, string[]>`.
    - `npx vue-tsc --build` completes with no type errors.
    - The field is inside the exported `Service` interface (not a separate type).
  </acceptance_criteria>
  <done>Service carries an optional sparse roleAssignmentOverrides map; project type-checks green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Create the pure serviceRoles resolver (RED→GREEN→REFACTOR)</name>
  <read_first>
    - src/stores/quarters.ts (buildResolveRolesForDate, ~lines 281-290 — the "override ?? computedDefault" + sort-by-order shape to copy)
    - src/components/QuarterGrid.vue (cellPeople / effectiveCountFor, ~lines 282-296 — the calendar[date][roleId] read shape)
    - src/types/roster.ts (Role, Person, Quarter, QuarterCalendar, RoleGroup)
    - src/types/service.ts (Service, incl. the field added in Task 1)
    - src/stores/__tests__/quarters.test.ts (fixture/object-literal conventions to mirror — Quarter/Role fixtures)
    - .planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md (section "src/utils/serviceRoles.ts (new utility)" — the exact recommended signatures)
  </read_first>
  <files>src/utils/serviceRoles.ts, src/utils/__tests__/serviceRoles.test.ts</files>
  <behavior>
    RED first — author src/utils/__tests__/serviceRoles.test.ts with inline Quarter/Role/Service fixtures (no Firebase/Pinia mocks; these are pure functions) covering:
    - Test A (seed): a role with a quarter calendar entry for the service date and NO override resolves effectivePersonIds to the quarter-scheduled ids (round-trip invariant, CR-02).
    - Test B (override wins): a role with roleAssignmentOverrides[roleId] present resolves effectivePersonIds to the override, ignoring the schedule (CR-03).
    - Test C (explicit clear vs inherit): an override of `[]` resolves effectivePersonIds to `[]` (nobody serving), while an ABSENT override for a scheduled role inherits the scheduled ids — the two cases are distinct.
    - Test D (no quarter): when no quarter's serviceDates includes service.date, every returned role has scheduledPersonIds `[]`, overriddenPersonIds null unless overridden, and no throw.
    - Test E (ordering): returned array is sorted by role.order ascending.
    - Test F (findQuarterForDate): returns the quarter whose serviceDates includes the date, undefined when none match.
    Run the suite and confirm it FAILS (module not yet implemented). Then implement to GREEN.
  </behavior>
  <action>
    Create src/utils/serviceRoles.ts as PURE functions (no store/Pinia/Firebase imports — import only TYPES from src/types/service.ts and src/types/roster.ts), following the same "pure function in utils/" convention as src/utils/slug.ts and src/utils/quarterDates.ts. Export:
    (1) interface `ResolvedRoleAssignment` with fields roleId, roleName, group (RoleGroup), scheduledPersonIds (string[]), overriddenPersonIds (string[] | null), effectivePersonIds (string[]).
    (2) `findQuarterForDate(quarters: Quarter[], date: string): Quarter | undefined` — returns the first quarter whose serviceDates includes date.
    (3) `resolveServiceRoleAssignments(service: Service, quarters: Quarter[], roles: Role[]): ResolvedRoleAssignment[]` — find the quarter for service.date, read scheduleForDate = quarter?.calendar[service.date] ?? {}, then map roles sorted by order to: scheduledPersonIds = scheduleForDate[role.id] ?? []; overriddenPersonIds = service.roleAssignmentOverrides?.[role.id] ?? null; effectivePersonIds = overriddenPersonIds ?? scheduledPersonIds. The `?? null` on overriddenPersonIds is what makes an override of `[]` distinct from an absent override (Test C). Match the signatures in 17-PATTERNS.md exactly. First write the failing test file, confirm red, then implement to green; refactor only for clarity while keeping green. Document the first-match tie-break behavior of findQuarterForDate in a short comment (accepted assumption — see planner assumption note in this plan's verification).
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/serviceRoles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/serviceRoles.test.ts` passes with tests A–F present and green.
    - `grep -n "export function resolveServiceRoleAssignments" src/utils/serviceRoles.ts` and `grep -n "export function findQuarterForDate" src/utils/serviceRoles.ts` both match.
    - `grep -c "import.*stores" src/utils/serviceRoles.ts` returns 0 (no store imports — proves purity). Filter comments first: `grep -v '^\s*//' src/utils/serviceRoles.ts | grep -c "stores/"` returns 0.
    - Test C asserts BOTH: an override of `[]` → effectivePersonIds length 0, AND an absent override on a scheduled role → effectivePersonIds equals the scheduled ids.
    - `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>The pure resolver exists, is store-free, and its suite (seed / override-wins / clear-vs-inherit / no-quarter / ordering / findQuarterForDate) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Pure functions operate on already-authorized, in-memory store data. No I/O, no network, no persistence, no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01-01 | Information Disclosure | resolveServiceRoleAssignments output | low | mitigate | Resolver returns only role/personId strings already present in authorized store data; it must NOT reach into Person to surface email/phone. Downstream name-resolution (17-03) maps ids→names; this function stays id-only. |
| T-17-01-02 | Tampering | findQuarterForDate tie-break | low | accept | Two quarters listing the same date is unenforced in the data model (research Open Q1). First-match is deterministic and documented; accepted as pre-existing behavior, not a regression this phase introduces. |
| T-17-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (17-RESEARCH.md: zero new dependencies). No supply-chain surface. |
</threat_model>

<verification>
- `npx vue-tsc --build` green.
- `npx vitest run src/utils/__tests__/serviceRoles.test.ts` green.
- Planner assumption (surfaced, not silently dropped): when two quarters both list the same service date, `findQuarterForDate` returns the first match in array order. Research Open Question 1 flags this as unenforced in the data model; this phase ACCEPTS first-match-wins as pre-existing behavior and does not add a cross-quarter uniqueness guard. If the team wants a deterministic finalized-first tie-break, that is a follow-up.
</verification>

<success_criteria>
- Service type has the optional sparse override field; project type-checks.
- The pure resolver and its full Vitest suite are green, with the round-trip invariant (un-overridden role === scheduled) explicitly asserted.
</success_criteria>

<output>
Create `.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-01-SUMMARY.md` when done.
</output>
