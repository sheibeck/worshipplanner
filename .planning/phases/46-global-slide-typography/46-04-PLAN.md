---
phase: 46-global-slide-typography
plan: 04
type: execute
wave: 3
depends_on: ["46-02"]
files_modified:
  - src/components/PresentationViewer.vue
  - src/components/slides/SlideGrid.vue
  - src/components/slides/SlideCard.vue
  - src/components/slides/EditSlideDrawer.vue
  - src/components/__tests__/PresentationViewer.test.ts
  - src/components/slides/__tests__/SlideCard.test.ts
autonomous: true
requirements: [R093, R094]
user_setup: []

must_haves:
  truths:
    - "The slide grid, the Edit Slide drawer preview, and the presenter view all render slide text in the church's chosen family/weight/size; the printed Order of Service (ServicePrintLayout.vue) is explicitly unaffected (R093 success criterion 2)."
    - "A church on Medium scale (the default) sees zero visual change to the grid or drawer today — md maps to the identity multiplier 1.0."
    - "The Slides-grid card body (line-clamp-6) and the drawer preview (scrollable region) already truncate independent of font metrics, so Large scale shows fewer lines before the existing clamp/scroll takes over — not a new failure mode."
    - { statement: "The presenter's slide canvas never renders before the font-load promise resolves — first paint stays on the existing 'Loading slideshow…' state until document.fonts.ready AND document.fonts.load('<weight> 1em \"<family>\"') for the chosen face settle (R094), proven by an automated test asserting the canvas is absent until the mocked promise resolves", verification: backstop }
    - { statement: "The presenter proceeds to render after FONT_LOAD_TIMEOUT_MS (3000ms) even if the font-load promise never resolves — a stalled font degrades to one possible fallback frame, never a stuck black loading screen (UI-SPEC unresolved item #1, resolved via bounded timeout), proven by a fake-timer test", verification: backstop }
  artifacts:
    - "CSS-variable wrappers (inline :style from cssVarsFor) on PresentationViewer's root, the SlideGrid container, and the EditSlideDrawer preview box"
    - "Scoped per-element font-size calc rules (base rem * var(--slide-font-scale)) + font-weight var on the primary slide text elements in each render site"
    - "presenter font-load gate in PresentationViewer.vue: a fontReady ref, an onMounted waitForSlideFont call, on-demand loadFontCss for a non-default chosen face, and first-paint gating that reuses the 'Loading slideshow…' state"
    - "font-gate test cases in PresentationViewer.test.ts (document.fonts stub; gate holds until resolve; proceeds after fake-timer timeout) and a CSS-var-presence case in SlideCard.test.ts"
  key_links:
    - "All three render sites read authStore.settings.slideTypography via cssVarsFor (46-02) — the single write point is the Settings card (46-03); render sites are pure readers"
    - "The presenter gate calls waitForSlideFont + FONT_LOAD_TIMEOUT_MS + loadFontCss (46-02); font-family/weight inherit from the wrapper, scale multiplies each element's existing fixed Tailwind size"
    - "ServicePrintLayout.vue is deliberately NOT wired to these variables — owner-locked exclusion"
---

<objective>
Make the church's chosen typography actually show up on all three slide surfaces, and gate the presenter's first paint on the chosen font being resident so a projector never flashes a fallback mid-service (R094).

Purpose: This is where R093 ("applies to every slide") and R094 ("no font flash before first paint") become observable. The render sites are pure readers of `authStore.settings.slideTypography` through the shared `cssVarsFor` — one write point (the Settings card), three readers. The presenter's hard gate closes the flash; its bounded timeout closes the UI-SPEC's unresolved failure path. The printed Order of Service is deliberately left untouched.

Output: CSS-variable wrappers on the three render sites + the presenter font-load gate + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-global-slide-typography/46-CONTEXT.md
@.planning/phases/46-global-slide-typography/46-UI-SPEC.md
@.planning/phases/46-global-slide-typography/46-RESEARCH.md
@.planning/phases/46-global-slide-typography/46-02-SUMMARY.md

@src/components/PresentationViewer.vue
@src/components/slides/SlideGrid.vue
@src/components/slides/SlideCard.vue
@src/components/slides/EditSlideDrawer.vue
@src/utils/slideTypography.ts
@src/config/slideFonts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the slide-font CSS variables to the three render sites</name>
  <files>src/components/PresentationViewer.vue, src/components/slides/SlideGrid.vue, src/components/slides/SlideCard.vue, src/components/slides/EditSlideDrawer.vue</files>
  <read_first>
    - src/components/PresentationViewer.vue — the root `viewerRoot` div (lines 3–13, `data-testid="presentation-viewer"`) and every slide text element with its current fixed Tailwind size/weight class: `presentation-body` (text-5xl font-normal, and a text-6xl font-semibold variant), `presentation-scripture-reference` (~line 157), `presentation-speaker` (~line 192, text-5xl font-normal), `presentation-congregational-section` (~line 199), `presentation-copyright-fine-print` (~line 138), and the text-2xl font-semibold label (~line 134)
    - src/components/slides/SlideGrid.vue — the container root (line 2, `data-testid="slide-grid"`)
    - src/components/slides/SlideCard.vue — the body preview (`data-testid="slide-card-body"`, `text-[13px] leading-normal`, line-clamp-6)
    - src/components/slides/EditSlideDrawer.vue — the preview box (`data-testid="drawer-preview"`, line ~84–108) and its `drawer-preview-text` (`text-[13px]`)
    - .planning/phases/46-global-slide-typography/46-UI-SPEC.md § Typography (B) — the base-size table; Tailwind base rems: text-6xl=3.75rem, text-5xl=3rem, text-2xl=1.5rem, text-xs=0.75rem, text-[13px]=13px
    - src/utils/slideTypography.ts (cssVarsFor)
  </read_first>
  <action>
    Import the auth store into each of the three render surfaces (PresentationViewer, SlideGrid, EditSlideDrawer) and bind `cssVarsFor(authStore.settings.slideTypography)` as an inline `:style` on ONE wrapper per site: PresentationViewer's `viewerRoot`; the SlideGrid `slide-grid` container; the EditSlideDrawer `drawer-preview` box (the preview only — NOT the whole drawer chrome). On each wrapper also set `font-family: var(--slide-font-family)` so family inherits to all descendant slide text (family is an inherited CSS property; no descendant declares its own font-family today). In a component-scoped `<style>` block in each component (NOT main.css, to avoid cross-plan file contention), add rules — which win over Tailwind's layered utilities because scoped styles are unlayered — that: (a) set `font-weight: var(--slide-font-weight)` on the PRIMARY slide text elements that are `font-normal` today (`presentation-body`, `presentation-scripture-reference`, `presentation-speaker`, `presentation-congregational-section`, `slide-card-body`, `drawer-preview-text`), leaving the deliberate `font-semibold` hierarchy elements (the copyright title/label) at their existing weight; and (b) override `font-size` to `calc(<that element's existing base rem> * var(--slide-font-scale))` for each slide text element, reading each element's actual current size class to get the base value (e.g. text-5xl -> 3rem, text-6xl -> 3.75rem, text-[13px] -> 13px). Do NOT alter the existing size/weight utility classes in the template — the scoped rules layer over them. Do NOT touch `ServicePrintLayout.vue` (owner-locked exclusion). Non-presenter surfaces need no extra load gating — @fontsource CSS already ships `font-display: swap`.
  </action>
  <acceptance_criteria>
    - Each of the three wrappers binds `cssVarsFor(authStore.settings.slideTypography)` (the three `--slide-font-*` custom properties appear in the rendered inline style; asserted by test for SlideCard/grid at minimum).
    - With the default settings (Inter/400/md), `--slide-font-scale` resolves to 1 so no size change occurs (identity).
    - The scoped rules apply `font-weight: var(--slide-font-weight)` to the primary body/scripture/speaker/congregational text and `font-size: calc(... * var(--slide-font-scale))` to each slide text element; the existing template size/weight utility classes are unchanged.
    - `ServicePrintLayout.vue` is not in the diff.
    - `npm run type-check` clean; `npx vitest run src/components/slides/__tests__/SlideCard.test.ts src/components/slides/__tests__/SlideGrid.test.ts src/components/slides/__tests__/EditSlideDrawer.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideCard.test.ts src/components/slides/__tests__/SlideGrid.test.ts src/components/slides/__tests__/EditSlideDrawer.test.ts</automated>
  </verify>
  <done>All three slide surfaces consume the CSS variables (family/weight inherit, scale multiplies each fixed size); print surface untouched; existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Presenter first-paint font gate (R094) with bounded timeout</name>
  <files>src/components/PresentationViewer.vue</files>
  <behavior>
    - The presenter shows the existing "Loading slideshow…" state and does NOT render the slide canvas until fontReady becomes true, even when slides are already present.
    - fontReady flips true once waitForSlideFont resolves ready (document.fonts.ready + document.fonts.load for the chosen face both settle).
    - fontReady flips true after FONT_LOAD_TIMEOUT_MS (3000ms) even if the font-load promise never resolves (bounded degradation — render anyway, never a stuck screen).
    - If the org's chosen family/weight differs from the eager default (Inter 400), the presenter first on-demand loads that face via loadFontCss before/alongside the waitForSlideFont call.
  </behavior>
  <read_first>
    - src/components/PresentationViewer.vue — `isLoadingState`/`isEmptyState` computeds (~lines 441–442), the loading-state template block (lines 40–55, "Loading slideshow…"), the slide-canvas `v-else-if="currentSlide"` (line ~68), and `onMounted` (~lines 841–848)
    - src/utils/slideTypography.ts (waitForSlideFont, FONT_LOAD_TIMEOUT_MS, loadFontCss, snapWeight)
    - src/components/__tests__/PresentationViewer.test.ts — the `beforeEach` fullscreen stub shape (~lines 220–228) to replicate for `document.fonts`
    - .planning/phases/46-global-slide-typography/46-RESEARCH.md § Pattern 2, § Pitfalls 1–2 (fonts.ready alone is insufficient; jsdom has no document.fonts)
    - .planning/phases/46-global-slide-typography/46-UI-SPEC.md § Copywriting Contract (reuse "Loading slideshow…" — do NOT add a second loading string)
  </read_first>
  <action>
    Add a `fontReady` ref (initial `false`) to PresentationViewer. In `onMounted`, read `authStore.settings.slideTypography`; compute the chosen family and `snapWeight(family, weight)`; if that pair differs from the eager default (Inter/400), call `loadFontCss[family]?.(weight)` to fetch the face on demand; then `await waitForSlideFont(family, weight, FONT_LOAD_TIMEOUT_MS)` and set `fontReady = true` regardless of ready-vs-timeout (bounded degradation — never leave it false). Gate first paint so the slide canvas is not shown until `fontReady` is true: extend the loading condition so the existing "Loading slideshow…" block shows while `!fontReady` (in addition to its current assembly-in-flight condition) and the `v-else-if="currentSlide"` canvas only renders once `fontReady` is true. Reuse the existing "Loading slideshow…" markup — do not introduce a second loading string or a distinct timeout indicator (RESEARCH Open Question 2: silently proceed, matching the component's other degraded-state precedents). Keep the exit affordance reachable during the gate (the existing `exitVisible` override already covers the loading state). Set the `viewerRoot` wrapper's `:style` (from Task 1) so the chosen family is applied to the canvas the moment it paints.
  </action>
  <acceptance_criteria>
    - With `document.fonts` stubbed and its `load` promise pending, mounting the presenter with slides present keeps `presentation-loading` shown and `presentation-slide` absent.
    - Resolving the stubbed `load`/`ready` promises flips to the slide canvas (no flash).
    - With the load promise never resolving, advancing fake timers by FONT_LOAD_TIMEOUT_MS flips `fontReady` true and renders the canvas.
    - A non-default chosen family triggers `loadFontCss` for that family (asserted via mock).
    - `npm run type-check` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/PresentationViewer.test.ts -t "font"</automated>
  </verify>
  <done>The presenter holds first paint on the chosen face (fonts.ready + explicit load), proceeds after a bounded 3000ms timeout, and on-demand loads a non-default face — reusing the existing loading state.</done>
</task>

<task type="auto">
  <name>Task 3: Presenter font-gate tests + render-site CSS-variable assertions</name>
  <files>src/components/__tests__/PresentationViewer.test.ts, src/components/slides/__tests__/SlideCard.test.ts</files>
  <read_first>
    - src/components/__tests__/PresentationViewer.test.ts — existing mount helpers, the fullscreen `Object.defineProperty` stub, and how slides/isLoading props are passed
    - src/components/slides/__tests__/SlideCard.test.ts — existing mount + auth-store stubbing pattern
    - src/utils/slideTypography.ts (cssVarsFor output keys)
  </read_first>
  <action>
    In PresentationViewer.test.ts, add a `document.fonts` stub in `beforeEach` using the exact `Object.defineProperty(document, 'fonts', { value: { ready: <controllable Promise>, load: vi.fn()... }, configurable: true, writable: true })` shape already used for `fullscreenElement`. Add cases (matchable by `-t "font"`): (1) canvas stays hidden / `presentation-loading` shown while the font-load promise is pending though slides are present; (2) canvas appears after the promise resolves; (3) with `vi.useFakeTimers()`, canvas appears after `vi.advanceTimersByTime(FONT_LOAD_TIMEOUT_MS)` when the promise never resolves; (4) a non-default `authStore.settings.slideTypography.fontFamily` causes `loadFontCss` to be invoked for that family. In SlideCard.test.ts, add a case asserting the rendered wrapper carries the `--slide-font-family`/`--slide-font-weight`/`--slide-font-scale` custom properties derived from `cssVarsFor` (default settings -> scale 1). Keep all assertions on DOM/inline-style/mock-calls — never on real font rendering (jsdom cannot).
  </action>
  <acceptance_criteria>
    - The four PresentationViewer font-gate cases pass under `npx vitest run src/components/__tests__/PresentationViewer.test.ts -t "font"`.
    - The SlideCard CSS-variable case passes.
    - Full app suite shows no new failing file beyond the documented 2-file baseline (`src/storage.rules.test.ts`, `RosterView.test.ts`).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/PresentationViewer.test.ts src/components/slides/__tests__/SlideCard.test.ts</automated>
  </verify>
  <done>The R094 gate (hold-until-ready + timeout-proceed + on-demand load) and the render-site CSS-variable wiring are proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore settings → DOM/CSS | `slideTypography` is read into a `font-family` CSS variable and a `document.fonts.load()` template string at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-02 | Tampering | presenter gate + render-site wrappers reading `slideTypography` | medium | mitigate | The font-family stack and the `document.fonts.load('<weight> 1em "<family>"')` string are built by `cssVarsFor`/`snapWeight` (46-02), which fall back to Inter/400/md for any value outside the curated `SLIDE_FONTS` set — a manually-tampered Firestore field can never inject free text into CSS or the load call (ASVS V5). |
| T-46-04 | Denial of Service | presenter first-paint gate | low | mitigate | `waitForSlideFont`'s `Promise.race` against `FONT_LOAD_TIMEOUT_MS` guarantees `fontReady` always settles, so a missing/stalled font asset can never hang the presenter on "Loading slideshow…". |
</threat_model>

<verification>
- `npm run type-check` clean (vue-tsc --build, per CLAUDE.md).
- `npx vitest run src/components/__tests__/PresentationViewer.test.ts src/components/slides/__tests__/SlideCard.test.ts src/components/slides/__tests__/SlideGrid.test.ts src/components/slides/__tests__/EditSlideDrawer.test.ts` green.
- Full app suite at the documented 2-file baseline: `npx vitest run`.
- `ServicePrintLayout.vue` absent from the diff.

## Manual-only verification (deferred under the v1.5 standing autonomy grant — record in PENDING-VERIFICATION.md § Phase 46)

| Behavior | Requirement | Why manual | Instruction |
|----------|-------------|------------|-------------|
| No fallback-font flash mid-service | R094 | jsdom cannot render real fonts or measure a real paint | On a real projector, present a service and confirm the chosen font is resident on the first slide — no visible swap from a fallback |
| Projection legibility of each curated family/weight/size | R093 | legibility at projection distance is a human visual judgment | Present with each curated family at each size on a real projector; confirm readability |
| Long-line overflow at Large (1.25) scale | R093 (UI-SPEC unresolved item #2) | overflow is a projector-visible layout effect; no auto-fit is in scope (REQUIREMENTS.md Out of Scope: no line-height/alignment/safe-area) | On a real projector at Large scale, present an already-long lyric/scripture line and confirm the overflow is acceptable; if it bites, revisit auto-fit in a later phase. **Planner assumption:** no shrink-to-fit is built this phase — Large scale can overflow a long line, same as the fixed base sizes can today. |
</verification>

<success_criteria>
- The grid, drawer preview, and presenter all render the chosen family/weight/size; the printed Order of Service is unaffected (R093 criterion 2).
- The presenter never paints the slide canvas before the chosen face is resident, and always proceeds within the bounded timeout (R094; UI-SPEC unresolved item #1 closed).
- Default (Medium) scale is a visual no-op.
</success_criteria>

<output>
Create `.planning/phases/46-global-slide-typography/46-04-SUMMARY.md` when done, and append the three manual-only items to `.planning/PENDING-VERIFICATION.md` § Phase 46.
</output>
