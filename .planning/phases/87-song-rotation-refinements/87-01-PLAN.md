---
phase: 87-song-rotation-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SongSlideOver.vue
  - src/components/__tests__/SongSlideOver.test.ts
  - src/components/ScriptureRotationTable.vue
  - src/components/__tests__/ScriptureRotationTable.test.ts
  - src/components/__tests__/RolesConfigPanel.test.ts
autonomous: true
requirements: [R249, R253, R256]

must_haves:
  truths:
    - "R249: Editing the Key field in the song drawer and saving persists the new key onto the resolved primary/first arrangement via the existing updateSong path."
    - "R249: A song with zero arrangements does not crash when the Key field is edited; a default arrangement is created to hold the value."
    - "R253: The Scripture rotation tab lists only SCRIPTURE slots; a service's sermonPassage never appears as a rotation row."
    - "R253: The empty-state copy no longer mentions a sermon passage."
    - "R256: No .vue UI surface describes the schedulable-roles default count with the deprecated soft-target phrasing; the copy states the scheduler auto-fills the count each service."
  artifacts:
    - "src/components/SongSlideOver.vue (always-visible editable Key input bound to the primary/first arrangement)"
    - "src/components/ScriptureRotationTable.vue (sermon-passage contribution removed; empty-state copy fixed)"
    - "src/components/__tests__/ScriptureRotationTable.test.ts (new regression test — sermon excluded, scripture included)"
    - "src/components/__tests__/SongSlideOver.test.ts (Key-persists-to-primary-arrangement test)"
    - "src/components/__tests__/RolesConfigPanel.test.ts (kept/strengthened negative-assertion test)"
  key_links:
    - "SongSlideOver Key input <-> resolved primary arrangement in form.arrangements <-> songStore.updateSong / addSong"
    - "ScriptureRotationTable.rotationEntries <-> service.slots (SCRIPTURE only), fully decoupled from service.sermonPassage"
---

<objective>
Three small, independent client-only refinements bundled into one plan (coarse granularity — no shared files):

- R249: Add an always-visible editable "Key" text input in the song edit drawer, bound to the primary/first arrangement's key, persisting through the existing save/upsert path.
- R253: Exclude the sermon passage from the Scripture rotation tab so only SCRIPTURE slots feed the rotation, and fix the now-inaccurate empty-state copy.
- R256: Verify (and correct only if a straggler exists) that no UI surface still calls the schedulable-roles default count a "soft planning target"; keep/strengthen the negative-assertion test.

Purpose: Close the last three v2.x refinement requirements — a planner-facing key edit, an accurate rotation view, and honest roles copy.
Output: Edited SongSlideOver.vue + ScriptureRotationTable.vue, a new ScriptureRotationTable.test.ts, and strengthened SongSlideOver / RolesConfigPanel tests.
Deploy note: Client-only. No Firestore rules, Storage rules, or Cloud Functions changes — nothing to deploy beyond the normal frontend build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-song-rotation-refinements/87-CONTEXT.md

@src/types/song.ts
@src/types/service.ts
@src/components/SongSlideOver.vue
@src/components/ScriptureRotationTable.vue
@src/components/RolesConfigPanel.vue
</context>

<artifacts_this_phase_produces>
- **src/components/SongSlideOver.vue** — new always-visible "Key" text input + a `primaryArrangementKey` get/set computed that reads and writes the resolved primary/first arrangement's key (R249).
- **src/components/__tests__/SongSlideOver.test.ts** — new test proving a Key edit persists onto the resolved primary arrangement through `updateSong`, including the zero-arrangement edge (R249).
- **src/components/ScriptureRotationTable.vue** — sermon-passage contribution block removed; empty-state copy corrected; unused `ScriptureRef` import dropped (R253).
- **src/components/__tests__/ScriptureRotationTable.test.ts** — NEW file (does not exist yet); regression test proving `sermonPassage` is excluded while SCRIPTURE slots appear (R253).
- **src/components/__tests__/RolesConfigPanel.test.ts** — kept/strengthened negative-assertion + accurate-phrasing test (R256).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (R249): Add an always-visible editable song Key bound to the primary/first arrangement</name>
  <files>src/components/SongSlideOver.vue, src/components/__tests__/SongSlideOver.test.ts</files>
  <read_first>
    - src/types/song.ts — `Arrangement.key` (line 15), `Song.primaryArrangementId` (line 33); there is NO top-level `Song.key`.
    - src/components/SongSlideOver.vue — the Details-tab template (~180-234, esp. the "Primary key" select at 217-234), `FormState`/`emptyForm`/`songToForm` (~319-360), `form` ref (362), and `onSave` (~479-538) which already resolves `primaryArrangementId` with a first-arrangement fallback (505-509).
    - src/components/__tests__/SongSlideOver.test.ts — existing `makeSong` factory (36-56), `mountDrawer` helper (61-72), and the "— save" describe block (74-149) as the pattern to copy.
    - src/utils/csvImport.ts (line 80) — the `crypto.randomUUID()` convention for minting arrangement ids.
  </read_first>
  <behavior>
    - Given a song with a single arrangement `{ id: 'a1', key: 'G' }`: the Key input shows "G"; setting it to "D" and clicking Save calls `updateSong` with `data.arrangements[0].key === 'D'`.
    - Given a multi-arrangement song whose `primaryArrangementId` points at the SECOND arrangement: editing the Key input writes to that second arrangement's `key`, leaving the first arrangement's key untouched.
    - Given a song with `primaryArrangementId: null` and two arrangements: the input reflects and edits `arrangements[0].key` (the first-arrangement fallback).
    - Given a song with zero arrangements: the input shows empty; setting it to "A" and saving does NOT crash and results in `data.arrangements` having exactly one arrangement with `key === 'A'`, and `data.primaryArrangementId` equal to that new arrangement's id.
  </behavior>
  <action>
    Implement R249 — an always-visible editable Key that targets the primary/first arrangement's key VALUE (distinct from the existing "Primary key" select at 217-234, which only picks WHICH arrangement is primary; keep that select unchanged).

    In the `<script setup>` block, add a writable computed `primaryArrangementKey` (typed `computed<string>({ get, set })`):
    - get: if `form.value.arrangements.length === 0` return empty string; otherwise resolve the target arrangement as `form.value.arrangements.find(a => a.id === form.value.primaryArrangementId) ?? form.value.arrangements[0]` and return `target?.key ?? ''`.
    - set(value): resolve the same target. If there are zero arrangements, mint a new arrangement with `crypto.randomUUID()` for the id and fields `{ id, name: 'Default', key: value, bpm: null, lengthSeconds: null, chordChartUrl: '', notes: '', teamTags: [] }`, push it onto `form.value.arrangements`, and set `form.value.primaryArrangementId` to the new id. Otherwise assign `value` to the resolved target's `key`.

    In the Details-tab template, add a new always-visible field immediately BEFORE the conditional "Primary key" block (~line 217). Use the same label/input styling as the surrounding fields: a `<label>` reading "Key" and a text `<input>` bound with `v-model="primaryArrangementKey"`, given `data-testid="song-key-input"` and a placeholder like "e.g. G, Am, D/F#". Do NOT gate it on arrangement count — it must render for single-arrangement and zero-arrangement songs too.

    No changes to `onSave` are required: it already passes `form.value.arrangements` through and re-resolves `primaryArrangementId` with a first-arrangement fallback, so writing into the resolved arrangement in place is persisted by the existing path.

    Add tests to src/components/__tests__/SongSlideOver.test.ts (new describe block "— key (R249)") covering all four cases in `<behavior>`. Reuse `makeSong`/`mountDrawer`; find the input via `wrapper.find('[data-testid="song-key-input"]')`, `setValue(...)`, click the Save button (`wrapper.findAll('button').find(b => b.text() === 'Save')`), and assert against `mockUpdateSong.mock.calls[0]` (or `mockAddSong` in create mode). For the multi-arrangement case, extend `makeSong` overrides with an `arrangements` array of two entries and an explicit `primaryArrangementId`.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongSlideOver.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - The Key input renders for a single-arrangement song (not gated on `arrangements.length > 1`).
    - Editing the Key and saving persists the value onto the resolved primary/first arrangement (`data.arrangements[...].key`).
    - Editing a multi-arrangement song's Key writes to the primary arrangement (per `primaryArrangementId`), not blindly to index 0.
    - A zero-arrangement song does not crash on Key edit; a single default arrangement is created holding the key and `primaryArrangementId` is set to it.
    - `npx vitest run src/components/__tests__/SongSlideOver.test.ts` passes; `npm run type-check` is clean.
  </acceptance_criteria>
  <done>The song drawer has an always-visible Key field bound to the primary/first arrangement's key, persisting through save, with tests proving all four behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (R253): Exclude the sermon passage from the Scripture rotation and add a regression test</name>
  <files>src/components/ScriptureRotationTable.vue, src/components/__tests__/ScriptureRotationTable.test.ts</files>
  <read_first>
    - src/components/ScriptureRotationTable.vue — the `rotationEntries` computed (135-171); the SCRIPTURE-slot loop (142-149) that stays; the sermon-passage block (151-156) to remove; the type import at line 105; and the empty-state copy at line 19.
    - src/types/service.ts — `Service` (155-), `Service.sermonPassage: ScriptureRef | null` (163), `ScriptureSlot` (73-94, `kind: 'SCRIPTURE'` with `book`/`chapter`/`verseStart`/`verseEnd`), and `ScriptureRef` (148-153).
    - src/components/__tests__/RolesConfigPanel.test.ts — the store-mock + `mount` pattern to mirror for a fresh component test (this component takes a plain `services` prop, so no store mock is needed).
    - NOTE: src/components/__tests__/ScriptureRotationTable.test.ts does NOT exist yet — you are creating it (the Nyquist scaffold for this task).
  </read_first>
  <behavior>
    - Given a service with one SCRIPTURE slot ("John 3:16") AND a non-null `sermonPassage` ("Romans 8:28"): `rotationEntries` contains a row for "John 3:16" and NO row whose key derives from the sermon passage ("Romans 8:28").
    - Given a service whose ONLY scripture content is `sermonPassage` (no SCRIPTURE slots): `rotationEntries` is empty (the table shows the empty state).
    - Given two services sharing the same SCRIPTURE passage on different dates: that passage's row lists both dates (existing behavior preserved).
  </behavior>
  <action>
    Implement R253 — the Scripture rotation must count only SCRIPTURE slots, never the sermon passage.

    In src/components/ScriptureRotationTable.vue:
    - Delete the sermon-passage contribution block inside `rotationEntries` (the `if (service.sermonPassage != null) { ... keysForService.add(key) }` block at ~151-156). Leave the SCRIPTURE-slot loop (142-149) and the date-accumulation loop (158-164) intact.
    - Remove the now-unused `ScriptureRef` from the type import at line 105 so the line reads `import type { Service, ScriptureSlot } from '@/types/service'` (leaving `ScriptureRef` in would fail the type-check as an unused import). `Service` and `ScriptureSlot` both remain used.
    - Fix the empty-state copy at line 19: drop the sermon-passage mention so it reads e.g. "No scripture passages found in these services. Add scripture slots to see rotation patterns." Do not reference a sermon passage anywhere in this component's template.

    Create src/components/__tests__/ScriptureRotationTable.test.ts covering the three `<behavior>` cases. Mount the component with a `services` prop of minimal `Service`-shaped objects (build a small `makeService` helper: only the fields the component reads — `id`, `date`, `slots`, `sermonPassage` — are needed; cast to the imported `Service` type as the other tests do for Firestore `Timestamp` fields). Assert on rendered rows (the passage-key cells) or, more robustly, on the presence/absence of the passage text via `wrapper.text()`: the SCRIPTURE passage text appears, the sermon passage text does not. Include an explicit regression assertion named for R253 proving `sermonPassage` does not surface as a rotation row while SCRIPTURE slots do.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ScriptureRotationTable.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `ScriptureRotationTable.vue` no longer reads `service.sermonPassage` anywhere; only `slot.kind === 'SCRIPTURE'` feeds `rotationEntries`.
    - The empty-state copy no longer mentions a sermon passage.
    - The `ScriptureRef` import is removed (no unused-import type error); `npm run type-check` is clean.
    - The new test proves a service's `sermonPassage` does not appear as a rotation row while its SCRIPTURE slots do, and that a sermon-only service yields an empty rotation.
    - `npx vitest run src/components/__tests__/ScriptureRotationTable.test.ts` passes.
  </acceptance_criteria>
  <done>The Scripture rotation is sourced solely from SCRIPTURE slots, the copy is accurate, and a regression test guards against the sermon passage reappearing.</done>
</task>

<task type="auto">
  <name>Task 3 (R256): Verify the schedulable-roles copy is accurate everywhere and strengthen the negative-assertion test</name>
  <files>src/components/__tests__/RolesConfigPanel.test.ts</files>
  <read_first>
    - src/components/RolesConfigPanel.vue — the header copy (lines 5-8): "Default count is the number of volunteers the scheduler auto-fills for this role each service." This is already accurate (v2.2 R246); expect NO production change here.
    - src/components/__tests__/RolesConfigPanel.test.ts — the existing R246 test (50-59) with its `planner-discipline-allow` markers; this is the assertion to keep and strengthen.
    - Verification surfaces to grep for a straggler: RolesConfigPanel.vue, the ServiceEditorView Roles section, QuarterView, and any tooltip copy. (Planner already grepped `.vue`: no straggler exists — only the negative test and an unrelated `scheduler.ts` code comment about hard caps.)
  </read_first>
  <action>
    R256 is satisfied-by-verification — the copy in RolesConfigPanel.vue already describes the scheduler's real behavior (it auto-fills the configured count each service). Do NOT change scheduler behavior, and do NOT alter the RolesConfigPanel.vue copy unless the grep in `<verify>` surfaces a straggler in a `.vue` UI surface.

    Steps:
    1. Run the grep gate in `<verify>` over `.vue` files. If it returns any match, correct that surface's copy to describe the real behavior (the scheduler fills up to the configured count each service) — mirror the RolesConfigPanel wording. If it returns no match (expected), make no production change and record R256 as verified-complete in the SUMMARY.
    2. Strengthen the existing test in src/components/__tests__/RolesConfigPanel.test.ts: keep the two negative assertions (they already carry `planner-discipline-allow` marker comments — do not remove those markers) and add a POSITIVE assertion that the header text contains the accurate phrasing (e.g. lowercased text contains "auto-fill" and "each service", which the current copy satisfies). This locks the accurate copy in place so a future edit that reintroduces soft-target framing fails.

    Note: the two deprecated literals must only ever appear inside this test file (guarded by the existing allow-markers) and inside the grep command below — never introduce them into any `.vue` file.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/RolesConfigPanel.test.ts</automated>
    <automated>test -z "$(grep -rniE 'soft planning target|not a hard cap' src --include='*.vue')" && echo NO-STRAGGLER || (echo STRAGGLER-FOUND; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - The grep over `src/**/*.vue` for the deprecated soft-target phrasing returns no matches (prints NO-STRAGGLER).
    - RolesConfigPanel.test.ts retains both negative assertions (with their allow-markers) and gains a positive assertion locking in the accurate "auto-fill … each service" phrasing.
    - No scheduler behavior is changed; RolesConfigPanel.vue copy is unchanged unless a straggler was found and corrected.
    - `npx vitest run src/components/__tests__/RolesConfigPanel.test.ts` passes.
  </acceptance_criteria>
  <done>R256 is verified complete: no UI surface uses the deprecated soft-target phrasing, and the roles copy is locked by a positive+negative test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (song write) | The Key value typed in SongSlideOver is written onto an existing org-scoped song document via the existing `songStore.updateSong`/`addSong` path. |
| services prop → rendered rotation table | Service data (including scripture text) is rendered as passage-key rows in ScriptureRotationTable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-01 | Tampering | SongSlideOver Key input → songStore.updateSong/addSong | low | accept | Key is free text stored on an existing org-scoped song doc through the unchanged upsert path; Firestore rules already gate song writes to org members, and no new field, endpoint, or trust boundary is introduced. The value is rendered as escaped text by Vue. |
| T-87-02 | Information Disclosure | ScriptureRotationTable rotation rows | low | accept | The change only REMOVES the sermon passage from the displayed rotation — it reduces displayed data and exposes nothing new. |
| T-87-03 | Injection (XSS) | Rendered Key / passage text | low | accept | All new/changed strings are interpolated via Vue text bindings (`{{ }}` / `v-model` on inputs), which HTML-escape by default; no `v-html` is introduced. |

Package legitimacy (T-87-SC): Not applicable — this phase installs no npm/pip/cargo packages (client-only edits to existing components and tests). No package-legitimacy checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/SongSlideOver.test.ts` — passes (R249).
- `npx vitest run src/components/__tests__/ScriptureRotationTable.test.ts` — passes (R253, new file).
- `npx vitest run src/components/__tests__/RolesConfigPanel.test.ts` — passes (R256).
- `npm run type-check` (vue-tsc --build, includes test files) — clean. NOTE: use this, NOT `-p tsconfig.app.json`, per CLAUDE.md.
- Grep gate: `grep -rniE 'soft planning target|not a hard cap' src --include='*.vue'` returns no matches (R256).
- Optional full-suite sanity: `npx vitest run` — the only expected failures are the documented 2-file baseline (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`), neither touched by this phase. Do NOT use `--dir src`.
</verification>

<success_criteria>
1. A planner can edit a song's Key in the drawer and the new Key persists onto the primary/first arrangement (R249), with the zero-arrangement edge handled without a crash.
2. The Scripture rotation tab lists only scripture placed in the service plan and never the sermon passage; empty-state copy is accurate (R253).
3. No `.vue` UI surface describes the schedulable-roles default count as a "soft planning target"; the copy accurately states the scheduler auto-fills the count each service, locked by a positive+negative test (R256).
4. `npm run type-check` is clean and the three targeted test files pass.
</success_criteria>

<output>
Create `.planning/phases/87-song-rotation-refinements/87-01-SUMMARY.md` when done.
</output>
