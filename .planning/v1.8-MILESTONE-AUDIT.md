---
milestone: v1.8
milestone_name: Cost & Billing Hardening
audited: 2026-08-20
status: passed
scores:
  requirements: 12/12
  phases: 3/3
  integration: PASS
  flows: PASS
owner_acceptance: "Verified — owner sign-off 2026-08-20"
gaps: {}
tech_debt:
  - phase: deferred
    items:
      - "R169 (in-app per-org storage-usage visibility) — deferred by design; not mapped to any v1.8 phase."
  - phase: owner-operational
    items:
      - "firestore.rules deny for aiUsage/aiRateLimits (Phase 65, defense-in-depth) — built + emulator-tested, UNDEPLOYED; owner runs `firebase deploy --only firestore:rules`."
      - "Storage deletion is OFF (all 4 crons deployed in dry-run). Activating real deletion is owner-gated: set each `*_CLEANUP_ENABLED=true` after reviewing the dry-run logs."
      - "Gating sendScheduledReminders off (R170) also pauses composer 'schedule-for-later' dispatch until SCHEDULED_MESSAGING_CRON_ENABLED=true — disclosed, reversible."
  - phase: next-milestone
    items:
      - "SEED-001: owner-only admin UI for these settings (replaces buried functions/.env vars) — owner's stated next milestone."
---

# v1.8 Cost & Billing Hardening — Milestone Audit

**Status: ✅ PASSED.** All 12 requirements satisfied and cross-phase-integrated; owner-accepted
("Verified", 2026-08-20). Safe config deployed to production 2026-08-20.

## Scope

Phases 65–67, requirements R161–R168 + R170–R173 (R169 deferred by design). All three phases modified
`functions/src/index.ts` (+ tests); Phase 67 also touched `render-service/DEPLOY.md`.

## Phase verifications (all read)

| Phase | VERIFICATION | Plans | Code review |
|-------|--------------|-------|-------------|
| 65 AI Proxy Cost Controls | passed (9/9) | 2/2 | 4 findings fixed, 0 blockers |
| 66 Storage Retention | passed (4/4) | 2/2 | 1 warning fixed (WR-01), 0 blockers |
| 67 Fan-out/Cron/Instance | passed (4/4) | 2/2 | 3 findings fixed, 0 blockers |

Plus a post-milestone hardening: retention windows made env-tunable + media default 14→30d (371/371 tests).

## Requirements coverage — 12/12 satisfied (3-source cross-reference)

Every requirement appears in its phase VERIFICATION (passed), its SUMMARY `requirements-completed`
frontmatter, and the REQUIREMENTS.md traceability table (all `[x]`). Integration checker confirmed each
has a live cross-phase wiring touchpoint.

| REQ | Control | Verified | Deployed |
|-----|---------|----------|----------|
| R161 | per-uid rate limit (429, fail-open) | ✓ | ✓ (`api`) |
| R162 | server-side model allow-list + max_tokens clamp | ✓ | ✓ |
| R163 | `aiUsage` ledger (Admin SDK) | ✓ | ✓ |
| R164 | `maxInstances:10` on `api` | ✓ | ✓ |
| R165 | media cleanup delete-branch proven; retention env-tunable | ✓ | ✓ (dry-run; deletion owner-gated) |
| R166 | orphan-render cleanup delete-branch proven | ✓ | ✓ (dry-run; deletion owner-gated) |
| R167 | `cleanupOrphanBackgrounds` (3-tier refs + 2 fail-safes) | ✓ | ✓ (dry-run; deletion owner-gated) |
| R168 | `cleanupPptxSources` (consumed+aged, keeps rendered/) | ✓ | ✓ (dry-run; deletion owner-gated) |
| R170 | `sendScheduledReminders` gated off (scan stopped) | ✓ | ✓ |
| R171 | Resend recipient cap + per-org daily quota | ✓ | ✓ |
| R172 | project-wide `setGlobalOptions({maxInstances:20})` | ✓ | ✓ |
| R173 | render-service `--max-instances=3 --concurrency=1` | ✓ | ✓ (Cloud Run, revision 00002) |

## Integration — PASS (0 blockers)

- `setGlobalOptions(20)` + `api`'s own `10` resolve correctly (api≤10, all else≤20).
- Three Admin-SDK counters (`aiRateLimits`, `aiUsage`, `orgEmailCounters`) — distinct top-level
  collections, no key/path collision, all kept top-level per the T-37-15 rules rationale.
- Shared `readNumericKnob` / `readDeleteCap` / retention-window readers consistent; every `process.env`
  knob matches the handover docs (no orphans either direction).
- 371/371 functions tests pass together; build clean.
- Every runaway-cost surface from the milestone goal has a live control (proxy, storage, reminder scan,
  email fan-out, instance ceilings).

## Deployment status (safe config LIVE 2026-08-20)

`firebase deploy --only functions` + the render-service `gcloud run deploy` succeeded. The AI proxy caps,
reminder-cron gate, send caps, global + render-service instance ceilings are LIVE; the four cleanup crons
are LIVE in dry-run (delete nothing until enabled).

## Owner-gated / deferred (intentional — not gaps)

- Activate storage deletion: set each `*_CLEANUP_ENABLED=true` after reviewing dry-run logs.
- Deploy the `firestore.rules` deny (Phase 65 defense-in-depth).
- R169 (in-app usage visibility) deferred; SEED-001 (admin settings UI) planted for the next milestone.

## Verdict

Milestone achieved its definition of done. Owner-accepted. Ready to complete + archive.
