---
milestone: v2.7
name: Rehearsal, Stage Plans & Presentation Polish
audited: 2026-09-01
status: passed
scores:
  requirements: 14/14
  phases: 4/4
  integration: 6/6 seams WIRED
  flows: plan→lock→Run→share E2E WIRED
gaps:
  requirements: []
  integration: []
  flows: []
deferred_human_uat:
  note: "Per owner instruction, all human/visual/hardware UAT was deferred to a batched end pass. Every requirement is verified against live code; these are observation-only confirmations, not code gaps. Full list in v2.7-DEFERRED-VERIFICATION.md."
  phases_human_needed: [105, 106, 107]
tech_debt:
  - phase: 104
    items:
      - "Stale duplicate test file src/stores/appConfig.test.ts (NOT under __tests__/) fails independently of v2.7 — pre-existing; logged to 104 deferred-items. Candidate for deletion (canonical src/stores/__tests__/appConfig.test.ts passes)."
      - "A full app-wide sweep migrating every remaining ad-hoc message surface onto the new notification store was not done (the shared primitive + the known stuck monitor warning shipped); integration check found no other stuck banners, so this is low-priority."
  - phase: 107
    items:
      - "IN-02: edit/remove buttons on a marker chip reveal on hover only; on touch they're reachable via tap-to-popover (mitigated, left as-is)."
---

# Milestone v2.7 Audit — Rehearsal, Stage Plans & Presentation Polish

**Status: PASSED.** All 14 requirements (R302–R315) are satisfied and independently verified against live
source; cross-phase integration is fully WIRED; zero new test regressions across the full suite (the only
two failing files are the documented pre-existing baselines — `storage.rules.test.ts` and the stale
duplicate `appConfig.test.ts`). `npm run type-check` is clean.

The three phases marked `human_needed` are so ONLY because of owner-deferred visual/hardware UAT
(observing solid black on a real monitor, a real touch-device drag, the loop pausing on a real second
display, a live share/print of the stage plot). Per the explicit "defer verification to the end"
instruction these were batched, not gated. Every claim those checks would confirm is already backed by a
passing automated test — so they are confirmations, not open code gaps.

## Requirements coverage (3-source cross-reference)

| REQ | Phase | VERIFICATION | SUMMARY | REQUIREMENTS.md | Final |
|-----|-------|--------------|---------|-----------------|-------|
| R302 inline black slide, no new section | 105 | human_needed (verified) | listed | [x] | satisfied |
| R303 black renders full-black all surfaces + nav | 105 | human_needed (verified) | listed | [x] | satisfied |
| R304 black slide doesn't corrupt numbering/mirroring | 105 | human_needed (verified) | listed | [x] | satisfied |
| R305 "Go to black" → Audience only | 105 | human_needed (verified) | listed | [x] | satisfied |
| R306 per-item loop, wrap to first slide | 106 | human_needed (verified) | listed | [x] | satisfied |
| R307 interval default 10s + preset/custom, persisted | 106 | human_needed (verified) | listed | [x] | satisfied |
| R308 no timer fights nav; go-to-black pauses loop | 106 | human_needed (verified) | listed | [x] | satisfied |
| R309 every message manually dismissible | 104 | passed | listed | [x] | satisfied |
| R310 condition-tied messages auto-clear (monitor warning) | 104 | passed | listed | [x] | satisfied |
| R311 user-menu church switcher (multi-org, not super-admin) | 104 | passed | listed | [x] | satisfied |
| R312 full org-scoped reset on switch, no stale data | 104 | passed | listed | [x] | satisfied |
| R313 Stage Layout tab, drag markers, on/off-stage zones | 107 | human_needed (verified) | listed | [x] | satisfied |
| R314 free-text labels + speaker mic, round-trip + resize-stable | 107 | human_needed (verified) | listed | [x] | satisfied |
| R315 saved per service, read-only on share/print | 107 | human_needed (verified) | listed | [x] | satisfied |

14/14 satisfied. No orphaned, unsatisfied, or partial requirements.

## Phase verifications

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 104 Notification & Multi-Church Foundations | passed | 4/4 | All 8 code-review findings (incl. Critical R312 listener leak) fixed + re-verified line-by-line |
| 105 Presentation Blackout & Inline Black Slide | human_needed | 4/4 verified | Only real-monitor/visual confirmations deferred; CR-01 background-leak fix re-confirmed in live code |
| 106 Per-Item Loop Playback | human_needed | 6/7 verified | Only the real second-display hardware spot-check deferred; go-to-black-pause proven by an automated output-window test |
| 107 Visual Stage Layout | human_needed | 10/10 verified | Only real touch-drag + visual share/print deferred; public-surface security confirmed (snapshot-only, text labels, no new rules) |

## Cross-phase integration — WIRED (6/6 seams)

1. **Run-flow triple seam (105+106):** loop pause reads the same `blackout` ref the Audience-only fix scopes; every loop advance routes through the single-writer `postIndex()`; an authored blackout slide under a loop is just another slide in the rotation (distinct from runtime go-to-black). WIRED.
2. **Org-scope reset (104↔107):** `stageLayout` lives on the service doc (services store already reset), so the Phase-104 `STAGELAYOUTS-RESET-OBLIGATION` marker is resolved with no separate store; a church switch can't leak a prior church's layout. CR-01 reactive re-subscription verified in both `TeamView` and `GettingStarted`. WIRED.
3. **Notification store reuse (104):** `ToastHost` mounted once at `App.vue`; the switcher error + monitor stickies route through it; no other stray stuck banner found. WIRED.
4. **Snapshot/share (107):** additive `stageLayout` denormalization (omit-when-empty) doesn't disturb `roleAssignments`/`slots`; public `ShareView` gains no new data access. WIRED.
5. **E2E flow:** plan a service (looping item + blackout slide + stage layout) → lock → Run (loop + go-to-black) → share (read-only plot) hangs together with no cross-phase break. WIRED.
6. **Requirements integration map:** all 14 REQ-IDs sit on at least one verified cross-phase connection.

## Gates
- `npm run type-check` (vue-tsc --build): clean.
- Full `npx vitest run`: 181/183 files, ~4954/4981 tests pass — only the 2 documented baselines fail. No new regressions.
- Zero new npm dependencies (as designed).
- No `firestore.rules`/`storage.rules` changes (stage layout is a service-doc field; no new public access).

## Deferred to the owner (NOT done by the autonomous run)
- **Human/visual/hardware UAT** — the batched list in `.planning/v2.7-DEFERRED-VERIFICATION.md` (per phase).
- **Deploy** — nothing is deployed; the client changes are local only. Deploy is owner-gated (confirm-then-deploy policy).
- **Milestone close/archive** — left OPEN pending owner acceptance (mirrors the v2.5/v2.6 "closed on owner acceptance" precedent).
