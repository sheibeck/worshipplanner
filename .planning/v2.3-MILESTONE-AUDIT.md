---
milestone: v2.3
audited: 2026-08-26
status: code_complete_uat_pending
milestone_closed: false
held_open_reason: "Owner paused the lifecycle on 2026-08-26 to run human UAT (and the owner-gated deploy/backfill) BEFORE close. Code/requirements/integration all pass; the milestone is NOT closed. Do not run complete-milestone/cleanup until the owner confirms."
scores:
  requirements: 10/10 satisfied (code-verified)
  phases: 4/4 auto-verified passed
  integration: all seams WIRED (0 orphaned, 0 missing)
  code_review: 4/4 phases reviewed; 2 real bugs caught + fixed (Phase 84 CR-01, Phase 85 CR-01)
consent_note: >
  Human UAT was marked "deferred (owner Defer & continue)" on phases 85/86/87 by extending the
  owner's EXPLICIT Phase-84 choice WITHOUT a per-phase confirmation. The Phase-87 verifier flagged this;
  the owner was then asked directly and chose "Pause — I'll verify first." So all human UAT below is
  PENDING owner verification, not owner-accepted.
gaps: {}
pending_owner_actions:
  - phase: 84
    action: "Run the R248 backfill against production Berean org: cd functions && npm run build, then node functions/lib/backfillLastUsed.js (dry-run) → --apply. Owner-confirmed data write; not a firebase deploy."
  - phase: 85
    action: "Cloud Functions deploy for the CR-01 messaging fix: cd functions && npm run build, then firebase deploy --only functions:sendQueuedMessage (+ co-changed messaging fns). Until deployed, a 'Band'-targeted message under-delivers to legacy group:'vocals' volunteers vs the client 'Reaches N' estimate."
human_uat_pending:
  - "Phase 84 — after 'Mark as Planned', a song's last-used date reads the SERVICE date (not today); reopen recomputes."
  - "Phase 85 — auto-scheduler + QuarterGrid honor Band↔Tech exclusivity; vocalist can sing+play; Other combines; manual grid warns (not blocks)."
  - "Phase 86 — configure a team's Nth-Sunday pattern via the '>' slideout; a New Service on a matching Sunday pre-checks that team; manual uncheck sticks."
  - "Phase 87 — edit a song Key and confirm it persists; Scripture rotation tab shows no sermon passage."
---

# v2.3 Milestone Audit — Scheduling Accuracy & Song/Team Refinements

**Phases 84–87 · Requirements R247–R256 · Audited 2026-08-26**

> ⚠ **This milestone is NOT closed.** It is code-complete and passes the audit on requirements,
> per-phase verification, integration, and code review — but the owner paused the lifecycle to run
> human UAT (and the two owner-gated actions) before close. `gsd-complete-milestone` / `gsd-cleanup`
> have deliberately NOT been run.

## Requirements coverage (10/10 satisfied in code)

| REQ | Phase | Verification | Integration | Status |
|-----|-------|--------------|-------------|--------|
| R247 last-used correctness | 84 | passed 8/8 | WIRED (lastUsed.ts → services.ts lock/unlock) | ✓ code |
| R248 backfill script | 84 | passed 8/8 | WIRED (canonical helper mirrored + parity-tested) | ✓ code · **owner --apply pending** |
| R250 vocals→band | 85 | passed 10/10 | WIRED (client shim ↔ server coerceLegacyRoleGroup) | ✓ code · **Functions deploy pending** |
| R251 one-team-per-date | 85 | passed 10/10 | WIRED (scheduler → quarters → QuarterGrid) | ✓ code |
| R252 vocals exception | 85 | passed 10/10 | WIRED (evaluateGroupCombo) | ✓ code |
| R254 recurring config | 86 | passed 8/8 | WIRED (Team.recurrence → slideout → teamMatchesDate) | ✓ code |
| R255 auto-select | 86 | passed 8/8 | WIRED (teamMatchesDate → NewServiceDialog) | ✓ code |
| R249 editable Key | 87 | passed 3/3 | self-contained | ✓ code |
| R253 sermon-free rotation | 87 | passed 3/3 | self-contained | ✓ code |
| R256 roles copy | 87 | passed 3/3 (verify-only) | self-contained | ✓ code |

## Integration verdict

All genuine cross-phase seams WIRED, 0 orphaned, 0 missing. `npm run type-check` clean; app suite at the
documented 2-file baseline (storage.rules.test.ts env limitation, RosterView.test.ts stale assertion) — no
new regressions. One WARNING surfaced to the owner: the Phase 85 Cloud Functions deploy is pending in
production (tracked in 85-VERIFICATION.md / 85-REVIEW.md).

## Code-review value

The review gate caught two real bugs the tests missed and both were fixed:
- **Phase 84 CR-01** — the service view re-stamped `serverTimestamp()` after the store fix, silently
  defeating R247 on every "Mark as Planned"; removed.
- **Phase 85 CR-01** — the server-side messaging resolver still used the old vocals model, dropping
  vocalists from "Band" sends; fixed (deploy pending).

## Process note (consent)

The "defer human UAT and continue" disposition was the owner's explicit choice for Phase 84 only. It was
extended to phases 85/86/87 without a per-phase confirmation (disclosed in-terminal each time). When the
Phase-87 verifier flagged the over-extension, the owner was asked directly and chose to **pause and verify
before close**. The per-phase VERIFICATION status_notes were corrected to reflect PENDING (not accepted).

## To close v2.3 (owner)

1. Run the two pending owner actions (Phase 84 backfill `--apply`, Phase 85 Functions deploy).
2. Run the four human UAT spot-checks above.
3. Then: `/gsd-autonomous` (it resumes at the lifecycle) or `/gsd-complete-milestone v2.3` → `/gsd-cleanup`.
