# v2.15 Deferred Verification — Batched Human/Browser/Hardware UAT

**Owner instruction (2026-09-09):** "use gsd-autonomous. defer uat to the end."

Every v2.15 phase is built and **auto-verified green** (unit tests + `npm run type-check` + code review +
nyquist validation + security audit). The items below are the checks automated tests **cannot** close —
real-browser behavior, multi-monitor hardware, and human visual/UX judgement. They are batched here for a
single owner pass at milestone end. **Status is PENDING** — nothing here is owner-accepted until the owner
runs it. The milestone stays OPEN (not archived) until this pass is done.

Resume any phase's formal verification with `/gsd-verify-work <phase>`.

---

## Phase 138 — Field Fixes: Church-Picker Deep-Link & Service-Update Email Link

Auto-verified: 7/7 must-haves, SECURED (5/5 threats), nyquist validated. Automated unit coverage exists for
all storage-helper logic.

- [ ] **Church-picker new-tab restore (R428)** — as a **multi-church** member, right-click a nav link (or
  paste a deep link) to open it in a **genuinely new browser tab**. Expected: the tab lands on the intended
  page, **not** `/select-church`. (jsdom can't model a new tab's empty sessionStorage vs. shared
  localStorage — this is the one gap unit tests can't close.)
  - [ ] Also confirm **sign-out** on a shared computer leaves no leftover church for the next user.
  - [ ] Confirm a **single-church** user is unaffected (goes straight in).
- [ ] **Service-update email plan link (R434)** — trigger a re-lock notification email for a service and
  confirm the delivered email contains a working "View the full plan" link that opens the correct service
  plan. (Automated tests confirm the token is appended + resolved server-side; this confirms real delivery.)

---

## Phase 139 — Rehearsal & Report Times

Auto-verified: 6/6 must-haves, SECURED (7/7 threats), nyquist validated. Code review found + fixed 2
criticals (undated-rehearsal "Invalid Date" on the public page; editor Times UI exposed to non-editors).
Automated unit coverage exists for the data model, sort/format util, copy-not-live-bind, both projections,
and the editor authorization gate.

- [ ] **Org defaults → new-service pre-fill (R429)** — in Settings, set default rehearsal time(s) + a
  default report time; **reload** to confirm they persist; then **create a new service** and confirm the
  rehearsal rows (default time, blank date) + report time pre-fill. Confirm editing the org default later
  does NOT change an already-created service.
- [ ] **Editor Times subsection (R430/R431)** — add/edit/remove multiple dated rehearsals + set a report
  time on a service; confirm they autosave and display chronologically. Confirm a **locked** service (or a
  non-editor) shows the times **read-only** (no add/remove controls).
- [ ] **Cross-surface display (R433)** — with a service that has 2 rehearsals + a report time, confirm the
  times show alongside the date, correctly 12-hour formatted, on: the **dashboard**, the **services-list
  card**, **My Schedule**, the **volunteer service view**, and the **public share/plan page**.

---

## Phase 140 — Vamps Library: CRUD & Storage

Auto-verified: 9/9 must-haves, SECURED (8/8 threats), nyquist validated (61 app-side tests in 7 files; rules
suite 310/310 with the emulator). Code review found + fixed 1 critical (silent MP3 upload failure — no
progress/error UI) and 2 warnings (VampTable given a pre-filtered list, so a no-match search showed the
"library is empty" state; MP3 replace/remove orphaned the superseded Storage object). Automated coverage
exists for the store CRUD, path/validation helpers, storage rules, church-switch reset, and component wiring.

- [ ] **Vamps tab visual fidelity (R436)** — open **Songs → Vamps**. Confirm the tab bar (with count badges),
  the flat one-vamp-per-row table (Vamp · Key · Tempo · Audio, amber "No MP3 attached" state), and the
  slide-out editor (Name, 12-key chip picker, Tempo, MP3 drop-zone/player, Delete) match `Vamps.dc.html`
  Turn-12 in the app's dark gray-950 language. Search by **name** and by **key** and confirm a no-match
  search shows "No vamps match your search" (not the empty-library CTA).
- [ ] **Real MP3 round-trip in a deployed environment (R435)** — create a vamp (drawer stays open after
  save), attach a real MP3 (≤50MB) and confirm upload progress, then playback and download. **Replace** it
  with a second MP3 and **remove** it; confirm the superseded object is gone from Storage (Firebase console
  → `orgs/{orgId}/vamp-files/…`). Attach a non-MP3 or an oversize file and confirm a visible error.
  Delete a vamp via the inline confirm. (The Storage emulator cannot model prod upload/CORS behavior.)
- [ ] **Church switch (R435)** — as a multi-church editor, open the Vamps tab in Church A, switch to Church
  B via the sidebar, confirm no Church-A vamp remains listed.
