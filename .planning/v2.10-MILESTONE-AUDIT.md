---
milestone: v2.10
audited: 2026-09-05
status: passed
scores:
  requirements: 22/22
  phases: 4/4
  integration: WIRED
  flows: WIRED
gaps: {}
accepted_residuals:
  - id: SEC-S-03
    note: "Share links never expire/rotate — intentional product design, out of milestone scope (recorded, not changed)."
  - id: R342
    note: "Super-admin members-write is universal by rule design (Phase 78 R225/T-78-03) — documented + pinned with an ALLOW rules test rather than narrowed; the no-write guarantee remains a client-code contract."
  - id: R347
    note: "Memorable share ids stay guessable — re-accepted in writing (changing the format breaks deployed links; v2.8's SEC-S-01 list-lock already removes enumeration)."
  - id: R348
    note: "role 'admin' == 'editor' today — documented with a warning + synonymity test; a future admin-gate must not inherit the self-escalation path."
  - id: WR-03
    note: "planningcenter has no per-caller throttle post-auth (residual compute-cost DoS from authenticated callers) — out of R339's auth-only scope; candidate for a future cost-hardening pass."
tech_debt:
  - phase: 120-god-module-decomposition
    items:
      - "Decomposition is 'begin' only — ServiceEditorView.vue (4409 lines) and functions/src/index.ts remain large; one responsibility each was extracted. Continuing the decomposition is future backlog."
  - phase: cross-milestone
    items:
      - "appConfig knob overloading (117 WR-01): aiProxy.rateLimit* / messaging.orgDailyEmailQuota each govern 2-3 subsystems (deliberate, avoids the out-of-scope frontend appConfig duplicate) — a future dedicated-knob split would remove the operator foot-gun."
      - "The confirmed-sound v2.8 findings (SEC-C-02..04, SEC-A-02, SEC-S-05, ARCH-004/005/015-019/021-023) were verified needing no action and are fully accounted for."
---

# Milestone v2.10 — Security & Architecture Hardening — Audit

**Status: PASSED.** 22/22 requirements satisfied, 4/4 phases verified, cross-phase integration WIRED.
This milestone promoted backlog **999.5** (v2.8 security Medium/Low) + **999.4** (v2.8 architectural
Medium/Low) and remediated all actionable findings. **Built & code-reviewed; UNDEPLOYED** — the security
fixes (firestore.rules + Cloud Functions) take effect only on a coordinated production deploy, which
awaits per-deploy owner confirmation.

## Requirements coverage (3-source cross-referenced: REQUIREMENTS traceability + phase VERIFICATION + SUMMARY frontmatter)

All R339–R360 marked `[x]` complete, each traced to a passed phase VERIFICATION and its SUMMARY.

| Phase | Requirements | Verification | Code Review |
|-------|-------------|--------------|-------------|
| 117 — Security: Proxy Auth, Rate Limits & Quotas | R339, R340, R344, R345 | passed 5/5 | 0 Critical / 0 High (3 in-scope fixes applied) |
| 118 — Security: Firestore Rules & Public Share Hardening | R341, R342, R343, R346, R347, R348 | passed 7/7 | 0 Critical / 0 High (WR-01/02/03 applied) |
| 119 — Architecture: Correctness, Batching & Store-Ownership | R349–R357 | passed 9/9 | 0 Critical / 0 High (6 findings applied) |
| 120 — Architecture: God-Module Decomposition | R358, R359, R360 | passed 6/6 | clean (0 Critical / 0 Warning) |

## Integration (cross-phase) — WIRED

Independently re-run at HEAD by the integration checker:
- `npx vitest run` → 189/190 files, 5126 tests pass (only accepted `src/storage.rules.test.ts` baseline red).
- `npm run type-check` (vue-tsc --build) → clean.
- `cd functions && npm test` → 660/660 pass; `cd functions && npm run build` → clean (proves the 117 + 120
  index.ts edits + cleanupSweeps re-exports resolve together — the deploy-safety gate for R359).
- No shared-file merge conflicts (117↔120 in index.ts; 119↔120 in services.ts/ServiceEditorView.vue).
- Store/composable wiring coherent (members/superAdmins stores + useAiSongSuggestions consumed; orgScoped
  teardown correct). Security choke points intact (toPublicServiceSnapshot single write site; AUTH_REQUIRED
  survives; cleanupSweeps re-exported).

## Highlights

- **SEC-A-01 closed** — the unauthenticated `/api/planningcenter` open-relay is now behind a hard-401
  fail-closed auth gate (the milestone's highest-priority finding).
- **A public PII leak beyond the planned scope was caught and closed** (WR-01): stage-marker free-text
  notes were still rendering on the anonymous `?view=stage` page — stripped at the same public choke point.
- **The R359 re-export trap was explicitly guarded** (grep + `functions build`), avoiding a silent
  `firebase deploy` function-drop.

## Deferred / batched

- **Human/visual UAT** deferred to milestone end (owner instruction) — see `.planning/v2.10-DEFERRED-VERIFICATION.md`
  (optional share-page eyeball + post-deploy smoke of PC import / rules-guarded editing). No blocking items.
- Accepted residuals + tech debt: see frontmatter above.

## Verdict

Ready for the owner's deploy + close decision. No gaps force a closure phase.
