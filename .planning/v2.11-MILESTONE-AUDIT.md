---
milestone: v2.11
audited: 2026-09-05
status: passed
scores:
  requirements: 13/13
  phases: 4/4
  integration: 6/6
  flows: complete
gaps: {}
tech_debt:
  - phase: 124-grouped-file-list-in-app-preview-play-download-remove
    items:
      - "Download Save-dialog in prod depends on the Firebase Storage bucket sending permissive CORS for the app origin (firebasestorage.googleapis.com download-token URLs usually do); if a fetch is blocked, the code falls back to opening the file in a new tab. Confirm at deploy; adjust bucket CORS if the Save dialog is required."
      - "IN-02 (latent): songFileStoragePath sanitizes the filename but not orgId/attachmentId — not reachable today (both internally generated); harden if a less-trusted caller is ever introduced."
      - "IN-04: the per-row 'Couldn't load this file.' error state from the UI-SPEC was not implemented (low practical impact)."
      - "PDF page-count is intentionally omitted from the metadata line (no new PDF dependency, per SEED-003); the row degrades gracefully."
  - phase: milestone
    items:
      - "DEPLOY REQUIRED before the feature is correct in production: firebase deploy --only storage (Phase 122 song-files rules block + isOrgEditor + catch-all exclusion) and --only hosting (all client code). Until storage.rules ships, the old catch-all governs song-files/ (member write <25MB, no type/editor/50MB gate)."
      - "Batched human UAT: owner tested locally 2026-09-05 and confirmed the core flow (upload auto-clear/dismiss, duplicate denial, download, remove-confirm copy). Remaining device/prod checks: real-browser download Save-dialog/CORS in prod, real drag-drop, PDF render + MP3 playback in prod."
---

# Milestone v2.11 — Song File Attachments — Audit

**Status: PASSED.** All 13 requirements (R361–R373) satisfied and verified; all 4 phases passed; all 6
cross-phase integration seams WIRED; owner-confirmed local UAT of the core flow. This is step 1 of the
file-storage backlog (999.13 / SEED-003) — the editor-facing attach/manage surface. Rehearse-mode
(public/authenticated playback) remains a future milestone.

## Requirements coverage (13/13 satisfied)

| REQ | Description | Phase | Verified | Integration |
|-----|-------------|-------|----------|-------------|
| R361 | Files tab in Edit Song slideout + count badge | 123 | ✅ | WIRED |
| R362 | Songs-list Files column | 123 | ✅ | WIRED |
| R363 | Multi-file upload + per-file progress | 123 | ✅ | WIRED |
| R364 | PDF/MP3 + ≤50MB enforced client AND storage.rules | 122 | ✅ | WIRED |
| R365 | External media link (YouTube/Drive/Dropbox), new tab | 123 | ✅ | WIRED |
| R366 | Documents/Audio grouped rows + metadata + empty states | 124 | ✅ | WIRED |
| R367 | In-app PDF preview modal + MP3 player | 124 | ✅ | WIRED |
| R368 | Download + remove (removes-everywhere confirm) | 124 | ✅ | WIRED |
| R369 | Attachments additive on Song doc + org-scoped prefix outside media/ | 122 | ✅ | WIRED |
| R370 | Exempt from every retention sweep (locked by test) | 122 | ✅ | WIRED |
| R371 | Deletion only via editor-remove or song delete; no orphaning | 122 | ✅ | WIRED |
| R372 | Editor-only mutation at the rules layer | 122 | ✅ | WIRED |
| R373 | App-fidelity design/UI spec before implementation | 121 | ✅ | WIRED |

## Phases (4/4 passed)

| Phase | Name | Verdict | Notes |
|-------|------|---------|-------|
| 121 | Song Files UI/Design Spec | passed | UI-SPEC checker-verified 6/6; app-fidelity HTML mock delivered |
| 122 | Durable Storage, Rules & Retention-Exempt Foundation | passed 5/5 | storage.rules song-files block + isOrgEditor + catch-all OR-fix; retention-lock test; hardDeleteSong cascade. Code review 0 Critical/0 Warning |
| 123 | Files Tab, Songs List Column, Upload & External Link | passed 4/4 | Files tab, column, upload composable, links. Code review found + fixed a lost-update race (CR-01, arrayUnion) + 2 validation-alignment warnings |
| 124 | Grouped List, Preview/Play, Download & Remove | passed 3/3 | grouped rows, PDF modal (focus-trapped, scoped Escape), MP3 player, download, runTransaction remove. Code review found + fixed a remove-race (WR-01) + focus-trap + error-feedback; owner-reported download bug + upload-lingering + duplicate handling all fixed post-UAT |

## Cross-phase integration (6/6 seams WIRED)

Upload→persist→display · client/server validation agreement · remove→record+storage · design-contract→
implementation · external-link path · retention exemption — all WIRED with code evidence (see the
integration checker report). Producer `SongAttachment` shapes match the consumer type exactly; the 50MB /
PDF-MP3 numbers are identical client and server; retention exemption is structural (the path never
intersects any sweep prefix), not a fragile allowlist.

## Owner UAT (2026-09-05)

Owner tested locally and confirmed the core flow works, driving three post-UAT fixes that shipped: the
download-in-same-window bug (now a blob Save-dialog with new-tab fallback), lingering upload-progress rows
(now auto-clear + a manual dismiss), duplicate-file handling (now denied with a clear dismissable message),
and the remove-confirm copy (simplified — files ride with the song, not with services).

## Tech debt / standing follow-ups (non-blocking)

See frontmatter `tech_debt`. Headline items: (1) **deploy required** — `storage` rules + `hosting`; until
the storage.rules ship, song-files/ is governed by the old catch-all (no type/editor/50MB gate); (2)
**download Save-dialog** in prod depends on Storage-bucket CORS allowing the app origin (else falls back to
new-tab); (3) minor Info deferrals (IN-02 latent path sanitization, IN-04 per-row load-error state, PDF
page-count omitted by no-deps decision).

## Verdict

**PASSED** — ready to complete/archive on owner decision. Production correctness requires the storage-rules
+ hosting deploy (owner-gated, per the confirm-then-deploy policy).
