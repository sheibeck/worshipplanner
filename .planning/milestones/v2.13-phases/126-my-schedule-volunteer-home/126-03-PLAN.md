---
phase: 126-my-schedule-volunteer-home
plan: 03
type: execute
wave: 2
depends_on: [126-01]
files_modified:
  - src/stores/mySchedule.ts
  - src/stores/__tests__/mySchedule.test.ts
  - src/router/index.ts
  - src/views/VolunteerServicePlaceholderView.vue
  - src/router/__tests__/router.test.ts
autonomous: true
requirements: [R378, R382]

must_haves:
  truths:
    - "The mySchedule store runs the constrained collectionGroup('rehearseAccess') query using auth.currentUser.email lowercased, exposing docs / isLoading / error and retaining each doc's orgId (from ref.parent.parent.id)"
    - "A missing or empty signed-in email yields an empty result without throwing (R383 empty state reachable for the right reason)"
    - "/volunteer/service/:serviceId resolves to a real Phase-126 placeholder view (no lazy import() of a Phase-127 file) so `vite build` succeeds"
  artifacts:
    - "src/stores/mySchedule.ts — Pinia store with the collectionGroup query + loading/error state"
    - "src/stores/__tests__/mySchedule.test.ts — store unit tests (lowercasing, empty-email, error path)"
    - "src/router/index.ts — /volunteer/service/:serviceId placeholder route"
    - "src/views/VolunteerServicePlaceholderView.vue — build-safe placeholder for the Phase 127 Rehearse view"
    - "src/router/__tests__/router.test.ts — placeholder route resolves without a no-match warning"
  key_links:
    - "The array-contains filter value is derived ONLY from the signed-in token email (never a client-suppliable field) — V5 input-validation defense-in-depth on top of the Plan-01 rule"
    - "The placeholder route's path/name/meta stay stable so Phase 127 replaces ONLY the component import target"
    - "The placeholder route carries meta.isVolunteerRoute so a zero-membership volunteer session is exempt from the org-selection gate (router beforeEach line 216)"
---

<objective>
Wire the read path into the running app: a thin My Schedule store that issues the constrained collectionGroup query proven safe in Plan 01, plus a build-safe placeholder route for Phase 127's not-yet-built Rehearse view. (The `/my-schedule` route + the post-sign-in landing redirect are authored in Plan 04 alongside the view they point at, so no route ever lazy-imports a file that does not exist yet.)

Purpose: the store isolates the one Firestore query behind a testable interface and guarantees the filter value is always the signed-in identity (never a UI-suppliable email — defense-in-depth). The placeholder route encodes Phase 125's build-safety lesson: never `import()` a component file that does not exist on disk yet.

Output: `src/stores/mySchedule.ts` (+ tests), the `/volunteer/service/:serviceId` route, and `VolunteerServicePlaceholderView.vue`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-my-schedule-volunteer-home/126-RESEARCH.md
@.planning/phases/126-my-schedule-volunteer-home/126-PATTERNS.md
@src/utils/rehearseAccess.ts
@src/router/index.ts
@src/views/VolunteerLinkCompleteView.vue
@src/views/VolunteerSignInView.vue
@src/router/__tests__/router.test.ts
</context>

## Artifacts this phase produces (phase-wide roll-up)

- **Plan 01:** the constrained `list` rule arm, the COLLECTION_GROUP index, the R378 rules test, and `rolesByEmailLower`.
- **Plan 02:** pure helpers — `myScheduleGrouping.ts` + `roleChipIcon.ts` with tests.
- **Plan 03 (this plan):** the My Schedule store (`src/stores/mySchedule.ts`), the build-safe `/volunteer/service/:serviceId` placeholder route + view (`src/views/VolunteerServicePlaceholderView.vue`).
- **Plan 04:** the My Schedule view + card, the `/my-schedule` route, the post-sign-in landing redirect to My Schedule, and component tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mySchedule store — the constrained collectionGroup query</name>
  <files>src/stores/mySchedule.ts, src/stores/__tests__/mySchedule.test.ts</files>
  <read_first>
    - src/utils/rehearseAccess.ts (the RehearseAccessDoc type, now carrying rolesByEmailLower from Plan 01)
    - 126-RESEARCH.md Architecture Patterns Pattern 1 (the exact query) and Common Pitfall #2 (always .toLowerCase() on auth.currentUser.email)
    - 126-PATTERNS.md "src/stores/mySchedule.ts (NEW)" section and the "Lowercased-email normalization" shared pattern
    - an existing simple Pinia store for the defineStore + firebase import conventions (e.g. src/stores/services.ts imports of db/auth)
  </read_first>
  <behavior>
    - loadMySchedule(): reads auth.currentUser?.email; if absent/empty → sets docs = [], isLoading = false, error = null, and returns (no throw, no query).
    - Otherwise builds myEmailLower = email.toLowerCase(), runs query(collectionGroup(db,'rehearseAccess'), where('assignedEmailsLower','array-contains', myEmailLower), orderBy('serviceDate','asc')) via getDocs, maps each snapshot doc to its data() plus orgId taken from ref.parent.parent.id (so a doc's own field is not trusted for scoping and Phase 127 has the orgId it will need).
    - On query rejection: isLoading = false, error set to a truthy value, docs left empty — never rethrows to the caller.
    - isLoading is true only while the query is in flight.
  </behavior>
  <action>
    Create src/stores/mySchedule.ts as a Pinia store (defineStore) exposing reactive state `docs` (RehearseAccessDoc[] with a resolved orgId), `isLoading` (boolean), `error` (string|null), and an async action `loadMySchedule()` implementing the behavior above. Import collectionGroup/query/where/orderBy/getDocs from firebase/firestore and db/auth from '@/firebase'. Always lowercase the email before using it as the array-contains value (Pitfall 2). Do NOT accept an email argument from the UI — the filter value comes solely from auth.currentUser. Do the grouping/readiness/countdown in the VIEW (Plan 04) via Plan 02's helpers — keep this store limited to the query + load state. Write src/stores/__tests__/mySchedule.test.ts mocking '@/firebase' and firebase/firestore: assert the query filter is built from a lowercased email (feed a mixed-case currentUser.email and inspect the where() args), the empty-email path returns [] without calling getDocs, and a rejected getDocs sets error and leaves docs empty.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/mySchedule.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/__tests__/mySchedule.test.ts` green (lowercasing, empty-email no-query, error path).
    - `npm run type-check` clean.
    - The store never reads an email from any argument or client field — only auth.currentUser.email.
  </acceptance_criteria>
  <done>My Schedule has a thin, tested store that issues exactly the Plan-01-proven query, scoped to the signed-in identity, with orgId retained per doc.</done>
</task>

<task type="auto">
  <name>Task 2: Build-safe placeholder route + view for the Phase 127 Rehearse target</name>
  <files>src/router/index.ts, src/views/VolunteerServicePlaceholderView.vue, src/router/__tests__/router.test.ts</files>
  <read_first>
    - src/router/index.ts lines 154-172 (the volunteer-verify/volunteer-home route entries + meta.isVolunteerRoute idiom to replicate) and lines 205-226 (beforeEach: the org-selection gate exemption at 216)
    - src/views/VolunteerLinkCompleteView.vue lines 1-17 and 126-PATTERNS.md "VolunteerServicePlaceholderView.vue (NEW)" (the dark shell + message idiom to reuse for the placeholder)
    - src/router/__tests__/router.test.ts (the existing createRouter/mock test harness to extend)
  </read_first>
  <action>
    In src/router/index.ts add ONE route alongside the existing volunteer routes: path '/volunteer/service/:serviceId', name 'volunteer-service', component lazy-importing '../views/VolunteerServicePlaceholderView.vue' — a REAL placeholder authored in this task (RESEARCH Pattern 4: never an import() of a nonexistent Phase-127 file), `meta: { requiresAuth: true, isVolunteerRoute: true }`. Add a comment stating Phase 127 replaces the component import ONLY; path/name/meta stay stable. (Do NOT add the /my-schedule route here — it is authored in Plan 04 with MyScheduleView.vue so no route imports a nonexistent file.)

    Create src/views/VolunteerServicePlaceholderView.vue reusing VolunteerLinkCompleteView.vue's dark-shell idiom (min-h-screen bg-gray-950, centered card): a static "This service's Rehearse view is coming soon" message and a router-link back to /my-schedule (a link to a route Plan 04 registers — harmless as a static href even before that route exists; it only resolves once Plan 04 lands, same phase). No data fetch, no spinner needed.

    Extend src/router/__tests__/router.test.ts with an assertion that resolving path '/volunteer/service/anything' matches name 'volunteer-service' (no route "no match").
  </action>
  <verify>
    <automated>npx vitest run src/router/__tests__/router.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/router/__tests__/router.test.ts` green including the new placeholder route-resolution assertion.
    - `npm run build` succeeds (proves no import() targets a nonexistent file — the Phase 125 build-safety lesson).
    - The placeholder route carries a comment that Phase 127 swaps only the component; path/name/meta are stable.
  </acceptance_criteria>
  <done>The Rehearse/Open target route resolves to a real placeholder view and the production build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| volunteer client → Firestore | The store constructs the collectionGroup query; the filter value must be the signed-in identity, not a client parameter |
| volunteer client → router | A forward route must resolve to a real component even before Phase 127 ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-03 | Spoofing / Elevation of Privilege (V5 Input Validation) | mySchedule store query construction | medium | mitigate | The array-contains value is derived only from auth.currentUser.email.toLowerCase(); the store accepts no email argument or client field. Even if bypassed, the Plan-01 rule denies a mismatched value — defense-in-depth. Proven by the lowercasing/empty-email store tests. |
| T-126-06 | Denial of Service / build integrity | /volunteer/service/:serviceId route | low | mitigate | A REAL placeholder component (no import() of a nonexistent Phase-127 file) keeps `vite build` green; verified by the build gate in Task 2 (the guard from Phase 125's lesson). |
| T-126-SC | Tampering (supply chain) | npm installs | n/a | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/mySchedule.test.ts src/router/__tests__/router.test.ts` green.
- `npm run type-check` clean.
- `npm run build` succeeds.
</verification>

<success_criteria>
- The store issues the constrained collectionGroup query scoped to the signed-in email, with loading/error/empty handled (R378).
- The Rehearse/Open target route resolves to a build-safe placeholder Phase 127 will replace (R382).
- The production build is green with the placeholder route registered.
</success_criteria>

<output>
Create `.planning/phases/126-my-schedule-volunteer-home/126-03-SUMMARY.md` when done.
</output>
