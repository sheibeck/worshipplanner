---
phase: 126-my-schedule-volunteer-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/myScheduleGrouping.ts
  - src/utils/myScheduleGrouping.test.ts
  - src/utils/roleChipIcon.ts
  - src/utils/roleChipIcon.test.ts
autonomous: true
requirements: [R380, R381]

must_haves:
  truths:
    - "groupMySchedule buckets rehearseAccess docs into { thisWeek, laterThisMonth, past } by browser-local date (matching ServicesView.vue's own convention) and reports nextUpId = the soonest upcoming serviceId"
    - "countdownLabel returns the exact UI-SPEC copy for each boundary: Today / Tomorrow / In N days (future) and Yesterday / N days ago / Last week / N weeks ago (past)"
    - "readinessOf returns { state, missingCount } as 'ready' (all songs have PDF or MP3) / 'partial' (some do, some don't) / 'waiting' (none do, or zero songs)"
    - "roleChipIcon maps a free-text role name to a valid StageKindIcon glyph name via case-insensitive keyword match, falling back to 'music'"
    - {statement: "countdownLabel copy is correct at the exact 7-day boundary (Last week) and the exact 14-day boundary (N weeks ago), not off-by-one", verification: backstop}
    - {statement: "roleChipIcon selects the intended instrument glyph for the org's real (not just example) role names", verification: backstop}
  artifacts:
    - "src/utils/myScheduleGrouping.ts — groupMySchedule, todayYmd, endOfThisWeekYmd, countdownLabel, readinessOf (pure functions)"
    - "src/utils/myScheduleGrouping.test.ts — grouping-boundary, countdown-boundary, and readiness three-state unit tests"
    - "src/utils/roleChipIcon.ts — keyword→glyph mapping"
    - "src/utils/roleChipIcon.test.ts — keyword and fallback unit tests"
  key_links:
    - "All functions are pure and operate on RehearseAccessDoc/RehearseSong data already returned by the Plan-01-governed read — no I/O, no store, no date library"
    - "roleChipIcon's return values MUST be names StageKindIcon.vue actually renders (guitar/piano/drum/mic/strings/speaker/music), or the glyph silently falls back to a dot"
---

<objective>
Build the pure, independently-unit-testable derivation helpers the My Schedule view consumes: date grouping + countdown copy + readiness state, and the free-text-role-name → instrument-glyph mapping for role chips. Isolating these as pure functions (per the UI-SPEC and RESEARCH) keeps the boundary-value edge cases (exactly 7 / 14 days) under a held-out test instead of drifting inside template conditionals.

Purpose: R380 (grouping/countdown) and R381 (readiness, role-chip icons) have enough boundary conditions that inline logic would silently diverge from the UI-SPEC's copy contract. These functions are the single source of truth for that contract.

Output: `src/utils/myScheduleGrouping.ts` and `src/utils/roleChipIcon.ts`, each with a test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-my-schedule-volunteer-home/126-RESEARCH.md
@.planning/phases/126-my-schedule-volunteer-home/126-PATTERNS.md
@.planning/phases/126-my-schedule-volunteer-home/126-UI-SPEC.md
@src/views/ServicesView.vue
@src/utils/rehearseAccess.ts
@src/components/stage/StageKindIcon.vue
</context>

## Artifacts this phase produces (phase-wide roll-up)

- **Plan 01:** the constrained `list` rule arm, the COLLECTION_GROUP index, the R378 rules test, and `rolesByEmailLower`.
- **Plan 02 (this plan):** pure helpers — `src/utils/myScheduleGrouping.ts` (`groupMySchedule`, `todayYmd`, `endOfThisWeekYmd`, `countdownLabel`, `readinessOf`) and `src/utils/roleChipIcon.ts`, each with unit tests.
- **Plan 03:** the My Schedule store, the `/my-schedule` + `/volunteer/service/:serviceId` routes, the placeholder view, and the post-sign-in landing redirect.
- **Plan 04:** the My Schedule view, the service card, and component tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: myScheduleGrouping.ts — grouping, countdown, readiness (pure)</name>
  <files>src/utils/myScheduleGrouping.ts, src/utils/myScheduleGrouping.test.ts</files>
  <read_first>
    - src/views/ServicesView.vue lines 229-271 (the zero-padded browser-local `todayStr` date-string idiom + upcoming/past `localeCompare` split — the exact convention to reuse; do NOT introduce a date library)
    - src/utils/rehearseAccess.ts (RehearseAccessDoc: serviceId/serviceDate/songs; RehearseSong: attachments with kind)
    - 126-RESEARCH.md Architecture Patterns Pattern 3 (worked function bodies for groupMySchedule/todayYmd/endOfThisWeekYmd) and Code Examples ("Readiness derivation")
    - 126-UI-SPEC.md §3 (This week = today through the coming Saturday inclusive), §6 (readiness three-state rule), and the Copywriting Contract countdown boundary table
  </read_first>
  <behavior>
    - groupMySchedule(docs, now): upcoming = serviceDate >= todayYmd(now) sorted ascending; past = serviceDate < today sorted descending; thisWeek = upcoming with serviceDate <= endOfThisWeekYmd(now) (coming Saturday inclusive; getDay 0=Sun..6=Sat); laterThisMonth = the rest of upcoming; nextUpId = first upcoming serviceId or null.
    - countdownLabel(serviceDate, now): future → 'Today' (0), 'Tomorrow' (1), 'In {N} days' (2-6, and 7+); past → 'Yesterday' (1), '{N} days ago' (2-6), 'Last week' (exactly 7 through 13), '{N} weeks ago' (14+, N = floor(days/7)). Day delta is computed on calendar dates (browser-local, no time-of-day drift).
    - readinessOf(songs): hasMedia(song) = attachments.some(a => a.kind === 'pdf' || a.kind === 'mp3'); ready = every song has media; waiting = zero songs have media OR songs.length === 0; partial = otherwise, missingCount = songs without media.
  </behavior>
  <action>
    Create src/utils/myScheduleGrouping.ts exporting `todayYmd(now?)`, `endOfThisWeekYmd(now?)`, `groupMySchedule(docs, now?)`, `countdownLabel(serviceDate, now?)`, and `readinessOf(songs)` plus a `Readiness` type ({ state: 'ready'|'partial'|'waiting'; missingCount: number }). Reuse the zero-padded browser-local date-string idiom from ServicesView.vue verbatim (no dayjs/date-fns/luxon). Import the RehearseAccessDoc/RehearseSong types from src/utils/rehearseAccess.ts. All functions take `now` (defaulting to `new Date()`) so tests inject a fixed clock. Write src/utils/myScheduleGrouping.test.ts covering: the This-week/Later-this-month/Past bucketing and nextUpId; the countdown boundaries at exactly 7 days (past → 'Last week') and exactly 14 days (past → '2 weeks ago'), plus Today/Tomorrow/In N days; and readinessOf's three states including the zero-songs = 'waiting' edge. These boundary cases are the backstop items in must_haves — assert the exact UI-SPEC copy strings.
  </action>
  <verify>
    <automated>npx vitest run src/utils/myScheduleGrouping.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/myScheduleGrouping.test.ts` green, including the exactly-7-day and exactly-14-day countdown boundary assertions and the zero-songs readiness case.
    - `npm run type-check` clean.
    - No date-library import added anywhere (grep the new file — only `new Date()`/string math).
  </acceptance_criteria>
  <done>Grouping, countdown copy, and readiness state are deterministic pure functions matching the UI-SPEC copy contract, with boundary values held under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: roleChipIcon.ts — free-text role name → StageKindIcon glyph</name>
  <files>src/utils/roleChipIcon.ts, src/utils/roleChipIcon.test.ts</files>
  <read_first>
    - src/components/stage/StageKindIcon.vue (the ACTUAL glyph names it renders: mic-stage, mic, users, guitar, piano, drum, strings, music, plug, speaker, box, cup, tv, bolt, and the fallback dot — the mapping MUST return one of these or the icon silently falls back)
    - 126-UI-SPEC.md Icon Inventory (the keyword→glyph table: guitar/bass→guitar, key/piano→piano, drum→drum, vocal/vox/sing→mic, strings/violin→strings, sound/tech→speaker, else→music)
  </read_first>
  <behavior>
    - Case-insensitive substring match against the role name: contains 'guitar' or 'bass' → 'guitar'; 'key' or 'piano' → 'piano'; 'drum' → 'drum'; 'vocal' or 'vox' or 'sing' → 'mic'; 'strings' or 'violin' → 'strings'; 'sound' or 'tech' → 'speaker'; anything else → 'music'.
    - Every returned string is a glyph StageKindIcon.vue actually renders.
  </behavior>
  <action>
    Create src/utils/roleChipIcon.ts exporting `roleChipIcon(roleName: string): string` implementing the case-insensitive keyword match above, returning 'music' as the generic fallback. Do not render anything — this is a pure string mapping; the view pairs it with the existing StageKindIcon.vue component. Write src/utils/roleChipIcon.test.ts asserting each keyword branch (including a representative real-world role name per branch, e.g. 'Acoustic guitar', 'Harmony vocal', 'Keys', 'Sound tech') and the fallback for an unmatched name like 'Scripture reader'.
  </action>
  <verify>
    <automated>npx vitest run src/utils/roleChipIcon.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/roleChipIcon.test.ts` green across all keyword branches + fallback.
    - `npm run type-check` clean.
    - Every value the function can return is present as a rendered `name ===` branch in StageKindIcon.vue.
  </acceptance_criteria>
  <done>Role names resolve to valid instrument glyphs with a safe fallback; mismatches are cosmetic-only (backstop), never a runtime error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side functions operating only on data already returned by the Plan-01-governed rehearseAccess read; they introduce no new read/write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-09 | Tampering | myScheduleGrouping / roleChipIcon | low | accept | Pure, side-effect-free functions with no I/O; incorrect output is a display bug (covered by unit tests), not a security exposure. |
| T-126-SC | Tampering (supply chain) | npm installs | n/a | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx vitest run src/utils/myScheduleGrouping.test.ts src/utils/roleChipIcon.test.ts` green.
- `npm run type-check` clean.
- No new runtime dependency introduced.
</verification>

<success_criteria>
- Grouping/countdown/readiness match the UI-SPEC copy contract with boundary values under test (R380, R381).
- Role names resolve to valid StageKindIcon glyphs with a music fallback (R381).
</success_criteria>

<output>
Create `.planning/phases/126-my-schedule-volunteer-home/126-02-SUMMARY.md` when done.
</output>
