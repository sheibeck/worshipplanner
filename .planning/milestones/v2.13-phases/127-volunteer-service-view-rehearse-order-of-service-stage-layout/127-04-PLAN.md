---
phase: 127-volunteer-service-view-rehearse-order-of-service-stage-layout
plan: 04
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - src/components/rehearse/VolunteerOrderOfService.vue
  - src/components/rehearse/VolunteerStageLayoutTab.vue
  - src/components/rehearse/__tests__/VolunteerOrderOfService.test.ts
  - src/components/rehearse/__tests__/VolunteerStageLayoutTab.test.ts
autonomous: true
requirements: [R392, R393]
must_haves:
  truths:
    - "The Order of Service tab renders the running order read-only from the projection's orderOfService list, re-using ShareView.vue's per-kind slot branching (SONG/SCRIPTURE/HYMN/PRAYER/MESSAGE/ANNOUNCEMENTS/MISC) re-themed to dark gray-950 tokens — NOT a re-mount of ShareView.vue (R392)."
    - "The Order tab renders a 'Who's Serving' card from roleAssignments (roleName + personNames), matching ShareView's shape; a role with no people shows '[not assigned]'."
    - "No per-item free-text is ever rendered — the tab reads only the allowlisted orderOfService/roleAssignments fields the Plan 01 projection carries; no template branch reads a notes/body field (R346/SEC-S-04 discipline carried forward)."
    - "Empty order renders 'This service's order hasn't been built yet.'"
    - "The Stage Layout tab embeds StageLayoutView with theme='dark' :print='false' fed from stageLayout.elements — zero marker-position remapping, reusing the component verbatim so editing/locked/share/this-view render identically (v2.7 WYSIWYG bug avoidance, R393)."
    - "Empty stage renders 'No stage layout has been set up for this service.' (verbatim from ShareView)."
    - "The Stage Layout tab handles narrow viewports via StageLayoutView's own existing horizontal-scroll behavior — no separate mobile layout is built."
  artifacts:
    - src/components/rehearse/VolunteerOrderOfService.vue
    - src/components/rehearse/VolunteerStageLayoutTab.vue
  key_links:
    - "rehearseAccess.orderOfService + .roleAssignments -> VolunteerOrderOfService (re-themed ShareView row anatomy)"
    - "rehearseAccess.stageLayout?.elements -> VolunteerStageLayoutTab -> <StageLayoutView :elements theme='dark'>"
---

<objective>
Build the two read-only tab-body components for tech-team volunteers: `VolunteerOrderOfService.vue` (the running order, R392) and `VolunteerStageLayoutTab.vue` (the v2.7 stage diagram, R393). Both are pure presentational components fed by the Plan 01 projection. The Order tab re-themes ShareView.vue's proven per-kind slot branching to dark tokens (it does NOT re-mount the light/print-oriented ShareView). The Stage tab embeds StageLayoutView.vue verbatim (dark is its default theme) — pure data-plumbing, no marker-position logic.

Purpose: Tech-team members are volunteers too and need the order + stage, not just rehearsal media (the phase's whole reason for three tabs). Reusing the existing renderers avoids re-deriving the v2.7 WYSIWYG marker-centering fix and the R346/SEC-S-04 free-text discipline.

Output: Two Vue SFCs + their unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-volunteer-service-view-rehearse-order-of-service-stage-layout/127-UI-SPEC.md
@.planning/phases/127-volunteer-service-view-rehearse-order-of-service-stage-layout/127-PATTERNS.md
@src/views/ShareView.vue
@src/components/stage/StageLayoutView.vue
@src/utils/rehearseAccess.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VolunteerOrderOfService.vue — read-only running order, re-themed ShareView row anatomy</name>
  <files>src/components/rehearse/VolunteerOrderOfService.vue, src/components/rehearse/__tests__/VolunteerOrderOfService.test.ts</files>
  <read_first>
    - src/views/ShareView.vue:34-135 (the per-kind slot branching for SONG/SCRIPTURE/PRAYER/MESSAGE/ANNOUNCEMENTS/MISC/HYMN + the 'Who's Serving' roleAssignments card + the R346/SEC-S-04 'no per-item free-text' comment convention) — the exact field set/row shapes to re-theme.
    - 127-UI-SPEC.md §4 "Order of Service tab" (single reading column max-w-2xl, dark tokens, kind-label eyebrow, primary/secondary lines, Who's Serving card, empty copy) and §Copywriting Contract.
    - 127-PATTERNS.md "VolunteerOrderOfService.vue" (field-for-field re-theme mapping + the architectural note to optionally factor a shared theme-aware row template).
    - src/utils/rehearseAccess.ts (orderOfService: ServiceSlot[] + roleAssignments shape from Plan 01).
  </read_first>
  <behavior>
    - Given orderOfService with mixed slot kinds, renders each in running order with the kind label eyebrow + the kind-specific line(s) ShareView already renders (song title+key, scripture ref, hymn name+number+verses, prayer/message/announcements/misc labels), re-themed dark.
    - A SONG slot with no songId renders '[not assigned]'.
    - Renders a 'Who's Serving' card from roleAssignments; a role with empty personNames renders '[not assigned]'.
    - Renders NO free-text: no notes/body appears even if a fixture tries to smuggle one (the projection strips it; the template reads no such field).
    - Empty orderOfService renders 'This service's order hasn't been built yet.'
  </behavior>
  <action>
    Create `src/components/rehearse/VolunteerOrderOfService.vue` (`<script setup lang="ts">`). Props: `orderOfService: ServiceSlot[]`, `roleAssignments: RehearseRoleAssignment[]` (import both types from '@/utils/rehearseAccess'). Render a single reading column (max-w-2xl mx-auto per UI-SPEC §4). For each slot, render the kind-label eyebrow + the kind-specific detail line(s), copying ShareView.vue's per-kind branching shape field-for-field but swapping the light/print palette for the dark tokens in UI-SPEC §4 (kind label text-gray-500 uppercase, primary line text-gray-100, secondary line text-gray-400). Carry ShareView's '[not assigned]' fallbacks. Render the 'Who's Serving' card from roleAssignments (role name label + personNames joined, '[not assigned]' when empty) with the dark card treatment. Do NOT add any template branch that reads a per-slot notes/body field — the projection does not carry it and rendering one would reintroduce the R346/SEC-S-04 leak; carry ShareView's 'no per-item free-text' comment discipline forward. Handle the empty-order state with the UI-SPEC copy. Optionally (planner-sanctioned, not required) factor the per-kind row into a small theme-aware template both this component and ShareView could share; if you do NOT extract, do not modify ShareView.vue. Write `VolunteerOrderOfService.test.ts` asserting: each slot kind renders its expected line(s) re-themed; '[not assigned]' for an unassigned SONG slot and an empty role; the Who's Serving card from roleAssignments; and the empty-order copy. Add a case that passes a fixture with an extra stray notes-like field and asserts it is NOT rendered.
  </action>
  <verify>
    <automated>npx vitest run src/components/rehearse/__tests__/VolunteerOrderOfService.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/rehearse/__tests__/VolunteerOrderOfService.test.ts` passes covering: per-kind slot rendering (SONG/SCRIPTURE/HYMN/PRAYER/MESSAGE/ANNOUNCEMENTS/MISC), '[not assigned]' fallbacks, the Who's Serving card, the empty-order copy, and the no-free-text assertion.
    - The component reads only the allowlisted orderOfService/roleAssignments fields (no notes/body branch).
    - `npm run type-check` clean; if ShareView.vue was touched (shared-template extraction), the existing ShareView/services share-link tests stay green.
  </acceptance_criteria>
  <done>Order of Service tab renders the running order + Who's Serving read-only, dark-themed, no free-text; tests + type-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VolunteerStageLayoutTab.vue — embed StageLayoutView read-only, verbatim</name>
  <files>src/components/rehearse/VolunteerStageLayoutTab.vue, src/components/rehearse/__tests__/VolunteerStageLayoutTab.test.ts</files>
  <read_first>
    - src/components/stage/StageLayoutView.vue:7-15 (prop defaults — theme defaults to 'dark', print defaults to false; :elements is the marker input) — embed verbatim, do NOT re-derive marker positioning (v2.7 WYSIWYG bug).
    - src/views/ShareView.vue:16-32 (wide-cap wrapper + the exact empty-stage copy to reuse verbatim).
    - 127-UI-SPEC.md §5 "Stage Layout tab" (max-w-[1600px] wrapper, no extra header, no Print action, empty copy, mobile handled by StageLayoutView's own overflow-x-auto).
    - src/utils/rehearseAccess.ts (stageLayout?: { elements: PublicStageMarker[] } from Plan 01).
  </read_first>
  <behavior>
    - Given stageLayout.elements, renders <StageLayoutView :elements theme='dark' :print='false'> inside the wide-cap wrapper — no marker remapping.
    - Given no stageLayout / zero elements, renders 'No stage layout has been set up for this service.'
    - Adds no Print button (R393 does not ask for one) and no extra header beyond the tab.
    - Narrow-viewport behavior is delegated to StageLayoutView's own horizontal scroll — no separate mobile layout.
  </behavior>
  <action>
    Create `src/components/rehearse/VolunteerStageLayoutTab.vue` (`<script setup lang="ts">`). Prop: `elements?: PublicStageMarker[]` (import from '@/stores/services'), or accept the whole `stageLayout?: { elements }` — planner's choice; normalize to `stageMarkers = elements ?? []`. Render the wide-cap wrapper (max-w-[1600px] mx-auto per UI-SPEC §5); when stageMarkers.length, mount `<StageLayoutView :elements="stageMarkers" theme="dark" :print="false" />` (import from '@/components/stage/StageLayoutView.vue'); else render the empty copy 'No stage layout has been set up for this service.' verbatim. Add NO Print action and NO extra header. Do not touch StageLayoutView.vue. Write `VolunteerStageLayoutTab.test.ts` asserting: StageLayoutView is rendered with theme='dark', print=false, and the passed elements when markers exist; the empty copy renders (and StageLayoutView is absent) when there are no markers; no Print button exists.
  </action>
  <verify>
    <automated>npx vitest run src/components/rehearse/__tests__/VolunteerStageLayoutTab.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/rehearse/__tests__/VolunteerStageLayoutTab.test.ts` passes covering: StageLayoutView embedded with theme='dark' :print='false' + elements passthrough, the empty-stage copy when there are no markers, and absence of any Print button.
    - StageLayoutView.vue is reused verbatim (unmodified); no marker-position logic is re-implemented.
    - `npm run type-check` clean.
  </acceptance_criteria>
  <done>Stage Layout tab embeds StageLayoutView read-only (dark), shows empty copy otherwise, no Print; tests + type-check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01 projection → these read-only tab components | Components render already-allowlisted orderOfService/roleAssignments/stageLayout fields; the free-text strip happened upstream at the projection boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-01 | Information Disclosure | VolunteerOrderOfService free-text render | high | mitigate | Read only the allowlisted orderOfService/roleAssignments fields; add no notes/body template branch; carry ShareView's R346/SEC-S-04 'no per-item free-text' discipline; assert no-free-text by test. |
| T-127-08 | Information Disclosure | VolunteerStageLayoutTab marker note | medium | mitigate | Consume PublicStageMarker (note already stripped in Plan 01) and pass it verbatim to StageLayoutView; introduce no field the projection stripped. |

No new trust boundary — read-only rendering of already-gated projection data. No package installs.
</threat_model>

<verification>
- `npx vitest run src/components/rehearse/__tests__/VolunteerOrderOfService.test.ts` — green.
- `npx vitest run src/components/rehearse/__tests__/VolunteerStageLayoutTab.test.ts` — green.
- `npm run type-check` — clean (and existing ShareView tests green if a shared template was extracted).
- Manual/human-UAT (deferred): confirm Order + Stage render read-only on a real device (127-VALIDATION.md).
</verification>

<success_criteria>
- Order of Service tab renders the running order + Who's Serving read-only, dark-themed, no free-text (R392).
- Stage Layout tab embeds StageLayoutView read-only with zero marker remapping (R393).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04 scope)
- `src/components/rehearse/VolunteerOrderOfService.vue` — NEW: read-only running order + Who's Serving, re-themed ShareView anatomy (R392).
- `src/components/rehearse/VolunteerStageLayoutTab.vue` — NEW: read-only StageLayoutView embed (R393).
- Consumed by the Plan 06 shell as the Order of Service and Stage Layout tab panels.
</artifacts>

<output>
Create `.planning/phases/127-volunteer-service-view-rehearse-order-of-service-stage-layout/127-04-SUMMARY.md` when done.
</output>
