---
phase: 127-volunteer-service-view-rehearse-order-of-service-stage-layout
plan: 03
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - src/components/rehearse/RehearseFileReader.vue
  - src/components/rehearse/RehearseAudioPlayerBar.vue
  - src/components/rehearse/__tests__/RehearseFileReader.test.ts
  - src/components/rehearse/__tests__/RehearseAudioPlayerBar.test.ts
autonomous: true
requirements: [R387, R388, R389, R391]
must_haves:
  truths:
    - "The PDF reader mounts a single <iframe :src=downloadUrl> on desktop with a loading spinner and an error fallback that offers a Download link; page navigation is supplied by the native browser PDF viewer, NOT a custom paginator or any pdf.js dependency (R387)."
    - "The reader resets loading/errored state on attachment CHANGE (not just on open), so selecting a second song's PDF after the first errored shows a fresh spinner, never a stale error (Pitfall 3)."
    - "Print opens the attachment downloadUrl in a new tab (window.open, noopener) — never iframe.contentWindow.print() (cross-origin throw)."
    - "On mobile (mobileLinkFirst), the reader renders NO iframe — an 'Open PDF' primary button (window.open) and a 'Download' secondary button (fetch->blob) instead (R391)."
    - "Empty state (no song/PDF selected): 'Select a song's chart from the list to view it here.'"
    - "The audio player bar mounts exactly ONE native <audio> element (class sr-only, custom transport chrome) — selecting a new track replaces the current one, never layering a second element (R388)."
    - "Play/pause toggles playback; a native <input type=range> seek bar is bound to currentTime; elapsed/total time render as m:ss / m:ss in font-mono (R388)."
    - "The speed pill cycles 1× → 0.9× → 0.75× → 1.25× → 1× and sets audioEl.playbackRate; the Loop toggle sets audioEl.loop and is a real aria-pressed button (R389)."
    - "Player playback position is persisted lightly via localStorage keyed per uid+track (Phase 125 decision) and restored on re-selecting the same track."
    - "Long attachment/track names truncate with a title tooltip (backstop — visual spot-check); the loop/repeat glyph is the one net-new icon (backstop — eyeball once built)."
  artifacts:
    - src/components/rehearse/RehearseFileReader.vue
    - src/components/rehearse/RehearseAudioPlayerBar.vue
  key_links:
    - "RehearseSongDetail @open-pdf(attachment) -> RehearseFileReader :attachment"
    - "RehearseSongDetail @play(track) -> shared active-track state -> RehearseAudioPlayerBar :track"
    - "RehearseAudioPlayerBar single <audio> -> HTMLAudioElement.playbackRate/.loop/.currentTime (native, no library)"
---

<objective>
Build the two Rehearse playback components: `RehearseFileReader.vue` (Column 3 / mobile Screen C — the inline PDF panel with a native-iframe desktop viewer and a link-first mobile variant, R387/R391) and `RehearseAudioPlayerBar.vue` (the single persistent bottom transport with play/pause, seek, elapsed/total time, whole-track speed, and Loop, R388/R389). Both delegate entirely to native Web Platform APIs — the browser's built-in PDF viewer and native `HTMLAudioElement` — with NO new npm dependency (no pdf.js, no audio library).

Purpose: These are the "consume the media" half of the Rehearse tab. The file reader adapts SongFilePreviewModal.vue's iframe loading/error state machine (dropping the modal chrome); the player centralizes SongFilesTab.vue's per-row `<audio>` idiom into one persistent instance with custom transport chrome.

Output: Two Vue SFCs + their unit tests, consuming the RehearseAttachment shape from the Plan 01 projection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-volunteer-service-view-rehearse-order-of-service-stage-layout/127-UI-SPEC.md
@.planning/phases/127-volunteer-service-view-rehearse-order-of-service-stage-layout/127-RESEARCH.md
@.planning/phases/127-volunteer-service-view-rehearse-order-of-service-stage-layout/127-PATTERNS.md
@src/components/SongFilePreviewModal.vue
@src/components/SongFilesTab.vue
@src/utils/rehearseAccess.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RehearseFileReader.vue — native-iframe desktop PDF panel + link-first mobile variant</name>
  <files>src/components/rehearse/RehearseFileReader.vue, src/components/rehearse/__tests__/RehearseFileReader.test.ts</files>
  <read_first>
    - src/components/SongFilePreviewModal.vue:96-198 (iframe markup + loading/errored/onLoad/onError + the watch that resets state on attachment change) — adapt internals, drop Teleport/Transition/backdrop/focus-trap.
    - 127-UI-SPEC.md §2 "Column 3 — reader" and §3 Screen C (mobile link-first: Open PDF + Download buttons, no iframe).
    - 127-RESEARCH.md "Don't Hand-Roll" (native iframe/OS viewer, no custom paginator, window.open for print) and Pitfall 3 (reset on attachment change).
    - src/components/SongFilesTab.vue:443-459 (downloadAttachment — copy verbatim for the mobile Download button).
    - src/utils/rehearseAccess.ts (RehearseAttachment shape).
  </read_first>
  <behavior>
    - Desktop (mobileLinkFirst false): mounts <iframe :src=attachment.downloadUrl> with a loading spinner until @load, and on @error shows 'Couldn't preview this file.' + a Download link.
    - Changing the attachment prop resets loading=true, errored=false BEFORE the new src loads (no stale error carryover).
    - A Print action opens attachment.downloadUrl in a new tab (window.open with noopener,noreferrer).
    - Mobile (mobileLinkFirst true): renders NO iframe — an 'Open PDF' primary button (window.open) and a 'Download' secondary button (fetch->blob), plus a back affordance.
    - No attachment selected: renders 'Select a song's chart from the list to view it here.'
    - The iframe has tabindex=-1 (keyboard-trap boundary, mirrors SongFilePreviewModal).
  </behavior>
  <action>
    Create `src/components/rehearse/RehearseFileReader.vue` (`<script setup lang="ts">`). Props: `attachment?: RehearseAttachment`, `mobileLinkFirst?: boolean`. Emit `back` for the mobile drill-down. Carry the `loading`/`errored` refs and the `watch(() => props.attachment, () => { loading.value = true; errored.value = false })` reset VERBATIM from SongFilePreviewModal.vue (this is the Pitfall-3 guard — do not drop it). Render the desktop branch (`v-if="!mobileLinkFirst"`) with the header (file name truncate + a Print button that calls window.open(attachment.downloadUrl,'_blank','noopener,noreferrer')), the `<iframe :src="attachment.downloadUrl" tabindex="-1" @load @error>`, a loading spinner overlay while `loading`, and the errored fallback (copy + Download link). Build NO custom page-nav chrome — the native viewer supplies it (the chevron-left/right glyphs from UI-SPEC Icon Inventory are only a fallback affordance, not a paginator). Render the mobile branch (`v-else`) with the back button, file name, an 'Open PDF' primary button (window.open) and a 'Download' secondary button (copy downloadAttachment fetch->blob from SongFilesTab.vue). Handle the no-attachment empty state with the UI-SPEC copy. Dark tokens + focus rings per UI-SPEC. Write `RehearseFileReader.test.ts` asserting: iframe present on desktop / absent on mobile-link-first (Open PDF + Download buttons present instead); loading/errored reset when the attachment prop changes; Print/Open call window.open with the noopener args; the empty-state copy with no attachment; iframe tabindex=-1.
  </action>
  <verify>
    <automated>npx vitest run src/components/rehearse/__tests__/RehearseFileReader.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/rehearse/__tests__/RehearseFileReader.test.ts` passes covering: desktop iframe mount + tabindex=-1, loading/errored state RESET on attachment change (Pitfall 3), Print/Open via window.open(noopener), mobile-link-first branch renders Open PDF + Download and NO iframe (R391), and the empty-state copy.
    - No pdf.js / pdfjs-dist import and no custom paginator component exist; package.json is unchanged.
    - `npm run type-check` clean.
  </acceptance_criteria>
  <done>PDF reader shows a native iframe on desktop, link-first buttons on mobile, resets on attachment change, prints via new tab; tests + type-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RehearseAudioPlayerBar.vue — single persistent native player with play/pause, seek, time, speed, loop</name>
  <files>src/components/rehearse/RehearseAudioPlayerBar.vue, src/components/rehearse/__tests__/RehearseAudioPlayerBar.test.ts</files>
  <read_first>
    - 127-UI-SPEC.md §2 "Bottom audio player bar" (transport chrome, speed pill, loop toggle, seek, time, sr-only audio) and §3 "Pinned bottom player" (mobile stacked layout).
    - src/components/SongFilesTab.vue:284-304 (<audio> mount/@error idiom), :466-482 (togglePlay/playingId — adapt to a single shared track), 127-PATTERNS.md "RehearseAudioPlayerBar.vue" (speed/loop/one-track-at-a-time snippets).
    - 127-RESEARCH.md Code Examples (SPEEDS cycle, loop toggle — native HTMLMediaElement, no library) and Don't Hand-Roll (no howler/wavesurfer).
    - 127-UI-SPEC.md §Icon Inventory (loop/repeat is the one net-new glyph; play/pause reused from SongFilesTab).
  </read_first>
  <behavior>
    - Given an active track, mounts exactly one native <audio class=sr-only :src=track.downloadUrl>.
    - Selecting a NEW track replaces the current one (never two <audio> elements playing).
    - Play/pause toggles playback and updates the button aria-label (Play {name} / Pause {name}).
    - A native <input type=range> seek bar is bound to currentTime and updates on timeupdate; elapsed/total render m:ss / m:ss (font-mono) from currentTime/duration.
    - The speed pill cycles [1, 0.9, 0.75, 1.25] and sets audioEl.playbackRate; the label shows the current speed.
    - The Loop toggle (aria-pressed) sets audioEl.loop; active state uses the indigo pill treatment.
    - Playback position is written to localStorage keyed by uid+track id and restored on re-selecting the same track.
  </behavior>
  <action>
    Create `src/components/rehearse/RehearseAudioPlayerBar.vue` (`<script setup lang="ts">`). Props: `track?: RehearseAttachment`, `fixedBottom?: boolean` (mobile pinned layout). Mount ONE `<audio ref="audioEl" :src="track?.downloadUrl" class="sr-only" @error @timeupdate @loadedmetadata>` — never per-row, never v-if-toggled into a second instance. Implement play/pause (calling audioEl.play()/pause()), a native `<input type="range" class="accent-indigo-500" aria-label="Seek">` bound to currentTime with an @input handler that sets audioEl.currentTime, and elapsed/total time formatting to m:ss. Implement the SPEEDS = [1, 0.9, 0.75, 1.25] cycle setting audioEl.playbackRate (label 'Nx' font-mono), and a Loop `aria-pressed` toggle setting audioEl.loop (indigo pill when active) using the net-new repeat glyph from UI-SPEC Icon Inventory. Persist playback position to localStorage keyed by the current auth uid + track id (read the uid from the auth store; keep it light per the Phase 125 decision) and restore it on @loadedmetadata when the same track is re-selected. Provide both the desktop inline layout and the mobile stacked `fixedBottom` layout from UI-SPEC §3. Add focus rings and the aria labels from UI-SPEC §Accessibility. Write `RehearseAudioPlayerBar.test.ts` (mock the <audio> element / HTMLMediaElement play/pause as the repo's other media tests do) asserting: exactly one audio element; switching track replaces the src (not a second element); play/pause toggles + aria-label; the speed cycle order and that playbackRate is set; the loop toggle sets loop + aria-pressed; time formats to m:ss; and that a localStorage entry is written/restored per uid+track.
  </action>
  <verify>
    <automated>npx vitest run src/components/rehearse/__tests__/RehearseAudioPlayerBar.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/rehearse/__tests__/RehearseAudioPlayerBar.test.ts` passes covering: single <audio> element, track replacement (not layering), play/pause + aria-label toggle (R388), seek bound to currentTime + m:ss time formatting (R388), speed cycle 1→0.9→0.75→1.25→1 setting playbackRate (R389), Loop toggle setting loop + aria-pressed (R389), and localStorage position persistence per uid+track.
    - No new npm dependency (no howler/wavesurfer); package.json unchanged.
    - `npm run type-check` clean.
  </acceptance_criteria>
  <done>One persistent native player: play/pause/seek/time + whole-track speed + loop + light position persistence; tests + type-check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| volunteer client → Firebase Storage (download-token URL) | iframe src, window.open, fetch download, and <audio> src are all bearer-URL loads over an already-issued download token; no new storage.rules surface. |
| localStorage (per-uid playback position) | Client-local, non-sensitive numeric position keyed by uid+track; not a cross-user boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-04 | Tampering | print / mobile download / iframe | high | mitigate | window.open(url,'_blank','noopener,noreferrer') for Print/Open (never iframe.contentWindow.print() — cross-origin throw); fetch->blob download copied verbatim from SongFilesTab (124-REVIEW FIX A); iframe tabindex=-1 keeps the cross-origin browsing context out of the keyboard trap. |
| T-127-07 | Information Disclosure | localStorage playback key | low | accept | Store only a numeric position keyed by uid+track id (no media bytes, no PII); light per-device persistence per the Phase 125 decision, cross-device sync explicitly out of scope. |

No new trust boundary — media is fetched via the same download tokens v2.11 already issued. No package installs.
</threat_model>

<verification>
- `npx vitest run src/components/rehearse/__tests__/RehearseFileReader.test.ts` — green.
- `npx vitest run src/components/rehearse/__tests__/RehearseAudioPlayerBar.test.ts` — green.
- `npm run type-check` — clean.
- Manual/human-UAT (deferred, not blocking): real PDF rendering in the desktop iframe + real MP3 playback with speed/loop on desktop AND a phone, and the mobile Open-PDF OS hand-off (127-VALIDATION.md Manual-Only).
</verification>

<success_criteria>
- PDF reader: native iframe (desktop) / link-first (mobile), resets on attachment change, prints via new tab — no pdf.js, no custom paginator (R387, R391).
- Audio player: one persistent native <audio> with play/pause, seek, elapsed/total time, whole-track speed + Loop — no audio library (R388, R389).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03 scope)
- `src/components/rehearse/RehearseFileReader.vue` — NEW: Column 3 desktop iframe reader + mobile Screen C link-first panel (R387, R391).
- `src/components/rehearse/RehearseAudioPlayerBar.vue` — NEW: the single persistent bottom transport (R388, R389) with light per-uid+track position persistence.
- Consumed by the Plan 06 shell (which owns the active-track state and passes it here).
</artifacts>

<output>
Create `.planning/phases/127-volunteer-service-view-rehearse-order-of-service-stage-layout/127-03-SUMMARY.md` when done.
</output>
