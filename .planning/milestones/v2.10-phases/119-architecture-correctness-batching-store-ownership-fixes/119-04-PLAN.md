---
phase: 119-architecture-correctness-batching-store-ownership-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R352, R357]

must_haves:
  truths:
    - "reopenPcWarning renders an accurate Planning-Center-export sentence with no unreachable dead date branch remaining (R352)"
    - "A regression test proves a remote snapshot arriving during an in-flight reorder save is handled safely — no stale overwrite and no lost edit (R357)"
  artifacts:
    - src/views/ServiceEditorView.vue
    - src/views/__tests__/ServiceEditorView.test.ts
  key_links:
    - "the reorder-save path and the remote-merge watcher are proven to coordinate safely by test"
---

<objective>
Two data-flow findings that both live in the 4600-line ServiceEditorView.vue monolith, kept in their
own plan so they never collide with the store/lifecycle plans: remove the unreachable reopenPcWarning
date branch (R352/ARCH-012) and add the regression test that proves the autosave/reorder-save
coordination window is safe (R357/ARCH-013).

Purpose: Close a dead-code correctness finding and the one untested coordination window the review
flagged — surgically, without starting the Phase 120 decomposition.
Output: A trimmed reopenPcWarning computed and a new coordination regression test (plus a minimal
guard only if the test proves one is needed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Per-finding detail (locations, problem, recommendation)
@.planning/milestones/v2.8-phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md
@.planning/phases/119-architecture-correctness-batching-store-ownership-fixes/119-CONTEXT.md

# The monolith this plan surgically touches + its existing test harness
@src/views/ServiceEditorView.vue
@src/views/__tests__/ServiceEditorView.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the unreachable reopenPcWarning date branch (R352)</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
    Fix R352 by REMOVING the dead date branch in the reopenPcWarning computed — this is the
    lower-risk of the two options CONTEXT grants (fix-the-clone vs remove-the-branch). Rationale for
    the choice: the JSON deep-clone idiom that strips the Timestamp is load-bearing across seven
    localService assignment sites and gates the autosave dirty-check (ARCH-023), so altering the clone
    risks the whole save round-trip, whereas deleting one unreachable computed branch is isolated and
    provably safe. Reduce reopenPcWarning to return only the accurate fallback sentence ("This service
    was exported to Planning Center. That plan is still there - reopening here does not change or remove
    it.") and delete the pcExportedAt read, the toDate guard, the `when` variable, and the
    date-formatted return. This is behavior-preserving relative to current runtime behavior — the date
    branch never rendered because the guard was always false. Short comment cites R352 / ARCH-012. Do
    NOT touch any of the JSON.parse(JSON.stringify(...)) deep-clone sites or any other reopen logic —
    stay surgical in the monolith (the decomposition is Phase 120).
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>reopenPcWarning returns the accurate PC-export sentence with no unreachable date branch and no toDate() call on the cloned pcExportedAt; a test asserts the returned string for a PC-exported service; ServiceEditorView suite + type-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression test for the reorder-save / remote-snapshot coordination window (R357)</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts, src/views/ServiceEditorView.vue</files>
  <behavior>
    - Given a mounted ServiceEditorView with a loaded service, when a reorder save is in flight (the manual reorder-save path has called updateService whose promise has not yet resolved), and a DIFFERING remote value is pushed into serviceStore.services (firing the remote-merge watcher), then after the save resolves localService still reflects the user's reordered slots — the in-flight reorder wins; the remote merge does not clobber it and no edit is lost.
    - The test drives the real harness (the same shallowMount + real useSaveStatus + mocked serviceStore this file already uses); updateService is held pending during the window and then resolved.
  </behavior>
  <action>
    Add a regression test to src/views/__tests__/ServiceEditorView.test.ts that exercises the
    coordination window ARCH-013 flags: start a reorder save via the manual reorder-save path (which
    calls autoSave.cleanup(), sets saveStatus 'saving', and awaits serviceStore.updateService), hold
    the updateService promise pending, then push a differing remote value into serviceStore.services so
    the remote-merge watcher runs while the save is in flight, then resolve updateService and assert
    localService still holds the reordered slots (no stale overwrite, no lost edit). If the existing
    own-write-echo (isOwnWriteEcho) + byte-level dirty-check guards already make this pass, the
    deliverable is the passing proving test with NO production change to ServiceEditorView.vue. ONLY if
    the test exposes a genuine unguarded window, close it minimally — e.g. have the reorder-save path
    suppress the remote merge for its in-flight window using the same signal the composable's own saves
    use — and do NOT refactor the monolith beyond that one guard. Short comment cites R357 / ARCH-013.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>a regression test exercises a remote snapshot during an in-flight reorder save and proves safety (no stale overwrite / no lost edit); any guard added (if the test proved one necessary) is scoped to the reorder-save path; full ServiceEditorView suite + type-check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore onSnapshot -> local editor state | A remote snapshot must not silently overwrite the user's unsaved in-flight edit (integrity of the service document round-trip) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-07 | Tampering | reopenPcWarning dead-code removal (R352) | low | accept | Pure removal of an always-false computed branch; no data path, auth, or write behavior changes — cosmetic string only. |
| T-119-08 | Tampering | reorder-save / remote-merge coordination (R357) | low | mitigate | Regression test proves a remote snapshot during an in-flight reorder save does not clobber the user's edit; a minimal reorder-scoped merge-suppression guard is added only if the test proves the window is genuinely unguarded. |

No npm/pip/cargo installs in this plan — no supply-chain (T-*-SC) checkpoint required; no new external surface.
</threat_model>

<verification>
- Full app suite green: `npx vitest run` (excludes firestore rules + render-service + storage.rules per vite.config.ts — a bare run should be fully green).
- Types clean: `npm run type-check` (vue-tsc --build; typechecks test files too, per CLAUDE.md).
</verification>

<success_criteria>
- reopenPcWarning has no unreachable date branch and returns the accurate sentence (R352).
- A regression test proves the remote-snapshot-during-reorder-save window is safe; any guard added is minimal and reorder-scoped (R357).
- ServiceEditorView.vue is not otherwise refactored (Phase 120 owns decomposition); app suite + type-check green.
</success_criteria>

<output>
Create `.planning/phases/119-architecture-correctness-batching-store-ownership-fixes/119-04-SUMMARY.md` when done
</output>
