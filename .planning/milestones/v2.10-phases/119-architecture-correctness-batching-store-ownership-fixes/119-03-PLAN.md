---
phase: 119-architecture-correctness-batching-store-ownership-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServicesView.vue
  - src/components/SongLyricEditor.vue
  - src/components/ScriptureSlideEditor.vue
  - src/views/__tests__/ServicesView.test.ts
  - src/components/__tests__/SongLyricEditor.test.ts
  - src/components/__tests__/ScriptureSlideEditor.test.ts
autonomous: true
requirements: [R353, R354]

must_haves:
  truths:
    - "ServicesView's org-switch watcher explicitly tears down teamsStore locally, matching RosterView/DashboardView/TeamView (R353)"
    - "SongLyricEditor and ScriptureSlideEditor reactively re-subscribe/teardown when their org prop changes while mounted, instead of only subscribing once at mount (R354)"
  artifacts:
    - src/views/ServicesView.vue
    - src/components/SongLyricEditor.vue
    - src/components/ScriptureSlideEditor.vue
  key_links:
    - "watch(() => authStore.orgId) in ServicesView calls teamsStore.unsubscribeAll()"
    - "watch(() => props.orgId, { immediate: true }) in both editors unsubscribes then re-subscribes"
---

<objective>
Close the two org-switch lifecycle defense-in-depth gaps the review flagged: ServicesView's watcher
not tearing down teamsStore locally (R353/ARCH-002), and SongLyricEditor / ScriptureSlideEditor
subscribing once on mount with no reactive re-subscribe if the org changes while mounted
(R354/ARCH-003). Both are behavior-preserving under today's real call sites; the fixes add the local
defense-in-depth the sibling views/components already established.

Purpose: Bring these three surfaces to parity with the church-switch teardown/re-subscribe idiom
(ADR-0066 / quick 260901-lua) so a stale-org bleed cannot appear if a future path changes orgId
without the global reset having run first.
Output: One added teardown call and two reactive re-subscribe watchers, each with a regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Per-finding detail (locations, problem, recommendation)
@.planning/milestones/v2.8-phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md
@.planning/phases/119-architecture-correctness-batching-store-ownership-fixes/119-CONTEXT.md

# Files this plan changes + the sibling teardown/re-subscribe idiom to copy
@src/views/ServicesView.vue
@src/views/RosterView.vue
@src/components/SongLyricEditor.vue
@src/components/ScriptureSlideEditor.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServicesView org-switch watcher tears down teamsStore locally (R353)</name>
  <files>src/views/ServicesView.vue, src/views/__tests__/ServicesView.test.ts</files>
  <action>
    In ServicesView.vue's org-switch watcher (the watch(() => authStore.orgId, ...) block), add
    `teamsStore.unsubscribeAll()` alongside the existing `serviceStore.unsubscribeAll()` before the
    `if (orgId) initStore(orgId)` line — matching the canonical pattern RosterView.vue uses (it tears
    down both rosterStore and teamsStore and clears its retained stopTeamsSeedWatch handle). initStore
    currently creates a local stopTeamsSeedWatch inside its if-block; if that handle is not already
    reachable from the watcher, hoist it to a component-scoped ref and stop+null it in the watcher too,
    mirroring the Roster idiom, so a stale seed watch cannot survive a switch. This is defense-in-depth
    only — the global resetOrgScopedStores() still runs. Short comment cites R353 / ARCH-002. Purely
    additive teardown; no change to subscribe/seed behavior on the current call sites.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServicesView.test.ts</automated>
  </verify>
  <done>ServicesView's org-switch watcher calls teamsStore.unsubscribeAll() (and stops any retained teams seed watch) for parity with RosterView/DashboardView/TeamView; ServicesView tests + type-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Reactive re-subscribe on org-prop change in both editors (R354)</name>
  <files>src/components/SongLyricEditor.vue, src/components/ScriptureSlideEditor.vue, src/components/__tests__/SongLyricEditor.test.ts, src/components/__tests__/ScriptureSlideEditor.test.ts</files>
  <action>
    In SongLyricEditor.vue, replace the `onMounted(() => songLyricsStore.subscribeLyrics(props.orgId,
    props.songId))` with a reactive watcher — watch([() => props.orgId, () => props.songId], () => {
    songLyricsStore.unsubscribeLyrics(); songLyricsStore.subscribeLyrics(props.orgId, props.songId) },
    { immediate: true }) — so an org (or song) change while the component stays mounted tears down and
    re-subscribes; keep the existing onUnmounted unsubscribeLyrics(). In ScriptureSlideEditor.vue apply
    the same reactive re-subscribe to its onMounted subscribeReadings(props.orgId): drive
    store.subscribeReadings via watch(() => props.orgId, () => { store.unsubscribeReadings();
    store.subscribeReadings(props.orgId) }, { immediate: true }); keep the readingId-gated one-time
    getReading fetch that populates the local refs (it may stay in onMounted — only the subscription
    becomes reactive). Use the church-switch re-subscribe idiom (ADR-0066). Short comment cites R354 /
    ARCH-003.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongLyricEditor.test.ts src/components/__tests__/ScriptureSlideEditor.test.ts</automated>
  </verify>
  <done>both editors re-subscribe/teardown when props.orgId changes while mounted; initial mount still subscribes once via { immediate: true }; both component suites + type-check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org context switch -> org-scoped listeners | An org switch must not leave a previous org's Firestore listener streaming into the new org's UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-05 | Information Disclosure | ServicesView teamsStore teardown (R353) | low | mitigate | Adds a local teamsStore.unsubscribeAll() so the previous org's teams collection cannot continue streaming into a new org's Services page even if a future path changes orgId without the global reset — defense-in-depth for the same-tenant-only path that exists today. |
| T-119-06 | Information Disclosure | editor re-subscribe on org change (R354) | low | mitigate | Reactive teardown + re-subscribe ensures a mounted lyric/scripture editor cannot sit showing another org's (or a silently-emptied) subscription after an in-flight org switch. |

No npm/pip/cargo installs in this plan — no supply-chain (T-*-SC) checkpoint required; no new external surface.
</threat_model>

<verification>
- Full app suite green: `npx vitest run` (excludes firestore rules + render-service + storage.rules per vite.config.ts — a bare run should be fully green).
- Types clean: `npm run type-check` (vue-tsc --build; typechecks test files too, per CLAUDE.md).
</verification>

<success_criteria>
- ServicesView's org-switch watcher tears down teamsStore locally, matching its sibling views (R353).
- SongLyricEditor and ScriptureSlideEditor reactively re-subscribe/teardown on props.orgId change (R354).
- Behavior unchanged on today's real call sites; app suite + type-check green.
</success_criteria>

<output>
Create `.planning/phases/119-architecture-correctness-batching-store-ownership-fixes/119-03-SUMMARY.md` when done
</output>
