---
phase: 59-messages-composer-send-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/package.json
  - functions/src/serviceRoles.ts
  - functions/src/serviceRoles.test.ts
autonomous: true
requirements: [R131, R139]
# Legitimacy gate on the resend@6.19.0 pin was DISCHARGED by the orchestrator on 2026-08-14 with direct
# npm-registry diligence (see Task 1) — the SUS/too-new flag applied to 6.20.0, not the 6.19.0 pin. Under
# the v1.7 grant (which authorizes building the send path against the Resend SDK) and the reasonable-safe-
# default rule, this plan runs autonomously. Owner re-confirms before the eventual deploy (PENDING-VERIFICATION).

must_haves:
  truths:
    - "resend is pinned EXACTLY to 6.19.0 in functions/package.json ONLY (not ^, not 6.20.0) and does NOT appear anywhere under src/ — the provider SDK never reaches the client bundle (R131)"
    - "functions/src/serviceRoles.ts is a self-contained port (zero ../src or @/ imports) of resolveServiceRoleAssignments + findQuarterForDate with functions-local type declarations, compiling under functions/tsconfig.json (R131/R139)"
    - "resolveMessageRecipients(assignments, people, selection) returns per-reachable-person roleNames so sendQueuedMessage can render {{their_roles}} per recipient — person A's roleNames differ from person B's (R139)"
    - "The port's dedup + unreachable-count + stale-person-skip behavior matches the client resolveRecipients exactly, proven by tests kept in lockstep"
  artifacts:
    - functions/package.json
    - functions/src/serviceRoles.ts
    - functions/src/serviceRoles.test.ts
  key_links:
    - "functions/ CANNOT import ../src (tsconfig include:['src'], no @/ alias) — the resolver is DUPLICATED, never imported, matching the functions/src/pptxParser.ts precedent"
    - "the ported resolve step stays PURE (types only, no Firestore) so sendQueuedMessageHandler (59-03) does the Admin-SDK loading in the caller and feeds arrays through this pure function"
---

<objective>
Stand up the server-side send-path infrastructure that holds no secret yet: add the Resend SDK to the
functions package (pinned exact, gated behind a human legitimacy check), and PORT the pure recipient
resolver into `functions/src/serviceRoles.ts` so the send trigger (59-03) can re-resolve recipients
server-side with per-recipient roles — without importing anything from the client `src/` tree.

Purpose: R131 (the provider SDK is confined to `functions/` and never enters the client bundle) and R139
(per-recipient "their roles" needs a server-side resolver that yields each reachable person's own role
names). `functions/` cannot import `../src` (separate tsconfig, no `@/` alias — 59-RESEARCH.md Pitfall 2),
so the resolver is duplicated exactly as `functions/src/pptxParser.ts` already duplicates client parsing.
Output: `resend@6.19.0` in `functions/package.json`, new `functions/src/serviceRoles.ts` (self-contained
port + a server-enriched recipient split carrying roleNames), and `functions/src/serviceRoles.test.ts`.

DEPLOY-GATED (v1.7 grant): nothing is deployed and no secret is set this plan. The provider account,
`RESEND_API_KEY`, domain DNS, and `firebase deploy` are owner steps handed over in 59-02/59-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — authoritative decisions, package audit, port instructions
@.planning/phases/59-messages-composer-send-path/59-CONTEXT.md
@.planning/phases/59-messages-composer-send-path/59-RESEARCH.md
@.planning/phases/59-messages-composer-send-path/59-PATTERNS.md

# Exact analogs — the resolver to port, the reachability split, the functions-local-duplicate precedent, and the local-type-decl precedent
@src/utils/serviceRoles.ts
@src/utils/messagingRecipients.ts
@functions/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legitimacy gate on resend@6.19.0 — DISCHARGED by orchestrator diligence (record, do not re-block)</name>
  <action>This legitimacy gate is ALREADY SATISFIED — do NOT stop or wait for a human. The orchestrator performed the due-diligence the gate demanded via direct npm-registry queries on 2026-08-14 (before this plan ran) and recorded the evidence below. Simply carry the evidence into 59-01-SUMMARY.md and route the "owner re-confirms before deploy" note to .planning/PENDING-VERIFICATION.md (do NOT mark the eventual deploy passed). Then proceed to Task 2.</action>
  <what-built>Nothing installed by this task — it records the completed verification. The 59-RESEARCH.md § Package Legitimacy Audit SUS/too-new flag keys on the LATEST release's publish date, which is 6.20.0 (published 2026-08-13). The PIN is 6.19.0, which is NOT too-new. Evidence gathered from the npm registry:
    - resend@6.19.0 published 2026-08-10 (an established prior release; 6.18.0 was 2026-07-21) — 4 days old, not a same-day drop.
    - Maintainers are the official Resend org (jopcmelo@resend.com, carolmoreschi@resend.com, zenorocha = Zeno Rocha / Resend, gabrielmfern, lucasfcosta, vcapretz, et al.).
    - NO install-time scripts: package.json has no preinstall/install/postinstall; the only lifecycle hook is prepublishOnly (runs on the maintainer's publish, never on consumer install). So `npm install` executes no package code.
    - Proper dist.integrity sha512 + canonical registry tarball (registry.npmjs.org/resend), repo github.com/resend/resend-node, ~9.5M weekly downloads — not a slopsquat.
    - Residual-risk posture THIS phase is effectively zero: resend is functions-only, UNDEPLOYED, and vi.mock'd in every test, so the real module never executes until the owner deploys — at which point the owner re-confirms the pin.
  This satisfies the v1.7 grant's "pick the reasonable, SAFE default, state it, continue" rule; it is completed supply-chain diligence, not a self-approved feature verification.</what-built>
  <verify>
    <automated>cd functions && npm view resend@6.19.0 version dist.integrity >/dev/null 2>&1 && echo "resend@6.19.0 exists on the official registry with an integrity hash (legitimacy evidence recorded in-plan)"</automated>
  </verify>
  <done>The resend@6.19.0 legitimacy evidence is recorded in 59-01-SUMMARY.md; the owner-re-confirm-before-deploy note is in PENDING-VERIFICATION.md; execution proceeds to Task 2 without blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Add resend@6.19.0 to functions/package.json (server-only, exact pin)</name>
  <files>functions/package.json</files>
  <action>
    In `functions/package.json` add `"resend": "6.19.0"` to `dependencies` (EXACT, no caret — 59-RESEARCH.md
    Assumption A3 / audit: avoid pulling the <24h-old 6.20.0 release; 6.19.0 is a stable line with the same
    `emails.send`/tags API). Then run the install from the functions directory: `cd functions && npm install
    resend@6.19.0`. Do NOT add resend to the ROOT `package.json` and do NOT import it anywhere under `src/`
    — it is server-side only (R131; 59-CONTEXT.md "the client never imports it"). Do NOT deploy and do NOT
    set any secret. This plan does not yet USE resend (no `import { Resend }` yet) — that import lands in
    59-03; here we only make the dependency present so 59-03's `functions/src/index.ts` can compile against
    it.
  </action>
  <verify>
    <automated>cd functions && node -e "const d=require('./package.json').dependencies; if(d.resend!=='6.19.0'){console.error('resend pin wrong:',d.resend);process.exit(1)} require.resolve('resend'); console.log('resend 6.19.0 present and resolvable')"</automated>
  </verify>
  <done>functions/package.json declares resend exactly "6.19.0"; the module resolves under functions/node_modules; resend appears in NO file under src/ and NOT in the root package.json; nothing deployed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port the pure resolver + server-enriched recipient split into functions/src/serviceRoles.ts</name>
  <files>functions/src/serviceRoles.ts, functions/src/serviceRoles.test.ts</files>
  <behavior>
    - findQuarterForDate returns the first quarter whose serviceDates includes the date, else undefined (verbatim tie-break)
    - resolveServiceRoleAssignments returns, per role sorted by order, effectivePersonIds = overriddenPersonIds ?? scheduledPersonIds (override ?? quarter-scheduled ?? [])
    - resolveMessageRecipients dedups a person assigned to two matching teams/roles to ONE reachable entry
    - a reachable person carries roleNames = the names of every matched role they effectively fill (person A ["guitar"] ≠ person B ["sound","livestream"]) — the R139 per-recipient basis
    - a matched person with email === '' is excluded from reachable and increments unreachableCount
    - a matched personId absent from people (stale/deleted) is silently skipped and does NOT increment unreachableCount
    - includeEveryone matches every assigned role regardless of group; an explicit team list matches only those groups; individualPersonIds are always included
  </behavior>
  <action>
    Create NEW file `functions/src/serviceRoles.ts` porting `src/utils/serviceRoles.ts` (findQuarterForDate
    :24-26, resolveServiceRoleAssignments :33-56) and the reachability split of
    `src/utils/messagingRecipients.ts::resolveRecipients` (:52-83). Per 59-PATTERNS.md (functions/src/serviceRoles.ts
    section) this is a DUPLICATE — do NOT `import ... from '../src'` or `@/...` (they do not resolve in the
    functions build; 59-RESEARCH.md Pitfall 2). Re-declare the MINIMAL functions-local domain types the
    algorithm touches, exactly as `functions/src/index.ts` declares `PptxRenderDoc` locally rather than
    importing client types:
      - `type RoleGroup = 'band' | 'tech' | 'vocals' | 'other'`
      - a local `PortedRole` = { id: string; name: string; group: RoleGroup; order: number }
      - a local `PortedService` = { date: string; roleAssignmentOverrides?: Record<string, string[]> }
      - a local `PortedQuarter` = { serviceDates: string[]; calendar: Record<string, Record<string, string[]>> }
      - a local `PortedPerson` = { id: string; name: string; email: string }
      - a `RecipientSelection` = { teams: RoleGroup[]; individualPersonIds: string[]; includeEveryone: boolean }
    Export `findQuarterForDate`, `resolveServiceRoleAssignments`, the `ResolvedRoleAssignment` interface,
    `resolveMessageRecipients`, and the types. Copy the resolve BODY verbatim (override ?? scheduled ?? []).
    Keep the resolve step PURE — types only, NO Firestore inside it (59-PATTERNS.md Trap): 59-03's
    sendQueuedMessageHandler will Admin-SDK-load the service/quarters/roles/people arrays in the CALLER and
    feed them through this pure function.

    `resolveMessageRecipients(assignments, people, selection)` extends the client split with roleNames:
    walk assignments, and for each person in effectivePersonIds of a matched role, accumulate that role's
    name onto the person's roleNames set (so a person filling two matched roles carries both names). Return
    `{ reachable: { id, name, email, roleNames: string[] }[]; unreachableCount }` with the same dedup /
    empty-email-unreachable / stale-skip semantics as the client. This roleNames enrichment is the ONLY
    behavioral addition over the client resolver — it exists solely to make {{their_roles}} correct per
    recipient (R139); it is unit-tested here and consumed in 59-03.

    Create `functions/src/serviceRoles.test.ts` (picked up by functions/vitest.config.ts `include:
    ['src/**/*.test.ts']`) covering the behavior block with plain object fixtures (no Firestore, no mocks) —
    kept in lockstep with src/utils/__tests__ resolver coverage (59-VALIDATION.md Wave 0). Assert the
    per-person roleNames divergence explicitly (person A ≠ person B) so the R139 basis cannot silently
    regress.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/serviceRoles.test.ts</automated>
  </verify>
  <done>functions/src/serviceRoles.ts compiles under functions/tsconfig.json with zero ../src / @/ imports; findQuarterForDate + resolveServiceRoleAssignments match the client resolver; resolveMessageRecipients returns per-recipient roleNames with correct dedup/unreachable/stale-skip; serviceRoles.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → functions build | The `resend` package is pulled into `functions/node_modules`; a slopsquat or compromised release would run server-side and could hold the future provider key |
| functions/ build ↔ client src/ | The resolver is duplicated; a drift between the two would make the server send list disagree with the composer's "Reaches N" estimate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-01a | Information Disclosure | `resend` reaching the client bundle / exposing the provider surface | high | mitigate | resend is added to functions/package.json ONLY, never imported under src/; Task 1 verify asserts the pin and (by construction) its absence from the client — the whole point of R131's server-confined send path. |
| T-59-01b | Tampering (supply chain) | installing a malicious/typosquatted `resend` | high | mitigate | Exact pin `6.19.0` (not ^, not the too-new 6.20.0). Legitimacy DISCHARGED by orchestrator npm-registry diligence 2026-08-14 (Task 1): official Resend-org maintainers, published 2026-08-10, NO install scripts, valid integrity hash — `npm install` runs no package code. Residual risk ~zero this phase (functions-only, undeployed, vi.mock'd). Owner re-confirms before deploy. |
| T-59-01c | Tampering | ported resolver drifting from the client resolver (server sends to a different set than "Reaches N" implied) | medium | mitigate | The port copies the resolve body verbatim and its test is kept in lockstep with the client resolver's coverage; re-resolution at send time is a correctness feature (roster may change), not a divergence bug. |
| T-59-SC | Tampering | npm/pip/cargo installs | high | mitigate | Exactly one package (`resend`). The audit's SUS/too-new flag applied to 6.20.0; the 6.19.0 pin (published 2026-08-10, no install scripts, official maintainers) was verified legitimate by orchestrator diligence 2026-08-14 and pinned exactly. |
</threat_model>

<verification>
- `cd functions && npx vitest run src/serviceRoles.test.ts` green (R131 confinement basis + R139 per-recipient roleNames).
- `cd functions && npm run build` clean (`tsc`) — the port compiles under functions/tsconfig.json with no `../src`/`@/` imports (proves Pitfall 2 is avoided).
- resend is pinned EXACTLY `6.19.0` in functions/package.json and absent from the root package.json and all of src/ (Task 1 verify).
- The new functions test is self-contained (plain fixtures, no Firebase) so it survives BOTH the node functions run and the root jsdom run (`npx vitest run` app suite stays at its 2-file known-failing baseline).
- HANDOVER (record in the SUMMARY and route to .planning/PENDING-VERIFICATION.md — do NOT mark passed): the owner still must, later (59-02/59-03), create the Resend account, `firebase functions:secrets:set RESEND_API_KEY`, add SPF/DKIM/DMARC DNS, and `firebase deploy`. Nothing is deployed or secret-set here.
</verification>

<success_criteria>
- resend@6.19.0 is a functions-only dependency, confirmed legitimate by a human gate, never in the client bundle (R131).
- functions/src/serviceRoles.ts is a compiling, self-contained port of the pure resolver plus a server-enriched recipient split that carries per-recipient roleNames (R139).
- The port's behavior matches the client resolver (dedup, unreachable, stale-skip) and is proven by a lockstep test.
</success_criteria>

<output>
Create `.planning/phases/59-messages-composer-send-path/59-01-SUMMARY.md` when done.
</output>
