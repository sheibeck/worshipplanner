---
phase: 59-messages-composer-send-path
plan: 04
type: execute
wave: 2
depends_on: [59-02]
files_modified:
  - src/views/serviceEditorActionBar.ts
  - src/views/__tests__/serviceEditorActionBar.test.ts
  - src/components/MessageComposer.vue
  - src/components/__tests__/MessageComposer.test.ts
  - src/views/ServiceEditorView.vue
  - src/components/actionBarItems.ts  # ONLY if Task 1 must add a mail glyph to the ActionBarIcon union; prefer reusing an existing icon (plan-check note 1)
autonomous: true
requirements: [R136, R137, R138, R140, R141]

must_haves:
  truths:
    - "A ✉ Messages action appears in the service action bar to the left of Share, editor-gated, and is DISABLED (not hidden) with a Settings tooltip when isMessagingEnabled() is false (R136; UI-SPEC #0)"
    - "MessageComposer opens with teams-first recipient chips (MESSAGING_TEAM_LABELS) + Everyone + addable individuals, writing the { teams, individualPersonIds, includeEveryone } selection shape (R136)"
    - "A live 'Reaches N people' count (with the muted unreachable note) recomputes on every selection change via resolveRecipients, pluralizing correctly at 0/1/many (R140)"
    - "Three message types seed subject/body defaults behind a dirty-guard; token chips insert {{service_date}}/{{service_link}}/{{their_roles}}/{{song_list}} at the caret and the body stores the RAW template (R137/R138)"
    - "Options write attachServiceLink/sendCopyToSelf/scheduledFor; Send is disabled on zero-reachable / both-empty / past-scheduledFor / in-flight; Send calls the queueServiceMessage wrapper with the selector only — never a raw email list (R141/R131)"
  artifacts:
    - src/components/MessageComposer.vue
    - src/components/__tests__/MessageComposer.test.ts
    - src/views/serviceEditorActionBar.ts
    - src/views/__tests__/serviceEditorActionBar.test.ts
    - src/views/ServiceEditorView.vue
  key_links:
    - "messagingEnabled is a REQUIRED field on ActionBarContext so the compiler forces the ServiceEditorView call site to supply isMessagingEnabled() (mirrors aiEnabled/pcEnabled)"
    - "the composer calls resolveRecipients (client estimate) and stores the RAW token template; the authoritative per-recipient render happens server-side in sendQueuedMessage (59-03), never in the composer"
    - "the client queueServiceMessage wrapper sends { orgId, serviceId, type, subject, body, recipientSelector, options, scheduledFor } — the selector is who-to-resolve intent, no email list crosses to the server"
---

<objective>
Ship the ✉ **Messages** composer: a new `MessageComposer.vue` modal opened from a ✉ action-bar entry in
`ServiceEditorView.vue`, with teams-first recipients, three message types, a subject/body with insertable
merge tokens, a live "Reaches N people" count, the three options, and a Send that calls the
`queueServiceMessage` callable (59-02) with the recipient SELECTOR only.

Purpose: R136 (✉ entry point disabled-when-off + teams-first recipients + individuals), R137 (three message
types seeding defaults), R138 (subject/body + insertable tokens, body stores the raw template), R140 (live
"Reaches N" via the Phase 58 pure resolver), R141 (attach-link / send-me-a-copy / schedule-for-later). The
composer reuses the shipped `PptxImportModal.vue` shell verbatim and the Phase 58
`resolveRecipients`/`MESSAGING_TEAM_LABELS`/`isMessagingEnabled()`; it invents no new visual language
(59-UI-SPEC.md is the authoritative contract).
Output: `buildMessagesItem` + `messagingEnabled`/`onMessages` on the action-bar context, the new
`MessageComposer.vue` + client callable wrapper, the composer mount + context threading in
`ServiceEditorView.vue`, and the two new/extended test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — the LOCKED UI contract, decisions, and analogs with file:line
@.planning/phases/59-messages-composer-send-path/59-UI-SPEC.md
@.planning/phases/59-messages-composer-send-path/59-CONTEXT.md
@.planning/phases/59-messages-composer-send-path/59-PATTERNS.md

# Depends on 59-02: the queueServiceMessage request/response contract the client wrapper must match
@.planning/phases/59-messages-composer-send-path/59-02-SUMMARY.md

# Exact analogs (read the cited ranges before editing)
@src/components/PptxImportModal.vue
@src/components/__tests__/PptxImportModal.test.ts
@src/views/serviceEditorActionBar.ts
@src/utils/messagingRecipients.ts
@src/utils/messaging.ts
@src/components/TeamTagPill.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ✉ Messages action-bar item — editor-gated, disabled-when-off</name>
  <files>src/views/serviceEditorActionBar.ts, src/views/__tests__/serviceEditorActionBar.test.ts</files>
  <behavior>
    - buildActionBarItems('service-order', ctx) includes a 'messages' item for an editor
    - a viewer (ctx.isEditor false) gets NO 'messages' item
    - when ctx.messagingEnabled is false the messages item is present but disabled:true with the Settings tooltip (disabled, NOT hidden — UI-SPEC #0)
    - when ctx.messagingEnabled is true the messages item is enabled and its onClick is ctx.handlers.onMessages
    - the messages item is ordered BEFORE the share item in the service-order tab
  </behavior>
  <action>
    In `src/views/serviceEditorActionBar.ts` add `messagingEnabled: boolean` to `ActionBarContext` (:66-105)
    as a REQUIRED field with a doc-comment matching the aiEnabled/pcEnabled rationale (:70-85: required so
    the compiler forces every call site to supply it), and add `onMessages: () => void` to
    `ActionBarHandlers` (:57-64). Add `buildMessagesItem(ctx): ActionBarItem | undefined` modeled on
    buildShareItem (:209-223): `if (!ctx.isEditor) return undefined` (editor-gated like every mutating
    action). Then — DIVERGING from buildShareItem's hide-on-fail and from the AI hide-don't-disable rule —
    return the item with `disabled: !ctx.messagingEnabled` and `title: ctx.messagingEnabled ? undefined :
    'Turn on Messaging in Settings to email volunteers'`, `onClick: ctx.handlers.onMessages`, label
    'Messages'. State this deliberate disabled-not-hidden divergence in the builder's doc-comment (59-PATTERNS.md
    :142; 59-UI-SPEC.md #0 "DISABLED, not hidden" resolved default). Push it in `buildServiceOrderItems`
    (:225-254) BEFORE the share item (UI-SPEC #0: "left of ⇱ Share"). Pick an existing ActionBarIcon or add
    a mail glyph to the ActionBarIcon union in src/components/actionBarItems.ts if none fits — implementer
    discretion, but keep it consistent with the existing icon set.

    Extend `src/views/__tests__/serviceEditorActionBar.test.ts` covering the behavior block — call
    buildActionBarItems for editor/viewer and messagingEnabled true/false and assert item presence,
    disabled, title, ordering-before-share, and that onClick === the passed onMessages handler.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/serviceEditorActionBar.test.ts</automated>
  </verify>
  <done>ActionBarContext requires messagingEnabled and ActionBarHandlers has onMessages; buildMessagesItem is editor-gated, disabled-with-tooltip when messaging is off, ordered before Share; serviceEditorActionBar.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MessageComposer.vue — recipients, types, tokens, Reaches-N, options, Send</name>
  <files>src/components/MessageComposer.vue, src/components/__tests__/MessageComposer.test.ts</files>
  <behavior>
    - renders team chips from MESSAGING_TEAM_LABELS + an Everyone chip + an Individuals panel; toggling writes { teams, individualPersonIds, includeEveryone }
    - the footer "Reaches N people" recomputes via resolveRecipients on every selection change and pluralizes 0/1/many for both reachable and unreachable counts
    - selecting a message type sets messages.type and seeds subject/body defaults ONLY when the user has not edited them (dirty-guard)
    - clicking a token chip inserts {{token}} at the textarea caret; the body stores the RAW template (never pre-rendered)
    - options write attachServiceLink/sendCopyToSelf; toggling schedule-for-later reveals the datetime input and flips the primary label Send now↔Schedule send
    - Send is disabled when: zero reachable, OR subject AND body both empty, OR schedule-on with empty/past scheduledFor, OR a send is in flight
    - clicking Send calls the queueServiceMessage wrapper with { orgId, serviceId, type, subject, body, recipientSelector, options, scheduledFor } and on success emits 'sent' + closes; on failure shows the inline error and re-enables
    - the payload contains the recipientSelector only — NO raw email list crosses to the server
  </behavior>
  <action>
    Create `src/components/MessageComposer.vue` reusing the PptxImportModal.vue shell VERBATIM (59-PATTERNS.md
    MessageComposer section): the `<Teleport to="body">` + dual `<Transition>` (:2-31), the three-region flex
    shell with `max-w-2xl` (widened from max-w-lg — UI-SPEC #1), the ✕ close button (:39-48), Escape/backdrop/✕
    all calling one `onCancel` that no-ops while a send is in flight (:444-447 guard), and the
    props/emit/watch(open) reset pattern (:226-288). `defineProps<{ open: boolean; service: Service; quarters:
    Quarter[]; roles: Role[]; people: Person[]; orgId: string }>()`, `defineEmits<{ cancel: []; sent:
    [messageId: string] }>()`. Build the 11 sub-components EXACTLY per 59-UI-SPEC.md § Component Inventory:
      - #3 SEND TO team chips (role="checkbox" aria-checked, TeamTagPill idiom, selected indigo / unselected
        gray, Everyone supersedes teams) — each chip shows `{label} · {count}` from resolveRecipients.
      - #4 Individuals panel with ＋ Add someone (person picker over `people`, excluding already-added) and
        removable pills.
      - #5 muted unreachable / open-roles helper line.
      - #6 MESSAGE TYPE segmented control (role="radio" in role="radiogroup") mapping to
        'oneoff'|'reminder'|'share-link', default One-off, seeding subject/body defaults behind a dirty-flag
        guard (share-link pre-inserts {{service_link}}).
      - #7 Subject input, #8 Message textarea (stores the RAW token template) + the four ＋token chips that
        insert `{{service_date}}`/`{{service_link}}`/`{{their_roles}}`/`{{song_list}}` at the caret and
        restore focus; helper "Tokens fill in per person when the email sends."
      - #9 Sample preview labelled SAMPLE (rendered against the first reachable recipient else the current
        user — NOT the final per-person text).
      - #10 Options card: Attach service order link (default checked), Send me a copy (default unchecked),
        Schedule for later (reveals a datetime-local; note the Phase 61 daily-job dependency).
      - #11 Footer: live "Reaches {reachable.length} {people|person}" (aria-live="polite") + muted unreachable
        span; Preview / Cancel secondary buttons; primary Send button whose label is dynamic Send now ↔
        Schedule send, disabled per the four rules (UI-SPEC :316-326), loading state Sending…/Scheduling….
    Use `resolveRecipients(service, quarters, roles, people, selection)` on every selection change for the
    counts (R140) — reuse VERBATIM, do NOT reimplement (59-PATTERNS.md token-render section). For the sample
    preview render only, the client may use buildServiceSnapshot for song_list/service_link; the body still
    stores the raw template (R138/R139 — the authoritative per-recipient render is server-side in 59-03).

    Add the client callable wrapper INSIDE the component (59-PATTERNS.md client-wrapper section; mirror
    PptxImportModal.vue :213-322): `import { httpsCallable } from 'firebase/functions'` + `import { functions
    } from '@/firebase'`, `httpsCallable<QueueMessageRequest, { messageId: string }>(functions,
    'queueServiceMessage')` typed to match the 59-02 contract, invoked with the selector-only payload. Copy
    the error-handling shape (:355-368): on failure set the inline error to the UI-SPEC copy (generic
    "Couldn't send this message. Please try again." and the kill-switch-rejection variant "Messaging is
    turned off for your organization. Turn it on in Settings to send."), re-enable, preserve the draft. On
    success emit 'sent' + close + a ToastHost toast (UI-SPEC :313).

    Create `src/components/__tests__/MessageComposer.test.ts` modeled on PptxImportModal.test.ts (:1-75):
    the `mount` + `flushPromises` + `DOMWrapper` `body()` Teleport helper (:64-74) + `enableAutoUnmount`
    (:70); the `vi.mock('firebase/functions', ...)` + `mockQueueServiceMessage` seam (:27-39); `vi.mock(
    '@/firebase', () => ({ functions: {} }))` (:51-54). Prefer REAL fixtures for the pure resolver (matching
    serviceRoles.test discipline) over mocking @/utils/messagingRecipients. Assert: team/individual selection
    writes the selector shape; "Reaches N" pluralization at 0/1/many (the UI-SPEC :401 zero-one-many
    backstop); token chip insertion at caret with raw-template storage; the disabled-Send states
    (zero-reachable, both-empty, past-scheduledFor); that Send calls queueServiceMessage with { orgId,
    serviceId, recipientSelector, ... } and NO raw email list; and the emitted 'cancel'/'sent'.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/MessageComposer.test.ts</automated>
  </verify>
  <done>MessageComposer renders teams-first recipients + individuals, live Reaches-N (pluralized), three types with dirty-guarded seeding, caret token insertion storing the raw template, the three options with schedule reveal, and the disabled-Send states; Send calls queueServiceMessage with the selector-only payload and emits sent; MessageComposer.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the ✉ item + composer mount into ServiceEditorView.vue</name>
  <files>src/views/ServiceEditorView.vue</files>
  <action>
    In `src/views/ServiceEditorView.vue`: add `import MessageComposer from '@/components/MessageComposer.vue'`
    and `import { isMessagingEnabled } from '@/utils/messaging'` alongside the existing component imports
    (:1520-1526). Add a `const messageComposerOpen = ref(false)` near the other modal refs. In the
    `activeActionItems` computed's ctx object (:2258-2285) add the REQUIRED `messagingEnabled:
    isMessagingEnabled()` field and, in the `handlers` object (:2277-2284), add `onMessages: () => {
    messageComposerOpen.value = true }`. (Both are compile-enforced by Task 1's required-field / handler
    additions — omitting either is a type error by design, 59-PATTERNS.md :144.) Mount `<MessageComposer
    :open="messageComposerOpen" :service="localService" :quarters="..." :roles="..." :people="..."
    :org-id="authStore.orgId" @cancel="messageComposerOpen = false" @sent="messageComposerOpen = false" />`
    near the other teleported modals — passing the quarters/roles/people from the same stores the editor
    already loads (read-only). Do NOT duplicate resolver logic here; the composer owns it.

    This task is primarily compile-enforced wiring (the required ActionBarContext field + the handler). Keep
    the existing ServiceEditorView.test.ts green — if the mount changes any queried DOM, update only what the
    wiring requires; do not restructure the view.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>ServiceEditorView threads messagingEnabled: isMessagingEnabled() and onMessages into buildActionBarItems and mounts MessageComposer bound to messageComposerOpen; npm run type-check is clean (the required field/handler are supplied); ServiceEditorView.test.ts stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (composer) → queueServiceMessage callable | The composer sends the recipient SELECTOR + subject/body/options; it is UX only — the callable (59-02) re-authorizes and the trigger (59-03) re-resolves. No email list or provider key is client-side |
| isMessagingEnabled() UI gate | The disabled ✉ entry point is convenience; the server kill-switch re-check (59-02) is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-04a | Information Disclosure | a raw recipient email list crossing to the server / into the bundle | high | mitigate | The composer sends only the recipientSelector (who-to-resolve intent); the test asserts no email list is in the queueServiceMessage payload; server re-resolves (59-03). |
| T-59-04b | Elevation | a viewer reaching the composer | medium | mitigate | The ✉ item is editor-gated in buildMessagesItem (returns undefined for non-editors); the callable re-checks editor-tier server-side (59-02). |
| T-59-04c | Elevation | composing/sending while messaging is off | medium | mitigate | The ✉ item is disabled+tooltip when isMessagingEnabled() is false; the callable re-reads the kill-switch server-side (59-02) — the UI gate is convenience, the server is the boundary. |
| T-59-04d | Input Validation | both-empty / zero-reachable / past-schedule send | low | mitigate | Send is disabled per the four UI-SPEC rules with explanatory titles; the callable also validates server-side (59-02). |
| T-59-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed this plan (client reuses shipped Phase 58 utils + firebase/functions). No supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/serviceEditorActionBar.test.ts` green (R136 entry-point gating).
- `npx vitest run src/components/__tests__/MessageComposer.test.ts` green (R136/R137/R138/R140/R141 + zero-one-many backstop + selector-only payload).
- `npm run type-check` clean — this is `vue-tsc --build` (typechecks tests too; NOT `-p tsconfig.app.json`, per CLAUDE.md). The required `messagingEnabled` field + `onMessages` handler are compile-enforced.
- `npx vitest run` (full app suite) stays at the 2-file known-failing baseline — no new regressions; ServiceEditorView.test.ts stays green.
- Manual (deferred to owner at /gsd-verify-work 59 per the v1.7 grant; record in .planning/PENDING-VERIFICATION.md, do NOT mark passed): open a service, click ✉ Messages, compare to DESIGN-messaging.md §5a (teams-first + live Reaches-N); turn messaging off in Settings and confirm the ✉ item is disabled with the tooltip.
</verification>

<success_criteria>
- The ✉ Messages action opens the composer, is editor-gated, and is disabled-with-tooltip (not hidden) when messaging is off (R136).
- The composer delivers teams-first recipients + individuals (R136), three types seeding defaults (R137), subject/body + caret-inserted tokens storing the raw template (R138), live pluralized "Reaches N" (R140), and the three options incl. schedule reveal (R141).
- Send calls queueServiceMessage with the recipient selector only (no email list crosses the boundary); the disabled-Send states hold; type-check is clean.
</success_criteria>

<output>
Create `.planning/phases/59-messages-composer-send-path/59-04-SUMMARY.md` when done.
</output>
