---
phase: 62-relock-change-notice-scoped-diff
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/serviceLockDiff.ts
  - src/utils/__tests__/serviceLockDiff.test.ts
autonomous: true
requirements: [R146, R147]

must_haves:
  truths:
    - "fingerprintSlideGroups(groups, serviceId) returns a deterministic { [slotId]: hash } map: for each SlideGroup with g.serviceId === serviceId it sorts g.slides by .order, serializes each entry's sourceRef identity by kind (lyric:songId:sectionId / copyright:songId / scripture:speaker:text:verseRange:scriptureReadingId / imported:importId:innerSlideId:renderedPage / text:title:body / video:videoSrc), joins in order and runs a dependency-free string hash (DJB2/FNV-1a); the SAME groups always produce the SAME map (no Math.random, no object-key-order dependence), and slide TEXT is never resolved — only sourceRef identities (slideGroup.ts invariant 3) (R146 SLIDES)"
    - "diffServiceSnapshots(previous, current, prevFingerprint, currFingerprint) is PURE (no Firestore/Pinia imports, types only) and returns ChangeEntry[] with type ∈ SONG|ORDER|ROLE|NOTES|SLIDES: SONG = a slot matched by stable id whose songId/songTitle changed; ORDER = a shared slot id whose index moved in the already-section-ordered slots array without content change (plus slot add/remove folded in — an added/removed SONG slot also emits SONG); ROLE = a roleAssignments[i].personNames change for a roleId; NOTES = notes string inequality; SLIDES = the two fingerprint maps differ by any key or value (R146)"
    - "affectedTeams follows R147 EXACTLY: a ROLE entry tags ONLY roleAssignments[i].group (the one narrow tag), and SONG/ORDER/NOTES/SLIDES tag broad = every RoleGroup whose current-snapshot roleAssignments entry has personNames.length > 0 (groups with ≥1 assigned person) — a ROLE entry NEVER falls back to broad (R147)"
    - "two identical snapshots with identical fingerprints return [] — the empty-diff result the 62-04 lock hook branches on to overwrite lockSnapshots/current silently with no prompt (R146 empty path)"
    - "ChangeEntry and SlideFingerprint are EXPORTED from serviceLockDiff.ts: ChangeEntry = { type:'SONG'|'ORDER'|'ROLE'|'NOTES'|'SLIDES'; description:string; affectedTeams:RoleGroup[] } and SlideFingerprint = Record<string,string> — the modal (62-03) and the lock hook (62-04) both import them"
  artifacts:
    - src/utils/serviceLockDiff.ts
    - src/utils/__tests__/serviceLockDiff.test.ts
  key_links:
    - "diffServiceSnapshots consumes the ServiceSnapshot shape buildServiceSnapshot already writes to lockSnapshots/current (services.ts:80-95, section-major slot order via orderSlotsBySection :111) — so the diff inputs can never disagree on field shape with what the hook persists"
    - "ServiceSlot carries a stable id via MediaAttachableSlot (service.ts:39-47, D-01) — SONG matches slots by this id and ORDER detects an index move of a shared id; the diff must NEVER key on array index or position, both of which a drag-reorder rewrites"
    - "fingerprintSlideGroups is EXPORTED and pure so 62-04's lock hook calls it on every lock (replacing the Phase 61 slideGroupsFingerprint:null stub) and this plan proves determinism in isolation before any Firestore read exists to obscure a hash bug"
    - "the exported ChangeEntry type is the same shape the functions-local ChangeEntry (62-01) mirrors — client RoleGroup[] here, functions string[] there; the values are the identical enum strings"
---

<objective>
Ship the pure heart of Phase 62: a new `src/utils/serviceLockDiff.ts` module (same no-Firestore/no-Pinia,
types-only convention as `messagingRecipients.ts`/`serviceRoles.ts`) exporting (1) `fingerprintSlideGroups`, a
deterministic per-slide-group hash map over each group's ordered `sourceRef` identities, and (2)
`diffServiceSnapshots`, a pure function over two `ServiceSnapshot`s + two fingerprint maps that returns the typed
`ChangeEntry[]` (SONG/ORDER/ROLE/NOTES/SLIDES) with the R147 affected-teams tagging. Both are unit-tested with
plain fixtures — zero mocking — because they are the phase's heaviest risk surface.

Purpose: R146 (the scoped typed diff) + R147 (each entry team-tagged, ROLE narrow / others broad). Keeping this
pure and in its own Wave-1 plan lets the modal (62-03) and the lock hook (62-04) import a proven `ChangeEntry`
contract, and lets every detection branch + the fingerprint determinism be verified in isolation before any
Vue mount or Firestore seam exists to obscure a bug.

DECISIONS (locked, from 62-CONTEXT.md / 62-RESEARCH.md / 62-PATTERNS.md — do NOT re-derive):
- Detection against the shipped `ServiceSnapshot` (`services.ts:80-95`): SONG matched by stable slot `id`
  (`service.ts:39-47`); ORDER = a shared slot id's index moved in the ALREADY section-ordered `slots` array
  (do NOT re-sort — `buildServiceSnapshot` calls `orderSlotsBySection`, `services.ts:111`); ROLE = a
  `roleAssignments[i].personNames` change for a `roleId`; NOTES = string inequality; SLIDES = the two fingerprint
  maps differ.
- Slot add/remove is folded INSIDE the pure diff so it is unit-tested: a changed slot-id set → an ORDER entry;
  additionally a SONG entry when a SONG slot was added or removed (62-RESEARCH § Open Question 1 / A2).
- `affectedTeams`: ROLE tags EXACTLY `roleAssignments[i].group`; SONG/ORDER/NOTES/SLIDES tag broad = the set of
  `group` values whose CURRENT-snapshot `roleAssignments` entry has `personNames.length > 0`.
- SLIDES fingerprint hashes ordered `sourceRef` IDENTITIES, NOT resolved slide text — slide text is not stored on
  the group doc (`slideGroup.ts:22-36` invariant 3); it resolves live via `sourceRef`. Known limitation A1 (a
  live lyric edit to an unchanged song is NOT caught — acceptable for v1.7's coarse SLIDES entry) — DOCUMENT it
  in a module comment. Use a hand-written DJB2/FNV-1a string hash — NO npm package.

Output: `src/utils/serviceLockDiff.ts` exporting `ChangeEntry`, `SlideFingerprint`, `fingerprintSlideGroups`,
`diffServiceSnapshots`, plus `src/utils/__tests__/serviceLockDiff.test.ts`. No package installed, no Firestore,
nothing deployed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — the detection table, the affectedTeams rule, the SLIDES-fingerprint correction (A1), the fresh-hash flag
@.planning/phases/62-relock-change-notice-scoped-diff/62-CONTEXT.md
@.planning/phases/62-relock-change-notice-scoped-diff/62-RESEARCH.md
@.planning/phases/62-relock-change-notice-scoped-diff/62-PATTERNS.md

# The input shapes and the pure-util analog to copy (read the cited ranges before writing)
@src/stores/services.ts
@src/types/service.ts
@src/types/slideGroup.ts
@src/types/roster.ts
@src/utils/messagingRecipients.ts
@src/utils/__tests__/messagingRecipients.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fingerprintSlideGroups + SlideFingerprint type (deterministic per-group hash)</name>
  <files>src/utils/serviceLockDiff.ts, src/utils/__tests__/serviceLockDiff.test.ts</files>
  <behavior>
    - fingerprintSlideGroups(groups, serviceId) returns a plain object keyed by each in-service group's slotId; a group with a different serviceId contributes no key
    - the SAME groups in the SAME state produce a byte-identical map on repeated calls, and re-ordering the input array (or a group's slides array before its own .order sort) does NOT change any hash — determinism is order-independent on input, order-dependent only on slide .order
    - a group whose slide set is added-to / removed-from / reordered / has an authored text/scripture/video sourceRef field edited produces a DIFFERENT hash for that slotId than before the change
    - two lyric entries that differ only in songId (a song swap) hash differently; the same lyric entry hashes identically across calls
    - an empty or absent group yields a stable hash (never undefined)
  </behavior>
  <action>
    Create `src/utils/serviceLockDiff.ts` opening with the pure-util header verbatim from
    `messagingRecipients.ts:1-9`/`serviceRoles.ts:1-4` ("No Firestore/Pinia/store imports — types only, same
    'pure function in utils/' convention"). Import types only: `SlideGroup`, `GroupSlideEntry`, `SourceRef` from
    `@/types/slideGroup`. Export `type SlideFingerprint = Record<string, string>`.

    Implement per 62-RESEARCH.md § Pattern 2 and 62-PATTERNS.md § fresh-hash flag:
      - a private `refKey(ref: SourceRef): string` switching on `ref.kind` and serializing the discriminating
        identity of EACH variant (lyric → songId+sectionId; copyright → songId; scripture →
        speaker+text+verseRange+scriptureReadingId; imported → importId+innerSlideId+renderedPage; text →
        title+body; video → videoSrc), coalescing optional fields to '' so a missing leaf is stable. The
        SourceRef union is at `slideGroup.ts:165-186` — cover every member (a `switch` with a default is
        acceptable but every current kind must be handled explicitly).
      - a private deterministic string hash `djb2` (or FNV-1a): `let h = 5381; for each char h = ((h<<5)+h+code)|0; return (h>>>0).toString(36)`. NO Math.random, NO Date, NO package.
      - `export function fingerprintSlideGroups(groups: SlideGroup[], serviceId: string): SlideFingerprint` —
        for each group where `g.serviceId === serviceId`, sort a COPY of `g.slides` by `.order`, map each to
        `refKey`, `join('|')`, hash, and assign `out[g.slotId] = hash`. Return the map.
    Add a module comment documenting limitation A1 (the hash is over sourceRef IDENTITIES, so a live lyric edit
    to an unchanged song is NOT flagged SLIDES — acceptable for v1.7's coarse SLIDES entry; broadening later would
    re-couple the lock hook to live text resolution, which is deliberately avoided). Do NOT import `useSlideGroups`
    or any store — this is pure over the passed-in array (62-PATTERNS § TRAP: stable across runs/machines).

    Create `src/utils/__tests__/serviceLockDiff.test.ts` (copy the plain-fixture describe/it structure from
    `messagingRecipients.test.ts` — no Firestore/Pinia mocking). Add a `describe('fingerprintSlideGroups')` block
    asserting every behavior-block row: determinism on repeat calls, order-independence of the input array,
    per-slotId hash change on add/remove/reorder/authored-edit, songId-swap divergence, serviceId filtering, and
    the empty-group stable hash. Build plain `SlideGroup` literals.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/serviceLockDiff.test.ts</automated>
  </verify>
  <done>serviceLockDiff.ts exports SlideFingerprint and a pure, dependency-free fingerprintSlideGroups that hashes each in-service group's ordered sourceRef identities into a deterministic { [slotId]: hash } map (A1 documented); the fingerprint block of serviceLockDiff.test.ts is green; npm run type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diffServiceSnapshots + ChangeEntry type (SONG/ORDER/ROLE/NOTES/SLIDES + affectedTeams)</name>
  <files>src/utils/serviceLockDiff.ts, src/utils/__tests__/serviceLockDiff.test.ts</files>
  <behavior>
    - a slot matched by stable id whose songId or songTitle changed → exactly one SONG entry; an unchanged slot → none
    - a shared slot id whose index moved in the (already section-ordered) slots array without content change → one ORDER entry; identical order → none
    - a SONG slot added or removed → a SONG entry AND an ORDER entry (slot-id set changed); a non-SONG slot added/removed → an ORDER entry only
    - a roleAssignments[roleId].personNames change → one ROLE entry tagging EXACTLY that role's group; identical names → none
    - a notes string change → one NOTES entry; identical notes → none
    - prevFingerprint vs currFingerprint differ by any key or value → one coarse SLIDES entry; identical (incl. both null) → none
    - SONG/ORDER/NOTES/SLIDES affectedTeams = every group with ≥1 assigned person in the CURRENT snapshot's roleAssignments; a ROLE entry's affectedTeams is exactly [that role's group] and never broad
    - two identical snapshots with identical fingerprints → [] (the empty-diff branch)
  </behavior>
  <action>
    In `src/utils/serviceLockDiff.ts` add `import type { RoleGroup } from '@/types/roster'` and
    `import type { ServiceSnapshot } from '@/stores/services'` (type-only — no store instance), plus
    `SongSlot`/`ServiceSlot` types from `@/types/service` as needed. Export
    `interface ChangeEntry { type: 'SONG'|'ORDER'|'ROLE'|'NOTES'|'SLIDES'; description: string; affectedTeams: RoleGroup[] }`.

    Implement `export function diffServiceSnapshots(previous: ServiceSnapshot, current: ServiceSnapshot, prevFingerprint: SlideFingerprint | null, currFingerprint: SlideFingerprint | null): ChangeEntry[]`
    per 62-RESEARCH.md § Pattern 1 and the 62-PATTERNS.md detection table:
      - Compute `broad` first: a private `groupsWithAssignments(current.roleAssignments)` returning the distinct
        `group` values whose entry has `personNames.length > 0` (R147 broad default, derived from CURRENT).
      - Build `Map` of prev/curr slots by `slot.id` (`service.ts:47`). SONG: for a slot id present in BOTH where
        both are kind 'SONG' and `songId`/`songTitle` differ → push a SONG entry (broad). Slot add/remove: for a
        SONG slot id present in only one snapshot → push a SONG entry (broad).
      - ORDER: push ONE ORDER entry (broad) if the ordered sequence of shared slot ids differs (an index move) OR
        the slot-id SET differs (add/remove). Do NOT re-sort — both arrays are already section-major.
      - ROLE: for each `current.roleAssignments` entry, find the prior by `roleId`; if `personNames` differ (order-
        insensitive compare) → push a ROLE entry tagging `[cur.group]` ONLY (the one narrow tag, R147 — never
        broad).
      - NOTES: `previous.notes !== current.notes` → push a NOTES entry (broad).
      - SLIDES: a private `fingerprintsDiffer(prev, curr)` returning true if any key is added/removed or any shared
        key's value changed (treat null as an empty map) → push ONE coarse SLIDES entry (broad).
      - `description` phrasing is at discretion (e.g. "Song changed", "Service order changed",
        "{roleName} assignment changed", "Service notes changed", "Slides changed") — human-readable, no code
        fences. Return the accumulated array; identical inputs → `[]`.

    Extend `serviceLockDiff.test.ts` with a `describe('diffServiceSnapshots')` block asserting every behavior-block
    row using plain `ServiceSnapshot` literals: each of SONG/ORDER/ROLE/NOTES/SLIDES in isolation; ROLE tags
    exactly the changed role's group (assert affectedTeams === [that group], NOT broad); broad = only groups with
    ≥1 assigned person; a SONG-slot add/remove yields SONG+ORDER; a non-SONG add/remove yields ORDER only;
    identical snapshots + identical fingerprints → []; fingerprint null-vs-map and map-vs-map for SLIDES.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/serviceLockDiff.test.ts</automated>
  </verify>
  <done>serviceLockDiff.ts exports ChangeEntry and a pure diffServiceSnapshots detecting SONG/ORDER/ROLE/NOTES/SLIDES (with slot add/remove folded in) and tagging affectedTeams per R147 (ROLE narrow, others broad), returning [] on identical inputs; all diff cases in serviceLockDiff.test.ts are green; npm run type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ServiceSnapshot / SlideGroup data → diffServiceSnapshots / fingerprintSlideGroups | Pure functions over already-org-scoped, client-computed data — no I/O, no auth boundary crossed; the diff is DATA, never evaluated or rendered as a template |
| changeDiff descriptions → the audit doc (downstream, 62-03/62-04) | ChangeEntry.description may embed volunteer NAMES (from roleAssignments[].personNames); it carries NO emails — the same PII surface as the already-shipped snapshot's roleAssignments |
| npm registry → app build | No package installed (the slide hash is a hand-written DJB2/FNV-1a) — zero new supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-02a | Tampering (correctness) | a ROLE entry falls back to broad, over-notifying teams a role change did not touch (R147 violation) | medium | mitigate | The ROLE branch tags EXACTLY [cur.group]; Task 2 asserts affectedTeams === [that group], never the broad set. broad is derived only for SONG/ORDER/NOTES/SLIDES |
| T-62-02b | Tampering (correctness) | a real edit produces no diff entry (a slot add/remove or a fingerprint-detectable slide change is missed), so the notice silently drops it | medium | mitigate | Slot add/remove is folded into ORDER (+SONG for a SONG slot) INSIDE the pure diff and unit-tested; the fingerprint hashes ordered sourceRef identities so add/remove/reorder/authored-edit all change the hash. A1 (live lyric edit not caught) is documented as accepted v1.7 scope |
| T-62-02c | Cryptography (misuse) | the slide hash is mistaken for a security control | low | accept | DJB2 is a NON-security change-detector only — never used for auth, integrity, or signing (62-RESEARCH § V6). A collision's worst case is one missed SLIDES entry that self-corrects on the next re-lock |
| T-62-02d | Information Disclosure | changeDiff descriptions leak volunteer PII beyond the org | low | mitigate | Descriptions carry NAMES only (from roleAssignments), no emails; they live only in the pure return value here and, downstream, inside the org-scoped member-read messages/{id} doc (62-01 threat model) |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | ZERO packages installed this plan — the hash is hand-written (62-RESEARCH.md § Package Legitimacy Audit: no install this phase). No legitimacy checkpoint required |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/serviceLockDiff.test.ts` green — every detection type (SONG/ORDER/ROLE/NOTES/SLIDES) in isolation, slot add/remove folding, ROLE-narrow/others-broad affectedTeams, empty-diff → [], and fingerprint determinism + change detection (R146 / R147).
- `npm run type-check` clean — this is `vue-tsc --build` (typechecks the test file too; NOT `-p tsconfig.app.json`, per CLAUDE.md). The exported ChangeEntry/SlideFingerprint types and the type-only imports of ServiceSnapshot/SourceRef/RoleGroup compile.
- `npx vitest run` (full app suite) stays at the 2-file known-failing baseline (`storage.rules.test.ts`, `RosterView.test.ts`) — this plan adds a new pure module + test only; no existing app-suite file regresses.
- No package installed, no Firestore, nothing deployed — the module is pure and needs no `.env.local`.
</verification>

<success_criteria>
- `serviceLockDiff.ts` exports a pure `diffServiceSnapshots` that returns the typed `ChangeEntry[]` (SONG/ORDER/ROLE/NOTES/SLIDES, slot add/remove folded in) with R147 affected-teams tagging (ROLE narrow, others broad) and `[]` on identical inputs (R146 / R147).
- `fingerprintSlideGroups` returns a deterministic `{ [slotId]: hash }` map over ordered `sourceRef` identities with no npm dependency, proven stable across calls and sensitive to add/remove/reorder/authored-edit (R146 SLIDES; A1 documented).
- `ChangeEntry` and `SlideFingerprint` are exported for 62-03 and 62-04 to import; type-check and the app suite (minus the 2-file baseline) are clean.
</success_criteria>

<output>
Create `.planning/phases/62-relock-change-notice-scoped-diff/62-02-SUMMARY.md` when done.
</output>
