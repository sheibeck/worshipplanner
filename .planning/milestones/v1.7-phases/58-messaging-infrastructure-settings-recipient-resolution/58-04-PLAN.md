---
phase: 58-messaging-infrastructure-settings-recipient-resolution
plan: 04
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - src/views/SettingsView.vue
  - src/views/__tests__/SettingsView.test.ts
autonomous: true
requirements: [R130, R132, R133]

must_haves:
  truths:
    - "An org owner sees a 'Messaging' card on Settings with a global on/off kill-switch that starts unchecked for a fresh org (R130)"
    - "Toggling the kill-switch auto-saves via updateDoc dot-path 'settings.messaging.enabled' and mirror-writes authStore.settings.messaging.enabled (R130)"
    - "When enabled, the card reveals org-level automatic-email defaults (lock-notify, reminder-enabled, reminder days-before select, optional From-name/Reply-to) that auto-save to their dot-paths (R132)"
    - "reminderDaysBefore persists as a NUMBER, not the string from the <select> (R132)"
    - "An always-visible organization-timezone <select> auto-saves 'settings.timezone' (R133)"
  artifacts:
    - src/views/SettingsView.vue
    - src/views/__tests__/SettingsView.test.ts
  key_links:
    - "the Messaging card is byte-consistent with the existing AI Features / Planning Center / Bible Translation cards (same shell, save triad, revert-on-error)"
    - "the kill-switch local ref seeds false (not true) to match DEFAULT_ORG_SETTINGS.messaging.enabled"
---

<objective>
Add the new "Messaging" card to `SettingsView.vue` (appended after the Slide Typography card), mirroring
the existing AI Features / Planning Center / Bible Translation toggle cards exactly. It carries the global
messaging kill-switch (default OFF), a conditionally-revealed "Automatic email defaults" sub-block
(lock-notify default, reminder-enabled default, reminder days-before select, optional From-name/Reply-to
under one Save button), and an always-visible organization-timezone select.

Purpose: ROADMAP success criteria 1 (owner sees/toggles the global switch; fresh org OFF) and 2 (org sets
its local timezone). R130 (kill-switch UI), R132 (org-level automatic-email defaults that per-service
overrides inherit from), R133 (timezone). No composer, no send — plumbing only.
Output: the Messaging card markup + save handlers in SettingsView.vue and its component tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — the locked UI contract + accepted deviations + save-triad analog
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-UI-SPEC.md
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-CONTEXT.md
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-PATTERNS.md

# Depends on 58-01: OrgSettings.messaging + timezone types and loadOrgContext merge must exist
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-01-SUMMARY.md

# Exact analogs in the file being edited
@src/views/SettingsView.vue
@src/views/__tests__/SettingsView.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kill-switch + Automatic-email defaults sub-block</name>
  <files>src/views/SettingsView.vue, src/views/__tests__/SettingsView.test.ts</files>
  <behavior>
    - The kill-switch checkbox's local ref seeds false for a fresh org (matches DEFAULT_ORG_SETTINGS.messaging.enabled)
    - Toggling it calls updateDoc with the single quoted dot-path 'settings.messaging.enabled' = newValue AND sets authStore.settings.messaging.enabled = newValue (mirror-write)
    - On updateDoc rejection the local ref reverts to its prior value and the inline error 'Failed to save. Please try again.' shows
    - The automatic-email defaults sub-block renders only when the kill-switch is on (v-if)
    - lock-notify and reminder-enabled checkboxes auto-save dot-paths 'settings.messaging.lockNotifyDefault' / 'settings.messaging.reminderEnabled' with mirror-write
    - the reminder days-before <select> is revealed only when reminder-enabled is checked, and persists a NUMBER (Number(value)) to 'settings.messaging.reminderDaysBefore'
    - From-name / Reply-to save together under one explicit Save button to 'settings.messaging.fromName' / 'settings.messaging.replyTo'
  </behavior>
  <action>
    Append a new "Messaging" card AFTER the Slide Typography card (the current last card) in
    src/views/SettingsView.vue, reusing the exact card shell (`rounded-lg bg-gray-900 border
    border-gray-800 p-4 mt-6`, `h2 text-sm font-semibold text-gray-300 mb-3`) and the explain-first
    ordering of the AI Features card (~254-301). Copy the save-handler shape of `onToggleAiEnabled`
    (~846-866) verbatim for `onToggleMessagingEnabled`: guard `authStore.orgId && authStore.isEditor`,
    read the local ref, `updateDoc(doc(db,'organizations',authStore.orgId), { 'settings.messaging.enabled':
    newValue })`, then mirror-write `authStore.settings.messaging.enabled = newValue`, 2s Saved! feedback,
    and on catch set the error string and revert the local ref to `!newValue`. IMPORTANT: seed the
    kill-switch local ref default `false` (the one deliberate divergence from the AI/PC refs, per
    58-PATTERNS.md) so a fresh org renders unchecked.

    Reveal the "Automatic email defaults" sub-block with `v-if="messagingEnabledInput"` wrapped in `mt-6
    pt-6 border-t border-gray-800` (mirrors the PC Integration card's credential-reveal at ~99). Inside:
    lock-notify default checkbox and reminder-enabled default checkbox (each auto-saves its dot-path with
    the same save triad + mirror-write); a reminder days-before `<select>` revealed only when
    reminder-enabled is on (`v-if="reminderEnabledInput"`) with options 1,2,3,5,7,10,14 (default 7) — its
    @change handler MUST wrap the value in `Number(...)` before writing/mirroring so it persists as a
    number, not the string "7" (58-RESEARCH.md Pitfall 5); and the two optional From-name/Reply-to text
    inputs styled like the Organization Name input (`w-full sm:w-80 bg-gray-800 border border-gray-700
    ...`), grouped under ONE explicit `Save` button (reuse the Organization Name explicit-Save pattern;
    empty string clears the field). Per 58-UI-SPEC.md's accepted deviations, keep the CTA label `Save`,
    the `font-medium` button weight, and the 12px (`gap-3`/`mb-3`/`px-3`/`space-y-3`) spacing tokens —
    these are app-wide consistency with the sibling cards, not new visual language. Use the card intro
    copy from 58-UI-SPEC.md's Copywriting Contract.

    Extend src/views/__tests__/SettingsView.test.ts (mount the view with the store seeded, as the existing
    AI/PC/Bible card tests do) to cover the behavior block — assert the updateDoc mock is called with the
    exact dot-path AND that authStore.settings.messaging.<field> reflects the new value (both halves, so
    the mirror-write gap can't hide — 58-RESEARCH.md Pitfall 2), and assert the value passed for
    reminderDaysBefore has `typeof === 'number'`.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/SettingsView.test.ts</automated>
  </verify>
  <done>The Messaging card renders with a default-OFF kill-switch; toggles/selects auto-save their dot-paths and mirror-write the store; reminderDaysBefore persists as a number; From-name/Reply-to save under one button; SettingsView.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Organization timezone select</name>
  <files>src/views/SettingsView.vue, src/views/__tests__/SettingsView.test.ts</files>
  <behavior>
    - An always-visible (NOT gated by the kill-switch) organization-timezone <select> renders the curated 7-zone shortlist
    - Its value reflects authStore.settings.timezone (default 'America/Chicago')
    - Changing it auto-saves updateDoc dot-path 'settings.timezone' and mirror-writes authStore.settings.timezone
    - On save failure the select reverts to the prior value and the inline error shows
  </behavior>
  <action>
    Add the organization-timezone `<select>` to the Messaging card per 58-UI-SPEC.md § Component Inventory
    item 4 — ALWAYS visible (it is conceptually org-wide, NOT gated by the kill-switch), with helper text
    "Used by the scheduled service-link reminder, once messaging is turned on above." Options are the
    curated shortlist labeled by common name, valued by IANA string: America/New_York (Eastern),
    America/Chicago (Central), America/Denver (Mountain), America/Phoenix (Arizona, no DST),
    America/Los_Angeles (Pacific), America/Anchorage (Alaska), Pacific/Honolulu (Hawaii). Auto-save on
    @change using the same triad as `onChangeBibleVersion` (~901-923): updateDoc dot-path
    'settings.timezone', mirror-write authStore.settings.timezone, Saved! feedback, revert-to-prior-value
    on error (restore the previous string, not a two-way flip). Default rendered value is
    authStore.settings.timezone (which resolves to 'America/Chicago' via 58-01's merge).

    Extend src/views/__tests__/SettingsView.test.ts to assert the select persists 'settings.timezone' via
    updateDoc and mirror-writes the store, and that it renders independent of the kill-switch state.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/SettingsView.test.ts</automated>
  </verify>
  <done>The timezone select renders the 7-zone shortlist always-visible, auto-saves 'settings.timezone' with mirror-write, reverts on error; SettingsView.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Settings card) → Firestore `organizations/{orgId}` | Editor writes `settings.messaging.*` / `settings.timezone` via scoped dot-path updateDoc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-04a | Elevation of Privilege | a non-editor writing org settings | medium | mitigate | Every save handler guards `authStore.isEditor` and the inputs are `:disabled="!authStore.isEditor"` (mirrors AI/PC cards); the existing `organizations/{orgId}` rules enforce editor-only writes server-side. |
| T-58-04b | Tampering | whole-`messaging`-map overwrite racing a concurrent Settings save in another tab | medium | mitigate | Write only single quoted dot-path leaves (`'settings.messaging.enabled'` etc.), never the whole map — matches `onToggleAiEnabled`'s exact shape (58-RESEARCH.md Anti-Patterns). |
| T-58-04c | Input Validation | free-text From-name/Reply-to | low | accept | These values are stored only, never sent anywhere this phase; no injection surface. Max-length is an unresolved UI-SPEC item (long-text), left to implementer discretion — not a security gap. |
| T-58-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/SettingsView.test.ts` green (R130/R132/R133 UI).
- `npm run type-check` clean (`vue-tsc --build`, not `-p tsconfig.app.json`).
- `npx vitest run` (full app suite) stays at the 2-file known-failing baseline — no new regressions.
- Manual (deferred to owner at `/gsd-verify-work 58` per the v1.7 grant): eyeball the Messaging card
  against its AI/PC/Bible siblings; confirm a fresh org renders the switch OFF; set a timezone, reload,
  confirm it stuck. Record these as DEFERRED in `.planning/PENDING-VERIFICATION.md`.
</verification>

<success_criteria>
- A "Messaging" card exists on Settings with a default-OFF global kill-switch, an enabled-gated
  automatic-email defaults sub-block, and an always-visible timezone select.
- Every toggle/select auto-saves its scoped dot-path and mirror-writes the store; From-name/Reply-to save
  under one button; reminderDaysBefore persists as a number.
- The card is byte-consistent with the sibling Settings cards (accepted deviations honored, no new visual
  language).
</success_criteria>

<output>
Create `.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-04-SUMMARY.md` when done.
</output>
