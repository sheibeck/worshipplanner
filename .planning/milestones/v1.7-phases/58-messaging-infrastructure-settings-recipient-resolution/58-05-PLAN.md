---
phase: 58-messaging-infrastructure-settings-recipient-resolution
plan: 05
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - src/stores/services.ts
  - src/stores/__tests__/services.test.ts
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R132]

must_haves:
  truths:
    - "A Draft service shows a per-service 'Messaging defaults' panel whose overrides inherit from the org Settings until explicitly changed (R132)"
    - "A locked service's per-service messaging panel is read-only (R132, ROADMAP criterion 3)"
    - "setServiceMessagingDefaults writes only the changed 'messaging.<key>' dot-path(s) + updatedAt, and throws ServiceLockedError when the service is not draft"
    - "reminderDaysBefore is persisted as a number|null, never the raw <select> string"
  artifacts:
    - src/stores/services.ts
    - src/stores/__tests__/services.test.ts
    - src/views/ServiceEditorView.vue
    - src/views/__tests__/ServiceEditorView.test.ts
  key_links:
    - "the panel's inherit-or-override <select> reuses the exact <option value=''>Default (…)</option> idiom already shipped for the per-slot Bible-version override"
    - "@change calls setServiceMessagingDefaults directly (scoped dot-path), NOT updateService — so it bypasses the R036 draft-content guard's affectedKeys() path"
---

<objective>
Deliver the per-service automatic-email default overrides (R132): a new scoped store action
`setServiceMessagingDefaults` writing `services/{id}.messaging.<key>` dot-paths, and a small "Messaging
defaults" panel on the Service Order tab of `ServiceEditorView.vue` with three inherit-or-override selects
(lock-notify, service-link reminder, reminder days-before) that are editable only while the service is in
Draft and read-only once locked.

Purpose: ROADMAP success criterion 3 — a Draft service shows per-service overrides inheriting from
Settings until explicitly changed; a locked service's overrides are read-only. Reuses the exact
inherit-or-override select idiom and Draft-editable/locked-read-only branch already shipped for the
per-scripture-slot Bible-version override. No send happens — only the override storage the auto-send
phases (61/62) will later consume.
Output: `setServiceMessagingDefaults` in services.ts + store test, and the panel in ServiceEditorView.vue
+ component test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — locked action shape + panel UI contract
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-CONTEXT.md
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-UI-SPEC.md
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-PATTERNS.md

# Depends on 58-01: Service.messaging? type + OrgSettings.messaging defaults must exist
@.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-01-SUMMARY.md

# Exact analogs
@src/stores/services.ts
@src/stores/__tests__/services.test.ts
@src/views/ServiceEditorView.vue
@src/views/__tests__/ServiceEditorView.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setServiceMessagingDefaults scoped dot-path store action</name>
  <files>src/stores/services.ts, src/stores/__tests__/services.test.ts</files>
  <behavior>
    - setServiceMessagingDefaults(serviceId, { lockNotifyEnabled: true }) calls updateDoc with only { 'messaging.lockNotifyEnabled': true, updatedAt: <serverTimestamp> } — no other messaging keys touched
    - passing a null leaf writes 'messaging.<key>': null (inherit the org default)
    - a reminderDaysBefore value is written as a number|null, never a raw string
    - when the service's stored status is not 'draft' it throws ServiceLockedError and does NOT call updateDoc
    - the local services.value entry mirrors the applied patch (same mirror pattern as setRoleOverride)
  </behavior>
  <action>
    In src/stores/services.ts add `async function setServiceMessagingDefaults(serviceId, patch)` modeled
    exactly on `setRoleOverride`/`clearRoleOverride` (~442-494): guard `if (!orgId.value) return`; read
    `const stored = storedStatusOf(serviceId)`; `if (stored !== 'draft') throw new
    ServiceLockedError(serviceId, stored, 'set messaging defaults on')`; build an updates map seeded with
    `updatedAt: serverTimestamp()`, then for each `[key, value]` in `patch` set `updates['messaging.'+key]
    = value` (a `null` value is written as null to mean inherit); `await updateDoc(doc(db,'organizations',
    orgId.value,'services',serviceId), updates)`; then mirror the applied patch into the local
    `services.value` entry the same way setRoleOverride does (~462-467). The `patch` type is
    `Partial<{ lockNotifyEnabled: boolean|null; reminderEnabled: boolean|null; reminderDaysBefore:
    number|null }>`. Do NOT route through `updateService` — that would subject the write to the R036
    draft-content guard's `affectedKeys()` check which has no `messaging` carve-out (58-RESEARCH.md
    Anti-Patterns). ADD `setServiceMessagingDefaults` to the store's returned object (mirror the
    `setRoleOverride,` entry ~761).

    Extend src/stores/__tests__/services.test.ts (reuse its setRoleOverride test setup) to cover the
    behavior block, including the not-draft ServiceLockedError case (assert updateDoc was NOT called) and
    a reminderDaysBefore-is-a-number assertion.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts</automated>
  </verify>
  <done>setServiceMessagingDefaults writes only changed 'messaging.<key>' dot-paths + updatedAt, throws ServiceLockedError off-draft, mirrors locally, and is exported from the store; services.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-service "Messaging defaults" panel on the Service Order tab</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <behavior>
    - On a Draft service (canEditService true), three inherit-or-override selects render: Lock notification, Service-link reminder, Reminder days-before
    - Each select's empty-string option reads 'Default (Settings: …)' interpolating the resolved org value; explicit On/Off/number options persist an override
    - selecting the Default option writes null (inherit); selecting an explicit value writes it via setServiceMessagingDefaults
    - the Reminder days-before select shows only when the reminder row resolves to on (explicitly or via inherited default)
    - on a locked service (isEditor && isLocked) and for a viewer, the panel renders a static read-only summary line per field, no editable controls
    - a save failure surfaces the inline 'Failed to save. Please try again.' message
  </behavior>
  <action>
    Add a small "Messaging defaults" card (same shell `rounded-lg bg-gray-900 border border-gray-800 p-4`)
    to the Service Order tab of src/views/ServiceEditorView.vue, near the top alongside other
    service-level metadata (same tier as sermon topic/passage), visible only to `authStore.isEditor`. Reuse
    the EXACT inherit-or-override select idiom and the three-branch Draft-editable/locked-read-only
    structure already shipped for the per-slot Bible-version override at ~1061 / ~1078-1104: `v-if=
    "canEditService"` renders editable selects; `v-else-if="authStore.isEditor && isLocked"` and `v-else`
    (viewer) render a static read-only summary line (`text-sm text-gray-200`, mirroring the read-only
    scripture paragraph at ~1098-1100).

    Editable selects (per 58-UI-SPEC.md § Component Inventory item 2): Lock notification → `Default
    (Settings: {{ on/off }})` | On | Off writing `lockNotifyEnabled` (empty option = null); Service-link
    reminder → same shape writing `reminderEnabled`; Reminder days-before → `Default (Settings: {{ N }}
    days)` | 1 | 2 | 3 | 5 | 7 | 10 | 14 writing `reminderDaysBefore`, shown only when the reminder row
    resolves to on. Each select's `@change` calls `servicesStore.setServiceMessagingDefaults(serviceId,
    patch)` directly (NOT queued into the debounced autosave watcher, matching the Roles tab override
    selects) — map the empty string to `null` and coerce the days value with `Number(...)`. Read the
    resolved org default for each label from `authStore.settings.messaging.*`. The read-only summary line
    shows the effective value with `(default)` vs `(overridden)` per 58-UI-SPEC.md. Reuse the existing
    inline error copy on failure; no new Save button.

    Extend src/views/__tests__/ServiceEditorView.test.ts to cover the behavior block — Draft renders
    editable selects and a change calls setServiceMessagingDefaults with the right patch (including the
    null-on-Default and number-coercion cases); a locked service renders the read-only summary and no
    editable select.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>The Service Order tab shows a Draft-editable / locked-read-only Messaging defaults panel whose selects inherit-or-override via setServiceMessagingDefaults; ServiceEditorView.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Service Order tab) → Firestore `services/{id}` | Editor writes `messaging.*` leaves via scoped dot-path updateDoc while the service is in Draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-05a | Tampering | writing messaging defaults on a locked (non-draft) service | medium | mitigate | `setServiceMessagingDefaults` re-checks `storedStatusOf(serviceId) === 'draft'` and throws `ServiceLockedError` before any write (same guard shape as setRoleOverride); the panel also hides editable controls once locked. |
| T-58-05b | Tampering | whole-`messaging`-map overwrite racing a sibling field write | low | mitigate | The action writes only the changed `messaging.<key>` dot-path leaves + updatedAt, never the whole map. |
| T-58-05c | Elevation of Privilege | a non-editor reaching the write path | low | mitigate | Panel edit controls gate on `canEditService`; server-side `services/{id}` rules enforce editor-only writes. |
| T-58-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/services.test.ts` green (R132 store action).
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` green (R132 panel).
- `npm run type-check` clean (`vue-tsc --build`, not `-p tsconfig.app.json`) — exercises the new
  `Service.messaging?` field from 58-01 across store + component.
- `npx vitest run` (full app suite) stays at the 2-file known-failing baseline.
- Manual (deferred to owner at `/gsd-verify-work 58` per the v1.7 grant): on a Draft service edit an
  override, then lock the service and confirm the panel goes read-only. Record as DEFERRED in
  `.planning/PENDING-VERIFICATION.md`.
</verification>

<success_criteria>
- `setServiceMessagingDefaults` persists only changed `messaging.<key>` dot-paths, enforces Draft-only,
  coerces days to number|null, and is exported from the store.
- The Service Order tab shows a per-service Messaging defaults panel that inherits from Settings, is
  editable only in Draft, and is read-only once locked.
- No override write routes through `updateService` (bypasses the R036 draft-content guard's affectedKeys
  path).
</success_criteria>

<output>
Create `.planning/phases/58-messaging-infrastructure-settings-recipient-resolution/58-05-SUMMARY.md` when done.
</output>
