---
phase: 64-composer-refinements
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - src/components/MessageComposer.vue
  - src/components/__tests__/MessageComposer.test.ts
autonomous: true
requirements: [R152, R153, R154, R155, R156]

must_haves:
  truths:
    - "R152: the '＋ Add someone' affordance is a VISIBLE standalone labeled <select data-testid='add-someone-select'> (no longer a <label>-wrapped hidden select) whose first option is a DISABLED placeholder '＋ Add someone…'; choosing a person routes through the existing onAddIndividual (@change), pushes into selection.individualPersonIds, resets to the placeholder, shows a removable pill, and bumps 'Reaches N'; an empty picker shows a disabled 'No one left to add' and the select itself is disabled (per D-R152)"
    - "R152: the p.active filter on addablePeople is KEPT (Person.active is required/reliable, roster.ts:22 — inactive people SHOULD be hidden); the stated fallback (relax to only-exclude-already-selected) applies ONLY if verification finds real people lacking active"
    - "R153: the sample-preview block renders ALWAYS (the v-if='showPreview' gate, the Preview button, and the showPreview ref are all removed) and updates live as subject/body change — samplePreview is already a reactive computed; the SAMPLE badge + caption stay (per D-R153)"
    - "R154 client: tokenChips drops {{song_list}} and adds {{name}} (final 4 chips: Service date · Service link · Their roles · Name); renderSample fills {{name}} from sampleRecipient.name (?? '[name]') mirroring their_roles; the {{song_list}} fill line stays (harmless, matches the server keeping song_list) so a legacy template still renders in the sample (per D-R154)"
    - "R155 composer: the Send button shows a WHITE in-button spinner (animate-spin, aria-hidden) + 'Sending…'/'Scheduling…' while sending and stays disabled; Cancel is disabled during send; the success toasts.push (and the now-unused useToasts import) are REMOVED — success relies on emit('sent') + close + history; the inline sendError error path is UNCHANGED (per D-R155)"
    - "R156: TYPE_DEFAULTS copy is aligned (oneoff blank; reminder subject 'Reminder: {{service_date}}' + body seeded with {{name}}/{{service_date}}/{{service_link}}; share-link subject 'Service plan for {{service_date}}', body '{{service_link}}'); a new recipientDirty flag (set on any team/individual/everyone change, reset in resetComposer) gates selectType('reminder') setting selection.includeEveryone=true; the subject/body dirty guards are kept; One-off/Share-link leave recipients as-is (per D-R156)"
  artifacts:
    - src/components/MessageComposer.vue
    - src/components/__tests__/MessageComposer.test.ts
  key_links:
    - "The add-person picker reuses the WORKING onAddIndividual handler (:449-454) + the existing pills/removeIndividual (:109-125,456-459) + the reactive resolved/reachableCount (:384-385) — only the affordance (the <label> wrapper) changes, not the mutation; QuarterGrid.vue:172-194 is the visible-select analog"
    - "samplePreview (:526-529) + renderSample (:518-524) already recompute reactively from subject/body/sampleRecipient — R153 is a pure DELETION of the showPreview gate; R154 client adds one fillToken('name', …) line so the always-on preview shows the real recipient name, faithful to the server's {{name}} (64-02)"
    - "Removing the success toasts.push (:592-596) fixes the ToastHost 'Save failed.' misrender (PENDING-VERIFICATION composer-toast defect) — the composer already emit('sent') (:597); the now-unused useToasts import (:274) + const toasts (:324) are removed so type-check stays clean"
    - "recipientDirty mirrors subjectDirty/bodyDirty (:341-342): set true in toggleTeam/toggleEveryone/onAddIndividual/removeIndividual, reset in resetComposer; selectType('reminder') sets includeEveryone (+ clears teams like toggleEveryone) only when !recipientDirty — so a manual recipient choice is never clobbered"
    - "This plan is Wave 2, depends_on 64-01, because BOTH edit src/components/__tests__/MessageComposer.test.ts (64-01 updates the R151 label assertions; this plan adds the R152/R153/R154/R155/R156 behavior tests) — they must not share a wave"
---

<objective>
The big composer pass: five owner-UAT refinements to `src/components/MessageComposer.vue` (and its test),
all in one already-shipped SFC. Make "＋ Add someone" a real working control (R152); make the email preview
live and always-on (R153); fix the merge-token palette — drop `{{song_list}}`, add `{{name}}` — and render
`{{name}}` in the sample (R154 client); give Send a visible in-progress spinner and remove the misrendering
success toast (R155 composer); and align each message type's seeded content, defaulting Reminder to
everyone behind a new dirty guard (R156).

Purpose (SC mapping):
- R152 (SC2) — "＋ Add someone" adds the chosen individual (Reaches N reflects them; removable).
- R153 (SC3) — the preview updates live as subject/body change; no click-to-preview.
- R154 client (SC4) — `{{song_list}}` leaves the palette; `{{name}}` renders the recipient's own name in
  the sample (the authoritative server `{{name}}` render is 64-02; keep client render faithful).
- R155 composer (SC5) — Send shows an in-progress spinner; the "Save failed." success-toast misrender is
  gone.
- R156 (SC6) — One-off / Reminder / Share-link seed distinct subject/body (and Reminder → everyone) behind
  dirty guards.

PENDING-VERIFICATION note: removing the success `toasts.push` (Task 2, R155) RESOLVES the disclosed Phase-59
composer success-toast defect ("⚠ DISCOVERED DEFECT" in `.planning/PENDING-VERIFICATION.md` — `ToastHost.vue`
hard-codes a red "Save failed." prefix, so a success toast reads "Save failed. Message queued…"). When
Phase 64 completes, mark that PENDING item RESOLVED (Option A: drop the redundant success toast).

WAVE 2 / DEPENDS_ON 64-01: this plan and 64-01 BOTH edit `src/components/__tests__/MessageComposer.test.ts`
(64-01 updates the R151 team-label assertions there; this plan adds the R152/R153/R154/R155/R156 behavior
tests). They must run in dependency order — never the same wave — to avoid a same-file collision. This plan
runs after 64-01 lands.

DECISIONS (locked, from 64-CONTEXT.md / 64-RESEARCH.md / 64-UI-SPEC.md / 64-PATTERNS.md):
- R152 control: a visible standalone `<select data-testid="add-someone-select">` (analog QuarterGrid.vue:
  172-194), styled like the app's other selects, first option a DISABLED placeholder `＋ Add someone…`
  (value=""), remaining options `addablePeople` by name; add on `@change` via the UNCHANGED `onAddIndividual`
  (:449-454), reset to placeholder. Empty picker → disabled `No one left to add` + the select itself
  `disabled`. Keep the `p.active` filter (:412); STATED FALLBACK — only if verification shows real roster
  people carrying a falsy/absent `active`, relax to exclude only already-selected ids. Keyboard-accessible
  (native select).
- R153: DELETE the `v-if="showPreview"` gate (:194), the Preview button (:243-246, `data-testid="preview-btn"`),
  the `showPreview` ref (:351), and its reset in `resetComposer` (:629). Render the sample-preview always;
  keep the SAMPLE badge + `sampleCaption` (:195-197). No change to the `samplePreview` computed.
- R154 client: in `tokenChips` (:362-367) remove `{ token: 'song_list', label: 'Song list' }` and add
  `{ token: 'name', label: 'Name' }` (final order: Service date · Service link · Their roles · Name). In
  `renderSample` (:518-524) add `out = fillToken(out, 'name', sampleRecipient.value?.name ?? '[name]')`
  mirroring the `their_roles` line (:521). KEEP the `song_list` fill line (:522) and the `songList` computed
  (:504-510) — harmless and matches the server keeping `song_list` support (a legacy template still renders
  in the sample).
- R155 composer: on the Send button (:253-260), when `sending` render a white spinner span before the label
  (`inline-block h-4 w-4 border-2 border-white/70 border-t-transparent rounded-full animate-spin`,
  `aria-hidden="true"`) with the button content in `inline-flex items-center gap-2`; the button stays
  `disabled` (`sendDisabled` already includes `sending`, :547-553; `sendLabel` already returns 'Sending…'/
  'Scheduling…', :559-562). Add `:disabled="sending"` + `disabled:opacity-50 disabled:cursor-not-allowed`
  to the Cancel button (:247-252). REMOVE the success `toasts.push(...)` block (:592-596) entirely (both
  branches); keep `emit('sent', …)` (:597) and the inline `sendError` catch (:600) UNCHANGED. Also remove
  the now-unused `useToasts` import (:274) and `const toasts = useToasts()` (:324) so type-check stays clean.
- R156: align `TYPE_DEFAULTS` (:371-381) — oneoff `{ subject:'', body:'' }`; reminder subject
  `Reminder: {{service_date}}`, body `Hi {{name}} — a reminder you're scheduled to serve on
  {{service_date}}. Here's the plan: {{service_link}}`; share-link subject `Service plan for
  {{service_date}}`, body `{{service_link}}`. Add a `recipientDirty` ref (default false, mirror
  subjectDirty/bodyDirty :341-342), set `true` in `toggleTeam`/`toggleEveryone`/`onAddIndividual`/
  `removeIndividual` (:431-459), reset in `resetComposer` (:614-630). In `selectType`, when `t ===
  'reminder' && !recipientDirty.value` set `selection.includeEveryone = true` and `selection.teams = []`
  (mirroring `toggleEveryone`). Keep the subject/body dirty guards. One-off / Share-link leave recipients
  as-is.

Output: the refined `MessageComposer.vue` + the extended `MessageComposer.test.ts`. jsdom tests; no deploy,
no `.env.local`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — every composer decision, the analogs, and the client/server token parity note
@.planning/phases/64-composer-refinements/64-CONTEXT.md
@.planning/phases/64-composer-refinements/64-RESEARCH.md
@.planning/phases/64-composer-refinements/64-UI-SPEC.md
@.planning/phases/64-composer-refinements/64-PATTERNS.md

# The disclosed defect resolved by R155 (drop the success toast) — mark RESOLVED when Phase 64 lands
@.planning/PENDING-VERIFICATION.md

# The component to edit — Individuals picker :93-107, sample-preview :193-201, footer buttons :240-261,
# tokenChips :362-367, TYPE_DEFAULTS :371-381, addablePeople :412, mutations :431-459, selectType :462-467,
# renderSample :514-524, samplePreview :526-529, sendDisabled/sendLabel :547-562, onSend :569-604,
# resetComposer :614-630
@src/components/MessageComposer.vue
# The test to extend — fixtures :45-124, chips/add-person :131-192, type-seeding :194-216, tokens :218-231,
# send/toast :292-328 (:327 asserts the toast — flip to not.toHaveBeenCalled)
@src/components/__tests__/MessageComposer.test.ts

# Analog for the visible add-person select + explicit affordance (mirror its shape, not its handler)
@src/components/QuarterGrid.vue
# The spinner idiom to reuse (recolored white for the indigo Send fill)
@src/components/ServiceMessageHistory.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Working add-person picker + always-on live preview + token palette {{name}} (R152 + R153 + R154 client)</name>
  <files>src/components/MessageComposer.vue, src/components/__tests__/MessageComposer.test.ts</files>
  <behavior>
    - The add-someone-select is a standalone visible select (its first option is a disabled placeholder reading '＋ Add someone…'); selecting a person by value pushes a removable pill and 'Reaches N' bumps; removing the pill drops it back
    - When addablePeople is empty (everyone reachable already added), the select is disabled and shows a disabled 'No one left to add' placeholder
    - The sample-preview block exists immediately on mount (no Preview click); there is NO preview-btn; editing the subject updates the rendered sample subject live
    - The token palette contains a 'Name' chip (token-name) and NO 'Song list' chip (token-song_list absent); with {{name}} in the body, the sample renders the sample recipient's own name
  </behavior>
  <action>
    In `src/components/MessageComposer.vue`:
    1. R152 — replace the Individuals-panel picker (:97-107). Remove the `<label>` wrapper. Keep the
       left-hand "Individuals" label `<p>` (:96). Add, right-aligned in the header row, a standalone
       `<select data-testid="add-someone-select">` styled like the app's selects (`bg-gray-800 border
       border-gray-700 rounded-md px-2 py-1 text-xs text-gray-100 focus:outline-none focus:ring-2
       focus:ring-indigo-500`, plus `disabled:opacity-50 disabled:cursor-not-allowed`), bound `@change=
       "onAddIndividual"` (handler UNCHANGED). Its options:
         - a FIRST placeholder option `<option value="" disabled>` whose text is `＋ Add someone…` when
           `addablePeople.length > 0`, else `No one left to add`;
         - `v-for` over `addablePeople` → `<option :value="person.id">{{ person.name }}</option>`.
       Add `:disabled="addablePeople.length === 0"` on the select so the empty state is a quiet disabled
       resting control. Keep the removable pills block (:109-125) and the `addablePeople` `p.active` filter
       (:412) AS-IS. (STATED FALLBACK: only if verification shows real roster people with a falsy/absent
       `active`, relax the filter to `!selection.individualPersonIds.includes(p.id)`.)
    2. R153 — delete the click-to-preview gate. Remove `v-if="showPreview"` from the sample-preview block
       (:194) so it renders always; keep the SAMPLE badge + `sampleCaption` (:195-197) and the
       subject/body lines (:199-200). Remove the Preview button (:243-246, `data-testid="preview-btn"`).
       Remove the `showPreview` ref declaration (:351) and its reset line in `resetComposer` (:629). Do NOT
       touch the `samplePreview` computed — it already recomputes reactively.
    3. R154 client — in `tokenChips` (:362-367) remove `{ token: 'song_list', label: 'Song list' }` and add
       `{ token: 'name', label: 'Name' }` (final order: Service date · Service link · Their roles · Name).
       In `renderSample` (:518-524) add `out = fillToken(out, 'name', sampleRecipient.value?.name ?? '[name]')`
       alongside the existing `their_roles` fill (:521). KEEP the `song_list` fill line (:522) and the
       `songList` computed (:504-510) — do not remove them (harmless; mirrors the server keeping song_list).

    In `src/components/__tests__/MessageComposer.test.ts`:
    4. R152 — the existing add/remove test (:177-192) keeps working (same `add-someone-select` testid, same
       onAddIndividual). ADD: (a) the select's first option is a disabled placeholder whose text contains
       '＋ Add someone' when people are addable; (b) an empty-picker case — mount with a people set where all
       reachable people are already selected (or a props.people of only inactive/added people) and assert the
       select is `disabled` and its placeholder text contains 'No one left to add'. (Use the existing
       `mountComposer`/fixture helpers; construct the empty-picker props from the fixtures.)
    5. R153 — ADD: right after `mountComposer()` (no Preview click), `q('sample-preview').exists()` is TRUE
       and `q('preview-btn').exists()` is FALSE; then fill the subject with a token-free string and assert
       the rendered `sample-preview` subject text reflects it live (updates without any preview toggle).
    6. R154 client — ADD: `q('token-name').exists()` is TRUE and `q('token-song_list').exists()` is FALSE;
       select a team so a sample recipient exists, put `{{name}}` in the body, and assert the sample-preview
       body renders that recipient's name (matches the fixture's reachable[0] name).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/MessageComposer.test.ts</automated>
  </verify>
  <done>The visible add-someone-select adds a removable person + bumps Reaches N and shows a disabled 'No one left to add' when empty; the sample-preview renders on mount with no Preview button and updates live; the token palette has Name and not Song list, and the sample renders {{name}} as the recipient's name; MessageComposer.test.ts green; `npm run type-check` (vue-tsc --build) clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Send-button spinner + drop the success toast + aligned type seeds with recipientDirty (R155 composer + R156)</name>
  <files>src/components/MessageComposer.vue, src/components/__tests__/MessageComposer.test.ts</files>
  <behavior>
    - While a send is in flight, the Send button shows a spinner (an element with class animate-spin inside send-btn), reads 'Sending…' (or 'Scheduling…'), and stays disabled; the Cancel button is disabled
    - On a successful send, NO toast is pushed (the toasts store push is never called) and 'sent' is still emitted with the messageId; on failure the inline send-error still shows and 'sent' is not emitted (unchanged)
    - Selecting Reminder on a clean composer seeds subject 'Reminder: {{service_date}}' and a non-blank body containing {{service_link}}, and sets Everyone (everyone-chip aria-checked true / Reaches N = everyone) when recipients are untouched
    - Selecting Reminder AFTER the user picked a team does NOT flip to Everyone (recipientDirty guard); One-off and Share-link never auto-set Everyone; the subject/body dirty guards still protect an edited draft
  </behavior>
  <action>
    In `src/components/MessageComposer.vue`:
    1. R155 spinner — in the Send button (:253-260), wrap the content in an `inline-flex items-center gap-2`
       and, when `sending`, render before `{{ sendLabel }}` a spinner span:
       `<span v-if="sending" class="inline-block h-4 w-4 border-2 border-white/70 border-t-transparent
       rounded-full animate-spin" aria-hidden="true"></span>`. Leave `:disabled="sendDisabled"` and the
       `@click="onSend"` unchanged (`sendDisabled` already includes `sending`; `sendLabel` already returns
       'Sending…'/'Scheduling…').
    2. R155 Cancel — on the Cancel button (:247-252) add `:disabled="sending"` and
       `disabled:opacity-50 disabled:cursor-not-allowed` to its class, so the footer reads inert during a
       send (`onCancel` already no-ops while sending, :608-609).
    3. R155 toast — in `onSend` (:569-604) REMOVE the success `toasts.push(...)` block (:592-596) entirely
       (both the scheduled and send-now branches). Keep `emit('sent', result.data.messageId)` (:597) and the
       `catch` inline `sendError` path (:600) UNCHANGED. Then remove the now-unused `import { useToasts }`
       (from :274) and `const toasts = useToasts()` (:324) so no unused local remains and type-check stays
       clean. Do not touch the error strings or the kill-switch branch.
    4. R156 seeds — align `TYPE_DEFAULTS` (:371-381):
         - `oneoff: { subject: '', body: '' }` (unchanged);
         - `reminder: { subject: 'Reminder: {{service_date}}', body: "Hi {{name}} — a reminder you're
           scheduled to serve on {{service_date}}. Here's the plan: {{service_link}}" }`;
         - `'share-link': { subject: 'Service plan for {{service_date}}', body: '{{service_link}}' }`.
    5. R156 recipientDirty — add `const recipientDirty = ref(false)` next to `subjectDirty`/`bodyDirty`
       (:341-342). Set `recipientDirty.value = true` at the top of `toggleTeam`, `toggleEveryone`,
       `onAddIndividual` (when an id is actually added), and `removeIndividual` (:431-459). Reset
       `recipientDirty.value = false` in `resetComposer` (:614-630) alongside the other dirty flags. In
       `selectType` (:462-467), after the subject/body dirty-guarded seeding, add:
       `if (t === 'reminder' && !recipientDirty.value) { selection.includeEveryone = true; selection.teams =
       [] }`. One-off and Share-link do not set recipients.

    In `src/components/__tests__/MessageComposer.test.ts`:
    6. R155 spinner — ADD a test: make the callable pend (e.g. `mockQueueServiceMessage.mockReturnValueOnce`
       a promise you resolve manually), select a team + fill a subject, click Send; during the pending
       window assert `q('send-btn')` is disabled, `q('send-btn').find('.animate-spin').exists()` is TRUE,
       `q('send-btn').text()` contains 'Sending', and `q('cancel-btn').element.disabled` is TRUE; then
       resolve + `flushPromises()`.
    7. R155 toast — in the existing 'calls queueServiceMessage with the recipient SELECTOR only' test
       (:292-328), change `:327` from `expect(mockToastPush).toHaveBeenCalledTimes(1)` to
       `expect(mockToastPush).not.toHaveBeenCalled()` and update the `:324` comment ("+ raises a toast" →
       success emits 'sent', no toast). Keep the `emitted('sent')` assertions (:325-326). The failure test
       (:351-365) is unchanged.
    8. R156 — UPDATE the type-seeding test (:194-216): the reminder subject is now non-blank ('Reminder:
       {{service_date}}') so `:200` still holds; change `:201` from `toContain('{{their_roles}}')` to
       `toContain('{{service_link}}')` (present in the new reminder body); `:205` (share-link body contains
       '{{service_link}}') still holds. ADD a recipient-default test: on a clean composer, clicking
       `type-reminder` sets Everyone — assert `q('everyone-chip').attributes('aria-checked')` is 'true' (or
       'Reaches N' equals the everyone count). ADD a guard test: after clicking a team chip first, clicking
       `type-reminder` does NOT set Everyone (`everyone-chip` aria-checked stays 'false'). ADD that clicking
       `type-oneoff` / `type-share-link` never auto-sets Everyone.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/MessageComposer.test.ts</automated>
  </verify>
  <done>The Send button shows a spinner + 'Sending…' and stays disabled while sending, with Cancel disabled; a successful send pushes NO toast and still emits 'sent'; the three message types seed distinct subject/body and Reminder defaults Everyone only when recipients are clean (recipientDirty guard); the subject/body dirty guards hold; the now-unused useToasts import is gone; MessageComposer.test.ts green; `npm run type-check` (vue-tsc --build) clean; the full app suite (`npx vitest run`, ~300s extended timeout — a timeout is not a failure) stays at the 2-file baseline with no new failing file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer (client) → server callable | The composer sends only a recipient SELECTOR (teams/individualPersonIds/includeEveryone) — never a resolved email list; the server re-resolves authoritatively (Phase 58/59). R152/R156 mutate only the selector; the existing test (:318-322) asserts no '@example.com' crosses the wire, and it must stay green |
| Client sample render vs server render ({{name}}) | The always-on preview (R153) + `{{name}}` client fill (R154) is a REPRESENTATIVE sample only; the authoritative per-recipient render is server-side (64-02). Client fills `sampleRecipient.name` — the same field the server passes (`target.name`) — so the preview is faithful, not authoritative |
| npm registry → app build | No package installed — in-component markup/state edits + tests. Zero supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-03a | Information Disclosure | The composer accidentally starts sending resolved emails (e.g. a picker change leaks addresses into the payload) | medium | mitigate | R152 only mutates `selection.individualPersonIds` via the unchanged `onAddIndividual`; the selector-only payload test (:293-322, no '@example.com') is the regression net and stays green |
| T-64-03b | Tampering (correctness) | Removing the success toast leaves an unused `useToasts` import / `toasts` local, or the error-path toast is also removed | medium | mitigate | The action removes the import + const AND keeps the inline `sendError` catch untouched; `npm run type-check` (vue-tsc --build) is the gate; the failure test (:351-365) stays green |
| T-64-03c | Tampering (correctness) | The recipientDirty guard is wired to the wrong mutations, so Reminder clobbers a manual recipient choice (or never defaults Everyone) | medium | mitigate | recipientDirty is set in all four selection mutations and reset in resetComposer; two tests assert BOTH the clean-default (Everyone set) and the guarded case (team-first → Everyone NOT set) |
| T-64-03d | Tampering (correctness) | The new reminder body drops {{their_roles}} and breaks the old type-seeding assertion, or the empty-picker/placeholder changes break the existing add/remove test | low | mitigate | Task 2 updates :201 to a token present in the new body ({{service_link}}); Task 1 keeps the `add-someone-select` testid + `onAddIndividual` so the existing add/remove test (:177-192) stays green |
| T-64-03e | Tampering/Injection | The client sample renders {{name}} into the preview | low | accept | The preview is Vue text interpolation (auto-escaped, not v-html) of a representative sample; no injection surface. The authoritative email is server-side plaintext (64-02) |
| T-64-03-SC | Tampering | npm/pip/cargo installs | low | accept | ZERO packages installed this plan — in-component edits + tests. No legitimacy checkpoint required |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/MessageComposer.test.ts` green — the visible add-person select
  adds/removes + bumps Reaches N and disables on empty (R152); the sample-preview renders on mount with no
  Preview button and updates live (R153); the palette has Name / not Song list and the sample renders
  {{name}} (R154 client); Send shows a spinner + stays disabled with Cancel disabled and pushes NO success
  toast while still emitting 'sent' (R155); the three types seed distinct content and Reminder defaults
  Everyone behind recipientDirty (R156).
- `npm run type-check` clean — `vue-tsc --build` (typechecks the test file; NOT `-p tsconfig.app.json`, per
  CLAUDE.md). The removed `useToasts` import leaves no unused local; the new `recipientDirty` ref and the
  `{{name}}` fill compile.
- `npx vitest run` (full app suite, ~300s extended timeout — a timeout is NOT a failure) stays at the
  2-file known-failing baseline (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`).
  No NEW failing app-suite file. Do NOT use `--dir src` or `--config vitest.rules.config.ts`.
- No deploy, no `.env.local` (jsdom, mocked callable + toasts store).
- Manual (deferred to owner at `/gsd-verify-work 64`): open the ✉ composer — add a person via the visible
  picker and watch Reaches N bump; watch the live preview update as you type and as you switch types; send
  and see the in-button spinner with no "Save failed." toast. Recorded as verification_deferred_human.
- On Phase 64 completion, mark the PENDING-VERIFICATION composer success-toast defect RESOLVED (Option A —
  the success toasts.push is removed here).
</verification>

<success_criteria>
- "＋ Add someone" adds the chosen individual (Reaches N reflects them; removable) via a visible working
  picker (R152 / SC2); the email preview updates live with no click-to-preview (R153 / SC3).
- `{{song_list}}` is gone from the palette and `{{name}}` renders the recipient's own name in the sample
  (R154 client / SC4); Send shows an in-progress spinner and the "Save failed." success-toast misrender is
  gone (R155 composer / SC5).
- One-off / Reminder / Share-link seed distinct subject/body and Reminder defaults recipients to everyone
  behind dirty guards (R156 / SC6); type-check and the app suite (minus the 2-file baseline) are clean.
</success_criteria>

<output>
Create `.planning/phases/64-composer-refinements/64-03-SUMMARY.md` when done.
</output>
