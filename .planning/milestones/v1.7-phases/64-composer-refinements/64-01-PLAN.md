---
phase: 64-composer-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/messagingRecipients.ts
  - src/utils/__tests__/messagingRecipients.test.ts
  - src/components/__tests__/MessageComposer.test.ts
  - src/components/__tests__/ReLockNotifyPrompt.test.ts
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R151]

must_haves:
  truths:
    - "MESSAGING_TEAM_LABELS (src/utils/messagingRecipients.ts:17-22) reads band:'Band', tech:'Tech', vocals:'Vocals', other:'Other' — the raw RoleGroup names matching Volunteer Roles (RolesConfigPanel.vue:119 groupLabels), replacing the v1.7 band→'Worship' / other→'Hosts' remap (R151, per D-R151)"
    - "The composer team chips and ReLockNotifyPrompt team tags re-label to Band/Vocals/Tech/Other automatically — the constant is the single source (MessageComposer.vue:396-400, ReLockNotifyPrompt), NO component markup changes"
    - "The four hard-coded old-label assertions are updated to the new values: messagingRecipients.test.ts:85-90 (object equals Band/Tech/Vocals/Other), MessageComposer.test.ts:140/141/170 (chips contain 'Band'/'Other' and /Band·1/), ReLockNotifyPrompt.test.ts:157/160 ('Worship'→'Band'), and the ServiceEditorView.test.ts:97 MOCK (band:'Band', other:'Other')"
    - "The constant stays its OWN literal — groupLabels is NOT imported (messagingRecipients.ts:11-16 deliberately keeps the two UIs independent); only the four RHS strings are copied verbatim"
    - "Unrelated 'Worship' strings are UNTOUCHED — ServiceEditorView.test.ts:1780 is a SERVICE SECTION header (Pre-Service·Worship·Message·Sending·Post-Service), NOT a messaging label; hymn titles and the PC 'Worship Song -' export prefix are also unrelated"
  artifacts:
    - src/utils/messagingRecipients.ts
    - src/utils/__tests__/messagingRecipients.test.ts
    - src/components/__tests__/MessageComposer.test.ts
    - src/components/__tests__/ReLockNotifyPrompt.test.ts
    - src/views/__tests__/ServiceEditorView.test.ts
  key_links:
    - "MESSAGING_TEAM_LABELS is imported by MessageComposer.vue:275 (teamChips computed :396-400) and by ReLockNotifyPrompt.vue for its team tags — one constant edit propagates to both surfaces; the tests that hard-code the old strings must move in lockstep or the app suite goes red"
    - "ServiceEditorView.test.ts:97 re-exports the constant via vi.mock only so MessageComposer's module-scope import resolves; NO assertion in that file reads a team-chip label off the mock (the only 'Worship' hit, :1780, is a section header) — update the mock for consistency, leave :1780 alone"
---

<objective>
Retire the v1.7 messaging label remap. `MESSAGING_TEAM_LABELS` in `src/utils/messagingRecipients.ts`
currently remaps `band→'Worship'` and `other→'Hosts'`; change it to the raw RoleGroup names
`band→'Band'`, `tech→'Tech'`, `vocals→'Vocals'`, `other→'Other'` so the composer's Send-To chips and
`ReLockNotifyPrompt`'s team tags read exactly what the owner sees in Volunteer Roles. This is a
one-constant change plus the four hard-coded test assertions it necessarily breaks — landed together in
one commit so the app suite never goes red.

Purpose:
- R151 (SC1) — Send-To team labels mirror Volunteer Roles (Band / Vocals / Tech / Other), everywhere
  messaging renders team names; the Worship/Hosts remap is gone.

This is FOUNDATIONAL for the phase and its own Wave-1 plan. It is deliberately split from the composer
work (64-03) because BOTH plans edit `src/components/__tests__/MessageComposer.test.ts`: 64-01 updates the
label assertions there, 64-03 later adds the composer-behavior tests. They must run in dependency order
(64-01 wave 1, 64-03 wave 2) — never the same wave — to avoid a same-file collision.

DECISIONS (locked, from 64-CONTEXT.md / 64-RESEARCH.md / 64-PATTERNS.md / 64-UI-SPEC.md):
- New values are `band:'Band'`, `tech:'Tech'`, `vocals:'Vocals'`, `other:'Other'` — copy the four RHS
  strings verbatim from `RolesConfigPanel.vue:119` `groupLabels`. Do NOT `import` groupLabels; the constant
  stays its own independent literal (messagingRecipients.ts:11-16 documents that the two UIs may describe
  the same enum differently — keep the constant, just change its values).
- No composer or ReLockNotifyPrompt markup changes — the chips render `{{ team.label }}` from the constant,
  so the one edit re-labels both surfaces (UI-SPEC: "the UI needs no per-chip change").
- The four test edits are: (1) `messagingRecipients.test.ts:85-90` object assertion; (2)
  `MessageComposer.test.ts:139` comment + `:140`/`:141`/`:170` assertions; (3)
  `ReLockNotifyPrompt.test.ts:156` comment + `:157`/`:159` comment + `:160` assertions; (4)
  `ServiceEditorView.test.ts:97` MOCK values.

CORRECTION vs 64-RESEARCH.md (bake in): the research (§R151) told the executor to ALSO change
`ServiceEditorView.test.ts:1780` `toContain('Worship')` to `'Band'`. That is WRONG — line 1780 asserts a
SERVICE SECTION header (the five SERVICE_SECTIONS are Pre-Service · Worship · Message · Sending ·
Post-Service, :1778-1783), which has nothing to do with `MESSAGING_TEAM_LABELS`. Changing it would break
the section-header test. Update ONLY the mock at `:97`; leave `:1780` exactly as it is. (This matches the
authoritative user constraint, which lists only `:97`.)

Output: the relabelled `messagingRecipients.ts` and the four updated test files. No production markup edits,
nothing deployed, no `.env.local` needed (these are jsdom / pure-unit tests that mock Firebase).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — the R151 decision, the analog label source, and the exact test-impact table
@.planning/phases/64-composer-refinements/64-CONTEXT.md
@.planning/phases/64-composer-refinements/64-RESEARCH.md
@.planning/phases/64-composer-refinements/64-PATTERNS.md
@.planning/phases/64-composer-refinements/64-UI-SPEC.md

# The constant to change (:17-22) + the file-comment that forbids importing groupLabels (:11-16)
@src/utils/messagingRecipients.ts
# The authoritative label source to copy the four strings FROM (do NOT import) — groupLabels :119
@src/components/RolesConfigPanel.vue

# The four test files that hard-code the old labels — edit the cited lines only
@src/utils/__tests__/messagingRecipients.test.ts
@src/components/__tests__/MessageComposer.test.ts
@src/components/__tests__/ReLockNotifyPrompt.test.ts
@src/views/__tests__/ServiceEditorView.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relabel MESSAGING_TEAM_LABELS to Band/Vocals/Tech/Other + update the four assertions it breaks (R151)</name>
  <files>src/utils/messagingRecipients.ts, src/utils/__tests__/messagingRecipients.test.ts, src/components/__tests__/MessageComposer.test.ts, src/components/__tests__/ReLockNotifyPrompt.test.ts, src/views/__tests__/ServiceEditorView.test.ts</files>
  <behavior>
    - MESSAGING_TEAM_LABELS deep-equals { band: 'Band', tech: 'Tech', vocals: 'Vocals', other: 'Other' } (messagingRecipients.test.ts)
    - The composer band chip text contains 'Band' (not 'Worship') and the other chip contains 'Other' (not 'Hosts'); the band chip matches /Band\s*·\s*1/ (MessageComposer.test.ts:140/141/170)
    - The ReLockNotifyPrompt band-derived team tags (change-row-0, change-row-1) contain 'Band' (not 'Worship'); the tech tag on change-row-0 still contains 'Tech'; change-row-1 still does not contain 'Tech' (ReLockNotifyPrompt.test.ts:157/158/160/161)
    - No section-header or hymn/PC-export 'Worship' string changes — only the two messaging labels (band, other) and their test assertions
  </behavior>
  <action>
    1. In `src/utils/messagingRecipients.ts` (`MESSAGING_TEAM_LABELS`, :17-22): change `band: 'Worship'` →
       `band: 'Band'` and `other: 'Hosts'` → `other: 'Other'`. Leave `tech: 'Tech'` and `vocals: 'Vocals'`.
       Do NOT add an import of `RolesConfigPanel`'s `groupLabels` — copy the four literal strings only; the
       constant remains its own independent Record (keep the :11-16 comment intact).
    2. In `src/utils/__tests__/messagingRecipients.test.ts` (:85-90): update the `toEqual({...})` object so
       `band: 'Band'` and `other: 'Other'` (tech/vocals unchanged).
    3. In `src/components/__tests__/MessageComposer.test.ts`:
       - `:139` comment "Team labels come from MESSAGING_TEAM_LABELS (Worship/Tech/Vocals/Hosts)." → make it
         read Band/Tech/Vocals/Other.
       - `:140` `expect(q('team-chip-band').text()).toContain('Worship')` → `.toContain('Band')`.
       - `:141` `expect(q('team-chip-other').text()).toContain('Hosts')` → `.toContain('Other')`.
       - `:170` `expect(q('team-chip-band').text()).toMatch(/Worship\s*·\s*1/)` → `/Band\s*·\s*1/`.
       Touch NOTHING else in this file (64-03 owns the rest of MessageComposer.test.ts in a later wave).
    4. In `src/components/__tests__/ReLockNotifyPrompt.test.ts`:
       - `:156` comment "(band→Worship, tech→Tech)." → "(band→Band, tech→Tech)."
       - `:157` `expect(q('change-row-0').text()).toContain('Worship')` → `.toContain('Band')`.
       - `:159` comment "exactly one chip (Worship), no Tech." → "exactly one chip (Band), no Tech."
       - `:160` `expect(q('change-row-1').text()).toContain('Worship')` → `.toContain('Band')`.
       Leave `:158` (`toContain('Tech')`) and `:161` (`not.toContain('Tech')`) as-is.
    5. In `src/views/__tests__/ServiceEditorView.test.ts` (:97): update the vi.mock re-export literal
       `MESSAGING_TEAM_LABELS: { band: 'Worship', ... other: 'Hosts' }` → `{ band: 'Band', tech: 'Tech',
       vocals: 'Vocals', other: 'Other' }`, for consistency with reality. This mock has no dependent
       assertion. Do NOT touch line `:1780` `toContain('Worship')` — it asserts a SERVICE SECTION header
       (Pre-Service · Worship · Message · Sending · Post-Service), not a messaging label; changing it would
       break the section-header test. Do NOT touch the other 'Worship' hits (drag/section/PC-export lines).
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/messagingRecipients.test.ts src/components/__tests__/MessageComposer.test.ts src/components/__tests__/ReLockNotifyPrompt.test.ts src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>MESSAGING_TEAM_LABELS reads Band/Tech/Vocals/Other; the four scoped test files are green with the updated Band/Other assertions; ServiceEditorView.test.ts:1780 and all other 'Worship' strings are unchanged; `npm run type-check` (vue-tsc --build) clean; the full app suite (`npx vitest run`, ~300s extended timeout — a timeout is not a failure) stays at the 2-file known-failing baseline (storage.rules.test.ts, RosterView.test.ts) with NO new failing file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Presentation label (client) → user | `MESSAGING_TEAM_LABELS` is a display remap of the RoleGroup enum; it crosses no auth/network boundary. The real recipient resolution (`resolveRecipients`) keys off the RoleGroup enum values, NOT the display labels — so relabelling cannot change who a message reaches |
| npm registry → app build | No package installed — a two-string literal edit + test-assertion updates. Zero supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01a | Tampering (correctness) | The relabel accidentally edits an unrelated 'Worship' string (a service-section header, a hymn title, or the PC 'Worship Song -' export prefix) and regresses a shipped surface | medium | mitigate | The action changes ONLY the two constant values (band, other) and the four cited test assertions; it explicitly forbids `ServiceEditorView.test.ts:1780` (a section header) and the other 'Worship' hits. The scoped test run + the full app suite at baseline are the gate |
| T-64-01b | Tampering (correctness) | The constant changes but a hard-coded old-label assertion is missed, leaving the app suite red after a Wave-1 landing | medium | mitigate | All four grep-confirmed assertion sites (messagingRecipients.test.ts, MessageComposer.test.ts, ReLockNotifyPrompt.test.ts, ServiceEditorView.test.ts mock) are updated in the SAME commit; the `grep -rln "Hosts"` set (4 files) is fully covered |
| T-64-01-SC | Tampering | npm/pip/cargo installs | low | accept | ZERO packages installed this plan — pure literal + test edits. No legitimacy checkpoint required |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/messagingRecipients.test.ts src/components/__tests__/MessageComposer.test.ts src/components/__tests__/ReLockNotifyPrompt.test.ts src/views/__tests__/ServiceEditorView.test.ts`
  green — the constant reads Band/Tech/Vocals/Other and every dependent assertion matches (R151 / SC1).
- `npm run type-check` clean — `vue-tsc --build` (typechecks the test files; NOT `-p tsconfig.app.json`, per
  CLAUDE.md). A literal-value change is type-neutral, but run the project gate.
- `npx vitest run` (full app suite, ~300s extended timeout — a timeout is NOT a failure) stays at the 2-file
  known-failing baseline (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`). No NEW
  failing app-suite file. Do NOT use `--dir src` or `--config vitest.rules.config.ts` (they change the
  exclude set / suite per CLAUDE.md).
- Manual (deferred to owner at `/gsd-verify-work 64`): open the ✉ composer and confirm the Send-To chips
  read Band / Vocals / Tech / Other; open a re-lock prompt and confirm its team tags match. Recorded as
  verification_deferred_human, NOT marked passed here.
</verification>

<success_criteria>
- The composer's Send-To team chips and the re-lock prompt's team tags read **Band / Vocals / Tech / Other**,
  mirroring Volunteer Roles; the Worship/Hosts remap is gone (R151 / SC1).
- The four hard-coded old-label assertions are updated; no unrelated 'Worship' string (section header, hymn,
  PC export prefix) is touched; type-check and the app suite (minus the 2-file baseline) are clean.
</success_criteria>

<output>
Create `.planning/phases/64-composer-refinements/64-01-SUMMARY.md` when done.
</output>
