---
phase: 90-slidecanvas-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/slides/SlideCanvas.vue
  - src/components/PresentationViewer.vue
  - src/components/slides/__tests__/SlideCanvas.test.ts
autonomous: true
requirements: []   # none — enabling refactor; no v2.4 requirement maps here by design (ROADMAP Basis note)
must_haves:
  truths:
    - "SlideCanvas.vue renders every supported content kind (lyric, copyright, scripture normal + congregational, text, image, video) with the SAME data-testid markers PresentationViewer used"
    - "SlideCanvas gates its background layer on the suppressBackground prop: shows presentation-background + scrim when false/absent, renders neither (black) when true"
    - "SlideCanvas preserves the T-23-08 pause→reset→play instant-swap media invariant via exposed play()/pause() plus an internal slide-change reset"
    - "PresentationViewer composes SlideCanvas at its one call site with zero observable behavior change — its existing test suite passes UNMODIFIED"
    - "npm run type-check is clean and npx vitest run shows only the documented storage.rules.test.ts baseline failure (no new failures)"
    - "SlideCanvas has focused unit tests covering each content kind, suppressBackground, media pause/play, and interactive gating"
  artifacts:
    - src/components/slides/SlideCanvas.vue
    - src/components/slides/__tests__/SlideCanvas.test.ts
    - src/components/PresentationViewer.vue
  key_links:
    - "PresentationViewer template: <SlideCanvas :slide=currentSlide interactive /> at the v-else-if=currentSlide position (the one existing call site)"
    - "PresentationViewer goToIndex / slides-length watcher / onMounted / onBeforeUnmount / exitPresentation route media through slideCanvasRef.play()/pause() in the exact T-23-08 order"
    - "--slide-font-* CSS custom properties stay set on the PresentationViewer viewer root (typographyStyle) and inherit into SlideCanvas's moved scoped typography rules"
---

<objective>
Extract `PresentationViewer.vue`'s per-slide rendering + media playback into a reusable
`src/components/slides/SlideCanvas.vue` and refactor `PresentationViewer.vue` to compose it at its one
existing call site, with ZERO observable behavior change. This establishes the single slide-rendering
source of truth the downstream Run/Audience/Confidence windows (Phases 93–95) compose instead of forking.

Purpose: `slideshowAssembler.ts` (pure) + `SlideCanvas.vue` (presentational) together become the whole
reusable slide engine — no lyric/scripture/copyright/image/video render logic, media lifecycle, or the
R070 background cascade is ever duplicated across the three future windows.

Output: `SlideCanvas.vue` (props `slide` / `suppressBackground` / `interactive`, exposed `play()`/`pause()`),
a refactored `PresentationViewer.vue`, and a focused `SlideCanvas.test.ts`. The overriding acceptance
gate is that `src/components/__tests__/PresentationViewer.test.ts` passes UNMODIFIED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-slidecanvas-extraction/90-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

# THE file being refactored — the source of truth for every render branch, media
# lifecycle, background layer, and the scoped typography <style> block:
@src/components/PresentationViewer.vue
# The behavior contract that MUST keep passing unmodified (data-testid markers):
@src/components/__tests__/PresentationViewer.test.ts
# Types and the render-failure sentence helper the extracted code depends on:
@src/types/slide.ts
@src/components/slides/slideDisplay.ts
# The font-load gate + typography CSS-var helpers that STAY in PresentationViewer:
@src/utils/slideTypography.ts
# The one existing call site — NOT modified this phase; confirm its prop contract is untouched:
@src/views/ServiceEditorView.vue
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- **`src/components/slides/SlideCanvas.vue`** (NEW) — presentational per-slide renderer + media playback.
  - Props: `slide: AssembledSlide | null`, `suppressBackground?: boolean`, `interactive?: boolean`.
  - Exposes (via `defineExpose`): `play()`, `pause()` — the media handles PresentationViewer drives.
  - Owns: background layer + scrim, the six per-`slideKind` render branches, render-pending/failed
    canvas states, `AudioPlayer`/`VideoPlayer` refs, degraded-state flags, and the scoped
    `--slide-font-*`-consuming typography `<style>` rules.
  - Does NOT own: exit button, nav chrome, keyboard, fullscreen, Escape teardown, or the font-load gate.
- **`src/components/slides/__tests__/SlideCanvas.test.ts`** (NEW) — focused unit tests per content kind
  plus `suppressBackground`, media pause/play ordering, and `interactive` affordance gating.
- **`src/components/PresentationViewer.vue`** (MODIFIED) — now composes `<SlideCanvas>` at its one call
  site; keeps all chrome, keyboard, fullscreen, R061 jump, and the R094 font-load gate.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SlideCanvas.vue by extracting the per-slide render + media + background from PresentationViewer</name>
  <files>src/components/slides/SlideCanvas.vue</files>
  <read_first>
    - src/components/PresentationViewer.vue (THE source of truth — copy every render branch, the
      background layer, the media wiring, and the scoped &lt;style&gt; verbatim from here)
    - src/components/__tests__/PresentationViewer.test.ts (the data-testid markers + behaviors the
      extracted markup must reproduce exactly)
    - src/types/slide.ts (AssembledSlide / Slide / LyricSlide / CopyrightSlide / ScriptureSlide /
      CongregationalSection / TextSlide / ImageSlide / VideoSlide shapes)
    - src/components/slides/slideDisplay.ts (renderFailureSentence — the one sanctioned failure-slug path)
  </read_first>
  <behavior>
    - Given a `lyric` slide, renders `presentation-body` containing the joined `lines`, no sectionLabel.
    - Given a `copyright` slide (contentKind 'lyric', has `title`, no `sectionId`), renders
      `presentation-body` title at `text-6xl`, authors (`presentation-copyright-authors`), and
      `presentation-copyright-fine-print` with the CCLI lines.
    - Given a normal `scripture` slide, renders `presentation-scripture-reference` + `presentation-body`.
    - Given a congregational `scripture` slide (one section), renders `presentation-speaker` (correct
      label text + colour class) + `presentation-congregational-section`, and NO reference.
    - Given a `text` slide, renders `presentation-body` with the body only (title never projected).
    - Given an `image` slide, renders `presentation-image` with src/alt/object-contain/max-h-[80vh].
    - Given a `video` slide, renders `presentation-video` wrapping a chromeless VideoPlayer with the src.
    - Given renderState 'pending'/'failed', renders `presentation-render-pending`/`presentation-render-failed`
      FIRST (ahead of the per-kind chain), never a broken image/body.
    - `suppressBackground` true → `currentBackgroundUrl` resolves null (no background/scrim, black only);
      false/absent → resolves the slide's own `backgroundImageUrl` (null when a video is playing, R070).
    - Exposed `pause()` then `play()` issues pause before play (T-23-08); a video/audio `error` shows
      `presentation-media-unavailable` and removes the media wrapper.
    - `interactive` false → the three autoplay affordances never render; true → they render as today.
  </behavior>
  <action>
    Create `src/components/slides/SlideCanvas.vue` (the `slides/` folder already exists). Move — do not
    reinvent — the following out of `PresentationViewer.vue`, re-pointing every `currentSlide.value`
    reference to `props.slide`:

    TEMPLATE. Copy the background layer (`presentation-background` + `presentation-background-scrim`,
    PresentationViewer lines 27–38) and the ENTIRE `v-else-if="currentSlide"` slide block
    (`presentation-slide` div and all descendants, lines 70–324) verbatim, preserving every `data-testid`
    (`presentation-slide`, `presentation-render-pending`, `presentation-render-failed`, `presentation-body`,
    `presentation-copyright-fine-print`, `presentation-scripture-reference`, `presentation-speaker`,
    `presentation-congregational-section`, `presentation-image`, `presentation-video`, `presentation-audio`,
    `presentation-media-unavailable`, `presentation-audio-affordance`, `presentation-video-affordance`,
    `presentation-muted-chip`) and the `presentation-copyright-authors` class. Emit these as sibling
    ROOT nodes (a multi-root `<template>`: background, scrim, slide-content) so that when composed inside
    PresentationViewer's teleported viewer root they stay direct children in the same DOM order — this
    preserves the existing inline negative-z (`zIndex: -1`) background layering. Drop the outer
    `v-else-if="currentSlide"` guard on the slide-content div (PresentationViewer keeps that guard around
    the `<SlideCanvas>` element instead); inside SlideCanvas the slide block renders whenever `props.slide`
    is non-null.

    PROPS. `const props = defineProps<{ slide: AssembledSlide | null; suppressBackground?: boolean;
    interactive?: boolean }>()`.

    SCRIPT. Move these computeds/state, re-pointed to `props.slide`: `CardKind` type, `cardKind()`,
    `slideKind`, `currentRenderState`, `currentRenderFailureSentence`, `isCongregational`,
    `speakerLabelText`, `speakerColorClass`, `currentAudioUrl`, `currentVideoUrl`, `currentVideoKey`,
    `currentAudioKey`; the refs `audioRef`/`videoRef`; the degraded-state flags `mediaFailed`,
    `audioBlocked`, `videoBlocked`, `videoMutedPlaying`; the handlers `onMediaError`,
    `onAudioAutoplayBlocked`, `onVideoAutoplayBlocked`, `onUnmuteClick`; and the media-driving functions
    `playCurrentMedia`, `pauseCurrentMedia`, `resetMediaState`. Redefine `currentBackgroundUrl` to gate on
    the prop per Architecture Pattern 2: return null when `props.suppressBackground` is true; else null
    when `currentVideoUrl` is truthy; else `props.slide?.slide.backgroundImageUrl ?? null` (the R070
    cascade is already resolved upstream — read the single winning value, never re-derive the tier).

    MEDIA INVARIANT (T-23-08). Expose `play()` and `pause()` via `defineExpose`, delegating to
    `playCurrentMedia`/`pauseCurrentMedia` exactly as today (void the video/audio `play()`, no await, no
    added catch beyond what the players handle). Reset degraded-state via an internal
    `watch(() => props.slide?.slide.id, () => resetMediaState())` so flags clear on every slide-identity
    change (equivalent to today's `resetMediaState()` inside `goToIndex`); the media children remain keyed
    on `currentVideoKey`/`currentAudioKey` so the WR-02 fresh-instance and R030 bed-continuity behaviors
    are byte-identical. Gate the three affordance controls (`presentation-audio-affordance`,
    `presentation-video-affordance`, `presentation-muted-chip`) additionally on `props.interactive` so a
    non-interactive preview shows none.

    DO NOT copy from PresentationViewer: `handleFullscreenChange`/the `fullscreenchange` listener,
    `handleKeydown`/Escape teardown, `enterPresentation`/`exitPresentation`, `registerActivity`/chrome
    auto-hide, or the font-load gate. SlideCanvas is chrome-free rendering only (PITFALLS Pitfall 6/19 —
    a deliberate NON-copy).

    STYLE. Move the scoped `<style>` per-element font block (PresentationViewer lines 975–1017,
    the `[data-testid='presentation-body'].text-5xl` etc. rules) into SlideCanvas so it keeps targeting
    the moved elements; the `--slide-font-*` custom properties stay defined on PresentationViewer's viewer
    root and inherit down.

    IMPORTS. `renderFailureSentence` from `./slideDisplay`; `AudioPlayer`/`VideoPlayer` from
    `../AudioPlayer.vue` / `../VideoPlayer.vue`; slide types from `@/types/slide`. SlideCanvas must NOT
    import `@/stores/auth` or anything from `@/utils/slideTypography` — the store read and font gate stay
    in PresentationViewer.
  </action>
  <acceptance_criteria>
    - `src/components/slides/SlideCanvas.vue` exists and `defineProps` declares `slide`,
      `suppressBackground`, and `interactive`.
    - `defineExpose` exposes `play` and `pause`.
    - The file contains no import of `@/stores/auth` and no import from `@/utils/slideTypography`.
    - The file does NOT reference `requestFullscreen`, `fullscreenchange`, `handleKeydown`, or
      `exitPresentation` (chrome/teardown stays in PresentationViewer).
    - `npm run type-check` is clean (SlideCanvas.vue type-checks as part of vue-tsc --build).
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
    <automated>grep -Eq "suppressBackground" src/components/slides/SlideCanvas.vue && grep -Eq "defineExpose" src/components/slides/SlideCanvas.vue && echo PROPS_AND_EXPOSE_OK</automated>
  </verify>
  <done>
    SlideCanvas.vue exists with the three props and exposed play()/pause(), holds every moved render
    branch + media wiring + background layer + scoped typography style, imports no auth store or font
    gate, and type-check is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose SlideCanvas into PresentationViewer at its one call site with zero behavior change</name>
  <files>src/components/PresentationViewer.vue</files>
  <read_first>
    - src/components/PresentationViewer.vue (the file being refactored)
    - src/components/slides/SlideCanvas.vue (the component created in Task 1 — its props + exposed play/pause)
    - src/components/__tests__/PresentationViewer.test.ts (the unmodified behavior contract this task must satisfy)
    - src/views/ServiceEditorView.vue (the one call site — confirm its :slides/:is-loading/:initial-index/@exit contract stays untouched)
  </read_first>
  <behavior>
    - The `slides` / `isLoading` / `initialIndex` props and the `exit` emit of PresentationViewer are unchanged.
    - The loading (`presentation-loading`), empty (`presentation-empty-state`), exit (`presentation-exit`),
      and bottom chrome (`presentation-chrome`/`presentation-prev`/`presentation-progress`/`presentation-next`)
      elements render exactly as before.
    - The R094 font gate still holds `presentation-slide` (now inside SlideCanvas) absent until `fontReady`.
    - Navigation drives media in the T-23-08 order: pause outgoing BEFORE the currentIndex write, play
      incoming AFTER nextTick, via `slideCanvasRef`.
    - Every assertion in `src/components/__tests__/PresentationViewer.test.ts` passes with that test file byte-unchanged.
  </behavior>
  <action>
    In `src/components/PresentationViewer.vue`:

    TEMPLATE. Replace the inline background layer (lines 27–38) AND the entire `v-else-if="currentSlide"`
    slide block (lines 70–324) with a single element in the slide position:
    `<SlideCanvas v-else-if="currentSlide" ref="slideCanvasRef" :slide="currentSlide" interactive />`.
    Keep the `v-if="showLoadingState" / v-else-if="isEmptyState" / v-else-if="currentSlide"` chain intact
    so the font gate keeps suppressing SlideCanvas until `fontReady` (the font-gate tests assert
    `presentation-slide` is absent while the gate holds). Leave the exit button and the entire bottom
    `presentation-chrome` bar exactly where they are.

    SCRIPT — remove (moved to SlideCanvas): `audioRef`, `videoRef`; the flags `mediaFailed`,
    `audioBlocked`, `videoBlocked`, `videoMutedPlaying`; `CardKind`/`cardKind`/`slideKind`,
    `isCongregational`, `speakerLabelText`, `speakerColorClass`, `currentAudioUrl`, `currentVideoUrl`,
    `currentBackgroundUrl`, `currentVideoKey`, `currentAudioKey`, `currentRenderState`,
    `currentRenderFailureSentence`; the handlers `onMediaError`/`onAudioAutoplayBlocked`/
    `onVideoAutoplayBlocked`/`onUnmuteClick`; the driving functions `playCurrentMedia`/`pauseCurrentMedia`/
    `resetMediaState`; the `renderFailureSentence` import and the `AudioPlayer`/`VideoPlayer` imports; and
    the scoped `<style>` per-element font block.

    SCRIPT — add: `import SlideCanvas from './slides/SlideCanvas.vue'` and
    `const slideCanvasRef = ref<InstanceType<typeof SlideCanvas> | null>(null)`.

    SCRIPT — keep unchanged: `currentSlide`, `currentIndex` + the R061 initialIndex clamp, `hasSlides`,
    `isLoadingState`, `isEmptyState`, `fontGateActive`, `showLoadingState`, `exitVisible`, `atFirst`,
    `atLast`, `progressLabel`, the slides-length watcher, `handleKeydown`/`trapFocus`/`getFocusableElements`,
    `registerActivity` + `chromeTimer` auto-hide, `enterPresentation`/`handleFullscreenChange`/
    `exitPresentation`/`isTrueFullscreen`, the `typographyStyle` computed with `cssVarsFor`/`authStore`
    (still sets `--slide-font-*` on the viewer root), and the R094 font-load gate in `onMounted`
    (`resolvedFontChoice`/`waitForSlideFont`/`loadFontCss`/`FONT_LOAD_TIMEOUT_MS`/`fontReady`).

    MEDIA ROUTING (preserve T-23-08 exactly). Wherever `pauseCurrentMedia()` was called, call
    `slideCanvasRef.value?.pause()`; wherever `playCurrentMedia()` was called, call
    `slideCanvasRef.value?.play()`. Specifically: in `goToIndex`, `slideCanvasRef.value?.pause()` BEFORE the
    `currentIndex.value = next` write and `slideCanvasRef.value?.play()` after `await nextTick()`; the same
    two substitutions in the slides-length watcher (WR-03); `slideCanvasRef.value?.play()` in `onMounted`'s
    deferred first-slide play; and `slideCanvasRef.value?.pause()` in `onBeforeUnmount` and in
    `exitPresentation`. Delete the now-removed `resetMediaState()` calls — degraded-state reset happens
    inside SlideCanvas's own slide-change watcher now.

    Do NOT edit `src/views/ServiceEditorView.vue` — its `<PresentationViewer :slides :is-loading
    :initial-index @exit>` call site is unchanged by design.
  </action>
  <acceptance_criteria>
    - PresentationViewer.vue imports `SlideCanvas` from `./slides/SlideCanvas.vue` and its template
      contains `<SlideCanvas` with `:slide="currentSlide"` and `interactive`.
    - PresentationViewer.vue no longer imports `AudioPlayer`/`VideoPlayer` and no longer contains a
      `[data-testid='presentation-body']` scoped style rule (both moved to SlideCanvas).
    - `src/components/__tests__/PresentationViewer.test.ts` is byte-unchanged (git diff reports no changes).
    - `npx vitest run src/components/__tests__/PresentationViewer.test.ts` passes (all assertions green).
    - `npm run type-check` is clean.
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code -- src/components/__tests__/PresentationViewer.test.ts</automated>
    <automated>npx vitest run src/components/__tests__/PresentationViewer.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    PresentationViewer composes `<SlideCanvas :slide="currentSlide" interactive>` at its one call site,
    routes all media through `slideCanvasRef.play()/pause()` in the T-23-08 order, keeps all chrome +
    keyboard + fullscreen + font gate, and its existing test suite passes with the test file unmodified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused SlideCanvas unit tests for each content kind, suppressBackground, media, and interactive</name>
  <files>src/components/slides/__tests__/SlideCanvas.test.ts</files>
  <read_first>
    - src/components/slides/SlideCanvas.vue (the component under test — its props + exposed play/pause)
    - src/components/__tests__/PresentationViewer.test.ts (reuse its AssembledSlide fixture builders and jsdom media stubs)
    - src/types/slide.ts (AssembledSlide / per-kind slide shapes for fixtures)
  </read_first>
  <behavior>
    - One test per content kind asserts the correct data-testid marker renders from a directly-mounted
      `SlideCanvas` (lyric, copyright, scripture normal, scripture congregational, text, image, video).
    - `suppressBackground` false/absent with a resolved `backgroundImageUrl` → `presentation-background` +
      `presentation-background-scrim` present; `suppressBackground` true → NEITHER present.
    - Calling the exposed `pause()` then `play()` issues pause before play (HTMLMediaElement stubs); a
      `video` `error` shows `presentation-media-unavailable` and removes `presentation-video`.
    - `interactive` false suppresses the three affordances even when a play rejection would show them;
      `interactive` true renders them (same as PresentationViewer today).
  </behavior>
  <action>
    Create `src/components/slides/__tests__/SlideCanvas.test.ts`. Mount `SlideCanvas` directly (no
    Teleport, so assert on the wrapper's own DOM — SlideCanvas is not teleported) with a `slide` prop per
    content kind and assert the same `data-testid` markers as the PresentationViewer suite: `lyric` →
    `presentation-body` with joined `lines`; `copyright` (contentKind 'lyric', `title`, no `sectionId`) →
    `presentation-body` title carrying `text-6xl` + `presentation-copyright-fine-print` with the CCLI lines;
    normal `scripture` → `presentation-scripture-reference` + full-text `presentation-body`;
    congregational `scripture` (readingMode 'congregational', one `section`) → `presentation-speaker`
    (label + colour class) + `presentation-congregational-section`, and NO `presentation-scripture-reference`;
    `text` → `presentation-body` with the body only; `image` → `presentation-image` with src/alt/object-contain;
    `video` → `presentation-video` chromeless VideoPlayer with the src.

    Add a `suppressBackground` block: mount with a slide carrying a resolved `backgroundImageUrl` and
    `suppressBackground: false` → assert `presentation-background` + `presentation-background-scrim` exist;
    remount with `suppressBackground: true` → assert NEITHER exists (the Phase 94 confidence-monitor
    contract, landing here in Phase 90).

    Add a media block: stub `window.HTMLMediaElement.prototype.play`/`pause` (jsdom implements neither);
    obtain the exposed methods via the mounted wrapper's `vm` and assert calling `pause()` then `play()`
    records pause before play; trigger `error` on the `presentation-video` `video` element and assert
    `presentation-media-unavailable` appears and `presentation-video` is removed.

    Add an `interactive` block: with `play` rejecting `NotAllowedError`, mount an audio-carrying slide
    with `interactive: false` and assert `presentation-audio-affordance` does NOT render; mount the same
    with `interactive: true` and assert it DOES render.

    Reuse the AssembledSlide fixture shapes from the PresentationViewer suite (copy the small builders
    into this file). No `document.fonts` stub is needed — SlideCanvas has no font gate.
  </action>
  <acceptance_criteria>
    - `src/components/slides/__tests__/SlideCanvas.test.ts` exists with a test for each of the seven
      content-kind cases, a suppressBackground pair, a media pause/play + error case, and an interactive pair.
    - `npx vitest run src/components/slides/__tests__/SlideCanvas.test.ts` passes.
    - `npm run type-check` is clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideCanvas.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    SlideCanvas.test.ts covers every content kind, the suppressBackground true/false contract, the
    exposed pause/play ordering + media-error degradation, and interactive affordance gating — and passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AssembledSlide content (slide text, media/background URLs) → projected DOM | Same already-resolved data PresentationViewer renders today; no NEW untrusted input crosses here (this is a pure move of existing render code). |

No new external input, no network call, no package install, and no Firestore/Storage rule change is
introduced by this phase (ROADMAP scope note: v2.4 is client-side only, zero new npm dependency).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-90-01 | Tampering (XSS) | SlideCanvas slide-text rendering | low | mitigate | Render ALL slide text through Vue `{{ }}` text interpolation (never `v-html`), byte-identical to PresentationViewer today. The existing markup test ("angle-bracket markup renders literally, not as child elements") passes unmodified against the composed SlideCanvas, and the new SlideCanvas suite mounts a text slide with markup and asserts no `<script>`/`<b>`/`<i>` child element is created. |
| T-90-02 | Information disclosure | media / background URLs | low | accept | The URLs SlideCanvas renders are the identical already-resolved values PresentationViewer emits today; the extraction adds no new exposure surface. Accept. |
</threat_model>

<verification>
## Phase-level checks (run after all three tasks)

1. `npm run type-check` — clean (vue-tsc --build; typechecks test files too, per CLAUDE.md). This is the
   authoritative type gate — do NOT substitute the narrower `-p tsconfig.app.json` form.
2. `npx vitest run` — the app suite. Expected: only `src/storage.rules.test.ts` fails (the documented
   Storage-emulator environment limitation). NO new failures vs that baseline; `PresentationViewer.test.ts`
   and the new `SlideCanvas.test.ts` both green. Do NOT chase the storage.rules baseline; do NOT run with
   `--dir src` (it bypasses vite.config.ts excludes).
3. `git diff --exit-code -- src/components/__tests__/PresentationViewer.test.ts` — the existing behavior
   contract is byte-unchanged (the zero-behavior-change proof).
4. `src/views/ServiceEditorView.vue` is unmodified (the one call site's prop contract is untouched).
</verification>

<success_criteria>
1. `src/components/slides/SlideCanvas.vue` exists with `slide` / `suppressBackground` / `interactive`
   props and renders every supported slide content type (lyric, copyright, scripture, image, video, text)
   with the same data-testid markers.
2. `PresentationViewer.vue` composes `<SlideCanvas>` at its one existing call site with zero observable
   behavior change — `src/components/__tests__/PresentationViewer.test.ts` passes UNMODIFIED, and its
   data-testid markers are preserved.
3. `npm run type-check` and `npx vitest run` show no NEW failures vs the documented baseline
   (`storage.rules.test.ts` only). The T-23-08 pause→reset→play instant-swap invariant is preserved
   exactly; PresentationViewer's `fullscreenchange`/Escape teardown is NOT copied into SlideCanvas.
4. `SlideCanvas.test.ts` adds focused unit coverage for each content kind, `suppressBackground`, media
   pause/play, and `interactive` gating, and passes.
</success_criteria>

<output>
Create `.planning/phases/90-slidecanvas-extraction/90-01-SUMMARY.md` when done.
</output>
