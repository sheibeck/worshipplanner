---
phase: 97-run-service-redesign
plan: 07
type: execute
wave: 2
depends_on: ["97-03"]
files_modified:
  - src/views/__tests__/AudienceOutputView.test.ts
  - src/views/__tests__/ConfidenceOutputView.test.ts
autonomous: true
requirements: [R278, R279, R280]

must_haves:
  truths:
    - "BLACKOUT OVERLAY (R280): both output suites gain coverage that emitState({ blackout: true }) renders the full-bleed black overlay (data-testid=audience-blackout / confidence-blackout) OVER the SlideCanvas, and emitState({ blackout: false }) removes it (slide visible again). The existing emitState harness already accepts a blackout arg — no new channel plumbing."
    - "SELF-FULLSCREEN (R278): a fake getScreenDetails resolving screens whose computeFingerprint matches a seeded monitor mapping proves requestFullscreen is CALLED on mount with a screen argument (the { screen } option object) for the role the view passes; and with NO getScreenDetails installed (or an unresolvable screen) the mount does NOT throw and falls back to a plain requestFullscreen() auto-attempt — never a crash. Every window/screen is faked; no real fullscreen, no real getScreenDetails."
    - "CONFIDENCE LEFT/RIGHT (R279): ConfidenceOutputView coverage asserts the root uses a horizontal (flex-row) split with the border-l seam, the current-region sits before the next-region, BOTH panes keep suppressBackground (no presentation-background), and the last-slide no-reflow holds (the next-region wrapper stays present when nextSlide is null). The existing suppression + no-reflow assertions stay green."
    - "NO REAL WINDOWS / RECEIVE-ONLY PRESERVED: the suites open no real window, call no real getScreenDetails, and re-confirm the outputs never postState. The pre-97 blocks (subscribe gate, media invariant, reenter affordance) stay green; new blocks are additive."
  artifacts:
    - "src/views/__tests__/AudienceOutputView.test.ts — new blocks: blackout overlay on/off; self-fullscreen requestFullscreen(screen) on mount with a fake getScreenDetails + seeded mapping; no-throw when unresolvable."
    - "src/views/__tests__/ConfidenceOutputView.test.ts — new blocks: blackout overlay on/off; left/right layout (flex-row + border-l + region order + suppression + no-reflow); self-fullscreen for role confidence."
  key_links:
    - "emitState(index, seq, true) -> useOutputWindow blackout ref -> view `<div data-testid={role}-blackout>` present; emitState(index, seq2, false) -> overlay absent."
    - "install fake getScreenDetails([screenA,screenB]) + seedMapping(audience->fingerprint(screenA)) -> mount AudienceOutputView(role audience) -> Element.prototype.requestFullscreen called with an arg carrying { screen: screenA }."
    - "no getScreenDetails installed -> mount -> no throw + requestFullscreen() auto-attempt (plain) called at most once."
---

<objective>
Prove the Phase 97 output-window behaviour from 97-03: the blackout overlay renders/clears on the channel
blackout field (R280), each output self-fullscreens on its assigned monitor on mount when a screen resolves and
degrades safely when it does not (R278), and the confidence window is a left/right split with suppression and
last-slide no-reflow intact (R279). Every window/screen/channel message is faked; no real window opens.

Purpose: R278, R279, R280 behavioural coverage for the output side. The control-side blackout controls are
covered in 97-10; this suite covers the outputs OBEYING blackout + self-fullscreen + the confidence layout.

Output: extended AudienceOutputView.test.ts + ConfidenceOutputView.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-UI-SPEC.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/composables/useOutputWindow.ts
@src/views/AudienceOutputView.vue
@src/views/ConfidenceOutputView.vue
@src/utils/monitorConfig.ts
@src/views/__tests__/AudienceOutputView.test.ts
@src/views/__tests__/ConfidenceOutputView.test.ts
@src/views/__tests__/MonitorSetupView.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AudienceOutputView.test.ts — blackout overlay + self-fullscreen(screen) + no-throw fallback</name>
  <files>src/views/__tests__/AudienceOutputView.test.ts</files>
  <read_first>
    - src/views/__tests__/AudienceOutputView.test.ts — the harness: emitState(index, seq, blackout=false)
      (:156-157), the fullscreenElement defineProperty (:163) + Element.prototype.requestFullscreen stub (:179),
      the reenter-affordance blocks (:427-468). Reuse the emitState blackout arg + the requestFullscreen stub.
    - src/composables/useOutputWindow.ts (97-03) — selfFullscreen resolves the role's screen via getScreenDetails
      + loadMapping + computeFingerprint and calls requestFullscreen({ screen }); role 'audience' is passed by the
      view; blackout is returned and rendered as audience-blackout.
    - src/utils/monitorConfig.ts — computeFingerprint (:69) + MONITOR_CONFIG_STORAGE_KEY (:59) so the test can
      seed a mapping whose audience fingerprint matches a fake screen.
    - src/views/__tests__/MonitorSetupView.test.ts — the fake getScreenDetails install idiom (a resolved object
      with a screens array) to mirror here.
  </read_first>
  <action>
    Add these describe blocks (reuse the existing mountView/emitState/requestFullscreen stub; add a small
    installGetScreenDetails helper + a seedMapping helper if not present):
    1. BLACKOUT OVERLAY (R280): mount; emitState(0, 1, true) → assert audience-blackout exists; emitState(0, 2,
       false) → assert audience-blackout absent (the overlay clears, the slide is visible). Confirm the SlideCanvas
       still renders underneath (the overlay is additive, not a teardown).
    2. SELF-FULLSCREEN with a resolvable screen (R278): make screenA/screenB fixtures with distinct fingerprints;
       localStorage.setItem(MONITOR_CONFIG_STORAGE_KEY, JSON.stringify({ assignments: [{ role:'audience',
       fingerprint: computeFingerprint(screenA) }], savedAt: 0 })); install a fake window.getScreenDetails
       resolving { screens: [screenA, screenB] }; mount (isFullscreen starts false); await flushPromises; assert
       Element.prototype.requestFullscreen was called with an argument whose .screen === screenA (inspect
       mock.calls). Clean up getScreenDetails after.
    3. NO-THROW FALLBACK (R278): with NO getScreenDetails installed (delete it) and no mapping, mount + await
       flushPromises → expect no throw; requestFullscreen was still auto-attempted at most once (plain, no screen)
       — assert it was called (toHaveBeenCalled) OR at least did not throw; the manual reenter affordance path
       still works (the existing block already covers the click).
    Keep every window/screen faked; never open a real window.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/AudienceOutputView.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "audience-blackout" src/views/__tests__/AudienceOutputView.test.ts &amp;&amp; grep -q "getScreenDetails" src/views/__tests__/AudienceOutputView.test.ts &amp;&amp; grep -q "requestFullscreen" src/views/__tests__/AudienceOutputView.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New blocks prove: emitState blackout true/false shows/removes audience-blackout over the slide; a fake
      getScreenDetails + seeded mapping makes requestFullscreen be called on mount with { screen: screenA }; and no
      getScreenDetails / unresolvable screen does not throw (plain requestFullscreen auto-attempt). All prior
      blocks green; `npx vitest run` shows only the baseline; type-check clean; no `--dir src`.
  </acceptance_criteria>
  <done>AudienceOutputView.test.ts proves the blackout overlay obeys the channel field and the audience window self-fullscreens on its assigned screen (with a safe no-throw fallback), all windows faked.</done>
</task>

<task type="auto">
  <name>Task 2: ConfidenceOutputView.test.ts — blackout overlay + left/right layout + self-fullscreen(confidence)</name>
  <files>src/views/__tests__/ConfidenceOutputView.test.ts</files>
  <read_first>
    - src/views/__tests__/ConfidenceOutputView.test.ts — emitState(index, seq, blackout=false) (:225-226), the
      requestFullscreen stub (:246), the current/next region asserts (:283-284,:362-375) + the suppression + last-
      slide no-reflow blocks + the reenter block (:648-689). Reuse these; the region testids are unchanged.
    - src/views/ConfidenceOutputView.vue (97-03) — flex-row root, border-l seam, current-region LEFT / next-region
      RIGHT, next SlideCanvas wrapped in a transform: scale container, confidence-blackout overlay, role
      'confidence'.
    - src/utils/monitorConfig.ts — computeFingerprint + MONITOR_CONFIG_STORAGE_KEY for a confidence-fingerprint
      mapping.
  </read_first>
  <action>
    Add these describe blocks (reuse the harness):
    1. BLACKOUT OVERLAY (R280): mount; emitState(0, 1, true) → confidence-blackout exists over both panes;
       emitState(0, 2, false) → absent.
    2. LEFT/RIGHT LAYOUT (R279): mount with a current + next slide; assert the ROOT element's class list contains
       'flex-row' (horizontal split) and the next-region carries 'border-l' (the vertical seam); assert the
       current-region appears BEFORE the next-region in DOM order (left→right); re-assert BOTH panes render with
       suppressBackground (no presentation-background / the Phase 94 invariant) reusing the existing suppression
       assertion; and re-assert last-slide no-reflow: emit a last-slide index (nextSlide null) and confirm
       confidence-next-region still EXISTS (wrapper unconditional) while its inner canvas is absent.
    3. SELF-FULLSCREEN (R278) for role confidence: seed a mapping with a confidence fingerprint = computeFingerprint
       (screenB); install fake getScreenDetails resolving [screenA, screenB]; mount; await flushPromises; assert
       requestFullscreen called with an arg whose .screen === screenB.
    Keep every window/screen faked; the outputs still never postState (re-confirm via the existing never-post
    assertion or a posted-length check if the harness exposes it).
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ConfidenceOutputView.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "confidence-blackout" src/views/__tests__/ConfidenceOutputView.test.ts &amp;&amp; grep -q "flex-row" src/views/__tests__/ConfidenceOutputView.test.ts &amp;&amp; grep -q "border-l" src/views/__tests__/ConfidenceOutputView.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New blocks prove: blackout overlay on/off; the root is flex-row with a border-l seam and current-region
      before next-region, both panes suppressed, last-slide no-reflow preserved; and confidence self-fullscreens on
      screenB. All prior suppression/no-reflow/reenter blocks green; `npx vitest run` baseline only; type-check
      clean; no `--dir src`.
  </acceptance_criteria>
  <done>ConfidenceOutputView.test.ts proves the left/right split (with suppression + no-reflow), the blackout overlay, and self-fullscreen for the confidence role — all faked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> useOutputWindow selfFullscreen | The fake getScreenDetails + seeded localStorage mapping exercises the REAL screen resolution + requestFullscreen({screen}); a regression that stopped resolving or passed no screen fails here |
| test harness -> blackout render | emitState drives the REAL onState → blackout ref → overlay; a regression that ignored the blackout field or leaked the slide fails here |
| test file -> real window/screen | The suite opens no real window and calls no real getScreenDetails; requestFullscreen + getScreenDetails are stubbed, holding the never-open-a-real-window line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-07-01 | Information Disclosure | blackout not hiding the slide regression | high | mitigate | The overlay blocks assert audience/confidence-blackout present on blackout:true and absent on false — a regression that failed to render the overlay fails here. |
| T-97-07-02 | Denial of Service | self-fullscreen throwing on mount | high | mitigate | The no-throw block mounts with no getScreenDetails and asserts no throw + a plain auto-attempt; the resolvable block asserts requestFullscreen({screen}) — a throw or a missing call fails here. |
| T-97-07-03 | Tampering | confidence reflow / background reveal regression | medium | mitigate | The layout block re-asserts suppression + last-slide no-reflow alongside flex-row/border-l; a regression to vertical or a collapsing next-region fails here. |
| T-97-07-SC | Tampering | package installs (npm) | n/a | accept | No installs — the suites exercise existing modules only. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/AudienceOutputView.test.ts src/views/__tests__/ConfidenceOutputView.test.ts`
  green; `npx vitest run` shows only the baseline (src/storage.rules.test.ts); `npm run type-check` clean. Every
  window/screen/channel message faked; requestFullscreen + getScreenDetails stubbed; no real window opened. No
  `--dir src`; no rules suite.
- HUMAN-UAT (deferred): the real cross-monitor auto-fullscreen + confidence left/right legibility + blackout on
  the projector remain milestone-end hardware UAT.
</verification>

<success_criteria>
- Behavioural coverage proves the outputs black out / clear on the channel field (R280), self-fullscreen on their
  assigned monitor with a safe fallback (R278), and the confidence window is a left/right split with suppression
  and no-reflow intact (R279) — all faked, no baseline regression.
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-07-SUMMARY.md` when done.
</output>
