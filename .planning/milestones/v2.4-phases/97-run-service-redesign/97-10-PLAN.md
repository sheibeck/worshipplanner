---
phase: 97-run-service-redesign
plan: 10
type: execute
wave: 5
depends_on: ["97-09"]
files_modified:
  - src/views/__tests__/RunControlView.test.ts
  - src/views/__tests__/RunControlView.output.test.ts
autonomous: true
requirements: [R276, R277, R280, R282, R283]

must_haves:
  truths:
    - "REHEARSE WITHOUT SCREENS (R283): a new block clicks run-rehearse-btn and asserts NO window.open is called (openSpy not called), the view enters the live State B (run-current-preview appears / run-go-live-btn no longer shown), and slide 0 has been posted on the injected fake channel — proving rehearse drives the control without opening any output window."
    - "LIVE STATUS GREEN (R277): before go-live/rehearse the live status (run-live-status) is muted/amber ('Not open', no green token); after run-rehearse-btn (and, in the output suite, after a matched go-live) it is GREEN — asserted by the presence of the green token/class on run-live-status only once live."
    - "BLACKOUT (R280): a new block asserts the injected fake channel: pressing the 'B' key posts a state with blackout:true (and a strictly higher seq than the prior post), pressing 'B' again posts blackout:false; clicking run-blackout-btn posts blackout:true and run-clear-btn posts blackout:false. In the output suite (real go-live via faked window/screen), the same holds during a genuine live session. Every post keeps the seq monotonic."
    - "FILMSTRIP JUMP (R282) + NEXT-UP SCALE (R276): after entering live (rehearse), clicking a run-filmstrip-slide thumb posts that slide's global array index on the fake channel (the last state.index equals the clicked thumb's index); and run-next-preview renders the next slide inside a transform: scale container (smaller next-up font present)."
    - "GO-LIVE FROM PRE-FLIGHT (R276): the output suite drives go-live through run-go-live-btn now located in the State-A pre-flight panel (faking getScreenDetails/window.open/saveMapping), reaching run-status-placed and live — proving owner fix #5 (Go-live relocated) end-to-end. Every window/screen/channel message is faked; no real window opens; fake timers drive any poll/clock interaction."
  artifacts:
    - "src/views/__tests__/RunControlView.test.ts — new blocks: rehearse (no window.open, live green, slide 0), B-key + Black/Clear blackout posts (monotonic seq), filmstrip jump posts the array index, next-up scale present."
    - "src/views/__tests__/RunControlView.output.test.ts — new blocks: go-live from the pre-flight run-go-live-btn reaches placed + live-green; blackout posts during a real live session."
  key_links:
    - "run-rehearse-btn click -> rehearse() -> no openSpy call + live true (State B, run-current-preview) + fake.states() last index 0."
    - "'B' keydown / run-blackout-btn / run-clear-btn -> postBlackout -> fake.states() last message blackout true/false with strictly increasing seq."
    - "run-filmstrip-slide[n] click -> postIndex(indices[n]) -> fake.states() last index === that thumb's global index; run-next-preview inner canvas wrapped in a scale container."
    - "run-go-live-btn (pre-flight) click + faked getScreenDetails/window.open -> openPlaced -> run-status-placed + live green."
---

<objective>
Prove the Phase 97 control behaviours end-to-end against the REAL redesigned view + the injected fake channel:
rehearse without screens (R283), the honest green-when-live status (R277), blackout via the B key + Black/Clear
buttons (R280), the in-item filmstrip click-to-jump posting the correct global index and the smaller next-up
scale (R282/R276), and go-live from the relocated pre-flight panel (R276 owner fix #5). Every window, screen,
timer, and channel message is faked; no real window opens.

Purpose: R276, R277, R280, R282, R283 behavioural coverage for the control. Complements 97-07 (output side) and
the pre-97 regression suites (kept green through 97-09).

Output: extended RunControlView.test.ts + RunControlView.output.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-UI-SPEC.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/views/RunControlView.vue
@src/composables/useRunControl.ts
@src/components/run/RunPreflightPanel.vue
@src/components/run/RunFilmstrip.vue
@src/components/run/RunPreviewPair.vue
@src/views/__tests__/RunControlView.test.ts
@src/views/__tests__/RunControlView.output.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunControlView.test.ts — rehearse, live-green, B-key/Black-Clear blackout, filmstrip jump, next-up scale</name>
  <files>src/views/__tests__/RunControlView.test.ts</files>
  <read_first>
    - src/views/__tests__/RunControlView.test.ts — createFakeChannel (posted/states()/emitHello, :200-225), keydown
      helper (:227-229), mountView (:231-238), the beforeEach stubbing window.open→null + deleting getScreenDetails
      (:242-251). Reuse them; rehearse needs NO window mocking (it opens no window).
    - src/components/run/RunPreflightPanel.vue (run-rehearse-btn), RunFilmstrip.vue (run-filmstrip-slide +
      :data-index), RunPreviewPair.vue (run-next-preview + the scale container), RunHeader.vue (run-live-status +
      the green-when-live token).
    - src/composables/useRunControl.ts (97-08) — rehearse() sets live + posts slide 0 without window.open;
      postBlackout advances seq; the 'B' key toggles blackout; filmstrip @jump -> postIndex.
  </read_first>
  <action>
    Add these describe blocks (reuse the fake channel + keydown helpers; scope fake timers only where a clock/poll
    interaction needs it):
    1. REHEARSE (R283): mount; record openSpy call count; `await wrapper.find('[data-testid="run-rehearse-btn"]')
       .trigger('click'); await flushPromises()`. Assert window.open was NOT called (openSpy calls unchanged), the
       view is now live (run-current-preview exists AND run-go-live-btn no longer rendered), and fake.states() has
       an index-0 post. 
    2. LIVE GREEN (R277): before rehearse, assert run-live-status does NOT carry the green token (muted/amber);
       after run-rehearse-btn, assert run-live-status carries the green token/class.
    3. BLACKOUT via B + Black/Clear (R280): after rehearse (live), press keydown('b'); await; assert fake.states()
       last message blackout === true with a strictly higher seq than the previous; press keydown('b') again →
       blackout false. Then click run-blackout-btn → blackout true; click run-clear-btn → blackout false. Assert the
       seq stays strictly increasing across all posts.
    4. FILMSTRIP JUMP (R282): after rehearse, navigate so the active item has >1 slide (the default fixture's slot 0
       has slides at indices 0 and 1); find the run-filmstrip-slide thumbs; click the thumb whose data-index is 1;
       assert fake.states() last index === 1. (If the default fixture's active item has a single slide, extend the
       hoisted fixture so slot 0 has at least two assembled slides — mirror the existing H.defaultSlides shape.)
    5. NEXT-UP SCALE (R276): after rehearse at an index with a next slide, assert run-next-preview contains a scale
       transform (a scale class or transform:scale style) around its canvas — the smaller next-up font is present.
    Keep every existing block green; never open a real window.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RunControlView.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-rehearse-btn" src/views/__tests__/RunControlView.test.ts &amp;&amp; grep -q "run-blackout-btn" src/views/__tests__/RunControlView.test.ts &amp;&amp; grep -q "run-filmstrip-slide" src/views/__tests__/RunControlView.test.ts &amp;&amp; grep -q "run-live-status" src/views/__tests__/RunControlView.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New blocks prove: rehearse opens no window + enters live + posts slide 0; run-live-status is green only once
      live; the B key and Black/Clear buttons post blackout true/false with monotonic seq; a filmstrip thumb click
      posts that thumb's global index; run-next-preview renders a scaled next-up. Every prior block green;
      `npx vitest run` shows only the baseline; type-check clean; no `--dir src`.
  </acceptance_criteria>
  <done>RunControlView.test.ts proves rehearse-without-screens, the honest green-when-live status, blackout via B + Black/Clear (seq-correct), filmstrip click-to-jump, and the smaller next-up — all against the real view + fake channel, no real window opened.</done>
</task>

<task type="auto">
  <name>Task 2: RunControlView.output.test.ts — go-live from the pre-flight panel + blackout in a live session</name>
  <files>src/views/__tests__/RunControlView.output.test.ts</files>
  <read_first>
    - src/views/__tests__/RunControlView.output.test.ts — the go-live driver (clicks run-go-live-btn, fakes
      getScreenDetails/window.open/saveMapping) and the run-status-placed assertion; the fake channel capturing
      posts; the fake-timer poll idiom. run-go-live-btn now lives in the State-A pre-flight panel — the click
      target is the same testid, only its location changed.
    - src/composables/useRunControl.ts (97-08) — go-live success sets live=true + startElapsed; postBlackout in a
      live session posts blackout with a seq bump.
    - src/components/run/RunHeader.vue — run-live-status green token; RunPreflightPanel run-go-live-btn.
  </read_first>
  <action>
    Add these blocks (reuse the existing go-live driver + fake channel + fake timers):
    1. GO-LIVE FROM PRE-FLIGHT (R276 owner fix #5): confirm the existing matched-go-live path still works with
       run-go-live-btn located in the pre-flight panel — after a matched go-live, run-status-placed is present AND
       run-live-status carries the green token (live). If the existing go-live helper already clicks run-go-live-btn
       by testid, this may be an added assertion (green + placed) rather than a new driver.
    2. BLACKOUT IN A LIVE SESSION (R280): after a matched go-live (placed), press keydown('b') (or click
       run-blackout-btn) and assert the fake channel's last state message has blackout:true with a strictly higher
       seq; Clear/second-B posts blackout:false. This exercises blackout on the REAL placed path (both output
       windows faked-open), complementing the rehearse-path coverage in Task 1.
    Keep the recovery/reassign/WR-01/no-leak blocks unchanged and green. Every window/screen/timer is faked.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-go-live-btn" src/views/__tests__/RunControlView.output.test.ts &amp;&amp; grep -q "blackout" src/views/__tests__/RunControlView.output.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New/added assertions prove go-live from the pre-flight run-go-live-btn reaches run-status-placed + a green
      live status, and blackout posts (blackout:true/false, monotonic seq) during a real placed live session; the
      recovery/WR-01/no-leak blocks stay green. `npx vitest run` shows only the baseline; type-check clean; no
      `--dir src`; no rules suite.
  </acceptance_criteria>
  <done>RunControlView.output.test.ts proves go-live from the relocated pre-flight panel reaches placed + green-live and blackout posts correctly in a real live session, with the Phase 96 recovery coverage intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> real view + fake channel | Rehearse/blackout/filmstrip/go-live drive the REAL useRunControl through the injected channelFactory; a regression in the seq bump, the no-window rehearse, or the filmstrip index fails here against production logic |
| test file -> real window/screen | window.open is stubbed (null in the control suite; fake wins in the output suite) and getScreenDetails is faked; no real window opens, holding the never-open-a-real-window line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-10-01 | Repudiation | rehearse opening a real window / false live | high | mitigate | The rehearse block asserts openSpy is NOT called and the view is live + slide 0 posted — a regression routing rehearse through openOutputs fails here. |
| T-97-10-02 | Tampering | blackout dropped by stale-drop / wrong seq | high | mitigate | The blackout blocks assert the last channel state is blackout true/false with a strictly increasing seq (B key + Black/Clear + live-session) — a missing seq bump fails here. |
| T-97-10-03 | Tampering | filmstrip jumping to the wrong slide | medium | mitigate | The filmstrip block asserts the posted index equals the clicked thumb's global index — the array-index contract from 97-05 proven at the view level. |
| T-97-10-SC | Tampering | package installs (npm) | n/a | accept | No installs — the suites exercise existing modules only. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/RunControlView.test.ts src/views/__tests__/RunControlView.output.test.ts`
  green; `npx vitest run` shows only the baseline (src/storage.rules.test.ts); `npm run type-check` clean. Every
  window/screen/timer/channel message faked; no real window opened. No `--dir src`; no rules suite.
- HUMAN-UAT (deferred): the real go-live → auto-fullscreen, timers, filmstrip navigation, blackout on the
  projector, and closed/unplug recovery on hardware remain the milestone-end UAT set.
</verification>

<success_criteria>
- Behavioural coverage proves rehearse-without-screens (R283), the honest green-when-live status (R277), blackout
  via the B key + Black/Clear with monotonic seq (R280), the in-item filmstrip click-to-jump posting the correct
  global index + the smaller next-up (R282/R276), and go-live from the relocated pre-flight panel (R276) — all
  against the real view, no real window opened, no baseline regression.
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-10-SUMMARY.md` when done.
</output>
