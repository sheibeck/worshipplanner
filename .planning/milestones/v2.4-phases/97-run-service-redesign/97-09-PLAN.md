---
phase: 97-run-service-redesign
plan: 09
type: execute
wave: 4
depends_on: ["97-08", "97-04", "97-05", "97-06"]
files_modified:
  - src/views/RunControlView.vue
  - src/views/__tests__/RunControlView.test.ts
autonomous: true
requirements: [R276, R277, R280, R281, R282, R283]

must_haves:
  truths:
    - "STATE GATING BY `live` (R276/R277): RunControlView's template redesigns into State A (pre-flight, shown when !live) and State B (live, shown when live). RunHeader renders in BOTH states (service name run-service-name, live status GREEN only when live, clock+elapsed, displays dots, an End-service button run-exit-btn emitting @exit->openExitConfirm). The order-of-service rail (RunRail) renders in BOTH states. The channel/keyboard/nav machinery stays ACTIVE FROM MOUNT (unchanged) so the pre-97 seq/rail/keyboard/exit tests stay green."
    - "TESTID PRESERVATION IS THE HARD GATE: the ENTIRE existing output-status detail cluster (run-status-opening, run-status-placed, run-output-ready-audience/confidence, run-output-closed-audience/confidence, run-reopen-audience/confidence, run-output-closed-*-muted) and the banner band (run-reassign-banner, run-reassign-reopen, run-reassign-setup-link, run-fallback-banner, run-blocked-banner, run-blocked-retry, run-partial-banner, run-partial-retry, run-go-live-retry) stay INLINE in RunControlView VERBATIM — same testids, same outputStatus/recovery v-if conditions — so RunControlView.output.test.ts passes WITHOUT edits. The ONLY tested testid that MOVES is run-go-live-btn: it relocates from the old corner idle cluster into RunPreflightPanel (State A) — output.test.ts finds it by testid regardless of location (owner fix #5)."
    - "STATE A body (R276): when !live, the center shows RunPreflightPanel (@go-live->openOutputs, @rehearse->rehearse, props allRendered/renderedCount/slideCount/audienceLabel/confidenceLabel) beside the rail. STATE B body (R276/R282): when live, the center shows RunPreviewPair (:current/:next/:live, next-up scaled), RunFilmstrip (:slides=filmstripSlides :indices=filmstripIndices :currentIndex=index, @jump->postIndex), an Output panel with Black/Clear buttons (data-testid run-blackout-btn/run-clear-btn -> postBlackout(true/false)), RunDisplaysPanel (@reopen->reopenOutput, @manage->openManage), and RunTransportBar (:progress :positionLabel, @prev->goBySlide(-1), @next->goBySlide(1)). The inline recovery rows/banners still render (driven by outputStatus/recovery refs) so their testids persist."
    - "GREEN-KEEP THE ONE CHANGED TEST: the ONLY pre-97 control test that legitimately changes is RunControlView.test.ts's dual-preview block (:505-526), which asserted run-current-preview/run-next-preview PRE-live; in the redesign previews live in State B, so that block is reworked to click run-rehearse-btn (enter live WITHOUT windows) FIRST, then assert the previews + 'End of service'. Every other pre-97 control test (seq/rail/keyboard/exit/no-push-live) passes UNCHANGED because the channel/rail/keyboard stay active from mount and the rail + run-service-name + run-exit-btn render in State A."
    - "NOCTURNE RUN-SCOPED PALETTE (97-UI-SPEC): the Run root(s) adopt the blurple #9184d9 accent as local CSS custom properties / scoped classes; the rest of the app is NOT rethemed. No run-take/run-push-live control is introduced (single-selection contract preserved)."
  artifacts:
    - "src/views/RunControlView.vue — redesigned template: RunHeader + State A (RunPreflightPanel + RunRail) / State B (RunRail + RunPreviewPair + RunFilmstrip + Output panel + RunDisplaysPanel + RunTransportBar), the inline output-status cluster + banner band preserved verbatim, gated by live; script binds the 97-08 composable surface."
    - "src/views/__tests__/RunControlView.test.ts — the dual-preview block reworked to rehearse-first; all other blocks unchanged."
  key_links:
    - "!live -> State A (RunPreflightPanel center + RunRail); live -> State B (RunRail + RunPreviewPair + RunFilmstrip + Output panel + RunDisplaysPanel + RunTransportBar). RunHeader + inline output-status cluster + banner band render in both (driven by outputStatus/recovery refs)."
    - "emit wiring: RunPreflightPanel @go-live->openOutputs, @rehearse->rehearse; RunFilmstrip @jump(i)->postIndex(i); RunRail @jump(slot)->jumpToSlot, @jump-slide(i)->postIndex(i); RunTransportBar @prev->goBySlide(-1)/@next->goBySlide(1); Black/Clear->postBlackout(true/false); RunDisplaysPanel @reopen(role)->reopenOutput/@manage->openManage; RunHeader @exit->openExitConfirm."
    - "run-go-live-btn moves to RunPreflightPanel; the inline run-status-*/run-output-*/run-reopen-*/run-reassign-*/run-fallback-*/run-blocked-*/run-partial-* testids + conditions stay verbatim -> output.test.ts green with no edit."
---

<objective>
Redesign RunControlView's template into the owner-approved pre-flight (State A) / live (State B) experience,
wiring the seven presentational children (RunHeader, RunPreflightPanel, RunRail, RunPreviewPair, RunFilmstrip,
RunTransportBar, RunDisplaysPanel) and the 97-08 composable surface — while PRESERVING every existing testid +
condition so RunControlView.output.test.ts stays green with no edits, and reworking the single dual-preview
control test that legitimately moves behind go-live/rehearse.

Purpose: R276 (pre-flight + live states + preview split + smaller next-up), R277 (green-when-live), R280 (Black/
Clear controls), R281 (clock/elapsed), R282 (filmstrip), R283 (rehearse) — all wired into the real view.

Output: the redesigned RunControlView.vue + the one reworked control test. New behavioural coverage lands in 97-10.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-UI-SPEC.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/views/RunControlView.vue
@src/composables/useRunControl.ts
@src/components/run/RunHeader.vue
@src/components/run/RunPreflightPanel.vue
@src/components/run/RunRail.vue
@src/components/run/RunPreviewPair.vue
@src/components/run/RunFilmstrip.vue
@src/components/run/RunTransportBar.vue
@src/components/run/RunDisplaysPanel.vue
@src/views/__tests__/RunControlView.test.ts
@src/views/__tests__/RunControlView.output.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header + State gating — RunHeader in both states, State A pre-flight, run-go-live-btn relocation, inline status/banners preserved</name>
  <files>src/views/RunControlView.vue</files>
  <read_first>
    - src/views/RunControlView.vue — the WHOLE template. The header (:9-203): the service name (:11,
      run-service-name), the output-status cluster idle/opening/placed/blocked (:20-182) with run-status-opening/
      run-status-placed/run-go-live-retry + the placed ready/closed rows (run-output-ready/closed-*, run-reopen-*,
      run-*-muted), and the exit button (:184-202, run-exit-btn). The banner band (:205-386): run-reassign-banner
      + run-reassign-reopen + run-reassign-setup-link, run-fallback-banner, run-blocked-banner + run-blocked-retry,
      run-partial-banner + run-partial-retry. The main region (:388-512) rail + preview. The footer legend
      (:514-534). The exit dialog (:536-572).
    - src/composables/useRunControl.ts (97-08) — the bindings to destructure: live, blackout, postBlackout,
      rehearse, clock, elapsed, allRendered, renderedCount, slideCount, audienceLabel, confidenceLabel,
      audienceOpen, confidenceOpen, positionLabel, progress, filmstripSlides, filmstripIndices, openManage, plus
      the pre-existing outputStatus/recovery/nav/rail bindings.
    - src/components/run/RunHeader.vue (:props/emits) + src/components/run/RunPreflightPanel.vue (run-go-live-btn +
      run-rehearse-btn).
    - src/views/__tests__/RunControlView.output.test.ts — scan for EVERY data-testid it queries; that set is the
      preservation contract for this task.
  </read_first>
  <action>
    Destructure the full 97-08 surface from useRunControl in &lt;script setup&gt; and register the child components.

    HEADER (both states): replace the old top-bar row's service-name + exit button with `<RunHeader
    :serviceHeading :live :positionLabel :clock :elapsed :audienceOpen :confidenceOpen @exit="openExitConfirm"
    @reopen="reopenOutput" @manage="openManage" />`. RunHeader carries run-service-name + run-exit-btn (the
    identifiers the exit/name tests read).

    PRESERVE INLINE (verbatim testids + conditions), positioned in the header/banner region:
    - the output-status DETAIL cluster's opening/placed/blocked branches (run-status-opening, run-status-placed +
      the ready/closed rows run-output-ready/closed-*, run-reopen-*, run-*-muted, run-go-live-retry) — keep them
      exactly, driven by outputStatus + the recovery refs. DELETE ONLY the idle branch's corner Go-live button
      (run-go-live-btn moves to State A). If run-go-live-retry (the blocked-state inline retry) must remain, keep it.
    - the ENTIRE banner band (run-reassign-banner + run-reassign-reopen + run-reassign-setup-link, run-fallback-
      banner, run-blocked-banner + run-blocked-retry, run-partial-banner + run-partial-retry) verbatim.

    STATE A (v-if="!live"): the center column shows `<RunPreflightPanel :serviceName :slideCount :itemCount
    :renderedCount :allRendered :audienceLabel :confidenceLabel @go-live="openOutputs" @rehearse="rehearse" />`
    (run-go-live-btn lives here now) beside the rail (RunRail — added in Task 2). The State-A footer note
    'Nothing is on the screens yet. Slides advance only after you go live.' per 97-UI-SPEC.

    Keep the exit dialog (:536-572, run-exit-dialog/run-exit-cancel/run-exit-confirm) unchanged. Apply the Nocturne
    Run-scoped palette (blurple #9184d9 as local CSS vars / scoped classes on the Run root) — do NOT retheme the app.
    Do NOT alter the &lt;script&gt;'s channel/keyboard lifecycle (it lives in the composable). Do NOT introduce
    run-take/run-push-live.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>grep -q "RunHeader" src/views/RunControlView.vue &amp;&amp; grep -q "RunPreflightPanel" src/views/RunControlView.vue &amp;&amp; grep -q "run-status-placed" src/views/RunControlView.vue &amp;&amp; grep -q "run-reassign-banner" src/views/RunControlView.vue &amp;&amp; grep -q "run-fallback-banner" src/views/RunControlView.vue</automated>
    <automated>grep -q "v-if=\"!live\"\|v-if=\"live\"" src/views/RunControlView.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunHeader renders in both states (run-service-name + run-exit-btn preserved); State A shows RunPreflightPanel
      (run-go-live-btn relocated here) when !live; the inline output-status cluster (run-status-*, run-output-*,
      run-reopen-*) and the full banner band (run-reassign-*, run-fallback/blocked/partial-*) are preserved
      verbatim with their conditions; the exit dialog is unchanged.
    - `npx vitest run src/views/__tests__/RunControlView.output.test.ts` passes with NO edits to that file;
      type-check clean.
  </acceptance_criteria>
  <done>The header + State-A pre-flight are wired with RunHeader/RunPreflightPanel, run-go-live-btn relocated, and every tested output-status/recovery testid preserved inline — output.test.ts green without edits.</done>
</task>

<task type="auto">
  <name>Task 2: State B body — rail, preview pair, filmstrip, Output panel (Black/Clear), displays panel, transport</name>
  <files>src/views/RunControlView.vue</files>
  <read_first>
    - src/views/RunControlView.vue — the main region (:388-512, rail + dual preview) being replaced by the child
      components, and the footer legend (:514-534) being replaced by RunTransportBar.
    - src/components/run/RunRail.vue (rail-item/rail-item-empty/run-rail-empty + @jump/@jump-slide), RunPreviewPair
      (run-current-preview/run-next-preview), RunFilmstrip (@jump(index)), RunTransportBar (@prev/@next),
      RunDisplaysPanel (@reopen/@manage).
    - src/composables/useRunControl.ts — railRows, index (currentIndex), jumpToSlot, postIndex, goBySlide,
      filmstripSlides/filmstripIndices, progress, positionLabel, current/next, live, postBlackout, reopenOutput,
      openManage, audienceOpen/confidenceOpen, audienceLabel/confidenceLabel.
    - src/views/__tests__/RunControlView.test.ts rail (:353-411) + preview (:505-526) tests — RunRail must keep
      rail-item/rail-item-empty/run-rail-empty and RunPreviewPair run-current-preview/run-next-preview + 'End of
      service'.
  </read_first>
  <action>
    RAIL (both states): render `<RunRail :rows="railRows" :activeIndex="currentSlotIndex" :expandedSlides="..."
    @jump="jumpToSlot" @jump-slide="postIndex" />` as the left rail in State A AND State B (the rail is present in
    both per 97-UI-SPEC). Provide expandedSlides for the active item from filmstripSlides/filmstripIndices (each
    { arrayIndex, label, isCurrent }) so the active order item expands to its slides. RunRail preserves rail-item/
    rail-item-empty/run-rail-empty.

    STATE B center (v-if="live"):
    - `<RunPreviewPair :current="current" :next="next" :live="live" />` — run-current-preview / run-next-preview
      (next-up scaled), 'End of service' when next null.
    - `<RunFilmstrip :slides="filmstripSlides" :indices="filmstripIndices" :currentIndex="index"
      @jump="postIndex" />` — click-to-jump within the active item (R282).
    - an Output panel: a Black button (data-testid="run-blackout-btn", @click="postBlackout(true)") and a Clear
      button (data-testid="run-clear-btn", @click="postBlackout(false)"); indicate the current blackout state
      (e.g. Black is active when blackout is true). (OMIT Logo — no asset.)
    - `<RunDisplaysPanel :audience="{ open: audienceOpen, label: audienceLabel }" :confidence="{ open:
      confidenceOpen, label: confidenceLabel }" :live="live" @reopen="reopenOutput" @manage="openManage" />` — the
      additive displays surface (the tested recovery rows stay inline from Task 1).
    - `<RunTransportBar :progress="progress" :positionLabel="positionLabel" @prev="goBySlide(-1)"
      @next="goBySlide(1)" />` at the bottom (replaces the old footer legend).

    Wire every emit exactly as the key_links table specifies. Do NOT re-implement any post in the template — route
    intents to the composable functions. Nocturne palette scoping continues here. No run-take/run-push-live.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>grep -q "RunFilmstrip" src/views/RunControlView.vue &amp;&amp; grep -q "run-blackout-btn" src/views/RunControlView.vue &amp;&amp; grep -q "run-clear-btn" src/views/RunControlView.vue &amp;&amp; grep -q "RunPreviewPair" src/views/RunControlView.vue &amp;&amp; grep -q "RunTransportBar" src/views/RunControlView.vue</automated>
    <automated>! grep -qE "run-take|run-push-live" src/views/RunControlView.vue</automated>
  </verify>
  <acceptance_criteria>
    - RunRail (both states) preserves rail-item/rail-item-empty/run-rail-empty and expands the active item; State B
      shows RunPreviewPair (run-current-preview/run-next-preview), RunFilmstrip (@jump->postIndex), an Output panel
      (run-blackout-btn/run-clear-btn->postBlackout), RunDisplaysPanel (@reopen->reopenOutput/@manage->openManage),
      and RunTransportBar (@prev/@next->goBySlide); every emit routes to a composable function; no run-take/
      run-push-live.
    - output.test.ts still passes with no edits; type-check clean.
  </acceptance_criteria>
  <done>State B is wired end-to-end — rail, preview pair, filmstrip, Black/Clear, displays panel, and transport bar — every action routed to the composable, with the tested recovery testids intact.</done>
</task>

<task type="auto">
  <name>Task 3: Green-keep RunControlView.test.ts (dual-preview -> rehearse-first) + full-suite verification</name>
  <files>src/views/__tests__/RunControlView.test.ts</files>
  <read_first>
    - src/views/__tests__/RunControlView.test.ts — the dual-preview block (:505-526) asserting run-current-preview/
      run-next-preview + 'End of service' PRE-live; and the surrounding blocks (seq :270-351, rail :353-411,
      keyboard :414-456, exit :458-503, no-push-live :528-542) that must stay UNCHANGED and green.
    - src/components/run/RunPreflightPanel.vue — the run-rehearse-btn to click to enter live without windows.
    - src/composables/useRunControl.ts — rehearse() sets live=true without window.open, so State B (previews)
      renders after clicking run-rehearse-btn.
  </read_first>
  <action>
    Rework ONLY the dual-preview block: before asserting the previews, click run-rehearse-btn (`await wrapper.find(
    '[data-testid="run-rehearse-btn"]').trigger('click'); await flushPromises()`) to enter live (State B) WITHOUT
    opening any window, THEN assert run-current-preview shows 'a', run-next-preview shows 'b' after mount, 'b'/'c'
    after ArrowRight, and 'c' + 'End of service' on the last slide (the same assertions, now behind rehearse).
    Keep the 'no push-to-live control' block (:528-542) as-is (run-push-live/run-take still absent; nav still posts
    from mount).

    Do NOT edit any other block. Run the full control + output suites to confirm green. If (and only if) a
    non-preview block genuinely breaks because a tested testid's CONDITION changed, STOP and flag it — the design
    intent is that only the dual-preview block changes; a second broken block signals a testid-preservation miss in
    Task 1/2 that must be fixed in the SOURCE, not papered over in the test.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/views/__tests__/RunControlView.test.ts src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-rehearse-btn" src/views/__tests__/RunControlView.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The dual-preview block clicks run-rehearse-btn to reach State B, then asserts run-current-preview/
      run-next-preview + 'End of service' unchanged; every other pre-97 control block passes UNTOUCHED.
    - `npx vitest run src/views/__tests__/RunControlView.test.ts src/views/__tests__/RunControlView.output.test.ts`
      is green; `npx vitest run` shows only the baseline (src/storage.rules.test.ts); type-check clean; no
      `--dir src`.
  </acceptance_criteria>
  <done>The one control test whose behaviour legitimately moved behind go-live is reworked to rehearse-first; the whole control + output suites are green, confirming the redesign preserved every other tested testid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| redesigned template -> pre-97 test suites | The output.test.ts recovery/status assertions are the preservation contract; keeping the inline output-status cluster + banner band verbatim (testids + conditions) means the redesign cannot silently drop a tested honesty surface |
| child emits -> composable functions | Every child intent (go-live/rehearse/jump/prev/next/blackout/reopen/manage/exit) routes to a useRunControl function; no child posts to the channel itself, preserving the single-writer contract |
| live gating vs channel activity | State A/B is presentational; the channel/keyboard/nav stay active from mount, so pre-live navigation still posts (harmless, no output open) and the seq/keyboard tests stay green |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-09-01 | Tampering | dropping a tested honesty testid in the redesign | high | mitigate | The inline output-status cluster + banner band stay verbatim (run-status-*/run-output-*/run-reopen-*/run-reassign-*/run-fallback-*/run-blocked-*/run-partial-*); output.test.ts is the untouched gate; only run-go-live-btn relocates. |
| T-97-09-02 | Repudiation | pre-live green / red status | medium | mitigate | RunHeader's status is green only when live; State A shows amber 'Not open'; live is set only by go-live/rehearse (97-08). |
| T-97-09-03 | Elevation of Privilege | Black/Clear or filmstrip posting outside the single writer | medium | mitigate | Black/Clear route to postBlackout and filmstrip to postIndex — both composable-owned single-writer posts; no child opens its own channel. |
| T-97-09-SC | Tampering | package installs (npm) | n/a | accept | No installs — view + test edits only. |
</threat_model>

<verification>
- `npm run type-check` clean; `npx vitest run` shows only the baseline (src/storage.rules.test.ts).
  RunControlView.output.test.ts passes WITHOUT edits; RunControlView.test.ts passes with ONLY the dual-preview
  block reworked to rehearse-first. No `--dir src`; no rules suite.
- Manual check: State A (pre-flight, run-go-live-btn relocated) shows when !live and State B (previews/filmstrip/
  transport/Black-Clear/displays) when live; RunHeader carries run-service-name/run-exit-btn in both; the inline
  output-status cluster + banner band are verbatim; the channel/keyboard stay active from mount; Nocturne palette
  is Run-scoped only; no run-take/run-push-live.
- HUMAN-UAT (deferred): the full redesign on real hardware — pre-flight → go-live feel, timers, filmstrip
  navigation, blackout, the overall run/stop experience — added to the v2.4 DEFERRED-HUMAN-UAT set.
</verification>

<success_criteria>
- The control screen presents the pre-flight (State A, centered Go-live) and live (State B, program/next-up split,
  in-item filmstrip, transport bar) states with a green-when-live status, clock/elapsed, Black/Clear, a rehearse
  entry, and the displays panel (R276/R277/R280/R281/R282/R283) — every phase 92-96 correctness testid preserved,
  output.test.ts green untouched, only the dual-preview test reworked.
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-09-SUMMARY.md` when done.
</output>
