---
phase: 97-run-service-redesign
plan: 08
type: execute
wave: 3
depends_on: ["97-01", "97-04"]
files_modified:
  - src/composables/useRunControl.ts
autonomous: true
requirements: [R276, R277, R280, R281, R282, R283]

must_haves:
  truths:
    - "LIVE FLAG (R277): useRunControl gains `const live = ref(false)` set true by BOTH a successful go-live (in openPlaced AND openUnplaced, at the same point startClosedPoll is called) AND by rehearse() — NOT by outputStatus alone. partial/blocked do NOT set live (outputs did not both open). live is reset to false in confirmExit. The header reads live for its green-when-live status; pre-live it stays amber/'Not open' (owner fix #4)."
    - "BLACKOUT (R280): `const blackout = ref(false)`; postIndex and resendCurrent now post `blackout: blackout.value` (NOT a hard-coded false); a new postBlackout(v: boolean) sets blackout.value = v then, if index.value != null, seq += 1 and handle.postState({ index: index.value, blackout: blackout.value, seq }) — mirroring resendCurrent's seq-advance-then-post so runChannel's stale-drop accepts it. The 'B' key in handleKeydown (the reserved slot) calls postBlackout(!blackout.value). Because blackout defaults false, every existing post is still { blackout: false } and the pre-97 suites stay green."
    - "REHEARSE (R283): rehearse() enters the live UI WITHOUT opening any window — it sets live.value = true and calls startElapsed(), and (if index.value is still null and slides exist) posts slide 0 via postIndex(0); it NEVER calls openPlaced/openUnplaced/openWindow/getScreenDetails, so NO window.open fires. outputStatus stays 'idle' (honest: no screens), the displays dots stay amber 'Not open', but live drives State B."
    - "TIMERS + READINESS + FILMSTRIP DERIVATIONS (R281/R276/R282): useRunControl wires useRunTimers ({ clock, elapsed, startElapsed, resetElapsed }) — startElapsed on the first go-live/rehearse, resetElapsed in confirmExit; and exposes computeds allRendered (every assembled slide's slide.renderState is undefined — honest; renderState lives on AssembledSlide.slide/SlideBase), renderedCount, slideCount, audienceLabel/confidenceLabel (from loadMapping() fingerprints), audienceOpen/confidenceOpen (placed|fallback && !closed), positionLabel, progress, and the in-item filmstrip { filmstripSlides, filmstripIndices } (assembledSlideshow filtered by currentSlotIndex, PAIRED with each slide's global array index), plus openManage()."
    - "GREEN GATE: RunControlView.test.ts + RunControlView.output.test.ts stay green with NO edits — the new bindings are ADDED to the composable's return (never removing an existing one), the still-old template (from 97-01) does not render them, blackout defaults false so existing { blackout:false } post assertions pass, and the 'B' key is unexercised by those suites."
  artifacts:
    - "src/composables/useRunControl.ts — live/blackout/postBlackout + 'B' key, rehearse(), useRunTimers wiring, the readiness/label/open/position/progress/filmstrip computeds, openManage(), and an extended return object exposing all of them for 97-09."
  key_links:
    - "openPlaced/openUnplaced success -> live.value = true + startElapsed(); rehearse() -> live.value = true + startElapsed() + (postIndex(0) if index null) with NO window.open; confirmExit -> live.value=false + blackout.value=false + resetElapsed()."
    - "postIndex/resendCurrent post blackout: blackout.value; postBlackout(v) -> seq+1 + postState({ index, blackout:v, seq }); 'B' key -> postBlackout(!blackout.value)."
    - "filmstrip: forEach(assembledSlideshow) collecting slides + global indices where slotIndex === currentSlotIndex -> { filmstripSlides, filmstripIndices } consumed by RunFilmstrip (@jump(index) -> postIndex)."
---

<objective>
Add every control-side FEATURE to the extracted composable (useRunControl.ts) so the redesigned template
(97-09) is pure wiring: the honest live flag (R277), blackout state + postBlackout + the B key (R280), rehearse
mode (R283), the clock/elapsed timers (R281), and the pre-flight readiness + in-item filmstrip + header/transport
derivations (R276/R282). All bindings are ADDED to the composable's return; the old template still works, so the
pre-97 control suites stay green.

Purpose: R277, R280, R281, R283 and the R276/R282 data the children need. This is the composable brain; 97-09 is
the template; 97-10 is the behavioural coverage.

Output: an extended src/composables/useRunControl.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-UI-SPEC.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/composables/useRunControl.ts
@src/composables/useRunTimers.ts
@src/utils/runChannel.ts
@src/utils/monitorConfig.ts
@src/utils/serviceSlots.ts
@src/types/slide.ts
@src/views/__tests__/RunControlView.test.ts
@src/views/__tests__/RunControlView.output.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: live flag + blackout (postBlackout + B key) + rehearse + timers wiring</name>
  <files>src/composables/useRunControl.ts</files>
  <read_first>
    - src/composables/useRunControl.ts (97-01) — postIndex/resendCurrent (the two posters that hard-code
      blackout:false), handleKeydown's reserved 'B' slot, openPlaced/openUnplaced (where startClosedPoll is called
      on success), confirmExit (the teardown), and the return object to extend.
    - src/composables/useRunTimers.ts (97-04) — the { clock, elapsed, startElapsed, resetElapsed } surface;
      startElapsed is idempotent (first go-live/rehearse).
    - src/utils/runChannel.ts — postState posts { type:'state', index, blackout, seq }; the stale-drop accepts a
      strictly-higher seq (:133), which is why postBlackout must advance seq like resendCurrent.
    - src/views/__tests__/RunControlView.test.ts (:270-301) — the seq/blackout post assertions that must stay
      green: mount posts { index:0, blackout:false }, every nav strictly increases seq. blackout MUST default
      false so these pass.
  </read_first>
  <action>
    In useRunControl.ts:
    - Wire the timers: `const { clock, elapsed, startElapsed, resetElapsed } = useRunTimers()` near the top of the
      composable body.
    - Add `const live = ref(false)` and `const blackout = ref(false)`.
    - Change postIndex to post `blackout: blackout.value` (was false) and resendCurrent likewise. Add
      `function postBlackout(v: boolean) { blackout.value = v; if (index.value == null) return; seq += 1;
      handle?.postState({ index: index.value, blackout: blackout.value, seq }) }` — mirror resendCurrent's
      seq-advance-then-post so the stale-drop accepts it; do NOT hard-code blackout:false anywhere anymore.
    - In handleKeydown, replace the reserved 'B' no-op comment with a case that fires on the b key:
      `case 'b': case 'B': e.preventDefault(); postBlackout(!blackout.value); break` — the existing early returns
      already make it inert while the exit dialog is open and inside text inputs.
    - Set live on go-live SUCCESS: in openPlaced (after outputStatus.value = 'placed', where startClosedPoll() is)
      and in openUnplaced (after outputStatus.value = 'fallback'), add `live.value = true` and `startElapsed()`.
      Do NOT set live in bothOpened's partial/blocked branches.
    - Add `function rehearse() { live.value = true; startElapsed(); if (index.value == null &&
      assembledSlideshow.value.length > 0) postIndex(0) }` — NO openPlaced/openUnplaced/openWindow/getScreenDetails,
      so NO window.open fires; outputStatus stays 'idle'.
    - In confirmExit, add `live.value = false; blackout.value = false; resetElapsed()` alongside the existing
      goLiveRequestId bump / stopRecoveryWatchers / closeOutputs / handle.close / router.push (order relative to
      teardown does not matter for these three refs).
    - EXTEND the return object with: live, blackout, postBlackout, rehearse, clock, elapsed. Keep every existing
      returned binding.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/views/__tests__/RunControlView.test.ts src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "postBlackout" src/composables/useRunControl.ts &amp;&amp; grep -q "function rehearse" src/composables/useRunControl.ts &amp;&amp; grep -q "blackout: blackout.value" src/composables/useRunControl.ts</automated>
  </verify>
  <acceptance_criteria>
    - live is set true only by a successful go-live (openPlaced/openUnplaced) AND rehearse(), reset in confirmExit;
      blackout defaults false, postIndex/resendCurrent post blackout.value, postBlackout advances seq then posts
      the current index with the new blackout, and the 'B' key toggles it; rehearse() sets live + startElapsed
      without any window.open; timers are wired (startElapsed on go-live/rehearse, resetElapsed on exit).
    - RunControlView.test.ts + RunControlView.output.test.ts pass with NO edits; `npx vitest run` shows only the
      baseline; `npm run type-check` clean (no Array.prototype.at); no `--dir src`.
  </acceptance_criteria>
  <done>useRunControl now owns the honest live flag, blackout state + postBlackout + the B key (seq-correct), rehearse-without-screens, and the clock/elapsed timers — with the pre-97 control suites still green untouched.</done>
</task>

<task type="auto">
  <name>Task 2: pre-flight readiness + filmstrip data + header/transport derivations + openManage (extend return)</name>
  <files>src/composables/useRunControl.ts</files>
  <read_first>
    - src/composables/useRunControl.ts — assembledSlideshow, index, currentSlotIndex, outputStatus,
      audienceClosed/confidenceClosed, the return object.
    - src/types/slide.ts renderState (:50-62) — on SlideBase (AssembledSlide.slide.renderState); presence of
      'pending'/'failed' means NOT rendered; undefined means rendered/drawable. allRendered = every slide is
      undefined.
    - src/utils/monitorConfig.ts loadMapping (:124) + the fingerprint shape (:69-72, `label:WxH:left,top:primary`)
      — the pre-live monitor label is the fingerprint's first colon-segment for the role's saved assignment.
    - 97-UI-SPEC.md State A readiness + display cards (:56-60) and State B position/progress (:66-72,:89-91).
  </read_first>
  <action>
    Add these computeds/functions to useRunControl.ts and expose them in the return:
    - `const slideCount = computed(() => assembledSlideshow.value.length)`.
    - `const renderedCount = computed(() => assembledSlideshow.value.filter((s) => s.slide.renderState === undefined
      ).length)`.
    - `const allRendered = computed(() => slideCount.value > 0 && renderedCount.value === slideCount.value)` — an
      honest 'all N slides rendered' check from renderState (NOT CCLI).
    - `function mappingLabel(role: MonitorRole): string` — const saved = loadMapping(); const fp =
      saved?.assignments.find((a) => a.role === role)?.fingerprint; if (!fp) return 'No monitor assigned'; return
      fp.split(':')[0] || 'Assigned display'. `const audienceLabel = computed(() => mappingLabel('audience'))` and
      `const confidenceLabel = computed(() => mappingLabel('confidence'))`.
    - `const audienceOpen = computed(() => (outputStatus.value === 'placed' || outputStatus.value === 'fallback') &&
      !audienceClosed.value)` and the confidence mirror `confidenceOpen`.
    - `const positionLabel = computed(() => index.value == null ? '' : \`${index.value + 1} of ${slideCount.value}\`)`.
    - `const progress = computed(() => slideCount.value === 0 ? 0 : ((index.value ?? 0) + 1) / slideCount.value *
      100)`.
    - FILMSTRIP (R282): build the active item's slides + their global array indices in one pass:
      `const filmstrip = computed(() => { const slides: AssembledSlide[] = []; const indices: number[] = [];
      assembledSlideshow.value.forEach((s, i) => { if (s.slotIndex === currentSlotIndex.value) { slides.push(s);
      indices.push(i) } }); return { slides, indices } })`. Expose `filmstripSlides = computed(() =>
      filmstrip.value.slides)` and `filmstripIndices = computed(() => filmstrip.value.indices)` (or return the
      filmstrip object directly — pick one shape and expose it).
    - `function openManage() { window.open('/monitor-setup', '_blank', 'noopener') }` — a NEW TAB so the running
      control (index/seq/channel + open outputs) survives (mirrors the reassign banner's new-tab rule).
    - EXTEND the return with: slideCount, renderedCount, allRendered, audienceLabel, confidenceLabel, audienceOpen,
      confidenceOpen, positionLabel, progress, filmstripSlides, filmstripIndices (or filmstrip), openManage. Keep
      every prior returned binding. Import MonitorRole/AssembledSlide types if not already imported.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/views/__tests__/RunControlView.test.ts src/views/__tests__/RunControlView.output.test.ts</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "allRendered" src/composables/useRunControl.ts &amp;&amp; grep -q "filmstripIndices\|filmstrip" src/composables/useRunControl.ts &amp;&amp; grep -q "openManage" src/composables/useRunControl.ts &amp;&amp; grep -q "renderState === undefined" src/composables/useRunControl.ts</automated>
  </verify>
  <acceptance_criteria>
    - The composable exposes an honest allRendered/renderedCount/slideCount (from slide.renderState), audience/
      confidence labels (from loadMapping fingerprints) + open flags, positionLabel/progress, the in-item filmstrip
      slides paired with their global array indices, and openManage() (new tab); all added to the return.
    - RunControlView.test.ts + RunControlView.output.test.ts pass with NO edits; `npx vitest run` baseline only;
      type-check clean; no `--dir src`.
  </acceptance_criteria>
  <done>useRunControl exposes the pre-flight readiness, monitor labels, open flags, position/progress, in-item filmstrip (slides + global indices), and openManage — the derived data the redesigned template and children consume, with the pre-97 suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| postBlackout -> runChannel stale-drop | postBlackout advances seq before posting so the output's monotonic stale-drop accepts the blackout state; a post without a seq bump would be silently dropped and the projector would not black out |
| rehearse -> no window.open | rehearse enters live purely by setting state (live + slide 0) and opens NO window; a regression that routed it through openOutputs would open real windows during a rehearsal |
| renderState (localStorage-sourced assembly) -> readiness | allRendered reads slide.renderState (set only by the imported-render reconciler); an honest 'N of M rendered' is shown, never a fake all-clear |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-08-01 | Tampering | blackout dropped by the stale-drop | high | mitigate | postBlackout advances seq then posts the current index with the new blackout (mirrors resendCurrent); 97-10 asserts the injected channel receives { blackout:true } on B / Black. |
| T-97-08-02 | Repudiation | rehearse opening real windows / false live | high | mitigate | rehearse sets live + slide 0 with NO openOutputs/window.open; live is set only on a genuine go-live or rehearse, never partial/blocked; 97-10 asserts no window.open after rehearse. |
| T-97-08-03 | Repudiation | dishonest readiness | medium | mitigate | allRendered = every slide.renderState undefined; renderedCount/slideCount give an honest partial count when not all are ready. |
| T-97-08-SC | Tampering | package installs (npm) | n/a | accept | No installs — an existing composable extension only. |
</threat_model>

<verification>
- `npm run type-check` clean (vue-tsc --build; NODE_OPTIONS=--max-old-space-size=8192 only on OOM; NO
  Array.prototype.at). `npx vitest run` shows only the baseline (src/storage.rules.test.ts); RunControlView.test.ts
  + RunControlView.output.test.ts pass with NO edits (blackout defaults false; new bindings unused by the old
  template). No `--dir src`; no rules suite.
- Manual check: live is set only by go-live success + rehearse (reset on exit); postBlackout is seq-correct; the B
  key toggles blackout; rehearse opens no window; the readiness/filmstrip/label/position derivations are honest.
</verification>

<success_criteria>
- useRunControl owns the live flag (R277), blackout + postBlackout + B key (R280), rehearse-without-screens (R283),
  the clock/elapsed timers (R281), and the pre-flight readiness + in-item filmstrip + header/transport derivations
  (R276/R282) — all exposed for the template, with the pre-97 control suites green untouched.
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-08-SUMMARY.md` when done.
</output>
