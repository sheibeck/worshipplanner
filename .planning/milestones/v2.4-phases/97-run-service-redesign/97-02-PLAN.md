---
phase: 97-run-service-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ServiceCard.vue
  - src/components/__tests__/ServiceCard.test.ts
autonomous: true
requirements: [R284]

must_haves:
  truths:
    - "RUN BUTTON ON LOCKED ROWS (R284): ServiceCard's action footer (the Share+Print row at :42-58, OUTSIDE the card's router-link) gains a Run button FIRST in the row, mirroring ServiceEditorView's gating VERBATIM — `const isLocked = computed(() => props.service.status !== 'draft')` and `const canRun = computed(() => isLocked.value && !!authStore.orgId)` (VIEWER-INCLUSIVE per R275 — gate on canRun, NOT isEditor). The button is ABSENT (v-if=\"canRun\", not disabled) on a draft row and for an org-less user."
    - "NAVIGATE WITH @click.stop (R284): onRun does `router.push('/run/' + encodeURIComponent(props.service.id) + '?org=' + encodeURIComponent(authStore.orgId ?? ''))` and the button carries `@click.stop` so a click Runs the service WITHOUT triggering the card-body router-link navigation to the editor. data-testid=\"run-service-card-btn\", aria-label present, play-triangle SVG + label, indigo primary treatment matching ServiceEditorView's run-service-btn."
    - "NO REGRESSION: ServiceCard's existing rendering (date/slots/status/share/print) is unchanged; the existing ServiceCard.test.ts cases stay green (they mount a DRAFT service, so canRun is false → the Run button is absent → no existing assertion is disturbed). The auth store mock is extended with orgId so the new Run cases can assert presence/navigation."
  artifacts:
    - "src/components/ServiceCard.vue — isLocked + canRun computeds, onRun navigation, and the Run button in the action footer (first, @click.stop, run-service-card-btn)."
    - "src/components/__tests__/ServiceCard.test.ts — new cases: Run present on a locked (planned/exported) row for an org member, absent on a draft, absent when orgId is null, and click navigates to /run/:id?org= without the card-body navigation."
  key_links:
    - "props.service.status !== 'draft' -> isLocked -> canRun (isLocked && !!authStore.orgId) -> v-if renders run-service-card-btn; @click.stop onRun -> router.push('/run/'+id+'?org='+orgId)."
    - "auth store mock gains orgId:'org-1' -> canRun true on a locked fixture -> the Run assertions; a status:'draft' fixture keeps canRun false so the pre-existing cases are undisturbed."
---

<objective>
Add a Run affordance to each LOCKED service row in the service listing (owner fix #3, R284), beside the
existing Share/Print actions in ServiceCard.vue, gated exactly like the ServiceEditorView Run button
(isLocked && orgId, viewer-inclusive) and navigating to /run/:serviceId?org=. Fully self-contained: one
component + its test, disjoint from every other Phase 97 plan.

Purpose: R284 — any authenticated org member can start a live Run directly from the listing without opening
the editor first.

Output: the Run button in ServiceCard.vue's action footer + behavioural coverage in ServiceCard.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-run-service-redesign/97-CONTEXT.md
@.planning/phases/97-run-service-redesign/97-UI-SPEC.md
@.planning/phases/97-run-service-redesign/97-PATTERNS.md
@src/components/ServiceCard.vue
@src/components/__tests__/ServiceCard.test.ts
@src/views/ServiceEditorView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Run button + gating + navigation to ServiceCard.vue</name>
  <files>src/components/ServiceCard.vue</files>
  <read_first>
    - src/components/ServiceCard.vue — the action footer (:42-58): the Share (:43-51) + Print (:52-57) buttons
      live OUTSIDE the card-body router-link (:4-39), each a plain button; note the row is `flex items-center
      justify-end gap-1`. The script already imports useRouter (:64,:77), useAuthStore (:71,:80), and defines
      onPrint's router.push shape (:244-248). authStore currently reads only settings; you will read
      authStore.orgId.
    - src/views/ServiceEditorView.vue — the gating to MIRROR verbatim: `isLocked` (:2113, status !== 'draft'),
      `canRunService = isLocked && !!authStore.orgId` (:2125, viewer-inclusive — NOT isEditor), the run-service-btn
      markup (:101-113, play-triangle SVG + indigo), and onRun (:2132-2141, encodeURIComponent id + ?org=).
    - 97-PATTERNS.md ServiceCard section (:193-208) — location (footer first), lock check, viewer-inclusive gate,
      @click.stop requirement.
  </read_first>
  <action>
    In &lt;script setup&gt;: add `const isLocked = computed(() => props.service.status !== 'draft')` and
    `const canRun = computed(() => isLocked.value && !!authStore.orgId)` (computed + authStore are already
    imported). Add `function onRun() { router.push('/run/' + encodeURIComponent(props.service.id) + '?org=' +
    encodeURIComponent(authStore.orgId ?? '')) }` (router already available). VIEWER-INCLUSIVE: gate on canRun,
    never on an isEditor check — a viewer must be able to Run a locked service (R275).

    In the action footer (:42-58), add the Run button as the FIRST child of the flex row (before Share), so the
    row reads Run · Share · Print:
    - `&lt;button v-if="canRun" type="button" data-testid="run-service-card-btn" aria-label="Run this service
      live" class="inline-flex items-center gap-1 px-2 py-1 rounded text-[11px] font-medium text-white
      bg-indigo-600 hover:bg-indigo-500 transition-colors" @click.stop="onRun"&gt;` with the play-triangle SVG
      (copy the path from ServiceEditorView :109-111, class h-3.5 w-3.5) + the text `Run`.
    - The `@click.stop` is load-bearing: the whole card body is a router-link to the editor, so without .stop a
      Run click would ALSO navigate to /services/:id. Share/Print already use @click.stop on their inner anchors;
      the footer buttons themselves are outside the link, but keep .stop for safety/consistency.

    Change NOTHING else — the date/slot/status/share/print rendering stays exactly as is.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run src/components/__tests__/ServiceCard.test.ts</automated>
    <automated>grep -q "run-service-card-btn" src/components/ServiceCard.vue &amp;&amp; grep -q "canRun" src/components/ServiceCard.vue &amp;&amp; grep -q "@click.stop=\"onRun\"" src/components/ServiceCard.vue</automated>
  </verify>
  <acceptance_criteria>
    - isLocked (status !== 'draft') + canRun (isLocked && !!authStore.orgId, viewer-inclusive) gate a Run button
      that renders FIRST in the action footer, is absent on a draft and for an org-less user, and @click.stop
      onRun navigates to /run/:id?org= without triggering the card-body editor navigation.
    - The existing ServiceCard.test.ts cases (draft fixture) stay green; type-check clean.
  </acceptance_criteria>
  <done>ServiceCard's action footer shows a viewer-inclusive Run button on locked rows that navigates to the Run screen, mirroring ServiceEditorView's gating verbatim, with no change to existing rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the Run button — presence gating + navigation (ServiceCard.test.ts)</name>
  <files>src/components/__tests__/ServiceCard.test.ts</files>
  <read_first>
    - src/components/__tests__/ServiceCard.test.ts — the harness: the vue-router mock exposing a push spy
      (:7-11), the auth store mock (:28-32) currently `{ settings: { bibleVersion: 'ESV' } }` (NO orgId — you
      add it), the router-link passthrough stub (:133-138), and mockService (draft, :36-78). The push spy is
      created inside the vi.mock factory — to ASSERT navigation you need a captured, hoisted push spy.
    - src/components/ServiceCard.vue (Task 1) — run-service-card-btn, canRun gate, onRun target string.
  </read_first>
  <action>
    Upgrade the harness minimally, then add a `describe('ServiceCard — Run affordance (R284)')` block:
    - AUTH MOCK: give the auth store mock an `orgId` field. Use a mutable hoisted holder so a test can null it:
      `const authState = vi.hoisted(() => ({ orgId: 'org-1' as string | null }))`, and the mock returns
      `{ settings: { bibleVersion: 'ESV' }, get orgId() { return authState.orgId } }`. Reset authState.orgId =
      'org-1' in a beforeEach for this block.
    - PUSH SPY: hoist the router push spy so it is assertable: `const pushSpy = vi.hoisted(() => vi.fn(() =>
      Promise.resolve()))` and have the vue-router mock's useRouter return `{ push: pushSpy }`. Clear it per test.
    - LOCKED fixture: clone mockService with `status: 'planned'` (and a second with 'exported') — canRun true.

    Cases:
    1. Run present on a locked (planned) row for an org member: mount with the planned fixture + orgId 'org-1';
       assert `wrapper.find('[data-testid="run-service-card-btn"]').exists()` is true.
    2. Run present on an 'exported' row too (isLocked covers both non-draft statuses).
    3. Run ABSENT on a draft row: mount the original draft mockService; assert the button does not exist.
    4. Run ABSENT when orgId is null: planned fixture but authState.orgId = null; assert absent.
    5. Click navigates + does not open the editor: mount the planned fixture, `await wrapper.find(
       '[data-testid="run-service-card-btn"]').trigger('click')`; assert pushSpy called once with
       `'/run/' + encodeURIComponent(fixtureId) + '?org=org-1'`. (The @click.stop keeps the card-body
       router-link from also firing; asserting the SINGLE /run push is sufficient.)

    Keep every existing case intact.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ServiceCard.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -q "run-service-card-btn" src/components/__tests__/ServiceCard.test.ts &amp;&amp; grep -q "/run/" src/components/__tests__/ServiceCard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New cases prove the Run button appears on planned + exported rows for an org member, is absent on a draft
      and when orgId is null, and a click calls router.push once with '/run/:id?org=org-1'.
    - All pre-existing ServiceCard cases still pass; `npx vitest run` shows only the baseline; type-check clean;
      no `--dir src`.
  </acceptance_criteria>
  <done>ServiceCard.test.ts proves the R284 Run affordance's viewer-inclusive gating and navigation, with the auth/router mocks upgraded and every prior case green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| listing row click -> router.push('/run/:id?org=') | The org travels in ?org= sourced from authStore.orgId (not user input); the /run route re-scopes via Firestore org-membership rules, so a spoofed query cannot cross an org boundary |
| card-body router-link vs footer Run button | @click.stop keeps a Run click from also firing the editor navigation; the test asserts the single /run push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-02-01 | Elevation of Privilege | Run shown to the wrong user/state | medium | mitigate | canRun gates on isLocked && !!authStore.orgId (viewer-inclusive, but org-scoped); absent on a draft and for an org-less user; tests assert all four gate states. |
| T-97-02-02 | Tampering | id with URL-reserved chars corrupting the path | low | mitigate | encodeURIComponent on both id and orgId mirrors ServiceEditorView.onRun; Firestore ids are URL-safe today, this is defence against a future id shape. |
| T-97-02-SC | Tampering | package installs (npm) | n/a | accept | No installs — an existing component + test only. |
</threat_model>

<verification>
- `npm run type-check` clean; `npx vitest run` shows only the baseline (src/storage.rules.test.ts);
  `npx vitest run src/components/__tests__/ServiceCard.test.ts` green. No `--dir src`; no rules suite.
- Manual check: the Run button is viewer-inclusive (gates on canRun, not isEditor), absent on drafts/org-less,
  @click.stop, and navigates to /run/:id?org= — mirroring ServiceEditorView's run-service-btn.
</verification>

<success_criteria>
- A Run affordance appears on each locked service row in the listing for any authenticated member and navigates
  to the Run screen; drafts and org-less users do not see it; existing ServiceCard behaviour is unchanged (R284).
</success_criteria>

<output>
Create `.planning/phases/97-run-service-redesign/97-02-SUMMARY.md` when done.
</output>
