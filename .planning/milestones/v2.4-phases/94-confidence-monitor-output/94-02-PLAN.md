---
phase: 94-confidence-monitor-output
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - src/views/ConfidenceOutputView.vue
  - src/router/index.ts
autonomous: true
requirements: [R272]

must_haves:
  truths:
    - "ConfidenceOutputView.vue renders BOTH the current slide (dominant TOP region, flex 7 1 0%, ~70% height) and the next assembled slide (subordinate BOTTOM region, flex 3 1 0%, ~30% height), full-width vertical split, each via <SlideCanvas :suppressBackground=\"true\" :interactive=\"false\" />, consuming the shared useOutputWindow composable from 94-01 for all lifecycle (R272)."
    - "BOTH panes pass suppressBackground=true, so the actual background image is NEVER shown on either pane — SlideCanvas forces currentBackgroundUrl to null (SlideCanvas.vue:359-363, checked FIRST) and therefore emits no presentation-background / presentation-background-scrim element, for background-carrying AND video slides (R272 black-suppression invariant, the prop's first real consumer)."
    - "nextSlide = index == null ? null : (assembledSlideshow[index + 1] ?? null); on the last slide nextSlide is null, the next region renders pure black with the \"Next\" tag hidden (v-if=nextSlide), the 70/30 layout regions stay FIXED (the current pane does NOT reflow/grow), and an out-of-range index never throws or wraps to slide 0 (R272)."
    - "The CURRENT pane drives media exactly as Phase 93 (a view-local currentCanvasRef + watch(index) pause->nextTick->play + a watch(fontReady) deferred first-play), while the NEXT pane is a STATIC preview with no ref that is NEVER driven by play() — the band never hears the upcoming slide's audio nor sees its video motion (R272)."
    - "A single quiet \"Next\" tag (absolute top-2 left-3, text-sm font-medium uppercase tracking-wide text-gray-500, copy Next) labels the next pane ONLY; the current pane carries NO label; a border-t border-white/10 hairline seam separates the regions; and ZERO operator chrome (no arrows, slide counts, org labels, progress, exit) renders — the only interactive element is the inherited windowed-only Re-enter fullscreen affordance (R272)."
    - "The route /present/confidence/:serviceId (name confidence-output, lazy component ConfidenceOutputView.vue, meta requiresAuth ONLY, org via ?org= query) is registered among the static authed routes BEFORE the trailing public dynamic slug routes, mirroring the /present/audience entry; router.beforeEach is left untouched (R272/R275)."
  artifacts:
    - src/views/ConfidenceOutputView.vue
    - "Route /present/confidence/:serviceId registered in src/router/index.ts (name confidence-output, meta requiresAuth only)"
  key_links:
    - "useOutputWindow({ channelFactory: props.channelFactory }) -> { assembledSlideshow, index, fontReady, rootRef, rootStyle, isFullscreen, handleReenterFullscreen }; the confidence view forwards its channelFactory prop in (same test seam as audience)."
    - "currentSlide + nextSlide computed locally from index + assembledSlideshow; currentCanvasRef ref lives ONLY on the current pane; watch(index) + watch(fontReady) drive the current pane only; the next pane has no ref and no play() call."
    - "The route entry lazy-imports ../views/ConfidenceOutputView.vue and is placed adjacent to the /present/audience entry at src/router/index.ts:87-99, before the /owner-console + public dynamic slug routes."
---

<objective>
Build ConfidenceOutputView.vue — the worship band's glanceable stage-reference output window — and
register its route. It is the SIBLING of Phase 93's AudienceOutputView: it inherits the entire
lifecycle + chrome contract via the useOutputWindow composable (94-01) and diverges ONLY in the render
body — a current + next two-pane vertical split with BOTH backgrounds suppressed to plain black.

Purpose: Deliver R272 — the band sees the current slide (dominant) AND the next upcoming slide (clearly
subordinate, labeled), fullscreen, with the actual background image NEVER shown on either pane and zero
operator chrome. This is Phase 90 suppressBackground's first real consumer. The window self-bootstraps
(same as audience) and is directly loadable; Phase 95 will later open and place it on the assigned
monitor (out of scope here).

Output: `src/views/ConfidenceOutputView.vue` and a `/present/confidence/:serviceId` route (org via
`?org=`, requiresAuth only). Behavioral tests + a direct useOutputWindow unit test are authored in
94-03 (wave 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-confidence-monitor-output/94-CONTEXT.md
@.planning/phases/94-confidence-monitor-output/94-UI-SPEC.md
@.planning/phases/94-confidence-monitor-output/94-PATTERNS.md
@.planning/research/PITFALLS.md
@src/views/AudienceOutputView.vue
@src/composables/useOutputWindow.ts
@src/components/slides/SlideCanvas.vue
@src/router/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the /present/confidence/:serviceId route (requiresAuth only)</name>
  <files>src/router/index.ts</files>
  <read_first>
    - src/router/index.ts (the /present/audience/:serviceId entry lines 87-99 — the EXACT precedent, including its R270/R271/R275 comment and its placement among the static authed routes; the trailing /owner-console 100-107 and public dynamic slug routes 117-145; the beforeEach org-selection gate 149+ which needs NO change).
    - 94-PATTERNS.md router section (the new-entry shape + placement rule).
    - 94-CONTEXT.md routing/access decision (requiresAuth only, no new RBAC tier per R275).
  </read_first>
  <action>
    Add one route object to the routes array in src/router/index.ts, immediately adjacent to the
    /present/audience/:serviceId entry, with path '/present/confidence/:serviceId', name
    'confidence-output', a lazy component import of '../views/ConfidenceOutputView.vue', and meta of
    requiresAuth true ONLY. Do NOT add requiresEditor — mirror the audience entry (and /monitor-setup,
    /services), NOT /settings; per R272/R275 any authenticated org member may open this presentation-only
    window. Place it among the static authed routes, before the /owner-console entry and the trailing
    public dynamic slug routes, so Vue Router never shadows it. Add a short code comment citing
    R272/R275, noting the org travels in the ?org= query, that the window self-bootstraps and is opened
    programmatically by Phase 95, and that both output panes suppress their backgrounds to black — mirror
    the audience entry's comment style.

    Leave router.beforeEach untouched — requiresAuth already triggers the inherited auth + org-selection
    gate. Do NOT teach the org-selection gate about ?org= (out of scope; interacts with Phase 95's
    window.open flow — same boundary the audience route observed).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>grep -q "path: '/present/confidence/:serviceId'" src/router/index.ts && grep -q "ConfidenceOutputView.vue" src/router/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - The /present/confidence/:serviceId route exists with name 'confidence-output', meta requiresAuth:
      true, and NO requiresEditor.
    - It lazily imports ../views/ConfidenceOutputView.vue and sits among the static authed routes (before
      /owner-console and the public dynamic slug routes) with a comment citing R272/R275 + the ?org=
      convention.
    - router.beforeEach is unchanged.
    - `npm run type-check` passes once Task 2's component exists (run type-check after both tasks; the
      lazy import resolves then).
  </acceptance_criteria>
  <done>The confidence-output route is registered as any-member (requiresAuth only), adjacent to the audience route, with the guard untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build ConfidenceOutputView.vue — current+next two-pane, both backgrounds suppressed</name>
  <files>src/views/ConfidenceOutputView.vue</files>
  <read_first>
    - src/views/AudienceOutputView.vue (the refactored sibling from 94-01 — COPY its script shape: the channelFactory prop, the useOutputWindow destructure, the view-local currentSlide computed + slideCanvasRef + watch(index) media watcher + watch(fontReady) deferred first-play + onBeforeUnmount pause. COPY its re-enter affordance markup verbatim, changing only the data-testids. You diverge ONLY in the two-pane body.)
    - src/composables/useOutputWindow.ts (the returned surface you destructure: assembledSlideshow, index, fontReady, rootRef, rootStyle, isFullscreen, handleReenterFullscreen).
    - src/components/slides/SlideCanvas.vue (props { slide, suppressBackground?, interactive? }; suppressBackground forces currentBackgroundUrl null FIRST at :359-363, so the presentation-background/scrim elements at :14-25 do not render; defineExpose({ play, pause }) at :485-488; autoplay affordances additionally gated on interactive at :241/:251/:261).
    - 94-UI-SPEC.md "Render Body" (the 70/30 vertical split, full-width panes, the seam, the "Next" tag treatment, the last-slide no-reflow rule, the media invariant) and "Hard Invariant — backgrounds suppressed to black on BOTH panes".
    - 94-PATTERNS.md ConfidenceOutputView section (the render-body divergence + the nextSlide computed + the media-invariant CURRENT-only rule).
  </read_first>
  <action>
    Create src/views/ConfidenceOutputView.vue as a standalone route view (script setup + Vue 3 SFC +
    Tailwind). Its &lt;script setup&gt; is the audience view's shape; its &lt;template&gt; is the ONE divergence.

    SCRIPT: const props = defineProps&lt;{ channelFactory?: BroadcastChannelFactory }&gt;(). Destructure
    const { assembledSlideshow, index, fontReady, rootRef, rootStyle, isFullscreen,
    handleReenterFullscreen } = useOutputWindow({ channelFactory: props.channelFactory }). Build BOTH
    slides locally:
    - currentSlide = computed&lt;AssembledSlide | null&gt;(() =&gt; index.value == null ? null :
      (assembledSlideshow.value[index.value] ?? null)).
    - nextSlide = computed&lt;AssembledSlide | null&gt;(() =&gt; index.value == null ? null :
      (assembledSlideshow.value[index.value + 1] ?? null)) — note index+1 by direct index access, NOT
      Array.prototype.at (not in this project's TS lib target); an out-of-range index resolves to
      undefined then ?? null, so the last slide yields null (pure black), never a crash or wrap to 0.
    MEDIA (CURRENT pane only): currentCanvasRef = ref&lt;InstanceType&lt;typeof SlideCanvas&gt; | null&gt;(null)
    bound ONLY to the current pane. watch(index, async () =&gt; { currentCanvasRef.value?.pause(); await
    nextTick(); currentCanvasRef.value?.play() }) and watch(fontReady, (ready) =&gt; { if (!ready) return;
    void nextTick().then(() =&gt; currentCanvasRef.value?.play()) }) and onBeforeUnmount(() =&gt;
    currentCanvasRef.value?.pause()). Do NOT create a ref on the next pane and NEVER call play() on it —
    it is a static preview.

    TEMPLATE root: a full-bleed element ref="rootRef" data-testid="confidence-output"
    class="fixed inset-0 bg-black flex flex-col" :style="rootStyle" (rootStyle carries the shared CSS-var
    font wrapper + cursor:none-while-fullscreen). Inside, two full-width regions stacked vertically:
    - CURRENT region (dominant, top): a div class="relative flex-[7_1_0%] flex items-center
      justify-center overflow-hidden" containing
      &lt;SlideCanvas v-if="currentSlide &amp;&amp; fontReady" ref="currentCanvasRef" :slide="currentSlide"
      :suppressBackground="true" :interactive="false" /&gt;. No label on this pane.
    - NEXT region (subordinate, bottom): a div class="relative flex-[3_1_0%] flex items-center
      justify-center overflow-hidden border-t border-white/10" containing
      &lt;SlideCanvas v-if="nextSlide &amp;&amp; fontReady" :slide="nextSlide" :suppressBackground="true"
      :interactive="false" /&gt; and a &lt;span v-if="nextSlide" data-testid="confidence-next-label"
      class="absolute top-2 left-3 text-sm font-medium uppercase tracking-wide text-gray-500"&gt;Next&lt;/span&gt;.
      When nextSlide is null the region stays present (flex-[3_1_0%] fixed) but renders pure black with
      the tag hidden — DO NOT collapse it (collapsing would jump-resize the current pane on the final
      advance in front of the band).
    - RE-ENTER AFFORDANCE: copy AudienceOutputView's block verbatim (the v-if="!isFullscreen" wrapper +
      the button with the arrows-pointing-out SVG + aria-label "Re-enter fullscreen" + @click=
      "handleReenterFullscreen"), changing ONLY data-testid to "confidence-reenter-fullscreen". It is
      absolutely positioned (absolute inset-0 flex items-center justify-center) so it overlays both live
      panes; the fixed-positioned root is its containing block.

    CHROME: render NO exit button, nav chevrons, progress pill, slide count, or organizational label. The
    "Next" tag and the windowed-only re-enter affordance are the ONLY non-slide elements. No spinner / no
    "Loading" copy — loading and empty states are pure black (the composable gates fontReady; both panes'
    v-if hold until a slide + fontReady exist). Import ref, computed, watch, nextTick, onBeforeUnmount
    from vue; useOutputWindow from @/composables/useOutputWindow; SlideCanvas; the AssembledSlide type;
    and BroadcastChannelFactory from @/utils/runChannel. No Firestore/db import.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run</automated>
    <automated>grep -c 'suppressBackground="true"' src/views/ConfidenceOutputView.vue</automated>
  </verify>
  <acceptance_criteria>
    - ConfidenceOutputView renders a vertical 70/30 split: a dominant current pane (flex-[7_1_0%]) over a
      subordinate next pane (flex-[3_1_0%]) with a border-t border-white/10 seam, both full width.
    - BOTH SlideCanvas panes pass :suppressBackground="true" and :interactive="false" (the grep count is
      2); the current pane carries ref="currentCanvasRef", the next pane carries no ref.
    - nextSlide uses assembledSlideshow[index + 1] ?? null (direct index access, no .at()); the last slide
      renders the next region black with the "Next" tag hidden and the current pane NOT reflowing.
    - The current pane drives media via watch(index) + watch(fontReady) on currentCanvasRef only; the next
      pane is never driven by play().
    - No operator chrome renders; the confidence-reenter-fullscreen affordance is the only interactive
      element and appears only when not fullscreen; rootStyle applies cursor:none while fullscreen.
    - No Firestore/db import; `npm run type-check` passes and `npx vitest run` shows only the baseline
      (src/storage.rules.test.ts) — no `--dir src`.
  </acceptance_criteria>
  <done>ConfidenceOutputView shows a dominant current pane over a subordinate labeled next pane with both backgrounds suppressed to black, the next pane static and last-slide-safe, zero operator chrome, and the shared lifecycle inherited from useOutputWindow — type-check clean, no baseline regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BroadcastChannel -> confidence view (via composable) | Incoming `state` messages are untrusted; a stale/out-of-range/malformed index must never crash the band monitor or wrap the next pane to slide 0 |
| slide content -> band-facing render surface | Whatever renders is on the stage confidence monitor; the actual background image must NEVER appear on either pane (R272), and no operator chrome may leak |
| ?org= query + :serviceId param -> Firestore subscription | Org/service are URL-influenceable; reads gated by existing Firestore org-membership rules (unchanged, inherited) |
| router/nav -> view access | Any authenticated org member may open /present/confidence (intended per R275); presentation-only, no editor tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-04 | Information Disclosure | Background layer leaking to the band monitor | high | mitigate | The single most important R272 assertion: BOTH SlideCanvas panes pass suppressBackground=true, which forces currentBackgroundUrl null FIRST (SlideCanvas.vue:359-363) so no presentation-background/scrim element renders on EITHER pane, for background-carrying AND video slides. The two-panes-both-suppressed wiring is unit-asserted in 94-03 (view passes the prop on both panes; the real SlideCanvas emits no background element under it). |
| T-94-05 | Information Disclosure | Operator chrome on the band-facing surface | low | mitigate | Chromeless by construction — no exit/nav/progress/slide-count/org-label element; the only non-slide elements are the quiet "Next" tag and the windowed-only re-enter affordance; loading/empty is pure black; channel/wake-lock/fullscreen failures are silent (inherited from useOutputWindow). |
| T-94-06 | Tampering | Out-of-range / last-slide next index | low | mitigate | nextSlide = assembledSlideshow[index + 1] ?? null renders black on the last slide with no wrap-around and no throw; the 70/30 layout stays fixed so a malformed advance cannot jump-resize the current pane. |
| T-94-07 | Denial of Service | Next-pane media autoplay | low | mitigate | The next pane has no canvas ref and is never driven by play(); :interactive="false" suppresses its autoplay-blocked affordances (SlideCanvas :241/:251/:261) — the band never hears/sees upcoming media. |
| T-94-SC | Tampering | package installs (npm) | n/a | accept | No package-manager installs in this phase — reuse of in-repo components + composable only; supply-chain checkpoint not applicable. |
</threat_model>

<verification>
- `npm run type-check` (vue-tsc --build per CLAUDE.md — use `NODE_OPTIONS=--max-old-space-size=8192`
  only if it OOM-crashes; remember `Array.prototype.at` is NOT in this project's TS lib target — use
  index access) passes clean.
- `npx vitest run` shows the known baseline only (src/storage.rules.test.ts); no `--dir src`.
- Manual code check: both panes carry :suppressBackground="true" (grep count 2), only the current pane
  has a canvas ref, no operator chrome, no Firestore/db import, the route is any-member and the guard is
  untouched.
- Behavioral coverage (two-pane render, black-suppression on both panes, last-slide no-reflow, next-pane
  never autoplays, plus the reused Phase 93 assertions) is authored in plan 94-03 (wave 3).
- HUMAN-UAT (deferred, mark human_needed): a real confidence monitor on a second physical screen —
  true black-background suppression as seen by the band, and glanceable current/next legibility with the
  next pane at ~30% height — cannot be proven by jsdom; the autonomous run defers these to milestone-end
  UAT (94-CONTEXT `<specifics>`; the 30%-height legibility FLAG in 94-UI-SPEC "Risks to scrutinize").
</verification>

<success_criteria>
- Opening /present/confidence/:serviceId?org=... shows the current slide (dominant) AND the next
  upcoming slide (subordinate, labeled "Next"), clearly distinguished, both against plain black with the
  actual background image never shown, and zero operator chrome (R272).
- The last slide renders the next pane black with the tag hidden and no reflow of the current pane; the
  next pane never autoplays media.
- The window self-bootstraps its own service load + assembly + channel via the shared useOutputWindow,
  and the route is registered as any-member with the guard untouched.
</success_criteria>

<output>
Create `.planning/phases/94-confidence-monitor-output/94-02-SUMMARY.md` when done.
</output>
