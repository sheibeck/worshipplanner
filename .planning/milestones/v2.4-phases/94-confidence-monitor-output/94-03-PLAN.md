---
phase: 94-confidence-monitor-output
plan: 03
type: execute
wave: 3
depends_on: ["94-01", "94-02"]
files_modified:
  - src/views/__tests__/ConfidenceOutputView.test.ts
  - src/composables/__tests__/useOutputWindow.test.ts
autonomous: true
requirements: [R272]

must_haves:
  truths:
    - "An automated jsdom test proves ConfidenceOutputView renders BOTH a current pane and a next pane for a mid-deck index and that the two are distinguishable (different slide ids + the \"Next\" tag on the next pane only), and that a last-slide index renders the next pane empty with the \"Next\" tag HIDDEN, no crash, no wrap-around to slide 0, and both 70/30 regions still present (no reflow) (R272)."
    - "A test proves BOTH panes receive suppressBackground=true and interactive=false (the extended SlideCanvas stub records its props), AND a real-SlideCanvas integration assertion (vi.importActual) proves that under suppressBackground=true NEITHER presentation-background NOR presentation-background-scrim renders for a background-carrying slide AND for a video slide — closing the R272 black-suppression chain end to end (R272)."
    - "A test proves the NEXT pane never autoplays: on a slide change the current pane's canvas instance receives pause->play while the next pane's canvas instance play spy is never called (per-instance tagged spies keyed by slide id) (R272)."
    - "The confidence suite reuses Phase 93's proven assertions: channel-driven index + higher-seq advance + stale-seq drop, postHello on mount / NEVER postState / close on unmount, chrome absence + cursor:none while fullscreen, wake-lock present/re-acquire/absent, fullscreen-loss -> Re-enter affordance WITHOUT teardown, and the WR-02 org-scoped subscribe gate (fresh / different-org / same-org)."
    - "A direct unit test of useOutputWindow drives it with an injected fake channelFactory (via a trivial host component that calls it in setup()) and asserts: index set from onState, postHello on mount + never postState, the WR-02 subscribe gate, wake-lock acquire/re-acquire/release, isFullscreen toggling on a dispatched fullscreenchange, unsubscribeAll + channel close on unmount, and fontReady resolving true."
  artifacts:
    - src/views/__tests__/ConfidenceOutputView.test.ts
    - src/composables/__tests__/useOutputWindow.test.ts
  key_links:
    - "An in-memory BroadcastChannelFactory passed via the channelFactory prop / composable argument drives onState and records posted messages (postHello present, postState absent) — mirroring AudienceOutputView.test.ts createFakeChannel."
    - "The extended SlideCanvas stub records suppressBackground + interactive props and exposes PER-INSTANCE play/pause vi.fns tagged by slide id into a module-scope registry, so the test can assert both panes suppressed and the next pane never played."
    - "vi.importActual('@/components/slides/SlideCanvas.vue') mounts the REAL SlideCanvas for the presentation-background-absence assertion (extends SlideCanvas.test.ts:256-278 to the video kind)."
---

<objective>
Author the behavioral coverage for the confidence output window built in 94-02, plus a direct unit
test of the shared useOutputWindow composable extracted in 94-01. Prove the R272-critical properties —
two-pane current+next render, both backgrounds suppressed to black (view wiring AND the real
SlideCanvas emitting no background element), last-slide safety with no reflow, and the next pane never
autoplaying — and re-prove the inherited Phase 93 lifecycle (channel, handshake, chrome absence,
cursor, wake lock, fullscreen-loss recovery, WR-02) now that it lives in a shared composable.

Purpose: Lock R272 with automated jsdom tests so the autonomous run has proof for the mocked paths.
Real black-background suppression as seen by the band and glanceable two-pane legibility on real
second-monitor hardware remain HUMAN-UAT items (deferred — see the verification note). The direct
useOutputWindow unit test lets the shared lifecycle be asserted once, centrally, alongside the two view
suites that exercise it through their own render bodies.

Output: `src/views/__tests__/ConfidenceOutputView.test.ts` and
`src/composables/__tests__/useOutputWindow.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-confidence-monitor-output/94-CONTEXT.md
@.planning/phases/94-confidence-monitor-output/94-UI-SPEC.md
@.planning/phases/94-confidence-monitor-output/94-PATTERNS.md
@src/views/ConfidenceOutputView.vue
@src/composables/useOutputWindow.ts
@src/views/AudienceOutputView.vue
@src/views/__tests__/AudienceOutputView.test.ts
@src/components/slides/SlideCanvas.vue
@src/components/slides/__tests__/SlideCanvas.test.ts
@src/utils/runChannel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ConfidenceOutputView.test.ts (two-pane, black-suppression, last-slide, next-pane-static, inherited lifecycle)</name>
  <files>src/views/__tests__/ConfidenceOutputView.test.ts</files>
  <read_first>
    - src/views/__tests__/AudienceOutputView.test.ts END TO END (the harness to MIRROR): the reactive mockRoute + vue-router mock (40-44), vi.hoisted serviceStoreMock + fakeSlides + slideCanvasSpies (49-79), @/firebase inert (81), @/stores/auth (83-90), @/stores/services (92-94), useSlideshowAssembly mock (98-103), the SlideCanvas stub (108-130), createFakeChannel (138-160), setFullscreenElement (162-168), mountView (170-172), beforeEach/afterEach (176-209), and all 18 tests (213-509) — reuse every applicable one against the confidence view's data-testids.
    - src/views/ConfidenceOutputView.vue (the REAL view under test — its data-testid=confidence-output root, data-testid=confidence-reenter-fullscreen affordance, data-testid=confidence-next-label tag, the two SlideCanvas panes with the current pane's ref="currentCanvasRef", and that both panes pass :suppressBackground="true"). Author assertions against what it REALLY renders.
    - src/components/slides/__tests__/SlideCanvas.test.ts:256-278 (the existing suppressBackground component-level coverage — false/absent renders presentation-background+scrim, true renders NEITHER). EXTEND, don't duplicate: add the video-slide-under-suppressBackground case in the confidence integration assertion.
    - src/components/slides/SlideCanvas.vue (props order + the video slide shape contentKind:'video' + videoSrc, so the integration test can build a background-carrying slide AND a video slide).
    - src/utils/runChannel.ts (BroadcastChannelLike / BroadcastChannelFactory / RunState message shapes for the in-memory fake).
  </read_first>
  <action>
    Create src/views/__tests__/ConfidenceOutputView.test.ts by copying the AudienceOutputView.test.ts
    harness and retargeting it at ConfidenceOutputView + its data-testids. Keep the same mocks
    (vue-router reactive mockRoute seeded params.serviceId + query.org, @/firebase inert, @/stores/auth
    with orgId + settings.slideTypography, @/stores/services serviceStoreMock, useSlideshowAssembly
    returning a fixed fakeSlides ref, enableAutoUnmount(afterEach), the Fullscreen + document.fonts +
    visibilityState stubs).

    EXTEND THE SlideCanvas STUB so it records the R272 wiring and the media invariant:
    - Declare suppressBackground: { type: Boolean, default: false } (and keep interactive) on the stub's
      props so both are inspectable.
    - In setup(), create per-instance play/pause vi.fns, expose({ play, pause }), and push { slideId:
      props.slide?.slide?.id, suppressBackground: props.suppressBackground, interactive:
      props.interactive, play, pause } into a module-scope registry array (cleared in beforeEach). Render
      the slide id as text (as the audience stub does) so panes are identifiable.

    WRITE THE CONFIDENCE-SPECIFIC TESTS:
    - Two-pane mid-deck: emitState(1, seq); assert TWO slide-canvas stub instances render — a current
      showing 'b' and a next showing 'c' — and that the confidence-next-label ("Next") is present on the
      next pane only.
    - Both panes suppressed: from the registry after the mid-deck render, assert the current pane's
      instance and the next pane's instance BOTH have suppressBackground === true and interactive ===
      false. (This is the view's R272 wiring — the primary regression guard.)
    - Real-SlideCanvas black-suppression (integration, closes the R272 DOM chain): in a dedicated test,
      const Real = (await vi.importActual('@/components/slides/SlideCanvas.vue')).default; mount the REAL
      SlideCanvas with props { slide: aBackgroundCarryingSlide, suppressBackground: true } and assert
      neither [data-testid="presentation-background"] nor [data-testid="presentation-background-scrim"]
      exists; repeat with a video slide (contentKind 'video' + videoSrc) and assert the same absence. As
      the non-vacuous control, mount the same background-carrying slide with suppressBackground:false and
      assert presentation-background DOES render (so the suppressed assertion cannot pass trivially).
    - Last-slide (no next, no reflow): emitState(2, seq) (current = last 'c'); assert the current pane
      renders 'c', NO next-pane slide-canvas renders, the confidence-next-label is HIDDEN, and there is
      no wrap to 'a' and no thrown error. Assert BOTH region containers still exist (query the current +
      next region wrappers by a stable selector) so the 70/30 layout did not collapse.
    - Next pane never autoplays: emitState(0, s) then emitState(1, s+1); from the registry, assert the
      NEXT pane instance's play spy was NEVER called, while a current-pane instance's play spy WAS called
      and its pause fired before its play (mirror the audience T-23-08 ordering check, scoped to the
      current pane).

    REUSE THE PHASE 93 ASSERTIONS against the confidence view (retargeting testids): channel-driven index
    + higher-seq advance + stale-seq drop; postHello on mount / NEVER postState / close on unmount; chrome
    absence (no presentation-progress/next/prev/exit/chrome) + cursor:none while fullscreen; pure-black
    gate (index null and out-of-range render no slide-canvas and no copy); wake-lock present (request on
    mount + re-request on visibilitychange->visible) / released on unmount / absent (no throw);
    fullscreen-loss -> confidence-reenter-fullscreen affordance renders WITHOUT close()/unmount and
    restores cursor:auto, and clicking it calls requestFullscreen; WR-02 subscribe gate (fresh subscribes
    to ?org=, different-org re-subscribes, same-org does not).

    Prefer querying by data-testid / role / accessible label over brittle class selectors.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ConfidenceOutputView.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - Two-pane mid-deck renders a current ('b') and a next ('c') pane with the "Next" tag on the next pane
      only; both panes' stub instances record suppressBackground === true and interactive === false.
    - The real-SlideCanvas integration test proves no presentation-background/scrim under
      suppressBackground=true for a background-carrying AND a video slide, with a non-vacuous
      false-control that DOES render the background.
    - Last-slide index renders the next pane empty with the "Next" tag hidden, no wrap-around, no throw,
      and both 70/30 regions still present (no reflow).
    - The next pane's canvas instance play spy is never called; the current pane's pause fires before its
      play on a slide change.
    - The reused Phase 93 assertions (channel/handshake/chrome/cursor/wake-lock/fullscreen-loss/WR-02) all
      pass against the confidence view.
    - `npx vitest run src/views/__tests__/ConfidenceOutputView.test.ts` passes and `npm run type-check`
      is clean.
  </acceptance_criteria>
  <done>The confidence view's two-pane render, both-panes black-suppression (view wiring + real SlideCanvas DOM), last-slide no-reflow safety, next-pane-never-autoplays invariant, and the inherited channel/chrome/cursor/wake-lock/fullscreen/WR-02 behavior are all proven by passing jsdom tests.</done>
</task>

<task type="auto">
  <name>Task 2: Author useOutputWindow.test.ts (direct unit test of the shared lifecycle-core)</name>
  <files>src/composables/__tests__/useOutputWindow.test.ts</files>
  <read_first>
    - src/composables/useOutputWindow.ts (the composable under test — its argument { channelFactory }, its returned surface { assembledSlideshow, index, fontReady, rootRef, rootStyle, isFullscreen, handleReenterFullscreen }, and the onMounted/onUnmounted behavior to assert).
    - src/views/__tests__/AudienceOutputView.test.ts (reuse its mock shapes verbatim: vue-router reactive route, @/firebase inert, @/stores/auth, @/stores/services serviceStoreMock, useSlideshowAssembly mock, createFakeChannel, the wake-lock install/delete idiom, the Fullscreen + document.fonts + visibilityState stubs, enableAutoUnmount(afterEach)). The composable needs the SAME environment as the view since it reads the same route/stores.
    - src/utils/runChannel.ts (message shapes for the fake channel).
  </read_first>
  <action>
    Create src/composables/__tests__/useOutputWindow.test.ts. Because a composable must run inside a
    component setup() (it registers onMounted/onUnmounted), define a trivial host component with
    defineComponent({ setup() { const api = useOutputWindow({ channelFactory }); return () => h('div');
    }, }) — or expose api on the instance — and mount it with @vue/test-utils so the lifecycle hooks
    fire. Pass the injected createFakeChannel().factory in as channelFactory (via a prop the host
    forwards, or a module-scope closure the host reads). Reuse the AudienceOutputView.test.ts mocks
    (route, stores, firebase, useSlideshowAssembly, fullscreen/fonts/visibility stubs, wake-lock
    install/delete). Do NOT stub SlideCanvas here — the composable never imports it.

    WRITE THESE TESTS (assert against the composable's returns + the fake channel + the mocked store):
    - index from onState: after mount, emitState(2, 1) and assert the returned index ref becomes 2; a
      higher-seq state advances it, a stale/lower-seq state is dropped (runChannel's real guard).
    - handshake: assert the fake channel recorded a postMessage type 'hello' on mount and recorded NO
      'state' message across mount + several emits + unmount.
    - WR-02 subscribe gate: with serviceStoreMock.orgId null and query.org 'org-1', assert subscribe was
      called with 'org-1'; with orgId 'org-other' assert it re-subscribes to 'org-1'; with orgId 'org-1'
      assert subscribe was NOT called.
    - wake lock: with navigator.wakeLock present, assert request('screen') on mount and again on a
      dispatched visibilitychange->visible, and that the sentinel's release() is called on unmount; with
      navigator.wakeLock absent, assert mount does not throw.
    - fullscreen toggle: assert isFullscreen tracks document.fullscreenElement across a dispatched
      fullscreenchange (null -> falsy, an element -> truthy) and that dispatching it NEVER calls the
      channel's close() (no teardown, Pitfall 6).
    - fontReady: after flushPromises the returned fontReady ref is true (the bounded gate always resolves).
    - unmount cleanup: assert handle close() and serviceStore.unsubscribeAll() both fire on unmount.
    Prefer asserting on the returned refs and the fake channel's recorded calls over reaching into
    component internals.
  </action>
  <verify>
    <automated>npx vitest run src/composables/__tests__/useOutputWindow.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - index-from-onState (+ higher-seq advance + stale-seq drop), postHello-on-mount + never-postState,
      and the WR-02 subscribe gate (fresh / different-org / same-org) all pass driving the composable
      directly.
    - Wake-lock acquire-on-mount + re-acquire-on-visibilitychange + release-on-unmount, and the absent-API
      no-throw case, pass.
    - isFullscreen toggles on a dispatched fullscreenchange without ever closing the channel; fontReady
      resolves true; handle.close() + serviceStore.unsubscribeAll() fire on unmount.
    - `npx vitest run src/composables/__tests__/useOutputWindow.test.ts` passes and `npm run type-check`
      is clean.
  </acceptance_criteria>
  <done>useOutputWindow's channel handshake, receive-only index, WR-02 gate, wake-lock acquire/re-acquire/release, non-teardown fullscreen toggle, font gate, and unmount cleanup are all proven by a direct passing unit test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production behavior | The tests assert the R272 invariants (both panes suppressed, next-pane static, last-slide safe) and the inherited lifecycle; a vacuous assertion (e.g. a stub that trivially cannot render a background) would give false confidence — the real-SlideCanvas integration + false-control guards against it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-08 | Repudiation | Vacuous black-suppression assertion | low | mitigate | The black-suppression proof does not rely on the SlideCanvas stub (which cannot render a background) alone: a real-SlideCanvas integration test asserts no presentation-background/scrim under suppressBackground=true for a background-carrying AND a video slide, plus a false-control mount that DOES render the background — so the assertion cannot pass trivially. |
| T-94-SC | Tampering | package installs (npm) | n/a | accept | No package-manager installs in this phase — test authoring against in-repo modules only; supply-chain checkpoint not applicable. |
</threat_model>

<verification>
- `npx vitest run` shows the same baseline as before (src/storage.rules.test.ts) plus the two new
  passing test files; no `--dir src`.
- `npm run type-check` (vue-tsc --build per CLAUDE.md — use `NODE_OPTIONS=--max-old-space-size=8192`
  under memory pressure) passes clean, including both new test files.
- HUMAN-UAT (deferred, mark human_needed): a real confidence monitor on a second physical screen —
  true black-background suppression as seen by the band and glanceable current/next legibility with the
  next pane at ~30% height — is not provable by jsdom; the verifier flags these and the autonomous run
  defers them to milestone-end UAT (94-CONTEXT `<specifics>`; 94-UI-SPEC "Risks to scrutinize" 30%-height
  legibility FLAG).
</verification>

<success_criteria>
- Two-pane render, both-panes black-suppression (view wiring + real SlideCanvas DOM, with a false
  control), last-slide no-reflow safety, and the next-pane-never-autoplays invariant are covered by
  passing automated tests (R272).
- The inherited channel/handshake/chrome/cursor/wake-lock/fullscreen-loss/WR-02 behavior is re-proven
  against the confidence view AND directly against the shared useOutputWindow composable.
- No baseline regression in `npx vitest run`; type-check clean.
</success_criteria>

<output>
Create `.planning/phases/94-confidence-monitor-output/94-03-SUMMARY.md` when done.
</output>
