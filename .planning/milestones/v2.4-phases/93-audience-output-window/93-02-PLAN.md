---
phase: 93-audience-output-window
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - src/views/__tests__/AudienceOutputView.test.ts
autonomous: true
requirements: [R270, R271]

must_haves:
  truths:
    - "An automated jsdom test proves currentSlide follows the run channel: driving an injected in-memory channel with a state message whose index selects a known slide renders that slide, and a later higher-seq state advances it (R270/R271)."
    - "A test proves postHello() is posted on mount (the hello handshake) and that the audience view NEVER posts a state message — control is the single writer (R270/R271)."
    - "A test proves ZERO operator chrome renders: no exit button, no nav chevrons, no progress pill, no slide count, no organizational label — and that cursor: none is applied while fullscreen (R270)."
    - "A test proves the loading/empty state is pure black: before any state arrives (index null) and when the index is out of range, no SlideCanvas, no spinner, and no copy render (congregation-safe divergence)."
    - "A test proves Screen Wake Lock behavior: with navigator.wakeLock present, request('screen') is called on mount and re-called on a dispatched visibilitychange->visible; with navigator.wakeLock absent, mount does not throw (R271)."
    - "A test proves fullscreen-loss recovery: setting document.fullscreenElement null and dispatching fullscreenchange renders the 'Re-enter fullscreen' affordance over the live slide, does NOT close the channel or unmount, restores the cursor, and clicking the affordance calls requestFullscreen (R271; Pitfall 6)."
  artifacts:
    - src/views/__tests__/AudienceOutputView.test.ts
  key_links:
    - "An in-memory BroadcastChannelFactory passed as the channelFactory prop -> the test posts state/reads posted messages to drive onState and assert postHello fired + postState never fired."
    - "Per-test install/delete of navigator.wakeLock (present vs absent) mirroring MonitorSetupView.test.ts's window.* install/delete idiom; assert request('screen') on mount and on visibilitychange->visible."
    - "Per-test stub of Element.prototype.requestFullscreen + Object.defineProperty(document,'fullscreenElement') + dispatched fullscreenchange (PresentationViewer.test.ts idiom), asserting the OPPOSITE of PresentationViewer: affordance renders, channel stays open."
    - "Mocked vue-router reactive route (params.serviceId + query.org) and a stubbed services store / mocked useSlideshowAssembly returning fixed assembledSlideshow + isLoading refs."
---

<objective>
Author the behavioral test coverage for the Audience Output Window built in 93-01. Prove the slide
follows the receive-only run channel, the hello handshake fires and the view never writes state, no
operator chrome renders and the cursor is hidden while fullscreen, the loading/empty state is pure
black, the Screen Wake Lock is acquired and re-acquired (and tolerates absence), and fullscreen loss
surfaces the re-enter affordance WITHOUT tearing down the session.

Purpose: Lock the R270/R271 behavior with automated jsdom tests so the autonomous run has proof for
the mocked paths. Real fullscreen on a second monitor, real wake-lock endurance over a service
length, and true chrome-free/cursor-free projection remain HUMAN-UAT items (deferred — see the
verification note).

Output: `src/views/__tests__/AudienceOutputView.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-audience-output-window/93-CONTEXT.md
@.planning/phases/93-audience-output-window/93-UI-SPEC.md
@.planning/phases/93-audience-output-window/93-PATTERNS.md
@src/utils/runChannel.ts
@src/views/AudienceOutputView.vue
@src/views/__tests__/ServiceEditorView.test.ts
@src/components/__tests__/PresentationViewer.test.ts
@src/views/__tests__/MonitorSetupView.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author AudienceOutputView.test.ts (channel, chrome-absence, wake lock, fullscreen recovery)</name>
  <files>src/views/__tests__/AudienceOutputView.test.ts</files>
  <read_first>
    - src/views/AudienceOutputView.vue (the finished view — its channelFactory prop, the testids / accessible labels / classes it actually exposes, the index->currentSlide gate, the re-enter affordance's aria-label, the cursor binding, the wake-lock and fullscreenchange lifecycle). Author tests against what the view REALLY renders, not against this plan's prose.
    - src/views/__tests__/ServiceEditorView.test.ts (the mount + router mock + Pinia + @/firebase inert-stub harness: the reactive mockRoute lines 41-46 — seed params.serviceId + query.org; the vi.mock('@/firebase') block lines 81-85; enableAutoUnmount(afterEach) line 33 so the view's onUnmounted channel/lock/listener cleanup actually runs)
    - src/components/__tests__/PresentationViewer.test.ts (the fullscreen stubbing beforeEach lines 317-325 — Element.prototype.requestFullscreen, document.exitFullscreen, Object.defineProperty(document,'fullscreenElement'); the dispatch-fullscreenchange assertion pattern lines 520-527; the Service/AssembledSlide type-faking imports lines 6-10)
    - src/views/__tests__/MonitorSetupView.test.ts (the per-test install/delete of a window.* / navigator.* capability lines 39-55 & 71-74 — mirror this shape for navigator.wakeLock present vs absent)
    - src/utils/runChannel.ts (BroadcastChannelLike lines 48-52, BroadcastChannelFactory line 55, RunState lines 26-30, the RunStateMessage/HelloMessage discriminators lines 32-41 — the test builds an in-memory fake channel matching BroadcastChannelLike and posts these message shapes)
  </read_first>
  <action>
    Create src/views/__tests__/AudienceOutputView.test.ts using the ServiceEditorView.test.ts harness
    shape (describe/it, mount, flushPromises, enableAutoUnmount(afterEach)). Mock vue-router with a
    reactive route seeded params.serviceId and query.org and a stubbed useRouter. Keep @/firebase inert
    (vi.mock('@/firebase', () => ({ auth: {}, db: {}, functions: {} }))). Stub @/stores/auth with a
    module-scope getter mock exposing at least orgId and settings.slideTypography (so the font-gate's
    cssVarsFor/resolvedFontChoice read a valid shape). Prefer mocking useSlideshowAssembly to return a
    fixed { assembledSlideshow: ref([...fake AssembledSlide[]]), isLoading: ref(false) } and stubbing the
    services store's services/subscribe/unsubscribeAll, rather than driving real Firestore — this keeps
    the test about THIS view's behavior. Stub the SlideCanvas child (a component stub exposing play/pause
    vi.fns and rendering a data-testid slot marker) so the test can assert whether a slide is rendered
    without the full canvas.

    CHANNEL FAKE: build an in-memory BroadcastChannelFactory that returns a BroadcastChannelLike whose
    postMessage records every message and whose addEventListener stores the message callback so the test
    can (a) read what the view posted and (b) push messages INTO the view. Pass it to the view via the
    channelFactory prop. Because the view uses the real openRunChannel with this fake, the test drives
    onState by invoking the stored listener with { data: { type: 'state', index, blackout: false, seq } }.

    Write these tests:
    - Channel-driven slide: mount, then push a state with an index selecting a known fake slide, await
      nextTick, and assert the SlideCanvas stub received that slide (or its testid renders). Then push a
      higher-seq state with a different index and assert the rendered slide advances.
    - Hello handshake + never-writes-state: after mount, assert the fake channel recorded a postMessage
      with type 'hello'. Across the whole lifecycle (mount, several onState pushes, unmount) assert the
      fake channel recorded NO postMessage with type 'state' — the audience view never writes state.
    - Channel closed on unmount: unmount and assert the fake channel's close() was called.
    - No operator chrome: mount with a valid slide showing and assert there is no exit button, no nav
      chevron, no progress pill, no slide-count, and no organizational-label element (query by the
      accessible roles/labels/testids PresentationViewer uses for those — they must be ABSENT here). Assert
      the root surface carries cursor: none while fullscreen (fullscreenElement truthy).
    - Pure-black loading/empty: mount with index still null (no state pushed) and assert NO SlideCanvas
      renders, no spinner, and no loading/empty copy. Push a state whose index is out of range and assert
      the same pure-black result (no SlideCanvas, no error text).
    - Wake lock present: install navigator.wakeLock with request as a vi.fn resolving a fake sentinel;
      mount, await, and assert request('screen') was called once; dispatch a visibilitychange with
      document.visibilityState forced to 'visible' and assert request('screen') was called again.
    - Wake lock absent: delete navigator.wakeLock (feature absent) and assert mount + flushPromises does
      not throw. Restore/delete navigator.wakeLock in afterEach so cases do not bleed.
    - Fullscreen-loss recovery (the OPPOSITE of PresentationViewer): stub the fullscreen API per
      PresentationViewer.test.ts; with fullscreenElement set null, dispatch fullscreenchange and assert
      the 'Re-enter fullscreen' affordance renders (by its aria-label), the channel's close() was NOT
      called and the component is still mounted (no teardown), and the cursor is restored (root is not
      cursor:none while windowed). Then click the affordance and assert Element.prototype.requestFullscreen
      was called. Also assert that with fullscreenElement truthy the affordance is NOT rendered.
    Prefer querying by data-testid / role / accessible label over brittle class selectors.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/AudienceOutputView.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - currentSlide-follows-channel and higher-seq-advances assertions pass against the injected fake.
    - postHello-on-mount and never-posts-state assertions pass; channel close() on unmount is proven.
    - Chrome-absence assertions (no exit/nav/progress/slide-count/org-label) pass; cursor:none-while-
      fullscreen is asserted.
    - Pure-black loading (index null) and out-of-range index both render no SlideCanvas and no copy.
    - Wake-lock present -> request('screen') on mount and again on visibilitychange->visible; wake-lock
      absent -> no throw.
    - Fullscreen loss renders the re-enter affordance, does NOT close the channel / unmount, restores the
      cursor, and clicking it calls requestFullscreen; affordance absent while fullscreen.
    - `npx vitest run src/views/__tests__/AudienceOutputView.test.ts` passes and `npm run type-check` is
      clean.
  </acceptance_criteria>
  <done>The audience view's channel-driven index, hello handshake, never-writes-state, chrome-absence, cursor toggle, pure-black gate, wake-lock acquire/re-acquire/absence, and no-teardown fullscreen recovery are all proven by passing jsdom tests.</done>
</task>

</tasks>

<verification>
- `npx vitest run` shows the same baseline as before (src/storage.rules.test.ts) plus the new passing
  test file; no `--dir src`.
- `npm run type-check` (vue-tsc --build, per CLAUDE.md — use `NODE_OPTIONS=--max-old-space-size=8192`
  under memory pressure) passes clean.
- HUMAN-UAT (deferred, mark human_needed): real fullscreen on a second physical monitor, actual
  Screen Wake Lock endurance over a realistic service length, true chrome-free/cursor-free projection,
  and the calm re-enter affordance's findability from the booth under live pressure — none provable by
  jsdom; the verifier flags these and the autonomous run defers them to the milestone-end UAT pass.
</verification>

<success_criteria>
- Channel-driven index, hello handshake, never-writes-state, chrome-absence + cursor toggle, pure-black
  loading gate, wake-lock acquire/re-acquire/absence, and no-teardown fullscreen recovery are all
  covered by passing automated tests (R270, R271).
- No baseline regression in `npx vitest run`; type-check clean.
</success_criteria>

<output>
Create `.planning/phases/93-audience-output-window/93-02-SUMMARY.md` when done.
</output>
