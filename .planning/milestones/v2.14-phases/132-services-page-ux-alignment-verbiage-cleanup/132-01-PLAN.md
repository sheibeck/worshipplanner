---
phase: 132-services-page-ux-alignment-verbiage-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServicesView.vue
autonomous: true
requirements: [R406, R407]
must_haves:
  truths:
    - "/services renders a standard page header: an <h1 class=\"text-xl font-semibold text-gray-100\">Services</h1> matching SongsView/RosterView/SettingsView"
    - "At phone width the Services tab strip and action buttons do not cause horizontal overflow of the page body — tabs scroll/wrap and Share/Service-Template/New-Service buttons stay tappable"
    - "Desktop (sm: and up) tab + action-button layout is visually unchanged from today"
  artifacts:
    - src/views/ServicesView.vue
  key_links:
    - "The <h1> uses the exact standard classes text-xl font-semibold text-gray-100 (grep-verifiable)"
    - "Responsive behavior is driven by the app's existing Tailwind sm:/md: breakpoint utilities; the old single-row `flex items-center gap-1 … flex-1` tab bar is restructured so it no longer overflows"
---

<objective>
Bring `src/views/ServicesView.vue` up to the app's standard page-header + mobile conventions.

Two changes, both template-only, on a single file:
- R406 — add the standard `<h1>Services</h1>` page-header block (there is no shared PageHeader component; match the established inline pattern used by SongsView/RosterView/SettingsView).
- R407 — make the tab strip scroll/wrap and let the action buttons (Service Template / New Service) reposition at phone widths, using the app's existing `sm:`/`md:` Tailwind conventions, while preserving today's desktop layout and comfortable tap targets.

Purpose: `/services` currently jumps straight into a single-row tab bar with no title, and that row overflows and cramps on a phone. This makes it consistent with every other main view.
Output: an updated `ServicesView.vue` template (no script/logic changes, no new components, no new dependencies).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-services-page-ux-alignment-verbiage-cleanup/132-CONTEXT.md

# The file being changed:
@src/views/ServicesView.vue

# Header pattern to match exactly (h1 classes + wrapper structure):
@src/views/SongsView.vue
@src/views/RosterView.vue
@src/views/SettingsView.vue
</context>

## Artifacts this phase produces (Plan 01)
- Modified `src/views/ServicesView.vue`: a standard `<h1 class="text-xl font-semibold text-gray-100">Services</h1>` header block above the tab bar, and a responsive tab/action-button layout that no longer overflows at phone width.

<tasks>

<task type="auto">
  <name>Task 1: Add the standard "Services" page header (R406)</name>
  <files>src/views/ServicesView.vue</files>
  <read_first>
    - src/views/ServicesView.vue (lines 1–68: the `px-6 py-8` container and the current tab bar, which today is the first element inside the container — there is no header block)
    - src/views/RosterView.vue lines 4–11 and src/views/SettingsView.vue lines 4–8 (the exact standard inline header structure to mirror; note the shared `<h1 class="text-xl font-semibold text-gray-100">` element)
  </read_first>
  <action>
    Inside `ServicesView.vue`'s `<div class="px-6 py-8">`, immediately BEFORE the existing tab bar `<div>` (the current `flex items-center gap-1 … border-b` row), add a page-header block containing exactly `<h1 class="text-xl font-semibold text-gray-100">Services</h1>` — the same h1 element/classes RosterView, SongsView and SettingsView use. Match this pattern rather than inventing markup; there is no shared PageHeader component and RunHeader.vue is run-mode only, so keep it inline. Do NOT give the header wrapper its own `border-b border-gray-800`: the tab bar directly below already carries that bottom border, and a second one would render a double horizontal divider — use a plain spacing wrapper (a small bottom margin) so the tab bar's border stays the single divider. A short muted subtitle line under the h1 is at your discretion (RosterView/SongsView show a count subtitle; SettingsView shows the org name) but is not required. Leave the `<script setup>` block, all computeds, tab logic, and the tab bar's own contents unchanged in this task.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/ServicesView.vue','utf8'); if(!/<h1 class=\"text-xl font-semibold text-gray-100\">\s*Services\s*<\/h1>/.test(s)) { console.error('missing standard Services h1'); process.exit(1);} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `ServicesView.vue` contains `<h1 class="text-xl font-semibold text-gray-100">Services</h1>` (whitespace between the tag and text permitted), placed above the tab bar inside the `px-6 py-8` container.
    - The h1 classes match SongsView/RosterView/SettingsView exactly.
    - No double horizontal divider appears between the title and the tab bar (header wrapper has no `border-b`; tab bar keeps its existing `border-b border-gray-800`).
    - `npm run type-check` still passes (no new errors introduced).
  </acceptance_criteria>
  <done>ServicesView renders the exact standard `<h1 class="text-xl font-semibold text-gray-100">Services</h1>` above the tab bar, no double divider, no script changes, type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Make the tab strip + action buttons phone-friendly (R407)</name>
  <files>src/views/ServicesView.vue</files>
  <read_first>
    - src/views/ServicesView.vue lines 5–67 (the tab bar: an outer `flex items-center gap-1 mb-6 border-b border-gray-800 pb-0`, three tab buttons, a `<div class="flex-1" />` spacer, then the Service Template and New Service buttons — all on one row)
    - src/views/RosterView.vue lines 5–33 and src/views/SongsView.vue lines 15–22 (the app's established responsive header/button conventions: `flex flex-col sm:flex-row … gap-…`, `sm:flex-wrap`, and button groups that go full-width on mobile via `w-full sm:w-auto` / `[&>*]:w-full sm:[&>*]:w-auto`)
  </read_first>
  <action>
    Restructure the ServicesView tab bar so it stops overflowing on a phone while looking identical on desktop. Concretely: change the outer tab-bar container so it stacks vertically on mobile and returns to a single row at the `sm:` breakpoint (mobile column, `sm:` row), keeping its existing bottom border as the section divider. Wrap the three tab buttons (Services / Song Rotation / Scripture Rotation) in their own inner horizontal container that scrolls horizontally when it can't fit (an `overflow-x-auto` flex row) so long tab labels never push the page body wider than the viewport. Remove the fixed `<div class="flex-1" />` spacer and instead push the action-button group to the right only at `sm:` and up (e.g. `sm:ml-auto`); wrap the Service Template and New Service buttons together in a `flex items-center gap-2 flex-wrap` group that sits on its own full-width row BELOW the tabs on mobile and inline to the right on `sm:`+. Preserve every button's existing `px-3 py-2` sizing and the `v-if="authStore.isEditor"` guards so tap targets and permissions are unchanged. Whether the tab strip ultimately scrolls vs wraps, and the exact breakpoint utilities used, are your discretion per CONTEXT — the hard requirement is: no horizontal overflow of the page body at ~360px width, comfortably tappable controls, and a desktop layout that matches today (tabs on the left, action buttons on the right). Template-only; do not change `<script setup>`, tab state, or handlers.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/ServicesView.vue','utf8'); const responsive=/overflow-x-auto/.test(s) && /sm:/.test(s); const noSpacer=!/<div class=\"flex-1\" \/>/.test(s); if(!(responsive&&noSpacer)){console.error('tab bar not restructured: responsive='+responsive+' spacerRemoved='+noSpacer); process.exit(1);} console.log('ok');"</automated>
    <human-check>Load /services in a phone-width viewport (DevTools ~360px). Confirm: the three tabs scroll/wrap without pushing the page wider than the screen (no horizontal page scrollbar), the Service Template and New Service buttons are fully visible and tappable, and at desktop width the layout looks the same as before this phase.</human-check>
  </verify>
  <acceptance_criteria>
    - The old single `flex items-center gap-1 … flex-1` overflowing row is gone; the tab buttons live in an `overflow-x-auto` container and the `<div class="flex-1" />` spacer is removed.
    - At ~360px width there is no horizontal overflow of the page body and all action buttons remain reachable/tappable.
    - At `sm:` and above, tabs sit left and action buttons sit right — matching the current desktop appearance.
    - Editor-only guards (`v-if="authStore.isEditor"`) on Service Template and New Service are preserved.
    - `npm run type-check` passes.
  </acceptance_criteria>
  <done>Tabs scroll/wrap and action buttons reflow below them at phone width with no page-body horizontal overflow; desktop layout unchanged; editor guards intact; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → app UI (authenticated) | Editor-guarded action buttons trigger service creation/template editing; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-01a | Elevation of Privilege | ServicesView action buttons (New Service / Service Template) | low | mitigate | Preserve the existing `v-if="authStore.isEditor"` guards during restructure — no non-editor gains a create/edit affordance |
| T-132-01b | Information Disclosure | ServicesView template | low | accept | Presentational/layout-only change; renders no new data and adds no new fields |
| T-132-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (template-only edits); no new supply-chain surface |
</threat_model>

<verification>
- `npm run type-check` passes (vue-tsc --build; NOT the narrower `-p tsconfig.app.json` per CLAUDE.md).
- `npx vitest run` shows only the known baseline failure `src/storage.rules.test.ts` (no new failures).
- Manual phone-width visual check per Task 2's human-check.
</verification>

<success_criteria>
- `/services` shows the standard `<h1 class="text-xl font-semibold text-gray-100">Services</h1>` header (R406).
- The Services tabs + action buttons are usable and non-overflowing at phone width, and unchanged on desktop (R407).
- No type or test regressions beyond the documented baseline.
</success_criteria>

<output>
Create `.planning/phases/132-services-page-ux-alignment-verbiage-cleanup/132-01-SUMMARY.md` when done.
</output>
