---
phase: 132-services-page-ux-alignment-verbiage-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServiceEditorView.vue
autonomous: true
requirements: [R409]
must_haves:
  truths:
    - "Every user-visible 'Planning Center' string is inventoried and CLASSIFIED (KEEP vs REMOVE/REWORD) in an auditable table recorded in the PLAN and the SUMMARY"
    - "The Planned/locked banner no longer contains the sentence asserting the external tool already holds the plan (removed from the lockBannerBody computed in ServiceEditorView.vue)"
    - "Every KEEP-classified Planning Center integration/export/import string is unchanged (Settings PC section, Export to/Exported to Planning Center, Import from Planning Center, PC/CSV/roster import modals, etc.)"
    - "No code identifier, function/variable name, inline comment, API/config string, or test fixture was modified — only user-visible incidental copy"
  artifacts:
    - src/views/ServiceEditorView.vue
    - "Classification table (KEEP vs REMOVE/REWORD) in 132-03-SUMMARY.md"
  key_links:
    - "The confirmed removal lives in the `lockBannerBody` computed (ServiceEditorView.vue ~L2189), NOT in serviceEditorActionBar.ts"
---

<objective>
Remove incidental "Planning Center" UI copy WITHOUT touching the app's real, functioning Planning Center integration/export copy (R409). This is a load-bearing, inventory-first task: a blind find-and-replace would garble substantive integration copy.

Two tasks, in order:
- Task 1 (FIRST, before any edit): grep-inventory every user-visible "Planning Center" string and classify each KEEP vs REMOVE/REWORD per the CONTEXT rule; record the auditable table in the SUMMARY.
- Task 2: apply only the classified removals — the one owner-confirmed removal is the Planned/locked banner's leading sentence; apply any additional incidental-copy reclassifications the inventory surfaces, per your recorded classification.

Purpose: the phrase should appear only where it genuinely describes the PC integration/export/import; incidental mentions add nothing.
Output: an updated `ServiceEditorView.vue` (banner sentence removed) plus an auditable classification table. Identifiers, comments, API strings, and tests are never touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-services-page-ux-alignment-verbiage-cleanup/132-CONTEXT.md

# The file being changed (banner computed):
@src/views/ServiceEditorView.vue
</context>

## Artifacts this phase produces (Plan 03)
- Modified `src/views/ServiceEditorView.vue`: the `lockBannerBody` computed no longer contains the sentence asserting the external tool already holds the plan; the trailing "Reopen it for editing…" guidance is preserved.
- An auditable KEEP vs REMOVE/REWORD classification table (in the PLAN below and reproduced/confirmed in `132-03-SUMMARY.md`).

## Starting classification (planner's inventory — Task 1 must verify/complete this against current source)

The app has a REAL Planning Center integration/export/import. The default disposition is KEEP; only clearly-incidental user-visible copy is REMOVE/REWORD. Code identifiers, function/variable names, inline comments, API/config strings, and test fixtures are NEVER touched regardless of classification.

<!-- planner-discipline-allow: already has this plan -->

| # | Location (approx) | User-visible copy (summary) | Classification |
|---|-------------------|------------------------------|----------------|
| 1 | ServiceEditorView.vue ~L2189 (`lockBannerBody` computed, PC-evidence branch) | Leading sentence asserting the external tool "already has this plan", before the "Reopen it for editing to change the order, slides or roles here." guidance | **REMOVE** (owner-confirmed) — drop the leading sentence, keep the Reopen guidance |
| 2 | SettingsView.vue ~L69/85/89/138/149/159 | "Planning Center Integration" heading, "Enable Planning Center integration", "…Export to Planning Center are hidden…", credential placeholders | KEEP — genuine integration settings copy |
| 3 | ServiceEditorView.vue ~L285/472/654/686/2021/2201/3736/4074 | "Export to Planning Center", "Exported to Planning Center", export-credential prompt, delete/reopen/reload warnings that describe the real export | KEEP — substantive export copy |
| 4 | RosterView.vue ~L21/81/89/367 | "Import from Planning Center" buttons/empty-state, "not synced from Planning Center" phone note | KEEP — real import affordance/behavior copy |
| 5 | RosterImportModal.vue ~L36/83/172/192/233 | Import-from-PC modal headings/status/errors | KEEP — real PC roster import UI |
| 6 | PcImportModal.vue ~L36/79/99/119 | Import songs from PC modal headings/status | KEEP — real PC song import UI |
| 7 | CsvImportModal.vue ~L39/93/95 | "Planning Center export format supported", "How to export from Planning Center", PC Music → Songs step | KEEP — real CSV-from-PC import guidance |
| 8 | SongTable.vue ~L43 | "Import your songs from Planning Center or add them one at a time." | KEEP — points at the real import affordance |
| 9 | GettingStarted.vue ~L143 | "Import songs from Planning Center CSV or add them manually." | KEEP — onboarding pointer to the real import |
| — | Comments: SettingsView.vue L67/597/1014; ServiceEditorView.vue L275/628/1859/1874/2171/3256/3728/4068; ScriptureInput.vue L261 | inline comments mentioning Planning Center | NEVER TOUCH (comment) |
| — | Non-.vue: planningCenterApi.ts, pcSongImport.ts, csvImport.ts, scripture.ts, stores/*, types/*, all __tests__/* | identifiers, API/config strings, fixtures | NEVER TOUCH (identifier/API/test) |

Net result of this starting inventory: exactly **one** user-visible REMOVE (row 1). Task 1 must re-run the grep against current source, confirm no additional incidental user-visible copy was missed, and record the final table in the SUMMARY. If Task 1 finds an additional clearly-incidental string, classify and record it, and remove it in Task 2 with the same auditability.

<tasks>

<task type="auto">
  <name>Task 1: Inventory + classify every user-visible "Planning Center" string (do this BEFORE any edit)</name>
  <files>132-03-SUMMARY.md (classification table output); reads across src/ (no source edits)</files>
  <read_first>
    - .planning/phases/132-services-page-ux-alignment-verbiage-cleanup/132-CONTEXT.md (the R409 classify rule: KEEP = real PC integration/export/import copy; REMOVE/REWORD = incidental UI copy; identifiers/comments/API strings/tests never touched)
    - The "Starting classification" table above (planner's inventory — your baseline to verify and complete)
  </read_first>
  <action>
    Run a full inventory of every "Planning Center" occurrence under `src/` (a recursive grep across all file types, capturing file and line). For each occurrence, classify it as one of: KEEP (user-visible copy that genuinely describes the real PC integration/export/import), REMOVE/REWORD (incidental user-visible copy that mentions PC where it adds nothing), or NEVER-TOUCH (inline comment, code identifier, function/variable name, API/config string, or test fixture — these are out of bounds regardless). Verify every row of the planner's Starting classification against the current source and reconcile any drift (line numbers may have shifted from the header work in sibling plans if run after them, but these plans are independent and touch different files, so ServiceEditorView.vue should be stable). Confirm the single owner-specified removal (the Planned/locked banner's leading sentence in the `lockBannerBody` computed) is present and located. Produce the FINAL classification table and record it in `132-03-SUMMARY.md` so the removals are auditable. Do NOT edit any source file in this task — inventory and classification only.
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process'); const out=cp.execSync('git grep -n \"Planning Center\" -- src/ || true',{encoding:'utf8'}); const n=out.split('\n').filter(Boolean).length; if(n < 20){console.error('inventory returned too few occurrences ('+n+') — expected the full PC surface'); process.exit(1);} console.log('inventory occurrences: '+n);"</automated>
  </verify>
  <acceptance_criteria>
    - A classification table covering EVERY user-visible "Planning Center" occurrence is recorded in `132-03-SUMMARY.md`, each row marked KEEP, REMOVE/REWORD, or NEVER-TOUCH.
    - The table identifies exactly the owner-confirmed removal (the banner's leading sentence) plus any additional incidental copy the inventory surfaces (expected: none beyond the banner).
    - No source file was modified in this task (inventory-only).
  </acceptance_criteria>
  <done>An auditable KEEP/REMOVE/NEVER-TOUCH classification table covering every "Planning Center" occurrence is recorded in 132-03-SUMMARY.md; the single owner-confirmed removal is identified; no source edited.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the classified incidental banner sentence (R409)</name>
  <files>src/views/ServiceEditorView.vue</files>
  <read_first>
    - src/views/ServiceEditorView.vue lines 2179–2202 (the `lockBannerLead`, `lockBannerBody`, and `reopenPcWarning` computeds; the target is `lockBannerBody`'s `hasPcExportEvidence.value ? … : …` ternary at ~L2187–2191)
    - The final classification table from Task 1 (the authoritative list of what to remove)
  </read_first>
  <action>
    In `ServiceEditorView.vue`'s `lockBannerBody` computed, edit the PC-evidence (true) branch of the `hasPcExportEvidence` ternary so it drops its leading sentence — the sentence stating the external planning tool already holds this plan — and retains only the "Reopen it for editing to change the order, slides or roles here." guidance. Change ONLY that string literal; leave the `hasPcExportEvidence` condition, the non-evidence branch, `lockBannerLead`, `reopenPcWarning`, and every surrounding comment untouched (the comment at ~L2171 about gating on evidence is a comment and stays). Apply any additional REMOVE/REWORD items the Task 1 table recorded (expected: none beyond this banner) with the same minimal, string-only edits. Do NOT touch any KEEP string — the Settings PC section, "Export to Planning Center" / "Exported to Planning Center", the delete/reopen/reload export warnings, and all "Import from Planning Center" affordances must read exactly as before. Do NOT touch identifiers, comments, API/config strings, or tests.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/ServiceEditorView.vue','utf8'); const removed=!/already has this plan/.test(s); const keptReopen=/Reopen it for editing to change the order, slides or roles/.test(s); const keepA=/Export to Planning Center/.test(s); const keepB=/Exported to Planning Center/.test(s); const keepC=/exported to Planning Center/.test(s); if(!(removed&&keptReopen&&keepA&&keepB&&keepC)){console.error(JSON.stringify({removed,keptReopen,keepA,keepB,keepC})); process.exit(1);} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The removed banner sentence is gone from `ServiceEditorView.vue` (the `already has this plan` phrase no longer occurs in the file).
    - The retained guidance "Reopen it for editing to change the order, slides or roles…" is still present in the banner.
    - KEEP anchors are unchanged: "Export to Planning Center", "Exported to Planning Center", and the "…exported to Planning Center…" export warnings all still occur.
    - Only `src/views/ServiceEditorView.vue` changed; no identifier, comment, API string, or test was modified.
    - `npm run type-check` passes; `npx vitest run` shows only the baseline `src/storage.rules.test.ts` failure.
  </acceptance_criteria>
  <done>The banner's leading "already has this plan" sentence is removed and the Reopen guidance retained; all KEEP anchors unchanged; only ServiceEditorView.vue edited; type-check and suite clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (copy-only) | This plan changes user-visible display copy in one computed; it introduces no new inputs, data flows, or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-03 | Tampering | Planning Center integration/export copy | medium | mitigate | Inventory-first classification + KEEP-anchor grep assertions ensure the string edit does not garble the real integration copy or touch identifiers/API strings/tests |
| T-132-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (single string edit); no new supply-chain surface |
</threat_model>

<verification>
- Task 1's classification table is recorded in `132-03-SUMMARY.md` (auditable), with exactly the owner-confirmed removal called out.
- `npm run type-check` passes (vue-tsc --build).
- `npx vitest run` shows only the known baseline failure `src/storage.rules.test.ts` (removing the banner sentence breaks no test — no test asserts that sentence).
</verification>

<success_criteria>
- Every user-visible "Planning Center" string is classified in an auditable table (R409).
- The Planned/locked banner no longer contains the "already has this plan" sentence; the Reopen guidance remains.
- Every KEEP-classified integration/export/import string is unchanged; no identifiers/comments/API strings/tests were touched.
- No type or test regressions beyond the documented baseline.
</success_criteria>

<output>
Create `.planning/phases/132-services-page-ux-alignment-verbiage-cleanup/132-03-SUMMARY.md` when done (include the final classification table).
</output>
