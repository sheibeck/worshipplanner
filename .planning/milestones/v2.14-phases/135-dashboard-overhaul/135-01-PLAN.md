---
phase: 135-dashboard-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/DashboardView.vue
  - src/utils/dashboardReadiness.ts
  - src/utils/__tests__/dashboardReadiness.test.ts
autonomous: true
requirements: [R414, R415, R416, R419]
must_haves:
  truths:
    - "The 'Volunteer coverage' section (Active volunteers count + under-staffed roles) no longer appears anywhere on the dashboard (R414)."
    - "The dashboard leads with an upcoming-services 'needs your attention' feed, soonest first, full width (R415)."
    - "Each feed service shows one combined worst-of readiness signal (songs-needed → media-missing → draft → ready) computed via dashboardReadinessOf, which REUSES readinessOf from myScheduleGrouping — no second media-readiness definition exists (R416)."
    - "A service with zero SONG slots (total === 0) never reads as 'media-missing' — the total > 0 guard keeps readinessOf([])'s waiting default from leaking in (R416 zero-slots edge)."
    - "When upcomingServices.length === 0 the feed is replaced by a friendly 'You're all caught up' card; the Song library still renders (R419)."
    - "Readiness state is communicated by color AND an always-visible text label (never color alone) on both the hero pill and the list rows."
  artifacts:
    - "src/utils/dashboardReadiness.ts — pure serviceReadinessSongs + dashboardReadinessOf (imports readinessOf)"
    - "src/utils/__tests__/dashboardReadiness.test.ts — unit tests for the worst-of order + zero-slots guard"
    - "src/views/DashboardView.vue — Volunteer coverage removed; single-column feed with readiness signal; all-caught-up empty state"
  key_links:
    - "dashboardReadinessOf → readinessOf(songs) is the single source of media-readiness truth"
    - "serviceReadinessSongs maps service SONG slots → songStore.songs attachments → RehearseSong[] fed to readinessOf"
    - "upcomingServices.length === 0 gates the R419 branch vs the feed"
---

<objective>
Reframe DashboardView.vue from a metrics panel into a "needs your attention" feed. Remove the undefined
"Volunteer coverage" section (R414), make the upcoming-services list the full-width spine (R415), attach a
combined worst-of per-service readiness signal that REUSES the v2.12 readinessOf computation (R416), and add
the "you're all caught up" empty/first-run state (R419).

Purpose: give a planner one glanceable place to see what is coming up and what needs action, without
inventing a second readiness definition or a new data model.
Output: a pure, tested dashboardReadiness util + a restructured DashboardView feed with an empty state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-dashboard-overhaul/135-CONTEXT.md
@.planning/phases/135-dashboard-overhaul/135-UI-SPEC.md
@src/views/DashboardView.vue
@src/utils/myScheduleGrouping.ts
@src/components/ScheduleServiceCard.vue
</context>

<artifacts_produced>
- src/utils/dashboardReadiness.ts (new) — `serviceReadinessSongs(service, songsById)` and
  `dashboardReadinessOf(songStats, songs, status)`; imports `readinessOf` from myScheduleGrouping.
- src/utils/__tests__/dashboardReadiness.test.ts (new) — worst-of ordering + zero-slots guard coverage.
- src/views/DashboardView.vue (modified) — Volunteer coverage section removed, single-column feed,
  readiness signal wired into hero pill + list rows, R419 empty state.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure dashboard readiness rollup util (reuses readinessOf)</name>
  <files>src/utils/dashboardReadiness.ts, src/utils/__tests__/dashboardReadiness.test.ts</files>
  <read_first>
    - src/utils/myScheduleGrouping.ts:98-113 (readinessOf + hasMedia — the media definition to REUSE; note
      state is 'ready' | 'partial' | 'waiting', waiting is also the zero-songs default)
    - src/utils/rehearseAccess.ts:15-40 (RehearseSong / RehearseAttachment shapes; SongAttachmentKind is
      'document' | 'audio' | 'link' — readinessOf keys media off 'document'/'audio')
    - src/utils/rehearseAccess.ts:109-121 (distinctSongSlots dedupe-by-songId precedent to mirror)
    - src/views/DashboardView.vue:190-204 (serviceSongStats {filled,total}, isServiceReady — the existing
      song-assignment stat this rollup composes with)
    - src/types/service.ts (Service, ServiceSlot/SongSlot, and the status literal — confirm 'draft')
    - src/types/song.ts (Song.attachments shape)
  </read_first>
  <behavior>
    - serviceReadinessSongs(service, songsById): returns distinct filled SONG slots (kind === 'SONG' && songId,
      deduped by songId, first-occurrence order) mapped to minimal RehearseSong `{ id, title, attachments }`;
      a slot whose songId is missing from songsById becomes a stub `{ id, title: '(song removed)', attachments: [] }`
      (mirrors buildRehearseAccess's stub so it counts as missing media, never silently dropped).
    - dashboardReadinessOf(songStats, songs, status) returns the FIRST matching worst→best state:
      - Test: filled < total (total > 0) → { state: 'songs-needed', songsNeeded: total - filled }.
      - Test: total > 0, filled === total, readinessOf(songs).state === 'partial' → { state: 'media-missing',
        missingMediaCount: readinessOf(songs).missingCount }.
      - Test: total > 0, filled === total, all songs missing media (readinessOf → 'waiting') → 'media-missing'
        with missingMediaCount === songs.length.
      - Test: total > 0, filled === total, readinessOf(songs).state === 'ready', status === 'draft' → { state: 'draft' }.
      - Test: total > 0, filled === total, media ready, status === 'planned' → { state: 'ready' }.
      - Test (ZERO-SLOTS GUARD): total === 0, status === 'planned' → { state: 'ready' } (NOT 'media-missing');
        total === 0, status === 'draft' → { state: 'draft' }. readinessOf([])'s waiting default must never
        surface as media-missing because both songs-needed and media-missing checks are gated on total > 0.
  </behavior>
  <action>
    Create src/utils/dashboardReadiness.ts as a pure, store-free module (types-only imports, matching the
    myScheduleGrouping.ts convention). Export type DashboardReadinessState = 'songs-needed' | 'media-missing'
    | 'draft' | 'ready' and interface DashboardReadiness { state: DashboardReadinessState; songsNeeded: number;
    missingMediaCount: number }. Import readinessOf from '@/utils/myScheduleGrouping' and RehearseSong from
    '@/utils/rehearseAccess' — do NOT re-derive hasMedia or any media rule here; the whole point of R416 is that
    readinessOf is the single media-readiness definition. Implement serviceReadinessSongs(service, songsById:
    Map<string, Song>) using the distinct-by-songId dedupe idiom. Implement dashboardReadinessOf as a sequence of
    if-return checks in the worst→best order from 135-UI-SPEC.md Widget 1's rollup table; the media-missing check
    MUST be written with `total > 0 &&` as its first conjunct so a zero-slot service short-circuits before
    readinessOf is consulted. Default songsNeeded/missingMediaCount to 0 for states where they are not meaningful.
    Write the test file first (RED), covering every bullet in this task's behavior block plus the zero-slots guard,
    then implement to green. Follow the project comment convention (short comments; rationale in the behavior/ADRs).
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/dashboardReadiness.test.ts && npm run type-check</automated>
  </verify>
  <done>All readiness rollup tests pass (worst-of order + zero-slots guard), type-check clean, and
  dashboardReadiness.ts imports readinessOf rather than redefining media detection.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Volunteer coverage + restructure feed to full width + R419 empty state</name>
  <files>src/views/DashboardView.vue</files>
  <read_first>
    - src/views/DashboardView.vue:15-149 (the outer 2-col grid, Next service section, Volunteer coverage
      section ~L92-129 to delete, Song library section ~L131-147 to keep)
    - src/views/DashboardView.vue:215-226 (understaffedRoles computed + MIN_VOLUNTEERS_PER_ROLE — dead after
      R414, delete)
    - src/views/DashboardView.vue:275-289 (orgId watch — KEEP the rosterStore.subscribe call; Plan 02 R417
      needs rosterStore.people for emailLower→name mapping)
    - 135-UI-SPEC.md:104-123 (overall structure top-to-bottom), :235-253 (Widget 4 all-caught-up card + the
      GettingStarted checkmark svg path M5 13l4 4L19 7)
    - src/components/GettingStarted.vue (the done-state checkmark svg to reuse for the empty state icon)
  </read_first>
  <action>
    Delete the entire "Volunteer coverage" &lt;section&gt; and the now-dead understaffedRoles computed and
    MIN_VOLUNTEERS_PER_ROLE constant. Replace the outer `grid grid-cols-1 lg:grid-cols-2 gap-6 items-start`
    wrapper with a single-column vertical flow (e.g. `space-y-6`) so the feed and Song library each render full
    width per 135-UI-SPEC.md's top-to-bottom structure. Establish the R419 conditional skeleton: when
    upcomingServices.length === 0, render the all-caught-up card (dashed-border `rounded-lg border border-dashed
    border-gray-700 p-6 text-center`, full width, the reused GettingStarted checkmark svg path M5 13l4 4L19 7
    sized h-8 w-8 text-green-500, heading "You're all caught up", body "No upcoming services need your attention
    right now.", and a "Schedule a service" link to /services) IN PLACE of the feed; otherwise render the feed
    (the existing Next service hero + upcomingAfterNext list, now full width). The Song library section renders
    regardless of schedule state (outside the empty/feed branch). Do NOT remove the rosterStore import or its
    subscription in the orgId watch — a later plan depends on it. Keep GettingStarted, the page header, and the
    Song library markup unchanged. Match the app's dark gray-950 card language; introduce no new colors/sizes.
    Do NOT leave a comment in the file naming the removed section.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/DashboardView.vue','utf8'); if(/understaffedRoles|MIN_VOLUNTEERS_PER_ROLE/.test(s)) throw new Error('dead volunteer-coverage code still present'); if(!/You.re all caught up/.test(s)) throw new Error('R419 empty state missing'); if(!/rosterStore\.subscribe/.test(s)) throw new Error('rosterStore subscription was removed'); console.log('ok')" && npm run type-check</automated>
    <human-check>Load the dashboard at / with and without upcoming services: no Volunteer coverage panel; feed is full width; the all-caught-up card shows only when there are zero upcoming services; Song library always shows.</human-check>
  </verify>
  <done>Volunteer coverage section and its dead computed/constant are gone; feed and Song library are full
  width; the R419 all-caught-up card renders exactly when upcomingServices.length === 0; rosterStore
  subscription retained; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the R416 readiness signal into the hero card + feed rows</name>
  <files>src/views/DashboardView.vue</files>
  <read_first>
    - src/views/DashboardView.vue:40-89 (hero card pills at L40-57 to replace with the readiness pill; the
      upcomingAfterNext list rows at L73-88 with the bare dot + title tooltip to upgrade)
    - src/utils/dashboardReadiness.ts (Task 1 — serviceReadinessSongs + dashboardReadinessOf)
    - 135-UI-SPEC.md:132-173 (readiness signal rollup table, copy per state, hero pill recipe, list-row
      dot+label a11y rule, focus-ring utility)
    - src/components/ScheduleServiceCard.vue:6,150-160 (the focus:ring-2 focus:ring-indigo-500
      focus:ring-offset-2 focus:ring-offset-gray-950 utility to add to the hero + list rows)
  </read_first>
  <action>
    In the script, build a `songsById` Map from songStore.songs and a helper that, given a service, returns
    dashboardReadinessOf(serviceSongStats(service), serviceReadinessSongs(service, songsById), service.status)
    plus the derived label and color classes. Map states to the existing vocabulary from 135-UI-SPEC.md Widget 1:
    songs-needed → amber, copy "{songsNeeded} song(s) still needed"; media-missing → amber, copy
    "{missingMediaCount} song(s) missing media" when missingMediaCount is less than the service's readiness song
    count else "Songs missing media" (this reproduces readinessOf's partial-vs-waiting copy split); draft → gray,
    copy "Draft"; ready → green, copy "Ready". On the hero card, replace the existing songs-assigned/scripture
    two-pill block's FIRST pill with a single readiness pill using the existing `inline-flex items-center gap-1.5
    px-2.5 py-1 rounded-full text-xs font-medium border` recipe mapped to bg-{color}-900/40 border-{color}-700/50
    text-{color}-300 (gray uses bg-gray-800 border-gray-700 text-gray-400); keep the "Scripture set / No scripture"
    pill alongside it unchanged. On each upcomingAfterNext row, replace the bare dot + title tooltip with a
    dot-plus-inline-text-label pair (`<span class="h-2 w-2 rounded-full {colorDot}">` then `<span class="text-xs
    {colorText}">{label}</span>`) so state is conveyed by text, not color alone. Add the ScheduleServiceCard focus
    ring utility to the hero router-link and each list-row router-link (they currently lack a visible keyboard
    focus ring). Remove the now-unused isServiceReady function only if nothing else references it. Do NOT
    reimplement any media-readiness rule inline — read it only through dashboardReadinessOf/readinessOf.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/DashboardView.vue','utf8'); if(!/dashboardReadinessOf/.test(s)) throw new Error('readiness rollup not wired'); if(!/focus:ring-indigo-500/.test(s)) throw new Error('focus ring missing on feed rows'); console.log('ok')" && npm run type-check && npx vitest run src/utils/__tests__/dashboardReadiness.test.ts</automated>
    <human-check>Each feed service shows one worst-of readiness signal with a visible text label (not a bare dot); a zero-song-slot service never shows a media-missing signal; keyboard-tabbing the feed rows shows a focus ring.</human-check>
  </verify>
  <done>Hero card and list rows render the combined worst-of readiness signal (color + text label) via
  dashboardReadinessOf; focus rings present; zero-slots services never read as media-missing; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authStore.orgId → Firestore reads | The dashboard reads only the current org's services/songs already loaded by the org-scoped stores; no new collection paths are introduced this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-135-01 | Information Disclosure | DashboardView reads (services/songs) | low | accept | Reuses the already-subscribed org-scoped serviceStore/songStore; no new query paths, no cross-org reads added. |
| T-135-02 | Tampering | dashboardReadinessOf rollup | low | mitigate | Pure function, no writes, no I/O; readiness is a display signal, never a gate on any action. |

No package installs this plan (no npm/pip/cargo) — no supply-chain threat.
</threat_model>

<verification>
- `npx vitest run` shows the known baseline (only src/storage.rules.test.ts failing) — no new failures.
- `npm run type-check` (vue-tsc --build) clean.
- dashboardReadiness.ts imports readinessOf (grep) — no duplicate media definition.
</verification>

<success_criteria>
Volunteer coverage removed (R414); full-width upcoming-services feed (R415); each service shows one worst-of
readiness signal via dashboardReadinessOf reusing readinessOf, with the zero-slots guard (R416); all-caught-up
empty state on zero upcoming services (R419). Type-check and the readiness unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/135-dashboard-overhaul/135-01-SUMMARY.md` when done.
</output>
