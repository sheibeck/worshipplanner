---
phase: 135-dashboard-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["135-02"]
files_modified:
  - src/views/DashboardView.vue
  - src/utils/presenceRollup.ts
  - src/utils/__tests__/presenceRollup.test.ts
  - src/composables/usePresenceRollup.ts
autonomous: true
requirements: [R418]
must_haves:
  truths:
    - "The dashboard shows an editor-presence roll-up of who is currently editing which service, reading the Phase 134 per-service presence data (R418)."
    - "Presence reads are READ-ONLY: the dashboard never writes a presence doc — no setDoc/deleteDoc/heartbeat path exists in the rollup (dashboard viewer is not editing a specific service)."
    - "Stale editors are excluded via isPresenceStale(lastSeenMs, now, PRESENCE_STALE_TTL_MS) — never shown greyed-out."
    - "Reads are bounded to the ≤6 upcoming-services window; listeners are torn down on window/org change and unmount."
    - "The presence card is HIDDEN entirely when there are zero active editors; when hidden, the Unconfirmed volunteers card expands to lg:col-span-2 to fill the row."
  artifacts:
    - "src/utils/presenceRollup.ts — pure activePresenceRows filter/shape (reuses isPresenceStale)"
    - "src/utils/__tests__/presenceRollup.test.ts — staleness exclusion + shaping tests"
    - "src/composables/usePresenceRollup.ts — read-only ≤6-service presence fan-out with a nowMs staleness tick"
    - "src/views/DashboardView.vue — Currently editing card + hide-when-empty col-span interplay"
  key_links:
    - "presence subcollection onSnapshot (read-only) → activePresenceRows(isPresenceStale) → live editor list"
    - "presenceRollup emptiness → Unconfirmed card lg:col-span-2 expansion"
---

<objective>
Add the read-only editor-presence roll-up card (R418): aggregate the Phase 134 per-service presence docs
across the bounded upcoming-services window, filter out stale editors, and show who is currently editing which
service — hiding the card entirely when nobody is editing. The dashboard NEVER writes presence.

Purpose: give a planner ambient awareness of concurrent editing without adding write traffic or a new data model.
Output: a pure staleness-filter util (tested), a read-only presence fan-out composable, and the card.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-dashboard-overhaul/135-CONTEXT.md
@.planning/phases/135-dashboard-overhaul/135-UI-SPEC.md
@src/views/DashboardView.vue
@src/utils/presence.ts
@src/composables/useServicePresence.ts
</context>

<artifacts_produced>
- src/utils/presenceRollup.ts (new) — `activePresenceRows(rows, nowMs, ttlMs)` filtering stale docs via
  isPresenceStale and shaping to the rollup row.
- src/utils/__tests__/presenceRollup.test.ts (new).
- src/composables/usePresenceRollup.ts (new) — read-only ≤6-service presence fan-out with a nowMs tick.
- src/views/DashboardView.vue (modified) — Currently editing card + the hide-when-empty / col-span-2 interplay
  with the Unconfirmed volunteers card.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure active-presence filter/shape util (reuses isPresenceStale)</name>
  <files>src/utils/presenceRollup.ts, src/utils/__tests__/presenceRollup.test.ts</files>
  <read_first>
    - src/utils/presence.ts:14-39 (PresenceDoc {uid, displayName, lastSeen}, PRESENCE_STALE_TTL_MS,
      isPresenceStale(lastSeenMs, nowMs, ttlMs) — REUSE this, do not reimplement staleness)
    - src/composables/useServicePresence.ts:19-24,159-163 (PresenceRow {uid, displayName, lastSeen,
      lastSeenMs} + the presentViewers filter that excludes stale rows — mirror the same filter shape)
  </read_first>
  <behavior>
    - activePresenceRows(rows, nowMs, ttlMs) takes rows of `{ serviceId, serviceName, uid, displayName,
      lastSeenMs }` and returns only the non-stale rows (isPresenceStale === false), preserving input fields.
    - Test: a row with lastSeenMs within ttl of nowMs is kept.
    - Test: a row with lastSeenMs older than ttl is dropped.
    - Test: a row with lastSeenMs null/NaN is dropped (isPresenceStale treats it as stale).
    - Test: empty input → [].
  </behavior>
  <action>
    Create src/utils/presenceRollup.ts as a pure, store-free module. Define the rollup row interface
    (serviceId, serviceName, uid, displayName, lastSeenMs). Import isPresenceStale from '@/utils/presence' and
    filter rows where isPresenceStale(row.lastSeenMs, nowMs, ttlMs) is false — do NOT re-derive the TTL
    comparison. Write tests first (RED) covering every behavior bullet, then implement to green.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/presenceRollup.test.ts && npm run type-check</automated>
  </verify>
  <done>Staleness-exclusion tests pass; isPresenceStale is reused (not reimplemented); type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Read-only presence fan-out composable (≤6 services, no writes)</name>
  <files>src/composables/usePresenceRollup.ts</files>
  <read_first>
    - src/composables/useServicePresence.ts:31-166 (the WHOLE composable — copy the onSnapshot read shape at
      L112-128 and the nowMs NOW_TICK_MS staleness tick at L29,146-148; DO NOT copy writeHeartbeat/start's
      setDoc, heartbeatInterval, deleteDoc, or the visibilitychange write path — this variant is read-only)
    - src/utils/presence.ts:14-39 (PresenceDoc, PRESENCE_STALE_TTL_MS)
    - src/utils/presenceRollup.ts (Task 1)
    - 135-UI-SPEC.md:207-233 (Widget 3 data/read-only requirement/states)
  </read_first>
  <action>
    Create src/composables/usePresenceRollup.ts exporting usePresenceRollup(orgId, services) where services is a
    reactive getter for the bounded window (≤6 upcoming services). For each service, open a single READ-ONLY
    onSnapshot on collection(db,'organizations',orgId,'services',svcId,'presence'), mapping each doc to a row
    {serviceId, serviceName, uid, displayName, lastSeenMs} exactly as useServicePresence maps its snapshot
    (lastSeenMs via lastSeen.toMillis(), displayName fallback 'Someone'); console.error on listener error and
    treat that service's rows as empty (presence is a nice-to-have — never a visible error). Keep a nowMs ref
    driven by a ~10s interval (mirror NOW_TICK_MS) so staleness recomputes even without a new snapshot. Expose a
    computed `activeEditors` = activePresenceRows(all rows, nowMs, PRESENCE_STALE_TTL_MS). Manage lifecycle like
    useUnconfirmedVolunteers: per-serviceId unsubscribe handles, tear down listeners leaving the window (and all
    on orgId change) before opening new ones, clear the nowMs interval and all listeners onUnmounted. CRITICAL:
    this composable must contain NO setDoc, deleteDoc, serverTimestamp write, heartbeat interval, or
    visibilitychange write handler — it only reads. Bound the fan-out to the passed window (caller guarantees ≤6).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/composables/usePresenceRollup.ts','utf8'); if(/setDoc|deleteDoc|serverTimestamp|writeHeartbeat/.test(s)) throw new Error('presence rollup must be read-only'); if(!/activePresenceRows/.test(s)) throw new Error('pure filter not used'); if(!/onUnmounted/.test(s)) throw new Error('missing teardown'); console.log('ok')" && npm run type-check</automated>
  </verify>
  <done>Read-only presence fan-out over ≤6 services with a nowMs staleness tick; activeEditors excludes stale
  rows via the pure filter; listeners torn down on window/org change and unmount; NO write path exists;
  type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Currently editing card + hide-when-empty col-span interplay</name>
  <files>src/views/DashboardView.vue</files>
  <read_first>
    - src/views/DashboardView.vue (post-Plan-02 shape: attention-cards row grid grid-cols-1 lg:grid-cols-2
      gap-6 with the Unconfirmed volunteers card; the placeholder note for the presence card)
    - src/composables/usePresenceRollup.ts (Task 2)
    - 135-UI-SPEC.md:207-233 (Widget 3 card shell/header/row/live-dot a11y), :107-117 &amp; :224-229 (hide-when-empty →
      Unconfirmed card spans lg:col-span-2), :294-295 (live-pulse dot aria-hidden, text carries meaning)
  </read_first>
  <action>
    Call usePresenceRollup(() => authStore.orgId, () => upcomingWindow) where upcomingWindow is the ≤6
    upcoming-services window (reuse the same bounded window getter the feed/attention section already computes;
    presence is not restricted to Planned). Add the "Currently editing" card into the attention-cards row using
    the identical shell `rounded-lg border border-gray-800 bg-gray-900 p-5` and an uppercase `text-xs
    font-semibold tracking-widest text-gray-500` header. Render one row per active editor: a small green dot
    (`bg-green-500`, optionally animate-pulse, aria-hidden="true") then text "{displayName} is editing
    {serviceName}" as a router-link to `/services/{serviceId}` (the word "editing" carries meaning, not color
    alone). Apply the ScheduleServiceCard focus-ring utility and min-w-0 truncate. HIDE the entire card with
    v-if when activeEditors is empty (and on error treat as empty/hidden — no banner). When the presence card is
    hidden, the Unconfirmed volunteers card must span lg:col-span-2 (bind a conditional class on it so it fills
    the row rather than leaving a dead column). Do NOT introduce any presence write; this card only reads.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/views/DashboardView.vue','utf8'); if(/usePresenceRollup[\s\S]*setDoc/.test(s)||/presence[\s\S]*serverTimestamp/.test(s)) throw new Error('dashboard must not write presence'); if(!/usePresenceRollup/.test(s)) throw new Error('presence card not wired'); if(!/lg:col-span-2/.test(s)) throw new Error('col-span expansion missing'); console.log('ok')" && npm run type-check && npx vitest run src/utils/__tests__/presenceRollup.test.ts</automated>
    <human-check>Open a service in another tab/browser as an editor: the dashboard's Currently editing card appears with a live green dot and "{name} is editing {service}"; closing that editor removes the row within ~60s; when nobody is editing the card is absent and the Unconfirmed card spans the full row.</human-check>
  </verify>
  <done>Currently editing card renders active (non-stale) editors read-only, hides entirely when empty, and the
  Unconfirmed card expands to lg:col-span-2 when it is hidden; the dashboard writes no presence; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authStore.orgId → presence reads | The dashboard reads other editors' presence docs; reads must stay org-scoped and must never write a presence doc for the (non-editing) dashboard viewer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-135-05 | Tampering | usePresenceRollup / DashboardView | medium | mitigate | Read-only composable — no setDoc/deleteDoc/serverTimestamp/heartbeat/visibilitychange write path (grep-gated in Task 2 & Task 3 verify); the dashboard viewer never fabricates a "currently editing" presence doc. |
| T-135-06 | Information Disclosure | presence fan-out | low | mitigate | Listeners built on organizations/{authStore.orgId}/services/{id}/presence under the R423 org-scoped get/list-split rules (v2.8 SEC-S-01 replay guard); only displayName (already denormalized, non-PII) is shown. |
| T-135-07 | Denial of Service (cost) | bounded presence fan-out | low | mitigate | Window capped to ≤6 services; listeners + nowMs interval torn down on window/org change and unmount. |

No package installs this plan — no supply-chain threat.
</threat_model>

<verification>
- `npx vitest run` shows only the known baseline failure (src/storage.rules.test.ts); the new filter tests pass.
- `npm run type-check` clean.
- usePresenceRollup and DashboardView contain no presence write path (grep) — dashboard never writes presence.
</verification>

<success_criteria>
Editor-presence roll-up reads Phase 134 presence read-only, excludes stale editors via isPresenceStale, is
bounded to ≤6 services, hides entirely when empty (Unconfirmed card expands to fill the row), and adds no
presence writes (R418). Type-check and unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/135-dashboard-overhaul/135-03-SUMMARY.md` when done.
</output>
