---
phase: 133-volunteer-responsibility-confirmation
plan: 03
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.confirmations.test.ts
autonomous: true
requirements: [R411]
must_haves:
  truths:
    - "The Roles tab shows each (role, person) assignment's confirmation status: Confirmed / Unconfirmed / Needs reconfirmation"
    - "Status is read LIVE via onSnapshot on the confirmations subcollection, never a one-time getDoc/getDocs"
    - "A volunteer confirming (or a relock flipping to needsReconfirmation) updates the planner's chip without a reload"
    - "The listener tears down on unmount and re-subscribes when the service/org changes"
  artifacts:
    - src/views/__tests__/ServiceEditorView.confirmations.test.ts
  key_links:
    - "onSnapshot(services/{id}/confirmations) -> Map<key, ConfirmationStatus>"
    - "confirmationStatusFor(roleId, personId) -> chip, via person.email.toLowerCase() + confirmationKey"
---

<objective>
Give the planner live per-assignment confirmation status on the Roles tab of ServiceEditorView
(R411, per D-R411). A live `onSnapshot` on the service's confirmations subcollection drives a
status chip next to each person in each role — Confirmed, Unconfirmed, or Needs reconfirmation —
that self-heals as volunteers confirm and as relock reconciliation (Plan 04) invalidates.

Purpose: R411 explicitly mandates a live read (not a one-time fetch) so a planner never acts on a
stale checkmark (RESEARCH Pitfall 1). This is the planner's window into confirmation state and the
basis for the unconfirmed-only nudge (Plan 05).
Output: a confirmations subscription + status chips in the ServiceEditorView Roles tab.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-volunteer-responsibility-confirmation/133-RESEARCH.md
@.planning/phases/133-volunteer-responsibility-confirmation/133-01-SUMMARY.md

@src/views/ServiceEditorView.vue
@src/utils/confirmations.ts
</context>

<artifacts>
## Artifacts this phase produces (Plan 03)

| Artifact | Kind | Notes |
|----------|------|-------|
| confirmations `onSnapshot` in ServiceEditorView | edit | live `Map<key, ConfirmationStatus>` keyed by confirmationKey(roleId, emailLower) |
| `confirmationStatusFor(roleId, personId)` helper + Roles tab chips | edit | one chip per person per role in the effectiveNames area |
| `src/views/__tests__/ServiceEditorView.confirmations.test.ts` | new test | live-status render + no getDocs in the display read path |
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live confirmations subscription in ServiceEditorView</name>
  <files>src/views/ServiceEditorView.vue</files>
  <read_first>
    - src/views/ServiceEditorView.vue:4143-4256 (Roles tab: resolvedRoleAssignments, effectiveNames, onToggleOverridePerson) and 1404-1488 (Roles tab template)
    - src/utils/confirmations.ts (ConfirmationStatus, ConfirmationDoc, confirmationKey — from Plan 01)
    - src/stores/mySchedule.ts (onSnapshot subscribe/unsubscribe lifecycle convention to mirror)
  </read_first>
  <behavior>
    - When the editor has a loaded service and authStore.orgId, an onSnapshot on organizations/{orgId}/services/{serviceId}/confirmations maintains a reactive Map<key, ConfirmationStatus>
    - confirmationStatusFor(roleId, personId): resolves the person's email (rosterStore.people), lowercases it, builds confirmationKey(roleId, emailLower), returns 'confirmed' | 'needsReconfirmation' | 'unconfirmed' (unconfirmed = no doc, or person has empty email)
    - The listener re-subscribes when the service id or orgId changes and unsubscribes on unmount
    - NO getDoc/getDocs anywhere in this display read path (RESEARCH Pitfall 1)
  </behavior>
  <action>
Add a reactive `confirmationStatuses = ref<Map<string, ConfirmationStatus>>(new Map())` and an `onSnapshot` subscription in ServiceEditorView, scoped to `organizations/{authStore.orgId}/services/{localService.value.id}/confirmations`. Mirror mySchedule.ts's subscribe/unsubscribe discipline: tear down any prior listener first, re-subscribe via a `watch` on `[() => authStore.orgId, () => localService.value?.id]` (immediate), unsubscribe in `onUnmounted`, and pair the error callback with a `console.error` (the mySchedule convention). The snapshot handler maps each doc to `confirmationKey(doc.roleId, doc.emailLower) -> doc.status`. Add a `confirmationStatusFor(roleId: string, personId: string): ConfirmationStatus | 'unconfirmed'` function: find `rosterStore.people` for personId; if no person or empty email return `'unconfirmed'`; else return `confirmationStatuses.value.get(confirmationKey(roleId, person.email.toLowerCase())) ?? 'unconfirmed'`. Do NOT use getDoc/getDocs in this path — only onSnapshot (enforce in review; Pitfall 1). This subscription is read-only and independent of the volunteer confirm bar (Plan 02) — different file, no shared module edited.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/views/ServiceEditorView.vue','utf8').split(/\r?\n/).filter(l=>!/^\s*\/\//.test(l)&&!/^\s*\*/.test(l)).join('\n');if(!/confirmationStatusFor/.test(s))throw new Error('confirmationStatusFor missing');if(!/onSnapshot[\s\S]{0,200}confirmations/.test(s))throw new Error('confirmations onSnapshot missing');console.log('confirmations subscription present')"</automated>
  </verify>
  <done>ServiceEditorView holds a live confirmationStatuses map fed by an onSnapshot on the confirmations subcollection, with a confirmationStatusFor(roleId, personId) resolver; the listener re-subscribes on service/org change and unsubscribes on unmount; no getDoc/getDocs in the display path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-assignment status chips in the Roles tab</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.confirmations.test.ts</files>
  <read_first>
    - src/views/ServiceEditorView.vue:1427-1486 (Roles tab card template — the effectiveNames join line 1455-1457 to restructure)
    - src/components/ScheduleServiceCard.vue (chip styling language, dark gray-950)
  </read_first>
  <behavior>
    - Each role card renders its assigned people such that every person carries a confirmation chip reflecting confirmationStatusFor(roleId, personId)
    - Confirmed -> a positive/green chip; Unconfirmed -> a neutral gray chip; Needs reconfirmation -> an amber chip
    - A live snapshot change updates the chip without reload; the test drives a fake onSnapshot emission and asserts the chip text/state changes
  </behavior>
  <action>
Restructure the Roles-tab effective-names line (currently `effectiveNames(assignment).join(', ')`, ServiceEditorView.vue:1455-1457) to render each assigned person individually with a status chip next to the name, driven by `confirmationStatusFor(assignment.roleId, personId)`. Iterate `assignment.effectivePersonIds` (resolve the display name as effectiveNames already does — person name fallback to id). Keep the "Nobody scheduled" fallback when there are no effective people. Use the app's dark gray-950 chip language (mirror ScheduleServiceCard's role chips) — Claude's discretion on exact colors/labels per D-Claude's-Discretion; suggested labels Confirmed / Unconfirmed / Needs reconfirmation with green / gray / amber accents. Add data-testids (`role-confirm-chip-{roleId}-{personId}`) for the test. This is a display-only change — do NOT alter onToggleOverridePerson / setRoleOverride or any write path.

Write `src/views/__tests__/ServiceEditorView.confirmations.test.ts` (or reuse an existing ServiceEditorView test file if the project keeps one) with a stubbed Firestore onSnapshot: mount the Roles tab as an editor, emit a snapshot with one confirmed doc for (roleId, personId) and assert that person's chip shows Confirmed while an unconfirmed peer shows Unconfirmed; then emit a needsReconfirmation doc and assert the chip flips — proving live (onSnapshot) behavior without reload.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.confirmations.test.ts</automated>
  </verify>
  <done>Roles tab shows a live per-person confirmation chip per role (Confirmed/Unconfirmed/Needs reconfirmation); a snapshot emission changes a chip in-place; the confirmations test is green and no write path was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor -> confirmations read | an org member reading confirmation status; already granted by the Plan 01 rule's isOrgMember get/list arm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-11 | Information Disclosure | confirmations read scope | low | mitigate | onSnapshot is scoped to organizations/{authStore.orgId}/services/{id}/confirmations — orgId from the authenticated session, id from the loaded service; the Plan 01 isOrgMember arm confines reads to the member's own org |
| T-133-12 | Tampering | stale-status display (acting on a lie) | medium | mitigate | live onSnapshot only; NO getDoc/getDocs in the display path (Pitfall 1), so a reassignment or volunteer confirm reflects without reload |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.confirmations.test.ts` green
- `npm run type-check` clean
- Grep guard: no getDoc/getDocs added to the confirmation display path (only onSnapshot)
- Manual (batched UAT): a volunteer confirms in one browser; the planner's Roles-tab chip flips to Confirmed without reload
</verification>

<success_criteria>
- The Roles tab shows live Confirmed/Unconfirmed/Needs reconfirmation per assignment
- Status is onSnapshot-driven and self-heals across confirm + reassignment
- No write path modified
</success_criteria>

<output>
Create `.planning/phases/133-volunteer-responsibility-confirmation/133-03-SUMMARY.md` when done
</output>
