---
phase: 16.1-song-list-tags-columns-customization
plan: 07
type: execute
wave: 3
depends_on: [16.1-01, 16.1-02, 16.1-03, 16.1-04]
files_modified:
  - src/components/SongSlotPicker.vue
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
  - src/stores/songs.ts
autonomous: true
requirements: [D-02, D-04, D-11, D-16]

must_haves:
  truths:
    - "The service-plan picker renders song pills from the folded tags/themes (no separate team pills) and keeps its own compact layout with no column cog (D-11)"
    - "When VW mode is OFF, the picker and service editor hide their VW/Category badges (D-16)"
    - "Both the Songs-management list search (songs.ts filteredSongs) and the picker search pass authStore.vwModeEnabled to songMatchesQuery so a `type:` query returns nothing when VW mode is off (D-16)"
    - "The Orchestra AI-library filter reads the folded song.tags instead of song.teamTags (D-04 ripple)"
    - "Service-level team scheduling (which teams play a Sunday) is unchanged (D-02)"
  artifacts:
    - path: "src/components/SongSlotPicker.vue"
      provides: "teamTags pill rows removed; SongBadge gated by vwModeEnabled; tag union over themes ∪ tags; search threads vwModeEnabled into songMatchesQuery"
      contains: "vwModeEnabled"
    - path: "src/views/ServiceEditorView.vue"
      provides: "SongBadge gated by vwModeEnabled; orchestra AI filter over song.tags"
      contains: "vwModeEnabled"
    - path: "src/stores/songs.ts"
      provides: "filteredSongs threads authStore.vwModeEnabled into songMatchesQuery (Songs-list type: gate)"
      contains: "vwModeEnabled"
  key_links:
    - from: "src/views/ServiceEditorView.vue suggestAllSongs / AI cache"
      to: "song.tags"
      via: "orchestra library filter reads folded tags"
      pattern: "tags"
    - from: "src/components/SongSlotPicker.vue SongBadge"
      to: "authStore.vwModeEnabled"
      via: "v-if gate on each badge occurrence"
      pattern: "vwModeEnabled"
    - from: "src/stores/songs.ts filteredSongs + SongSlotPicker.vue tagFilteredSongs"
      to: "authStore.vwModeEnabled"
      via: "songMatchesQuery(song, query, authStore.vwModeEnabled) gates the type: prefix"
      pattern: "vwModeEnabled"
---

<objective>
Repoint the service-plan picker and service editor to the folded tag model, gate their VW badges, and thread the VW-mode flag into every `songMatchesQuery` caller so the `type:` search prefix hides app-wide.

Purpose: The team-decouple ripple (D-04) reaches the picker and editor: three `song.teamTags` pill rows in SongSlotPicker fold away, the Orchestra AI-library filter in ServiceEditorView reads `song.tags`, and every SongBadge occurrence gates on `vwModeEnabled` (D-16). D-11 keeps the picker's compact layout untouched (no column cog here). D-02 is a guardrail: the service's own team scheduling (AVAILABLE_TEAMS selection) is not changed. D-16 also requires the `type:` search prefix to hide when VW is off; plan 03 added the `vwModeEnabled` param to `songMatchesQuery`, so this plan wires it at both call sites — the Songs-management store search (`songs.ts` filteredSongs) and the picker search — since this is the wave that (a) depends on plan 03 (param exists) and plan 02 (vwModeEnabled) and (b) runs after plan 04 has finished its own songs.ts edits (Wave 2). The store wiring is one-line glue over plan 03's unit-tested gate, so it is type-check-verified here; the gate logic itself is unit-tested in plan 03 and the integrated behavior is confirmed in the plan 08 cross-surface checkpoint.

Output: repointed picker + editor with VW-gated badges, VW-gated Songs-list and picker search, and updated ServiceEditorView test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-CONTEXT.md
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-RESEARCH.md

<interfaces>
From src/components/SongSlotPicker.vue:
  - Three pill rows render `song.teamTags` variant="team" (lines 100, 141, 175) alongside themes+tags —
    remove the teamTags loops, keep themes + tags pills.
  - SongBadge occurrences at lines 105, 146, 180 — gate each with vwModeEnabled.
  - availableTags (252-260) and tagFilteredSongs (266-287) scan s.teamTags — drop the teamTags scans
    (themes ∪ tags only).
  - tagFilteredSongs search filter at line 303: `songMatchesQuery(s, q)` — after plan 03 this accepts a
    third `vwModeEnabled` arg; pass `authStore.vwModeEnabled` (the same store instance used to gate the
    badges) so the picker's `type:` search prefix hides when VW is off (D-16).
From src/stores/songs.ts:
  - `useAuthStore` is already imported (line 18). filteredSongs (line 39-...) calls
    `songMatchesQuery(song, searchQuery.value)` at line 43. Resolve the auth store once at the top of the
    computed and pass `authStore.vwModeEnabled` as the third arg (plan 03's VW-gate). This is the only
    change to songs.ts in this plan — do NOT touch the columnVisibility surface (owned by plan 04, which
    runs in the earlier wave). Plans 01 (Wave 1) and 04 (Wave 2) own the earlier songs.ts edits; this
    Wave-3 edit lands after both.
From src/views/ServiceEditorView.vue:
  - SongBadge at lines 500-503 (assigned-song category badge) — gate with vwModeEnabled.
  - Orchestra AI-library filter at lines 1452-1455 and 1562-1565: `s.teamTags.includes('Orchestra')`
    → `s.tags.includes('Orchestra')`. AVAILABLE_TEAMS (line 898) and localService.teams are the service's
    own team scheduling — LEAVE unchanged (D-02).
From src/stores/auth.ts (plan 02): vwModeEnabled.
Per RESEARCH Open Question 2: D-16 service-editor scope is satisfied by gating the SongBadge occurrence(s);
  do NOT alter the hardcoded buildSlots('1-2-2-3') template (D-18 forbids remodeling progression logic).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SongSlotPicker + Songs-list store — fold team pills, gate badges, VW-gate both search callers</name>
  <read_first>
    - src/components/SongSlotPicker.vue (pill rows 99-103/140-144/174-179; SongBadge 105/146/180; availableTags 252-260; tagFilteredSongs 266-287; search call line 303)
    - src/stores/songs.ts (filteredSongs 39-...; songMatchesQuery call line 43; useAuthStore import line 18)
    - src/stores/auth.ts (vwModeEnabled)
  </read_first>
  <action>
    In SongSlotPicker.vue: import useAuthStore. Remove the three `TeamTagPill v-for="t in ...teamTags" variant="team"` loops (keep the themes and tags pill loops). Wrap each of the three SongBadge occurrences in `v-if="authStore.vwModeEnabled"` (D-16). In availableTags and tagFilteredSongs, remove the `s.teamTags` scan lines so the union is `themes ∪ tags` only. Change the search call at line 303 from `songMatchesQuery(s, q)` to `songMatchesQuery(s, q, authStore.vwModeEnabled)`. Do not add a column cog or change the compact layout (D-11). Update the code comment that says "teamTags ∪ themes ∪ tags". In src/stores/songs.ts: in the filteredSongs computed, resolve the auth store once (e.g. `const authStore = useAuthStore()`) and change the search line at 43 to `songMatchesQuery(song, searchQuery.value, authStore.vwModeEnabled)` — no other change to that file. Both edits hide the `type:` prefix when VW is off (D-16); the gate logic is already unit-tested in plan 03.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <human-check>Open a service plan's song picker: confirm no gray team pills, themes and tags pills still show, with VW OFF the type badges are hidden, and typing `type:1` in BOTH the picker search and the Songs-list search returns no results while VW is off (and works again with VW on).</human-check>
  </verify>
  <acceptance_criteria>
    - SongSlotPicker.vue has no `song.teamTags` references remaining and all three SongBadge occurrences are gated by vwModeEnabled.
    - The picker tagFilteredSongs search call AND the songs.ts filteredSongs search call both pass authStore.vwModeEnabled as the third songMatchesQuery arg.
    - songs.ts change is limited to the filteredSongs search line (no columnVisibility edits).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Picker folds team pills, gates badges, and both Songs-list and picker searches hide the type: prefix when VW is off.</done>
</task>

<task type="auto">
  <name>Task 2: ServiceEditorView — gate badge + orchestra AI filter over tags</name>
  <read_first>
    - src/views/ServiceEditorView.vue (SongBadge 500-503; orchestra filter 1452-1455 and 1562-1565)
    - src/views/__tests__/ServiceEditorView.test.ts (existing teamTags references)
    - src/stores/auth.ts (vwModeEnabled)
  </read_first>
  <action>
    Add `&& authStore.vwModeEnabled` to the assigned-song SongBadge `v-if` at lines 500-503 (or wrap it) so the category badge hides when VW is off (D-16). Change both Orchestra AI-library filters from `s.teamTags.includes('Orchestra')` to `s.tags.includes('Orchestra')` (folded tags, D-04). Leave AVAILABLE_TEAMS and the service's teams selection unchanged (D-02). Update ServiceEditorView.test.ts fixtures/assertions that reference teamTags to use tags (or remove the teamTags-specific assertions if the orchestra-filter test now keys on tags). Confirm authStore is available in this view (import useAuthStore if not already).
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
    <human-check>With an Orchestra service, confirm AI suggestions still bias to orchestra-tagged songs; with VW OFF confirm the assigned-song category badge is hidden.</human-check>
  </verify>
  <acceptance_criteria>
    - ServiceEditorView.vue orchestra filters read s.tags (no s.teamTags remaining in the view).
    - The assigned-song SongBadge is gated by vwModeEnabled.
    - `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` exits 0 and `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Service editor gates its badge and biases AI by folded tags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service editor → AI suggestion input | song fields shape the AI library payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-13 | Tampering | orchestra AI filter | accept | Advisory suggestion biasing only; reads client-side tags, no persistence or auth boundary. |
| T-16.1-14 | Spoofing | VW badge gating | accept | Cosmetic hide of retained vwTypes data; no auth impact. |

Severity LOW: display gating + read-path repoint; service team scheduling untouched.
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` exits 0.
- `npm run type-check` exits 0.
</verification>

<success_criteria>
- Picker and editor use folded tags, gate VW badges, hide the type: search prefix (Songs list + picker) when VW is off, and leave service team scheduling intact.
</success_criteria>

<output>
Create `.planning/phases/16.1-song-list-tags-columns-customization/16.1-07-SUMMARY.md` when done.
</output>
</content>
