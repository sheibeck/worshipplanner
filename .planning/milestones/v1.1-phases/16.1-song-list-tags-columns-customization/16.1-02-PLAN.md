---
phase: 16.1-song-list-tags-columns-customization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/auth.ts
  - src/views/SettingsView.vue
  - src/stores/__tests__/auth.test.ts
autonomous: true
requirements: [D-15, D-16]

must_haves:
  truths:
    - "A new org with no vwModeEnabled field defaults to VW mode ON (D-15)"
    - "Toggling VW mode in Settings persists to the organizations/{orgId} doc AND updates the store immediately in the same session (D-15, Pitfall 2)"
    - "authStore.vwModeEnabled is the single source of truth every VW surface reads to gate indicators (D-16)"
  artifacts:
    - path: "src/stores/auth.ts"
      provides: "vwModeEnabled ref (default true), loadOrgContext read, logout/no-org reset, export"
      contains: "vwModeEnabled"
    - path: "src/views/SettingsView.vue"
      provides: "Vertical Worship settings section with toggle + updateDoc + store mirror-write"
      contains: "vwModeEnabled"
  key_links:
    - from: "src/views/SettingsView.vue toggle"
      to: "organizations/{orgId} doc + authStore.vwModeEnabled"
      via: "updateDoc then immediate ref reassignment (mirror-write)"
      pattern: "vwModeEnabled"
---

<objective>
Introduce the church-level VW-mode flag as the single source of truth that later waves consume to hide VW indicators app-wide, and expose a Settings toggle to control it.

Purpose: D-15 makes the Vertical Worship 1-2-3 methodology an optional, church-level (not per-user) package, default ON. It must live on the `organizations/{orgId}` Firestore doc alongside `orgName`/`orgSlug`/`pcAppId`, read once via `getDoc` in `loadOrgContext`, and mirror-written into the Pinia `auth` store on save (the org doc is NOT live — Pitfall 2). Every VW surface in later plans gates on `authStore.vwModeEnabled` (D-16), so this plan is a Wave-1 dependency for the UI wave.

Output: `vwModeEnabled` store state + a Settings section that reads/writes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-CONTEXT.md
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-RESEARCH.md

<interfaces>
From src/stores/auth.ts:
  - loadOrgContext(uid) (lines 76-131): getDoc(organizations/{orgId}); mirrors orgData.name/slug/
    pcAppId/pcSecret into refs. The exact spot to add `vwModeEnabled.value = (orgData.vwModeEnabled as boolean) ?? true`.
  - onAuthStateChanged else-branch (lines 139-146) and logout() (lines 280-290): reset refs on sign-out.
  - Existing exported refs pattern (orgSlug, pcAppId) to mirror for vwModeEnabled.

From src/views/SettingsView.vue:
  - onSaveSlug() (lines 284-321): updateDoc(organizations/{orgId}, { slug }) then authStore.orgSlug = claimed.
    This is the mirror-write template for the VW toggle.
  - Section markup pattern: <div class="rounded-lg bg-gray-900 border border-gray-800 p-4"> with an
    <h2 class="text-sm font-semibold text-gray-300 mb-3"> heading (Organization / Planning Center sections).
  - imports already include { doc, updateDoc } from firebase/firestore and db, authStore.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add vwModeEnabled church-level flag to the auth store</name>
  <read_first>
    - src/stores/auth.ts (refs declaration lines 28-41; loadOrgContext 76-131; logout 280-290; onAuthStateChanged 134-148; return block 300-321)
    - src/stores/__tests__/auth.test.ts (existing describe blocks; note it has no loadOrgContext coverage today — Wave 0 gap)
  </read_first>
  <behavior>
    - After loadOrgContext for an org doc WITHOUT a vwModeEnabled field, authStore.vwModeEnabled === true.
    - After loadOrgContext for an org doc with vwModeEnabled === false, authStore.vwModeEnabled === false.
    - After logout (and the no-org branch), vwModeEnabled resets to true (default).
  </behavior>
  <action>
    Add a `const vwModeEnabled = ref(true)` alongside orgSlug. In loadOrgContext's org-doc read block, set `vwModeEnabled.value = (orgData.vwModeEnabled as boolean) ?? true` (missing field == ON per D-15). In the no-org early-return branch, the onAuthStateChanged sign-out else-branch, and logout(), reset `vwModeEnabled.value = true`. Export `vwModeEnabled` from the store return object. Do NOT use onSnapshot — mirror the one-time getDoc pattern already used for pcAppId/orgSlug.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/auth.ts exports vwModeEnabled and reads it in loadOrgContext with a `?? true` default.
    - New auth.test.ts cases cover: default-true-on-missing-field, explicit-false, and logout reset.
    - `npx vitest run src/stores/__tests__/auth.test.ts` exits 0.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>vwModeEnabled is the exported source-of-truth ref with test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Vertical Worship toggle to Settings</name>
  <read_first>
    - src/views/SettingsView.vue (Organization + Planning Center section markup; onSaveSlug mirror-write lines 284-321; script imports)
    - src/stores/auth.ts (vwModeEnabled from Task 1)
  </read_first>
  <action>
    Add a new "Vertical Worship" settings section (same `rounded-lg bg-gray-900 border border-gray-800 p-4` card + `h2` heading pattern) after the Planning Center section. Render a labeled on/off toggle (checkbox or switch) bound to a local ref seeded from `authStore.vwModeEnabled`. On change, call `updateDoc(doc(db, 'organizations', authStore.orgId), { vwModeEnabled: newValue })`, then immediately set `authStore.vwModeEnabled = newValue` (mirror-write per Pitfall 2), with a brief "Saved!" feedback like onSaveSlug. Include one line of helper copy: "When off, all Vertical Worship 1-2-3 indicators are hidden across the app. Your song categories are kept and reappear when you turn it back on." Use static Tailwind class strings only (Pitfall 1 — no interpolated class names for the toggle states). Guard the write behind authStore.isEditor (viewers see the state read-only or disabled).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <human-check>In Settings, toggle Vertical Worship off then on; confirm "Saved!" feedback and that the state survives a page reload.</human-check>
  </verify>
  <acceptance_criteria>
    - SettingsView.vue contains a Vertical Worship section whose toggle calls updateDoc with { vwModeEnabled } and mirror-writes authStore.vwModeEnabled.
    - No interpolated Tailwind class strings introduced (grep for backtick class bindings is clean).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Settings exposes a persisted, editor-gated VW-mode toggle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → Firestore org doc | writes vwModeEnabled to organizations/{orgId} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-03 | Elevation of Privilege | vwModeEnabled write | mitigate | Reuses the existing isOrgEditor Firestore rule on organizations/{orgId} (firestore.rules lines 27-31) — no rule change, no new unauthenticated surface. UI additionally gates the control behind authStore.isEditor. |
| T-16.1-04 | Tampering | boolean flag value | accept | A tampered boolean only toggles UI visibility of retained data; no data deletion, no auth impact. |

Severity LOW: one boolean field on an already org-editor-gated doc. Confirmed the write path reuses the isOrgEditor gate — no new attack surface.
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/auth.test.ts` exits 0.
- `npm run type-check` exits 0.
- Manual: toggle persists across reload.
</verification>

<success_criteria>
- vwModeEnabled defaults ON, persists at church level, and is exported as the single VW source of truth.
</success_criteria>

<output>
Create `.planning/phases/16.1-song-list-tags-columns-customization/16.1-02-SUMMARY.md` when done.
</output>
