---
phase: 16.1-song-list-tags-columns-customization
plan: 08
type: execute
wave: 4
depends_on: [16.1-01, 16.1-03, 16.1-05, 16.1-06, 16.1-07]
files_modified:
  - src/types/song.ts
  - src/utils/csvImport.ts
  - src/utils/__tests__/planningCenterApi.test.ts
  - src/utils/__tests__/planningCenterExport.test.ts
  - src/components/__tests__/ServicePrintLayout.test.ts
  - src/components/__tests__/CsvImportModal.test.ts
autonomous: false
requirements: [D-01]

must_haves:
  truths:
    - "Song no longer declares a teamTags field (D-01 — field removed)"
    - "vue-tsc / npm run type-check is clean across the whole repo with no remaining Song.teamTags references (D-01)"
    - "The full unit suite passes"
  artifacts:
    - path: "src/types/song.ts"
      provides: "Song interface with teamTags removed (Arrangement.teamTags retained by decision)"
      contains: "removedThemes"
  key_links:
    - from: "src/types/song.ts"
      to: "all Song consumers"
      via: "compiler-enforced completeness — vue-tsc surfaces any residual teamTags reference"
      pattern: "teamTags"
---

<objective>
Remove `Song.teamTags` from the type and clear every residual reference the compiler surfaces, using vue-tsc as the exhaustive blast-radius completeness check.

Purpose: Plans 01/03/05/06/07 repointed every live consumer to the folded `tags` set while `Song.teamTags` stayed alive so intermediate waves kept a green build. This final wave deletes the field (completing D-01), lets `npm run type-check` enumerate any remaining references (test fixtures, the dead-code CSV import path), and clears them. `Arrangement.teamTags` is intentionally retained (RESEARCH Open Question 1 — dead-weight from PC import, reachable only via the dead CsvImportModal path; removing it is out of scope and left for a future cleanup).

Output: teamTags removed from Song, all residual references cleared, full suite + type-check green, and a final cross-surface VW-off human verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-CONTEXT.md
@.planning/phases/16.1-song-list-tags-columns-customization/16.1-RESEARCH.md

<interfaces>
From src/types/song.ts: remove `teamTags: string[]` from Song (keep removedThemes added in plan 01;
  keep Arrangement.teamTags).
Residual teamTags sites expected after Waves 1-3 (confirm via type-check, do not assume this list is
  exhaustive): src/utils/csvImport.ts (maps a Song teamTags), and test fixtures in
  planningCenterApi.test.ts, planningCenterExport.test.ts, ServicePrintLayout.test.ts,
  CsvImportModal.test.ts that construct Song objects with teamTags.
RESEARCH Wave 0 Gap: run vue-tsc immediately after the field removal to get a compiler-generated
  exhaustive reference list — stronger than grep.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Song.teamTags and clear compiler-surfaced references</name>
  <read_first>
    - src/types/song.ts (Song interface)
    - src/utils/csvImport.ts (Song construction / teamTags mapping)
  </read_first>
  <action>
    Delete `teamTags: string[]` from the Song interface in song.ts. Run `npm run type-check` and treat every resulting error as the worklist. For csvImport.ts, drop the Song-level teamTags assignment (fold any CSV team-tag values into `tags` if the path constructs a Song, or remove if it targets arrangements only — match existing behavior; this is dead code per STATE Phase 09 note, so the minimal fix is to stop referencing Song.teamTags). Do NOT remove Arrangement.teamTags. For each remaining reference the compiler flags in non-test source, repoint to `tags` or remove as appropriate. Re-run type-check until clean.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - src/types/song.ts Song interface has no teamTags field.
    - `npm run type-check` is the authoritative completeness gate and exits 0 (it enumerates every removed-field reference across the repo).
    - Sanity sweep: `grep -rniI "\.teamTags" src --include=*.ts --include=*.vue | grep -vi arrangement | grep -vi "\.test\."` returns only known arrangement-level accesses (e.g. `a.teamTags` in ArrangementAccordion.vue, the csvImport arrangement path). Any Song-level `.teamTags` hit is a failure — but treat type-check, not this case-insensitive grep, as the final arbiter (a bare `a.teamTags` on an arrangement variable carries no "arrangement" token on the line, so verify remaining hits by file/context).
  </acceptance_criteria>
  <done>Song.teamTags removed and all non-test source references cleared with clean type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Fix residual test fixtures and green the full suite</name>
  <read_first>
    - src/utils/__tests__/planningCenterApi.test.ts, src/utils/__tests__/planningCenterExport.test.ts
    - src/components/__tests__/ServicePrintLayout.test.ts, src/components/__tests__/CsvImportModal.test.ts
  </read_first>
  <action>
    In each test file the compiler flags, remove `teamTags` from Song fixture objects (add `removedThemes: []` where a full Song literal is required and the type now demands it). Do not change assertions unrelated to teamTags. If any fixture used teamTags to assert team-based behavior, repoint the assertion to `tags`. Run the full unit suite and fix any remaining fallout.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - No test file constructs a Song with a teamTags field.
    - `npm run test:unit` exits 0 (full suite green).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Full unit suite and type-check are green after the field removal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Completed team-tag fold (Song.teamTags removed) plus the full 16.1 feature set: folded tags, Themes column + cog, VW-mode toggle, and the 1-2-3 explainer.</what-built>
  <how-to-verify>
    1. Songs page: confirm one flat Tags set (no gray team pills anywhere), a separate Themes column, and the column cog persists choices across reload.
    2. In Settings toggle Vertical Worship OFF. Sweep the app and confirm ALL VW indicators are hidden with no data loss: the Songs Category column + badge, the Songs VW-type filter dropdown, the Add/Edit-Song VW Category selector, the service-plan picker badges, and the service editor's assigned-song badge. Also confirm the `type:` search prefix is now inert while VW is off — typing `type:1` in the Songs search (and in the picker search) returns NO songs (the prefix is hidden per D-16), rather than filtering by category. Toggle VW back ON and confirm every indicator returns AND `type:1` again matches Type-1 songs.
    3. Import from Planning Center (or re-run an import) and confirm previously team-tagged songs now show those tags as ordinary Tags, and user-added tags/themes are preserved.
    4. Confirm the explainer copy reads the 1-2-3 (1=Call to Worship, 2=Intimate, 3=Ascription) and does not introduce a 1-5 scale.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any gaps to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type system → whole repo | field removal is compiler-enforced across all consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-15 | Tampering | stale Firestore teamTags field | accept | Removing the type field does not delete the physical Firestore field on existing docs; it is harmless clutter, ignored by all reads, and naturally cleared on next edit/import. No integrity or auth impact (documented in plan 01). |
| T-16.1-SC | Tampering | package installs | n/a | Zero packages installed this phase. |

Severity LOW: pure type/reference cleanup with a compiler-enforced completeness gate.
</threat_model>

<verification>
- `npm run type-check` exits 0 (exhaustive blast-radius gate).
- `npm run test:unit` full suite exits 0.
- Final cross-surface VW-off human verification approved.
</verification>

<success_criteria>
- Song.teamTags is gone, the repo type-checks clean, the full suite is green, and the whole 16.1 feature set verifies end-to-end.
</success_criteria>

<output>
Create `.planning/phases/16.1-song-list-tags-columns-customization/16.1-08-SUMMARY.md` when done.
</output>
</content>
