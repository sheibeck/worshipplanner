---
phase: 17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi
plan: 05
type: execute
wave: 3
depends_on:
  - 17-02
  - 17-03
files_modified:
  - src/views/ShareView.vue
autonomous: false
requirements: [CR-04, CR-05]
must_haves:
  truths:
    - "ShareView resolves BOTH the opaque /share/:token path and the memorable /:slug/service-:date path, reading the serviceShares/{slug}__service-{date} doc for the latter"
    - "ShareView renders a 'Who's Serving' section from serviceSnapshot.roleAssignments (roleName + person names)"
    - "an unauthenticated visitor sees who is serving without any roster/org/auth store access — only the denormalized snapshot doc is read"
    - "a missing/nonexistent share doc renders the existing not-found state, not an error"
  artifacts:
    - src/views/ShareView.vue
  key_links:
    - "onMounted picks getDoc(shareTokens/token) when :token present, else getDoc(serviceShares/{slug}__service-{date}) from route params slug + date"
    - "the Who's Serving section reads serviceSnapshot.roleAssignments written by 17-03's createShareToken"
---

<objective>
Complete the public per-service share: make `ShareView.vue` serve both the existing opaque token route and the new memorable `/:slug/service-:date` route, and render a "Who's Serving" section from the names-only role snapshot — with zero roster/org/auth access on the public route.

Purpose: This is the ONLY way a non-editor sees who is serving (in-app roles stay editor-only per the Phase 16.2 removal). It mirrors the Phase 16 quarter share's dual-path public read exactly, so the unauthenticated route touches only the denormalized snapshot doc.
Output: A dual-path `onMounted` and a "Who's Serving" section in `src/views/ShareView.vue`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-RESEARCH.md
@.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md
@src/views/ShareView.vue
@src/views/QuarterShareView.vue
</context>

## Artifacts this phase produces (this plan's slice)

- **This plan:** `ShareView.vue` gains a dual-path `onMounted` (opaque token OR memorable `serviceShares/{slug}__service-{date}`) and a "Who's Serving" section rendering `serviceSnapshot.roleAssignments`.
- Consumed from earlier plans: the `service-memorable-share` route already points at `ShareView.vue` (17-02); the `serviceShares` doc + `roleAssignments` snapshot field are written by `createShareToken` (17-03); the serviceShares public-read rule is deployed (17-02).

<assumption_delta_decision>
Primary noun: the planned Service. Decision: **ADD-ALONGSIDE** — the public share renders music (existing) AND who-is-serving (new) from ONE self-contained snapshot doc; the Service and Quarter documents are never merged. Accepted, low-risk. Locked: public visibility of who-is-serving is delivered EXCLUSIVELY via this share link (not by expanding in-app viewer read access), and the snapshot carries person NAMES only (PII guard enforced in 17-03).
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Dual-path public read (opaque token + memorable serviceShares) in ShareView</name>
  <read_first>
    - src/views/ShareView.vue (the existing onMounted ~lines 136-151 reading shareTokens/token; the imports — confirm only vue/vue-router/firebase are imported, NO roster/auth store)
    - src/views/QuarterShareView.vue (the dual-path onMounted ~lines 250-266 — the exact token-vs-memorable branching to mirror)
    - .planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md (section "src/views/ShareView.vue")
  </read_first>
  <files>src/views/ShareView.vue</files>
  <action>
    Extend ShareView's `onMounted` to mirror QuarterShareView's dual-path pattern: read `route.params.token` as an optional string; when present, `getDoc(doc(db,'shareTokens', token))` (existing behavior); when absent (memorable route), `getDoc(doc(db,'serviceShares', `${slug}__service-${date}`))` using `route.params.slug` and `route.params.date`. Read `serviceSnapshot` from `snap.data()` in both cases (same doc shape). Preserve the existing not-found handling: `!snap.exists()` sets the existing notFound flag; a thrown error also sets notFound; `isLoading` cleared in finally (CR-04). Do NOT import any roster/org/auth store — the public route must read ONLY the snapshot doc (CR-05; STATE.md D-24 precedent). Keep the component's existing imports limited to vue / vue-router / firebase.
  </action>
  <verify>
    <automated>npx vue-tsc --build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "serviceShares" src/views/ShareView.vue` matches inside onMounted's memorable-path getDoc.
    - `grep -n "route.params.slug" src/views/ShareView.vue` (or equivalent slug+date param read) matches.
    - `grep -c "stores/roster\|stores/auth\|stores/quarters" src/views/ShareView.vue` returns 0 (no roster/auth/quarters store import — public route stays snapshot-only). Filter comments first: `grep -v '^\s*//' src/views/ShareView.vue | grep -c "stores/"` returns 0.
    - A nonexistent share doc still resolves to the existing not-found state (no unhandled error).
    - `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>ShareView serves both the opaque token and memorable service-share routes from the snapshot doc only.</done>
</task>

<task type="auto">
  <name>Task 2: Render the "Who's Serving" section from the role snapshot</name>
  <read_first>
    - src/views/ShareView.vue (the existing slot-list render ~lines 27-76 and the Notes card block ~lines 78-82 — the styling to match)
    - src/views/QuarterShareView.vue (role rendering conventions for reference)
    - .planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-PATTERNS.md (section "src/views/ShareView.vue" — "Who's Serving" section)
  </read_first>
  <files>src/views/ShareView.vue</files>
  <action>
    Add a "Who's Serving" section to ShareView's template rendering `serviceSnapshot.roleAssignments` (array of `{roleId, roleName, group, personNames}` written by 17-03). For each role show the role name and its person names (comma-separated or list); render nothing/omit the section gracefully when roleAssignments is absent or empty (older shares written before this phase, or services with no assignments). Style the section consistent with the existing Notes card block (same card/spacing Tailwind classes already in this view). This section shows NAMES only — the snapshot never contained email/phone (PII guard is enforced upstream in 17-03), so no filtering is needed here, just render what's present (CR-04).
  </action>
  <verify>
    <automated>npx vue-tsc --build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "roleAssignments" src/views/ShareView.vue` matches in the template.
    - The section renders role name + person names, and is omitted when roleAssignments is empty/absent (no crash on legacy shares).
    - `npx vue-tsc --build` green; `npm run test:unit` green.
  </acceptance_criteria>
  <done>The public share renders a Who's-Serving section from the names-only role snapshot, degrading gracefully for legacy shares.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ShareView now serves the memorable `/:slug/service-:date` route (and the existing `/share/:token`) and renders a "Who's Serving" section from the names-only role snapshot, reading only the public serviceShares/shareTokens doc.</what-built>
  <how-to-verify>
    1. As an editor, open a service that has role assignments and click Share (copies a `/share/:token` link). Open that link in a private/incognito window (unauthenticated). Confirm the music renders AND a "Who's Serving" section lists each role with the scheduled/overridden person names.
    2. Navigate to the memorable URL `/{your-church-slug}/service-{YYYY-MM-DD}` (the service's date) in the same unauthenticated window. Confirm the same page renders (this exercises the deployed serviceShares public-read rule from 17-02).
    3. Open browser dev tools → Network/Console on the unauthenticated page. Confirm NO request reads `organizations/.../roles|quarters|people`, and confirm the rendered/underlying snapshot contains person names only — no email or phone anywhere.
    4. Visit a made-up memorable URL for a nonexistent date; confirm the not-found state renders, not an error.
  </how-to-verify>
  <resume-signal>Type "approved" once the unauthenticated share (both routes) shows who-is-serving with names only, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser → serviceShares / shareTokens doc (read) | Anonymous public read of a denormalized snapshot; no auth token, no roster/org access. |
| memorable URL guessing → serviceShares doc | The `{slug}__service-{date}` id is guessable; anyone can read it (public read is by design). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05-01 | Information Disclosure | public route reading org-scoped roster/PII | high | mitigate | ShareView imports NO roster/org/auth store and reads ONLY the snapshot doc; the snapshot carries names only (17-03). Verified by the grep acceptance and the dev-tools human-check step (STATE.md D-24 precedent). |
| T-17-05-02 | Information Disclosure | scope of anonymous read | low | accept | Public read of names-only who-is-serving is the intended feature (mirrors quarterShares). No PII beyond display names is present in the snapshot. |
| T-17-05-03 | Denial of Service | crash on legacy/absent roleAssignments | low | mitigate | Section renders only when roleAssignments is present/non-empty; legacy shares (no roleAssignments) degrade gracefully. |
| T-17-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase. |
</threat_model>

<verification>
- `npx vue-tsc --build` green; `npm run test:unit` green.
- Human-verify checkpoint: unauthenticated read of BOTH routes shows who-is-serving with names only; dev-tools confirms no org/roster reads and no email/phone in the payload; nonexistent share shows not-found.
- Backstop for 17-02: this plan's human-check is the end-to-end confirmation that the deployed serviceShares public-read rule works in a real browser.
</verification>

<success_criteria>
- ShareView serves both the opaque and memorable service-share routes from the snapshot doc only, with no roster/org/auth access.
- The public page renders a names-only "Who's Serving" section and degrades gracefully for legacy shares.
</success_criteria>

<output>
Create `.planning/phases/17-sync-schedule-with-planned-services-add-a-roles-tab-to-servi/17-05-SUMMARY.md` when done.
</output>
