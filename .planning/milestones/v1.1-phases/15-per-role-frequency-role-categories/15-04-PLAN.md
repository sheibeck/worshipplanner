---
phase: 15-per-role-frequency-role-categories
plan: 04
type: tdd
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - src/stores/quarters.ts
  - src/stores/__tests__/quarters.test.ts
autonomous: true
requirements: [D-05, D-12]
must_haves:
  truths:
    - "generateProposal builds a roleId→group lookup from the roster's roles and passes it as roleGroupOf into proposeQuarterSchedule, so auto-propose enforces the group rules (D-12 wiring)"
    - "setPersonAvailability writes a per-role roleTiers map into the scoped personQuarterData.<personId> path (D-05 persistence), without touching any other person's quarter data"
  artifacts:
    - path: "src/stores/quarters.ts"
      provides: "buildRoleGroupOf helper + roleGroupOf arg wired into generateProposal; roleTiers included in setPersonAvailability write"
      contains: "roleGroupOf"
    - path: "src/stores/__tests__/quarters.test.ts"
      provides: "tests for the group-map wiring + scoped roleTiers write"
      contains: "roleTiers"
  key_links:
    - from: "quarters.ts generateProposal"
      to: "proposeQuarterSchedule roleGroupOf param"
      via: "buildRoleGroupOf(rosterStore.roles)"
      pattern: "roleGroupOf"
    - from: "quarters.ts setPersonAvailability"
      to: "Firestore personQuarterData.<personId>"
      via: "scoped dot-path write including roleTiers"
      pattern: "personQuarterData\\.\\$\\{personId\\}"
---

<objective>
Wire the roster's role-group data into the scheduler call and persist per-role tiers through the quarters store.

Purpose: `scheduler.ts` (15-02) now requires a `roleGroupOf` param — the caller `quarters.ts` must build it from `rosterStore.roles` and pass it into `proposeQuarterSchedule`, or auto-propose runs with the "everything is 'other'" default and the group rules never fire in production. Separately, D-05's per-role tier must be persisted by `setPersonAvailability` using the same scoped dot-path convention already established (never a whole-map rewrite), so the availability drawer (15-06) has somewhere to write.

Output: `buildRoleGroupOf` helper + `roleGroupOf` argument in `generateProposal`; `roleTiers` included in the scoped `personQuarterData.<personId>` write; unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-per-role-frequency-role-categories/15-CONTEXT.md
@.planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md

<interfaces>
<!-- quarters.ts insertion points. From 15-PATTERNS.md. -->
- `buildResolveRolesForDate(quarter, roles)` (~lines 206-215): projects Role[]→RoleSlotConfig[] (drops group). Add a sibling `buildRoleGroupOf(roles: Role[]): (roleId: string) => RoleGroup` — a Map lookup defaulting to 'other' for unknown ids.
- `generateProposal` (~lines 217-236): calls `proposeQuarterSchedule(...)` — add `buildRoleGroupOf(rosterStore.roles)` as the new final arg.
- `setPersonAvailability` (~lines 168-204): writes `{ [`personQuarterData.${personId}`]: { personId, ...data }, updatedAt: serverTimestamp() }`. The `data` object (`{ blackoutDates, pairedWith, frequencyTier, note }`) gains `roleTiers: Record<string, FrequencyTier>`.
scheduler (from 15-02): `proposeQuarterSchedule(people, serviceDates, resolveRolesForDate, personQuarterData, existingCalendar?, roleGroupOf?)`.
Types (from 15-01): `PersonQuarterData.roleTiers?: Record<string, FrequencyTier>`.
</interfaces>
</context>

<feature>
  <name>Group-map wiring + per-role tier persistence in quarters store</name>
  <files>src/stores/quarters.ts, src/stores/__tests__/quarters.test.ts</files>
  <behavior>
    - generateProposal passes a roleGroupOf derived from rosterStore.roles into proposeQuarterSchedule; an unknown roleId resolves to 'other'.
    - With that wiring, a generated proposal for a roster containing TECH + BAND roles never assigns the same person a TECH and a BAND role on one date (end-to-end through the store), confirming the 15-02 rules are actually engaged in production.
    - setPersonAvailability persists a roleTiers map inside personQuarterData.<personId>; another person's personQuarterData entry is untouched by the write.
  </behavior>
  <implementation>
    Add `buildRoleGroupOf(roles: Role[])` returning `(roleId) => map.get(roleId) ?? 'other'`. In generateProposal, pass `buildRoleGroupOf(rosterStore.roles)` as the final argument to proposeQuarterSchedule. In setPersonAvailability, extend the `data` object type + write to include `roleTiers` (written wholesale within the already-scoped `personQuarterData.${personId}` dot-path — still per-person scoped, richer value; other people's entries untouched per the T-13-09-02/T-14-03-01 concurrency-safety convention). Do not change blackout/pairing shapes (D-07).
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN): buildRoleGroupOf + roleGroupOf wired into generateProposal</name>
  <files>src/stores/quarters.ts, src/stores/__tests__/quarters.test.ts</files>
  <read_first>
    - src/stores/quarters.ts (buildResolveRolesForDate + generateProposal, ~lines 206-236)
    - src/stores/__tests__/quarters.test.ts (existing generateProposal test setup + mocked roster store)
    - .planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md (quarters.ts section)
    - src/utils/scheduler.ts (confirm the new roleGroupOf param position from 15-02)
  </read_first>
  <behavior>
    - buildRoleGroupOf(roles)(knownRoleId) returns that role's group; (unknownRoleId) returns 'other'.
    - generateProposal invokes proposeQuarterSchedule with a roleGroupOf argument built from rosterStore.roles.
    - End-to-end: a proposal over roles where 'sound' is group 'tech' and 'guitar' is group 'band' never places one person in both on the same date.
  </behavior>
  <action>
    Write a failing test asserting generateProposal engages the group rules (a person eligible for both a TECH and a BAND role is not double-assigned across groups on one date), then implement `buildRoleGroupOf` and pass it as the final arg to proposeQuarterSchedule per <implementation>. Run to GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/quarters.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/__tests__/quarters.test.ts` exits 0
    - `grep -n "buildRoleGroupOf" src/stores/quarters.ts` matches (helper defined) AND appears in the generateProposal call to proposeQuarterSchedule
    - Test proves an end-to-end proposal respects TECH exclusivity (group rules engaged, not just unit-level)
    - `npm run type-check` exits 0
    - Commits: `test(15-04): failing group-map wiring case` then `feat(15-04): wire roleGroupOf into generateProposal`
  </acceptance_criteria>
  <done>Production proposals enforce the group co-occurrence rules because the roster's role→group map is threaded into the scheduler.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (RED→GREEN): persist per-role roleTiers via setPersonAvailability</name>
  <files>src/stores/quarters.ts, src/stores/__tests__/quarters.test.ts</files>
  <read_first>
    - src/stores/quarters.ts (setPersonAvailability, ~lines 168-204)
    - src/stores/__tests__/quarters.test.ts (existing setPersonAvailability test + updateDoc assertion style)
    - .planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md (quarters.ts dot-path write section)
  </read_first>
  <behavior>
    - setPersonAvailability(quarterId, personId, { ..., roleTiers: { vocals: 'out', guitar: 'regular' } }) writes those tiers inside `personQuarterData.<personId>`.
    - The write uses the scoped `personQuarterData.${personId}` dot-path key (not a whole-map replacement), so a second person's quarter data is provably untouched.
    - Omitting roleTiers still works (back-compat).
  </behavior>
  <action>
    Write a failing test asserting the scoped write includes roleTiers under `personQuarterData.<personId>` and does not write a bare `personQuarterData` root key, then extend the `data` type + updateDoc payload in setPersonAvailability to carry roleTiers per <implementation>. Do not alter blackoutDates/pairedWith handling (D-07). Run to GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/quarters.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/__tests__/quarters.test.ts` exits 0
    - Test asserts `updateDoc` was called with a key matching `personQuarterData.<personId>` whose value contains `roleTiers`, and NOT with a bare `personQuarterData` root key
    - `grep -n "roleTiers" src/stores/quarters.ts` matches inside setPersonAvailability
    - blackoutDates/pairedWith handling in setPersonAvailability is unchanged (D-07)
    - `npx vitest run` full suite exits 0; `npm run type-check` exits 0
    - Commits: `test(15-04): failing roleTiers persistence case` then `feat(15-04): persist per-role roleTiers scoped write`
  </acceptance_criteria>
  <done>Per-role tiers persist through a per-person-scoped dot-path write with no risk of clobbering other volunteers' quarter data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drawer/UI → store write | Caller-supplied per-role tier map crosses into a scoped Firestore write |
| Roster roles → scheduler param | Role group data crosses from the roster store into the pure scheduler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | roleTiers write clobbering another person's quarter data | mitigate | Write uses the scoped `personQuarterData.${personId}` dot-path key exactly like the established convention; a test asserts no bare-root `personQuarterData` write. Sibling entries untouched. |
| T-15-04-02 | Spoofing / Integrity | Unknown/stale roleId in the group map | mitigate | `buildRoleGroupOf` defaults unknown roleIds to 'other' (least-restrictive but never crashes); tested explicitly. |
| T-15-04-03 | Repudiation | Group rules silently not applied in production | mitigate | End-to-end store test asserts a generated proposal actually respects TECH exclusivity, catching a missing/omitted roleGroupOf arg. |
| T-15-04-SC | Tampering | package installs | accept | No packages installed this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/quarters.test.ts` green
- roleGroupOf threaded into generateProposal; roleTiers persisted via scoped write
- `npm run type-check` green; full suite green
</verification>

<success_criteria>
Auto-propose enforces the group rules in production (map wired), and per-role tiers persist safely per-person-scoped. The availability drawer now has a store write to target.
</success_criteria>

<output>
Create `.planning/phases/15-per-role-frequency-role-categories/15-04-SUMMARY.md` when done.
</output>
