---
phase: 15-per-role-frequency-role-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/roster.ts
  - src/views/RosterView.vue
  - src/components/QuarterGrid.vue
  - src/components/RolesConfigPanel.vue
  - src/components/AvailabilityRosterTable.vue
autonomous: true
requirements: [D-04, D-05, D-08, D-09]
must_haves:
  truths:
    - "RoleGroup type includes 'vocals' (D-08) and the whole project compiles under vue-tsc --build"
    - "Person carries an optional roleFrequencies map and PersonQuarterData carries an optional roleTiers map (D-04/D-05 schema shape), both additive and back-compatible with pre-migration docs"
    - "DEFAULT_ROLES seeds the vocals role with group 'vocals' (D-09) and RolesConfigPanel lets a leader classify any role as Vocals (D-09 re-classify UI)"
  artifacts:
    - path: "src/types/roster.ts"
      provides: "RoleGroup +'vocals', Person.roleFrequencies?, PersonQuarterData.roleTiers?, UpsertPersonInput.roleFrequencies?, DEFAULT_ROLES vocals reseed"
      contains: "roleFrequencies"
    - path: "src/components/RolesConfigPanel.vue"
      provides: "Vocals group option + label + badge + grouped list"
      contains: "vocals"
  key_links:
    - from: "src/types/roster.ts RoleGroup"
      to: "Record<RoleGroup, string> maps in 4 Vue files"
      via: "TypeScript exhaustiveness check"
      pattern: "Record<RoleGroup"
---

<objective>
Land the per-role data-model schema and the `'vocals'` RoleGroup extension as one atomic, compiling change. This is the foundation wave: every downstream plan (scheduler, migrations, UI) depends on these type shapes existing and on the codebase compiling with the new group value.

Purpose: Adding `'vocals'` to `RoleGroup` breaks every `Record<RoleGroup, string>` exhaustive map (5 sites across 4 Vue files) plus RolesConfigPanel's hardcoded group `<option>` list. TypeScript's exhaustiveness check is the built-in gate — the phase cannot proceed until `vue-tsc --build` is green with the new union member. Schema fields are additive/optional so pre-migration Firestore docs never throw.

Output: Updated `src/types/roster.ts` (types + DEFAULT_ROLES) and `'vocals'` entries added to all group-keyed maps/arrays/options in RosterView.vue, QuarterGrid.vue, RolesConfigPanel.vue, AvailabilityRosterTable.vue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-role-frequency-role-categories/15-CONTEXT.md
@.planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md

<interfaces>
<!-- Current roster.ts contracts the executor extends. Extracted from src/types/roster.ts. -->
Current (src/types/roster.ts):
- `export type RoleGroup = 'band' | 'tech' | 'other'`  → becomes `'band' | 'tech' | 'vocals' | 'other'`
- `Person.frequencyTargetN: number` (RETAINED — becomes the fallback/default; do NOT delete)
- `PersonQuarterData.frequencyTier?: FrequencyTier` (RETAINED — the additive precedent to mirror)
- `UpsertPersonInput` has optional `frequencyTargetN?` (add sibling `roleFrequencies?`)
- `DEFAULT_ROLES[2]` = `{ name: 'vocals', group: 'band', defaultCount: 1, order: 2 }` → group 'vocals'

Record<RoleGroup, string> / GROUP_ORDER sites that MUST gain a 'vocals' entry (from 15-PATTERNS.md):
- RosterView.vue: `groupBadgeClasses` (~line 474)
- QuarterGrid.vue: `groupHeaderBg` (~line 198) + `GROUP_ORDER` array (~line 204)
- RolesConfigPanel.vue: `groupLabels` + `groupBadgeClasses` + `groupOrder` array (~lines 114-129) + hardcoded `<option>` list (~lines 81-88) + `groupedRoles` computed (~lines 125-129)
- AvailabilityRosterTable.vue: `ROLE_CHIP_CLASS` (~lines 111-115)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend roster.ts types + DEFAULT_ROLES for per-role schema and vocals group</name>
  <files>src/types/roster.ts</files>
  <read_first>
    - src/types/roster.ts (the file being modified — read the whole file, it is <100 lines)
    - .planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md (section "src/types/roster.ts" — the additive-optional precedent)
  </read_first>
  <action>
    In src/types/roster.ts make these additive, back-compatible changes:
    1. Change `RoleGroup` to `'band' | 'tech' | 'vocals' | 'other'` (D-08 — extend the existing enum, do NOT introduce a separate "category" concept).
    2. Add optional `roleFrequencies?: Record<string, number>` to `Person` (roleId → 1-in-N cadence), with a doc comment noting it is STANDING data, defaults per-role to `frequencyTargetN` (D-03) then to N=4 (D-02) when absent, and that `frequencyTargetN` is RETAINED as the fallback/default-for-new-roles value (do NOT delete `frequencyTargetN`).
    3. Add optional `roleTiers?: Record<string, FrequencyTier>` to `PersonQuarterData` (roleId → tier), quarter-scoped, with a doc comment noting it defaults per-role to the legacy `frequencyTier` then to `'regular'` when absent. Keep `frequencyTier?` as the retained fallback (D-05). Do NOT touch `blackoutDates` or `pairedWith` (D-07 — stay per-person, unchanged).
    4. Add optional `roleFrequencies?: Record<string, number>` to `UpsertPersonInput` alongside the existing `frequencyTargetN?`.
    5. In `DEFAULT_ROLES`, change the `vocals` entry from `group: 'band'` to `group: 'vocals'` (D-09 reseed). Leave all other entries unchanged.
    No logic changes, no read-site changes in this task — schema only.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/scheduler.test.ts src/stores/__tests__/roster.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "vocals" src/types/roster.ts` shows `RoleGroup` union includes `'vocals'` AND the DEFAULT_ROLES vocals entry uses `group: 'vocals'`
    - `grep -n "roleFrequencies" src/types/roster.ts` matches on `Person` and on `UpsertPersonInput`
    - `grep -n "roleTiers" src/types/roster.ts` matches on `PersonQuarterData`
    - `frequencyTargetN` and `frequencyTier` still present in roster.ts (retained, not deleted)
    - `blackoutDates: string[]` and `pairedWith: string[]` on PersonQuarterData are unchanged
  </acceptance_criteria>
  <done>roster.ts declares the new optional per-role fields and the vocals group; existing scheduler/roster tests still pass (they do not yet exercise the new fields).</done>
</task>

<task type="auto">
  <name>Task 2: Add 'vocals' to every RoleGroup-keyed map/array/option so vue-tsc compiles</name>
  <files>src/views/RosterView.vue, src/components/QuarterGrid.vue, src/components/RolesConfigPanel.vue, src/components/AvailabilityRosterTable.vue</files>
  <read_first>
    - src/views/RosterView.vue (groupBadgeClasses map, ~line 474)
    - src/components/QuarterGrid.vue (groupHeaderBg map + GROUP_ORDER array, ~lines 198-204)
    - src/components/RolesConfigPanel.vue (groupLabels/groupBadgeClasses/groupOrder + hardcoded option list + groupedRoles, ~lines 81-129)
    - src/components/AvailabilityRosterTable.vue (ROLE_CHIP_CLASS map, ~lines 111-115)
    - .planning/phases/15-per-role-frequency-role-categories/15-PATTERNS.md (sections for each of these four files — gives exact line anchors and the static-literal-Tailwind-class rule)
  </read_first>
  <action>
    Add a `vocals:` entry to every `Record<RoleGroup, string>` map and a `'vocals'` element to every `RoleGroup[]` order array, plus the config-panel option, using a single consistent, distinct, STATIC Tailwind color literal for vocals across all files (use the pink family, e.g. `bg-pink-900/50 text-pink-300 border-pink-800` for badge maps, `bg-pink-900/50` for header-bg, `text-pink-300 bg-pink-900/40 border border-pink-700/50` for chip maps). Never construct Tailwind classes by string interpolation (Tailwind v4 purge safety — match the existing static-literal convention).
    Concretely:
    - RosterView.vue `groupBadgeClasses`: add `vocals: 'bg-pink-900/50 text-pink-300 border-pink-800'`.
    - QuarterGrid.vue `groupHeaderBg`: add `vocals: 'bg-pink-900/50'`; `GROUP_ORDER`: insert `'vocals'` immediately after `'band'` (per the user's "1 instrument + vocals" mental model — vocals sits next to band).
    - RolesConfigPanel.vue: add `vocals: 'Vocals'` to `groupLabels`; add the vocals color literal to `groupBadgeClasses`; insert `'vocals'` after `'band'` in `groupOrder`; add `<option value="vocals">Vocals</option>` to the group `<select>`; add `vocals: rosterStore.roles.filter((r) => r.group === 'vocals')` to the `groupedRoles` computed. This `<option>` is the D-09 re-classification UI — leaders re-group any role here via the already-wired updateRole path; no new mechanism.
    - AvailabilityRosterTable.vue `ROLE_CHIP_CLASS`: add the vocals chip color literal.
    Do NOT add any per-role frequency/tier UI in this task — that is Wave 3. This task is purely the exhaustiveness fix to keep the build green.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` (vue-tsc --build) exits 0 with no RoleGroup exhaustiveness errors
    - `grep -rn "vocals:" src/views/RosterView.vue src/components/QuarterGrid.vue src/components/RolesConfigPanel.vue src/components/AvailabilityRosterTable.vue` shows a vocals entry in each of the 5 maps
    - `grep -n "value=\"vocals\"" src/components/RolesConfigPanel.vue` matches the new option
    - `grep -n "'vocals'" src/components/QuarterGrid.vue` shows 'vocals' present in GROUP_ORDER
    - `npx vitest run` full suite exits 0 (no behavioral regressions from the additive edits)
  </acceptance_criteria>
  <done>vue-tsc --build compiles cleanly with the vocals group present in all group-keyed maps/arrays/options, and the roles config panel offers Vocals as a selectable group.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → client type cast | `roster.ts` casts snapshot data to `Person`/`Role`; pre-migration docs lack the new optional fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | New optional schema fields read from legacy docs | mitigate | Both `roleFrequencies?` and `roleTiers?` are declared optional; no read site in this plan assumes presence. Downstream reads use `?? fallback`. Legacy docs never throw or lose data. |
| T-15-01-02 | Denial of Service | Type-change breaking the build | mitigate | Type change + all 5 exhaustive-map fixes land in one plan/wave so `vue-tsc --build` is green at the wave boundary; no half-migrated compile state ships. |
| T-15-01-SC | Tampering | package installs | accept | This phase installs zero external packages (RESEARCH Package Legitimacy Audit: N/A). No supply-chain surface added. |
</threat_model>

<verification>
- `npm run type-check` green (the exhaustiveness gate)
- `npx vitest run` green (no regressions)
- roster.ts declares `roleFrequencies?`, `roleTiers?`, RoleGroup includes 'vocals', DEFAULT_ROLES vocals is group 'vocals'
</verification>

<success_criteria>
Project compiles with the `'vocals'` RoleGroup and per-role schema fields present; all existing tests pass; RolesConfigPanel offers Vocals as a group. Downstream waves can now import and read the new field shapes.
</success_criteria>

<output>
Create `.planning/phases/15-per-role-frequency-role-categories/15-01-SUMMARY.md` when done.
</output>
