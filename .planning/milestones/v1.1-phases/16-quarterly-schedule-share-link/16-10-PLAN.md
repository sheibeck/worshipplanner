---
phase: 16-quarterly-schedule-share-link
plan: 10
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - src/components/useIsMobile.ts
  - src/components/QuarterShareMatrix.vue
  - src/views/QuarterShareView.vue
  - src/views/__tests__/QuarterShareView.test.ts
  - src/router/index.ts
  - src/router/__tests__/router.test.ts
autonomous: true
requirements: [R-01, R-02, R-03]
must_haves:
  truths:
    - "The public share page defaults to a roles-columns × dates-rows matrix, with a toggle to the existing list view (R-01, D-14)"
    - "On narrow/phone screens the page defaults to the list view; the toggle remains available (D-14)"
    - "A memorable /{slug}/quarter{N}-{YYYY} route resolves to the same public snapshot; the opaque token route still works (R-02)"
    - "The reserved-slug dynamic route cannot shadow existing static app routes (D-19)"
    - "A name filter hides dates where the named person serves no role, in both views, and view+name persist in the URL (R-03, D-15/D-16)"
    - "The page reads only the denormalized snapshot — no roster/auth store access (D-24)"
  artifacts:
    - path: "src/components/QuarterShareMatrix.vue"
      provides: "Read-only roles×dates matrix, comma-separated multi-person cells"
      contains: "join(', ')"
    - path: "src/views/QuarterShareView.vue"
      provides: "matrix/list toggle, name filter, URL persistence, memorable-route branch"
      contains: "quarterShares"
    - path: "src/router/index.ts"
      provides: "memorable /:slug/quarter route"
      contains: "quarter-memorable-share"
  key_links:
    - from: "src/views/QuarterShareView.vue"
      to: "quarterShares/{slug}__q{N}-{year}"
      via: "getDoc on the memorable route params"
      pattern: "quarterShares"
    - from: "src/views/QuarterShareView.vue"
      to: "route.query"
      via: "router.replace persistence of view + name"
      pattern: "router.replace"
---

<objective>
Build the public share-page consumer side: a read-only matrix view (default, R-01), a name filter
(R-03), URL persistence of view+name (D-16), the mobile fallback (D-14), and the memorable
`/{slug}/quarter{N}-{YYYY}` route that reads the quarterShares doc (R-02). All snapshot-only,
no store access (D-24).

Purpose: Volunteers open a memorable link, see a matrix, and filter to their own dates.
Output: `useIsMobile.ts`, `QuarterShareMatrix.vue`, reworked `QuarterShareView.vue`, new route, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md
@.planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
Snapshot shape (written by 16-09; identical to shareTokens): { label, serviceDates: string[], roles: {id,name,group}[], calendar: Record<date, Record<roleId, string[/*names*/]>> }.
QuarterShareView.vue current: imports only vue, vue-router, firebase/firestore, @/firebase (NO @/stores/* — preserve). onMounted reads getDoc(doc(db,'shareTokens', route.params.token)).quarterSnapshot; peopleFor(date, roleId) exists; list renders `peopleFor(...).join(', ')`; GROUP_ORDER/sortedRoles computed (~85-92).
Memorable route (RESEARCH Pattern 3): path '/:slug/quarter:num([1-4])-:year(\\d{4})', name 'quarter-memorable-share', component QuarterShareView, NO meta.requiresAuth. Append after existing routes — static routes out-rank dynamic (D-19), so it cannot shadow /songs, /schedule, etc.
Memorable doc read: getDoc(doc(db,'quarterShares', `${slug}__q${num}-${year}`)).quarterSnapshot.
Mobile fallback: useIsMobile() → window.matchMedia('(min-width: 640px)'). Initial view = route.query.view ?? (isDesktop ? 'matrix' : 'list').
URL persistence (SongsView pattern): watch([viewMode,nameFilter], ([view,name]) => router.replace({ query: { ...route.query, view, name: name || undefined } })).
Name typeahead (AvailabilityDrawer "Must serve with"): text input + absolute dropdown, @focus/@blur with window.setTimeout(150); candidates = deduped snapshot names (NOT rosterStore).
UI-SPEC copy: toggle "Matrix"/"List" (aria-label "Schedule view", matrix default); filter placeholder "Filter by name…"; dropdown empty "No matching names"; clear "Show everyone"; empty cell "—"; error "This shared schedule is no longer available or the link is invalid." / "Please ask your worship leader to share the schedule again." Light theme, grayscale accent only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memorable route + useIsMobile + QuarterShareView memorable-doc branch</name>
  <files>src/router/index.ts, src/router/__tests__/router.test.ts, src/components/useIsMobile.ts, src/views/QuarterShareView.vue</files>
  <read_first>
    - src/router/index.ts (existing /quarter-share/:token public route ~83-88 to mirror)
    - src/router/__tests__/router.test.ts (existing cases to extend)
    - src/views/QuarterShareView.vue (onMounted snapshot fetch ~110-125; imports; sortedRoles)
    - .planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md (Pattern 3 route + Pitfall 1 reserved-slug shadowing)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (sections "router/index.ts", "QuarterShareView.vue")
  </read_first>
  <action>
    Add the memorable route to router/index.ts: path `/:slug/quarter:num([1-4])-:year(\\d{4})`,
    name 'quarter-memorable-share', lazy `QuarterShareView`, NO meta.requiresAuth — appended after
    existing routes (Vue Router ranks static > dynamic, so it cannot shadow app routes, D-19).
    Extend router.test.ts: the memorable pattern resolves for a sample slug/quarter, and a reserved
    first segment (e.g. `/schedule`) still matches its existing static route (not the dynamic one).
    Create `src/components/useIsMobile.ts` — a composable returning a reactive `isDesktop` ref backed
    by `window.matchMedia('(min-width: 640px)')` (add/remove listener on mount/unmount). In
    QuarterShareView.vue onMounted, branch on the route: if `route.params.token` exists, read
    shareTokens (unchanged); else read `quarterShares/${route.params.slug}__q${route.params.num}-${route.params.year}`
    — same try/catch/finally and same notFound/error handling. Keep the component free of any
    @/stores/* import (D-24).
  </action>
  <verify>
    <automated>npx vitest run src/router/__tests__/router.test.ts 2>&1 | tail -20 && npx vue-tsc --build 2>&1 | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "quarter-memorable-share" src/router/index.ts` === 1; route has no `requiresAuth`
    - `npx vitest run src/router/__tests__/router.test.ts` passes incl. the reserved-segment-still-wins case
    - `src/components/useIsMobile.ts` exports a composable using `window.matchMedia('(min-width: 640px)')`
    - QuarterShareView reads quarterShares on the memorable route and shareTokens on the token route; `grep -c "@/stores" src/views/QuarterShareView.vue` === 0 (D-24)
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>Memorable route resolves to the quarterShares snapshot; mobile composable exists; token route still works; no store access.</done>
</task>

<task type="auto">
  <name>Task 2: QuarterShareMatrix component + matrix/list toggle (matrix default, mobile fallback)</name>
  <files>src/components/QuarterShareMatrix.vue, src/views/QuarterShareView.vue</files>
  <read_first>
    - src/components/QuarterGrid.vue lines 1-29 (table orientation to adapt — roles-across-top th, dates-down tr)
    - src/views/QuarterShareView.vue (existing list rendering, peopleFor, sortedRoles, GROUP_ORDER)
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md (light-theme matrix, spacing px-2 py-2, weights 400/600, toggle copy)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "QuarterShareMatrix.vue")
  </read_first>
  <action>
    Create `QuarterShareMatrix.vue` — a presentational, read-only, light-theme table: header row
    "Date" + one column per role (sortedRoles), body row per (filtered) date with the date label
    and, per role, `peopleFor(date, role.id).join(', ') || '—'` (comma-separated multi-person, em-dash
    empty). No @click, no badges, only a subtle `hover:bg-gray-50` row highlight. Props: the snapshot
    roles + dates + a peopleFor accessor (or the raw snapshot); derive nothing from stores (D-24).
    Cells `px-2 py-2`, weights 400/600 only, grayscale text. In QuarterShareView.vue, add a
    `viewMode` ref ('matrix' | 'list') initialized to `route.query.view ?? (isDesktop.value ? 'matrix'
    : 'list')` using useIsMobile; render a segmented "Matrix"/"List" toggle (aria-label "Schedule
    view", grayscale active state) above the content; render `<QuarterShareMatrix>` when
    viewMode==='matrix' else the existing list. Persist viewMode to `route.query.view` via
    router.replace (do not push).
  </action>
  <verify>
    <automated>npx vue-tsc --build 2>&1 | head -15 && grep -c "join(', ')\|QuarterShareMatrix" src/views/QuarterShareView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `QuarterShareMatrix.vue` renders roles as columns (`th v-for`) and dates as rows (`tr v-for`), cells `join(', ')` with `—` fallback
    - `grep -c "@/stores" src/components/QuarterShareMatrix.vue` === 0 (D-24)
    - QuarterShareView renders a "Matrix"/"List" toggle (aria-label "Schedule view"); matrix is default on desktop, list on mobile via useIsMobile
    - viewMode persisted via `router.replace` to `route.query.view`
    - `npx vue-tsc --build` exits 0
  </acceptance_criteria>
  <done>Read-only matrix component + default-matrix toggle with mobile fallback and URL-persisted view mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Name filter typeahead + URL persistence + share-page tests</name>
  <files>src/views/QuarterShareView.vue, src/views/__tests__/QuarterShareView.test.ts</files>
  <read_first>
    - src/components/AvailabilityDrawer.vue lines 179-201 (typeahead: input + absolute dropdown, @focus/@blur setTimeout(150))
    - src/views/SongsView.vue lines 291-296 (router.replace query pattern)
    - src/views/__tests__/ShareView.test.ts (existing public snapshot-view test harness — mocked getDoc — to mirror)
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md (filter copy: "Filter by name…", "No matching names", "Show everyone")
  </read_first>
  <action>
    Add a name-filter typeahead to QuarterShareView (light-theme adaptation of the "Must serve with"
    control): input placeholder "Filter by name…"; candidate list = deduped person names collected
    from the snapshot's calendar (NOT any store); dropdown empty state "No matching names";
    selecting a name sets `nameFilter` to that exact snapshot name (D-15 — only real names
    selectable). When `nameFilter` is set, compute `filteredDates` = dates where that person appears
    in ≥1 role's name list; both the matrix and the list render only `filteredDates`. Show a "Show
    everyone" text button (only when a filter is active) that clears it. Persist BOTH `view` and
    `name` in the URL via `watch([viewMode, nameFilter], … router.replace({ query: { ...route.query,
    view, name: name || undefined } }))` and hydrate both from `route.query` on mount (D-16). Create
    `QuarterShareView.test.ts` (mirror ShareView.test.ts's mocked getDoc): matrix renders roles×dates
    from a snapshot; toggling to list renders the list; setting a name filter hides dates where that
    person serves nothing (in both views); clearing restores all dates.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/QuarterShareView.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/views/__tests__/QuarterShareView.test.ts` passes (matrix render, list toggle, name filter hides non-serving dates, clear restores)
    - Filter candidates derive from the snapshot only — `grep -c "@/stores" src/views/QuarterShareView.vue` === 0
    - Filter copy "Filter by name…", "No matching names", "Show everyone" present verbatim
    - Both `view` and `name` are written to `route.query` via `router.replace` and hydrated on mount
  </acceptance_criteria>
  <done>Name filter hides non-serving dates in both views; view+name persist in the URL; share-page tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated public visitor → QuarterShareView / quarterShares | Read-only public page; reads only the denormalized snapshot doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10-01 | Information Disclosure | share page reaching roster/auth PII | mitigate | Component imports zero @/stores/*; all data from the fetched snapshot (D-24); grep-guarded in acceptance criteria |
| T-16-10-02 | Information Disclosure | slug/quarter enumeration via memorable URL | accept | Intentionally guessable URL — locked accepted tradeoff (SPEC round 2) |
| T-16-10-03 | Spoofing/Tampering | reserved slug shadowing an app route | mitigate | Reserved-word guard enforced at claim time (16-02); router ranks static > dynamic; router test asserts reserved segment still wins (D-19) |
| T-16-10-04 | Tampering | XSS via snapshot names rendered in matrix cells | mitigate | Vue text interpolation ({{ }}) auto-escapes; no v-html used for names/cells |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/QuarterShareView.test.ts` and `src/router/__tests__/router.test.ts` green; `npx vue-tsc --build` green.
- Manual: visit /{slug}/quarter1-2026 → matrix by default (list on phone); toggle + filter persist in URL; opaque token route still resolves.
- Requires deployed rules (16-02) + a finalized quarterShares doc (16-09) for live end-to-end.
</verification>

<success_criteria>
- Matrix-default share page with list toggle, mobile fallback, name filter, URL-persisted view+name, memorable route — all snapshot-only.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-10-SUMMARY.md` when done.
</output>
