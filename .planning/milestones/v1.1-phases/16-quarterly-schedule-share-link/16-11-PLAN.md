---
phase: 16-quarterly-schedule-share-link
plan: 11
type: execute
wave: 3
depends_on: ["16-04", "16-05", "16-06", "16-07"]
files_modified:
  - src/types/roster.ts
autonomous: true
requirements: [R-05]
must_haves:
  truths:
    - "The deprecated standing/quarter frequency fields are removed from the type model once no code references them (D-04)"
    - "roleFrequency is the single frequency source across the whole codebase (D-04/D-05)"
  artifacts:
    - path: "src/types/roster.ts"
      provides: "Cleaned type model — roleFrequency only, no legacy frequency fields"
      contains: "roleFrequency"
  key_links:
    - from: "src/types/roster.ts"
      to: "codebase"
      via: "zero references to removed fields (grep-gated)"
      pattern: "roleFrequency"
---

<objective>
Remove the four deprecated frequency fields (`Person.frequencyTargetN`, `Person.roleFrequencies`,
`PersonQuarterData.roleTiers`, `PersonQuarterData.frequencyTier`) that plan 16-01 left in place for
a green-build transition, now that every reader (scheduler 16-04, drawer 16-05, roster/table 16-06,
grid 16-07) reads `roleFrequency`.

Purpose: Finish the D-04 relocation cleanly — one frequency source, no orphaned type fields.
Output: Cleaned `src/types/roster.ts` with a full-suite green gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
Fields to remove (added-as-deprecated in 16-01): Person.frequencyTargetN, Person.roleFrequencies, PersonQuarterData.roleTiers, PersonQuarterData.frequencyTier.
Keep: RoleFrequencyEntry, PersonQuarterData.roleFrequency, FrequencyTier.
D-04: existing Firestore docs keep orphaned old fields — do NOT write migration/cleanup code to strip stored data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated frequency fields with a zero-reference grep gate</name>
  <files>src/types/roster.ts</files>
  <read_first>
    - src/types/roster.ts (the four @deprecated fields to remove)
    - .planning/phases/16-quarterly-schedule-share-link/16-RESEARCH.md (Pitfall 5 — audit all read sites before removal; do NOT write data-migration code, D-04)
  </read_first>
  <action>
    First run a grep across `src/` for `frequencyTargetN`, `roleFrequencies`, `roleTiers`, and
    `frequencyTier` to confirm zero remaining references outside `src/types/roster.ts` itself (all
    readers were repointed in 16-04/05/06/07). If any reference remains, fix that reader to use
    `roleFrequency` before proceeding. Then delete the four deprecated field declarations from
    `Person` and `PersonQuarterData` in roster.ts. Do NOT add any Firestore data-migration/cleanup
    code — existing docs keep the orphaned fields harmlessly (D-04). Run the full unit suite +
    type-check green.
  </action>
  <verify>
    <automated>grep -rn "frequencyTargetN\|roleFrequencies\|roleTiers\|frequencyTier" src/ | grep -v "roleFrequency\b" | grep -v "RoleFrequencyEntry"; echo "refs above must be empty"; npx vue-tsc --build 2>&1 | head -15 && npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "frequencyTargetN\|roleFrequencies\b\|roleTiers\|frequencyTier" src/` === 0 (no references anywhere, including types)
    - `grep -n "roleFrequency" src/types/roster.ts` still present (the surviving field)
    - No new migration/cleanup code added (grep for the removed field names returns nothing)
    - `npx vue-tsc --build` exits 0 and `npx vitest run` (full suite) passes
  </acceptance_criteria>
  <done>Deprecated frequency fields removed; roleFrequency is the sole source; full suite + type-check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type model only | Pure type deletion; no runtime/data boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11-01 | Tampering | removing fields still referenced somewhere (build/runtime break) | mitigate | Grep gate asserts zero references before/after removal; full suite + type-check must be green |
| T-16-11-02 | Information Disclosure | orphaned frequency data on existing Firestore docs | accept | D-04 authorizes leaving orphaned stored data; fields carry no PII |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- Grep gate: zero references to the four removed fields across `src/`.
- `npx vue-tsc --build` green; `npx vitest run` full suite green.
</verification>

<success_criteria>
- Legacy frequency fields removed; roleFrequency is the single source; whole suite green.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-11-SUMMARY.md` when done.
</output>
