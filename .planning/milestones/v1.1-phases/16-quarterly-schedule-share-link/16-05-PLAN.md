---
phase: 16-quarterly-schedule-share-link
plan: 05
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/AvailabilityDrawer.vue
  - src/components/__tests__/AvailabilityDrawer.test.ts
autonomous: true
requirements: [R-04, R-05, R-06, R-07, R-08]
must_haves:
  truths:
    - "The availability drawer has one per-role frequency control writing PersonQuarterData.roleFrequency; no separate/standing frequency write remains (R-05, D-05)"
    - "The date-range picker is gone; per-Sunday click-to-toggle is the only blackout entry method (R-08)"
    - "Blackout editing is framed as unavailable Sundays for the active quarter (R-06)"
    - "A person's roles can be edited from the Schedule-screen drawer, written through the roster store (R-04, D-09)"
    - "R-07 (cross-screen blackout editing) is descoped per D-10 — blackout is edited ONLY from this Schedule-screen drawer; no Volunteer-screen blackout editor is added"
  artifacts:
    - path: "src/components/AvailabilityDrawer.vue"
      provides: "Unified per-role frequency control, no date-range picker, roles-editing checklist"
      contains: "roleFrequency"
  key_links:
    - from: "src/components/AvailabilityDrawer.vue"
      to: "quartersStore.setPersonAvailability"
      via: "onSave with roleFrequency payload"
      pattern: "roleFrequency"
    - from: "src/components/AvailabilityDrawer.vue"
      to: "rosterStore roles write"
      via: "roles checklist upsert (D-09)"
      pattern: "upsertPeople|updatePerson"
---

<objective>
Consolidate the Schedule-screen per-quarter per-person editor (`AvailabilityDrawer.vue`):
remove the date-range picker (R-08), collapse the frequency surface to a single per-role control
that writes `roleFrequency` and drop the standing frequency write (R-05/D-05), keep the blackout
framing as unavailable Sundays (R-06), and add roles editing so roles are editable from the
Schedule screen too (R-04/D-09).

Scope note (D-10): R-07's cross-screen blackout editing is **descoped** — blackout Sundays are
edited ONLY from this Schedule-screen drawer against the active quarter. This plan adds NO
Volunteer-screen blackout editor (the Volunteer screen needs no quarter context, D-10). R-07 is
listed in requirements only to record this descope disposition, not to deliver a second editor.

Purpose: One frequency control, quarter-scoped; simpler blackout entry; roles editable without
flipping to the roster screen.
Output: Reworked `AvailabilityDrawer.vue` (+ tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-quarterly-schedule-share-link/16-CONTEXT.md
@.planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md
@.planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md

<interfaces>
quartersStore.setPersonAvailability(quarterId, personId, { blackoutDates, pairedWith, roleFrequency, note }) — from 16-01 (roleFrequency replaces frequencyTier/roleTiers in the payload).
rosterStore roles write path (D-09): the exact upsertPeople/updatePerson call RosterView already uses for the roles checklist — reuse it here.
RoleFrequencyEntry: { tier: FrequencyTier; n: number }.

Current AvailabilityDrawer.vue (grep-confirmed line refs):
- Date-range picker to REMOVE: lines ~118-145 (rangeStart/rangeEnd/applyRange + "Block Sundays in range" button). Keep Nth-Sunday quick-toggle (~104-117) + per-Sunday grid (~146-165).
- FREQ_PRESETS/selectRoleTierPreset (~58-97, fn ~398-401): 5 presets weekly/biweek/monthly/fillin/out — extend to write {tier,n} into draft.roleFrequency[roleId] in one write.
- Standing frequencyTargetN write on save to DELETE: lines ~537-541 (rosterStore.updatePerson frequency branch).
- onSave (~525-544): routes through quartersStore.setPersonAvailability — change payload key to roleFrequency.
- Teleport/Transition slide-out markup (~1-31) — do NOT change; it is the pattern plan 16-07 copies.
UI-SPEC copy (keep verbatim): blackout header "Unavailable Sundays" / sub "tap the Sundays they can't serve this quarter"; frequency header "Serve frequency" / sub "quarter tier — one control per role held"; presets "Every week"/"Twice a month"/"Monthly"/"As-needed (fill-in)"/"Out this quarter".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove date-range picker; unify frequency control to write roleFrequency</name>
  <files>src/components/AvailabilityDrawer.vue, src/components/__tests__/AvailabilityDrawer.test.ts</files>
  <read_first>
    - src/components/AvailabilityDrawer.vue (full file — draft model, FREQ_PRESETS, range picker, onSave)
    - src/components/__tests__/AvailabilityDrawer.test.ts (existing cases to update)
    - .planning/phases/16-quarterly-schedule-share-link/16-PATTERNS.md (section "src/components/AvailabilityDrawer.vue" — exact sections to remove/unify)
    - .planning/phases/16-quarterly-schedule-share-link/16-UI-SPEC.md (Copywriting Contract — blackout + frequency copy to keep verbatim)
  </read_first>
  <action>
    Delete the date-range picker block (rangeStart/rangeEnd refs, applyRange fn, and the "Block
    Sundays in range" button, ~118-145) — keep the Nth-Sunday quick-toggle and the per-Sunday grid.
    Change `selectRoleTierPreset(roleId, key)` to write `draft.roleFrequency[roleId] = { tier:
    preset.tier, n: preset.n }` in a single write (replacing the `draft.roleTiers[roleId]=tier`
    step). Initialize `draft.roleFrequency` from the loaded PersonQuarterData.roleFrequency (default
    each held role to {tier:'regular', n:4} when absent). In `onSave`, send `roleFrequency:
    draft.roleFrequency` in the setPersonAvailability payload (in place of frequencyTier/roleTiers)
    and DELETE the standing `rosterStore.updatePerson` frequency-write branch (~537-541) entirely.
    Keep blackout header/sub copy verbatim ("Unavailable Sundays" / "tap the Sundays they can't serve
    this quarter") — this already satisfies R-06 framing. Update AvailabilityDrawer.test.ts: assert no
    range inputs render, per-Sunday toggle still writes blackoutDates, and selecting a frequency preset
    results in a setPersonAvailability payload carrying roleFrequency[roleId]={tier,n} with no standing
    frequency write.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rangeStart\|rangeEnd\|applyRange\|Block Sundays in range" src/components/AvailabilityDrawer.vue` === 0
    - `grep -c "frequencyTargetN\|roleTiers\|frequencyTier" src/components/AvailabilityDrawer.vue` === 0
    - `grep -n "roleFrequency" src/components/AvailabilityDrawer.vue` shows preset write + onSave payload
    - Blackout copy "Unavailable Sundays" present verbatim (R-06)
    - `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts` passes
  </acceptance_criteria>
  <done>No date-range picker; single per-role frequency control writes roleFrequency; no standing frequency write; blackout framed as unavailable Sundays; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add roles-editing checklist to the drawer (D-09 cross-screen roles)</name>
  <files>src/components/AvailabilityDrawer.vue, src/components/__tests__/AvailabilityDrawer.test.ts</files>
  <read_first>
    - src/views/RosterView.vue (the existing roles-checklist markup + its rosterStore.upsertPeople/updatePerson write path — the D-09 template to replicate)
    - src/components/AvailabilityDrawer.vue (draft model + save flow after Task 1)
    - .planning/phases/16-quarterly-schedule-share-link/16-CONTEXT.md (D-09 — roles standing on Person, written through roster store from either surface, identical persisted result)
  </read_first>
  <action>
    Add a roles checklist section to the drawer that lists all configured roles (from rosterStore.roles)
    with checkboxes bound to the person's `roles` array. Toggling a role writes the person's standing
    `roles` through the SAME roster-store path RosterView uses (upsertPeople/updatePerson) — roles stay
    standing on `Person` (D-09), NOT on the quarter. Keep this write separate from the quarter's
    setPersonAvailability call (roles → roster store; frequency/blackout/pairing → quarters store), per
    D-08's separate-schemas rule. Persisted result must be identical to editing roles on the Volunteer
    screen. Extend AvailabilityDrawer.test.ts: toggling a role checkbox calls the roster-store roles
    write with the updated roles array (not the quarters store).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Drawer renders a roles checklist bound to person.roles
    - Toggling a role invokes the roster store (upsertPeople/updatePerson) — `grep -n "upsertPeople\|updatePerson" src/components/AvailabilityDrawer.vue` returns a match in the roles handler
    - Roles write does NOT go through quartersStore.setPersonAvailability (roles stay standing, D-09)
    - `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts` passes
  </acceptance_criteria>
  <done>Roles editable from the Schedule-screen drawer via the roster store, identical to the Volunteer screen; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated editor → quarters/roster stores | Editor-gated writes only; drawer is behind the app shell auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering | dual-write drift (roles via roster store, frequency via quarters store) | mitigate | Writes are to disjoint fields on disjoint docs (Person.roles vs PersonQuarterData.roleFrequency) — no shared field, no drift; D-08 mandates separate schemas |
| T-16-05-02 | Information Disclosure | drawer exposing PII | accept | Drawer runs only inside the authenticated app shell for org editors; no new PII surface |

No package installs in this plan — supply-chain (T-16-SC) not applicable.
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/AvailabilityDrawer.test.ts` green.
- Manual: open drawer for a person — no range inputs; one frequency control per held role; toggling roles reflects on the Volunteer screen after reload.
</verification>

<success_criteria>
- Single per-role frequency control writing roleFrequency; date-range picker removed; blackout framed as unavailable Sundays; roles editable via roster store.
- R-07 cross-screen blackout editing confirmed descoped (D-10): no Volunteer-screen blackout editor added.
</success_criteria>

<output>
Create `.planning/phases/16-quarterly-schedule-share-link/16-05-SUMMARY.md` when done.
</output>
