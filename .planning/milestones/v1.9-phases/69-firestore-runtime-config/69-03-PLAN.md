---
phase: 69-firestore-runtime-config
plan: 03
type: execute
wave: 3
depends_on: [69-02]
files_modified:
  - functions/DEPLOY-RUNTIME-CONFIG.md
autonomous: true
requirements: [R181]
user_setup:
  - service: firebase
    why: "The owner runs the functions deploy that makes the runtime-config reads live in production (v1.9 grant — all deploys owner-run)."
    dashboard_config:
      - task: "Run the handed-over `firebase deploy --only functions:...` command for the 7 managed functions"
        location: "functions/DEPLOY-RUNTIME-CONFIG.md (written by this plan)"
must_haves:
  truths:
    - "A hand-over note records the EXACT `firebase deploy --only functions:...` command listing the 7 managed functions, and states the change ships built + tested + UNDEPLOYED (v1.9 grant) — R181"
    - "The note states an empty-doc deploy is behavior-neutral (defaults-merge) and can/should be deployed alongside the Phase 68 functions"
  artifacts:
    - functions/DEPLOY-RUNTIME-CONFIG.md
  key_links:
    - "The note explicitly states NO .env.local / functions/.env writes are made and RESEND_API_KEY stays a server secret, never in appConfig"
    - "The note mirrors the placement + tone of functions/DEPLOY-SUPER-ADMIN.md (runbook sits next to the code, nothing in-repo runs it)"
---

<objective>
Write `functions/DEPLOY-RUNTIME-CONFIG.md` — the owner hand-over runbook for deploying the Phase 69
functions change. Per the v1.9 standing autonomy grant, the functions change ships built + tested +
UNDEPLOYED; this note hands the owner the exact `firebase deploy --only functions:...` command and the
context to run it safely.

Purpose: v1.9 deploys are owner-run (auth/rules/data-loss categories). Even though an empty-doc deploy
is behavior-neutral (Plan 01's defaults-merge guarantee), the deploy is still handed over, not executed.
This is a NON-executing documentation task — no deploy command is run, no secret/env file is written.

Output: `functions/DEPLOY-RUNTIME-CONFIG.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-firestore-runtime-config/69-CONTEXT.md

# Placement + tone precedent to mirror (runbook next to the code; nothing in-repo runs it):
@functions/DEPLOY-SUPER-ADMIN.md
# The swapped file whose functions get deployed (for the exact managed-function list):
@functions/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write functions/DEPLOY-RUNTIME-CONFIG.md (owner deploy hand-over, non-executing)</name>
  <files>functions/DEPLOY-RUNTIME-CONFIG.md</files>
  <action>
    Create `functions/DEPLOY-RUNTIME-CONFIG.md`, mirroring the structure and tone of
    functions/DEPLOY-SUPER-ADMIN.md (a top banner stating NOTHING in the repo runs any command in the
    file; the artifact ships built + tested + UNDEPLOYED per the v1.9 grant; the runbook sits next to the
    functions code by the same precedent as DEPLOY-SUPER-ADMIN.md / DEPLOY-ORG-CLAIMS.md).

    Content the note MUST contain:
    - WHAT is being rolled out: the Phase 69 config-source swap — the managed cost/cleanup/messaging knobs
      now read from appConfig/global at runtime via getAppConfig(). List the exact managed functions whose
      behavior changed: api, cleanupExpiredMedia, cleanupOrphanRenders, cleanupOrphanBackgrounds,
      cleanupPptxSources, sendScheduledReminders, sendQueuedMessage.
    - WHY it is safe to deploy now: while appConfig/global is absent/empty, the deep-merged defaults
      reproduce today's behavior byte-for-byte (R182) — the deploy is a no-op change until the owner or the
      Phase 70 UI writes a value. It can/should be deployed alongside the Phase 68 functions
      (syncSuperAdminClaim / setSuperAdminClaim) in the same session.
    - Pre-flight: functions suite green (`cd functions && npm test`), functions build clean
      (`cd functions && npm run build`), root type-check clean (`npm run type-check`); confirm the active
      project (`firebase use`, default worship-planner-bc515).
    - The EXACT deploy command (scoped to the 7 managed functions):
      `firebase deploy --only functions:api,functions:cleanupExpiredMedia,functions:cleanupOrphanRenders,functions:cleanupOrphanBackgrounds,functions:cleanupPptxSources,functions:sendScheduledReminders,functions:sendQueuedMessage --project worship-planner-bc515`
      Note that a full `firebase deploy --only functions` deploys the same shared index.ts build and is
      equivalent; the scoped list documents exactly which functions' runtime behavior changed.
    - What to observe: the Functions list shows the 7 functions redeployed with a fresh timestamp; behavior
      is unchanged until a value is written to appConfig/global.
    - Rollback: redeploy the prior index.ts build; harmless because an empty appConfig/global already
      reproduces today's behavior.
    - Explicit constraints (v1.9 grant): this file does NOT instruct writing .env.local or functions/.env;
      RESEND_API_KEY stays a functions server secret and NEVER enters the client-readable appConfig/global
      doc; the R185 deploy-time knobs (AI_PROXY_MAX_INSTANCES / GLOBAL_MAX_INSTANCES / render-service caps)
      are unchanged and still require a redeploy to alter.
    - Deferred manual verification (owner, via /gsd-verify-work 69), recorded per the grant, NOT run here:
      R181 (a live appConfig/global change takes effect on a hot path + on the next cron run with no
      redeploy) and R183 (real cross-instance TTL staleness window on a hot path). Point to
      .planning/phases/69-firestore-runtime-config/69-VALIDATION.md's Manual-Only table.
  </action>
  <verify>
    <automated>test -f functions/DEPLOY-RUNTIME-CONFIG.md && grep -q "firebase deploy --only functions:api,functions:cleanupExpiredMedia,functions:cleanupOrphanRenders,functions:cleanupOrphanBackgrounds,functions:cleanupPptxSources,functions:sendScheduledReminders,functions:sendQueuedMessage" functions/DEPLOY-RUNTIME-CONFIG.md && grep -q "UNDEPLOYED" functions/DEPLOY-RUNTIME-CONFIG.md && grep -q "RESEND_API_KEY" functions/DEPLOY-RUNTIME-CONFIG.md</automated>
  </verify>
  <done>functions/DEPLOY-RUNTIME-CONFIG.md exists, mirrors DEPLOY-SUPER-ADMIN.md's non-executing-runbook tone, lists the 7 managed functions, carries the exact scoped `firebase deploy --only functions:...` command, states the change ships built + tested + UNDEPLOYED and is behavior-neutral on an empty doc, and records the RESEND_API_KEY-stays-secret / no-.env-writes / R185-still-deploy-time constraints and the deferred R181/R183 manual verifications. No deploy command is executed by this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy hand-over (owner-run) → production functions | The note is documentation only; no code in the repo executes any command in it. The owner runs the deploy under the v1.9 grant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-09 | Tampering | firebase deploy hand-over | low | accept | The note is non-executing; the deploy is owner-run per the v1.9 grant; the artifact ships built + tested + UNDEPLOYED — no automated path deploys it |
| T-69-07 | Information Disclosure | note content | low | mitigate | The note explicitly states no .env.local/functions/.env writes are made and that RESEND_API_KEY stays a functions server secret, never entering the client-readable appConfig/global doc |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `functions/DEPLOY-RUNTIME-CONFIG.md` exists with the exact scoped deploy command, the 7 managed
  functions, the built+tested+UNDEPLOYED framing, and the secret/env-safety constraints.
- No deploy command is run by this plan (v1.9 grant — all deploys owner-run).
</verification>

<success_criteria>
- The owner has a self-contained runbook with the exact `firebase deploy --only functions:...` command
  and the safety context to run it (R181's live behavior becomes real once the owner deploys).
- The note records the RESEND_API_KEY-stays-secret / no-.env-writes / R185-still-deploy-time constraints
  and the deferred R181/R183 manual verifications.
</success_criteria>

<output>
Create `.planning/phases/69-firestore-runtime-config/69-03-SUMMARY.md` when done.
</output>