---
phase: 69-firestore-runtime-config
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - functions/src/index.ts
  - functions/src/index.test.ts
autonomous: true
requirements: [R181, R183, R184, R185]
must_haves:
  truths:
    - "Every managed handler (4 cleanup crons, api proxy, sendScheduledReminders, sendQueuedMessage) reads its knob from getAppConfig() — no managed process.env read remains in index.ts — R181"
    - "The 4 cleanup crons + sendScheduledReminders read getAppConfig(db,{fresh:true}); the api proxy + sendQueuedMessage read the cached form — so an emergency disable takes effect on the very next scheduled run — R183"
    - "cleanupOrphanBackgrounds' referencesComplete/floor-guard fail-safes are byte-unchanged and its existing unit tests pass UNCHANGED — the swap is a value-source change only — R190"
    - "AI_PROXY_MAX_INSTANCES and GLOBAL_MAX_INSTANCES still resolve from process.env at module load, unchanged — R185"
    - "MESSAGE_FROM_ADDRESS defineString read is REPLACED outright by config.sender.fromAddress (declaration + read-site both removed) — R181"
  artifacts:
    - functions/src/index.ts
    - functions/src/index.test.ts
  key_links:
    - "index.ts imports getAppConfig/DEFAULT_APP_CONFIG/AppConfig from ./appConfig (Plan 01)"
    - "sendQueuedMessageHandler calls getAppConfig ONCE before the recipient loop, never per-recipient"
    - "index.test.ts mocks ./appConfig via vi.mock (mirrors the existing ./pptxParser / ./renderInvoker mocks at index.test.ts:132-140); ~80+ env-mutation tests convert to config-object injection with assertions UNCHANGED"
---

<objective>
Swap the 17 enumerated managed read-sites in `functions/src/index.ts` from `process.env`/`defineString`
to `getAppConfig()` (Plan 01's reader), routing the four cleanup crons + `sendScheduledReminders`
through `{fresh:true}` and the `api` proxy + `sendQueuedMessage` through the cached form (R181, R183).
Convert `functions/src/index.test.ts`'s ~80+ env-var `beforeEach`/`afterEach` mutations to mocking the
`./appConfig` module — assertions and business logic unchanged (R190-preserving).

Purpose: This is the mechanical-but-precise value-source swap that makes a config change take effect
with no redeploy for the managed knobs. It is behavior-preserving while `appConfig/global` is empty
(Plan 01's defaults-merge guarantee), so it is a no-op change until the owner or Phase 70 writes a value.

Output: `functions/src/index.ts` (17 read-sites swapped, MESSAGE_FROM_ADDRESS replaced, R185 exclusions
untouched, R190 fail-safes byte-unchanged) and `functions/src/index.test.ts` (env→config-mock setup).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/69-firestore-runtime-config/69-RESEARCH.md
@.planning/phases/69-firestore-runtime-config/69-PATTERNS.md
@.planning/phases/69-firestore-runtime-config/69-CONTEXT.md
@.planning/phases/69-firestore-runtime-config/69-VALIDATION.md

# The reader this plan swaps to (Plan 01 output — read its exports/signature):
@functions/src/appConfig.ts
# The file being modified. Use RESEARCH's read-site table (17 rows, file:line) + PATTERNS' swap
# guidance to jump to each site — do NOT full-load the 3,124-line file:
@functions/src/index.ts
@functions/src/index.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ./appConfig test seam + reimplement the env-wrapper helpers over resolved config</name>
  <files>functions/src/index.ts, functions/src/index.test.ts</files>
  <action>
    In index.ts, add `import { getAppConfig, DEFAULT_APP_CONFIG, type AppConfig } from "./appConfig"`.

    DECISION (resolved open question 2 — record here and honor over RESEARCH's "retire them"
    recommendation): KEEP the existing env-wrapper functions exported and reimplement each as a thin
    passthrough over a resolved AppConfig instead of reading process.env. Do NOT delete-and-inline them
    (that balloons the diff and test churn). Convert these six: readDeleteCap (index.ts:961-964),
    readMediaRetentionDays (1009-1011), readOrphanRenderStaleHours (~1168-1170), readBackgroundRetentionDays
    (~1386-1388), readPptxSourceRetentionDays (~1632-1634), and readAiProxyLimits (209-226). Change each
    signature to take the resolved config (e.g. readMediaRetentionDays(config: AppConfig) => config.retention.mediaDays;
    readDeleteCap(config) => config.deleteCapPerRun; readAiProxyLimits(config) => remap config.aiProxy to
    the existing AiProxyLimits {maxPerMin,maxPerDay,allowedModels,maxTokensCeiling} shape). A wrapper
    becoming a trivial passthrough is fine. The coercion/default logic now lives in appConfig.ts, so any
    wrapper test that specifically asserted PARSING (e.g. readAiProxyLimits' comma-split/allow-list
    fallback) is now covered by 69-01's appConfig.test.ts — convert those to passthrough assertions.

    KEEP readNumericKnob unchanged (index.ts:193-207): it is a pure string-parser still used by the
    R185-excluded module-scope constants AI_PROXY_MAX_INSTANCES (237) / GLOBAL_MAX_INSTANCES (246) — those
    two lines and setGlobalOptions stay exactly as-is (R185). Its own describe() block stays unchanged.

    In index.test.ts, add `vi.mock("./appConfig", () => ({ getAppConfig: vi.fn(), DEFAULT_APP_CONFIG: <the
    real default object> }))` mirroring the existing sibling-module mocks at index.test.ts:132-140
    (./pptxParser, ./renderInvoker). Import DEFAULT_APP_CONFIG so tests can build an override object by
    spreading it. Convert the six wrapper-helper describe() blocks 1:1 from process.env mutation to calling
    the wrapper with a config object (spread DEFAULT_APP_CONFIG with the tested override); delete their
    now-obsolete process.env set/delete lines from beforeEach/afterEach.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/index.test.ts -t "readNumericKnob" && cd functions && npm run build</automated>
  </verify>
  <done>index.ts imports the ./appConfig reader; the six env-wrapper helpers take a resolved AppConfig and return the corresponding knob (no process.env inside them); readNumericKnob + the two *_MAX_INSTANCES module-scope reads are untouched; index.test.ts has the ./appConfig vi.mock seam and the six wrapper describe() blocks converted to config-injection; functions build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the four cleanup handlers to getAppConfig({fresh:true}); preserve R190 byte-for-byte</name>
  <files>functions/src/index.ts, functions/src/index.test.ts</files>
  <action>
    Swap the four cleanup cron handlers (RESEARCH read-site rows 5-13) so each resolves
    `const config = await getAppConfig(db, { fresh: true })` ONCE at the top and reads its knobs from it.
    Reuse each handler's existing getFirestore()/db binding — do NOT call getFirestore() twice.

    cleanupExpiredMediaHandler (~1038): dryRun becomes `!config.cleanup.mediaEnabled`; retention from
    readMediaRetentionDays(config); delete cap from readDeleteCap(config). PITFALL 2: this handler
    previously imported NO Firestore API — adding getAppConfig(db) introduces its first Firestore read.
    Update its safety doc-comment (index.ts:977-983 region) to state it now reads appConfig/global via the
    Admin SDK (bypasses rules) but still touches NO slide/service/song documents — correct the letter of
    the "no Firestore" claim while preserving its spirit. Do NOT write the literal process.env.<VARNAME>
    into the rewritten comment (comment-text discipline).

    cleanupOrphanRendersHandler (~1200): dryRun becomes `!config.cleanup.pptxRenderEnabled`; stale hours
    from readOrphanRenderStaleHours(config); cap from readDeleteCap(config).

    cleanupOrphanBackgroundsHandler (index.ts:1437-1521) — THE R190 ONE-LINE SWAP. Move the existing
    getFirestore() binding above the fail-safe block, add `const config = await getAppConfig(db,
    {fresh:true})`, and change ONLY the enable-flag line (1440) to `const dryRun = !config.cleanup.backgroundEnabled`.
    Everything from referencedPaths/referencesComplete (1443) through the floor guard (1499-1503) and
    `effectiveDryRun = dryRun || !referencesComplete` (1505) and BACKGROUND_PATH_GUARD is UNCHANGED — this
    is the exact scope boundary R190 protects. Its existing unit tests MUST pass with assertions unchanged.

    cleanupPptxSourcesHandler (~1668): dryRun becomes `!config.cleanup.pptxSourceEnabled`; retention from
    readPptxSourceRetentionDays(config); cap from readDeleteCap(config).

    In index.test.ts, convert the four cleanup describe() blocks: replace each `process.env.X_CLEANUP_ENABLED
    = "true"` / retention / cap mutation with `vi.mocked(getAppConfig).mockResolvedValue({ ...DEFAULT_APP_CONFIG,
    cleanup: { ...DEFAULT_APP_CONFIG.cleanup, <flag>: true }, retention: {...}, deleteCapPerRun: N })`;
    reset it in afterEach via vi.mocked(getAppConfig).mockReset(). The fake-db builders, file fixtures, and
    ALL assertions (referencesComplete, floor-guard, dry-run-never-capped, R167/R190 cases) stay unchanged
    (PATTERNS converted-shape sample). Convert ALL blocks in this pass — a partial conversion leaves a
    half-migrated file where a test passes for the wrong reason (RESEARCH Pitfall 1).
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/index.test.ts -t "cleanup" && cd functions && npm run build</automated>
  </verify>
  <done>All four cleanup handlers read enable/retention/cap from getAppConfig(db,{fresh:true}); cleanupOrphanBackgrounds' referencesComplete/floor-guard block is byte-unchanged and its existing tests pass with assertions unchanged (R190); cleanupExpiredMedia's safety comment is corrected; the four cleanup describe() blocks are converted env→config-mock; functions build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Swap api proxy (cached), messaging cron gate ({fresh:true}), sendQueuedMessage (cached) + sender address; finalize R181/R185 gates</name>
  <files>functions/src/index.ts, functions/src/index.test.ts</files>
  <action>
    api proxy (RESEARCH read-site rows 1-4, ~index.ts:553): resolve `const config = await
    getAppConfig(getFirestore())` (CACHED — no fresh flag; getFirestore() is already called later in this
    handler, so no new dependency class) and read AI limits via readAiProxyLimits(config). Anti-pattern to
    avoid: do not add {fresh:true} here — the hot path uses the TTL cache (R183).

    runScheduledMessagingCron (index.ts:1983-1988, read-site row 14): convert its `env: NodeJS.ProcessEnv =
    process.env` DI param to `db: Firestore = getFirestore()` (mirrors the file's existing db/now DI
    convention), then `const config = await getAppConfig(db, {fresh:true})` and gate on
    `!config.messaging.scheduledCronEnabled`. This single gate covers BOTH the reminder sweep and the
    dispatch sweep. Update the caller at ~2013 (sendScheduledReminders onSchedule) accordingly. This is a
    cron path → {fresh:true} (R183), so an emergency disable takes effect on the next scheduled run.

    sendQueuedMessageHandler (read-site rows 15-17): resolve `const config = await getAppConfig(db)` (CACHED)
    ONCE at the top of the handler, BEFORE any per-recipient loop (RESEARCH Anti-Pattern: never call inside
    the loop — the loop can iterate up to maxRecipients recipients). Read maxRecipients from
    config.messaging.maxRecipients (was 2744) and the org daily quota from config.messaging.orgDailyEmailQuota
    (was 2745). REPLACE the sender read at 2822 (`bareEmailAddress(MESSAGE_FROM_ADDRESS.value())`) with
    `bareEmailAddress(config.sender.fromAddress)`, and REMOVE the now-unused MESSAGE_FROM_ADDRESS defineString
    declaration at 2487 outright — do NOT leave it as a competing fallback (RESEARCH Pattern 4: two sources
    of truth, the defineString silently winning). Leave the OTHER two defineStrings (SERVICE_SHARE_BASE_URL,
    PPTX_RENDER_SERVICE_URL) untouched — out of scope per CONTEXT.md. Do NOT wire config.sender.fromName —
    it stays dormant this phase (resolved decision 1); the org-name display-name logic is unchanged.

    In index.test.ts, convert the api-proxy, runScheduledMessagingCron/sendScheduledReminders, and
    sendQueuedMessage describe() blocks from env-mutation to vi.mocked(getAppConfig).mockResolvedValue(...)
    with the tested overrides; assertions unchanged. Add at least one R181 "reads from config" assertion per
    handler group proving the handler's behavior is driven by the mocked getAppConfig return (VALIDATION.md
    `-t "reads from config"`). Confirm the R185 regression: the existing setGlobalOptions/*_MAX_INSTANCES
    test still passes unchanged.

    After the swap, confirm no managed knob still reads process.env: the ONLY remaining `process.env.`
    code references in index.ts must be the two *_MAX_INSTANCES lines (237, 246). Do not reference
    process.env.<VARNAME> for any managed knob in a comment.
  </action>
  <verify>
    <automated>cd functions && npm test && cd functions && npm run build && npm run type-check</automated>
  </verify>
  <done>api proxy + sendQueuedMessage read the CACHED config; the four cleanup crons + sendScheduledReminders read {fresh:true}; MESSAGE_FROM_ADDRESS declaration + read-site removed and replaced by config.sender.fromAddress; the only remaining process.env. code reads in index.ts are AI_PROXY_MAX_INSTANCES / GLOBAL_MAX_INSTANCES (R185, verified by `grep -n "process\.env\." functions/src/index.ts` showing exactly those two lines); the R185 setGlobalOptions test passes unchanged; full functions suite green; functions build + root type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| appConfig/global → managed handlers | Handlers read config via the Admin SDK (server-trusted); the destructive cleanup crons must never read a stale value. |
| TTL cache (per-instance memory) → hot-path consumers | The api/sendQueuedMessage cached read tolerates ≤60s staleness by design; cost guardrails, not security controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-04 | Tampering (mitigated by design) | cleanup crons + sendScheduledReminders | high | mitigate | Every destructive/cron path reads getAppConfig(db,{fresh:true}) — no stale-cache window; an emergency disable (flag → false) takes effect on the very next scheduled run (R183) |
| T-69-06 | Elevation of Privilege | cleanupOrphanBackgrounds R190 fail-safes | high | mitigate | The referencesComplete/floor-guard block is byte-unchanged; the swap is a value-source change to the enable flag only; existing tests pass with assertions unchanged — a song-linked background remains structurally undeletable even if the flag is somehow true |
| T-69-07 | Information Disclosure | sendQueuedMessage send path | medium | mitigate | RESEND_API_KEY stays a functions secret; only the non-secret config.sender.fromAddress is sourced from appConfig — no secret is read from or written to the client-readable doc |
| T-69-08 | Tampering | R185 excluded knobs | medium | accept | AI_PROXY_MAX_INSTANCES / GLOBAL_MAX_INSTANCES stay process.env module-load values (Cloud Functions v2 deploy-time) — not made client-editable; regression-guarded by the unchanged setGlobalOptions test |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd functions && npm test` green (all converted cleanup/proxy/messaging/send tests + the unchanged R190 and R185 blocks).
- `cd functions && npm run build` clean AND `npm run type-check` clean (the functions-standalone tsc gate the root vue-tsc misses per CLAUDE.md, plus the root vue-tsc --build).
- `grep -n "process\.env\." functions/src/index.ts` → exactly the two *_MAX_INSTANCES lines (R181/R185).
- `grep -n "MESSAGE_FROM_ADDRESS" functions/src/index.ts` → no matches (declaration + read-site removed).
- R181 live no-redeploy + R183 real cross-instance staleness are DEPLOY-dependent → manual UAT, deferred to `/gsd-verify-work 69` (VALIDATION.md Manual-Only table).
</verification>

<success_criteria>
- All 17 managed read-sites read from getAppConfig(); no managed process.env read remains (R181).
- Correct cache routing: crons {fresh:true}, hot paths cached (R183).
- R190 fail-safes byte-unchanged, tests unchanged; R185 exclusions untouched; MESSAGE_FROM_ADDRESS replaced.
- Full functions suite + functions build + root type-check all green.
</success_criteria>

<output>
Create `.planning/phases/69-firestore-runtime-config/69-02-SUMMARY.md` when done.
</output>
