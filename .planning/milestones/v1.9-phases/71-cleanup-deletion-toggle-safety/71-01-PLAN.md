---
phase: 71-cleanup-deletion-toggle-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/index.ts
  - functions/src/index.test.ts
autonomous: true
requirements: [R188, R190]
must_haves:
  truths:
    - "A super-admin can fetch a truthful would-delete count + bytes for any of the 4 cleanup types on demand, forced dry-run regardless of the stored *_CLEANUP_ENABLED flag (R188)"
    - "A non-super-admin caller is rejected server-side by TWO independent re-checks (ID-token claim AND fresh superAdmins/{uid} read) and never receives a count (R188)"
    - "The preview NEVER deletes, even when getAppConfig is mocked with the cleanup flag ENABLED (forced dry-run short-circuits before any delete branch) (R188)"
    - "The backgrounds referencesComplete/floor-guard fail-safes are byte-identical; the existing cleanupOrphanBackgroundsHandler test block passes with ZERO edits (R190)"
    - "The backgrounds preview surfaces referencesComplete via the SAME scan a live run uses (R190)"
  artifacts:
    - "functions/src/index.ts — four cleanup handlers gain optional {forceDryRun?} param; new previewCleanupDryRun onCall export"
    - "functions/src/index.test.ts — new previewCleanupDryRun describe block"
  key_links:
    - "previewCleanupDryRun dispatch → correct per-type summary field: backgrounds→orphanCount, media/orphanRenders/pptxSources→deletedObjectCount"
    - "opts.forceDryRun === true ternary evaluated FIRST, before the !config.cleanup.*Enabled read"
---

<objective>
Add the on-demand dry-run blast-radius preview for the four `*_CLEANUP_ENABLED` cleanups (R188), and
prove the song-linked-background fail-safes are untouched (R190). This is the server half of Phase 71:
a `{forceDryRun}` seam OR'd into each handler's single dry-run line, plus a super-admin-gated
`previewCleanupDryRun` onCall that dispatches to the four handlers with `forceDryRun:true` and maps
each summary onto `{wouldDeleteCount, wouldDeleteBytes, referencesComplete?}`.

Purpose: Give the Owner Console a truthful count of exactly what a cleanup would delete right now —
computed by the SAME scan/reference-detection the real cron uses — without ever deleting anything, so
no flag is ever flipped on blind.

Output: Modified `functions/src/index.ts` (four handler signatures + one dry-run line each + the new
callable) and a new `previewCleanupDryRun` test block in `functions/src/index.test.ts`.

Identity model — assumption-delta: NO CHANGE. Phase 71 introduces no new identity model; it adds a
preview+confirm gate in front of an existing flag and reuses the Phase 68 super-admin re-check verbatim.
The delta detector fired on a false positive (the word "custom" quoted from "superAdmin custom claim"
in the deploy-discipline text). No promote / add-alongside applies.

Deploy discipline (v1.9 grant): `previewCleanupDryRun` is a functions change → ships built + tested +
UNDEPLOYED. The deploy command (`firebase deploy --only functions:previewCleanupDryRun`) is owner
hand-over, folded into the milestone-end deploy hand-over (per STATE.md's standing autonomy grant — do
NOT deploy here, do NOT write `functions/.env`). Enabling a cleanup in production remains the owner's
button.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-cleanup-deletion-toggle-safety/71-RESEARCH.md
@.planning/phases/71-cleanup-deletion-toggle-safety/71-PATTERNS.md
@.planning/phases/71-cleanup-deletion-toggle-safety/71-VALIDATION.md

# Source of the exact seam + the auth re-check to mirror (read before editing):
@functions/src/index.ts
@functions/src/superAdminClaims.ts
@functions/src/superAdminClaims.test.ts
@functions/src/index.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add {forceDryRun} param to the four cleanup handlers (one line each)</name>
  <files>functions/src/index.ts</files>
  <action>
Add an optional `opts: { forceDryRun?: boolean } = {}` parameter to each of the four cleanup handler
signatures, and OR it into that handler's SINGLE existing dry-run line using a forceDryRun-FIRST
ternary. Exact edits (see 71-PATTERNS.md "the four cleanup handlers" for the verbatim before/after):

- `cleanupExpiredMediaHandler` (signature line 1053): line 1059 `const dryRun = !config.cleanup.mediaEnabled;`
  becomes `const dryRun = opts.forceDryRun === true ? true : !config.cleanup.mediaEnabled;`
- `cleanupOrphanRendersHandler` (line 1218): line 1224 `!config.cleanup.pptxRenderEnabled` → same ternary form.
- `cleanupOrphanBackgroundsHandler` (line 1461): line 1467 `!config.cleanup.backgroundEnabled` → same ternary form.
- `cleanupPptxSourcesHandler` (line 1690): line 1696 `!config.cleanup.pptxSourceEnabled` → same ternary form.

Per R188, the ternary MUST test `opts.forceDryRun === true` FIRST — never an `||` form where a falsy
config read could slip through before the force is evaluated (the single most load-bearing safety
property; see 71-PATTERNS.md "forceDryRun-first ternary").

R190 HARD CONSTRAINT — for `cleanupOrphanBackgroundsHandler`, the ONLY changes are the signature (add
`opts`) and the one `dryRun` line (1467). `referencesComplete` (1469-1528), the floor-guard block
(1527-1529), and `effectiveDryRun = dryRun || !referencesComplete` (1531) are BYTE-IDENTICAL — do NOT
"clean up the boolean logic while in there" (RESEARCH Pitfall 2). Prove this by diff-review, not a
re-read.

The four `onSchedule` wrappers (`cleanupExpiredMedia`, `cleanupOrphanRenders`, `cleanupOrphanBackgrounds`,
`cleanupPptxSources`) still call each handler with zero args, so `opts` defaults to `{}`,
`opts.forceDryRun` is `undefined`, and the real cron behavior is byte-identical to today. Leave the
wrappers untouched.
  </action>
  <verify>
    <automated>cd functions && npm run build && npx vitest run index.test.ts -t "cleanupOrphanBackgroundsHandler"</automated>
  </verify>
  <done>
Functions build is clean (standalone tsc). The existing `cleanupOrphanBackgroundsHandler` describe block
passes with ZERO edits to any test (R190 acceptance). All four handlers now accept `{forceDryRun}` and
short-circuit dry-run to true when it is set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add previewCleanupDryRun onCall + its test block</name>
  <files>functions/src/index.ts, functions/src/index.test.ts</files>
  <action>
Add `previewCleanupDryRunHandler` + `export const previewCleanupDryRun = onCall(previewCleanupDryRunHandler)`
to `functions/src/index.ts` (onCall/HttpsError/CallableRequest are already imported at line 1). Use the
recommended shape in 71-RESEARCH.md "The previewCleanupDryRun onCall" / 71-PATTERNS.md verbatim:

- Types: `CleanupPreviewType = "media" | "orphanRenders" | "backgrounds" | "pptxSources"`,
  `PreviewCleanupDryRunRequest { type }`, `PreviewCleanupDryRunResponse { wouldDeleteCount; wouldDeleteBytes; referencesComplete? }`.
- Super-admin caller re-check VERBATIM from `setSuperAdminClaimHandler` (superAdminClaims.ts:106-128):
  (1) `!request.auth` → HttpsError('unauthenticated','Sign in required.'); (2) `request.auth.token.superAdmin !== true`
  → HttpsError('permission-denied','You must be a super-admin.'); (3) fresh
  `getFirestore().collection("superAdmins").doc(request.auth.uid).get()`, `!callerDoc.exists` →
  same permission-denied. Do NOT trust a client-declared flag.
- Validate `type` against the 4-value allow-list; invalid → HttpsError('invalid-argument', message that
  lists ONLY the 4 valid values, no internal detail leaked).
- `switch (type)`: dispatch to the matching handler with `{ forceDryRun: true }` and map its summary per
  the PER-TYPE FIELD TABLE (RESEARCH Pitfall 1 / 71-PATTERNS.md): media/orphanRenders/pptxSources →
  `wouldDeleteCount: s.deletedObjectCount`; backgrounds → `wouldDeleteCount: s.orphanCount` (NOT
  deletedObjectCount — it stays 0 in dry-run) plus `referencesComplete: s.referencesComplete`. All four
  → `wouldDeleteBytes: s.deletedBytes`.
- Belt-and-suspenders (RESEARCH open question, one line): after each handler call, assert the returned
  summary's dry-run flag is true (`s.dryRun` for the three, the backgrounds summary's dry-run field for
  backgrounds) and throw an Error if not — so a future edit that breaks the ternary can never make the
  preview delete.

Then add a new `describe("previewCleanupDryRun", ...)` block in `functions/src/index.test.ts`, reusing
the file's existing `mockBucket`/`fakeFile`/`mockBackgroundDb`/`fakeBackgroundFile` helpers and mirroring
`superAdminClaims.test.ts`'s `fakeRequest` + superAdmins-doc mock shape for the auth cases. Cover, per
71-VALIDATION.md's map: 3 auth-reject cases (no auth / token.superAdmin !== true / superAdmins/{uid}
missing); invalid `type` → invalid-argument; 4 per-type dispatch + field-mapping cases (assert the mapped
`wouldDeleteCount` equals the number of orphan/candidate files the mock was given — especially backgrounds
→ orphanCount, not deletedObjectCount); the LOAD-BEARING "never deletes even when getAppConfig is mocked
cleanup-ENABLED" case (mock the flag true, invoke through the handler, assert no `file.delete()` fired AND
a real would-delete count still returned); and the backgrounds `referencesComplete:false` pass-through
case (reuse `mockBackgroundDb({ slideGroupsThrows: true })`).

Do NOT edit the existing `cleanupOrphanBackgroundsHandler` describe block (index.test.ts:1315+) — R190
requires it pass unchanged.
  </action>
  <verify>
    <automated>cd functions && npm run build && npm test</automated>
  </verify>
  <done>
`previewCleanupDryRun` exists and is super-admin-gated. The new describe block is green: all 3 auth
rejections, invalid-type, the 4 per-type field mappings (backgrounds via orphanCount), the never-deletes
case, and the backgrounds referencesComplete pass-through all pass. The full functions suite is green
and the existing backgrounds block required zero edits (R190). Callable ships UNDEPLOYED (owner hand-over).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `previewCleanupDryRun` callable | Any authenticated client can invoke the callable; the caller's declared privilege is untrusted and re-verified server-side |
| callable → cleanup handler | The callable forces `forceDryRun:true`; the handler's delete branches are structurally unreachable for the preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-01 | Elevation of Privilege | `previewCleanupDryRunHandler` caller check | high | mitigate | Two independent server-side re-checks — ID-token `superAdmin` claim AND a fresh `superAdmins/{uid}` Firestore read — verbatim from `setSuperAdminClaimHandler`; client gating is explicitly insufficient. Covered by the 3 auth-reject tests. |
| T-71-02 | Tampering | forced dry-run path in the four handlers | high | mitigate | `opts.forceDryRun === true ? true : ...` ternary short-circuits `dryRun`/`effectiveDryRun` to true before any delete branch; plus the post-dispatch `if (!s.dryRun) throw` assertion. Covered by the "never deletes even when live-enabled" test. |
| T-71-03 | Information Disclosure | invalid-`type` error message | low | accept | `invalid-argument` message lists only the 4 valid type strings — no internal path/detail leaked. Covered by the invalid-type test. |
| T-71-SC | Tampering (supply chain) | npm installs | low | accept | No new packages introduced this phase (zero `npm install`); Package Legitimacy Audit N/A per 71-RESEARCH.md. |
</threat_model>

<verification>
- Functions build clean: `cd functions && npm run build`.
- Full functions suite green: `cd functions && npm test`.
- R190 regression: `cd functions && npx vitest run index.test.ts -t "cleanupOrphanBackgroundsHandler"` passes with zero test edits.
- Diff-review `cleanupOrphanBackgroundsHandler`: only the signature + the one `dryRun` line changed; `referencesComplete`/`effectiveDryRun`/floor-guard byte-identical.
</verification>

<success_criteria>
- R188: `previewCleanupDryRun` returns a truthful `{wouldDeleteCount, wouldDeleteBytes, referencesComplete?}` per type, forcing dry-run regardless of the stored flag, super-admin only.
- R190: the backgrounds fail-safes are untouched (existing tests pass unchanged); the backgrounds preview surfaces `referencesComplete` via the same scan a live run uses.
- The callable never deletes, proven by the load-bearing "mocked cleanup-ENABLED" test.
- Ships built + tested + UNDEPLOYED; deploy command handed to the owner (no deploy, no `.env` writes here).
</success_criteria>

<output>
Create `.planning/phases/71-cleanup-deletion-toggle-safety/71-01-SUMMARY.md` when done.
</output>
