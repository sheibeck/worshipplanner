# v2.15 Deferred Verification — Batched Human/Browser/Hardware UAT

> **CLOSED 2026-09-21 — items below WAIVED by the owner at the v2.15 milestone close** (`/gsd-complete-milestone v2.15`,
> owner choice: "Waive and close"). Every automated gate passed (audit 13/13 reqs · 14/14 integration · 3/3 flows;
> 5/5 phases SECURED + Nyquist-compliant). The unticked items were **never owner-run**; they are waived, not
> accepted-as-verified. The owner has used prod since 2026-09-18 (feedback → backlog 999.6, quick 260919-k9j/-mvw).
> Anything that surfaces later is a quick task. File archived to `milestones/`.

**Owner instruction (2026-09-09):** "use gsd-autonomous. defer uat to the end."

Every v2.15 phase is built and **auto-verified green** (unit tests + `npm run type-check` + code review +
nyquist validation + security audit). The items below are the checks automated tests **cannot** close —
real-browser behavior, multi-monitor hardware, and human visual/UX judgement. They are batched here for a
single owner pass at milestone end. **Status is PENDING** — nothing here is owner-accepted until the owner
runs it. The milestone stays OPEN (not archived) until this pass is done.

> **Deployed to production 2026-09-18** (hosting + storage rules, owner-confirmed per-deploy) so the Storage
> round-trip items below can be run against prod. UAT items remain PENDING until the owner runs them.

Resume any phase's formal verification with `/gsd-verify-work <phase>`.

---

## Phase 138 — Field Fixes: Church-Picker Deep-Link & Service-Update Email Link

Auto-verified: 7/7 must-haves, SECURED (5/5 threats), nyquist validated. Automated unit coverage exists for
all storage-helper logic.

- [~] **Church-picker new-tab restore (R428)** — as a **multi-church** member, right-click a nav link (or
  paste a deep link) to open it in a **genuinely new browser tab**. Expected: the tab lands on the intended
  page, **not** `/select-church`. (jsdom can't model a new tab's empty sessionStorage vs. shared
  localStorage — this is the one gap unit tests can't close.)
  - [~] Also confirm **sign-out** on a shared computer leaves no leftover church for the next user.
  - [~] Confirm a **single-church** user is unaffected (goes straight in).
- [~] **Service-update email plan link (R434)** — trigger a re-lock notification email for a service and
  confirm the delivered email contains a working "View the full plan" link that opens the correct service
  plan. (Automated tests confirm the token is appended + resolved server-side; this confirms real delivery.)

---

## Phase 139 — Rehearsal & Report Times

Auto-verified: 6/6 must-haves, SECURED (7/7 threats), nyquist validated. Code review found + fixed 2
criticals (undated-rehearsal "Invalid Date" on the public page; editor Times UI exposed to non-editors).
Automated unit coverage exists for the data model, sort/format util, copy-not-live-bind, both projections,
and the editor authorization gate.

- [~] **Org defaults → new-service pre-fill (R429)** — in Settings, set default rehearsal time(s) + a
  default report time; **reload** to confirm they persist; then **create a new service** and confirm the
  rehearsal rows (default time, blank date) + report time pre-fill. Confirm editing the org default later
  does NOT change an already-created service.
- [~] **Editor Times subsection (R430/R431)** — add/edit/remove multiple dated rehearsals + set a report
  time on a service; confirm they autosave and display chronologically. Confirm a **locked** service (or a
  non-editor) shows the times **read-only** (no add/remove controls).
- [~] **Cross-surface display (R433)** — with a service that has 2 rehearsals + a report time, confirm the
  times show alongside the date, correctly 12-hour formatted, on: the **dashboard**, the **services-list
  card**, **My Schedule**, the **volunteer service view**, and the **public share/plan page**.

---

## Phase 140 — Vamps Library: CRUD & Storage

Auto-verified: 9/9 must-haves, SECURED (8/8 threats), nyquist validated (61 app-side tests in 7 files; rules
suite 310/310 with the emulator). Code review found + fixed 1 critical (silent MP3 upload failure — no
progress/error UI) and 2 warnings (VampTable given a pre-filtered list, so a no-match search showed the
"library is empty" state; MP3 replace/remove orphaned the superseded Storage object). Automated coverage
exists for the store CRUD, path/validation helpers, storage rules, church-switch reset, and component wiring.

- [~] **Vamps tab visual fidelity (R436)** — open **Songs → Vamps**. Confirm the tab bar (with count badges),
  the flat one-vamp-per-row table (Vamp · Key · Tempo · Audio, amber "No MP3 attached" state), and the
  slide-out editor (Name, 12-key chip picker, Tempo, MP3 drop-zone/player, Delete) match `Vamps.dc.html`
  Turn-12 in the app's dark gray-950 language. Search by **name** and by **key** and confirm a no-match
  search shows "No vamps match your search" (not the empty-library CTA).
- [~] **Real MP3 round-trip in a deployed environment (R435)** — create a vamp (drawer stays open after
  save), attach a real MP3 (≤50MB) and confirm upload progress, then playback and download. **Replace** it
  with a second MP3 and **remove** it; confirm the superseded object is gone from Storage (Firebase console
  → `orgs/{orgId}/vamp-files/…`). Attach a non-MP3 or an oversize file and confirm a visible error.
  Delete a vamp via the inline confirm. (The Storage emulator cannot model prod upload/CORS behavior.)
- [~] **Church switch (R435)** — as a multi-church editor, open the Vamps tab in Church A, switch to Church
  B via the sidebar, confirm no Church-A vamp remains listed.

---

## Phase 141 — Vamp Slide Assignment & Live Playback

Auto-verified: 5/5 must-haves, SECURED (18/18 threats — 10 mitigated with code evidence, 8 accepted),
nyquist validated (0 gaps; 11 phase test files, 699/699). Code review found + fixed 4 warnings over 3
iterations (picker open-state reset on drawer close, Cancel affordance on the inline picker, failed
assign/clear and rejected delete writes now surface a visible error instead of failing silently).
Full-suite regression gate 2026-09-18: 238/239 files, 5,864 tests passed, 0 failed (baseline-only failure);
`npm run type-check` clean. Owner decision (2026-09-13): vamp audio plays ONLY from the Run control window
— Audience, Confidence and Video outputs are structurally silent (`suppressAudio`). Formal items are
mirrored in `phases/141-…/141-UAT.md`.

- [~] **Real-speaker / fresh-profile audibility (R438/R439)** — in a **fresh browser profile** (no prior
  autoplay grant), open Run the Service on a service whose first slide has a vamp assigned. Do **not**
  click the arm toggle: advance to the vamp slide and confirm **no sound** and the header still reads
  "Audio: Off". Click **"Audio: Armed"**: the vamp plays **audibly from the control machine only**, loops,
  and stops on slide change / Go to black / Run exit. With Audience + Confidence + Video outputs open on the
  other monitors, confirm they are **silent** for the same slide — no echo / triple-play.
- [~] **Blocked-audio warning + retry (R439)** — with audio still un-armed in a real session, get the browser
  to reject `play()` (advance to a vamp slide before any gesture): confirm the persistent amber
  **"Audio blocked — click to play"** banner appears on the control screen, then click **Play audio** and
  confirm playback starts and the banner clears. Never a silent failure.
- [~] **Visual fidelity vs 141-UI-SPEC.md (R437/R439/R440)** — in the slide drawer: "Choose a vamp" picker
  (name · key · tempo, no-MP3 rows disabled, search, Cancel), the assigned row "Vamp: {label}" with
  Change / Clear, and the "(no longer in library)" stale hint; in Run: the "Audio: Off / Armed" toggle
  (amber pulse when the live/next slide carries audio while Off), playing dot, "Audio unavailable"
  indicator, and the **♪ Vamp** badges on the live/next preview panes; in Songs → Vamps: the delete-confirm
  warning line. All in the app's dark gray-950 language.
- [~] **Delete-after-assign keeps the MP3 (R440, real Firestore/Storage)** — assign a vamp to a slide in an
  **upcoming** service, then delete that vamp from the library: the confirm reads "Assigned in 1 upcoming
  service — those slides keep their audio"; after deleting, re-open the slide: it still plays (MP3 kept in
  Storage — check `orgs/{orgId}/vamp-files/…` in the console) and the drawer shows "Vamp: {label}
  (no longer in library)"; Clear still works. Then delete an **unassigned** vamp and confirm its MP3 **is**
  removed from Storage.

---

## Phase 142 — Slides Tab panel UX from Claude Design (added 2026-09-19)

Auto-verified: 17/17 must-haves, SECURED (19/19 threats — 14 mitigated with code evidence, 5 accepted),
nyquist validated (0 gaps; 12 map rows green, full suite at the 2-file baseline 240/241 files · 5965 tests,
`npm run type-check` clean). Code review clean after 1 warning fixed (WR-01: an open popover survived the
service locking mid-edit — now force-closes). UI audit 21/24; the two actionable fixes (keyboard focus rings,
literal `·`) were applied inline. **Deployed to production 2026-09-19** (owner-confirmed; `firebase deploy --only hosting` → bundle `index-D9t7Dynj.js` from `9b50ebd0`, together with quick 260919-k9j's Run fixes: on-screen LIVE red, Audio defaults On, End-of-item click). The browser items below can be run against prod.
Formal items are mirrored in `phases/142-…/142-UAT.md`. Design source: `docs/design/slides-tab.dc.html`
Turn 7 variant **7a** (three chips, each its own popover), owner-chosen at discuss.

- [~] **Chip row visual fidelity vs 142-UI-SPEC.md / mockup 7a** — on a group: one 34px row reading
  `Display · Full-screen ▾` · `Background · Add ▾` (dashed) · `Audio · Add ▾` (dashed), Loop / Congregational /
  Remove-imported trailing, and the muted "applies to all N slides in this group, unless a slide sets its own"
  caption (singular for 1 slide). Set states: Background shows a 12px thumbnail + filename; a vamp with no MP3
  reads amber "♪ {name} · no MP3"; a song-inherited background reads "… (song)" on a solid outline with no
  override in its popover. **Locked service / viewer:** all three chips render as inert text (no caret, no
  popover) — they are no longer hidden.
- [~] **Popover viewport-edge flip + no clipping** — open each chip's popover on a narrow window with the slide
  grid scrolled: fully visible, right-aligns near the viewport's right edge; only one open at a time; Esc and
  click-outside close it and focus returns to the chip; Tab shows a visible indigo focus ring on chips, tiles,
  swatches and tabs.
- [~] **Display popover** — Full-screen / Banner thumbnail tiles; hint switches between "Fills the screen on
  every output." and "Lower third over the live camera feed. Video output only."; the choice still drives the
  Video output (R425) exactly as before.
- [~] **Background popover + drag-and-drop (real Storage)** — None · up to 4 recent backgrounds from this service
  (most-recent group first, then song-inherited in plan order) · ＋ upload. Drag a PNG/JPG from the desktop onto
  ＋: progress row, then the new thumbnail highlighted and the chip updated; None clears it.
- [~] **Audio popover — one slot (real Storage + live)** — None / Track / Vamp segmented tabs. Track: upload,
  filename + duration after first ▶, ▶ preview plays audibly, Replace swaps the file, Remove clears. Vamp: inline
  search + list; **a vamp with no MP3 is selectable** — chip turns amber, popover shows "⚠ No MP3 attached yet —
  band plays it live", and in Run the Service that group plays **no sound** while the ♪ badge still shows.
  Switching Track→Vamp (and back) replaces rather than stacks — reload and confirm only one source persists.
- [~] **Backstops (plans 142-02 / 142-04)** — with the Vamp tab open, a failing vamp read never leaves the tab
  blank (loading / rows / a visible fallback); a rejected Firestore write (e.g. permission-denied while locked)
  leaves the chip on its last-saved value, never the attempted one.

---

## Owner UAT notes — 2026-09-18 (prod)

- **Fixed + redeployed:** creating a vamp with a blank optional BPM threw `Unsupported field value: undefined (found in field tempo)` — fixed in `19853a86` (store strips undefined on create, `deleteField()` on update), hosting redeployed (`index-Da2xQ1wb.js`).
- **Discoverability, not a bug (reproduced live):** the vamp picker is only reachable via slide card **⋮ → Edit details** (a single click merely selects the card) → drawer **Slide Audio → "Choose a vamp"**; vamps with no MP3 are listed but disabled. There is **no** vamp affordance on the group-level "＋ Add music for this group" control, and a brand-new service has 0-slide groups, so the picker is unreachable until a slide exists. Follow-up **built + deployed same day** as quick task 260918-nm2: "Choose a vamp" on the group media panel (`bedVampId`/`bedVampLabel`, vamp-sourced beds loop as an explicit D-04 exception, Run badge + rail label + R440 scan group-aware). Hosting redeployed (`index-6wGMF5op.js`).
- [~] **Group vamp bed (260918-nm2)** — on a 0-slide group (e.g. Prayer): panel shows "Choose a vamp"; pick Vamp C (after attaching its MP3) → row reads "Vamp: Vamp C · C · plays across all N slides" with Change/Clear; in Run the ♪ Vamp badge shows on that group's slides and the bed **loops** from the control window; uploading a file over it drops the vamp label; deleting Vamp C from the library warns and keeps the MP3.

---

## Milestone closeout checklist (owner pass)

- [~] Phases 138–142 items above run and ticked (or explicitly waived, per item).
- [x] **Deploy quick 260919-mvw (vamp audio from the live vamp doc)** — DONE 2026-09-19 (second deploy of the day, owner: "ship it"); `firebase deploy --only hosting` → bundle `index-DS7tZFO4.js`, HEAD `826d09d7`; full suite at the 1-file baseline (240/241, 5988 tests) + type-check clean. Prod now plays a vamp whose MP3 was attached/replaced after assignment. UAT: on prod, assign a no-MP3 vamp to a group, then attach its MP3 under Songs → Vamps, run the service → it plays; a viewer/volunteer session never subscribes vamps.
- [x] **Deploy Phase 142** (hosting) — DONE 2026-09-19, owner-confirmed; `firebase deploy --only hosting` → https://worship-planner-bc515.web.app (bundle `index-D9t7Dynj.js`, HEAD `9b50ebd0`; also carries quick 260919-k9j). Functions / rules / indexes unchanged since 09-18 — not deployed. master pushed.
- [x] Deploy hosting (+ any functions touched) with per-deploy confirmation (STATE.md deploy policy). **DONE 2026-09-18** — owner confirmed; `firebase deploy --only hosting,storage` → https://worship-planner-bc515.web.app (bundle `index-CuOoegJ0.js`, HEAD `33fdc29b`); storage.rules released (vamp-files block). Functions / firestore.rules / indexes unchanged since v2.14 — not deployed.
- [~] Post-deploy: repeat the Phase 140/141 Storage round-trips in **prod** (emulator can't model them).
- [~] Then `/gsd-audit-milestone` → `/gsd-complete-milestone v2.15`.



_Legend: `[x]` done · `[~]` waived by owner 2026-09-21 (not run)._
