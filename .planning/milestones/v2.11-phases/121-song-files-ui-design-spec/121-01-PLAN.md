---
phase: 121-song-files-ui-design-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/121-song-files-ui-design-spec/121-UI-SPEC.md
  - .planning/phases/121-song-files-ui-design-spec/121-song-files-mock.html
autonomous: true
requirements: [R373]
must_haves:
  truths:
    - "A reviewable UI-SPEC.md exists covering every Song Files surface (Files tab empty/populated/upload-in-progress, Songs-list Files column, Documents/Audio grouped rows, PDF preview, MP3 player, drop zone, link-instead field, remove-confirm) — verified by gsd-ui-checker (6/6 dimensions)."
    - "Every spec element is expressed in the app's real dark gray-950 / indigo Tailwind v4 tokens (not Nocturne), consistent with the existing SongSlideOver Details/Lyrics tabs."
    - "A standalone HTML mock renders the surfaces app-fidelity for owner review, using the app palette and hand-inlined Heroicons-outline SVGs, with the corrected PDF+MP3-only ≤50MB copy."
    - "No app source (src/**, functions/**, *.rules) is modified this phase — the deliverables are the design contract + throwaway mock only; implementation lands in phases 122-124."
  artifacts:
    - "121-UI-SPEC.md — the verified design contract."
    - "121-song-files-mock.html — the throwaway app-fidelity mock."
  key_links:
    - "121-UI-SPEC.md is the single design reference consumed by phase 123 (Files tab + upload UI) and phase 124 (preview/play/manage)."
---

<objective>
Produce the app-fidelity design contract for the Song Files feature (R373) — a verified UI-SPEC.md plus a
throwaway standalone HTML mock — before any implementation phase writes code. Map the owner's Nocturne
"Song Files" mock onto the app's real dark gray-950 / indigo design language.

This is a design-contract-only phase: NO app source is changed. The UI-SPEC governs phases 122-124.
</objective>

<context>
@.planning/phases/121-song-files-ui-design-spec/121-CONTEXT.md
@.planning/phases/121-song-files-ui-design-spec/121-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verified UI-SPEC design contract</name>
  <behavior>
    Produced via /gsd-ui-phase (gsd-ui-researcher authored, gsd-ui-checker verified 6/6 dimensions after
    one revision that reframed three inherited off-grid spacing values as a documented accepted exception).
    Covers all surfaces, states, copy, icons, access-control (editor vs viewer), and a resolved
    UI-Considerations state-coverage table (20 covered, 1 backstop = the legacy-song no-attachments partial
    state, flagged for a held-out test in the implementation phases).
  </behavior>
</task>

<task type="auto">
  <name>Task 2: Throwaway app-fidelity HTML mock</name>
  <behavior>
    Standalone self-contained HTML (121-song-files-mock.html) rendering the Songs-list Files column, the
    Edit Song slideout Files tab (populated + an upload-in-progress row + inline MP3 player), and the key
    states (empty, remove-confirm, client rejection, PDF-preview modal, viewer read-only). App palette
    (gray-950/900/800 + indigo-600/500/400), Heroicons-outline inline SVGs, corrected "PDF, MP3 · up to
    50 MB each" copy. For owner review before Phase 123.
  </behavior>
</task>

</tasks>
