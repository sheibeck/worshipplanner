---
phase: 111-architectural-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/auth.ts
  - src/components/AppShell.vue
  - src/stores/__tests__/auth.test.ts
autonomous: true
requirements: [R321]
must_haves:
  truths:
    - "A superseded/interleaved loadOrgContext call can never win the memberUnsub race: the memberUnsub onSnapshot assignment (auth.ts ~506-532) is gated by an epoch/generation token captured at the top of each loadOrgContext call, and no members listener is orphaned (per D-ARCH-001)."
    - "Rapidly double-clicking AppShell.vue's exit-to-owner-console button cannot fire onExitSuperAdminView twice concurrently — the button is disabled while an exit is in flight (mirrors switchingId / enteringOrgId)."
    - "The church-switch re-subscribe path (selectOrg / enterOrgAsSuperAdmin / exitSuperAdminView / logout, via resetOrgScopedStores + per-view watchers) still works — no regression from the epoch guard."
    - "npm run type-check exits 0; npx vitest run passes at the documented baseline (only src/storage.rules.test.ts failing); render-service is 39/39."
    - "No Medium/Low finding (ARCH-002..023) is touched by this plan; no production deploy is performed."
  artifacts:
    - src/stores/auth.ts
    - src/components/AppShell.vue
    - src/stores/__tests__/auth.test.ts
  key_links:
    - "The epoch re-check sits between loadOrgContext's last await and the memberUnsub onSnapshot assignment, so check-then-assign runs synchronously/atomically."
    - "AppShell.vue exit button :disabled binds to an in-flight ref set true before, and cleared in a finally after, the awaited authStore.exitSuperAdminView() call."
---

<objective>
Remediate **ARCH-001** — the ONLY High finding in the Phase 110 Architectural Review (0 Critical) — with
BOTH layers the owner locked in 111-CONTEXT.md:

1. A **store-layer generation/epoch guard** in `src/stores/auth.ts`'s `loadOrgContext`, so a
   superseded/interleaved call can never win the shared `memberUnsub` `onSnapshot` race or orphan a
   `members/{uid}` listener — defense-in-depth that protects ALL callers (selectOrg /
   enterOrgAsSuperAdmin / exitSuperAdminView / logout), not just the two with UI guards today.
2. The **missing UI in-flight guard** on `AppShell.vue`'s exit button (`onExitSuperAdminView`), mirroring
   the sibling `switchingId` (AppSidebar.vue) / `enteringOrgId` (OrganizationsTab.vue) patterns.

Plus a focused regression test proving a superseded `loadOrgContext` leaks no listener.

Purpose: Close the one High architectural finding with durable store-layer defense-in-depth, without
regressing the church-switch re-subscribe machinery (quick 260901-lua).
Output: Modified `src/stores/auth.ts`, `src/components/AppShell.vue`, and a regression test in
`src/stores/__tests__/auth.test.ts`. Build/commit only — NO production deploy (client-only; no
rules/functions touched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-architectural-remediation/111-CONTEXT.md
@.planning/phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store-layer epoch guard in auth.ts loadOrgContext + regression test</name>
  <files>src/stores/auth.ts, src/stores/__tests__/auth.test.ts</files>
  <read_first>
    - src/stores/auth.ts:31 (module-scope `let memberUnsub: Unsubscribe | null = null`)
    - src/stores/auth.ts:392-533 (`loadOrgContext` — the full function: early-return branches at
      459-462 / 484-487 / 497-501, and the target `memberUnsub?.(); memberUnsub = onSnapshot(...)`
      assignment at 506-532)
    - src/stores/auth.ts:301-316 (`resetOrgContext` — the single shared teardown that nulls memberUnsub)
    - src/stores/auth.ts:570-636 (`selectOrg`, `enterOrgAsSuperAdmin`, `exitSuperAdminView` — the callers;
      note enterOrgAsSuperAdmin deliberately does NOT open a members onSnapshot)
    - src/stores/auth.ts:536-565 (`onAuthStateChanged` — initial-load + logout callers of loadOrgContext)
    - src/stores/__tests__/auth.test.ts:1-70 (existing firebase/auth + firebase/firestore + @/firebase
      mocks — `onSnapshot: vi.fn(() => () => {})` is the seam to extend for listener-lifecycle assertions)
    - .planning/phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md ("## Critical/High" ARCH-001)
  </read_first>
  <behavior>
    - A single loadOrgContext call still opens exactly one members/{uid} onSnapshot and stores its
      handle in memberUnsub (unchanged happy path).
    - When two loadOrgContext calls interleave (the older one's awaits resolve AFTER a newer call has
      started), the OLDER (superseded) call must NOT assign to memberUnsub and must NOT leave a live
      members listener — exactly one members listener is live afterward (the newest call's).
    - The church-switch re-subscribe path is unbroken: a fresh loadOrgContext after
      resetOrgScopedStores() (selectOrg / exitSuperAdminView) still subscribes a new members listener.
  </behavior>
  <action>
    Add a module-scope incrementing generation counter next to `memberUnsub` (auth.ts:31) — e.g.
    `let loadOrgContextEpoch = 0` (exact name is Claude's discretion per D-ARCH-001). At the very top of
    `loadOrgContext` (right after the R213 `deactivatedOrgMessage.value = null` line, ~393), capture this
    call's generation into a local const via a pre-increment (e.g. `const myEpoch = ++loadOrgContextEpoch`).
    Immediately before the `memberUnsub?.(); memberUnsub = onSnapshot(...)` assignment at ~506-508, re-check
    the captured generation against the current module counter; if this call has been superseded (captured
    != current), it must NOT create/assign the members onSnapshot — return early WITHOUT touching
    memberUnsub, so the winning call's listener is preserved and this call leaks nothing. (If you instead
    create the onSnapshot into a local handle first, you MUST tear that local handle down on the superseded
    branch so it does not remain live — per D-ARCH-001's "ensure its own handle is torn down.") The check
    must sit AFTER loadOrgContext's last await (the getDoc at ~482) so that check-then-assign executes
    synchronously with no await between them. Do NOT alter the early-return deactivation/null-activeId
    branches' resetOrgContext() behavior — keep the fix scoped to the onSnapshot assignment race per
    locked CONTEXT. This is the store-layer half of ARCH-001 and protects selectOrg / exitSuperAdminView /
    logout / initial-load callers uniformly.

    Then add a regression test to src/stores/__tests__/auth.test.ts (extend the existing onSnapshot mock to
    return distinct, trackable unsubscribe spies so live-vs-torn-down listeners are countable). Drive two
    overlapping loadOrgContext invocations through a realistic public path (e.g. two rapid selectOrg calls,
    or a re-fired onAuthStateChanged callback) using controllable getDoc/onSnapshot mocks to force the older
    call's awaits to resolve last, and assert exactly one members listener is live afterward with the
    superseded call having created no orphan. Add a second assertion that a normal single church switch
    still opens a fresh members listener (guards the re-subscribe path). Do NOT export loadOrgContext just
    for the test — drive it through the store's existing public surface.
  </action>
  <verify>
    <automated>npm run type-check && npx vitest run src/stores/__tests__/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/auth.ts` declares a module-scope generation/epoch counter and captures it into a local
      at the top of `loadOrgContext`.
    - `src/stores/auth.ts` re-checks that captured generation immediately before the `memberUnsub`
      `onSnapshot` assignment (~506-508); a superseded call assigns nothing to memberUnsub and orphans no
      listener.
    - `src/stores/__tests__/auth.test.ts` contains a passing test proving a superseded/interleaved
      loadOrgContext leaves exactly one live members listener (no orphan), plus a passing assertion that a
      normal church switch still re-subscribes.
    - `npm run type-check` exits 0 and `npx vitest run src/stores/__tests__/auth.test.ts` passes.
  </acceptance_criteria>
  <done>Epoch guard present and checked before the memberUnsub onSnapshot assignment; regression test passes; type-check clean; church-switch re-subscribe unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: UI in-flight guard on AppShell.vue's exit button</name>
  <files>src/components/AppShell.vue</files>
  <read_first>
    - src/components/AppShell.vue:38-53 (the "viewing as super-admin" banner and the exit
      `<button type="button" @click="onExitSuperAdminView">` at 46-48 — currently no :disabled)
    - src/components/AppShell.vue:63-80 (`<script setup>` — `ref` already imported at :64;
      `onExitSuperAdminView` at 74-79 awaits `authStore.exitSuperAdminView()` then routes)
    - src/components/AppSidebar.vue:237,270-283 (`switchingId` ref + `handleSwitch` guard/finally pattern
      to mirror)
    - src/components/admin/OrganizationsTab.vue:769-796 (`enteringOrgId` ref + `onEnterChurch`
      guard/finally pattern to mirror)
  </read_first>
  <action>
    Add an in-flight ref in AppShell.vue's `<script setup>` (e.g. `const exiting = ref(false)` — exact
    name is Claude's discretion per D-ARCH-001), mirroring `switchingId` / `enteringOrgId`. In
    `onExitSuperAdminView`, early-return if the ref is already true, set it true before awaiting
    `authStore.exitSuperAdminView()`, and clear it in a `finally` block (so it resets even if the exit or
    the subsequent router.push throws). Bind `:disabled="exiting"` on the exit `<button>` at :46-48 and add
    a disabled affordance (e.g. `disabled:opacity-50 disabled:cursor-not-allowed` or equivalent) so the
    disabled state is visible. This is the UI half of ARCH-001 — belt-and-braces alongside Task 1's store
    guard. Do NOT change exitSuperAdminView's store logic here.
  </action>
  <verify>
    <automated>npm run type-check && grep -q ':disabled="exiting"' src/components/AppShell.vue</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AppShell.vue` declares an in-flight ref and guards `onExitSuperAdminView` with an
      early-return + set-before / clear-in-finally around the awaited `authStore.exitSuperAdminView()`.
    - The exit `<button>` binds `:disabled` to that in-flight ref (grep for `:disabled="exiting"` — or the
      chosen ref name — succeeds).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>Exit button is disabled while an exit is in flight; onExitSuperAdminView is re-entrancy-guarded; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: No-regression gate — type-check + full app suite (baseline) + render-service</name>
  <files>(no source edits — verification only)</files>
  <read_first>
    - CLAUDE.md (Testing + Type-checking sections: bare `npx vitest run` baseline = only
      src/storage.rules.test.ts failing; NO `--dir src`; render-service tested separately)
  </read_first>
  <action>
    Run the project's canonical gates and confirm ARCH-001's fix introduced no regressions. (1)
    `npm run type-check` (vue-tsc --build, which also type-checks test files) must exit 0. (2) bare
    `npx vitest run` must show the documented baseline — ONLY `src/storage.rules.test.ts` failing (a
    Storage-emulator cross-service limitation, not our defect) — and NO new failing files; the auth store
    regression test from Task 1 must be among the passing files. (3) `cd render-service && npm test` must
    be 39/39 (unaffected — proves the client-only change did not disturb the standalone package). Do NOT
    use `npx vitest run --dir src` (it bypasses vite.config.ts excludes and falsely runs
    src/rules.test.ts). Do NOT deploy anything.
  </action>
  <verify>
    <automated>npm run type-check && npx vitest run && (cd render-service && npm test)</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0.
    - `npx vitest run` shows exactly one failing file (`src/storage.rules.test.ts`) — no new failures; the
      auth regression test passes.
    - `cd render-service && npm test` reports 39/39 passing.
    - No production deploy command was run.
  </acceptance_criteria>
  <done>Type-check clean, app suite at documented baseline with no new failures, render-service 39/39, nothing deployed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client ↔ Firestore `members/{uid}` listener | The org-scoped members onSnapshot this fix governs; a leaked/superseded listener could keep streaming a prior org context (ARCH-001's isolation angle). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-01 | Information Disclosure | `src/stores/auth.ts` `loadOrgContext` memberUnsub race | high | mitigate | Store-layer epoch guard (Task 1) ensures a superseded call cannot keep a live members listener, closing the orphaned-listener / stale-org-context path ARCH-001 identified. |
| T-111-02 | Denial of Service | orphaned `members/{uid}` listeners accumulating over a session | medium | mitigate | Epoch guard (Task 1) + UI in-flight guard (Task 2) prevent repeated rapid exits from leaking listeners (memory/read-cost leak). |
| T-111-03 | Tampering | new external dependencies | low | accept | No packages installed; no npm/pip/cargo install tasks in this plan (client-only edit to existing files). No supply-chain surface introduced. |
</threat_model>

<verification>
- `npm run type-check` exits 0 (vue-tsc --build, includes test files).
- `npx vitest run` at the documented baseline (only `src/storage.rules.test.ts` failing); auth regression test passes.
- `cd render-service && npm test` = 39/39.
- Manual/visual (optional, not gating): in the running app, entering a church as super-admin then rapidly double-clicking "Exit to owner console" produces a single clean exit with the button disabled during the transition.
</verification>

<success_criteria>
- ARCH-001 fixed with BOTH the auth.ts epoch guard AND the AppShell.vue UI guard.
- A regression test proves a superseded loadOrgContext leaks no members listener.
- Church-switch re-subscribe path unregressed; type-check + app-suite baseline + render-service all green (baseline).
- No Medium/Low finding fixed here; no production deploy performed.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)

- `src/stores/auth.ts` — module-scope generation/epoch counter + re-check gating the `memberUnsub`
  `onSnapshot` assignment in `loadOrgContext` (store-layer half of ARCH-001).
- `src/components/AppShell.vue` — in-flight ref + `:disabled` binding on the exit-to-owner-console button
  and a re-entrancy guard in `onExitSuperAdminView` (UI half of ARCH-001).
- `src/stores/__tests__/auth.test.ts` — regression test proving a superseded `loadOrgContext` leaves no
  orphaned members listener and the normal re-subscribe path still works.
</artifacts>

<output>
Create `.planning/phases/111-architectural-remediation/111-01-SUMMARY.md` when done.
</output>
