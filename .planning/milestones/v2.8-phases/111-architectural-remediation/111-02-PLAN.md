---
phase: 111-architectural-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: [R321]
must_haves:
  truths:
    - "ROADMAP.md's `## Backlog` section contains exactly ONE new entry for the v2.8 Architectural Review Medium/Low findings (ARCH-002..023) — no per-finding stubs (per D-ARCH-001 triage decision)."
    - "The backlog entry references `110-ARCHITECTURE-REVIEW.md`'s Medium/Low section as the source of full per-finding detail, so nothing is dropped."
    - "The backlog entry's phase number comes from `gsd-tools query phase.next-decimal 999` at execution time (not hardcoded)."
    - "No Medium/Low finding (ARCH-002..023) is fixed in code by this plan — it is a documentation/triage change only."
  artifacts:
    - .planning/ROADMAP.md
  key_links:
    - "The new entry lives inside ROADMAP.md's existing `## Backlog` section (line ~723), alongside the existing 999.x items, and explicitly names ARCH-002..023 and the report path."
---

<objective>
Triage the **22 Medium/Low architectural findings (ARCH-002 through ARCH-023)** from the Phase 110
Architectural Review into ONE consolidated backlog entry in `ROADMAP.md`'s `## Backlog` section — the
"or explicitly deferred to a backlog with recorded rationale" half of R321.

Per 111-CONTEXT.md's locked triage decision: a SINGLE 999.x backlog item that points at
`110-ARCHITECTURE-REVIEW.md`'s `## Medium/Low (→ backlog)` section (which already carries full
per-finding detail). Nothing dropped; NO 22 near-empty stubs; NO Medium/Low finding fixed.

Purpose: Give R321's deferral half a durable, traceable home without polluting the backlog with 22 thin
stubs.
Output: One new entry appended to `ROADMAP.md`'s `## Backlog` section. Docs-only; no code, no deploy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/111-architectural-remediation/111-CONTEXT.md
@.planning/phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidated ARCH-002..023 Medium/Low backlog entry in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md:723-771 (the existing `## Backlog` section and its 999.2 / 999.3 entry format
      to mirror — heading shape `### Phase 999.N: <title> (BACKLOG)`, Goal / Requirements / Plans lines,
      then a checklist)
    - .planning/phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md:114-714 (the
      `## Medium/Low (→ backlog)` section carrying full ARCH-002..023 detail, plus the Summary Counts:
      13 Medium ARCH-002..014, 9 Low ARCH-015..023) — this is the detail the entry points to, so it is
      NOT duplicated into ROADMAP.md
    - .planning/phases/111-architectural-remediation/111-CONTEXT.md:34-39 (the locked one-entry triage
      decision + strict-defer rule)
  </read_first>
  <action>
    First run `gsd-tools query phase.next-decimal 999` (via the `gsd_run`/node launcher) and use the
    returned `next` value as the entry's phase number — do NOT hardcode (at planning time it resolved to
    999.4, but the owner may add more before execution, so re-query). Add ONE new
    `### Phase <next>: v2.8 Architectural Review — Medium/Low findings (ARCH-002..023) (BACKLOG)` entry
    inside ROADMAP.md's existing `## Backlog` section (do not create a new section; place it among the
    existing 999.x items, matching their Goal / Requirements: TBD / Plans: 0 plans shape). The entry MUST:
    (a) state it consolidates all 22 Medium/Low architectural findings deferred from Phase 111 per
    111-CONTEXT.md's locked triage decision; (b) reference
    `.planning/phases/110-architectural-review/110-ARCHITECTURE-REVIEW.md`'s `## Medium/Low (→ backlog)`
    section as the source of full per-finding detail (so nothing is dropped and no detail is duplicated);
    (c) note the split — 13 Medium (ARCH-002..014) and 9 Low (ARCH-015..023) — and call out the two items
    that are ALSO Phase 112 security handoffs (ARCH-005 undeployed org-provisioning functions, ARCH-018
    super-admin isOrgEditor residual) so they are not lost; (d) end with a "Promote with
    /gsd-review-backlog when ready" line matching the sibling entries. Do NOT create per-finding stubs and
    do NOT fix or change any code for any Medium/Low finding. Use `Edit` (scoped) on ROADMAP.md — do not
    rewrite the whole file.
  </action>
  <verify>
    <automated>grep -q 'ARCH-002..023' .planning/ROADMAP.md && grep -q '110-ARCHITECTURE-REVIEW.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md`'s `## Backlog` section contains exactly ONE new `### Phase 999.N:` entry
      titled for the v2.8 Architectural Review Medium/Low findings and naming `ARCH-002..023`.
    - That entry references `110-ARCHITECTURE-REVIEW.md`'s Medium/Low section as the detail source and
      notes the ARCH-005 / ARCH-018 Phase 112 handoffs.
    - The entry's phase number was obtained from `gsd-tools query phase.next-decimal 999` (not hardcoded).
    - No per-finding stubs were created; no source file under `src/`, `functions/`, `firestore.rules`, or
      `storage.rules` was modified by this plan.
  </acceptance_criteria>
  <done>One consolidated ARCH-002..023 backlog entry exists in ROADMAP.md's Backlog section, references the Phase 110 report, and fixes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change to `.planning/ROADMAP.md`; no trust boundary is crossed and no runtime code path is altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-04 | Repudiation | traceability of deferred findings | low | mitigate | The consolidated entry references the Phase 110 report so every deferred ARCH-002..023 finding stays auditable — nothing is silently dropped. |
| T-111-05 | Tampering | new external dependencies | low | accept | No packages installed; docs-only edit. No supply-chain surface. |
</threat_model>

<verification>
- `.planning/ROADMAP.md` `## Backlog` contains the single ARCH-002..023 entry referencing `110-ARCHITECTURE-REVIEW.md`.
- No `git diff` under `src/`, `functions/`, `firestore.rules`, or `storage.rules` from this plan.
</verification>

<success_criteria>
- Exactly one consolidated Medium/Low backlog entry (ARCH-002..023) added; no stubs; no findings fixed.
- Entry references the Phase 110 report and preserves the ARCH-005 / ARCH-018 Phase 112 handoff pointers.
- Backlog number sourced from `phase.next-decimal 999`.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)

- `.planning/ROADMAP.md` — one new `### Phase 999.N:` entry in the `## Backlog` section consolidating the
  22 Medium/Low architectural findings (ARCH-002..023), pointing at `110-ARCHITECTURE-REVIEW.md` for
  detail, with the ARCH-005 / ARCH-018 Phase 112 handoffs called out.
</artifacts>

<output>
Create `.planning/phases/111-architectural-remediation/111-02-SUMMARY.md` when done.
</output>
