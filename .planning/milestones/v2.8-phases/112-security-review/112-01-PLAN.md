---
phase: 112-security-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/112-security-review/112-FINDINGS-rules-isolation.md
autonomous: true
requirements: [R322]

must_haves:
  truths:
    - "The Firestore rules test suite was executed against the running :8080 emulator and its pass/fail result is recorded verbatim in the findings file (live allow/deny evidence)."
    - "Every Firestore/Storage rules and multi-tenant-isolation finding carries a severity (Critical/High/Medium/Low per the CONTEXT rubric) and a concrete location (rule path with line number, or file:line)."
    - "storage.rules is statically reviewed and the known cross-service firestore.exists() Storage-emulator limitation is noted as an environment limitation, NOT reported as a security finding."
    - "No files under src/, functions/, firestore.rules, or storage.rules were modified (review-only); no deploy occurred."
  artifacts:
    - .planning/phases/112-security-review/112-FINDINGS-rules-isolation.md
  key_links:
    - "Each rules finding cites the live emulator allow/deny behavior observed from the vitest.rules.config.ts run — not only what the rule appears to do on reading."
    - "Multi-tenant isolation findings tie a rule (or its absence) to a concrete cross-tenant read/write path."
---

<objective>
Conduct the security review pass for **Firestore & Storage security rules + multi-tenant data isolation** (ROADMAP areas 1 and 3 of 6), grounding every Firestore-rules finding in live emulator allow/deny evidence, and write the disjoint per-plan findings file `112-FINDINGS-rules-isolation.md`.

Purpose: Produce evidence-backed, severity-ranked findings that the Phase 112 consolidation (Plan 04) ranks into `112-SECURITY-REVIEW.md` and that Phase 113 remediates (Critical/High) with a required ALLOW-case emulator test.
Output: `.planning/phases/112-security-review/112-FINDINGS-rules-isolation.md`.

This is a REVIEW-ONLY plan. The executor reads rules + runs the rules test suite ITSELF (it is a subagent and CANNOT spawn sub-agents). It changes NO code, modifies NO rules, and deploys NOTHING.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-security-review/112-CONTEXT.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the Firestore rules suite and capture live allow/deny evidence</name>
  <files>.planning/phases/112-security-review/112-FINDINGS-rules-isolation.md</files>
  <read_first>
    - ./CLAUDE.md — the "Testing: two suites" and "storage.rules IS A REAL DEFECT" sections. CRITICAL: a bare `npx vitest run` EXCLUDES `src/rules.test.ts`; a Firestore emulator is already running on :8080; the rules harness scopes to projectId `test-project` while prod is `worship-planner-bc515`, so running it does NOT touch prod. The 2 failing storage.rules ALLOW-cases are an emulator `firestore.exists()` cross-service limitation, not a finding.
    - vitest.rules.config.ts — confirm the config path and scope before running.
  </read_first>
  <action>
    Execute the rules suite with the exact command `npx vitest run --config vitest.rules.config.ts` against the already-running :8080 emulator. Record the full pass/fail summary (file counts, per-test allow/deny outcomes for tenant-isolation cases) verbatim into the findings file under a "## Live rules-test evidence" section, including the exact command run and the projectId scope note (`test-project`, prod untouched). Do NOT start a second emulator and do NOT run `npm run test:rules` (it would fail "port taken"). Do NOT run `npx vitest run --dir src` (it bypasses the exclude and runs rules against a possibly-absent emulator). If the suite reports the 2 known storage.rules ALLOW-case failures, record them explicitly labelled as the known cross-service `firestore.exists()` Storage-emulator environment limitation (cite CLAUDE.md), not as findings.
  </action>
  <acceptance_criteria>
    - The findings file contains a "## Live rules-test evidence" section recording the exact command `npx vitest run --config vitest.rules.config.ts`, the projectId scope note, and the run's pass/fail summary.
    - Any storage.rules ALLOW-case failure is annotated as the known emulator `firestore.exists()` limitation, not a finding.
    - `git status --porcelain -- src functions firestore.rules storage.rules` is empty (no source/rules modified).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --config vitest.rules.config.ts 2>&1 | tail -20; test -f .planning/phases/112-security-review/112-FINDINGS-rules-isolation.md && grep -q "Live rules-test evidence" .planning/phases/112-security-review/112-FINDINGS-rules-isolation.md; git status --porcelain -- src functions firestore.rules storage.rules | grep -q . && echo "SOURCE MODIFIED - FAIL" || echo "no source changes OK"</automated>
  </verify>
  <done>The rules suite has been run and its verbatim result plus scope note are recorded in the findings file; no source or rules files are modified.</done>
</task>

<task type="auto">
  <name>Task 2: Review Firestore + Storage rules and multi-tenant isolation; write severity-ranked findings</name>
  <files>.planning/phases/112-security-review/112-FINDINGS-rules-isolation.md</files>
  <read_first>
    - firestore.rules (450 lines) — in particular the helpers isSignedIn/isOrgActive/isOrgMember/isOrgEditor/isSuperAdmin (lines ~5-50), the members match block and invite-role coupling (~160-180), lockSnapshots (~236-242), shareTokens/serviceShareLinks/quarterShares (~338-370), and every collection's allow read/write/create/update/delete scoping.
    - storage.rules (79 lines) — static review only; note the cross-service firestore.exists() clause and its emulator limitation (do not test it as a finding).
    - .planning/codebase/ARCHITECTURE.md — "Backend Behavioral Notes (R318) § firestore.rules" (the exists()-guard rationale the rules reference).
    - .planning/codebase/CONCERNS.md — existing tech-debt/security context for cross-referencing.
    - The "## Live rules-test evidence" section written in Task 1.
  </read_first>
  <action>
    Review the two dimensions area-by-area: (dimension A) Firestore & Storage security rules — for every collection/subcollection assess read/create/update/delete authorization, immutability guards (createdBy, orgId), invite-role coupling, share-token public read scope, and any allow rule broader than its data owner; (dimension B) multi-tenant data isolation — trace whether any authenticated user of org X can read or write org Y data through a rule gap, a missing orgId scope, or a claim-vs-rule mismatch. For EACH issue write a finding with: a stable id (SEC-R-01, SEC-R-02, ...), a severity per the CONTEXT rubric (Critical = cross-tenant leak / auth bypass / data loss; High = real isolation/authz weakness exploitable under real use; Medium = defense-in-depth gap or conditional bug; Low = nit), a concrete location (firestore.rules:LINE or storage.rules:LINE), the observed behavior grounded in the Task 1 emulator evidence where the rule was exercised, and the impact. For any rules issue rated Critical or High, name the specific ALLOW-case emulator test a Phase 113 fix must add to prove the fix (per R323 discipline). Group findings under "## Critical/High" and "## Medium/Low" headings. Add an "## Artifacts this phase produces" note pointing to this file and the eventual 112-SECURITY-REVIEW.md. Change NO rules and NO source; deploy NOTHING.
  </action>
  <acceptance_criteria>
    - 112-FINDINGS-rules-isolation.md covers both Firestore/Storage rules AND multi-tenant isolation.
    - Every finding has an id, a severity, and a concrete location (rule path with line, or file:line).
    - Findings are grouped into "## Critical/High" and "## Medium/Low" sections.
    - Each Critical/High rules finding names the ALLOW-case emulator test a Phase 113 fix must add.
    - The storage.rules cross-service exists() behavior is noted as an environment limitation, not a finding.
    - `git status --porcelain -- src functions firestore.rules storage.rules` is empty.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/112-security-review/112-FINDINGS-rules-isolation.md; test -f "$F" && grep -q "## Critical/High" "$F" && grep -q "## Medium/Low" "$F" && grep -qiE "firestore.rules:|storage.rules:" "$F"; git status --porcelain -- src functions firestore.rules storage.rules | grep -q . && echo "SOURCE MODIFIED - FAIL" || echo "no source changes OK"</automated>
  </verify>
  <done>The findings file enumerates severity-ranked, concretely-located Firestore/Storage-rules and isolation findings backed by the live emulator evidence, split C/H vs M/L, with required ALLOW-case tests noted for each C/H rules finding; no code or rules modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rules-test harness → live emulator | The review runs the rules suite; it must exercise the :8080 emulator scoped to projectId `test-project`, never prod `worship-planner-bc515`. |
| review process → source/rules tree | The review reads rules/source; it must not modify any rule or source file (review-only guarantee). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-01 | Tampering | rules-test run vs prod data | high | mitigate | Use only `npx vitest run --config vitest.rules.config.ts` (scoped to `test-project`); never deploy; never run a prod-pointed command. |
| T-112-02 | Tampering | source/rules files | high | mitigate | Review-only; acceptance gate asserts `git status --porcelain -- src functions firestore.rules storage.rules` is empty. |
| T-112-03 | Information disclosure | findings file content | low | accept | Findings enumerate vulnerabilities but live only in git-internal .planning/; not published. |
</threat_model>

<verification>
- The rules suite was executed with the exact scoped command and its result recorded.
- Findings cover Firestore/Storage rules + multi-tenant isolation with severity + concrete location each.
- Critical/High and Medium/Low are separated; each C/H rules finding names its required ALLOW-case test.
- No source, functions, or *.rules files modified.
</verification>

<success_criteria>
`112-FINDINGS-rules-isolation.md` exists with live emulator evidence, severity-ranked and concretely-located rules + isolation findings, C/H vs M/L separation, and no code/rules changes.
</success_criteria>

<output>
Create `.planning/phases/112-security-review/112-01-SUMMARY.md` when done.
</output>
