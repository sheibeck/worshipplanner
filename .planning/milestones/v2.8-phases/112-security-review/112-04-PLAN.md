---
phase: 112-security-review
plan: 04
type: execute
wave: 2
depends_on: [112-01, 112-02, 112-03]
files_modified:
  - .planning/phases/112-security-review/112-SECURITY-REVIEW.md
autonomous: true
requirements: [R322]

must_haves:
  truths:
    - "112-SECURITY-REVIEW.md exists at the locked path .planning/phases/112-security-review/112-SECURITY-REVIEW.md and covers all six ROADMAP areas: (1) Firestore & Storage rules, (2) auth/custom-claims + route guards, (3) multi-tenant data isolation, (4) Cloud Functions authorization, (5) share-token/public-page exposure + PII, (6) cost/abuse controls."
    - "Every finding in the consolidated report carries an explicit severity and a concrete location, presented in a summary table."
    - "The report cleanly separates a '## Critical/High (→ Phase 113)' section from a '## Medium/Low (→ backlog)' section."
    - "Firestore-rules Critical/High findings each note the ALLOW-case emulator test a Phase 113 fix must add (per R323 discipline), and the live rules-suite result from Plan 01 is referenced as evidence."
    - "ARCH-005 and ARCH-018 assessments are carried into the consolidated report with their final severities."
    - "No files under src/, functions/, firestore.rules, or storage.rules were modified; no deploy occurred."
  artifacts:
    - .planning/phases/112-security-review/112-SECURITY-REVIEW.md
  key_links:
    - "Every finding from the three per-plan findings files (rules-isolation, auth-functions, sharetoken-pii-abuse) appears in the single consolidated ranked report with a consistent severity."
    - "The Critical/High set is the exact, unambiguous remediation scope Phase 113 consumes."
---

<objective>
Consolidate the three per-plan findings files into the single severity-ranked deliverable `112-SECURITY-REVIEW.md` covering all six ROADMAP areas, with a summary table and an unambiguous split between Critical/High (→ Phase 113) and Medium/Low (→ backlog).

Purpose: This is the phase deliverable (R322) and the input Phase 113 remediates. It must give Phase 113 an unambiguous Critical/High remediation scope and, for each rules-related C/H item, the ALLOW-case test the fix must add (R323).
Output: `.planning/phases/112-security-review/112-SECURITY-REVIEW.md`.

This is a REVIEW-ONLY plan. The executor reads the three findings files ITSELF and writes the report (it is a subagent and CANNOT spawn sub-agents). It changes NO code and deploys NOTHING.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-security-review/112-CONTEXT.md
@.planning/phases/112-security-review/112-FINDINGS-rules-isolation.md
@.planning/phases/112-security-review/112-FINDINGS-auth-functions.md
@.planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rank all findings into the single 112-SECURITY-REVIEW.md report</name>
  <files>.planning/phases/112-security-review/112-SECURITY-REVIEW.md</files>
  <read_first>
    - .planning/phases/112-security-review/112-FINDINGS-rules-isolation.md — all rules + isolation findings and the "## Live rules-test evidence" section.
    - .planning/phases/112-security-review/112-FINDINGS-auth-functions.md — all auth/claims, route-guard, function-authz findings including the ARCH-005 and ARCH-018 assessments.
    - .planning/phases/112-security-review/112-FINDINGS-sharetoken-pii-abuse.md — all share-exposure, PII, and cost/abuse findings.
    - .planning/phases/112-security-review/112-CONTEXT.md — the severity rubric and the locked report location, to apply consistent severities.
    - .planning/ROADMAP.md — Phase 112 success criteria (six areas) and Phase 113's R323 ALLOW-case-test requirement.
  </read_first>
  <action>
    Author `112-SECURITY-REVIEW.md` at the locked path. Start with a one-paragraph scope statement (six areas, review-only, no code changed, no deploy) and a note that Firestore-rules findings are grounded in the live emulator run recorded in the rules-isolation findings file. Add a "## Summary table" mapping every finding to id, area (one of the six), severity, concrete location, and disposition (Phase 113 / backlog). Re-check each finding's severity against the CONTEXT rubric for consistency across the three source files and reconcile any duplicate or overlapping finding into a single entry. Then write two clearly separated sections: "## Critical/High (→ Phase 113)" containing the full detail (id, severity, location, behavior, impact, recommended fix direction) of every Critical and High finding, and "## Medium/Low (→ backlog)" containing the Medium and Low findings. For each rules-related Critical/High finding, carry through the ALLOW-case emulator test a Phase 113 fix must add. Ensure the ARCH-005 and ARCH-018 assessments appear with their final severities in the correct section. Confirm every one of the six ROADMAP areas is represented (state explicitly if an area produced no findings). Add an "## Artifacts this phase produces" section listing the three findings files and this report. Change NO code; deploy NOTHING.
  </action>
  <acceptance_criteria>
    - `112-SECURITY-REVIEW.md` exists at .planning/phases/112-security-review/112-SECURITY-REVIEW.md.
    - It contains a "## Summary table", a "## Critical/High (→ Phase 113)" section, and a "## Medium/Low (→ backlog)" section.
    - Every finding row has a severity and a concrete location; every one of the six ROADMAP areas is represented (or explicitly marked no-findings).
    - Each rules-related Critical/High finding names the ALLOW-case emulator test a Phase 113 fix must add, and the live rules-suite evidence is referenced.
    - ARCH-005 and ARCH-018 appear with final severities.
    - An "## Artifacts this phase produces" section lists the three findings files and this report.
    - `git status --porcelain -- src functions firestore.rules storage.rules` is empty (no code/rules changed; no deploy).
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/112-security-review/112-SECURITY-REVIEW.md; test -f "$F" && grep -q "## Summary table" "$F" && grep -q "## Critical/High" "$F" && grep -q "## Medium/Low" "$F" && grep -q "ARCH-005" "$F" && grep -q "ARCH-018" "$F" && grep -q "Artifacts this phase produces" "$F"; git status --porcelain -- src functions firestore.rules storage.rules | grep -q . && echo "SOURCE MODIFIED - FAIL" || echo "no source changes OK"</automated>
  </verify>
  <done>The single ranked 112-SECURITY-REVIEW.md exists covering all six areas with a summary table, C/H vs M/L separation, per-rules-finding ALLOW-case tests, and ARCH-005/018 carried through; no code or rules modified and nothing deployed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review process → source/rules tree | Consolidation reads the findings files and writes one report; it must not modify any source or rule file (review-only guarantee). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-10 | Tampering | source/rules files | high | mitigate | Review-only; acceptance gate asserts `git status --porcelain` clean for src/functions/rules and no deploy. |
| T-112-11 | Repudiation | severity mis-ranking hides a real risk | medium | mitigate | Severities re-checked against the CONTEXT rubric for consistency; C/H vs M/L split is the explicit Phase 113 scope. |
</threat_model>

<verification>
- The single deliverable exists at the locked path covering all six areas.
- Summary table plus C/H (→113) vs M/L (→backlog) separation, every finding with severity + location.
- Rules C/H findings note required ALLOW-case tests and reference the live rules evidence.
- ARCH-005 and ARCH-018 carried through with final severities.
- No source, functions, or *.rules files modified; no deploy.
</verification>

<success_criteria>
`112-SECURITY-REVIEW.md` is the single ranked report covering all six ROADMAP areas, cleanly splitting Critical/High (→ Phase 113) from Medium/Low (→ backlog), grounded in live rules evidence, with ARCH-005/018 assessed and no code changed.
</success_criteria>

<output>
Create `.planning/phases/112-security-review/112-04-SUMMARY.md` when done.
</output>
