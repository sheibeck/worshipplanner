---
phase: 109-behavioral-architectural-extraction-comment-convention
plan: 03
type: execute
wave: 2
depends_on: ['109-02']
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/STACK.md
  - src/utils/slideGroupMaterializer.ts
  - src/utils/slideshowAssembler.ts
  - src/utils/slideTypography.ts
  - src/utils/slotTypes.ts
  - src/utils/scripture.ts
  - src/utils/scriptureApi.ts
  - src/utils/scriptureBoundaries.ts
  - src/utils/congregationalText.ts
  - src/utils/claudeApi.ts
  - src/utils/firestoreListener.ts
  - src/utils/importedRenderReconciler.ts
  - src/utils/lastUsed.ts
  - src/utils/messaging.ts
  - src/utils/messagingRecipients.ts
  - src/utils/monitorConfig.ts
  - src/utils/nltApi.ts
  - src/utils/orgName.ts
  - src/utils/pcSongImport.ts
  - src/utils/planningCenterApi.ts
  - src/utils/pptxUpload.ts
  - src/utils/quarterDates.ts
  - src/utils/renderedPagePaths.ts
  - src/utils/rotationTable.ts
  - src/utils/runChannel.ts
  - src/utils/scheduler.ts
  - src/utils/serviceLockDiff.ts
  - src/utils/serviceSlots.ts
  - src/utils/shareTokens.ts
  - src/utils/songSearch.ts
  - src/utils/songSectionOrder.ts
  - src/utils/stripUndefined.ts
  - src/utils/suggestions.ts
  - src/utils/teamRecurrence.ts
  - src/utils/stageLayout.ts
autonomous: true
requirements: [R318]

must_haves:
  truths:
    - "Every Bucket B handoff entry whose source path is src/utils/** has its 'how it works' narration relocated into the target .planning/codebase/ map doc named for it in the 108 handoff, OR is explicitly kept inline with a one-line justification — no silent drops (R318)."
    - "Each relocated src/utils comment is reduced to a short map-doc pointer per the convention, so the source no longer carries multi-paragraph narration (R318)."
    - "The comment-only edits change no behavior: type-check passes and the app test suite shows no NEW failures versus the pre-edit baseline (R318)."
  artifacts:
    - ".planning/codebase/ARCHITECTURE.md / INTEGRATIONS.md / CONCERNS.md / STACK.md (src/utils behavioral notes relocated in)"
    - "src/utils/** source comments shrunk to pointers"
  key_links:
    - "each shrunk src/utils pointer -> the exact map-doc section that now holds its narration (self-consistent within this plan)"
    - "pure-module purity notes (slideshowAssembler / songSectionOrder / lastUsed) -> ARCHITECTURE.md, so the 'copyable into functions/' contract survives the shrink"
---

<objective>
Relocate the behavioral/architectural narration out of the `src/utils/**` source comments into the
`.planning/codebase/` map docs and shrink each source comment to a pointer — the R318 sweep for the utils
tree (~90 entries, the largest source tree in Bucket B, dominated by the slide-assembly and scripture
helpers).

This plan owns every Bucket B handoff entry whose source path is `src/utils/**`. It works the "Phase 109
Handoff" worklist in `108-COMMENT-INVENTORY.md` (lines ~6951-7272), which names a target map doc per entry.

Purpose: the utils layer holds the pure engines (slide-group materialization, scripture boundary/encoding,
share-token adoption, section-ordering) whose multi-paragraph "how it works" narration is exactly what the
map docs should carry per 109-CONTEXT.md.
Output: updated map docs + shrunk src/utils source comments.

Dependency note: this plan appends to the same four map docs as 109-02/04/05; it is serialized to Wave 2
(after 109-02) so no two plans write the shared docs concurrently. The dependency is doc-write
serialization, not content.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-behavioral-architectural-extraction-comment-convention/109-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@CLAUDE.md

Worklist (do NOT @-ref — ~488KB; read the named line range only):
`.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md` lines
~6951-7272 ("Phase 109 Handoff"). This plan's scope = every entry whose bolded source path begins
`src/utils/`.

Follow the RELOCATION PROTOCOL defined in 109-02-PLAN.md Task 1 (read the full source comment; move
narration to the handoff-named map doc under the most relevant existing section or a new `### ` subsection
keyed by source file; shrink to a `See .planning/codebase/<DOC>.md (<section>)` pointer; keep-inline only
residue code cannot convey, recorded with a reason; preserve comment syntax; hand-edit adjacent/shared
blocks). Heed the 108-02-SUMMARY.md corruption lessons — importedRenderReconciler.ts specifically had a
shared-JSDoc split hazard in Phase 108.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate + shrink the slide-assembly & scripture utils</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/STACK.md, .planning/codebase/CONCERNS.md, src/utils/slideGroupMaterializer.ts, src/utils/slideshowAssembler.ts, src/utils/slideTypography.ts, src/utils/slotTypes.ts, src/utils/scripture.ts, src/utils/scriptureApi.ts, src/utils/scriptureBoundaries.ts, src/utils/congregationalText.ts, src/utils/importedRenderReconciler.ts</files>
  <read_first>
    - 108-COMMENT-INVENTORY.md lines ~6951-7272: entries for slideGroupMaterializer, slideshowAssembler,
      slideTypography, slotTypes, scripture, scriptureApi, scriptureBoundaries, congregationalText,
      importedRenderReconciler (this task's sub-scope; slideGroupMaterializer alone has ~13 entries).
    - The four map docs' current section layout (relevant homes: ARCHITECTURE.md "Key Abstractions" /
      "Data Flow" for the assembler+materializer; INTEGRATIONS.md for scripture API/boundary flow;
      STACK.md for slideTypography/slotTypes helpers).
    - The full source comment for each entry (handoff truncates at "...").
  </read_first>
  <action>
    Apply the RELOCATION PROTOCOL to every in-scope entry in these files. Take special care with the
    materializer/assembler cluster: its comments encode the survival/carry rules, the pure-module purity
    contract ("NO firebase, NO vue imports"), and the reference-vs-congregational predicate — relocate the
    full narration to ARCHITECTURE.md and leave a one-line pointer plus any invariant code cannot convey.
    Hand-edit importedRenderReconciler.ts (shared-JSDoc hazard from Phase 108). Commit atomically.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['slideGroupMaterializer','slideshowAssembler','slideTypography','slotTypes','scripture','scriptureApi','scriptureBoundaries','congregationalText','importedRenderReconciler'].map(n=>'src/utils/'+n+'.ts');let c=0;for(const f of files){c+=(fs.readFileSync(f,'utf8').match(/See \.planning\/codebase\//g)||[]).length;}if(c<20){console.error('too few pointers:',c);process.exit(1);}console.log('pointers:',c)"</automated>
  </verify>
  <acceptance_criteria>
    - Every in-scope entry in the listed files is relocated+shrunk or recorded as kept-inline — none dropped.
    - The listed files collectively contain at least ~20 `See .planning/codebase/` pointers.
    - Narration each pointer references exists in the named map doc + section.
    - Comment-only diffs (no executable line changed).
  </acceptance_criteria>
  <done>Assembly + scripture utils relocated, shrunk, committed; pointers resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate + shrink the remaining src/utils modules</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/STACK.md, src/utils/claudeApi.ts, src/utils/firestoreListener.ts, src/utils/lastUsed.ts, src/utils/messaging.ts, src/utils/messagingRecipients.ts, src/utils/monitorConfig.ts, src/utils/nltApi.ts, src/utils/orgName.ts, src/utils/pcSongImport.ts, src/utils/planningCenterApi.ts, src/utils/pptxUpload.ts, src/utils/quarterDates.ts, src/utils/renderedPagePaths.ts, src/utils/rotationTable.ts, src/utils/runChannel.ts, src/utils/scheduler.ts, src/utils/serviceLockDiff.ts, src/utils/serviceSlots.ts, src/utils/shareTokens.ts, src/utils/songSearch.ts, src/utils/songSectionOrder.ts, src/utils/stripUndefined.ts, src/utils/suggestions.ts, src/utils/teamRecurrence.ts, src/utils/stageLayout.ts</files>
  <read_first>
    - 108-COMMENT-INVENTORY.md lines ~6951-7272: all `src/utils/` entries NOT covered by Task 1 (this
      task's sub-scope).
    - The four map docs after Task 1's additions, to append consistently.
  </read_first>
  <action>
    Apply the RELOCATION PROTOCOL to every remaining in-scope `src/utils/**` module. Note pptxUpload.ts's
    "these two size numbers must stay in lockstep with storage.rules" and firestoreListener.ts's sign-out
    unsubscribe note — these are invariants/gotchas that code cannot convey; keep a one-line inline residue
    and relocate the fuller narration. Commit atomically.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['claudeApi','firestoreListener','lastUsed','messaging','messagingRecipients','monitorConfig','nltApi','orgName','pcSongImport','planningCenterApi','pptxUpload','quarterDates','renderedPagePaths','rotationTable','runChannel','scheduler','serviceLockDiff','serviceSlots','shareTokens','songSearch','songSectionOrder','stripUndefined','suggestions','teamRecurrence','stageLayout'].map(n=>'src/utils/'+n+'.ts');let c=0;for(const f of files){c+=(fs.readFileSync(f,'utf8').match(/See \.planning\/codebase\//g)||[]).length;}if(c<20){console.error('too few pointers:',c);process.exit(1);}console.log('pointers:',c)"</automated>
  </verify>
  <acceptance_criteria>
    - Every remaining in-scope `src/utils/**` entry is relocated+shrunk or recorded as kept-inline — none dropped.
    - The listed modules collectively contain at least ~20 `See .planning/codebase/` pointers.
    - Comment-only diffs (no executable line changed).
  </acceptance_criteria>
  <done>Remaining utils relocated, shrunk, committed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove no behavior change + utils completeness count</name>
  <files>src/utils/scripture.ts, src/utils/slideGroupMaterializer.ts</files>
  <read_first>
    - CLAUDE.md: "Type-checking" (`npm run type-check`) and "Testing" (bare `npx vitest run`; baseline =
      only `src/storage.rules.test.ts` failing; do NOT use `--dir src`).
    - 108's deferred-items.md: the pre-existing `appConfig.test.ts` failure is not a regression.
  </read_first>
  <action>
    Count this plan's in-scope handoff entries (grep the handoff for bolded paths beginning `src/utils/`)
    and confirm relocated + kept-inline equals that count — no drops. Run the type-check gate and the app
    suite; compare failing files to the pre-edit baseline captured at the start — the only allowed failures
    are the known baseline (`src/storage.rules.test.ts`, plus the pre-existing `appConfig.test.ts`). Any NEW
    failing file is a regression to fix before finishing. This plan does not edit rules files, so the rules
    suite is out of scope.
  </action>
  <verify>
    <automated>npm run type-check && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0.
    - `npx vitest run` shows no NEW failing files versus the pre-edit baseline.
    - Relocated + kept-inline count equals the in-scope `src/utils/` handoff entry count — recorded in the
      SUMMARY; no drops.
  </acceptance_criteria>
  <done>Type-check clean, no new test failures, utils entry count reconciles to the handoff with zero drops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment edit -> compiled app | A malformed comment edit could drop a delimiter and change executable TS in a pure engine used everywhere (assembler/materializer). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-06 | Tampering | Block/JSDoc shrink in a pure engine dropping a delimiter -> invalid TS or altered logic | high | mitigate | Comment-only diffs; preserve opener/closer; hand-edit shared JSDoc (importedRenderReconciler); Task 3 type-check + app suite must show no new failures. |
| T-109-07 | Tampering | Losing the pptxUpload<->storage.rules "lockstep size" invariant when shrinking | medium | mitigate | Keep the lockstep note inline as a one-line invariant; relocate only the explanatory narration. |

No package-manager installs occur in this phase, so the supply-chain threat (T-{phase}-SC) does not apply.
</threat_model>

<verification>
- Every in-scope `src/utils/` handoff entry relocated+shrunk or explicitly kept-inline (count reconciles, no drops).
- src/utils source files carry `See .planning/codebase/` pointers resolving to real map-doc sections.
- `npm run type-check` exits 0; `npx vitest run` shows no NEW failures versus baseline.
- Comment-only diffs.
</verification>

<success_criteria>
- R318 (utils subset) satisfied: src/utils "how it works" narration relocated into the map docs, source
  comments shrunk, nothing dropped, zero behavior change.
</success_criteria>

## Artifacts this phase produces

- `.planning/codebase/ARCHITECTURE.md`, `INTEGRATIONS.md`, `CONCERNS.md`, `STACK.md` — src/utils behavioral
  notes relocated in (R318).
- `src/utils/**` (34 modules) — source comments shrunk to `See .planning/codebase/<DOC>.md (<section>)`
  pointers (R318).

<output>
Create `.planning/phases/109-behavioral-architectural-extraction-comment-convention/109-03-SUMMARY.md` when done.
Record the in-scope entry count, the relocated count, and every explicit "kept inline" decision with its reason.
</output>
