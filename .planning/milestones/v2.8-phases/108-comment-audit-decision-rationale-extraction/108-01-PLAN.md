---
phase: 108-comment-audit-decision-rationale-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md
autonomous: true
requirements: [R316]
must_haves:
  truths:
    - "A written triage inventory enumerates every load-bearing comment across src/**, functions/src/**, render-service/src/**, firestore.rules, and storage.rules, each with a file:line reference"
    - "Every enumerated comment is classified into exactly one of three buckets (decision-rationale, behavioral/architectural, genuinely-local) — no file:line appears in two buckets"
    - "The behavioral/architectural subset is presented as a complete, unambiguous Phase 109 handoff section — nothing dropped, nothing double-classified"
    - "The decision-rationale bucket is complete enough to drive R317 ADR extraction in plan 108-02 (each entry carries file:line plus the rationale summary or verbatim text)"
  artifacts:
    - .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md
  key_links:
    - "Inventory decision-rationale bucket -> consumed by plan 108-02 (ADR extraction, R317)"
    - "Inventory behavioral/architectural bucket -> consumed by Phase 109 (R318 relocation into .planning/codebase/)"
---

<objective>
Produce a written triage inventory (R316) enumerating every LOAD-BEARING comment across the
entire runtime codebase, each classified into exactly one of three buckets — decision-rationale,
behavioral/architectural, or genuinely-local — with a file:line reference for every entry.

Purpose: This inventory is the discovery step that drives BOTH R317 (ADR extraction, plan 108-02)
and R318 (behavioral/architectural relocation, Phase 109). The classification must be complete and
mutually exclusive so downstream phases can consume their bucket without re-doing the audit.

Output: `.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-comment-audit-decision-rationale-extraction/108-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

<audit_scope_note>
The knowledge graph at `.planning/graphs/` is STALE (built while `.gsd/` still existed and resolves
symbols to deleted quarantine paths). This audit MUST be grep-first over the real working tree. Verify
any path against real files under `src/`, `functions/src/`, `render-service/src/`. Do NOT run graph
queries for this audit.

In-scope trees (nothing outside these): `src/**`, `functions/src/**`, `render-service/src/**`,
`firestore.rules`, `storage.rules`.

"Load-bearing" = removing the comment loses information NOT recoverable from the code itself:
the `R-`/`WR-`/`CR-`/`Pitfall`-tagged "why" notes, multi-line "how it works" narration, and
non-obvious constraint/gotcha notes. EXCLUDE trivial restatements, TODOs, section dividers
(`// ---` / box-drawing rules), and JSDoc `@param`/`@returns` docs.

Do NOT classify test `describe`/`it` string arguments that merely reference a requirement id
(e.g. `describe('R-035 ...')`) — those are test labels, not comments-as-specs.
</audit_scope_note>

<tasks>

<task type="auto">
  <name>Task 1: Grep-first decision-rationale collection across all in-scope trees</name>
  <files>.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md</files>
  <read_first>
    - .planning/phases/108-comment-audit-decision-rationale-extraction/108-CONTEXT.md (the ADR convention + tag vocabulary + load-bearing definition)
    - .planning/codebase/CONVENTIONS.md (the "Comments" and "Design References" sections describe the (D-01)/(R-02)/(CR-01) tag pattern)
    - CLAUDE.md (grep-first / stale-graph rule; scope of runtime trees)
  </read_first>
  <action>
    Grep-first pass over `src/**`, `functions/src/**`, `render-service/src/**`, `firestore.rules`,
    `storage.rules` for the decision-rationale tag vocabulary. Use these patterns (regex, with a few
    lines of surrounding context so you can read the rationale without opening whole files):
    `R-\d`, `WR-\d`, `CR-\d`, and `Pitfall`. Also catch the parenthesized reference forms `(R-\d`,
    `(CR-\d`, `(WR-\d`. Known-rich examples to confirm your pass catches: `functions/src/orgMembershipClaims.ts`
    (`CR-01`, `WR-01`, `WR-03`, each citing `76-REVIEW.md`), `src/composables/useRunControl.ts`
    (`WR-01`, `WR-02`), and `firestore.rules` (~20 tagged lines).

    For EACH tagged comment record, in a "## Bucket A — Decision-Rationale" section of the inventory doc:
    a `file:line` reference, the tag id(s) it carries (e.g. `CR-01`, `WR-02`), and the rationale as a
    short summary plus the verbatim comment text (the verbatim text is what plan 108-02 will move into the ADR).
    Note explicitly when the SAME tag id appears across multiple files/lines (e.g. `WR-01` in both
    `useRunControl.ts` and `orgMembershipClaims.ts`) — these will map to ONE shared ADR id later, so group
    them under a single tag heading.

    Exclude test-file `describe`/`it` label strings that only echo a requirement id. Do NOT edit any
    source file in this task — you are only writing the inventory doc.
  </action>
  <verify>
    <automated>test -f .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md && grep -q 'Decision-Rationale' .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - The inventory doc exists and contains a "## Bucket A — Decision-Rationale" section.
    - Every tagged hit surfaced by grepping `R-\d`, `WR-\d`, `CR-\d`, `Pitfall` across the four in-scope
      trees (minus test label strings) appears in Bucket A with a `file:line` reference.
    - Spot check: `functions/src/orgMembershipClaims.ts` CR-01/WR-01/WR-03 lines and `firestore.rules`
      tagged lines are all present in Bucket A.
    - Each Bucket A entry carries both a summary and the verbatim comment text (so 108-02 can extract it).
    - No source file was modified (git status shows only the inventory doc changed).
  </acceptance_criteria>
  <done>Bucket A of the inventory lists every tagged decision-rationale comment across all in-scope trees with file:line + verbatim text; no source edited.</done>
</task>

<task type="auto">
  <name>Task 2: Untagged load-bearing scan and three-way classification</name>
  <files>.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md</files>
  <read_first>
    - .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md (Bucket A from Task 1)
    - .planning/codebase/CONVENTIONS.md (orientation for what counts as behavioral/architectural)
    - .planning/codebase/ARCHITECTURE.md and .planning/codebase/CONCERNS.md (Phase 109 relocation targets — useful for tagging behavioral entries with a suggested target doc)
  </read_first>
  <action>
    Lightweight grep-first scan for UNTAGGED load-bearing comments across the same four trees, catching
    multi-line "why"/"how it works" narration and non-obvious constraint/gotcha notes. Useful patterns:
    block comments longer than two lines, and inline markers `NOTE:`, `WARNING`, `HACK`, `IMPORTANT`,
    `must`, `do not`, `because`, `deliberately`, `careful`. Read enough surrounding context to judge
    whether the information is recoverable from the code alone.

    Classify EVERY load-bearing comment (the Task 1 tagged set plus the untagged set) into exactly one of
    three buckets, recorded in the inventory doc:
    - Bucket A — Decision-Rationale (the tagged "why" notes; already collected in Task 1 — add any untagged
      "why we chose X over Y" rationale here too).
    - Bucket B — Behavioral/Architectural ("how this feature works" narration — the Phase 109 / R318 target).
    - Bucket C — Genuinely-Local (load-bearing but the info is inherently tied to that spot; stays in code).
    Every Bucket B entry gets a `file:line`, a short description, and a SUGGESTED target `.planning/codebase/`
    map doc (ARCHITECTURE.md / CONCERNS.md / INTEGRATIONS.md / TESTING.md / STRUCTURE.md / STACK.md /
    CONVENTIONS.md). EXCLUDE trivial restatements, TODOs, section dividers, and JSDoc `@param`/`@returns`.

    HARD RULE: no `file:line` may appear in more than one bucket. If a comment mixes rationale and
    behavior, split the entry and put each half in its bucket, or place it in the dominant bucket and note
    the secondary content — never list the same location twice. Do NOT edit any source file.
  </action>
  <verify>
    <automated>grep -q 'Behavioral/Architectural' .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md && grep -q 'Genuinely-Local' .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - The inventory doc contains three bucket sections: "Decision-Rationale", "Behavioral/Architectural",
      "Genuinely-Local".
    - Every load-bearing comment (tagged + untagged) is classified into exactly one bucket.
    - Uniqueness holds: no `file:line` reference appears under two different bucket headings (manual scan
      of the doc confirms zero cross-bucket duplication).
    - Each Bucket B entry names a suggested `.planning/codebase/` target map doc for the Phase 109 handoff.
    - No source file was modified.
  </acceptance_criteria>
  <done>All load-bearing comments are three-way classified with unique file:line placement; Bucket B entries carry a suggested Phase 109 target doc; no source edited.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize inventory header, summary table, and Phase 109 handoff section</name>
  <files>.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md</files>
  <read_first>
    - .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md (Buckets A/B/C)
    - .planning/ROADMAP.md (the "### Phase 109" section — the consumer of the behavioral/architectural handoff)
  </read_first>
  <action>
    Add a document header to the inventory: scope statement (the four in-scope trees), audit method
    (grep-first, stale-graph caveat), date, and the load-bearing definition used. Add a summary table with
    per-bucket counts broken down per tree (src, functions/src, render-service/src, firestore.rules,
    storage.rules).

    Add an explicit "## Phase 109 Handoff — Behavioral/Architectural Subset" section that re-lists EVERY
    Bucket B entry with its `file:line` and suggested target `.planning/codebase/` doc, framed as a
    ready-to-consume worklist for R318. State the completeness guarantee in prose: nothing dropped, nothing
    double-classified. Do NOT edit any source file — this plan touches only the inventory doc.
  </action>
  <verify>
    <automated>grep -q 'Phase 109 Handoff' .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - The inventory has a header (scope, method, date, load-bearing definition) and a per-tree/per-bucket
      summary count table.
    - A "## Phase 109 Handoff — Behavioral/Architectural Subset" section re-lists every Bucket B entry with
      file:line + suggested target map doc.
    - The count of entries in the handoff section equals the count of Bucket B entries (no drops).
    - `git status` shows only the inventory doc changed by this entire plan (no source or test file touched)
      — this plan makes zero behavior change.
  </acceptance_criteria>
  <done>Inventory is finalized with header, summary table, and a complete Phase 109 handoff section; only the inventory doc was created/modified across the plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan produces a markdown inventory only. It adds no code paths, inputs, endpoints, or dependencies; existing trust boundaries are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-108-01 | Tampering | audit inventory doc | low | mitigate | Plan edits no source; `git status` restricted to the inventory doc is an acceptance criterion on every task, guaranteeing no accidental code change. |
| T-108-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs occur in this plan (grep + markdown authoring only); supply-chain gate is N/A. |
</threat_model>

<verification>
- `test -f .planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md`
- Inventory contains all three bucket headings and a Phase 109 handoff section.
- `git status` shows ONLY the inventory doc changed (no source/test/rules file modified by this plan).
- No `file:line` reference appears in more than one bucket (manual uniqueness scan).
</verification>

<success_criteria>
- A written triage inventory exists enumerating every load-bearing comment across the four in-scope trees,
  each with a file:line reference and a single-bucket classification (R316).
- The behavioral/architectural subset is handed off complete and unambiguous for Phase 109.
- The decision-rationale bucket is complete and carries verbatim comment text, ready for plan 108-02.
- Zero source/test/rules files were modified (comment-only phase; discovery step makes no code change).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 108-01)

- `.planning/phases/108-comment-audit-decision-rationale-extraction/108-COMMENT-INVENTORY.md` — the
  triage inventory with three classified buckets (Decision-Rationale, Behavioral/Architectural,
  Genuinely-Local), a per-tree/per-bucket summary table, and a "Phase 109 Handoff" section for the
  behavioral/architectural subset. This is the single source of truth consumed by plan 108-02 (R317) and
  Phase 109 (R318).
</artifacts_produced>

<output>
Create `.planning/phases/108-comment-audit-decision-rationale-extraction/108-01-SUMMARY.md` when done.
</output>
