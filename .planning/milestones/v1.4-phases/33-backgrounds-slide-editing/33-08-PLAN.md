---
phase: 33-backgrounds-slide-editing
plan: 08
type: execute
wave: 3
depends_on: ["33-02", "33-03", "33-04", "33-05"]
files_modified:
  - src/stores/slideGroups.ts
  - src/components/slides/SlideGrid.vue
  - src/components/slides/__tests__/SlideGrid.test.ts
autonomous: true
requirements: [R055, R063]

must_haves:
  truths:
    - "A group background can be set and cleared from the grid, mirroring the existing group-level music control that sits directly above it."
    - "Setting a group background never overwrites or clears any slide's own background — the slide keeps winning."
    - "A group with zero slides accepts and stores a background; nothing renders until a slide exists."
    - "A group background is a single scalar, not a list — there is no ordering to specify."
    - "E1 zero-one-many — exactly one menu is open at a time across the whole grid; opening a different card's trigger closes whichever was open."
    - "A SONG group's own background control is available (background is group media, like the bed audio beside it), while its slide STRUCTURE stays read-only."
    - "A card whose stored entry does not resolve gets an empty item list and therefore no menu at all — never a menu offering an action that would open an empty drawer."
    - "The group control shows the song's background as inherited only when the group's own is empty and the group is a SONG group; every other group kind has no song underneath it and shows nothing."
  artifacts:
    - "src/stores/slideGroups.ts — GroupBackgroundPatch and setGroupBackground(), structurally mirroring setGroupBedMedia"
    - "src/components/slides/SlideGrid.vue — a BackgroundControl row below the music control, openMenuEntryId state, per-card menu items, and a menu-action relay"
    - "src/components/slides/__tests__/SlideGrid.test.ts — group background, one-menu-open, and per-card item-list cases"
  key_links:
    - "The grid holds the single openMenuEntryId ref; cards hold no menu state, which is what makes one-open-at-a-time enforceable without cross-card coordination"
    - "Per-card items come from slideActionMenuItems with the grid's real gates — canMutateGroup for mutation, the selected plan item's kind for the Hymn discriminator"
    - "The group control's inherited display is derived from the already-resolved provenance on the group's own assembled slides, not from a new song-lyrics prop"
---

<objective>
Give the grid a group-level background control and make it the owner of menu state.

Purpose: R055 puts a background control beside the group's existing music control, and R063's
per-type item list needs a caller that supplies the real gates. Both belong at the grid, which
already owns group-level media writes and already knows the selected plan item's kind.
Output: a `setGroupBackground` store action, a `BackgroundControl` row, and a single
`openMenuEntryId` ref driving every card's menu.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-backgrounds-slide-editing/33-CONTEXT.md
@.planning/phases/33-backgrounds-slide-editing/33-RESEARCH.md
@.planning/phases/33-backgrounds-slide-editing/33-PATTERNS.md
@.planning/phases/33-backgrounds-slide-editing/33-UI-SPEC.md
@.planning/phases/33-backgrounds-slide-editing/33-VALIDATION.md
@.planning/phases/33-backgrounds-slide-editing/33-02-SUMMARY.md
@.planning/phases/33-backgrounds-slide-editing/33-03-SUMMARY.md
@.planning/phases/33-backgrounds-slide-editing/33-04-SUMMARY.md
@.planning/phases/33-backgrounds-slide-editing/33-05-SUMMARY.md
</context>

## Artifacts this phase produces

**New files (6):** `SlideActionMenu.vue` + test (33-02) · `useBackgroundUpload.ts` + test (33-03) ·
`BackgroundControl.vue` + test (33-03).

**New exported symbols:** `MenuItemKey`, `MenuItem`, `slideActionMenuItems()`,
`backgroundImageLabel()` (33-02) · `BACKGROUND_MAX_BYTES`, `UseBackgroundUploadReturn`,
`useBackgroundUpload()` (33-03) · `setSongBackground()` (33-06) · **`GroupBackgroundPatch`,
`setGroupBackground()` (this plan)**.

**Type fields ADDED:** `GroupSlideEntry.backgroundImageUrl?`, `SlideGroup.backgroundImageUrl?`,
`SongLyrics.backgroundImageUrl?`, `SlideBase.backgroundImageUrl?`,
`SlideBase.backgroundSource?: 'slide' | 'group' | 'song'` (all 33-01).

**Type field REMOVED:** the per-slide audio scope field on `GroupSlideEntry` (33-04).

**New props / emits**

| Component | Props added | Emits added | Owner |
|---|---|---|---|
| `SlideActionMenu.vue` | `entryId`, `items`, `open` | `toggle`, `select` | 33-02 |
| `BackgroundControl.vue` | `imageUrl?`, `caption`, `addLabel`, `inheritedFrom?`, `isEditor`, `orgId` | `attach`, `remove` | 33-03 |
| `SlideCard.vue` | `menuItems?`, `menuOpen?` | `menu-toggle`, `menu-select` | 33-05 |
| `SlideGrid.vue` | — | **`menu-action`** | **33-08** |
| `EditSlideDrawer.vue` | `mode`, `pendingAction` | `pending-action-consumed` | 33-07 |
| `EditSlideDrawer.vue` | — | `edit-in-scripture` **removed** | 33-09 |

**New `data-testid` values (this plan):** `slide-grid-group-background`.
Full phase-wide list: see `33-01-PLAN.md` § Artifacts.

## Deliberate deletions (phase-wide — none of these is breakage)

| Deletion | Owner |
|---|---|
| The per-slide audio scope field, the drawer's scope pills, its two class constants, its whole-group attach function, its scope branch, and its reset line | 33-04 |
| `src/stores/slideGroups.ts`'s stale "Open Question 1 resolved" doc-comment paragraph | 33-04 |
| `EditSlideDrawer.test.ts`'s `:813-957` describe block, wholesale | 33-04 |
| `SlidesTab.vue`'s `drawerOpen.value = true` line inside `onSelectSlide` — one line only | 33-09 |
| The drawer's in-body "Edit in song" / "Edit in scripture" link buttons and the `edit-in-scripture` emit | 33-09 |
| `SlideCard.vue`'s root `<button>` element, replaced by `<div role="button" tabindex="0">` | 33-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setGroupBackground store action</name>
  <read_first>
    - src/stores/slideGroups.ts lines 140-205 (`BedMediaPatch` and `setGroupBedMedia` in full — the existence check, the explicit-clear flag and its recorded reason, the skeleton-create merge and the race reasoning that makes it idempotent)
    - src/stores/slideGroups.ts lines 1-25 (imports, including the field-deletion helper and the undefined-stripping helper)
    - src/types/slideGroup.ts (`SlideGroup`, now carrying the group background field from 33-01)
    - src/components/slides/__tests__/SlideGrid.test.ts (the store-mocking idiom the grid's tests use)
  </read_first>
  <files>src/stores/slideGroups.ts</files>
  <behavior>
    - Against an existing group document with a URL supplied → updates only the background field and the timestamp; `slides` and the bed audio field are untouched.
    - Against an existing group document with the clear flag → removes the background field via an explicit field deletion.
    - Against a group document that does not exist yet → creates the same skeleton shape the bed-media path creates, carrying the background field, with a merge write.
    - Neither branch ever writes the `slides` array of an existing document.
  </behavior>
  <action>
Add `GroupBackgroundPatch` (carrying `serviceId`, an optional background URL, and an optional
`clearBackground` flag) and `setGroupBackground(orgId, slotId, patch)` to `src/stores/slideGroups.ts`,
exported from the store's return object.

★ Mirror `setGroupBedMedia`'s structure exactly rather than extending it: the same existence check,
the same dot-path-style single-field update on the existing branch, the same explicit-clear flag using
a field deletion (an undefined value would be stripped before the intent reached Firestore), and the
same skeleton `setDoc` with merge on the missing branch — including the merge, which is what keeps the
skeleton create idempotent against a concurrently-landing materialization that would otherwise be
clobbered back to an empty slides array.

A new single-purpose action is the right call here rather than widening the bed-media patch type:
mirroring an established pattern IS following it, while a `setGroupBedMedia` that also writes
backgrounds would leave the function's own name lying about what it does.

★ Setting a group background must never touch any slide's own background. The update writes exactly
one field on the group document; it never reads or rewrites the `slides` array. Assert that
explicitly — it is the R055 adjacency truth.

Add cases for all four behaviours to the slide groups store's test coverage.
  </action>
  <acceptance_criteria>
    - `grep -c 'setGroupBackground' src/stores/slideGroups.ts` returns at least 2 — the definition and the return-object export
    - `grep -c 'clearBackground' src/stores/slideGroups.ts` returns at least 2
    - A test asserts the update-branch payload contains exactly the background field and the timestamp — no `slides` key and no bed audio key
    - A test asserts the clear branch writes a field deletion rather than an undefined value
    - A test asserts the missing-document branch writes with merge enabled
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/stores --reporter=basic && npm run type-check</automated>
  </verify>
  <done>A single-purpose group background action mirrors the bed-media action's structure exactly and never touches the slides array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the group background control below the music control</name>
  <read_first>
    - src/components/slides/SlideGrid.vue lines 60-85 (the music control's wrapper, its E5 wrapper-level gating and the recorded reason for it, and the `pt-3` spacing the new row sits under)
    - src/components/slides/SlideGrid.vue lines 275-300 (`isSongGroup`, `canMutateGroup`, `canWriteGroupMedia` and the comment explaining why the media gate deliberately omits the song-group exclusion)
    - src/components/slides/SlideGrid.vue lines 315-350 (`onAttachGroupMusic` / `onRemoveGroupMusic` — the caller-does-the-write idiom to mirror)
    - src/components/slides/BackgroundControl.vue (created in 33-03 — props and emits)
    - src/components/slides/slideDisplay.ts (`backgroundImageLabel`, added in 33-02)
    - .planning/phases/33-backgrounds-slide-editing/33-UI-SPEC.md § Phase-Specific Component Contracts §6 (the markup, the placement and spacing, the gate, and the inherited-display rule) and § Copywriting Contract (the group strings) and § Empty / Edge States E2 and E3
  </read_first>
  <files>src/components/slides/SlideGrid.vue, src/components/slides/__tests__/SlideGrid.test.ts</files>
  <behavior>
    - With a group background set, the control renders the thumbnail, the filename and the caption `applies to all {N} slides in this group, unless a slide sets its own` with the real card count substituted.
    - With no group background and no write permission, the wrapper does not render at all — no empty bordered box.
    - With no group background and write permission, the empty-state add affordance renders with the label `＋ Add background for this group`.
    - The control's `attach` emit calls the store action with the URL; its `remove` emit calls it with the clear flag.
    - A SONG group renders the control, since background is group media exactly like the bed audio beside it.
    - ★ For a SONG group whose own background is empty while the song has one, the control renders the inherited display; for every non-SONG group the inherited prop is `undefined`.
    - A group with zero slides still renders the control when permitted, and a set background persists with nothing yet rendering it.
    - The music control above is untouched — same wrapper condition, same spacing, same write path.
  </behavior>
  <action>
Add a `BackgroundControl` row inside `SlideGrid.vue` as a NEW sibling wrapper directly BELOW the
existing group music control's wrapper, with `data-testid="slide-grid-group-background"` and classes
`px-6 pt-2`. The tighter top spacing (one step under the music control's own) makes the two read as a
related pair rather than two unrelated blocks; the music control keeps its existing spacing above it.

★ Gate the WRAPPER, not just the control's contents — `v-if="group?.backgroundImageUrl ||
canWriteGroupMedia"`, exactly the condition shape the music control already uses and for the same
recorded reason: with nothing set and no add affordance, the control would render an empty bordered
box, which a locked service hits constantly.

Use `canWriteGroupMedia` (the existing computed), NOT `canMutateGroup`. Background is group MEDIA,
exactly like the bed audio it sits beside, so it uses the same gate including that gate's deliberate
song-group carve-out. This is the same reasoning already written into that computed's own comment.

Props: `imageUrl` from the group's background field; `caption` per the Copywriting Contract with the
real card count substituted; `addLabel` of `＋ Add background for this group`; `isEditor` bound to
`canWriteGroupMedia`; `orgId` from the existing prop.

★ Planner decision for `inheritedFrom` — derive it from the ALREADY-RESOLVED provenance rather than
threading a new song-lyrics prop into this component. When the group has no background of its own and
the group is a SONG group, find the first of this group's assembled slides whose resolved provenance
is the song tier and use its resolved url, with `backgroundImageLabel` supplying the label. That
combination is reachable only in exactly the case the inherited display is for, and it needs no new
prop, no store read, and no second cascade derivation. For every non-SONG group it is `undefined` —
those groups have no associated song, so there is nothing at this level to inherit from.

Wire `attach` and `remove` to `setGroupBackground` (Task 1), following the existing music control
handlers' shape: the caller performs the scoped store write, the control stays emit-only.

Then add the behaviour list's cases to `src/components/slides/__tests__/SlideGrid.test.ts`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/slides/__tests__/SlideGrid.test.ts -t "background"` passes with 0 failures and at least 7 cases
    - A named test asserts the caption text for a three-card group is exactly `applies to all 3 slides in this group, unless a slide sets its own`
    - A named test asserts `[data-testid="slide-grid-group-background"]` reports `exists() === false` when there is no group background and no write permission, and `exists() === true` when there is a group background but no write permission
    - A named test asserts the mounted control's `attach` emit produces exactly one `setGroupBackground` call carrying the URL, and its `remove` emit produces one carrying the clear flag
    - ★ A named test asserts the `inheritedFrom` prop is a defined object for a SONG group with no own background whose slides resolve their background from the song tier, and is `undefined` for a PRAYER group in the same shape
    - A named test asserts the store call for a background never carries a `slides` key
    - A named test asserts the group music control's own wrapper, props and write path are unchanged — `[data-testid]` for the music control still resolves and its attach still calls the bed-media action
    - `npx vitest run src/components/slides/__tests__/SlideGrid.test.ts` passes in full
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideGrid.test.ts && npm run type-check</automated>
  </verify>
  <done>A group background control sits below the music control with matching gating and spacing, shows the song's background as inherited only where that is meaningful, and never touches the slides array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Menu ownership — one open at a time, per-card items, and the action relay</name>
  <read_first>
    - src/components/slides/SlideGrid.vue lines 125-145 (the card grid's `minmax(200px,1fr)` track and the `SlideCard` mount with its existing select relay)
    - src/components/slides/SlideGrid.vue lines 215-250 (props including `selectedSlot`, `group`, `isEditor`, `serviceLocked`, and the emits block) and lines 275-300 (the three gating computeds)
    - src/components/slides/SlideCard.vue (its `menuItems` / `menuOpen` props and `menu-toggle` / `menu-select` emits, added in 33-05)
    - src/components/slides/slideDisplay.ts (`slideActionMenuItems` — its exact three-parameter signature, added in 33-02)
    - .planning/phases/33-backgrounds-slide-editing/33-UI-SPEC.md § Phase-Specific Component Contracts §2, final bullet (the single `openMenuEntryId` at grid level) and §3 (the per-type table) and §11 (the locked/viewer row)
    - .planning/phases/33-backgrounds-slide-editing/33-UI-SPEC.md § UI Considerations E1 and E2
  </read_first>
  <files>src/components/slides/SlideGrid.vue, src/components/slides/__tests__/SlideGrid.test.ts</files>
  <behavior>
    - Each card receives the item list `slideActionMenuItems` returns for its own stored entry, the selected plan item's kind, and the grid's mutation gate.
    - A card whose slide id resolves to no stored entry receives an empty list and therefore renders no menu.
    - Toggling one card's menu opens it; toggling a different card's menu closes the first and opens the second.
    - Toggling the same card's menu twice closes it.
    - Selecting any item closes the open menu and re-emits upward as `menu-action` carrying the slide id and the key.
    - With the service locked, no card's list contains the duplicate or delete keys.
    - With a SONG group, no card's list contains the lyrics-edit, duplicate or delete keys.
    - A HYMN group's auto-derived pristine text slide receives a list without the lyrics-edit key, while a hand-added blank slide in the same group receives one with it.
  </behavior>
  <action>
Add a single `openMenuEntryId` ref (a slide id or null) to `SlideGrid.vue`. This one ref is what makes
"exactly one menu open at a time" true across the whole grid without any cross-card coordination —
the cards hold no menu state of their own.

Compute each card's item list by calling `slideActionMenuItems` with three arguments: the stored
`GroupSlideEntry` matched to that card by id out of the group's `slides`, the selected plan item's
kind, and `canMutateGroup` (the existing computed that already composes editor, not-locked and
not-a-song-group). Pass an empty array when no stored entry matches — the pre-materialization
fallback-id window — so that card renders no menu rather than one whose only action would open an
empty drawer.

★ Do NOT re-implement any part of the per-type decision here. `slideActionMenuItems` is the single
place that decides; this component supplies the inputs and renders the result. That is what keeps the
Hymn discriminator and both conservative fallbacks in one tested function instead of three copies of
a switch.

Bind each `SlideCard`'s `menuOpen` to whether `openMenuEntryId` equals that card's slide id. Handle
`menu-toggle` by setting the ref to that id when it is not already open, or to null when it is.
Handle `menu-select` by clearing the ref and re-emitting a new `menu-action` carrying the slide id and
the key.

Add `menu-action: [slideId: string, key: MenuItemKey]` to this component's emits. This grid does not
act on the keys itself — the tab one level up owns the dispatch, exactly as it already owns the
selection relay.

Then add the behaviour list's cases to `src/components/slides/__tests__/SlideGrid.test.ts`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/slides/__tests__/SlideGrid.test.ts -t "menu"` passes with 0 failures and at least 8 cases
    - ★ A named test mounts a grid with two cards, toggles the first card's menu, asserts the first card's `menuOpen` prop is true and the second's is false, then toggles the SECOND card's menu and asserts the first is now false and the second true
    - A named test asserts toggling the same card twice leaves its `menuOpen` prop false
    - A named test asserts a `menu-select` from a card produces exactly one `menu-action` on the grid carrying the slide id and the key, and that the open ref was cleared
    - A named test asserts a card whose slide id matches no stored entry receives `menuItems` of length 0
    - A named test asserts that with the service locked, no card's `menuItems` contains the `duplicate` or `delete` key
    - A named test asserts that for a SONG group, no card's `menuItems` contains the `edit-lyrics`, `duplicate` or `delete` key
    - A named test asserts a HYMN group's entry with no stored body receives a list without `edit-lyrics`, while an entry with an empty-string body in the same group receives one with it
    - `npx vitest run src/components/slides/__tests__/SlideGrid.test.ts` passes in full
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideGrid.test.ts && npm run type-check</automated>
  </verify>
  <done>One ref at the grid enforces one open menu at a time, every card's list comes from the single pure function with the grid's real gates, and item selections relay upward without being acted on here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grid gating computeds → per-card menu item lists | The grid's gate values decide which mutating actions every card offers |
| Browser file picker → the group document's background field | A group-level upload crosses into Storage and then into Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-19 | Elevation of Privilege | per-card item computation | critical | mitigate | The mutation gate passed to the pure function is the grid's existing `canMutateGroup`, which already composes editor, not-locked and not-a-song-group — the same computed the drop tile and add-slide already trust. Two dedicated tests assert the locked and song-group lists carry no mutating keys. The authoritative enforcement remains the Firestore rules, which gate group writes on org-editor plus parent-draft status |
| T-33-20 | Elevation of Privilege | `setGroupBackground` | high | mitigate | Reached only through handlers that re-check `canWriteGroupMedia` before writing, the same shape the bed-media handlers already use. The existing `slideGroups` update rule needs no change for a new field — verified: it checks only service-id immutability and parent-draft status, with no field allowlist |
| T-33-21 | Tampering | group background write blast radius | medium | mitigate | The action writes exactly one field and never the `slides` array, so a group background change can never clobber slide content or a slide's own background. Asserted by a test on the write payload's keys |
| T-33-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero new dependencies in this phase |
</threat_model>

<verification>
- `npx vitest run src/components/slides src/stores` is fully green.
- `npm run type-check` (the `vue-tsc --build` form) exits 0.
- No `firestore.rules` or `storage.rules` change was made.

## Manual-only (deferred under the standing autonomy grant)

- The menu does not interfere with dragging: drag a slide by its grip and confirm no menu opens; open
  the menu, then try to drag, and confirm sane behaviour. SortableJS drag behaviour is not exercised
  in jsdom.
- The widest four-item panel at the narrowest 200px card width, checked visually — the mounted-width
  assertion covers the class contract, not the rendered pixels.
</verification>

<success_criteria>
- A group background control sits below the music control with the same wrapper-level gating.
- One ref enforces one open menu at a time across the whole grid.
- Every card's item list comes from the single pure function, with an empty list producing no menu.
- The group background write touches exactly one field.
</success_criteria>

<output>
Create `.planning/phases/33-backgrounds-slide-editing/33-08-SUMMARY.md` when done.
</output>
