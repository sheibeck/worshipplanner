---
phase: 32-save-reliability-autosave-fix-persistent-status
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/useAutoSave.ts
  - src/composables/__tests__/useAutoSave.test.ts
autonomous: true
requirements: [R040, R041]

must_haves:
  truths:
    - "AutoSaveStatus is 'idle' | 'pending' | 'saving' | 'saved' | 'error' — the fifth member exists, which is precisely why ServiceEditorView had to hand-roll a failure state"
    - "A rejected saveFn sets status to 'error' on BOTH the debounced path and the flush() path — it is never left stranded at 'saving' and never silently dropped to 'idle' (BL-02's own lesson)"
    - "A 'saved' status persists indefinitely — advancing timers by 60 seconds leaves it 'saved'. The 3-second fade-to-idle R040 replaces is gone from both call sites"
    - "Two mutations inside one 800ms debounce window collapse into exactly one write carrying both, applied in the order they hit the watched source — the debounce coalesces, it never drops or reorders"
    - "A save with no intervening change is skipped rather than issuing a redundant write — flush() with a non-pending status is a no-op, and the timer callback drops to idle when isDirty is false"
    - "The composable's total test count stays flat or grows; a drop of two with no replacement is the RESEARCH-flagged warning sign that real coverage was deleted rather than updated"
    - statement: "A mutation dispatched while a save is in flight is not lost — it is either included in the in-flight write or triggers a follow-up write once that write settles."
      verification: backstop
  artifacts:
    - "src/composables/useAutoSave.ts — AutoSaveStatus extended with 'error'; both fade-timer blocks and their helper removed; a catch added to both save call sites"
    - "src/composables/__tests__/useAutoSave.test.ts — the two fade-asserting tests updated (not deleted), plus new error-path and coalescing tests"
  key_links:
    - "The composable's catch only sets 'error' generically — it must NOT know about ServiceLockedError; that discrimination stays in the caller's saveFn wrapper (plan 05)"
    - "useAutoSave has no Firestore knowledge and no echo concept; it is strictly above the R039 store-layer fix and does not participate in it"
    - "The watcher sets 'pending' synchronously on every dirty mutation, which is what makes flush()'s 'only flush if pending' guard safe for plan 05's onMarkAsPlanned migration"
  prohibitions:
    - statement: "The status must never report a save as succeeded when it has not been persisted. No path may set 'saved' or write a Saved HH:MM timestamp without the underlying write having resolved successfully, and no catch may swallow a rejection to keep the indicator looking clean."
      status: kept
      verification: flagged-unverified
---

<objective>
Give `useAutoSave` the two behaviours R040 and R041 need and it does not currently have: a
failure status, and a `Saved` state that persists instead of self-erasing after three seconds.

Purpose: every other status work in this phase reads from this composable. Landing the
contract change here, in isolation and against the composable's existing test file, means
plan 05's much larger `ServiceEditorView` migration is not simultaneously debugging the
composable it is migrating onto.

Output: a five-member `AutoSaveStatus`, a `catch` on both save paths, no fade timer, and a
test file whose count did not shrink.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-UI-SPEC.md
@.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-RESEARCH.md
@.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide registry (identical in every Phase 32 plan so no plan invents a competing name).

**New files**

| Path | Exports / contents |
|---|---|
| `src/stores/saveStatus.ts` | store id `'saveStatus'`; `useSaveStatus`; `SaveStatusEntry` interface; members `entries`, `set(surfaceId, entry)`, `clear(surfaceId)`, `entryFor(surfaceId)`, `mostUrgent` |
| `src/stores/toasts.ts` | store id `'toasts'`; `useToasts`; `Toast` interface; members `toasts`, `push(message)`, `dismiss(id)` |
| `src/components/SaveStatusIndicator.vue` | component `SaveStatusIndicator`; prop `surfaceId: string` |
| `src/components/ToastHost.vue` | component `ToastHost`; no props |
| `src/stores/__tests__/saveStatus.test.ts`, `src/stores/__tests__/toasts.test.ts`, `src/components/__tests__/SaveStatusIndicator.test.ts`, `src/components/__tests__/ToastHost.test.ts` | new test files |

**New members on existing files**

| File | New symbol |
|---|---|
| `src/stores/services.ts` | `ownWriteEchoIds: Ref<string[]>`, `isOwnWriteEcho(serviceId: string): boolean` (plan 01) |
| `src/composables/useAutoSave.ts` | `AutoSaveStatus` type-union gains the member `'error'` (**this plan**) |

**New `data-testid` values:** `save-status`, `save-status-error`, `service-save-status-bar`,
`toast-host`, `toast-{id}`

**Retired `data-testid` values (deliberate breaking rename — plans 05 and 06):** the three
per-status handles in each editor header, and the ServiceEditorView inline failure-text handle.

**New `surfaceId` conventions:** `service:{serviceId}`, `congregational:{readingId}`,
`scripture:{readingId}`, `song-lyrics:{songId}`
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: savedFadeTimer -->
<!-- planner-discipline-allow: clearSavedFadeTimer -->

<task type="auto" tdd="true">
  <name>Task 1: Update and extend useAutoSave's tests to describe the new contract (red)</name>
  <files>src/composables/__tests__/useAutoSave.test.ts</files>
  <read_first>
    - `src/composables/__tests__/useAutoSave.test.ts` — the whole file (13 tests, ~300 lines).
      Pay attention to the test at line 65 (`transitions through idle -> pending -> saving ->
      saved -> idle`) and the test at line 271 (`saved status fades to idle after 3 seconds`).
    - `src/composables/useAutoSave.ts` — the whole file (164 lines).
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-RESEARCH.md`
      § Common Pitfalls → Pitfall 5.
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-UI-SPEC.md` § 1.
  </read_first>
  <behavior>
    - A rejected `saveFn` on the debounced path leaves `status.value === 'error'`, not `'saving'`.
    - A rejected `saveFn` inside `flush()` leaves `status.value === 'error'`, not `'saving'`.
    - After a successful save, advancing timers by 60000ms leaves `status.value === 'saved'`.
    - The existing idle → pending → saving → saved transition sequence still holds, with the
      tail asserting persistence instead of the fade.
    - Two source mutations inside one debounce window produce exactly one `saveFn` call, and
      the value the save observes is the second (later) mutation.
    - A `flush()` with `status` not `'pending'` calls `saveFn` zero times.
    - A mutation dispatched while a save is in flight is not lost: with the inflight guard
      active, the reschedule fires a follow-up `saveFn` call after the in-flight one settles.
  </behavior>
  <action>
    Modify `src/composables/__tests__/useAutoSave.test.ts` — do not rewrite it.

    Edit the transition test at line 65: keep the whole idle/pending/saving/saved sequence and
    change ONLY its tail. Where it currently advances timers by 3000ms and asserts the status
    became idle, advance the same 3000ms and assert the status is still `'saved'`. Rename the
    test so its name no longer promises a return to idle.

    Replace the test at line 271 entirely, rather than deleting it: same setup, but advance
    timers by 60000ms and assert `status.value` is still `'saved'`. Name it for the behaviour
    R040 requires — that the timestamp persists until the next change. The point of replacing
    rather than deleting is that "the fade is removed" needs its own positive assertion, not
    merely the absence of the old one.

    Add three new tests, additive:
    (a) debounced path failure — `saveFn` rejects; after advancing past the debounce and
        flushing promises, `status.value` is `'error'`;
    (b) `flush()` path failure — arm a pending change, make `saveFn` reject, `await flush()`,
        assert `status.value` is `'error'`;
    (c) the concurrency case — dispatch a second source mutation while the first save is still
        in flight, and assert `saveFn` is ultimately called twice (the reschedule path), with
        the second call observing the later value. This is the R039/concurrency backstop; if
        the existing inflight-guard test at line 102 already proves half of it, extend that
        test rather than duplicating its setup, and say so in the SUMMARY.

    Add one coalescing test if the existing debounce test at line 35 does not already assert
    both halves: exactly one `saveFn` call for two mutations in one window, AND the saved value
    reflects the second mutation (order preserved, nothing dropped).

    Run the file. The three new failure/persistence assertions must be red; the coalescing and
    concurrency ones may already be green against today's code — that is fine and expected,
    they are regression locks, not repro tests. Note in the SUMMARY which were red and which
    were already green.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/composables/__tests__/useAutoSave.test.ts` exits non-zero.
    - Its output shows failures whose assertions are on `status.value` being `'error'` and on
      `'saved'` persisting — not module-resolution or type errors.
    - The number of `it(` occurrences in the file is at least 16 (13 today, minus 0 deleted,
      plus at least 3 new): `grep -c "  it(" src/composables/__tests__/useAutoSave.test.ts`
      returns 16 or more.
    - `grep -c "60000" src/composables/__tests__/useAutoSave.test.ts` is at least 1.
    - `git log -1 --name-only` lists only
      `src/composables/__tests__/useAutoSave.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/composables/__tests__/useAutoSave.test.ts</automated>
  </verify>
  <done>The test file describes the five-status contract, has not shrunk, and is red on exactly the new behaviours.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AutoSaveStatus with 'error', remove the fade, add the catch (green)</name>
  <files>src/composables/useAutoSave.ts</files>
  <read_first>
    - `src/composables/useAutoSave.ts` — the whole file. Note line 3 (the type union), line 48
      (the fade timer handle), lines 59-64 (its clearing helper), lines 87-93 (the fade block
      inside `scheduleSave`'s timeout), lines 143-148 (the identical fade block inside
      `flush()`), line 157 (the helper call inside `cleanup()`), and lines 84-96 / 140-151
      (the two `try`/`finally` blocks with no `catch`).
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-PATTERNS.md`
      § `src/composables/useAutoSave.ts` — it carries the exact before/after shapes.
    - `src/views/ServiceEditorView.vue` lines 2054-2108 — the BL-02 comment block explaining
      why a stranded `'saving'` is a real defect and not a cosmetic stall. Read it so you
      understand what the catch is protecting; do not copy its ServiceLockedError branch into
      the composable.
  </read_first>
  <action>
    In `src/composables/useAutoSave.ts`:

    Extend the exported `AutoSaveStatus` union with a fifth member, `'error'`.

    Delete the three-second fade entirely. That means: the timer handle declared at line 48;
    the helper that clears it declared at lines 59-64; both `setTimeout(..., 3000)` blocks that
    reset a `'saved'` status back to `'idle'` (one inside `scheduleSave`'s timeout, one inside
    `flush()`); and the helper's call inside `cleanup()`. All four sites go together — leaving
    the handle or the helper behind is dead code, and 32-UI-SPEC.md § 1 says so explicitly.
    `cleanup()` keeps clearing the debounce timer; only the fade half goes. Update
    `cleanup()`'s doc comment so it no longer promises to clear a timer that no longer exists.

    Add a `catch` to BOTH `try` blocks (the one in `scheduleSave`'s timeout and the one in
    `flush()`), each setting `status.value = 'error'`. Keep the `finally` that clears the
    inflight flag. The catch must be generic — it sets the status and nothing else. It must NOT
    inspect the error type, must NOT know about `ServiceLockedError`, and must NOT revert any
    caller state; that discrimination is caller business logic and stays in the `saveFn`
    wrapper the caller passes (plan 05 owns it).

    Update the composable's header doc comment: it currently describes a four-status lifecycle.
    State the five statuses and that a rejected `saveFn` surfaces as a status rather than an
    unhandled rejection. Describe the removed fade by behaviour ("the saved state is terminal
    until the next pending transition"), not by naming the deleted identifiers.

    Change nothing else. The debounce, the inflight guard with its reschedule, `flush()`'s
    "only if pending" guard, the `isDirty` skip, the first-trigger suppression and the
    `onUnmounted(cleanup)` registration all stay exactly as they are — 32-CONTEXT.md is
    explicit that `useAutoSave` is extended, not rewritten.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/composables/__tests__/useAutoSave.test.ts` exits 0 with every test
      passing, including the ones that were already green in Task 1.
    - `grep -c "'error'" src/composables/useAutoSave.ts` is at least 3 (the union member plus
      the two catch assignments).
    - `grep -v '^\s*\*' src/composables/useAutoSave.ts | grep -v '^\s*//' | grep -c 'savedFadeTimer'` returns 0.
    - `grep -v '^\s*\*' src/composables/useAutoSave.ts | grep -v '^\s*//' | grep -c 'clearSavedFadeTimer'` returns 0.
    - `grep -c '3000' src/composables/useAutoSave.ts` returns 0.
    - `grep -c 'catch' src/composables/useAutoSave.ts` is 2.
    - `grep -c 'ServiceLockedError' src/composables/useAutoSave.ts` returns 0.
    - `npm run type-check` exits 0.
    - `npx vitest run src/` shows no failing FILE beyond the freshly-measured baseline.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/composables/__tests__/useAutoSave.test.ts && npm run type-check</automated>
  </verify>
  <done>The composable has five statuses, catches on both save paths, no fade machinery, and its full test file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller `saveFn` → composable | A rejected promise from arbitrary caller code crosses into the composable; it must be contained, not propagated as an unhandled rejection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-04 | Spoofing (of success) | `useAutoSave` status reporting | medium | mitigate | `'saved'` is assigned only on the line immediately after `await saveFn()` resolves; the new catch sets `'error'` and never `'saved'` or `'idle'`. Tests (a) and (b) in Task 1 assert both paths. |
| T-32-05 | Repudiation / silent failure | the missing `catch` | high | mitigate | Today a rejection propagates uncaught and strands the status, which (per BL-02) also permanently closes the caller's remote-merge branch. The catch closes it. |
| T-32-06 | Information disclosure | error detail in status | low | mitigate | The catch sets a bare enum value and never stores or renders the caught error's message, so raw Firestore error text cannot reach user-facing copy through this path. |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed in this phase — `32-RESEARCH.md` § Package Legitimacy Audit records "not applicable". No install task exists to gate. |
</threat_model>

<verification>
- `npx vitest run src/composables/__tests__/useAutoSave.test.ts` — all green, count not below 16.
- `npm run type-check` (the `vue-tsc --build` form — the `-p tsconfig.app.json` form silently
  skips test files and is NOT sufficient evidence) — exit 0.
- `npx vitest run src/` — no failing file beyond the freshly-measured baseline
  (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`). Re-measure; STATE.md's
  "10-file baseline" is obsolete since the 2026-07-29 deletion of `.gsd/`.
- Consumer check: the three editors already call `useAutoSave` and render on its status. Adding
  a union member and removing the fade must not break their existing suites — run
  `npx vitest run src/components/__tests__/CongregationalEditor.test.ts src/components/__tests__/ScriptureSlideEditor.test.ts src/components/__tests__/SongLyricEditor.test.ts`
  and report any breakage rather than fixing it here (plan 06 owns those files).
</verification>

<success_criteria>
1. `AutoSaveStatus` has five members including `'error'`.
2. Both save paths catch and set `'error'`; neither can strand the status at `'saving'`.
3. No fade machinery remains, and a positive test asserts `'saved'` survives 60 seconds.
4. The composable's test count did not shrink.
5. `npm run type-check` clean; full suite shows no new failing file.
</success_criteria>

<execution_notes>
- Do NOT run `git stash`. Do NOT run a project-wide `lint --fix`. Do NOT create, modify or
  commit `.env.local`.
- This plan is Wave 1 and runs in parallel with plan 01. It shares no file with plan 01.
- If the three editors' component suites break because they mock `useAutoSave` and assert on
  its four-status union, record it and hand it to plan 06 — do not edit those files here.
</execution_notes>

<output>
Create `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-02-SUMMARY.md` when done.
State which Task 1 assertions were red and which were already green.
</output>
