---
phase: 32-save-reliability-autosave-fix-persistent-status
plan: 04
type: execute
wave: 3
depends_on: ["32-03"]
files_modified:
  - src/components/SaveStatusIndicator.vue
  - src/components/__tests__/SaveStatusIndicator.test.ts
  - src/components/ToastHost.vue
  - src/components/__tests__/ToastHost.test.ts
  - src/components/AppShell.vue
autonomous: true
requirements: [R040, R041]

must_haves:
  truths:
    - "SaveStatusIndicator at idle renders nothing at all — no placeholder box and no reserved layout height. Before the first edit in a session there is nothing to report"
    - "Saving… in italic gray IS this component's loading state — no spinner, no skeleton, no separate loading affordance is introduced"
    - "The error state renders red text carrying data-testid save-status-error, showing one of exactly two fixed strings, inside the SAME single aria-live polite region — there is no nested second live region"
    - "Exactly one of four mutually exclusive spans renders at a time, and Saved h:mm AM/PM persists until the next change because the three-second fade was removed from the composable"
    - "Every string this component renders is a fixed enum; no user-supplied text ever reaches it — the only variable segment is the timestamp, at most eight characters"
    - "Zero toasts leaves the flex column container with no children, collapsed to zero height, so it neither renders nor intercepts pointer events over the page beneath it"
    - "A toast has no loading state — it is pushed fully formed on the error edge and contains no async content"
    - "ToastHost IS the error surface: role alert, a bold Save failed. lead, the mirrored body, and a dismiss control"
    - "A populated toast card renders an aria-hidden warning icon, the bold lead, the mirrored body and the dismiss button on the red-950 background with red-800 border and red-400 text"
    - "The toast body wraps rather than clipping — min-w-0 flex-1 on the paragraph, a max width cap above the sm breakpoint, and full width minus side margins below it"
    - "Toasts are array-backed and stack vertically with an eight-pixel gap, each on its own timer; edge-triggering bounds the stack structurally, so no cap logic exists"
    - "Both possible toast bodies are fixed enum strings of at most fifty-nine characters; no user-supplied text ever enters a toast"
    - statement: "The longest rendered string — the fifty-nine-character generic failure sentence — must not overflow or clip the narrowest host header; the SongLyricEditor header group is the tightest, and a mounted-width test must assert the text wraps rather than clips."
      verification: backstop
    - statement: "A toast raised by a surface that unmounts before its six-second auto-dismiss elapses must still dismiss cleanly and must not leak its timer; a test must assert unmounting the raising surface leaves no pending timer and no orphaned toast."
      verification: backstop
  artifacts:
    - "src/components/SaveStatusIndicator.vue — one shared component, prop surfaceId, reads its own entry from useSaveStatus"
    - "src/components/__tests__/SaveStatusIndicator.test.ts — one test per rendered state plus the accessibility attributes and the overflow backstop"
    - "src/components/ToastHost.vue — fixed-position stack rendering useToasts, role alert per card, dismiss control"
    - "src/components/__tests__/ToastHost.test.ts — empty/populated/many, role alert, dismiss, auto-dismiss, mirrored copy, unmount backstop"
    - "src/components/AppShell.vue — ToastHost mounted once, as a sibling after main"
  key_links:
    - "SaveStatusIndicator reads useSaveStatus().entryFor(surfaceId) through a computed, so an unknown or not-yet-known surfaceId renders idle rather than throwing"
    - "aria-live polite wraps ALL four states including error; the assertive interrupt is the toast's role=alert, which is a separate element"
    - "ToastHost sits at z-[60], above every existing z-50 Teleport dialog and above AppShell's own z-20 backdrop — a genuinely new top layer reserved for this purpose"
    - "AppShell is the single mount point; no route or view mounts a second ToastHost"
---

<objective>
Build the two components R040 and R041 specify: `SaveStatusIndicator.vue`, the one shared
status element every autosaving surface will render, and `ToastHost.vue`, the app-level failure
alert, mounted once in `AppShell.vue`.

Purpose: 32-UI-SPEC.md already fixes the markup for both, verbatim. Building them here — before
any surface is migrated — means plans 05 and 06 are a swap, not a design exercise, and means
both components get their own focused test file rather than being asserted at second hand
through a view test.

Output: two components, two test files, one mount point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-UI-SPEC.md
@.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-PATTERNS.md
@.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide registry (identical in every Phase 32 plan so no plan invents a competing name).

**New files**

| Path | Exports / contents |
|---|---|
| `src/stores/saveStatus.ts` | `useSaveStatus`, `SaveStatusEntry`, `entries`/`set`/`clear`/`entryFor`/`mostUrgent` (plan 03) |
| `src/stores/toasts.ts` | `useToasts`, `Toast`, `toasts`/`push`/`dismiss` (plan 03) |
| `src/components/SaveStatusIndicator.vue` | component `SaveStatusIndicator`; prop `surfaceId: string` (**this plan**) |
| `src/components/ToastHost.vue` | component `ToastHost`; no props (**this plan**) |
| `src/components/__tests__/SaveStatusIndicator.test.ts`, `src/components/__tests__/ToastHost.test.ts` | new test files (**this plan**) |

**New members on existing files**

| File | New symbol |
|---|---|
| `src/stores/services.ts` | `ownWriteEchoIds`, `isOwnWriteEcho(serviceId)` (plan 01) |
| `src/composables/useAutoSave.ts` | `AutoSaveStatus` gains `'error'` (plan 02) |
| `src/components/AppShell.vue` | renders `<ToastHost />` (**this plan**) |

**New `data-testid` values (all minted here):** `save-status`, `save-status-error`,
`toast-host`, `toast-{id}`. Plan 05 mints `service-save-status-bar`.

**Retired `data-testid` values (deliberate breaking rename — plans 05 and 06):** the three
per-status handles in each editor header, and the ServiceEditorView inline failure-text handle.

**New `surfaceId` conventions:** `service:{serviceId}`, `congregational:{readingId}`,
`scripture:{readingId}`, `song-lyrics:{songId}`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SaveStatusIndicator.vue — the one shared status component</name>
  <files>src/components/SaveStatusIndicator.vue, src/components/__tests__/SaveStatusIndicator.test.ts</files>
  <read_first>
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-UI-SPEC.md` § 2 —
      **the authoritative markup, copy it rather than inferring it**; plus § Copywriting
      Contract (the four state strings and the timestamp format) and § UI Considerations → E1.
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-PATTERNS.md`
      § `src/components/SaveStatusIndicator.vue` — the same markup plus the `formattedSavedAt`
      derivation.
    - `src/stores/saveStatus.ts` as written by plan 03.
    - `src/components/CongregationalEditor.vue` lines 1-25 — the four-state span idiom this
      component consolidates, so you can see what is being retired.
    - `src/components/__tests__/CollapsibleSection.test.ts` — a small existing component test for
      the file's shape and import conventions.
  </read_first>
  <behavior>
    - status idle: the component renders its wrapper but no inner span, and the wrapper's text
      content is empty.
    - status pending: renders `Saving soon…`.
    - status saving: renders `Saving…`.
    - status saved with a savedAt of 14:47 local: renders `Saved 2:47 PM`.
    - status error with the reorder text: renders that exact sentence and carries
      `data-testid="save-status-error"`.
    - status error with the generic text: renders that exact sentence.
    - The wrapper always carries `aria-live="polite"`, `aria-atomic="true"` and
      `data-testid="save-status"`, in every state including error.
    - There is exactly one element with an `aria-live` attribute in the mounted subtree — never
      two, never a nested one inside the error branch.
    - An unknown `surfaceId` renders as idle rather than throwing.
    - Changing the store entry re-renders the component reactively.
  </behavior>
  <action>
    Create `src/components/SaveStatusIndicator.vue` using the markup in 32-UI-SPEC.md § 2
    verbatim: a single wrapper `div` with class `text-xs`, `aria-live="polite"`,
    `aria-atomic="true"` and `data-testid="save-status"`, containing a `v-if` / `v-else-if`
    chain of four mutually exclusive spans for pending, saving, saved and error. The pending and
    saving spans use `italic text-gray-400`; saved uses `text-green-400`; error uses
    `text-red-400` and carries `data-testid="save-status-error"`. There is no branch for idle —
    that is what makes idle render nothing, with no placeholder and no reserved height.

    Script: `<script setup lang="ts">`, one prop `surfaceId: string`. Derive the entry with a
    `computed` calling `useSaveStatus().entryFor(props.surfaceId)` so an unknown key resolves to
    idle. Derive `formattedSavedAt` with
    `toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit' })` — the same `'en-US'`
    locale the rest of this codebase already uses for wall-clock formatting. Wall-clock, not
    relative time; R040's wording is literal on that.

    Deliberate design points to preserve, all from 32-UI-SPEC.md § 2:
    - One `aria-live` region wrapping all four states, error included. A save failure is read in
      place, politely; the assertive interrupt is the toast, a separate element. Do not add a
      second nested live region inside the error branch.
    - `aria-atomic` is true because each transition replaces the whole sentence, so the reader
      should speak it whole rather than diffing.
    - The component is icon-free by design. The three editors' current colour-only yellow dot
      with a `title` attribute is replaced by real text precisely so the change is announced,
      not merely seen.
    - No variant prop and no per-surface wrapper component. The four call sites differ only in
      which `surfaceId` they pass.

    Create `src/components/__tests__/SaveStatusIndicator.test.ts` covering every case in the
    behavior block. Mount with real Pinia (`setActivePinia(createPinia())` plus
    `global.plugins`), drive the store directly with `useSaveStatus().set(...)`, and assert
    rendered text and attributes. Assert the single-live-region rule as a count over the mounted
    subtree — `wrapper.findAll('[aria-live]').length` is 1 — rather than by eye; that count-based
    style is the precedent this codebase set in `SongLyricsTab.r035.test.ts`.

    Add the E1 overflow backstop test: render the error state carrying the fifty-nine-character
    generic sentence inside a narrow fixed-width parent, and assert the rendered element has no
    truncation class and that its text content is the complete sentence rather than an elided
    one. jsdom cannot measure real layout, so state plainly in the SUMMARY that this test proves
    "no truncation class and full text present", not "does not visually clip", and record the
    visual check as a deferred manual verification.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/SaveStatusIndicator.test.ts` exits 0.
    - The test file contains at least 10 `it(` blocks.
    - `grep -c 'aria-live="polite"' src/components/SaveStatusIndicator.vue` is 1.
    - `grep -c 'aria-atomic="true"' src/components/SaveStatusIndicator.vue` is 1.
    - `grep -c 'data-testid="save-status"' src/components/SaveStatusIndicator.vue` is 1.
    - `grep -c 'data-testid="save-status-error"' src/components/SaveStatusIndicator.vue` is 1.
    - `grep -c "v-else-if" src/components/SaveStatusIndicator.vue` is 3.
    - `grep -c "'en-US'" src/components/SaveStatusIndicator.vue` is 1.
    - A test asserts the idle wrapper's `text()` is the empty string.
    - A test asserts `wrapper.findAll('[aria-live]')` has length 1 in the error state.
    - A test asserts the saved state's rendered text matches the pattern
      `Saved \d{1,2}:\d{2} (AM|PM)`.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/SaveStatusIndicator.test.ts</automated>
  </verify>
  <done>One shared indicator component renders four mutually exclusive states inside a single polite live region, with idle rendering nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToastHost.vue — the app-level failure alert stack</name>
  <files>src/components/ToastHost.vue, src/components/__tests__/ToastHost.test.ts</files>
  <read_first>
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-UI-SPEC.md` § 4 —
      **the authoritative markup including the exact SVG path, copy it rather than inventing an
      icon**; plus § Copywriting Contract (the lead and the dismiss control's label), § Color,
      § UI Considerations → E3, and § Empty / Edge States rows E3, E4, E8, E10.
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-PATTERNS.md`
      § `src/components/ToastHost.vue`.
    - `src/stores/toasts.ts` as written by plan 03.
    - `src/views/LoginView.vue` around line 77 — the existing fixed-position red error box whose
      colour triple this reuses.
  </read_first>
  <behavior>
    - Zero toasts: the host renders its container with no toast children.
    - One toast: renders a card with `role="alert"`, the bold `Save failed.` lead, the store's
      message as the body, an `aria-hidden` icon, and a dismiss button labelled `Dismiss`.
    - The card carries `data-testid` of the form `toast-{id}` and the container carries
      `data-testid="toast-host"`.
    - Clicking the dismiss button removes that card and leaves any other card present.
    - Advancing timers 6000ms after a push removes the card without any interaction.
    - Two toasts render two cards, stacked, each removable independently.
    - The body text is exactly the message the store holds — the component never composes its
      own failure sentence.
    - Unmounting the host does not throw when a pending auto-dismiss timer later fires, and
      leaves no orphaned entry once that timer resolves.
  </behavior>
  <action>
    Create `src/components/ToastHost.vue` from 32-UI-SPEC.md § 4's markup verbatim. The
    container is `fixed`, positioned bottom-right at and above the `sm` breakpoint and
    full-width-minus-side-margins below it, at `z-[60]`, a `flex flex-col` with an eight-pixel
    gap and `data-testid="toast-host"`. Each card is a `v-for` over the store's array keyed by
    id, with `role="alert"`, the red-950 background / red-800 border / red-400 text triple, the
    aria-hidden warning SVG copied exactly from the spec, a paragraph with `min-w-0 flex-1`
    holding a `font-medium` lead span followed by the interpolated message, and a dismiss button
    carrying `aria-label="Dismiss"` that calls the store's dismiss with that id.

    Design points to preserve, all from 32-UI-SPEC.md § 4:
    - `role="alert"` only. It already implies an assertive, atomic live region per the ARIA
      spec; pairing it with an explicit assertive live-region attribute would be redundant.
    - `z-[60]` is a genuinely new top layer, deliberately above every existing `z-50` Teleport
      dialog — a `SongLyricEditor`-family failure can occur behind a modal, and the alert has to
      be visible there.
    - Bottom-right placement avoids the centred confirm dialogs and avoids the top of the
      viewport, which the sticky status bar and the Phase 31 lock banner already own.
    - No focus trap and no auto-focus. It is a passive notification, not a modal.
    - The body is always the store's message. There is no independently authored toast copy for
      either failure variant.
    - No hover-to-pause, no priorities, no categories, no cap logic.

    Do NOT wire this to `lifecycleError`. That is Phase 31's own surface for a rejected
    `Mark as Planned` or `Reopen`, and 32-CONTEXT.md's phase boundary puts lock/reopen semantics
    out of scope. Leave `lifecycleError` exactly as shipped. Likewise do not migrate the
    existing inline Planning Center export banner onto this host — 32-CONTEXT.md defers it
    explicitly.

    Create `src/components/__tests__/ToastHost.test.ts` covering the behavior block, with real
    Pinia and fake timers. Include the E3 partial backstop: push a toast, unmount the host
    before 6000ms elapses, advance timers past 6000ms, and assert no error is thrown and the
    store's array is empty — the timer lives in the store, so this must hold structurally.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/ToastHost.test.ts` exits 0.
    - The test file contains at least 9 `it(` blocks.
    - `grep -c 'role="alert"' src/components/ToastHost.vue` is 1.
    - `grep -c 'aria-label="Dismiss"' src/components/ToastHost.vue` is 1.
    - `grep -c 'data-testid="toast-host"' src/components/ToastHost.vue` is 1.
    - `grep -c 'z-\[60\]' src/components/ToastHost.vue` is 1.
    - `grep -c 'aria-hidden="true"' src/components/ToastHost.vue` is 1.
    - `grep -c 'v-html' src/components/ToastHost.vue` returns 0.
    - `grep -c 'lifecycleError' src/components/ToastHost.vue` returns 0.
    - A test asserts the rendered body text strictly equals the message put into the store.
    - A test asserts that after unmounting the host and advancing 6000ms, the store's array is
      empty and no error was thrown.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/ToastHost.test.ts</automated>
  </verify>
  <done>A minimal, hand-written failure-toast host renders the store's array as assertive alerts, dismissible manually and automatically, with no orphaned timers.</done>
</task>

<task type="auto">
  <name>Task 3: Mount ToastHost once in AppShell</name>
  <files>src/components/AppShell.vue</files>
  <read_first>
    - `src/components/AppShell.vue` — the whole file (about 47 lines). Note the outer
      `flex h-screen` wrapper, the `z-20` mobile backdrop, and the `<main class="flex-1
      overflow-y-auto">` slot host.
    - `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-UI-SPEC.md` § 4 —
      the exact insertion point.
  </read_first>
  <action>
    In `src/components/AppShell.vue`, add `<ToastHost />` as a new sibling immediately after the
    closing `</main>`, still inside the inner flex column so it stays fixed regardless of scroll.
    Import it alongside the existing sidebar import.

    Mount it exactly once, here. No view, route or dialog may mount a second one — the store is
    global, so a second host would render every toast twice.

    Change nothing else in this file: the backdrop, the sidebar, the mobile header and the main
    slot all stay as they are. This phase does not redesign app chrome.

    Confirm no existing test that mounts `AppShell` breaks. Several view tests stub `AppShell`
    with a plain slot-forwarding template, which is unaffected; any test mounting the real
    component will now need an active Pinia, so run the full suite and report anything that
    breaks.
  </action>
  <acceptance_criteria>
    - `grep -c 'ToastHost' src/components/AppShell.vue` is 2 (the import and the tag).
    - `grep -rc 'ToastHost' src/ --include=*.vue | grep -v 'ToastHost.vue' | grep -v 'AppShell.vue'`
      finds no other component rendering it.
    - `npx vitest run src/` shows no failing FILE beyond the freshly-measured baseline
      (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`).
    - `npm run type-check` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check && npx vitest run src/</automated>
  </verify>
  <done>The toast host is mounted once, app-wide, and the whole suite is at baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store state → rendered DOM | Both components render text held in a Pinia store into user-visible UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-10 | Tampering (stored XSS) | `SaveStatusIndicator.vue`, `ToastHost.vue` | medium | mitigate | Both render exclusively through Vue's default `{{ }}` interpolation, which escapes. `v-html` is not used and an acceptance criterion greps that it is absent. |
| T-32-11 | Information disclosure | rendered failure text | medium | mitigate | Both components render only the fixed enum strings from 32-UI-SPEC.md § Copywriting Contract plus a formatted timestamp. No caught error's `message` is rendered, so raw Firestore error text cannot reach user-facing copy. |
| T-32-12 | Denial of Service (of interaction) | `ToastHost` fixed overlay | low | mitigate | With zero toasts the container has no children and collapses to zero height, so it cannot intercept pointer events over the page. Asserted by the empty-state test. |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed in this phase — `32-RESEARCH.md` § Package Legitimacy Audit records "not applicable". No install task exists to gate. |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/SaveStatusIndicator.test.ts src/components/__tests__/ToastHost.test.ts` — green.
- `npm run type-check` (the `vue-tsc --build` form — the `-p tsconfig.app.json` form silently
  skips test files) — exit 0.
- `npm run build` — exit 0. Note `vite.config.ts` aborts the build if any `VITE_FIREBASE_*` var
  is missing; if you are in a worktree, symlink or copy `.env.local` from
  `C:\projects\worshipplanner\.env.local` first. Do not create or edit that file's contents.
- `npx vitest run src/` — no failing file beyond the freshly-measured baseline.
- Spacing audit: every class in the new markup is on the 4-pixel grid. No `py-0.5`, `gap-1.5` or
  `mt-0.5` anywhere — the toast icon is aligned with `items-start` on its flex row rather than a
  manual nudge (32-UI-SPEC.md § Spacing Scale states this has no exceptions).
</verification>

<success_criteria>
1. `SaveStatusIndicator.vue` renders four mutually exclusive states plus a rendering-nothing
   idle, inside exactly one polite, atomic live region.
2. `ToastHost.vue` renders the toast array as `role="alert"` cards with a bold lead, the store's
   message verbatim, and a labelled dismiss control, at `z-[60]`.
3. Both components have their own test file, count-based where the assertion is structural.
4. `ToastHost` is mounted exactly once, in `AppShell.vue`.
5. `npm run type-check` and `npm run build` are clean; the suite is at baseline.
</success_criteria>

<execution_notes>
- Do NOT run `git stash`. Do NOT run a project-wide `lint --fix`. Do NOT create, modify or
  commit `.env.local` — but a worktree needs a copy or symlink of it before `npm run build`.
- 32-UI-SPEC.md's markup blocks are authoritative. Copy them; do not paraphrase the class lists
  or re-derive the SVG path.
- This project has no shadcn tooling and no `components.json`. Do not create one — shadcn is a
  React-only registry and 32-UI-SPEC.md § Registry Safety rules it out explicitly.
- The knowledge graph at `.planning/graphs/` is STALE — read real files under `src/`.
</execution_notes>

<output>
Create `.planning/phases/32-save-reliability-autosave-fix-persistent-status/32-04-SUMMARY.md` when done.
Record the E1 overflow backstop's honest scope — what the jsdom test does and does not prove —
and append the visual check to `.planning/PENDING-VERIFICATION.md`.
</output>
