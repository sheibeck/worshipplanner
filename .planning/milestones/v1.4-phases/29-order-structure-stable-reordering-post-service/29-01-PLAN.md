---
phase: 29-order-structure-stable-reordering-post-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/__tests__/ServiceEditorView.test.ts
  - src/components/slides/__tests__/SlideGrid.test.ts
autonomous: true
requirements: [R044, R049, R050]

must_haves:
  truths:
    - "A committed, running test reproduces the reported ZTXcpNRcJTalEQp42fTx symptom (Sending mid-list, Message last, Worship twice) from a service-item drag."
    - "A committed, running test reproduces the SlideGrid wrong-index splice with a non-.slide-card sibling present in the container."
    - "A committed, running test reproduces the append-order defect: a slide added after a reorder does not land at the true end with contiguous orders."
    - "Every reorder assertion compares slot.id / entry.id identity sequences, never array indices."
    - "The unit suite still passes for both touched files — the repro failures are declared expected via it.fails, so nothing goes red in CI."
  artifacts:
    - "src/views/__tests__/ServiceEditorView.test.ts — multi-instance Sortable capture harness, header-inclusive fixture, simulateSlotDrag helper, 'Phase 29 reorder repro' describe block"
    - "src/components/slides/__tests__/SlideGrid.test.ts — multi-instance capture, sibling-inclusive drag fixture, 'Phase 29 reorder repro' describe block"
  key_links:
    - "simulateSlotDrag derives BOTH index pairs (oldIndex/newIndex and oldDraggableIndex/newDraggableIndex) from the live rendered DOM, so the same helper drives the pre-fix flat container and the post-fix per-section containers unchanged."
    - "Assertions read the payload handed to the mocked serviceStore.updateService / slideGroupsStore.replaceGroupSlides — the persisted array is what the bug corrupts."
---

<objective>
Build the failing reproduction FIRST, before any source change. Three defects (wrong index source,
single-step DOM revert, unstable v-for key) survived multiple fix attempts precisely because the
existing tests pass indices by hand into header-free fixtures and assert on positions — they go green
with the bug present. This plan replaces that with a harness that cannot lie: fixtures contain
section-header nodes, indices are derived from the real rendered DOM, and every assertion is on
`slot.id` / `entry.id` identity.

Purpose: make the defect visible and make the fix provable. Every later plan in this phase is verified
by flipping tests this plan writes.
Output: two extended test files, both green (repro failures declared with `it.fails`), zero source
changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/29-order-structure-stable-reordering-post-service/29-CONTEXT.md
@.planning/phases/29-order-structure-stable-reordering-post-service/29-PATTERNS.md

@src/views/ServiceEditorView.vue
@src/views/__tests__/ServiceEditorView.test.ts
@src/components/slides/SlideGrid.vue
@src/components/slides/__tests__/SlideGrid.test.ts
@src/types/service.ts
@src/utils/slotTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-instance Sortable capture harness + header-inclusive fixture in ServiceEditorView.test.ts</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
This file has NO sortablejs mock today (the real library runs against jsdom on every mount here).
Add a hoisted mock at the top of the file, alongside the existing `vi.mock` block group
(lines 26-298), modelled on `SlideGrid.test.ts:91-101` but generalized to capture MANY instances:

- Keep a module-scope `sortableCaptures` array of `{ el: HTMLElement; options: SortableOptions }`.
- `Sortable.create` pushes each call and returns `{ destroy: <a shared vi.fn> }`.
- Add a `resetSortableCaptures()` called from the new describe block's `beforeEach`.
- Expose a lookup `captureForSection(section)` that returns the capture whose
  `el.dataset.section === section`, and `flatCapture()` that returns the single capture with no
  `data-section` attribute. Today only `flatCapture()` resolves; after 29-03 only the per-section
  lookups resolve. Both accessors must exist now so no later plan has to rewrite the harness.

Then add a fixture builder `makeSectionedService()` producing the reported
`ZTXcpNRcJTalEQp42fTx` shape — 8 slots, every slot carrying a stable `id` and a `section`, laid out
section-major across the EXISTING four sections only (no `'post-service'` — that section does not
exist yet and this plan must not anticipate it):

  s1 SONG pre-service · s2 SONG worship · s3 SONG worship · s4 SCRIPTURE worship ·
  s5 MESSAGE message · s6 PRAYER message · s7 SONG sending · s8 PRAYER sending

Follow the file's existing service-fixture conventions (see the Phase 20-04 and Phase 24-06 describe
blocks) for the surrounding `Service` fields and the reactive store wiring, and reuse the file's
existing `mockUpdateService` services-store mock rather than adding a second one.

Confirm the existing describe blocks in this file still pass with the mock in place. If any existing
assertion regresses because the real library no longer runs, fix that assertion in place — the
documented 10-file failing baseline must not grow.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>
The Sortable mock records every `create` call with its container element; `makeSectionedService()`
returns the 8-slot four-section fixture with stable ids; the full file passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: DOM-derived drag helper and the ZTXcpNRcJTalEQp42fTx repro (R044)</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
Add `simulateSlotDrag(wrapper, { fromSection, fromPos, toSection, toPos })` in the new
`describe('ServiceEditorView - Phase 29 reorder repro', ...)` block. The helper must derive every
index from the LIVE rendered DOM — never accept a hand-passed index — so it drives the current flat
container and the per-section containers 29-03 introduces without being rewritten:

1. Resolve the source container: prefer `[data-testid="section-list-{fromSection}"]`; when absent
   (the current flat render) fall back to the single container that holds the `.slot-item` nodes,
   and locate the section's slots by scanning `.slot-item` siblings that follow the
   `[data-testid="section-header-{fromSection}"]` node and precede the next `.section-header`.
   Resolve the destination container the same way.
2. Compute, from that DOM: `oldIndex` = the dragged node's index among ALL element children of its
   container (header nodes counted), and `oldDraggableIndex` = its index among `.slot-item` children
   only. Compute `newIndex` / `newDraggableIndex` for the destination position identically.
   With the current flat render these two pairs DIFFER — that difference is the defect being
   reproduced, so the helper must never collapse them.
3. Invoke the matching capture's `onEnd` with an event object carrying `oldIndex`, `newIndex`,
   `oldDraggableIndex`, `newDraggableIndex`, `item` (the real dragged element), `from` (source
   container) and `to` (destination container).
4. `await` the handler and `await flushPromises()` so the mocked write settles.

Then add the repro itself, declared `it.fails(...)` so the suite stays green while the defect stands:

  it.fails('lands a service item exactly where it was dropped (R044 — repro, unfix pending)')

Body: mount with `makeSectionedService()`, drag the worship slot `s4` to the first position of the
`message` section, then assert on the array handed to `mockUpdateService`:
`payload.slots.map((s) => s.id)` equals `['s1','s2','s3','s5','s4','s6','s7','s8']`… — derive the
exact expected sequence from the intended semantics (source removed from its old place, inserted at
the requested position of the destination section, sections still in `SERVICE_SECTIONS` order) and
write it as an explicit literal array of ids. Also assert `payload.slots.map((s) => s.section)`
still reads section-major, and that no id appears twice
(`new Set(ids).size === ids.length`) — that last assertion is what pins the "Worship appeared twice"
symptom.

Add a second `it.fails` for a WITHIN-section multi-position move (drag `s2` to the last position of
`worship`), asserting the resulting id sequence — the single-step DOM revert only undoes one adjacent
step, so a multi-position move is where DOM and state diverge.

Every assertion in this block compares id sequences. Do not assert on `position`, on array index, or
on `data-testid="slot-N"` ordinals.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>
Both repro tests are declared `it.fails` and the file passes, proving the drags currently produce the
wrong id sequence. Removing `.fails` makes them red today — that is the RED state 29-03 turns green.
  </done>
</task>

<task type="auto">
  <name>Task 3: SlideGrid repro — sibling-inclusive drag fixture and the append-order defect (R049, R050)</name>
  <files>src/components/slides/__tests__/SlideGrid.test.ts</files>
  <action>
Widen the existing options-capture mock (lines 91-101) to the same many-instance shape used in Task 1
(array of `{ el, options }` plus a `latestCapture()` accessor), keeping `capturedSortableOptions`
working for the existing Task-3 drag tests so they do not have to be rewritten.

Replace `simulateDragEnd` (lines ~417-424) — do not extend it. Its parent is four bare divs with no
non-card sibling and it takes hand-passed indices, which is exactly why it goes green with the bug
present. Write `simulateCardDrag(wrapper, { fromPos, toPos })` that:
- reads the REAL container the component rendered (the element the mock captured), which contains the
  drop tile as a non-`.slide-card` sibling,
- derives `oldIndex`/`newIndex` from element-child position (tile counted) and
  `oldDraggableIndex`/`newDraggableIndex` from `.slide-card` position only,
- passes `from`/`to` as that same container and awaits the handler.

Add `describe('SlideGrid - Phase 29 reorder repro', ...)` with:

1. `it.fails('drags a slide to the position it was dropped in (R049 — repro, unfix pending)')` —
   group of four entries with distinct ids and orders 0..3; drag entry 3 to position 0; assert the
   array passed to `mockReplaceGroupSlides` has `map((e) => e.id)` equal to the explicit expected id
   sequence and `map((e) => e.order)` equal to `[0,1,2,3]`.

2. `it.fails('appends a new slide at the true end of the group with contiguous orders (R050 — repro, unfix pending)')` —
   drive `ensureGroupMaterialized` to resolve entries whose ARRAY order disagrees with their `order`
   values (e.g. `[{id:'e1',order:0},{id:'e3',order:2},{id:'e2',order:1}]`), click
   `[data-testid="slide-grid-add-slide"]`, then assert the payload's LAST element is the newly minted
   entry and `map((e) => e.order)` equals `[0,1,2,3]` with the ids in ascending-order sequence
   `['e1','e2','e3', <new id>]`. Today `onAddSlide` (line 410) appends to the raw array and derives
   `nextOrder` from `Math.max(...)`, so array order and `order` values stay divergent — the contiguity
   assertion is the RED.

3. `it('reorder failure surfaces and does not leave the grid showing an unsaved order (R049 — pending)')`
   declared `it.fails`: make `mockReplaceGroupSlides` reject once, run a drag, and assert
   `[data-testid="slide-grid-reorder-error"]` exists. That testid does not exist yet; 29-04 adds it.

If any of these three unexpectedly PASSES as written (Vitest reports "expected test to fail"), convert
that one to a plain `it`, keep it, and record in the SUMMARY which symptom was already covered by the
index fix rather than being an independent defect. Do not delete a test to make the suite green.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/components/slides/__tests__/SlideGrid.test.ts</automated>
  </verify>
  <done>
`simulateDragEnd` is gone, replaced by a DOM-derived helper whose container holds a non-card sibling;
three repro tests exist; the file passes; the failing-file baseline has not grown.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source | None. This plan writes tests only; it introduces no runtime code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering | Test fixtures masking a live defect | medium | mitigate | Fixtures include section-header / drop-tile siblings and derive indices from the rendered DOM; hand-passed indices are prohibited by Task 2 and Task 3. |
| T-29-02 | Repudiation | A repro silently deleted to turn the suite green | medium | mitigate | Task 3 forbids deletion; an unexpectedly-passing repro must be converted to `it` and recorded in the SUMMARY. |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | No package install task in this plan — `sortablejs`, `vitest` and `@vue/test-utils` are already resolved dependencies. No legitimacy audit required because no new package enters the tree. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/views/__tests__/ServiceEditorView.test.ts src/components/slides/__tests__/SlideGrid.test.ts` passes.
- `git diff --name-only` lists ONLY the two test files — no source file changed by this plan.
- Removing `.fails` from any Phase 29 repro turns it red (spot-check at least one manually before
  restoring `.fails`); this proves the repro actually reproduces.
</verification>

<success_criteria>
- The reported ZTXcpNRcJTalEQp42fTx symptom is reproducible from a committed test.
- Every reorder assertion in the new blocks compares id sequences; `grep -n "toEqual(\[" ` over the new
  blocks shows id arrays, not index arrays.
- The failing-FILE set of `npm run test:unit -- --run` is unchanged from the documented 10-file baseline.
</success_criteria>

<output>
Create `.planning/phases/29-order-structure-stable-reordering-post-service/29-01-SUMMARY.md` when done.
</output>
