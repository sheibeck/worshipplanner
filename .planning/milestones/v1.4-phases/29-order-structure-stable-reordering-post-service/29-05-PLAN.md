---
phase: 29-order-structure-stable-reordering-post-service
plan: 05
type: execute
wave: 3
depends_on: ["29-03", "29-04"]
files_modified:
  - src/types/service.ts
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
  - src/utils/__tests__/slotTypes.test.ts
  - src/composables/__tests__/useSlideshowAssembly.test.ts
  - src/components/__tests__/ServicePrintLayout.test.ts
  - src/utils/__tests__/planningCenterExport.test.ts
autonomous: false
requirements: [R042, R043]

must_haves:
  truths:
    - "A fifth Post-Service section exists and renders last, after Sending, in the Service Order tab."
    - "Post-Service accepts any slot kind, exactly like the other four sections."
    - "Post-Service is visible with its placeholder on every existing service, including services that have never heard of it, with no migration and no write against production documents."
    - "Post-Service flows through to the slideshow assembly, the print layout, the share link and the Planning Center export, ordered last."
    - "The section select dropdown offers Post-Service without a separate change, because it already iterates SERVICE_SECTIONS."
    - "A real cross-section drag in a browser lands the item where it was dropped — verified by a human, because jsdom cannot synthesize a genuine drag."
  artifacts:
    - "src/types/service.ts — 'post-service' in the union, the array and the label map"
    - "src/views/ServiceEditorView.vue — Post-Service placeholder copy branch per UI-SPEC §2"
    - "Four consumer test files carrying explicit five-section assertions"
  key_links:
    - "SERVICE_SECTIONS is the single enumeration source (D005) — every consumer already iterates it or iterates service.slots in array order, so the fifth member propagates without a source edit anywhere but service.ts."
    - "ServiceSlot.section is already optional, so an additive union member needs no migration and no backfill write against shipped v1.0 service documents."
---

<objective>
Add the fifth section — Post-Service — now that the reordering mechanism has been proven against the
existing four. Widening a broken mechanism hides the fix; this plan runs last for that reason.

The change itself is three lines in one type file. The work is proving that the fifth member reaches
every surface (assembly, print, share, Planning Center export, plan rail, the section dropdown)
without a hard-coded four-section assumption anywhere, and confirming by human hand that a real
browser drag across sections behaves.

Purpose: R042 (Post-Service exists) and R043 (five sections, fixed order, always visible).
Output: a five-section service model, four consumer audits confirmed by test, and one human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/29-order-structure-stable-reordering-post-service/29-CONTEXT.md
@.planning/phases/29-order-structure-stable-reordering-post-service/29-UI-SPEC.md
@.planning/phases/29-order-structure-stable-reordering-post-service/29-PATTERNS.md
@.planning/phases/29-order-structure-stable-reordering-post-service/29-03-SUMMARY.md
@.planning/phases/29-order-structure-stable-reordering-post-service/29-04-SUMMARY.md

@src/types/service.ts
@src/views/ServiceEditorView.vue
@src/composables/useSlideshowAssembly.ts
@src/components/ServicePrintLayout.vue
@src/utils/planningCenterExport.ts
@src/components/slides/SlidePlanRail.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Post-Service to the section model and the editor</name>
  <files>src/types/service.ts, src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts, src/utils/__tests__/slotTypes.test.ts</files>
  <behavior>
    - SERVICE_SECTIONS has five members and 'post-service' is the last one.
    - SERVICE_SECTION_LABELS maps it to 'Post-Service'.
    - Mounting any service renders five section headers in SERVICE_SECTIONS order, with the Post-Service header last.
    - A service with no Post-Service items still renders the Post-Service header, its list container and its placeholder.
    - The Post-Service placeholder carries the purpose-naming copy from UI-SPEC §2; the other four carry the generic copy.
    - A slot whose section is 'post-service' renders inside the Post-Service container and orders last in the persisted array.
    - Every slot kind is accepted in Post-Service — no per-section kind restriction is introduced.
    - The existing section select offers five options with Post-Service last, with no change to that markup.
  </behavior>
  <action>
In `src/types/service.ts` (lines 8-22) append `'post-service'` to the `ServiceSection` union, to
`SERVICE_SECTIONS` and to `SERVICE_SECTION_LABELS` (label `'Post-Service'`) — appended last in all
three, because array order is what fixes render order (UI-SPEC §4). Update the doc comment at line 9
so it states the correct member count and notes the Phase 29 addition in the `(D-XX)` design-reference
style CONVENTIONS.md documents. Leave `ServiceSlot.section` optional — it already is, which is exactly
why this needs no migration and no backfill write against shipped v1.0 service documents.

In `src/views/ServiceEditorView.vue`, add the Post-Service branch to the empty-section placeholder
that 29-03 built, using UI-SPEC §2's copy verbatim: the four original sections keep the generic
"Drag an item here, or set its Section to {label}." line, and Post-Service gets the variant naming
its purpose. That copy branch is the ONLY Post-Service-specific UI in this phase — no unique icon,
accent or subtitle in the header (UI-SPEC §4), because inventing bespoke chrome here would front-run
Phase 36's rebuild.

Nothing else in the view should need editing. If it does — if any five-section assertion fails because
of a literal section name or a length assumption in this file — fix it and name the site in the
SUMMARY; that is a finding worth recording, not a silent patch.

Run `npx vue-tsc --build` deliberately after the union grows. Files that consume `ServiceSection` as a
type (`src/types/slide.ts:124,153`, `src/types/importedDeck.ts:15`, `PptxImportModal.vue`,
`PresentationViewer.vue:414`) are structurally safe per PATTERNS.md, but an exhaustive switch or a
`Record<ServiceSection, …>` literal anywhere would surface here as a compile error — that is the point
of running it as a gate rather than assuming.

Extend `src/utils/__tests__/slotTypes.test.ts` so the 29-02 grouping helpers are exercised with a
Post-Service slot: it must bucket under the fifth key and flatten last. Those helpers iterate
`SERVICE_SECTIONS` and should need no source change — assert that, do not edit them.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/views/__tests__/ServiceEditorView.test.ts src/utils/__tests__/slotTypes.test.ts</automated>
  </verify>
  <done>
Five sections render in fixed order with Post-Service last and always visible; its placeholder carries
the §2 copy; `vue-tsc --build` is clean; `src/utils/slotTypes.ts` was not edited.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm the four downstream consumers by test, not by rework</name>
  <files>src/composables/__tests__/useSlideshowAssembly.test.ts, src/components/__tests__/ServicePrintLayout.test.ts, src/utils/__tests__/planningCenterExport.test.ts</files>
  <behavior>
    - assembledSections emits a Post-Service group, positioned after Sending and before the trailing Ungrouped group.
    - assembledSections still omits a section that has no slides, and still emits section-less slides trailing as 'Ungrouped'.
    - ServicePrintLayout renders Post-Service items last for a section-major slots array.
    - planningCenterExport emits Post-Service items last for a section-major slots array.
    - SlidePlanRail renders a Post-Service plan item without special-casing, in service order.
  </behavior>
  <action>
PATTERNS.md's expected finding is that all four consumers already iterate `SERVICE_SECTIONS`
(`useSlideshowAssembly.ts:548`) or `service.slots` in array order (`ServicePrintLayout.vue:17`,
`planningCenterExport.ts:56`), and that `SlidePlanRail.vue` references sections not at all — so this is
confirmation-by-test, not rework. Treat a needed source edit as a finding to record, not a routine
change.

Add, to each of the three named test files, one focused case using a five-section fixture whose slots
include at least one Post-Service item:
- `useSlideshowAssembly.test.ts` — assert the emitted section sequence by section key, including the
  Post-Service group's position relative to Sending and to the trailing `Ungrouped` group, and assert
  the empty-section omission still holds (that composable's grouping is for the assembled *slideshow*
  output; the editor's always-visible empty sections are a separate rendering concern and this
  behaviour must not change).
- `ServicePrintLayout.test.ts` — assert the rendered item order for a section-major slots array places
  the Post-Service item last.
- `planningCenterExport.test.ts` — assert the exported item sequence places the Post-Service item last.

For `SlidePlanRail.vue`, verify by reading that it carries no section references at all (D-06: its
order is locked and it has no drag). If that holds, record it in the SUMMARY as audited-no-test-needed
rather than inventing a test for an absent concern. If it does reference sections, add the assertion.

Record all four audit outcomes explicitly in the SUMMARY, one line each, naming the file and whether a
source change was required. "No change required" is the expected and acceptable result — but it must
be stated, not assumed, because Pitfall 9 is precisely the assumption going unchecked.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/composables/__tests__/useSlideshowAssembly.test.ts src/components/__tests__/ServicePrintLayout.test.ts src/utils/__tests__/planningCenterExport.test.ts</automated>
  </verify>
  <done>
Three consumer test files carry explicit Post-Service-ordered-last assertions; the fourth consumer is
audited by reading; all four outcomes are recorded in the SUMMARY.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — a real browser drag across sections</name>
  <action>
Stop and hand control to the developer. Do not self-approve, do not simulate the drag, and do not
proceed to the SUMMARY until the resume signal arrives. Everything below is for the human to perform
in a real browser; jsdom cannot synthesize a genuine drag, which is the entire reason this checkpoint
exists. If the developer reports a failure, record exactly which step and what they saw, and treat it
as a defect against this phase rather than closing the plan.
  </action>
  <what-built>
Five fixed service sections (Pre-Service → Worship → Message → Sending → Post-Service), each its own
drag container, always visible with a placeholder when empty; service items reorder within and across
sections and save immediately; slides reorder and append at the true end in the Slides tab; a rejected
save reverts the order and shows an inline failure message.

Automated coverage proves the handler semantics — id sequences, cross-section payloads, revert on
rejection — but jsdom cannot synthesize a genuine OS drag. This one check covers what the test suite
structurally cannot.
  </what-built>
  <how-to-verify>
1. `npm run dev`, open a service in the Service Order tab as an editor.
2. Confirm all five section headers render, in order, top to bottom: Pre-Service, Worship, Message,
   Sending, Post-Service — and that every empty one shows its "No items yet" placeholder.
3. Try to drag a section header itself. Nothing should move — headers are not draggable.
4. Drag an item from Worship and drop it in the MIDDLE of Message (not the end). It must land exactly
   between the two items you dropped it between, immediately, with no flicker back and no page
   refresh needed. Its Section dropdown should now read Message.
5. Drag an item into the empty Post-Service section. It must land there and the placeholder must
   disappear.
6. Drag an item across three or more positions inside one section. It must land exactly where dropped.
7. Reload the page. The order you left must be the order you get back — this is the check that catches
   the old "correct only after refresh" signature in reverse.
8. Switch to the Slides tab, select a plan item with several slides, drag one to a new position, then
   reload. It must have stayed. Click "Add slide" and confirm the new slide appears at the END of that
   group (for a song group, the copyright slide legitimately stays last).
9. Optional but valuable: with DevTools open, set the network to offline, drag a service item, and
   confirm the order snaps back and an inline "Couldn't save this order — reverted." message appears
   rather than the move silently appearing to succeed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe exactly which step misbehaved and what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped v1.0 Firestore service documents → widened section union | Production documents were written when four sections existed and none carries `'post-service'`. |
| section value → downstream render/export surfaces | A section string reaches the print layout, the public share link and the Planning Center export payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-15 | Tampering | Production service documents | high | mitigate | Purely additive union member; `ServiceSlot.section` stays optional; no migration, no backfill, no write is issued by this plan. Existing services render the new section empty. |
| T-29-16 | Information Disclosure | Post-Service content on the public share link | medium | accept | Post-Service items follow the same visibility rules as every other section — the share link already exposes the whole plan, and this phase adds no new exposure surface. Confirmed by the print/share ordering assertion in Task 2. |
| T-29-17 | Denial of Service | Planning Center export rejecting an unknown section | medium | mitigate | Task 2 asserts the export emits Post-Service items in array order; the exporter iterates `service.slots` and never enumerates section names, so an unknown section cannot break the payload. |
| T-29-18 | Tampering | Silent four-section assumption surviving the audit | high | mitigate | Every consumer outcome must be stated in the SUMMARY with the file named; `vue-tsc --build` is run as a deliberate gate to surface exhaustive switches and `Record<ServiceSection, …>` literals. |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | No package install task in this plan; no new dependency enters the tree. |
</threat_model>

<verification>
- `npm run test:unit -- --run` — the failing-FILE set has not grown beyond the documented 10-file
  baseline (8 stale `.gsd/quarantine/worktrees/**` duplicates, `src/storage.rules.test.ts`,
  `src/views/__tests__/RosterView.test.ts`). Those three causes are pre-existing; do not fix them here.
- `npx vue-tsc --build` reports no new type errors after the union grows.
- `npm run lint` passes.
- `grep -c "post-service" src/types/service.ts` returns 3 or more (union, array, label map).
- No migration script, backfill routine, or one-off write was added anywhere:
  `git diff --name-only` lists only the seven files in `files_modified`.
- The human-verify checkpoint is approved.
</verification>

<success_criteria>
- A fifth Post-Service section exists in the service plan and reaches the Slides tab (R042).
- The five sections render in fixed order, are never draggable, and are always visible including when
  empty (R043).
- All four downstream consumers are audited with their outcome recorded, and none needed a
  four-section assumption removed.
- A human has confirmed a real cross-section browser drag lands where dropped and survives a reload.
</success_criteria>

<output>
Create `.planning/phases/29-order-structure-stable-reordering-post-service/29-05-SUMMARY.md` when done.
</output>
