---
phase: 35-presentation-correctness-lyric-editor
plan: 04
type: execute
wave: 2
depends_on: ["35-03"]
files_modified:
  - src/components/SongLyricEditor.vue
  - src/components/__tests__/SongLyricEditor.test.ts
  - src/components/LyricPasteDialog.vue
  - src/components/__tests__/LyricPasteDialog.test.ts
autonomous: true
requirements: [R065, R066]

must_haves:
  truths:
    - "Exactly ONE paste surface exists once this plan lands. LyricPasteDialog.vue is deleted, not kept as a fallback — two paste surfaces is precisely the ambiguity R066 removes (35-CONTEXT.md: 'the modal does not survive as a fallback')."
    - "Header swap (35-UI-SPEC §4): while paste mode is active the editor's lyrics-header row is replaced by lyrics-paste-header. SaveStatusIndicator, History and Paste lyrics do not render in the paste header — they belong to the Sections view being replaced."
    - "Body swap (35-UI-SPEC §4): while paste mode is active the song-background row, the section-rows list and the add-section row are all hidden; the drawer swaps in place rather than stacking a second surface over the first."
    - "E6 — Opening Paste lyrics with existing sections present starts with an EMPTY textarea, never pre-filled with the current sections. A paste always represents fresh incoming text. The host's v-if makes this fall out of the mount lifecycle rather than a reset watcher."
    - "E10 — Clicking '‹ Back to sections' or 'Cancel' with unsaved pasted text present fires the native confirm guard 'You have unsaved changes. Discard them?', unchanged from today's dialog, from BOTH exits."
    - "E9 — After a failed save the region stays open with the paste and the override checkbox state intact, so the user can retry without re-pasting or re-checking."
    - "Both entry points reach the same surface: the header's paste-lyrics-btn (lyrics present) and the empty state's paste-cta-btn (no lyrics yet) both enter paste mode."
    - "35-UI-SPEC §5 — SongLyricEditor.vue is reachable only from SongSlideOver.vue and is never service-locked. No serviceLocked prop is added to its prop chain and no lock banner or lock-aware gating is introduced; the paste region stays gated only on authStore.isEditor, exactly as the rest of the editor is."
    - "The Phase 32 SaveStatusIndicator and the Phase 33 song-level BackgroundControl both survive untouched in the Sections view; hosting the paste region must not disturb either."

  artifacts:
    - "src/components/SongLyricEditor.vue — showPasteDialog ref renamed to pasteMode; template split into a !pasteMode branch and the LyricPasteRegion mount; LyricPasteDialog import and mount removed."
    - "src/components/LyricPasteDialog.vue — DELETED."
    - "src/components/__tests__/LyricPasteDialog.test.ts — DELETED (its coverage lives in LyricPasteRegion.test.ts from plan 03, plus the two host-mechanism tests added here)."
    - "src/components/__tests__/SongLyricEditor.test.ts — the LyricPasteDialog stub removed from mountEditor; new paste-mode describe block."

  key_links:
    - "SongLyricEditor's pasteMode ref → LyricPasteRegion's v-if → the region's mount/unmount, which IS the reset mechanism for rawText and overrideCopyright. Replacing the v-if with v-show would silently break E6."
    - "The region's close emit → pasteMode = false; its saved emit → onPasteSaved, which also sets pasteMode = false and lets the Firestore subscription re-render the Sections view with the new sections — identical to today's onPasteSaved behavior."
    - "currentSectionCount is bound from sectionRows.length so the footer caption reports what the paste will actually replace."

  prohibitions:
    - statement: "No UI copy, code comment, SUMMARY, or rationale may state or imply that CCLI requires copyright on the first and last slide. R060 exceeds the documented minimum deliberately, and CCLI's primary license text has now failed retrieval twice. Safety-margin framing only."
      status: kept
      verification: "grep -rEin 'ccli (requires|mandates|requirement)|licen[cs]e requires' src/ returns 0 matches"
    - statement: "The paste must be completable in every case. The override checkbox is always available and never conditional on any other field — no state may leave a user holding pasted text they cannot save."
      status: kept
      verification: "The host adds no second gate: SongLyricEditor.vue contains no reference to overrideCopyright, canConfirm, or parsed, and cannot condition the save. Verified by grep returning 0 for each."
    - statement: "ccliParser.test.ts and songSectionOrder.test.ts's normalizeParsedSections tests must not be edited — they are outside this phase's scope. The 9 parsing/save tests moved out of LyricPasteDialog.test.ts must survive assertion-for-assertion, not be weakened during the modal→inline conversion."
      status: kept
      verification: "git diff --name-only HEAD -- src/utils/__tests__/ccliParser.test.ts src/utils/__tests__/songSectionOrder.test.ts is empty; and every mockSaveLyrics assertion present in the deleted LyricPasteDialog.test.ts is present in LyricPasteRegion.test.ts before the deletion commit lands."
---

<objective>
R066, part 2 of 2 — host the inline paste region and retire the modal.

Plan 03 built `LyricPasteRegion.vue` standing alone. This plan wires it into
`SongLyricEditor.vue` so the drawer swaps to the paste view **in place**, deletes
`LyricPasteDialog.vue` and its test file, and adds the two host-mechanism tests plan 03 handed
forward.

Purpose: exactly one paste surface, reached from both existing entry points, with the modal gone
rather than left as a fallback.

Output: a swapped `SongLyricEditor.vue`, two deleted files, and a new paste-mode describe block.

**Wave 2 by necessity, not by preference:** this plan imports the component plan 03 creates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-presentation-correctness-lyric-editor/35-UI-SPEC.md
@.planning/phases/35-presentation-correctness-lyric-editor/35-RESEARCH.md
@.planning/phases/35-presentation-correctness-lyric-editor/35-VALIDATION.md
@.planning/phases/35-presentation-correctness-lyric-editor/35-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
| Symbol / path | Kind | Home |
|---|---|---|
| `pasteMode` | `ref<boolean>` (renamed from `showPasteDialog`) | `src/components/SongLyricEditor.vue` |
| `LyricPasteRegion` | new import + mount | `src/components/SongLyricEditor.vue` |
| `showPasteDialog` | **removed** ref | `src/components/SongLyricEditor.vue` |
| `LyricPasteDialog` | **removed** import and mount | `src/components/SongLyricEditor.vue` |
| `src/components/LyricPasteDialog.vue` | **deleted file** | — |
| `src/components/__tests__/LyricPasteDialog.test.ts` | **deleted file** | — |
| `confirm-btn` | **retired** `data-testid` (it lived only in the deleted dialog) | — |
| `lyrics-header` | unchanged `data-testid`, now rendered only while `!pasteMode` | `src/components/SongLyricEditor.vue` |

Consumed from plan 03 (not created here): `src/components/LyricPasteRegion.vue`, its props
`songId`/`orgId`/`currentSectionCount`, its emits `close`/`saved`, and the `paste-*` `data-testid`
family. Sibling-plan artifacts: plan 01 adds `presentStartIndex`, the `initialIndex` prop and
`onPresent`; plan 02 adds no runtime symbols.
</artifacts_this_phase_produces>

<sanctioned_test_edits>
★ Read this before touching a test file. This plan carries the tail of the phase's second sanctioned
edit (the `LyricPasteDialog.test.ts` migration begun in plan 03).

| Edit | Status |
|---|---|
| **Delete** `src/components/__tests__/LyricPasteDialog.test.ts` | ✅ **Sanctioned.** Its 13 tests were fully accounted for by plan 03's migration map: 9 moved verbatim and 2 reshaped into `LyricPasteRegion.test.ts`, and the remaining 2 (rows 12 and 13 — "renders nothing when closed" and "resets textarea when reopened") are re-homed by **this** plan into `SongLyricEditor.test.ts`, because the open/close and reopen mechanisms are now host-driven. **Verify plan 03's file exists and is green BEFORE deleting.** |
| `SongLyricEditor.test.ts` `mountEditor` — remove the `LyricPasteDialog` key from `global.stubs` | ✅ **Sanctioned harness change**, anticipated by 35-RESEARCH.md § Wave 0 Gaps. It is a mount-harness edit, not an assertion edit: no test in the file asserts on `paste-dialog-stub`. |
| `SongLyricEditor.test.ts:186-204` — the empty-state and header-actions tests | ❌ Must pass **UNMODIFIED**. Verified: they assert `paste-cta-btn` and `paste-lyrics-btn` exist and carry their labels, all of which survive the swap unchanged. |
| Every other pre-existing test in `SongLyricEditor.test.ts` (autosave, drag reorder, history, background, repeat pooling) | ❌ Must pass **UNMODIFIED**. `pasteMode` defaults `false`, so the Sections view they exercise renders exactly as before. |
| `src/utils/__tests__/ccliParser.test.ts` (19) and `src/utils/__tests__/songSectionOrder.test.ts`'s `normalizeParsedSections` (8) | ❌ **Must not be touched at all** (P-03). |
| Any other pre-existing test | ❌ If one needs editing, **stop** and re-examine the change. |
</sanctioned_test_edits>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap SongLyricEditor into paste mode in place, and delete the modal</name>
  <files>src/components/SongLyricEditor.vue, src/components/LyricPasteDialog.vue (DELETED), src/components/__tests__/LyricPasteDialog.test.ts (DELETED)</files>

  <read_first>
    - `src/components/SongLyricEditor.vue` in full (700 lines) — the header row at lines 4-27, the
      song-background row at 29-45, the loading/empty/scroll-region branches at 47-249, the
      `LyricPasteDialog` mount at 251-258, its import at line 281, the `showPasteDialog` ref at line
      293, and `onPasteSaved` at lines 663-665
    - `src/components/LyricPasteRegion.vue` (created by plan 03) — its props, emits and root
      structure
    - `.planning/phases/35-presentation-correctness-lyric-editor/35-UI-SPEC.md` §4 (the header-swap
      and body-swap contract), §5 (no lock gating), and § Empty/Edge States rows E6, E9, E10
    - `.planning/phases/35-presentation-correctness-lyric-editor/35-CONTEXT.md` § Inline Paste — "One
      paste path only. The modal does not survive as a fallback."
    - `.planning/phases/35-presentation-correctness-lyric-editor/35-03-SUMMARY.md` — plan 03's
      recorded file-fate decision and the confirmed emit names
  </read_first>

  <behavior>
    - Test 1: on mount, `lyrics-header` renders and neither `lyrics-paste-header` nor `paste-region`
      does.
    - Test 2: clicking `paste-lyrics-btn` swaps the surfaces — `lyrics-paste-header` and
      `paste-region` render, while `lyrics-header`, `song-background-row`, `lyrics-scroll-region`,
      `section-rows` and `add-section-row` all disappear.
    - Test 3: from the empty state (no lyrics loaded), clicking `paste-cta-btn` reaches the same
      paste region.
    - Test 4: the rendered output contains no modal chrome — no backdrop element, no
      `fixed inset-0` positioning, and the paste region is a descendant of the editor's own root
      rather than a Teleported sibling.
  </behavior>

  <action>
    In `src/components/SongLyricEditor.vue`:

    Rename the `showPasteDialog` ref to `pasteMode`, keeping `ref(false)`. Update its three existing
    writers — the header button's `@click` at line 18, the empty-state CTA's `@click` at line 64, and
    `onPasteSaved` at line 664 — to the new name. Nothing else about those three sites changes; both
    buttons keep their `data-testid`, label and classes.

    Wrap the existing header row, the song-background row, and the loading / empty-state /
    scroll-region `v-if`-`v-else-if`-`v-else` chain in a single `<template v-if="!pasteMode">`. Do not
    reorder, reindent-beyond-necessity, or otherwise disturb the chain itself — it is one connected
    conditional and splitting it would change which branch renders. Then mount the region as that
    template's `v-else`.

    Replace the `<LyricPasteDialog>` element at lines 251-258 with nothing — the region is mounted as
    the `v-else` above, not as a sibling at the bottom. Bind the region with `:song-id="props.songId"`,
    `:org-id="props.orgId"` and `:current-section-count="sectionRows.length"`, and wire
    `@close="pasteMode = false"` and `@saved="onPasteSaved"`.

    Use `v-if`/`v-else`, never `v-show`. The region's mount/unmount IS the reset mechanism for
    `rawText` and `overrideCopyright` (E6: the textarea starts empty on every open, never pre-filled
    with the existing sections). `v-show` would keep the component alive across closes and silently
    break that contract.

    Swap the `import LyricPasteDialog from './LyricPasteDialog.vue'` at line 281 for
    `import LyricPasteRegion from './LyricPasteRegion.vue'`.

    Then delete `src/components/LyricPasteDialog.vue` and
    `src/components/__tests__/LyricPasteDialog.test.ts` from the repository with `git rm`. Before
    deleting the test file, confirm `src/components/__tests__/LyricPasteRegion.test.ts` exists and is
    green — plan 03 owns that file and this deletion is only safe once its migration landed. The modal
    does not survive as a fallback; leaving it importable would reintroduce the two-surface ambiguity
    R066 exists to remove.

    Do not touch `SaveStatusIndicator` or the song-level `BackgroundControl` — both stay exactly where
    they are inside the `!pasteMode` branch. Do not add a `serviceLocked` prop, a lock banner or any
    lock-aware gating anywhere in this file: this editor is reachable only from `SongSlideOver.vue`,
    is never service-locked, and Phase 31's lock pattern does not apply here (35-UI-SPEC §5). Do not
    reference `overrideCopyright`, `canConfirm`, `parsed`, or any part of the paste gate from the
    host — the gate lives entirely in the region, and a host-side condition on top of it would be the
    P-02 violation.
  </action>

  <verify>
    <automated>npm run type-check</automated>
  </verify>

  <acceptance_criteria>
    - Behavior assertion: `test ! -e src/components/LyricPasteDialog.vue && test ! -e src/components/__tests__/LyricPasteDialog.test.ts`
      succeeds — both files are gone.
    - Source assertion: `grep -rc 'LyricPasteDialog' src/` returns `0` across every file — no import,
      no stub key, no stale reference anywhere.
    - Source assertion: `grep -c 'showPasteDialog' src/components/SongLyricEditor.vue` returns `0` and
      `grep -c 'pasteMode' src/components/SongLyricEditor.vue` returns at least `4` (the ref, the two
      entry-point writers, the template branch, and `onPasteSaved`).
    - Source assertion: `grep -c 'v-show="pasteMode"\|v-show="!pasteMode"' src/components/SongLyricEditor.vue`
      returns `0` — the branch is a real `v-if` so unmount resets the region.
    - Source assertion: `grep -cE 'serviceLocked|overrideCopyright|canConfirm' src/components/SongLyricEditor.vue`
      returns `0` (35-UI-SPEC §5 and P-02).
    - Source assertion: `grep -c 'SaveStatusIndicator' src/components/SongLyricEditor.vue` returns at
      least `2` and `grep -c 'BackgroundControl' src/components/SongLyricEditor.vue` returns at least
      `2` — Phase 32's and Phase 33's additions survived.
    - Type gate: `npm run type-check` (`vue-tsc --build`) exits 0. Per CLAUDE.md this is the ONLY
      sufficient type gate; `-p tsconfig.app.json` silently skips test files and has previously hidden
      real `TS2339` errors across two full phases.
    - Build gate: `npm run build` succeeds. Note per CLAUDE.md that `vite.config.ts` aborts the build
      when any `VITE_FIREBASE_*` var is missing, so `.env.local` must be present in the working
      checkout (symlink or copy it from `C:\projects\worshipplanner\.env.local` if this runs in a
      worktree).
  </acceptance_criteria>

  <done>
    The lyric editor swaps to the paste view in place from either entry point, the modal is gone from
    the repository, and nothing about the Sections view — autosave status, song background, section
    rows, history — changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover the host-driven paste mechanisms in SongLyricEditor.test.ts</name>
  <files>src/components/__tests__/SongLyricEditor.test.ts</files>

  <read_first>
    - `src/components/__tests__/SongLyricEditor.test.ts` lines 1-60 (fixtures and mocks — note the
      `@/stores/songLyrics` mock at line 82 already provides `saveLyrics` as `mockSaveLyrics`, which
      the un-stubbed region needs), lines 125-150 (the `mountEditor` helper and its `stubs` object),
      and lines 186-205 (the empty-state and header-actions tests that must stay unmodified)
    - `src/components/__tests__/LyricPasteRegion.test.ts` (created by plan 03) — the two rows this
      task re-homes are documented there by a comment; do not duplicate coverage that already lives
      in that file
    - Plan 03's `<sanctioned_test_edits>` migration map, rows 12 and 13
    - `.planning/phases/35-presentation-correctness-lyric-editor/35-UI-SPEC.md` § Empty/Edge States
      rows E6 and E10
  </read_first>

  <behavior>
    - Test 1 (re-homed row 12): `paste-region` does not exist until `paste-lyrics-btn` is clicked;
      `lyrics-header` does. This replaces the deleted "renders nothing when closed".
    - Test 2 (header swap): after entering paste mode, `lyrics-paste-header` exists and
      `lyrics-header`, `song-background-row`, `lyrics-scroll-region`, `section-rows` and
      `add-section-row` do not.
    - Test 3 (re-homed row 13, E6): type into `paste-textarea`, exit via `paste-cancel-btn` with
      `window.confirm` stubbed to return true, re-enter via `paste-lyrics-btn`, and assert the
      textarea's value is the empty string. This replaces the deleted "resets textarea when reopened".
    - Test 4 (E10, both exits): with pasted text present, clicking `paste-back-btn` fires
      `window.confirm` and, when it returns false, leaves paste mode active with the text intact.
      Repeat for `paste-cancel-btn`.
    - Test 5: from the empty state (`currentLyrics` null), `paste-cta-btn` reaches the same
      `paste-region`.
    - Test 6 (R066): the rendered editor contains a `paste-region` that is a descendant of the
      component's own root element, and no element in the output carries the modal's former
      `fixed inset-0` backdrop classes.
    - Test 7: a successful paste returns to the Sections view — after the region emits `saved`,
      `paste-region` is gone and `lyrics-header` is back.
  </behavior>

  <action>
    In `src/components/__tests__/SongLyricEditor.test.ts`, remove the `LyricPasteDialog` key from
    `mountEditor`'s `global.stubs` object. Keep the `Teleport` stub — other parts of the file may rely
    on it and removing it is out of this task's scope. Do NOT add a stub for `LyricPasteRegion`: these
    tests must exercise the real inline region, which is the whole point of R066's coverage. The
    file's existing `@/stores/songLyrics` mock already supplies `saveLyrics`, so the un-stubbed region
    mounts cleanly.

    Add one new top-level describe block titled
    `paste mode — the lyric paste happens inline, not in a modal (R066)`. The token `paste` in that
    title keeps 35-VALIDATION.md's `-t "paste"` filter matching a real test rather than filtering
    everything out. Cover the seven behaviors above, using the file's existing `mountEditor`,
    `makeLyrics` and `flushPromises` conventions and its `mockCurrentLyrics` / `mockIsLoading` refs to
    drive the loaded and empty states.

    For Test 3 and Test 4, spy on `window.confirm` with `vi.spyOn` and restore it at the end of each
    test, matching the pattern the deleted `LyricPasteDialog.test.ts` used at its lines 173-184.

    For Test 7, drive the save through the real region: paste a CCLI sample into `paste-textarea`,
    click `paste-replace-btn`, and await `mockSaveLyrics`. Reuse a CCLI sample constant defined
    locally in this file rather than importing one across test files.

    Add a short comment at the top of the new describe block recording that these tests are the
    re-homed rows 12 and 13 of plan 03's `LyricPasteDialog.test.ts` migration map, so a later reader
    auditing the migration can close the loop.

    **Change nothing else in this file.** Every other test exercises the Sections view, which renders
    identically because `pasteMode` defaults `false`. If one of them goes red, that is a signal Task 1
    disturbed the template chain — **stop and re-examine Task 1** rather than adjusting the test.
    **Do not touch `src/utils/__tests__/ccliParser.test.ts` or
    `src/utils/__tests__/songSectionOrder.test.ts`** (P-03).
  </action>

  <verify>
    <automated>npx vitest run src/components/__tests__/SongLyricEditor.test.ts src/components/__tests__/LyricPasteRegion.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Test command: `npx vitest run src/components/__tests__/SongLyricEditor.test.ts` is green with a
      non-zero passing count.
    - Test command: `npx vitest run src/components/__tests__/SongLyricEditor.test.ts -t "paste"`
      matches at least 9 tests (the 2 pre-existing paste-button tests plus the 7 new ones) and passes.
      A "0 passed" result is a FAILURE.
    - Source assertion: `grep -c 'paste-dialog-stub' src/components/__tests__/SongLyricEditor.test.ts`
      returns `0`.
    - Behavior assertion: `git diff src/components/__tests__/SongLyricEditor.test.ts` shows exactly two
      kinds of change — the one-line stub removal in `mountEditor`, and the appended describe block.
      No assertion inside a pre-existing test is modified.
    - Behavior assertion: `git diff --name-only HEAD -- src/utils/__tests__/ccliParser.test.ts src/utils/__tests__/songSectionOrder.test.ts`
      is empty (P-03).
    - Type gate: `npm run type-check` exits 0.
    - Full suite: `npx vitest run src/` shows no failures beyond the known-failing baseline of
      `src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts` (9 tests / 2 files).
      The total test count should be roughly `~2219 + the phase's net additions`; a DROP relative to
      2219 means migrated coverage was lost and must be investigated before the phase closes.
  </acceptance_criteria>

  <done>
    The host-driven open, close, reopen-reset and both-exit-guard mechanisms are covered against the
    real inline region, the dialog stub is gone, and every pre-existing test in the file still passes
    untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Songs catalog UI → `SongLyricEditor` | An org editor's session reaches canonical catalog data. Unchanged by this plan — the same `authStore.isEditor` gate and the same Firestore rule on the lyrics subcollection apply. |
| Host `pasteMode` → region lifecycle | The host controls when the region mounts, and therefore when `rawText` and `overrideCopyright` reset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-14 | Tampering | `pasteMode` branch using `v-show` instead of `v-if` | medium | mitigate | `v-show` would keep the region mounted across closes, carrying one song's pasted text and override state into the next open. The plan mandates `v-if` and an acceptance-criteria grep asserts no `v-show` binds `pasteMode`; Test 3 pins the reset behaviour. |
| T-35-15 | Information disclosure | Stale paste carried between songs | medium | mitigate | Same mitigation as T-35-14 — unmount clears `rawText`, so song A's lyrics can never appear pre-filled while editing song B. |
| T-35-16 | Denial of service (user lockout) | A host-side condition layered on the region's gate | high | mitigate | P-02. The host references none of `overrideCopyright`, `canConfirm` or `parsed`; asserted by grep returning 0. The only gate is the region's own three-clause `canConfirm`. |
| T-35-17 | Repudiation | Deleting `LyricPasteDialog.test.ts` before its coverage lands | high | mitigate | Task 1 requires confirming `LyricPasteRegion.test.ts` exists and is green before the deletion, and the plan's `depends_on: ["35-03"]` enforces the wave ordering. The full-suite acceptance criterion flags a net test-count drop. |
| T-35-18 | Access control | Adding lock-aware gating that does not belong | low | accept | 35-UI-SPEC §5 verified no `serviceLocked` prop exists anywhere in `SongSlideOver.vue`'s or `SongLyricEditor.vue`'s prop chain. The plan forbids introducing one; a grep asserts it. Accepted as unchanged behaviour rather than a new control. |
| T-35-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages (35-RESEARCH.md § Package Legitimacy Audit: "Not applicable"). No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
1. `npx vitest run src/components/__tests__/SongLyricEditor.test.ts src/components/__tests__/LyricPasteRegion.test.ts` — green.
2. `npx vitest run src/components/__tests__/SongLyricEditor.test.ts -t "paste"` — at least 9 matched and passing.
3. `grep -rc 'LyricPasteDialog' src/` — `0` everywhere; both files absent from the working tree.
4. `npm run type-check` — exits 0. **Not** `-p tsconfig.app.json`.
5. `npx vitest run src/` — no failures beyond the 2-file baseline, and no net drop in total test count versus the ~2219 pre-phase figure.
6. `npm run build` — succeeds (requires `.env.local`; see CLAUDE.md).
7. `grep -rEin 'ccli (requires|mandates|requirement)|licen[cs]e requires' src/` — 0 matches (P-01).
8. `git diff --name-only HEAD -- src/utils/__tests__/ccliParser.test.ts src/utils/__tests__/songSectionOrder.test.ts` — empty (P-03).
9. Phase-level close-out: `grep -rc 'sectionLabel' src/components/PresentationViewer.vue` is `0` (plan 01) and `git diff --name-only HEAD -- src/utils/slideshowAssembler.ts src/utils/slideGroupMaterializer.ts` is empty (plan 02) — confirming the phase's two structural invariants held across all four plans.
</verification>

<success_criteria>
- Pasting lyrics happens inline in the editor; the modal no longer exists in the repository.
- Both entry points (`paste-lyrics-btn`, `paste-cta-btn`) reach the same single surface.
- Opening paste mode always starts with an empty textarea.
- Both exits fire the unsaved-changes guard.
- A successful paste returns to the Sections view with the new sections rendered.
- `SaveStatusIndicator` and the song-level `BackgroundControl` are undisturbed.
- No lock gating was introduced; no host-side condition was layered on the paste gate.
- No pre-existing assertion in `SongLyricEditor.test.ts` was modified, and no migrated
  `mockSaveLyrics` assertion was lost.
</success_criteria>

<output>
Create `.planning/phases/35-presentation-correctness-lyric-editor/35-04-SUMMARY.md` when done.
Record: the total test count before and after the phase (to evidence that migration lost nothing);
confirmation that `grep -rc 'LyricPasteDialog' src/` returns 0; and an explicit statement that the
two sanctioned pre-existing-test edits — the inverted `PresentationViewer.test.ts` R059 assertion
(plan 01) and the `LyricPasteDialog.test.ts` migration (plans 03-04) — were the only ones made, with
`ccliParser.test.ts` and `songSectionOrder.test.ts` untouched. Do not describe copyright placement
as a licensing requirement.
</output>
