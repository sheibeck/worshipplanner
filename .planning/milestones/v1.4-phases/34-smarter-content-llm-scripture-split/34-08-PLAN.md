---
phase: 34-smarter-content-llm-scripture-split
plan: 08
type: execute
wave: 4
gap_closure: true
depends_on: [34-05, 34-06, 34-07, 34-09, 34-10, 34-11, 34-12]
files_modified:
  - src/utils/__tests__/congregationalReadingPipeline.test.ts
  - .planning/phases/34-smarter-content-llm-scripture-split/34-VALIDATION.md
  - .planning/PENDING-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [R064]

must_haves:
  truths:
    - "A scripture item can be split into a leader/congregation congregational reading and that reading reaches the projected slide (ROADMAP Success Criterion 1) — the truth that FAILED in 34-VERIFICATION.md."
    - "Two consecutive congregational sections with the same speaker are never merged, and a section whose text begins where the previous one ends yields adjacent entries in the assembled slide's sections array with no duplicated and no dropped text."
    - "A SCRIPTURE slot whose congregationalSections is absent or empty materializes exactly as it does today under R047 — one reference-only slide, readingMode 'normal', no sections key — with zero change to any existing service's slides."
    - "Congregational sections reach the assembled slide in stored array order, and that order is stable across re-materialization (buildInitialGroup) and group rebuild (rebuildGroup) — nothing re-sorts them."
    - statement: "Section text written onto ScriptureSlot and read back through the full slot -> group -> assembled slide path is byte-identical to the ESV source including non-ASCII punctuation (U+2018/U+2019/U+201C/U+201D curly quotes, U+2014 em dash) — no normalization, trim, or case folding anywhere in that path."
      verification: backstop
    - "R064's recorded status matches what is actually delivered — no repeat of the erroneous Complete flip that 34-VERIFICATION.md had to correct."
    - "R070 and R071 — the two requirements the UAT plans proposed and wrote — carry statuses matching the same observed gates, flipped in this one place and nowhere else."
    - "The phase record distinguishes R064's gap closure from the four owner UAT findings planned alongside it, so a future reader does not read F2..F5 as R064 scope."
    - "Every human-only check remains open and unapproved in PENDING-VERIFICATION.md; none is recorded as passed."
  artifacts:
    - "src/utils/__tests__/congregationalReadingPipeline.test.ts — the composed slot -> group -> slide contract"
    - ".planning/phases/34-smarter-content-llm-scripture-split/34-VALIDATION.md — appended gap-closure section covering plans 34-05..34-12"
    - ".planning/PENDING-VERIFICATION.md — item 34.2 resolved; items 34.3..34.6 opened"
    - ".planning/REQUIREMENTS.md — R064, R070 and R071 statuses corrected to match delivery"
    - ".planning/ROADMAP.md — Phase 34 plan list extended to 12 plans"
  key_links:
    - "ScriptureSlot.congregationalSections -> assembleSlideshow (fallback path) -> ScriptureSlide.sections"
    - "ScriptureSlot.congregationalSections -> buildInitialGroup -> assembleSlideshow (stored-group path) -> ScriptureSlide.sections"
    - "ScriptureSlide.readingMode + sections -> PresentationViewer.isCongregational (Phase 35, untouched)"
  prohibitions:
    - "The AI split runs only as the direct result of an explicit user action on the split control — never automatically on mount, on passage fetch, on reference change, or on any watcher."
    - "Mounting, opening or closing the editor never itself emits a change: a user who opens the panel and closes it without touching anything causes zero writes to the service document."
    - "A stored congregational reading is never projected under a passage reference it was not derived from: changing a SCRIPTURE slot's reference to a different passage clears that slot's congregationalSections."
---

<objective>
Prove the composed path end to end, then record honestly what this gap closure did and did not deliver.

Purpose: the individual pieces are tested in their own plans, but the failure this phase is closing was
precisely that every piece passed while the composition reached no user. This plan asserts the composed
contract in one place, then updates the four planning records that currently describe Phase 34 as
partially delivered.

Output: a pipeline test file, an extended validation strategy, a resolved PENDING-VERIFICATION item, a
corrected requirement, and an updated roadmap.

## ★ Two distinct bodies of work share this phase — the record must not blur them

Plans **34-05 .. 34-08** close R064's reachability gap (`34-VERIFICATION.md` Truth 1).

Plans **34-09 .. 34-12** close four owner UAT findings from the 2026-08-03 hands-on session
(`34-UAT.md` F2..F5). They are **not R064 work** — F3 and F4 are Phase 33 and Phase 32 defects, F2 is a
Phase 33 layout finding, F5 is a Planning Center / auth finding whose owner-side diagnosis was
incorrect. They were planned here because the owner chose to close them in this phase rather than
split them across phases, and each of those plans says so in its own objective.

Every record this plan writes keeps that distinction visible. R064's status describes R064's delivery
only; R070 and R071 are the two requirements those UAT plans had to write because the behaviours they
deliver had no requirement covering them at all.

## Artifacts this phase produces

Everything created or changed across plans 34-05 through 34-12:

**Types (`src/types/service.ts`)**
- `ScriptureSlot.congregationalSections?: CongregationalSection[]` — new optional field. No new type is
  declared; `CongregationalSection` (`src/types/slide.ts:70-74`) is reused as-is.

**Utilities (`src/utils/scripture.ts`)**
- `congregationalSlideFieldsFromSlot(slot: ScriptureSlot): { readingMode: 'normal' | 'congregational'; sections?: CongregationalSection[] }`
  — the single congregational-ness predicate for the whole assembly path.
- `scriptureSlotAfterReferenceChange(slot: ScriptureSlot, ref: ScriptureRef | null): ScriptureSlot`
  — the reference write plus the stale-reading clearing rule, in one place.

**Assembler (`src/utils/slideshowAssembler.ts`)**
- `resolveEntryContent` scripture branch: spreads `congregationalSlideFieldsFromSlot(slot)`.
- SCRIPTURE fallback branch: spreads the same helper. No new function, no third construction path.

**Component (`src/components/CongregationalEditor.vue`)**
- Props: `reference: ScriptureRef | null`, `sections: CongregationalSection[]`.
- Emits: `update:sections` (CongregationalSection[]), `update:reference` (ScriptureRef), `close`.
- Internal: `draftSections` ref, `emitSections()` helper.
- New test id: `congregational-close-btn`.
- Removed: `orgId` and `readingId` props, `currentReadingId`, `surfaceId`, `doAutoSave`, the
  `defineExpose` test seam, the scripture-slides store dependency, the auto-save composable, the shared
  save-status store, and the header save-status child.

**Slide-side routes to the editor (34-07)**
- `src/components/slides/slideDisplay.ts` — the scripture menu item relabelled and reclassified off the navigation tone.
- `src/components/slides/EditSlideDrawer.vue` — `edit-scripture-text` emit, `drawer-edit-scripture-text-btn`, rewritten `SCRIPTURE_TEXT_CAPTION`.
- `src/components/slides/SlidesTab.vue` — `onDrawerEditScriptureText()`, converging on the existing `requestEditInScripture()`.

**View (`src/views/ServiceEditorView.vue`)**
- `congregationalSlotIndex: Ref<number | null>` and the `congregationalSlot` computed.
- `handleNavigateToScriptureEditor` — same name, same relay seat, body replaced: it opens the editor instead of switching tabs and scrolling.
- `onCongregationalSectionsChange(index: number, sections: CongregationalSection[]): void`, `closeCongregationalEditor()`.
- `onScriptureChange` routed through `scriptureSlotAfterReferenceChange`.
- New test ids: `congregational-editor-modal`, `congregational-editor-panel`, `congregational-editor-close`.
- Static import of `CongregationalEditor` — the mount point whose absence was the gap.

**Owner UAT findings (34-09 .. 34-12) — not R064 work**
- `src/components/PresentationViewer.vue` — `currentBackgroundUrl`, the background and scrim layers (F3, R070).
- `.planning/REQUIREMENTS.md` — `R070` (background display at presentation time) and `R071` (the no-credentials explanation), both newly written because the behaviours had no requirement at all.
- `src/stores/saveStatus.ts` — `hasVisibleSaveStatus(entry)`; `src/views/ServiceEditorView.vue` — the sticky bar's wrapper gate (F4).
- `src/components/slides/SlideGrid.vue` — `showGroupMusicControl` / `showGroupBackgroundControl` and the merged group-media panel (F2).
- `src/views/ServiceEditorView.vue` — `pc-credentials-missing-note` and its Settings route (F5); `src/stores/__tests__/auth.test.ts` — the `hasPcCredentials` field-shape matrix and the load-order diagnosis.

**Tests**
- `src/utils/__tests__/scripture.test.ts` — helper unit and source-inspection cases.
- `src/utils/__tests__/slideshowAssembler.test.ts` — dual-path parity, backward compatibility.
- `src/utils/__tests__/slideGroupMaterializer.test.ts` — no-change verification.
- `src/components/__tests__/CongregationalEditor.test.ts` — emit contract, manual path, AI path, prohibitions.
- `src/views/__tests__/ServiceEditorView.test.ts` — reachability, WR-04 keyed mount, permissions, the save-status wrapper gate, the PC no-credentials note.
- `src/components/slides/__tests__/slideDisplay.test.ts`, `EditSlideDrawer.test.ts`, `SlidesTab.test.ts` — the two slide-side routes and the relabel.
- `src/components/__tests__/PresentationViewer.test.ts` — background rendering, video precedence, absence, scrim.
- `src/components/__tests__/SaveStatusIndicator.test.ts` — the predicate/indicator agreement over the whole status union.
- `src/components/slides/__tests__/SlideGrid.test.ts` — merged-panel render matrix and the no-behaviour-change regression block.
- `src/stores/__tests__/auth.test.ts` — `hasPcCredentials` field-shape matrix and load-order cases.
- `src/utils/__tests__/congregationalReadingPipeline.test.ts` — NEW, the composed contract.

**Deliberately NOT produced:** no package install or upgrade; no change under `functions/` or
`render-service/`; no change to `src/utils/scriptureBoundaries.ts`, to the split/schema/validator logic
in `src/utils/claudeApi.ts`, or to `src/components/PresentationViewer.vue`; no deletion of
`src/stores/scriptureSlides.ts` (still consumed by `useSlideshowAssembly.ts` and
`ScriptureSlideEditor.vue`); no deploy of any kind.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-VERIFICATION.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-UAT.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-VALIDATION.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-05-SUMMARY.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-06-SUMMARY.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-07-SUMMARY.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-09-SUMMARY.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-10-SUMMARY.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-11-SUMMARY.md
@.planning/phases/34-smarter-content-llm-scripture-split/34-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert the composed slot -> group -> slide contract in one place</name>
  <files>src/utils/__tests__/congregationalReadingPipeline.test.ts</files>
  <read_first>
    - src/utils/slideshowAssembler.ts lines 39-60 (AssemblyInputs) and line 273 (`assembleSlideshow`)
    - src/utils/slideGroupMaterializer.ts line 164 (`buildInitialGroup`) and line 699 (`rebuildGroup`)
    - src/utils/__tests__/slideshowAssembler.test.ts (the AssemblyInputs fixture builder to reuse — build the same shape here rather than inventing a second one)
    - src/utils/scripture.ts (`congregationalSlideFieldsFromSlot` from 34-05)
    - src/components/PresentationViewer.vue lines 444-455 (`isCongregational` — the predicate the assembled slide must satisfy)
    - src/types/slide.ts lines 70-85 (CongregationalSection, ScriptureSlide)
  </read_first>
  <behavior>
    - Fallback path: a Service with one SCRIPTURE slot holding three sections and no stored group assembles to exactly ONE scripture slide whose readingMode is 'congregational' and whose sections deep-equal the stored three, in stored order.
    - Stored-group path: feeding that slot through buildInitialGroup and assembling with the resulting group in groupsBySlotId produces the same one slide with the same readingMode and sections.
    - Rebuild stability: running rebuildGroup on that group and reassembling yields the same sections in the same order.
    - Same-speaker adjacency: two consecutive sections both marked CONGREGATION both appear, unmerged, as separate entries.
    - Text adjacency: concatenating the assembled sections' text reproduces the source passage with nothing duplicated and nothing dropped.
    - Backward compatibility, absent: a SCRIPTURE slot with no congregationalSections assembles to one scripture slide with readingMode 'normal', empty text, empty verseRange and no sections key — on both paths.
    - Backward compatibility, empty: the same holds for congregationalSections set to [].
    - Encoding backstop: a section whose text contains U+2018 U+2019 U+201C U+201D U+2014 survives the whole path with strict === equality.
    - Presentation predicate: for the congregational case, replicating PresentationViewer's own condition (readingMode 'congregational' AND sections.length greater than zero) over the assembled slide evaluates true; for both backward-compatible cases it evaluates false.
  </behavior>
  <action>
    Create `src/utils/__tests__/congregationalReadingPipeline.test.ts`. This file exists because Phase 34
    shipped 118 passing tests over machinery no user could reach; its job is to assert the COMPOSITION,
    not to re-test the parts. State that in a header comment naming `34-VERIFICATION.md` Truth 1.

    Build one shared `Service` fixture containing a single SCRIPTURE slot with a stable `id`, a real
    book/chapter/verse reference, and a `congregationalSections` array of three sections alternating
    LEADER / CONGREGATION / CONGREGATION — the third deliberately repeating the second's speaker so the
    non-merging case is covered by the main fixture rather than a special one. Derive each section's
    text by partitioning one source passage string so the concatenation check is meaningful.

    Write a small helper inside the test file that assembles the fixture two ways — once with an empty
    `groupsBySlotId` map (fallback path) and once with the map populated from `buildInitialGroup` — and
    returns both assembled scripture slides, so every case below can assert against both paths without
    duplicating setup. Assert the presentation predicate by inlining PresentationViewer's own condition
    rather than importing the component: the point is that the two agree, so restating the condition is
    the assertion.

    Cover every item in the behavior list. For the rebuild-stability case use `rebuildGroup` on the
    group produced by `buildInitialGroup`, reassemble, and compare the sections array to the
    pre-rebuild one. For the encoding backstop use a non-ASCII fixture and assert with strict `===` on
    each section's text, mirroring the `NON_ASCII_FIXTURE` technique already established in
    `src/utils/__tests__/scriptureBoundaries.test.ts`.

    Do not modify any production file in this task.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/congregationalReadingPipeline.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/__tests__/congregationalReadingPipeline.test.ts` passes with at least 9 cases.
    - A test asserts the fallback-path and stored-group-path assembled scripture slides have deep-equal `readingMode` and `sections`.
    - A test asserts exactly ONE scripture slide is produced for a slot with three sections — not three slides.
    - A test asserts the concatenation of the assembled sections' text equals the source passage string exactly.
    - A test asserts two consecutive same-speaker sections both survive as separate array entries.
    - Tests assert `readingMode` equals `'normal'` and `Object.prototype.hasOwnProperty.call(slide, 'sections')` is `false` for both the absent and empty backward-compatibility cases, on both paths.
    - A test asserts strict `===` equality on non-ASCII section text end to end.
    - A test asserts the inlined PresentationViewer predicate is `true` for the congregational case and `false` for both backward-compatible cases.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>The composed contract — the thing that was broken — is asserted in one readable file, on both materialization paths and across a rebuild.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the validation strategy and resolve the blocking PENDING-VERIFICATION item</name>
  <files>.planning/phases/34-smarter-content-llm-scripture-split/34-VALIDATION.md, .planning/PENDING-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/34-smarter-content-llm-scripture-split/34-VALIDATION.md (the whole file — its Test Infrastructure table, Sampling Rate, Per-Task Verification Map and Manual-Only Verifications sections are the format to extend)
    - .planning/PENDING-VERIFICATION.md lines 290-340 (items 34.1 and 34.2 in full, including the WR-04 contract text carried in 34.2)
    - .planning/STATE.md lines 22-62 (the STANDING AUTONOMY GRANT — in particular "never record a deferred check as passed" and "no deploys")
    - CLAUDE.md (the type-check and test-suite rules that the validation table must state correctly)
  </read_first>
  <action>
    APPEND to `34-VALIDATION.md`; do not overwrite or restructure what is there. The existing content
    describes the structural-correctness work that is done and verified, and it stays as the record of
    that. Add a new top-level section titled `Gap Closure (plans 34-05 .. 34-12)` after the existing
    `Manual-Only Verifications` section and before `Validation Sign-Off`, containing:

    A per-task verification table in the same column shape as the existing one, with a row for each task
    in 34-05, 34-06, 34-07, 34-08, 34-09, 34-10, 34-11 and 34-12, each carrying its plan, wave, test
    type, and the exact automated command from that task's verify block. Every row must have an
    automated command — there are no Wave 0 gaps in this closure because every test file involved
    already exists except `congregationalReadingPipeline.test.ts`, which is created by the task that
    asserts against it.

    The requirement column is NOT R064 for every row. Rows from 34-05..34-08 carry R064; rows from
    34-09 carry R055/R056/R070; 34-10 carries R040/R041; 34-11 carries R055; 34-12 carries R071. Getting
    that column right is the point of the table — writing R064 across all twelve plans would restate
    exactly the false premise this phase already had to correct once.

    A short note recording the two test-command hazards this closure must respect, taken from CLAUDE.md
    and STATE.md rather than restated from memory: the only sufficient type gate is `npm run type-check`
    (`vue-tsc --build`), and a bare `npx vitest run src/` also matches `render-service/src/render.test.ts`
    by substring and fails on a Vitest version mismatch, so the app-suite gate is `npx vitest run --dir src`
    with the known-failing baseline of `src/storage.rules.test.ts` and
    `src/views/__tests__/RosterView.test.ts`.

    A `Manual-Only Verifications (gap closure)` subsection with one row per check that jsdom genuinely
    cannot make:
    - the mounted affordance itself — open a real service, reach the congregational panel from a
      scripture SLIDE (both the 3-dot menu and the drawer), build a reading, and confirm it projects;
      jsdom cannot judge whether the route is discoverable or whether the projected Leader/Congregation
      layout reads correctly on a real screen;
    - the background scrim's legibility (34-09) — whether `bg-black/50` keeps projected text readable
      over a real photograph on a real projector is a perceptual judgment, not a DOM assertion;
    - the merged group-media panel (34-11) — whether one panel actually reads as one panel;
    - the Planning Center org-document check (34-12) — presence or absence of the credential fields,
      reported by the owner as booleans, never as values.

    Set `nyquist_compliant: true` in the frontmatter only if every row in the new table has an automated
    command, and tick the Validation Sign-Off boxes that the new section satisfies. Leave `Approval`
    as it is — that is not this plan's to grant.

    In `.planning/PENDING-VERIFICATION.md`, rewrite item **34.2** from an open owner-decision blocker to
    a resolved record: state that the direction was already decided (R047's slot-as-source-of-truth; the
    separate-reading-document model was rejected in `3da5fe4`, superseded by `5c531b1`), name what was
    built to close it (`ScriptureSlot.congregationalSections`, `congregationalSlideFieldsFromSlot`, both
    assembler call sites, the editor's persistence rewrite, and the keyed modal mount reached from the
    scripture SLIDE), and record that the WR-04 call-site contract the item carried is now honored by a
    `:key` on the slot's stable id with a dedicated slot-swap test. Record the 2026-08-03 owner decision
    that produced that mount seam: the scripture slide's edit route opens `CongregationalEditor`, and no
    free-text scripture override was added. Mark it resolved with the date. Leave item **34.1** exactly
    as it is — it is still open and must not be touched.

    Add four new open items, each marked `☐`, each explicitly noted as deferred under the standing
    autonomy grant and NOT self-approved:

    - **34.3** — the now-reachable feature: open a draft service, reach the congregational panel from a
      scripture slide by BOTH routes (the 3-dot menu and the drawer's Slide Text section), build a
      reading by hand and with the AI split, present the service, and confirm the Leader/Congregation
      layout projects correctly and legibly. Also confirm that changing the passage on a slot that
      already has a reading clears it as intended rather than surprising the user.
    - **34.4** (from 34-09 / UAT F3) — set a background on a group, present the service, and confirm the
      image appears behind every slide of that group and that the projected words stay readable over it
      on a real projector. The scrim opacity is the knob to revisit if they do not.
    - **34.5** (from 34-11 / UAT F2) — confirm the merged group-media panel reads as one panel rather
      than two rows in a box, and that a locked service still shows what it has without an empty box.
    - **34.6** (from 34-12 / UAT F5) — open the Firebase console for this organization's document and
      confirm whether the `pcAppId` and `pcSecret` fields exist and are non-empty. **Report presence or
      absence only — never the values.** This is the half of the F5 diagnosis that cannot be observed
      from a test environment, and it decides whether the symptom was cause 1 (no credentials
      configured) or something else. Carry over the verdict 34-12 Task 1 recorded in its SUMMARY.
  </action>
  <verify>
    <automated>grep -c "Gap Closure (plans 34-05" .planning/phases/34-smarter-content-llm-scripture-split/34-VALIDATION.md</automated>
    <automated>grep -c "34.3" .planning/PENDING-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Gap Closure (plans 34-05" .planning/phases/34-smarter-content-llm-scripture-split/34-VALIDATION.md` outputs a value of `1` or more.
    - The pre-existing `## Test Infrastructure`, `## Sampling Rate`, `## Per-Task Verification Map`, `## Wave 0 Requirements` and `## Manual-Only Verifications` headings all still exist in `34-VALIDATION.md` — nothing was overwritten.
    - Every row of the new gap-closure verification table has a non-empty automated command column.
    - The new table has a row for at least one task from each of the eight plans 34-05 .. 34-12.
    - The new table's requirement column contains at least one row naming R070 and at least one naming R071 — it is not R064 all the way down.
    - `.planning/PENDING-VERIFICATION.md` item 34.1 is still present and still marked `☐`.
    - `.planning/PENDING-VERIFICATION.md` item 34.2 records a resolution with a date, names the keyed mount, and names the 2026-08-03 owner decision on the slide-side mount seam.
    - `.planning/PENDING-VERIFICATION.md` contains new items 34.3, 34.4, 34.5 and 34.6, all marked `☐`.
    - Item 34.6's text asks for presence or absence only and contains no credential value.
  </acceptance_criteria>
  <done>The validation strategy covers the closure without losing the original record, the blocking item is resolved with its reasoning, and the new human check is open rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Correct R064, R070 and R071 and the roadmap to match delivery, then run the phase gate</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 186-210 (the R064 `[~]` block and the original requirement text below it)
    - .planning/REQUIREMENTS.md lines 81-96 (R047 — the delivered-shape note that handed the mount to Phase 34)
    - .planning/REQUIREMENTS.md — the R070 entry inserted by 34-09 (Backgrounds and Media) and the R071 entry inserted by 34-12 (Service Lifecycle), plus both traceability rows
    - .planning/ROADMAP.md lines 236-261 (the Phase 34 block: goal, success criteria, plan list, and the 2026-08-03 planning-corrections note whose item (c) this closure resolves)
    - .planning/phases/34-smarter-content-llm-scripture-split/34-VERIFICATION.md (the gap being closed, and the exact wording of the Truth 1 failure)
    - .planning/phases/34-smarter-content-llm-scripture-split/34-UAT.md (the five owner findings, so the record separates F1's R064 scope from F2..F5)
    - .planning/phases/34-smarter-content-llm-scripture-split/34-12-SUMMARY.md (the F5 diagnosis verdict, which the record must state rather than restating the owner's incorrect diagnosis)
    - .planning/STATE.md lines 22-62 (the standing autonomy grant — no deploys, never record a deferred check as passed)
  </read_first>
  <action>
    Run the phase gate FIRST, before editing any record, so the status written is the status observed:
    `npm run type-check`, then `npx vitest run --dir src`, then `npm run build`.

    Treat all three gates as ONE condition. If and only if the type gate is clean, the app suite's
    failing FILE SET is exactly the documented baseline (`src/storage.rules.test.ts` and
    `src/views/__tests__/RosterView.test.ts`) and the build succeeds, flip the three statuses below. If
    ANY gate is not clean, flip nothing and record precisely what failed instead. R064 was already
    flipped to Complete in error once and had to be corrected — do not repeat that by writing an
    optimistic status, and do not flip R070 or R071 "since they're small".

    In `.planning/REQUIREMENTS.md`, rewrite the R064 block: change the marker from `[~]` to `[x]` and
    replace the partial-delivery text with a delivered-shape note in the style R047 already uses in this
    file. That note must state: the structural guarantee from plans 34-01..34-04 (schema permits no
    string field but the speaker enum, byte-exact slicing, boundaries computed once, 19 distinct
    validator rejection cases); the reachability closure from 34-05..34-08 (the slot field, the single
    derivation helper, both assembler call sites, the persistence rewrite off the rejected
    reading-document model, and the keyed modal mount reached from the scripture SLIDE — the 2026-08-03
    owner decision, with no free-text scripture override); and — in the same breath, not a footnote —
    that two checks remain human-only and open, `PENDING-VERIFICATION.md` items 34.1 (empirical split
    determinism against Psalm 136 and Psalm 24) and 34.3 (the mounted affordance and its projection).
    The note must describe R064 ONLY. Plans 34-09..34-12 are owner UAT findings, not R064 scope, and
    folding them into R064's delivered-shape note would restate the false premise this phase already had
    to correct.

    Then flip `R070` (inserted by 34-09) and `R071` (inserted by 34-12) from unchecked to `[x]`, and
    update their Traceability rows to the file's convention for delivered. Append to R070's entry a
    one-line delivered-shape note naming `PresentationViewer.currentBackgroundUrl`, the scrim, and the
    video-precedence rule, and stating that `PENDING-VERIFICATION.md` item 34.4 — the perceptual
    legibility check on a real projector — is open. Append to R071's entry a one-line note naming the
    no-credentials explanation and its Settings route, stating that the export affordance remained
    gated, and recording the F5 verdict from `34-12-SUMMARY.md` plus the open item 34.6.

    In `.planning/ROADMAP.md`, update the Phase 34 block with a scoped edit — never rewrite the file.
    The eight gap-closure and UAT plan entries were already added at plan time; change the `**Plans**:`
    line to read `12/12 plans executed` once every entry is checked, and extend the
    `**Planning corrections (2026-08-03)**` note with two short closing sentences. First: correction
    (c) — `CongregationalEditor.vue` mounted nowhere — is closed by plans 34-05..34-08 via
    slot-as-source-of-truth, and the direction was never actually an open owner question (R047 had
    already decided it and R064's own text records the rejected alternative); the remaining open
    question, WHERE it mounts, was answered by the owner on 2026-08-03 in favour of the scripture
    slide's own edit route. Second: plans 34-09..34-12 close owner UAT findings F2..F5 and are NOT R064
    scope — two of them (F3, F5) required requirements that had never been written, now R070 and R071.

    Deploy nothing. Run no `firebase deploy`, no `gcloud`, no container build. The standing v1.4
    decision keeps deployment as the owner's step.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npx vitest run --dir src</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0.
    - `npx vitest run --dir src` completes with a failing FILE SET no larger than the documented baseline of `src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts`; any additional failing file is a regression and must be fixed before this task is done.
    - `npm run build` exits 0.
    - `grep -c "34-12-PLAN.md" .planning/ROADMAP.md` outputs a value of `1` or more.
    - `grep -c "12/12 plans executed" .planning/ROADMAP.md` outputs a value of `1` or more.
    - The R064 block in `.planning/REQUIREMENTS.md` names both `PENDING-VERIFICATION.md` items 34.1 and 34.3 as open, and names neither F2..F5 nor R070/R071 as part of its own delivery.
    - The R070 entry is `[x]` and names open item 34.4; the R071 entry is `[x]` and names open item 34.6.
    - `grep -c "^- \[~\] \*\*R064" .planning/REQUIREMENTS.md` outputs `0` and `grep -c "^- \[ \] \*\*R070" .planning/REQUIREMENTS.md` outputs `0` — all three statuses moved together, or none did.
    - `git log --oneline -1` shows no deploy-related command was run; no file under `functions/` or `render-service/` is modified by this phase.
  </acceptance_criteria>
  <done>The planning records state what was actually delivered, keep R064's scope distinct from the UAT findings, the three-command phase gate is green, and nothing was deployed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning record -> future planner/verifier | An overstated status causes the next run to build on a false premise — the exact failure mode this phase already suffered |
| Slot -> group -> assembled slide -> projected surface | The composed path this plan asserts end to end |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-08-01 | Repudiation | R064 status in REQUIREMENTS.md | high | mitigate | The status flip is conditional on three observed gates, the gates run before the edit, and the open human items are named in the requirement text itself |
| T-34-08-02 | Tampering | Composed path divergence between materialization routes | high | mitigate | The pipeline test asserts both routes plus a rebuild produce identical sections |
| T-34-08-03 | Information Disclosure | Scripture text projected under the wrong reference | high | mitigate | Covered by 34-07 Task 3's clearing rule; the pipeline test's backward-compatibility cases confirm a cleared slot returns to the reference-only shape |
| T-34-08-04 | Elevation of Privilege | Accidental deploy during the phase gate | medium | mitigate | The gate is exactly three commands (`type-check`, `vitest`, `build`); an explicit acceptance criterion asserts nothing under `functions/` or `render-service/` changed |
| T-34-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | **This plan installs and upgrades zero packages.** No package-manager task exists in any plan of this closure, so the Package Legitimacy Gate has nothing to audit |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/congregationalReadingPipeline.test.ts` — green.
- `npm run type-check` — exits 0 (`vue-tsc --build`; the `-p tsconfig.app.json` form is not sufficient evidence).
- `npx vitest run --dir src` — failing file set no larger than the documented two-file baseline. A bare
  `npx vitest run src/` must NOT be used as the gate: it also matches `render-service/src/render.test.ts`
  by substring and fails on a Vitest version mismatch.
- `npm run build` — exits 0. Requires `.env.local` in the working tree; the canonical copy is at
  `C:\projects\worshipplanner\.env.local`.
- Nothing deployed, containerized or provisioned.
</verification>

<success_criteria>
- The composed slot -> group -> slide contract is asserted on both materialization paths and across a rebuild.
- All four edge behaviours (adjacency, empty/backward compatibility, encoding, ordering) have executable assertions.
- `34-VALIDATION.md` is extended, not replaced, and covers all eight closure plans with the right requirement per row.
- PENDING-VERIFICATION item 34.2 is resolved; 34.1 is untouched and open; 34.3, 34.4, 34.5 and 34.6 are new and open.
- R064, R070 and R071 all carry statuses matching observed gate results, flipped together or not at all, with every open human item named.
- R064's delivered-shape note describes R064 only — the four owner UAT findings are recorded as what they are.
- ROADMAP Phase 34 reads 12/12 with all plans listed.
</success_criteria>

<output>
Create `.planning/phases/34-smarter-content-llm-scripture-split/34-08-SUMMARY.md` when done.
</output>
