---
phase: 36-ui-rework-service-order-contextual-action-bars
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/actionBarItems.ts
  - src/components/ContextualActionBar.vue
  - src/components/__tests__/ContextualActionBar.test.ts
  - src/views/serviceEditorActionBar.ts
  - src/views/__tests__/serviceEditorActionBar.test.ts
autonomous: true
requirements: [R068]

must_haves:
  truths:
    - "One shared `ContextualActionBar.vue` exists and renders a declarative `items` array; it owns no state and decides nothing about what is in the list."
    - "R068 acceptance is answerable as DATA, not DOM: `buildActionBarItems(tab, ctx)` returns the key list for a tab and can be asserted without mounting anything."
    - "Neither `suggest-all-songs` nor `export-pc` nor `copy-pc` appears in the Slides or the Roles item list, for any combination of context flags."
    - "The Roles item list is empty for every combination of context flags."
    - "GATING INVARIANT: for every (tab x canEditService x hasPcCredentials x status) combination, the emitted key set equals the set of controls that same user could press before this phase — no key is emitted that the pre-phase markup would not have rendered, and none is withheld that it would."
    - "R068 edge/adjacency: in the Slides list, `present` sits immediately before `save` — asserted as `keys.indexOf('present') === keys.indexOf('save') - 1`, matching design 1a's header row."
    - "R068 edge/ordering: the Service Order list order is exactly suggest, then export-or-copy, then save — the same left-to-right lifecycle order the page header renders today."
    - "R068 edge/idempotency: two calls with the same context return key-identical lists, and building for tab A then B then A again returns a list key-identical to the first A."
    - "R068 edge/empty: an empty `items` array renders zero buttons and no chrome — no border, background, padding or margin utility on the container, so no empty box appears (31-UI-SPEC E5)."
    - "R068 edge/concurrency: while `aiSuggestingAll` is true the suggest item is `disabled`, and while `isExporting` is true the export item is `disabled` — a second dispatch cannot be issued from the bar mid-flight."
    - "The `present` tone is visually distinct from the `primary` tone: outlined indigo versus filled indigo, matching design 1a's own distinction between Present and Save."
    - "Every action-bar entry renders a native `<button type=\"button\">`; a disabled entry does not invoke its handler when clicked."
    - statement: "The action-bar row must not force horizontal scrolling at the narrowest supported viewport — the existing header row's wrapping behaviour is not regressed by the consolidation."
      verification: backstop
  artifacts:
    - "src/components/actionBarItems.ts — `ActionBarItem`, `ActionBarTone`, `ActionBarIcon`"
    - "src/components/ContextualActionBar.vue — the one shared bar"
    - "src/views/serviceEditorActionBar.ts — `ActionBarTab`, `ActionBarContext`, `buildActionBarItems`"
    - "src/components/__tests__/ContextualActionBar.test.ts"
    - "src/views/__tests__/serviceEditorActionBar.test.ts — the R068 acceptance suite (pure, no mount)"
  key_links:
    - "`buildActionBarItems` is pure and takes handlers by reference — the bar dispatches the EXACT existing handler functions, never a reimplementation"
    - "`ContextualActionBar`'s dynamic `hint-{key}` slot is what lets R071's note carry a live router-link; a plain string field could not"
  prohibitions:
    - "Do not reimplement any handler inside the bar or inside the builder. Both are dispatch-only; every write guard stays where it already lives."
    - "Do not let a control become pressable by a user who could not press it before. Where the UI-SPEC's illustrative code and the live source disagree about a gate, the live source wins — that is this phase's stated central invariant."
  assumptions:
    - "★ FLAGGED SPEC DIVERGENCE (recorded, not silently applied): `36-UI-SPEC.md` §3's illustrative code puts the export/copy item inside `if (canEditService)`, and its Empty-State row E3 asserts 'every Service Order item is canEditService-gated'. **Both are false against live source.** `ServiceEditorView.vue:166` gates the export button on `authStore.hasPcCredentials` alone and `:199` is its bare `v-else`; neither carries `canEditService`, and the enclosing header div at `:97` has no gate either — so a viewer, and an editor on a locked service, see Export/Copy today. This plan PRESERVES that (the phase invariant outranks illustrative spec code) and records E3 as inaccurate. A future phase may deliberately narrow it; this one must not do so by accident."
    - "★ FLAGGED SPEC DIVERGENCE: the UI-SPEC's `hint?: string` field cannot carry R071's `<router-link>` to Settings. Implemented as a dynamic `hint-{key}` slot instead, so `34-12`'s note moves verbatim including its link and its testid. Dropping the link would have been a silent regression of a named prior fix."
    - "★ FLAGGED SPEC EXTENSION: the UI-SPEC's `icon` union omits a clipboard glyph, but the live `copy-pc` button renders one. The union gains a `copy` member so no relocated control loses its icon."
    - "UNRESOLVED (stays unresolved, per 36-UI-SPEC § UI Considerations): whether Roles SHOULD expose zero actions is the spec's own recommendation, not a wireframe fact — neither drawn turn shows a Roles header state. The empty list is implemented; the DESIGN question stays open."
---

<objective>
Build the two things R068 needs before anything can be wired: the one shared `ContextualActionBar.vue`,
and a **pure** per-tab item builder that makes R068's acceptance test answerable against data instead of
against rendered DOM in three places.

Purpose: `36-CONTEXT.md` is explicit that a DOM-level R068 test is the shape that let
`Suggest All Songs` leak onto Roles in the first place. A pure builder cannot pass for the wrong reason.
Output: two new components/modules and two new test files, none of which touches an existing file — so
this plan runs in wave 1 alongside `36-01` with zero collision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-UI-SPEC.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-CONTEXT.md
@src/views/ServiceEditorView.vue
@src/components/slides/SlidesTab.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The ActionBarItem contract and the shared ContextualActionBar component</name>
  <files>src/components/actionBarItems.ts, src/components/ContextualActionBar.vue, src/components/__tests__/ContextualActionBar.test.ts</files>

  <read_first>
    - `36-UI-SPEC.md` § Phase-Specific Component Contracts §2 (the interface and the markup),
      § Spacing Scale (including the exceptions note), § Typography, § Color, § Accessibility Note
    - `src/views/ServiceEditorView.vue` lines 96–246 — the four header buttons being consolidated, read
      for their exact classes, labels, titles, disabled conditions and inline SVG paths
    - `src/components/slides/SlidesTab.vue` lines 12–23 — the Present button's exact outlined-indigo
      class string and its aria-hidden glyph treatment
    - `src/components/SaveStatusIndicator.vue` or any sibling in `src/components/` — for the file's
      `<script setup lang="ts">` + Tailwind house style
  </read_first>

  <behavior>
    - `items: []` renders the container, zero buttons, and no chrome classes.
    - Each item renders one native button with `type="button"`, the item's label as its accessible name,
      `:disabled` bound to `item.disabled`, `:title` bound to `item.title`.
    - A disabled item's handler is not invoked on click.
    - `testId` overrides the default `action-bar-item-{key}`.
    - `tone` selects one of four class sets: default, primary (filled indigo), destructive, present
      (outlined indigo).
    - `icon` renders the matching inline SVG or glyph before the label; `none`/absent renders no icon.
    - A `hint-{key}` slot, when the parent supplies one AND that key is present in `items`, renders
      after that item's button. No slot content renders nothing — never an empty span.
  </behavior>

  <action>
    Create `src/components/actionBarItems.ts` exporting: `type ActionBarTone = 'default' | 'primary' |
    'destructive' | 'present'`; `type ActionBarIcon = 'none' | 'ai-sparkle' | 'upload' | 'check' |
    'copy' | 'present' | 'spinner'`; and `interface ActionBarItem { key: string; label: string; onClick:
    () => void; disabled?: boolean; title?: string; tone?: ActionBarTone; icon?: ActionBarIcon; testId?:
    string }`. The `copy` icon member is an extension over the UI-SPEC's union, added so the existing
    `copy-pc` button does not lose its clipboard glyph; the `present` tone is the fourth tone the
    UI-SPEC §2 explicitly requires so Present and Save stay visually distinct.

    Create `src/components/ContextualActionBar.vue` with a single `items: ActionBarItem[]` prop and no
    internal state, no store access and no emits. Root element: a `div` with class `flex items-center
    gap-3` and `data-testid="contextual-action-bar"` — no border, no background, no padding, no margin,
    so an empty list contributes no visible box and no extra `v-if` gate is needed.

    Inside, `v-for` over `items` keyed on `item.key`, rendering a `button` with `type="button"`,
    `:disabled="item.disabled"`, `:title="item.title"`,
    `:data-testid="item.testId ?? \`action-bar-item-${item.key}\`"`, `@click="item.onClick"`, and the
    shared base class `print:hidden inline-flex items-center gap-1.5 rounded-md px-3 py-2 text-sm
    font-medium transition-colors border disabled:opacity-50 disabled:cursor-not-allowed`. Map `tone` to
    exactly these class sets: `primary` → `text-white bg-indigo-600 hover:bg-indigo-500
    border-transparent`; `present` → `bg-transparent border-indigo-400/60 text-indigo-300
    hover:bg-indigo-500/10`; `destructive` → `text-red-300 bg-transparent hover:bg-red-950/30
    border-red-800`; default/absent → `text-gray-200 bg-gray-800 hover:bg-gray-700 border-gray-700`.
    Prefer a plain lookup object over a nested ternary chain.

    Render the icon before the label. Lift the SVG path data VERBATIM from the source buttons so no
    relocated control changes appearance: `ai-sparkle` and `spinner`/`upload`/`check` from
    `ServiceEditorView.vue`'s suggest and export buttons, `copy` from its copy button. `present` renders
    the existing glyph treatment — a `span` carrying `aria-hidden="true"` and the right-pointing
    triangle entity — so the glyph stays out of the accessible name exactly as it is today. Every SVG
    gets `aria-hidden="true"`; the visible label is the accessible name.

    Render `<slot :name="\`hint-${item.key}\`" />` immediately after each item's button, inside the
    `v-for`. Do not wrap it in any element — the parent supplies its own span, which is how R071's note
    keeps its own testid and its router-link.

    Add a head comment recording three things: that this component owns no state and dispatches only
    handler references (matching `SlideActionMenu.vue`'s precedent); that the empty-list case needs no
    `v-if` because the container carries no chrome, unlike the save-status bar which does; and that
    `Save`'s padding normalises from `px-4 py-2` to the bar's declared `px-3 py-2` per the UI-SPEC's
    Spacing Scale, which is the ONE relocated control this phase restyles — `＋ Add slide` is the one it
    explicitly does not.
  </action>

  <verify>
    <automated>npx vitest run src/components/__tests__/ContextualActionBar.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `items: []` → `[data-testid="contextual-action-bar"]` exists, `findAll('button')` has length 0,
      and the container's class attribute contains none of `border`, `bg-`, `px-`, `py-`, `rounded`,
      `mb-`, `mt-` (assert the empty-box rule directly, not by eye).
    - Three items → three buttons, in array order, each `type="button"`, each testid
      `action-bar-item-{key}`; an item with `testId: 'copy-pc-btn'` renders that testid instead.
    - `disabled: true` → the button carries the `disabled` attribute and `trigger('click')` does not
      invoke the item's `onClick` spy.
    - `disabled` absent → `trigger('click')` invokes `onClick` exactly once.
    - `title` binds to the button's `title` attribute; absent `title` leaves the attribute undefined.
    - `tone: 'primary'` class contains `bg-indigo-600`; `tone: 'present'` class contains
      `border-indigo-400/60` and `text-indigo-300` and does NOT contain `bg-indigo-600`. Assert both, so
      the two can never collapse into one treatment.
    - `icon: 'present'` renders an element with `aria-hidden="true"` and the button's text content
      contains the label; every rendered `svg` carries `aria-hidden="true"`.
    - A `hint-export-pc` slot supplied while `items` contains no `export-pc` key renders nothing; the
      same slot with that key present renders the slot content after that button.
  </acceptance_criteria>

  <done>
    One shared bar exists, renders a declarative list, renders nothing but an invisible container when
    the list is empty, and keeps Present and Save visually distinct.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The pure per-tab item builder and the R068 data-level acceptance suite</name>
  <files>src/views/serviceEditorActionBar.ts, src/views/__tests__/serviceEditorActionBar.test.ts</files>

  <read_first>
    - `src/views/ServiceEditorView.vue` lines 96–246 — read each of the four buttons' EXACT `v-if`,
      `:disabled`, `:title` and label expressions; these are the gates being reproduced. Note
      specifically that the export button's gate is `authStore.hasPcCredentials` alone, that the copy
      button is its bare `v-else`, and that neither is nested inside any `canEditService` element.
    - `src/views/ServiceEditorView.vue` lines 1697–1700 (`isLocked`, `canEditService`), 2074–2082
      (`isDirty`, `hasSermonContext`)
    - `src/components/slides/SlidesTab.vue` lines 12–23 and 200–210 and 405–426 (`canPresent`,
      `presentStartIndex`, `onPresentClick`, and the Present button's disabled title text)
    - `36-UI-SPEC.md` § Phase-Specific Component Contracts §3 and § Copywriting Contract
    - `36-CONTEXT.md` § Specific Ideas (why this test must be data-level)
    - `src/components/actionBarItems.ts` (from Task 1)
  </read_first>

  <behavior>
    - `buildActionBarItems('roles', ctx)` is `[]` for every context.
    - `buildActionBarItems('slides', ctx)` is `['present']` when `canEditService` is false and
      `['present','save']` when it is true.
    - `buildActionBarItems('service-order', ctx)` with `canEditService` true is
      `['suggest-all-songs', 'export-pc', 'save']` when credentials exist and
      `['suggest-all-songs', 'copy-pc', 'save']` when they do not.
    - `buildActionBarItems('service-order', ctx)` with `canEditService` false is `['export-pc']` or
      `['copy-pc']` — one item, preserving today's ungated export/copy pair. See the flagged spec
      divergence in this plan's frontmatter.
    - Labels, titles and disabled conditions reproduce the source expressions exactly.
  </behavior>

  <action>
    Create `src/views/serviceEditorActionBar.ts` exporting `type ActionBarTab = 'service-order' | 'roles'
    | 'slides'` (structurally identical to `ServiceEditorView`'s `activeTab` union, which is
    order-independent and stays where it is), an `ActionBarContext` interface, and a pure
    `buildActionBarItems(tab: ActionBarTab, ctx: ActionBarContext): ActionBarItem[]`.

    `ActionBarContext` fields, each a plain value the view already holds: `canEditService`,
    `hasService`, `hasSermonContext`, `aiSuggestingAll`, `hasPcCredentials`, `isExporting`, `pcCopied`,
    `serviceStatus` (`'draft' | 'planned' | 'exported'`), `isDirty`, `isSaving`, `canPresent`; plus
    `handlers` holding `suggestAllSongs`, `onExportToPC`, `onCopyForPC`, `onSave`, `onPresent`. The
    module imports nothing from Vue, Pinia or the router — that is what makes it testable as data.

    Reproduce each control's live semantics exactly:

    `suggest-all-songs` — emitted only when `canEditService`. Label `Suggesting...` when
    `aiSuggestingAll`, else `Suggest All Songs`. `disabled: !hasSermonContext || aiSuggestingAll`.
    `title` is `Add a sermon topic or passage for AI suggestions` when `!hasSermonContext`, else
    undefined. `icon: 'ai-sparkle'`. `onClick: handlers.suggestAllSongs`.

    Export/copy — emitted regardless of `canEditService`, matching live source. When `hasPcCredentials`:
    key `export-pc`, `testId: 'export-pc-btn'`, label `Exporting...` when `isExporting`, `Exported` when
    `serviceStatus === 'exported'`, else `Export to PC`; `disabled: isExporting || serviceStatus !==
    'planned'`; `title` `Mark service as Planned to export` for draft, `Already exported to Planning
    Center` for exported, else undefined; icon `spinner` while exporting, `check` when exported, else
    `upload`; `onClick: handlers.onExportToPC`. Otherwise: key `copy-pc`, `testId: 'copy-pc-btn'`, label
    `Copied!` when `pcCopied` else `Copy for PC`; `disabled: !hasService`; icon `check` when `pcCopied`
    else `copy`; `onClick: handlers.onCopyForPC`. Keeping the two original testids is a deliberate
    choice: it preserves the existing R071/F5 assertions and avoids test churn that would obscure which
    edits are moved-control edits.

    `save` — emitted only when `canEditService`, on BOTH `service-order` and `slides`. Label `Saving...`
    when `isSaving` else `Save`. `disabled: !isDirty || isSaving`. `tone: 'primary'`.
    `onClick: handlers.onSave`.

    `present` — emitted on `slides` only, with no editor gate (it has none today). Label `Present`,
    `icon: 'present'`, `tone: 'present'`, `disabled: !canPresent`, `title` `Add songs or scripture to
    build a slideshow to present.` when `!canPresent` else undefined,
    `testId: 'action-bar-item-present'`, `onClick: handlers.onPresent`. It is pushed BEFORE `save` so
    the Slides row reads Present then Save, matching design 1a.

    `roles` returns a new empty array.

    Create `src/views/__tests__/serviceEditorActionBar.test.ts` as the R068 acceptance suite. Structure
    it around an explicit expectation TABLE — rows of (tab, canEditService, hasPcCredentials,
    serviceStatus) mapped to the expected key array — so a future change to any gate fails a named row
    rather than an incidental assertion. Include a comment above the table stating that the expected
    values were derived from the pre-phase markup at the file:line ranges named in `<read_first>`, and
    naming the export/copy divergence from UI-SPEC §3 explicitly.
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/serviceEditorActionBar.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - **Leak test (R068's headline criterion):** for the full cartesian product of the boolean context
      flags, `buildActionBarItems('slides', ctx)` and `buildActionBarItems('roles', ctx)` contain none
      of `suggest-all-songs`, `export-pc`, `copy-pc`. Assert over the generated product, not three
      hand-picked cases.
    - **Roles empty:** `buildActionBarItems('roles', ctx).length === 0` across that same product.
    - **Gating matrix:** every row of the expectation table matches exactly (`toEqual` on the key array,
      not `toContain`) — including the two `canEditService: false` Service Order rows that yield exactly
      one export-or-copy key, which is what proves no relocated control was silently narrowed.
    - **Adjacency:** for `slides` with `canEditService` true,
      `keys.indexOf('present') === keys.indexOf('save') - 1`.
    - **Ordering:** for `service-order` with `canEditService` true, the key array equals
      `['suggest-all-songs', 'export-pc', 'save']` (credentialed) / `['suggest-all-songs', 'copy-pc',
      'save']` (uncredentialed) — order asserted, not membership.
    - **Idempotency:** two successive calls with the same context object return equal key arrays; and
      `service-order` → `slides` → `service-order` returns a key array equal to the first call's.
    - **Concurrency:** `aiSuggestingAll: true` → the suggest item's `disabled` is `true` and its label is
      `Suggesting...`; `isExporting: true` → the export item's `disabled` is `true` and its icon is
      `spinner`.
    - **Handler identity:** each emitted item's `onClick` is reference-equal (`toBe`) to the handler
      passed in `ctx.handlers` — proving dispatch-only, no reimplementation.
    - **Disabled conditions:** `save` is disabled when `!isDirty`, when `isSaving`, and enabled only
      when dirty and not saving; `present` is disabled exactly when `!canPresent`.
  </acceptance_criteria>

  <done>
    "Which actions does tab X expose?" is a pure function call with a table-driven test behind it. The
    gating invariant is machine-checked before a single line of `ServiceEditorView.vue` changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-tab item list → rendered control | the list decides which write actions a user is offered |
| bar `onClick` → existing mutation handlers | dispatch crosses from presentation into write logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-02-01 | Elevation of Privilege | `buildActionBarItems` gate reproduction | high | mitigate | Table-driven matrix test asserts the emitted key set for every (tab x canEditService x hasPcCredentials x status) row equals the pre-phase rendered set, derived from named file:line ranges. A widened gate fails a named row. |
| T-36-02-02 | Tampering | `ContextualActionBar` dispatch | medium | mitigate | The bar owns no state and calls only handler references; handler identity is asserted with `toBe`. Every underlying handler keeps its own internal write guard, so the bar is never the only gate. |
| T-36-02-03 | Information Disclosure | the `hint-{key}` slot | low | accept | The slot renders parent-supplied markup only; this plan ships no hint content. R071's note and its gate arrive in `36-03` unchanged from `34-12`. |
| T-36-02-04 | Repudiation | n/a | low | accept | No audit, identity or logging surface is touched. |
| — | Supply chain | n/a | — | n/a | No package-manager install in this plan. No `T-36-SC` row claimed; no Package Legitimacy Audit required. |
</threat_model>

## Artifacts this phase produces

| Artifact | Kind | Detail |
|---|---|---|
| `src/components/actionBarItems.ts` | module | `ActionBarItem`, `ActionBarTone`, `ActionBarIcon` |
| `ActionBarItem` | type | `key, label, onClick, disabled?, title?, tone?, icon?, testId?` |
| `ActionBarTone` | type | `'default' \| 'primary' \| 'destructive' \| 'present'` |
| `ActionBarIcon` | type | `'none' \| 'ai-sparkle' \| 'upload' \| 'check' \| 'copy' \| 'present' \| 'spinner'` |
| `src/components/ContextualActionBar.vue` | component | prop `items`; no state, no emits |
| `contextual-action-bar` | testid | the bar's container |
| `action-bar-item-{key}` | testid | default per-item testid, overridable via `testId` |
| `hint-{key}` | slot | dynamic named slot rendered after item `{key}` |
| `src/views/serviceEditorActionBar.ts` | module | `ActionBarTab`, `ActionBarContext`, `buildActionBarItems` |
| `action-bar-item-present` | testid | the relocated Present button (new home for the Slides CTA) |
| `export-pc-btn` / `copy-pc-btn` | testid | preserved via `testId` override |

<verification>
- `npx vitest run src/components/__tests__/ContextualActionBar.test.ts src/views/__tests__/serviceEditorActionBar.test.ts` passes.
- No existing source file is modified by this plan — confirm with `git status --short` that only the
  five new files appear.
</verification>

<success_criteria>
- R068's "only relevant actions per tab" claim is provable against data.
- The gating invariant has a machine-checked expectation table.
- The bar renders nothing visible for an empty list.
</success_criteria>

<output>
Create `.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-02-SUMMARY.md` when done.
It MUST record the three flagged spec divergences from this plan's frontmatter `assumptions` — the
export/copy gate (and the resulting inaccuracy of UI-SPEC E3), the hint-slot substitution for
`hint?: string`, and the `copy` icon extension.
</output>
