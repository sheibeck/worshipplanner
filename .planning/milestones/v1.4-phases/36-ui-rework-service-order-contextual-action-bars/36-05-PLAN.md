---
phase: 36-ui-rework-service-order-contextual-action-bars
plan: 05
type: execute
wave: 4
depends_on: ["36-04"]
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R067]

must_haves:
  truths:
    - "The bottom-of-list add control is an always-visible dashed chip row offering exactly the five kinds `addSlot` already supports — Song, Scripture, Prayer, Message, Hymn — matching Turn 3's palette treatment."
    - "The palette has no open/closed state: every chip is directly clickable, and the dropdown's click-away backdrop and floating panel are deleted along with the ref that drove them."
    - "Each palette chip still calls the same `addSlot` with the same kind and vw-type arguments the dropdown item it replaces used, so slots created from the palette are byte-identical to slots created from the old menu."
    - "The palette does not render for a viewer or on a locked service — removed, not disabled, the pattern this file already uses for every other write control."
    - "R067 edge/idempotency: clicking a palette chip twice adds exactly two slots, and the palette does not need to be reopened between clicks because it has no closed state."
    - "PRESERVATION SWEEP: after the rebuild the Service Order tab still offers drag-reorder, the per-row section select, the per-row remove control, the scripture slot editor, the sermon-context inputs, the teams row, the lock banner and the save-status bar — each asserted by name, not by eye."
    - "The five section bands still enumerate 1:1 against `SERVICE_SECTIONS` and the empty-band placeholder copy is unchanged."
    - "The full app suite and the binding type gate both pass: `npm run type-check` (`vue-tsc --build`, which typechecks test files) reports zero errors, and the scoped app suite's failing-file set is exactly the two documented baseline files."
    - statement: "The palette chip labels do not truncate or overflow their dashed container at the narrowest supported viewport."
      verification: backstop
  artifacts:
    - "src/views/ServiceEditorView.vue — dashed palette row replaces the add-element dropdown; its open-state ref is deleted"
    - "src/views/__tests__/ServiceEditorView.test.ts — palette coverage and the Service Order preservation sweep"
  key_links:
    - "each palette chip → the unchanged `addSlot(kind, vwType)` with no third argument → today's inherit-from-last-slot behaviour, deliberately preserved for the bottom palette (per-band targeting is the band links' job, `36-04`)"
    - "deleting the open-state ref also removes the line in `addSlot` that reset it — the two must land in the same commit or the file will not compile"
  prohibitions:
    - "Do not complete the wireframe's palette to its nine drawn chips. Four of them have no `SlotKind` behind them; adding them is a data-model change requiring a new requirement, not a layout rebuild."
    - "Do not remove a Service Order capability because the wireframe does not draw it. The mockup is a layout reference, not an inventory of what the tab is allowed to do."
    - "Do not mark any deferred human-verification item as passed. Phase 34's open items 34.1 and 34.3-34.6 are not closed by this phase and must remain unchecked, per the standing autonomy grant's explicit limit."
  assumptions:
    - "★ RECORDED RESOLUTION OF ROADMAP CRITERION 4: the criterion reads '\"Add slide\" lives in the contextual action bar.' Per 36-UI-SPEC § Finding 2's discretionary call, `＋ Add slide` stays physically rendered in `SlideGrid`'s own header row — it is group-scoped, not tab-scoped, and relocating it would need four-hop event plumbing for a control whose enabled state depends on selection the page-level bar does not track. The criterion is satisfied in INTERACTION PATTERN (a bordered ghost button performing a contextual, scope-local action) and NOT in visual unification or shared-component architecture. The spec recommends against full relocation on cost/benefit grounds but does not forbid it. Surfaced so a verifier judges the criterion against the recorded resolution rather than its literal text."
    - "★ RECORDED SUPERSESSION: the same criterion's '\"Add music to this group\" lives in the action bar' clause is superseded and already corrected, with dated evidence, in both REQUIREMENTS.md (R053) and ROADMAP.md. Group music stays in the `34-11` merged panel."
    - "UNRESOLVED (stays unresolved, per 36-UI-SPEC § Finding 1): `Print`, `Share`, `Undo`, `Mark as Planned` and `Delete service` also render on every tab today. They are outside this phase's named scope and were deliberately left where they are. Flagged, not fixed — a checker should not read R068 as covering them."
---

<objective>
Replace the bottom-of-list add-element dropdown with Turn 3's dashed chip palette, then sweep the whole
Service Order tab to prove the rebuild removed nothing, and close the phase with the binding type gate
and the full app suite.

Purpose: ROADMAP criterion 1's remaining half, plus the evidence that "rebuilt" did not mean "quietly
reduced".
Output: one replaced control, one deleted ref, a preservation suite, and a green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-UI-SPEC.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-CONTEXT.md
@.planning/PENDING-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The dashed palette row replaces the bottom add-element dropdown</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>

  <read_first>
    - `src/views/ServiceEditorView.vue` lines 1194–1231 — the add-element trigger button, the click-away
      backdrop, the floating panel and its five menu entries, read for the exact `addSlot` arguments each
      entry passes
    - `src/views/ServiceEditorView.vue` line 1528 (the menu's open-state ref) and the line inside
      `addSlot` that resets it
    - `src/views/__tests__/ServiceEditorView.test.ts` around lines 1030–1045, 2420–2450 and 3670–3690 —
      every existing assertion that finds the add control by its label text
    - `36-UI-SPEC.md` § Phase-Specific Component Contracts §8, § Copywriting Contract (the palette row
      prompt and chip labels), § Color (the accent-reserved list), § Spacing Scale
  </read_first>

  <behavior>
    - Editor, unlocked: a dashed-bordered row renders below the last band, with the prompt text and five
      always-visible chips.
    - Clicking a chip adds a slot of that kind, inheriting the last slot's section exactly as the menu
      entry it replaces did.
    - No backdrop, no floating panel, no toggle: the row has one state.
    - Viewer or locked: the whole row is absent.
  </behavior>

  <action>
    Replace the `v-if="canEditService"` wrapper that currently holds the add-element trigger, the
    click-away backdrop and the floating dropdown panel with a single dashed row. The new wrapper keeps
    `v-if="canEditService"`, drops the `relative` positioning it only needed for the popover, and carries
    classes `mt-2 flex flex-wrap items-center gap-2 rounded-lg border border-dashed border-gray-700 px-4 py-3`
    and `data-testid="add-to-service-palette"`.

    Inside it: a `span` with classes `text-[11px] text-indigo-400` reading `＋ Add to the service`, then
    five `button type="button"` chips with classes
    `rounded-md border border-gray-700 px-2 py-1 text-[11px] font-medium text-gray-300 hover:bg-gray-800 transition-colors`
    and testids `palette-add-song`, `palette-add-scripture`, `palette-add-prayer`, `palette-add-message`,
    `palette-add-hymn`, labelled `Song`, `Scripture`, `Prayer`, `Message`, `Hymn`. Each chip's `@click`
    calls `addSlot` with the SAME arguments the dropdown entry it replaces passed — read them from the
    source rather than retyping from memory; the Song entry passes a vw-type argument the other four do
    not, and none passes a third argument, so the bottom palette keeps today's inherit-from-last-slot
    behaviour.

    Note the chip label wording: the scripture entry's menu text was longer than the chip label the
    Copywriting Contract specifies. The chip reads `Scripture`. This is a label change on a restyled
    control, not a behaviour change — the `SlotKind` it creates is identical.

    Delete the ref that held the dropdown's open state, and delete the line inside `addSlot` that reset
    it. Both deletions must land together or the file will not compile. Keep the head comment's
    explanation of why this control is removed rather than disabled when locked, updating its wording to
    describe the palette instead of a menu.
    <!-- planner-discipline-allow: showAddMenu -->

    Update the three groups of existing tests that locate the add control by its old label. The two
    behavioural ones — that no PowerPoint/image import entry is offered, and that the five non-import
    kinds are still offered — keep their intent and move to the palette testids. The locked-service
    assertion that the add control is absent moves to the palette testid. **Comment each as a
    moved-and-restyled-control edit**: `addSlot`'s logic and every argument it receives are unchanged.
    <!-- planner-discipline-allow: Add Element -->
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Editor + unlocked: `[data-testid="add-to-service-palette"]` exists and contains exactly five
      buttons, with the five `palette-add-*` testids and the labels `Song`, `Scripture`, `Prayer`,
      `Message`, `Hymn` in that order.
    - No button anywhere in the view has the old add-control label text, and
      `grep -v '^\s*//' src/views/ServiceEditorView.vue | grep -c 'showAddMenu'` returns 0 — the ref and
      both of its references are gone.
    - Clicking `palette-add-song` appends a slot whose `kind` is `SONG` with the same vw-type the old
      menu entry produced; assert the created slot deep-equals what the pre-phase menu test asserted.
    - Clicking each of the other four chips appends a slot of the matching kind.
    - Clicking `palette-add-prayer` twice, with no intervening open/close, appends exactly two slots.
    - There is no element acting as a click-away backdrop and no floating panel: assert the palette's
      chips are queryable and clickable immediately after mount, with no prior interaction.
    - Viewer, and editor on a `planned` service: `[data-testid="add-to-service-palette"]` does not exist.
    - No import/PowerPoint entry exists in the palette — the pre-phase assertion's intent, re-expressed
      against the new testids.
  </acceptance_criteria>

  <done>
    The add control is a single-state chip row. `addSlot` is untouched by this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service Order preservation sweep and the phase gate</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>

  <read_first>
    - `src/views/ServiceEditorView.vue` lines 700–760 (teams row), 750–805 (sermon context), 805–860
      (bands and the empty-band placeholder), 857–872 (drag handle), 1160–1192 (section select and remove
      control), 291–299 (the save-status bar), 300–320 (the lock banner)
    - `36-CONTEXT.md` § "Service Order rebuild (R067)" — the explicit list of capabilities that must keep
      working
    - `36-UI-SPEC.md` § Empty / Edge States rows E6 and E7
    - `CLAUDE.md` — the type-check and test-suite rules, in full. `npm run type-check` runs
      `vue-tsc --build` and is the ONLY sufficient type gate; the `-p tsconfig.app.json` form silently
      skips test files and has already let five errors survive two phases. Bare `npx vitest run src/`
      substring-matches a sibling project's test file and fails on a version mismatch — use
      `npx vitest run --dir src`.
    - `.planning/PENDING-VERIFICATION.md` — the five open Phase 34 items this phase does not close
  </read_first>

  <action>
    Add one `describe` block to `ServiceEditorView.test.ts` named for the R067 preservation sweep. It
    asserts, for an editor on an unlocked service with a fully sectioned fixture, that every capability
    `36-CONTEXT.md` names as load-bearing is still present and still functional — not merely rendered.
    Prefer behavioural assertions over existence checks where a behaviour is cheap to drive: change a
    row's section through the select and assert the slot moved band; click a row's remove control and
    assert the slot count dropped.

    Add a second `describe` block asserting the locked and viewer variants of the rebuilt tab: band
    labels, dividers and counts stay visible while every write affordance introduced or moved by this
    phase is absent rather than disabled.

    Then run the phase gate and record its output in the summary: `npm run type-check` and
    `npx vitest run --dir src`. Do not substitute the narrower `-p tsconfig.app.json` form for the type
    gate under any circumstance. Compare the failing-file set against the two documented baseline files
    and state explicitly whether it matches; a third failing file is a regression this phase owns.

    Do not run any deploy, emulator-provisioning or infrastructure command. Do not modify
    `.planning/PENDING-VERIFICATION.md` to mark any Phase 34 item verified.
  </action>

  <verify>
    <automated>npm run type-check &amp;&amp; npx vitest run --dir src</automated>
  </verify>

  <acceptance_criteria>
    - Drag-reorder: the existing Sortable tests pass unchanged, and `.slot-item` elements still exist for
      an editor with `draggable: '.slot-item'` still the configured selector.
    - Per-row section select: `[data-testid="section-select"]` exists for each row for an editor;
      changing one moves the slot into the chosen band and the array is re-ordered section-major.
    - Per-row remove: the remove control exists for each row for an editor and removing one decreases
      `localService.slots.length` by exactly 1.
    - Teams row, sermon topic input and sermon passage input all still render for an editor and still
      write to `localService`.
    - The scripture slot's editor still renders for a scripture slot.
    - The lock banner renders for a locked editor; `[data-testid="service-save-status-bar"]` renders for
      an unlocked editor with no chrome classes at idle (34-10, re-asserted at phase close).
    - Five section headers, five band labels, five counts; the empty-band placeholder still renders its
      pre-phase copy verbatim for both the editor and the locked variants.
    - Locked and viewer: none of `[data-testid="add-to-service-palette"]`,
      `[data-testid^="section-add-item-"]`, `[data-testid^="section-add-menu-"]` renders, and no `Save`
      button renders — while the band labels and counts still do.
    - `npm run type-check` exits 0 with zero errors.
    - `npx vitest run --dir src` completes with the failing-FILE set equal to exactly
      `src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts`. Any third failing file is
      recorded as a regression, not waved through.
    - `.planning/PENDING-VERIFICATION.md` is unmodified — confirm with `git status --short`.
  </acceptance_criteria>

  <done>
    The rebuilt Service Order tab provably lost nothing, and the phase's type and test gates are green
    against the documented baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| palette chips → `addSlot` → `localService.slots` | the tab's primary create path |
| locked / viewer session → every write affordance on the tab | the sweep is what proves the lock still holds after a layout rebuild |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-05-01 | Elevation of Privilege | the palette row | high | mitigate | The wrapper keeps `v-if="canEditService"` and `addSlot`'s own leading guard is untouched. Viewer and locked fixtures assert absence of the whole row, not disablement. |
| T-36-05-02 | Elevation of Privilege | the rebuilt tab as a whole | critical | mitigate | The dedicated locked/viewer sweep asserts every write affordance introduced or moved by Phase 36 is absent for a non-editor, catching a gate lost anywhere across the four preceding plans rather than only in the one that introduced it. |
| T-36-05-03 | Tampering | deleting the dropdown's open-state ref | low | mitigate | Both the declaration and the reset line are removed together; a grep assertion and a compiling type gate prove no dangling reference. |
| T-36-05-04 | Repudiation | deferred human-verification records | medium | mitigate | The plan forbids editing `PENDING-VERIFICATION.md` and asserts the file is unmodified. The standing autonomy grant permits deferring a check, never recording a deferred check as passed. |
| — | Supply chain | n/a | — | n/a | No package-manager install anywhere in Phase 36. No `T-36-SC` row claimed; no Package Legitimacy Audit required. |
| — | Deployment | n/a | — | n/a | Nothing in this phase deploys, containerises or provisions. No `firebase deploy`, no `gcloud`, no emulator provisioning. |
</threat_model>

## Artifacts this phase produces

| Artifact | Kind | Detail |
|---|---|---|
| `add-to-service-palette` | testid | the dashed chip row replacing the add-element dropdown |
| `palette-add-song` | testid | chip creating a `SONG` slot |
| `palette-add-scripture` | testid | chip creating a `SCRIPTURE` slot |
| `palette-add-prayer` | testid | chip creating a `PRAYER` slot |
| `palette-add-message` | testid | chip creating a `MESSAGE` slot |
| `palette-add-hymn` | testid | chip creating a `HYMN` slot |
| (removed) the add-element trigger, backdrop, floating panel and open-state ref | markup + state | deleted from `ServiceEditorView.vue` |

<verification>
- `npm run type-check` — zero errors. This is the binding gate; `vue-tsc --noEmit -p tsconfig.app.json`
  is not an acceptable substitute.
- `npx vitest run --dir src` — failing-file set equals the two documented baseline files.
- `git status --short` shows no change to `.planning/PENDING-VERIFICATION.md`.
</verification>

<success_criteria>
- ROADMAP criterion 1 is true: the Service Order tab matches Turn 3's structure — bands with labels,
  counts and per-band adds, and a dashed palette in place of the dropdown.
- Every capability `36-CONTEXT.md` named as load-bearing is asserted still working.
- The phase's type and test gates are green, and nothing was deployed or self-approved.
</success_criteria>

<output>
Create `.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-05-SUMMARY.md` when done.
It MUST record: the exact `npm run type-check` and `npx vitest run --dir src` results including the
failing-file set; the recorded resolution of ROADMAP criterion 4 from this plan's frontmatter
`assumptions` (interaction-pattern satisfaction for `＋ Add slide`, supersession for group music); the
three items that remain deliberately unresolved across the phase (the Roles-tab action set, the
row-level kebab and `Change` link, and R069's `unclassified` edge row); and an explicit statement that
Phase 34's open verification items were not touched.
</output>
