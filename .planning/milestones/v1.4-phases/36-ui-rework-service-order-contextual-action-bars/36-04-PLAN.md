---
phase: 36-ui-rework-service-order-contextual-action-bars
plan: 04
type: execute
wave: 3
depends_on: ["36-03"]
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R067]

must_haves:
  truths:
    - "Every one of the five section bands renders a header carrying its label, a divider, a slide-count caption and — for an editor — a `＋ Add item` affordance, matching Turn 3's band structure (UI-SPEC Finding 4 item 5)."
    - "The five bands are enumerated 1:1 from the existing `SERVICE_SECTIONS` constant; no sixth section was invented and the trailing ungrouped bucket still renders no header."
    - "`addSlot` gains ONE optional trailing `targetSection` parameter and nothing else changes: every existing call site omits it and behaves byte-identically, still inheriting the last slot's section."
    - "Clicking a specific band's `＋ Add item` creates the new slot IN that band, not appended after whichever slot happens to be last in the service."
    - "R067 edge/empty: an empty band still renders its header, its label, its divider, a zero slide count and its `＋ Add item` link, and still renders the existing empty-band placeholder copy unchanged."
    - "R067 edge/ordering: bands render in `SERVICE_SECTIONS` order, and a slot added through any band is still ordered by the existing `orderSlotsBySection` + `reindexSlots` pipeline — the add path did not acquire its own ordering rule."
    - "R067 edge/adjacency: adding the count and the `＋ Add item` link did not displace or remove the band's existing label or divider, and the header remains a structural SIBLING of the section list container so it stays outside that container's Sortable instance."
    - "R067 edge/idempotency: clicking a band's `＋ Add item` chip twice adds exactly two slots, and re-rendering a band does not duplicate its header, its count or its link."
    - "R067 edge/concurrency: two successive adds targeting two different bands each land in their own band — the second does not inherit the first's section."
    - "Locked or viewer: `＋ Add item` does not render, while the band label, divider and count stay visible — 'removed, not disabled', the pattern every other write control in this file already follows."
    - "R067 long-text: every new label is a fixed enum string; no user-supplied text reaches the band header or the add chips."
    - statement: "The `＋ Add item` label does not truncate or wrap at the narrowest band width the existing layout supports."
      verification: backstop
    - statement: "Adding via one band's link while the service's last slot lives in a different band places the new slot in the clicked band — asserted with `WORSHIP` clicked and the last slot in `SENDING`."
      verification: backstop
  artifacts:
    - "src/views/ServiceEditorView.vue — `addSlot(kind, vwType?, targetSection?)`, `sectionSlideCount()`, `openSectionAddKey`, band-header label/count/add-link, per-band chip row"
    - "src/views/__tests__/ServiceEditorView.test.ts — per-band add-routing and band-header coverage"
  key_links:
    - "`＋ Add item` → `addSlot(kind, vwType, group.key)` → `createSlot(kind, vwType, section)` — the explicit `targetSection` bypasses the inherit-from-last-slot fallback entirely, which is what makes an empty band work identically to a populated one"
    - "`sectionSlideCount` reads `assembledSlideshow` filtered by `slotIndex`, the SAME derivation `SlidePlanRail` already uses for its per-row count — not `group.slides.length`, which reads zero for an unmaterialized group"
  prohibitions:
    - "Do not silently drop a Service Order capability while rebuilding its layout. The drag handles, the per-row section select, the remove control, the scripture editor, the empty-band placeholder copy, the lock banner and the save-status bar must all still be there and still work."
    - "Do not change any existing `addSlot` call site's behaviour. The new parameter is additive; a caller that omits it must produce a byte-identical result to today."
    - "Do not implement the row-level kebab or the song `Change` link. Both appear in the wireframe with no defined behaviour and no code equivalent; inventing a menu from a mockup glyph is exactly the drift this contract exists to catch."
  assumptions:
    - "UNRESOLVED (stays unresolved, per 36-UI-SPEC § UI Considerations): the wireframe's row-level `⋯` kebab and the assigned-song `Change` link have no drawn behaviour and no current-code equivalent. Explicitly NOT implemented. A checker must not read their absence as an omission."
    - "★ FLAGGED SPEC CORRECTION: UI-SPEC §9's illustrative markup gives the band slide-count caption `text-[10.5px]`, a FIFTH font size, while the same document's Typography section declares exactly four sizes (11px, 11.5px/12px, 14px, 22px). Implemented as `text-[11px]` — a declared size — rather than shipping a value the spec's own typography contract forbids."
    - "★ FLAGGED DISCRETIONARY CALL: the band slide-count caption's exact copy is absent from the Copywriting Contract. Implemented as `{n} slides` (`1 slide` singular), matching the plan rail's existing per-row convention rather than inventing a new phrasing."
    - "★ FLAGGED DISCRETIONARY CALL: UI-SPEC §9 leaves popover-vs-inline for `＋ Add item` to the planner. Implemented INLINE — clicking a band's link toggles a chip row directly beneath that band's header. No popover, no click-away backdrop, no z-index; strictly simpler than the dropdown it will replace in `36-05`."
    - "★ FLAGGED SPEC-DIRECTED RESTYLE: the band label changes from `text-xs font-semibold` (12px/600) to `text-[11px]` with no weight class (11px/400) and `tracking-[.14em]`, and the divider changes from a border rule to the gradient span, per UI-SPEC § Typography and §9. Recorded because it restyles an existing element rather than only adding to it."
---

<objective>
Rebuild the Service Order tab's section bands against Turn 3: each band gains a slide count and its own
`＋ Add item` affordance, and `addSlot` gains the one small additive parameter that makes a per-band add
land in the band it was clicked from.

Purpose: ROADMAP criterion 1, the structural half.
Output: one additive function parameter, one new pure helper, one new UI state ref, and a rebuilt band
header. The bottom-of-page palette is `36-05`'s.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-UI-SPEC.md
@.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-CONTEXT.md
@src/types/service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: addSlot gains an optional targetSection, and a pure per-band slide-count helper lands</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>

  <read_first>
    - `src/views/ServiceEditorView.vue` lines 2596–2615 (`addSlot` in full, including its head comment
      about inheriting the last slot's section), lines 1774–1790 (`slotSectionGroups` and the shape of
      each group's `entries`), lines 1846–1856 (`assembledSlideshow` from `useSlideshowAssembly`)
    - `src/types/service.ts` lines 1–30 — `SlotKind`, `ServiceSection`, `SERVICE_SECTIONS`,
      `SERVICE_SECTION_LABELS` (the exact literal values are `pre-service`, `worship`, `message`,
      `sending`, `post-service`)
    - `src/components/slides/SlidePlanRail.vue` lines 125–146 — the per-row count derivation and the
      comment explaining why it filters `assembledSlideshow` by `slotIndex` rather than reading
      `group.slides.length`
    - `36-UI-SPEC.md` § Phase-Specific Component Contracts §7 and §9, and § Empty / Edge States row E5
  </read_first>

  <behavior>
    - `addSlot(kind, vwType)` behaves exactly as today.
    - `addSlot(kind, vwType, 'worship')` places the new slot in the worship band regardless of where the
      current last slot sits.
    - `addSlot(kind, vwType, 'worship')` on a service whose worship band is empty places it as that
      band's only entry.
    - `addSlot` still refuses to do anything when `canEditService` is false.
    - `sectionSlideCount` returns the total number of assembled slides produced by the slots in a band,
      `0` for an empty band.
  </behavior>

  <action>
    Change `addSlot`'s signature to take a third optional parameter `targetSection?: ServiceSection`.
    The only line that changes in the body is the one computing the section handed to `createSlot`: it
    becomes `targetSection` when supplied, falling back to the current expression that reads the last
    slot's section. Leave the `canEditService` guard, the null check on `localService`, the push, and the
    `reindexSlots(orderSlotsBySection(...))` call exactly as they are. Import `ServiceSection` if the
    file does not already have it in scope.

    Update `addSlot`'s head comment: keep the existing explanation of the inherit-from-last-slot
    fallback and add that an explicit `targetSection` bypasses it, which is what makes a per-band add
    land in an empty band correctly.

    Add a pure helper `sectionSlideCount(entries)` taking a band's `entries` array — the same
    `{ slot, index }[]` shape `slotSectionGroups` already produces — and returning the number of
    `assembledSlideshow` items whose `slotIndex` matches one of those entries' `index` values. Build a
    `Set` of the band's indices and filter once; do not call `filter` per entry. Add a comment recording
    that this deliberately mirrors `SlidePlanRail`'s derivation and deliberately does NOT read a slide
    group's `slides.length`, because an unmaterialized group reads zero while the grid shows a full
    group. Note in the same comment that the helper's signature differs from UI-SPEC §9's illustrative
    `sectionSlideCount(group.key)` — taking the entries avoids re-deriving the section-to-slots mapping
    `slotSectionGroups` already computed, with identical output.

    Add `const openSectionAddKey = ref<ServiceSection | null>(null)` for Task 2's inline chip row, and a
    `toggleSectionAdd(key)` function that sets it to `key` when closed and back to `null` when the same
    key is clicked again. It is UI state only and performs no writes.
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - With the service's last slot in `sending`, adding through the view with an explicit `worship`
      target produces a new slot whose `section` is `worship`; adding with no target produces one whose
      `section` is `sending`. Both asserted on the resulting `localService.slots`.
    - Targeting a band that currently has zero entries produces a slot in that band and leaves every
      other band's membership unchanged.
    - With `canEditService` false (locked service), the targeted add produces no new slot.
    - The resulting `slots` array is still section-major ordered and re-indexed — assert that
      `slots.map(s => s.section)` is grouped in `SERVICE_SECTIONS` order and that `position` values are
      contiguous, exactly as the existing add tests already assert for the untargeted path.
    - Every pre-existing `addSlot` test in `ServiceEditorView.test.ts` passes unchanged, with no edited
      expectations. An edit here would mean the additive parameter was not additive.
    - `sectionSlideCount` returns `0` for an empty band and the correct total for a band whose slots
      contribute assembled slides.
  </acceptance_criteria>

  <done>
    Per-band adding is possible and provably does not change any existing caller's behaviour.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Section-band headers gain the label treatment, the slide count and the ＋ Add item chip row</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>

  <read_first>
    - `src/views/ServiceEditorView.vue` lines 805–856 — the band loop, the existing `section-header`
      div, the section list container with its `ref` callback and `data-section` attribute, and the
      empty-band placeholder with its locked-variant copy
    - `src/views/ServiceEditorView.vue` lines 2000–2030 — the Sortable lifecycle watcher and its
      `draggable: '.slot-item'` option, read to confirm the header and the new chip row stay outside the
      draggable set
    - `36-UI-SPEC.md` § Phase-Specific Component Contracts §9, § Typography, § Color (the accent-reserved
      list), § Spacing Scale (the 4-multiple rule and its single declared exception), § Copywriting
      Contract, § Empty / Edge States rows E5 and E6
  </read_first>

  <behavior>
    - Each of the five bands shows: label, divider, `{n} slides`, and (editor only) `＋ Add item`.
    - Clicking `＋ Add item` opens a chip row directly beneath that band's header with the five
      addable kinds; clicking the same link again closes it; opening another band's closes the first.
    - Clicking a chip adds that kind into that band and closes the row.
    - The ungrouped bucket renders no header at all, therefore no count and no add link.
    - Locked or viewer: the label, divider and count remain; the add link and the chip row do not render.
  </behavior>

  <action>
    Rebuild the contents of the existing `section-header` div, keeping the div itself, its
    `v-if="group.label"` gate, its `section-header` class, its `data-testid` and its
    `flex items-center gap-2 pt-3 pb-1 first:pt-0` layout classes. Inside it:

    The label span's classes become `text-[11px] uppercase tracking-[.14em] text-indigo-300/80` — an
    11px, 400-weight, wide-tracked treatment per the UI-SPEC's Typography row for this element. The
    previous `text-xs font-semibold ... tracking-wider` is replaced, not supplemented.

    The divider span becomes `h-px flex-1 bg-gradient-to-r from-gray-800 to-transparent`, replacing the
    `flex-1 border-t border-gray-800` rule. It stays decorative with no text content and therefore needs
    no `aria-hidden`.

    Add a count span with classes `text-[11px] text-gray-500` rendering `sectionSlideCount(group.entries)`
    followed by ` slide` or ` slides` (singular only at exactly 1), with
    `:data-testid="\`section-slide-count-${group.key}\`"`.

    Add the add link as a real `button` with `type="button"`, `v-if="canEditService"`, classes
    `text-[11.5px] font-medium text-indigo-400 hover:text-indigo-300`, label `＋ Add item`,
    `:data-testid="\`section-add-item-${group.key}\`"` and `@click="toggleSectionAdd(group.key)"`. Its
    visible text is its accessible name; add no `aria-label`.

    Immediately AFTER the header div and BEFORE the section list container, add a chip row rendered when
    `canEditService && openSectionAddKey === group.key`, with
    `:data-testid="\`section-add-menu-${group.key}\`"` and classes
    `flex flex-wrap items-center gap-2 rounded-lg border border-dashed border-gray-700 px-4 py-3`. It
    holds five `button type="button"` chips with classes
    `rounded-md border border-gray-700 px-2 py-1 text-[11px] font-medium text-gray-300 hover:bg-gray-800 transition-colors`
    and testids `section-add-{kind}-{group.key}`, labelled `Song`, `Scripture`, `Prayer`, `Message`,
    `Hymn`, calling `addSlot` with `'SONG'` plus the vw-type argument the existing bottom dropdown
    already passes for Song, and with `'SCRIPTURE'`, `'PRAYER'`, `'MESSAGE'`, `'HYMN'` and no vw-type
    for the rest — each with `group.key` as the third argument. Each chip's click also clears
    `openSectionAddKey`.

    Do NOT put the chip row inside the header div (it would break the header's single flex row) and do
    NOT put it inside the section list container (it would enter that container's Sortable membership).
    A sibling between the two is the only correct position, and the header's own comment already
    explains why sibling-not-member matters here.

    Leave the section list container, its `ref` callback, its `data-section` attribute, its `role`/
    `aria-label`, the drag-over `:class`, the empty-band placeholder and all of its copy exactly as they
    are. Leave every slot row untouched — no kebab, no `Change` link, no change to the drag handle, the
    section select or the remove control.

    Do not narrow the chip set beyond the five kinds `addSlot` already supports, and do not widen it to
    the wireframe's nine — the four extra chips the mockup draws have no `SlotKind` behind them, which is
    a data-model change and out of scope.
  </action>

  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - For an editor on an unlocked service, all five `[data-testid^="section-header-"]` elements exist,
      exactly five of them, and each contains its band's label text, a
      `[data-testid="section-slide-count-{key}"]` and a `[data-testid="section-add-item-{key}"]`.
    - `wrapper.findAll('[data-testid^="section-header-"]')` has length 5 even with a legacy ungrouped
      slot present — the ungrouped bucket renders no header.
    - Clicking `section-add-item-worship` renders `section-add-menu-worship` with exactly five chips;
      clicking the same link again removes it; clicking `section-add-item-sending` while worship's row is
      open leaves exactly one chip row rendered.
    - **Per-band routing (the backstop's concrete case):** with the service's last slot in the sending
      band, clicking `section-add-item-worship` then the `Prayer` chip appends a slot whose `section` is
      `worship`, and the sending band's membership is unchanged.
    - Clicking a chip twice (reopening the row between clicks) adds exactly two slots.
    - Two successive targeted adds into two different bands each land in their own band.
    - Empty band: its header, label, divider and a count reading `0 slides` render, its
      `section-add-item-{key}` renders for an editor, and the existing
      `[data-testid="section-empty-{key}"]` placeholder still renders with its current copy verbatim.
    - Locked service and viewer: no `[data-testid^="section-add-item-"]` and no
      `[data-testid^="section-add-menu-"]` render, while all five section headers, labels and counts
      still do.
    - The count reads `1 slide` at exactly one and `{n} slides` otherwise.
    - Drag-reorder still works: the existing Sortable tests pass unchanged, and
      `wrapper.findAll('.slot-item')` is unchanged in count by the header rebuild.
  </acceptance_criteria>

  <done>
    Each band is a labelled, counted, individually-addable region. Nothing inside a band's list changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-band add control → `addSlot` → `localService.slots` | a new entry point into an existing service mutation |
| band UI state (`openSectionAddKey`) → rendered write controls | decides whether write chips are offered |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-04-01 | Elevation of Privilege | per-band `＋ Add item` and its chips | high | mitigate | Both the link and the chip row carry `v-if="canEditService"`, the same gate the bottom-of-page add control uses, and `addSlot`'s own leading `if (!canEditService.value) return` is retained as an independent guard. Locked and viewer fixtures assert absence, not merely disablement. |
| T-36-04-02 | Tampering | the new `targetSection` parameter | medium | mitigate | Typed as `ServiceSection`, supplied only from `group.key` values derived from the `SERVICE_SECTIONS` constant, and normalised by the unchanged `orderSlotsBySection` + `reindexSlots` pipeline. Existing add tests pass unchanged, proving no caller's behaviour moved. |
| T-36-04-03 | Denial of Service | Sortable membership of new sibling elements | medium | mitigate | The chip row is a sibling of the section list container, never a child, so it cannot enter the `.slot-item` draggable set or shift index arithmetic. Asserted via unchanged `.slot-item` counts and passing reorder tests. |
| T-36-04-04 | Information Disclosure | band slide counts | low | accept | Counts are derived from data already rendered on the page for the same user; no new data source or permission scope is introduced. |
| — | Supply chain | n/a | — | n/a | No package-manager install in this plan. No `T-36-SC` row claimed. |
</threat_model>

## Artifacts this phase produces

| Artifact | Kind | Detail |
|---|---|---|
| `addSlot(kind, vwType?, targetSection?)` | signature | one additive optional trailing parameter |
| `sectionSlideCount(entries)` | function | pure; counts assembled slides for a band |
| `openSectionAddKey` | ref | `ServiceSection \| null`; which band's chip row is open |
| `toggleSectionAdd(key)` | function | UI state only, no writes |
| `section-slide-count-{key}` | testid | per-band slide count caption |
| `section-add-item-{key}` | testid | per-band `＋ Add item` button |
| `section-add-menu-{key}` | testid | per-band inline chip row |
| `section-add-{kind}-{key}` | testid | the five per-band kind chips |
| `section-header-{key}` | testid | unchanged testid, rebuilt contents |

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` passes.
- No pre-existing `addSlot` expectation was edited — confirm by reviewing the diff of the test file for
  changed (as opposed to added) assertions in the add-slot describe blocks.
</verification>

<success_criteria>
- Five labelled, counted bands each with their own add affordance.
- A per-band add lands in that band, including when the band is empty.
- Every existing Service Order capability inside a band is untouched.
</success_criteria>

<output>
Create `.planning/phases/36-ui-rework-service-order-contextual-action-bars/36-04-SUMMARY.md` when done.
It MUST record the four flagged items from this plan's frontmatter `assumptions` — the `text-[10.5px]`
typography correction, the slide-count copy call, the inline-versus-popover call, and the band-label
restyle — and restate that the row-level kebab and the song `Change` link remain deliberately
unimplemented.
</output>
