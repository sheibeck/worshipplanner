---
phase: 139-rehearsal-report-times
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/rehearsalTimes.ts
  - src/utils/__tests__/rehearsalTimes.test.ts
  - src/types/service.ts
  - src/types/organization.ts
  - src/stores/services.ts
  - src/utils/rehearseAccess.ts
  - src/stores/__tests__/services.rehearsalDefaults.test.ts
  - src/stores/__tests__/services.rehearsalProjection.test.ts
autonomous: true
requirements: [R429, R430, R431, R432, R433]

must_haves:
  truths:
    - "A service created after org defaults are set carries its OWN copied rehearsals[] (time-filled, date blank) and reportTime; a later org-default edit never mutates it."
    - "sortRehearsals returns rehearsals in chronological order (date then time) regardless of stored array order; undated rehearsals (date === '') sort last."
    - "formatWallClockTime renders an 'HH:mm' string as 12-hour wall-clock text identically regardless of the runner's system timezone."
    - "Both public projections (buildServiceSnapshot AND buildRehearseAccess) carry rehearsals and reportTime; undated rehearsals are filtered out of both."
  artifacts:
    - src/utils/rehearsalTimes.ts
    - src/utils/__tests__/rehearsalTimes.test.ts
    - src/stores/__tests__/services.rehearsalDefaults.test.ts
    - src/stores/__tests__/services.rehearsalProjection.test.ts
  key_links:
    - "createService reads authStore.settings.rehearsalTimeDefaults/reportTimeDefault once and writes copies into BOTH the addDoc payload AND the parallel `created: Service` object (services.ts:458-491)."
    - "buildServiceSnapshot (services.ts) and buildRehearseAccess (rehearseAccess.ts) BOTH import sortRehearsals from src/utils/rehearsalTimes.ts and conditional-spread the new fields."
---

<objective>
Land the entire data + logic foundation for rehearsal/report times so the display and editor UI (Plan 139-02) can render fields the types actually have. This is the load-bearing wave: additive types, one shared time utility, org-level defaults, copy-at-create pre-fill, and BOTH hand-maintained public projections — edited together so no volunteer/share surface silently shows nothing.

Purpose: Give services real, timezone-safe wall-clock rehearsal/report times (R429–R433) using plain-string storage, one shared chronological sort, and copy-not-live-bind org defaults — the three things STATE.md flags this phase must get right.
Output: `src/utils/rehearsalTimes.ts` (`Rehearsal`, `sortRehearsals`, `formatWallClockTime`); `Service.rehearsals`/`Service.reportTime`; `OrgSettings.rehearsalTimeDefaults`/`reportTimeDefault` (+ `DEFAULT_ORG_SETTINGS`); `ServiceSnapshot`/`RehearseAccessDoc` carrying both fields; three new unit-test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-rehearsal-report-times/139-CONTEXT.md
@.planning/phases/139-rehearsal-report-times/139-RESEARCH.md
@.planning/phases/139-rehearsal-report-times/139-PATTERNS.md
@.planning/phases/139-rehearsal-report-times/139-VALIDATION.md
</context>

## Artifacts this phase produces

New symbols originating in this plan (consumed by 139-02 and every display surface):

- `src/utils/rehearsalTimes.ts` — exports:
  - `interface Rehearsal { id: string; date: string; time: string }`
  - `function sortRehearsals(rehearsals: Rehearsal[]): Rehearsal[]` — chronological (date then time), undated last
  - `function formatWallClockTime(hhmm: string): string` — 12-hour wall-clock, timezone-stable
- `src/types/service.ts` — new optional `Service.rehearsals?: { id: string; date: string; time: string }[]` and `Service.reportTime?: string`
- `src/types/organization.ts` — new `OrgSettings.rehearsalTimeDefaults: string[]` and `OrgSettings.reportTimeDefault: string` (plus `DEFAULT_ORG_SETTINGS` seeds `[]` / `''`)
- `src/stores/services.ts` — `ServiceSnapshot.rehearsals?` / `ServiceSnapshot.reportTime?`
- `src/utils/rehearseAccess.ts` — `RehearseAccessDoc.rehearsals?` / `RehearseAccessDoc.reportTime?`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared rehearsal-times util (sortRehearsals + formatWallClockTime) with unit test — Wave 0 seam</name>
  <files>src/utils/rehearsalTimes.ts, src/utils/__tests__/rehearsalTimes.test.ts</files>
  <read_first>
    - .planning/phases/139-rehearsal-report-times/139-PATTERNS.md (the `src/utils/rehearsalTimes.ts (NEW utility)` section — the exact composed code to write)
    - .planning/phases/139-rehearsal-report-times/139-RESEARCH.md (§6, final paragraph — the throwaway-local-Date formatting idiom; and Pitfall 3 — timezone stability)
    - src/composables/useRunTimers.ts (lines ~47-50 — the `toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' })` reference idiom that yields "8:00 AM")
    - src/components/ServiceCard.vue (lines ~123-126 — the `date.split('-').map(Number)` idiom this util consolidates; do NOT add a 6th copy)
  </read_first>
  <behavior>
    - sortRehearsals: given rehearsals inserted out of order (not just appended), returns them ordered by date ascending then time ascending, using plain string comparison on zero-padded 'YYYY-MM-DD'/'HH:mm' (no numeric parse).
    - sortRehearsals: rows with date === '' (undated) sort AFTER all dated rows; two undated rows order by time.
    - sortRehearsals: returns a NEW array (input not mutated).
    - formatWallClockTime('08:00') === '8:00 AM'; formatWallClockTime('13:05') === '1:05 PM'; formatWallClockTime('00:00') === '12:00 AM'.
    - formatWallClockTime is timezone-stable: the same input yields the same string irrespective of the process/runner timezone (construct the throwaway Date from numeric components — never parse a combined date+time ISO string).
  </behavior>
  <action>
    Create src/utils/rehearsalTimes.ts as a pure, store-free module (no Firestore/Pinia imports, so both projection builders can import it without a dependency-direction violation). Export the `Rehearsal` interface ({ id, date, time }), `sortRehearsals`, and `formatWallClockTime` exactly as specified in 139-PATTERNS.md's composed-pattern block. sortRehearsals sorts a spread copy by date-then-time via localeCompare on the zero-padded strings, with undated (date === '') rows forced last. formatWallClockTime splits the 'HH:mm' string into numeric hour/minute, constructs a throwaway LOCAL date from the numeric components only (new Date(2000, 0, 1, h, m)) purely to borrow toLocaleTimeString, and returns toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' }) — never build a combined ISO string and never add a date library (per CLAUDE.md). Then write src/utils/__tests__/rehearsalTimes.test.ts covering every case in the behavior block. For the timezone-stability assertion, force the runner zone deterministically (set process.env.TZ before the module/format call, or assert equality across two explicit TZ settings) so a UTC CI box and a local-zone dev box both pass.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/rehearsalTimes.test.ts</automated>
  </verify>
  <done>rehearsalTimes.ts exports Rehearsal, sortRehearsals, formatWallClockTime; the test file passes green, including the out-of-order sort, undated-last, and timezone-stability cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive types (Service + OrgSettings/DEFAULT_ORG_SETTINGS) + createService copy-not-live-bind pre-fill, with copy test</name>
  <files>src/types/service.ts, src/types/organization.ts, src/stores/services.ts, src/stores/__tests__/services.rehearsalDefaults.test.ts</files>
  <read_first>
    - .planning/phases/139-rehearsal-report-times/139-RESEARCH.md (§1 Service shape, §2 OrgSettings + the "no deep-merge needed" verification, §4 createService)
    - .planning/phases/139-rehearsal-report-times/139-PATTERNS.md (the `src/types/service.ts`, `src/types/organization.ts`, and `createService` pattern sections)
    - src/types/service.ts (lines ~219-270 — where the additive optional fields go, mirroring stageLayout?/messaging? JSDoc style)
    - src/types/organization.ts (lines ~26-178 — OrgSettings + DEFAULT_ORG_SETTINGS, the bibleVersion/timezone flat-field style)
    - src/stores/services.ts (lines 440-494 — createService, the addDoc payload at 458-468 and the parallel `created: Service` object at 478-487)
    - src/stores/auth.ts (lines ~399-411 — applyOrgSnapshot; confirm the two new flat fields ride the shallow `...orgSettings` spread and need NO new deep-merge branch)
    - src/stores/__tests__/services.sharePii.test.ts (the full mock scaffold + `await import('../services')` pattern to copy)
  </read_first>
  <behavior>
    - createService copies org defaults at creation: given rehearsalTimeDefaults ['18:30','19:00'] and reportTimeDefault '08:00', the created service's stored rehearsals is two rows each { id, date: '', time } (one per default, date blank) and reportTime is '08:00'.
    - Copy-not-live-bind (R432): after createService returns, mutating authStore.settings.rehearsalTimeDefaults/reportTimeDefault leaves the already-created service's captured stored values byte-identical.
    - Fresh-copy inverse: a service created AFTER a defaults change picks up the NEW defaults (guards against a stale settings snapshot).
  </behavior>
  <action>
    Add two additive optional fields to the Service interface, appended after the existing fields with the same JSDoc-with-rationale style as stageLayout?/messaging?: `rehearsals?: { id: string; date: string; time: string }[]` and `reportTime?: string`. Both are plain strings ('YYYY-MM-DD' + 'HH:mm'), never Timestamp/Date — matching Service.date's convention to avoid timezone rebinding; absent means "no times set" (no migration). In organization.ts add two FLAT fields to OrgSettings alongside timezone — `rehearsalTimeDefaults: string[]` and `reportTimeDefault: string` — and seed DEFAULT_ORG_SETTINGS with `rehearsalTimeDefaults: []` and `reportTimeDefault: ''`. Do NOT add a deep-merge branch in applyOrgSnapshot for these two: RESEARCH §2 verified they are flat (unlike slideTypography/messaging) and the existing shallow `...orgSettings` spread already covers them. In createService (services.ts:440-494), before the addDoc call, synthesize `const rehearsals = authStore.settings.rehearsalTimeDefaults.map((time) => ({ id: crypto.randomUUID(), date: '', time }))` and `const reportTime = authStore.settings.reportTimeDefault` — a one-time COPY, mirroring the existing defaultServiceTemplate read at line 452 (never a live read from any display component). Add `rehearsals` and `reportTime` to the addDoc payload (alongside status: 'draft') AND to the parallel `created: Service` object built at lines 478-487 for ensureShareLink — omitting the second construction makes the first buildServiceSnapshot for a brand-new service silently drop the fields until the next edit. Then write src/stores/__tests__/services.rehearsalDefaults.test.ts using the sharePii mock scaffold: seed authStore.settings via the store, call createService, and assert on the addDoc mock's captured payload for all three behavior cases (copy, copy-not-bind, fresh-copy inverse).
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.rehearsalDefaults.test.ts</automated>
  </verify>
  <done>Service has rehearsals?/reportTime?; OrgSettings + DEFAULT_ORG_SETTINGS have rehearsalTimeDefaults/reportTimeDefault; createService copies defaults into both the addDoc payload and the created object; the copy-not-live-bind + fresh-copy tests pass; `npm run type-check` is clean for the touched types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread both public projections atomically (buildServiceSnapshot + buildRehearseAccess) with a dual-builder projection test</name>
  <files>src/stores/services.ts, src/utils/rehearseAccess.ts, src/stores/__tests__/services.rehearsalProjection.test.ts</files>
  <read_first>
    - .planning/phases/139-rehearsal-report-times/139-RESEARCH.md (§5 — exact interface + return-object edits for BOTH builders; and Pattern 2 — filter undated before sort/attach; and Pitfall 2 — the single most likely omission)
    - .planning/phases/139-rehearsal-report-times/139-PATTERNS.md (the buildServiceSnapshot and buildRehearseAccess pattern sections)
    - src/stores/services.ts (ServiceSnapshot interface lines ~90-114, buildServiceSnapshot 123-207, toPublicServiceSnapshot 237-251 — the existing stageLayout conditional-spread to mirror)
    - src/utils/rehearseAccess.ts (RehearseAccessDoc interface lines ~55-107, buildRehearseAccess 131-278 — the serviceDate: service.date field to sit alongside)
    - src/stores/__tests__/services.sharePii.test.ts (dual-builder assertion + mock scaffold)
  </read_first>
  <behavior>
    - buildServiceSnapshot(service) carries `rehearsals` (sorted) and `reportTime` when present; buildRehearseAccess(service, ...) carries the same two fields — a known dated input value surfaces in BOTH.
    - An undated rehearsal (date: '') is filtered from BOTH projections but remains present in the raw Service doc.
    - Rehearsals in each projection are in chronological order even when the source array is out of order.
    - An empty rehearsals array / unset reportTime is OMITTED (not written as [] / '') — the same conditional-spread behavior as the existing stageLayout omission, so Firestore's undefined-rejection round-trips cleanly.
  </behavior>
  <action>
    Treat editing both builders as ONE atomic task (Pitfall 2 — the omission risk that leaves volunteer/share surfaces blank with no compile error). Add `rehearsals?: { id: string; date: string; time: string }[]` and `reportTime?: string` to the ServiceSnapshot interface (services.ts) and to the RehearseAccessDoc interface (rehearseAccess.ts), each alongside the existing date/serviceDate field. In BOTH builders, before the return object, compute `const datedRehearsals = (service.rehearsals ?? []).filter((r) => r.date !== '')` (Pattern 2 — undated rows are editor-only, meaningless on a read-only surface), then conditional-spread `...(datedRehearsals.length > 0 ? { rehearsals: sortRehearsals(datedRehearsals) } : {})` and `...(service.reportTime ? { reportTime: service.reportTime } : {})`, mirroring the existing stageLayout conditional-spread. Import sortRehearsals from src/utils/rehearsalTimes.ts in both files (rehearseAccess.ts is store-free/pure, so importing the pure util is dependency-safe). These fields are NOT PII (they behave like date, not like notes) — do NOT add a strip in toPublicServiceSnapshot. Then write src/stores/__tests__/services.rehearsalProjection.test.ts using the sharePii scaffold, asserting all four behavior cases against BOTH builders.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.rehearsalProjection.test.ts</automated>
  </verify>
  <done>ServiceSnapshot and RehearseAccessDoc both declare and carry rehearsals?/reportTime?; both builders filter undated rows, sort via sortRehearsals, and omit empties; the dual-builder projection test passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore `services/{id}` | Editor writes `rehearsals`/`reportTime` on a draft-status service doc |
| client → Firestore `organizations/{orgId}.settings` | Editor writes the two new default fields |
| server projection → public reader | `buildServiceSnapshot` (share doc) and `buildRehearseAccess` (rehearseAccess doc) expose service fields to volunteer/public surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-01 | Tampering | `services/{docId}` update rule | low | accept | Existing `isOrgEditor(orgId)` gate + draft-status branch already governs arbitrary field writes (no field allowlist); RESEARCH verified zero rules change — `rehearsals`/`reportTime` are ordinary draft-editable fields. No new mitigation needed. |
| T-139-02 | Information disclosure | rehearsal/report times on public share + rehearseAccess projections | low | accept | Times are intended-public scheduling info for a single physical venue (R429–R433 design), NOT PII. The public projection boundary already passed PII review (v2.8 SEC-S-04 / services.sharePii.test.ts); these fields behave like `date`, which is already public. Filtering undated rows (Pattern 2) also avoids leaking half-entered editor state. |
| T-139-03 | Information disclosure | frozen `RehearseAccessDoc` after a post-lock edit | low | accept | Shows an OLD (not forged) value until the existing reopen→edit→re-lock cycle re-runs `buildRehearseAccess` — the same already-accepted latency contract every RehearseAccessDoc field has. Not new to this phase; documented in the phase UAT note. |
| T-139-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed this phase (RESEARCH "Installation: none"); pure additive TS/Vue against the existing stack. No package legitimacy audit required. |
</threat_model>

<verification>
- Per task: the task's `<automated>` vitest file is green.
- Per wave (before merging to 139-02): bare `npx vitest run` shows only the documented `storage.rules.test.ts` baseline failure (nothing new), AND `npm run type-check` (vue-tsc --build, per CLAUDE.md — NOT `-p tsconfig.app.json`) is clean.
- Goal-backward: all four `must_haves.truths` demonstrated by the three new test files (util behavior, copy-not-bind, dual-builder projection).
</verification>

<success_criteria>
- `src/utils/rehearsalTimes.ts` exports `Rehearsal`, `sortRehearsals`, `formatWallClockTime`, all unit-tested (incl. timezone stability).
- `Service.rehearsals?`/`reportTime?` and `OrgSettings.rehearsalTimeDefaults`/`reportTimeDefault` (+ `DEFAULT_ORG_SETTINGS`) exist and type-check.
- `createService` copies org defaults into the new doc (both the addDoc payload and the parallel `created` object); a later org-default edit does not mutate an existing service (test-proven).
- Both `buildServiceSnapshot` and `buildRehearseAccess` carry `rehearsals`/`reportTime`, filter undated rows, and sort — proven by one dual-builder test.
- Full app suite green except the documented baseline; `npm run type-check` clean.
</success_criteria>

<output>
Create `.planning/phases/139-rehearsal-report-times/139-01-SUMMARY.md` when done.
</output>
