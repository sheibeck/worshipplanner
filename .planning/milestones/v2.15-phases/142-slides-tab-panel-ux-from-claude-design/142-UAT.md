---
status: testing
phase: 142-slides-tab-panel-ux-from-claude-design
source: [142-VERIFICATION.md]
started: 2026-09-19T03:00:00Z
updated: 2026-09-19T03:00:00Z
---

## Current Test

number: 1
name: Popover viewport-edge flip and clipping
expected: |
  Open each chip's popover (Display · Background · Audio) on a narrow/resized window with the slide grid
  scrolled. The popover is always fully visible and flips to right-align near the viewport's right edge.
awaiting: user response

## Tests

### 1. Popover viewport-edge flip and clipping
expected: Open each chip's popover on a narrow/resized window with the slide grid scrolled — popover is always fully visible; flips to right-align near the viewport's right edge.
result: [pending]

### 2. Drag-and-drop background upload
expected: Drag a PNG/JPG from the desktop onto the Background popover's ＋ tile — upload progress renders, then the new thumbnail appears highlighted and the Background chip updates.
result: [pending]

### 3. Live audio playback (Track preview and no-MP3 vamp silence)
expected: Assign a Track and press ▶ — it plays audibly. Assign a vamp that has no MP3 and run the service — no sound plays live while the amber "♪ … · no MP3" chip and popover warning stay visible.
result: [pending]

### 4. Vamp-tab subscription-error surfacing (backstop, plan 142-02)
expected: With the Audio popover's Vamp tab open, a failing vamp subscription (e.g. transient Firestore read failure) never renders the tab blank — it shows loading, existing rows, or a visible fallback. (useVampStore has no dedicated error state; inherited from Phase 141.)
result: [pending]

### 5. Firestore write-failure-then-render surfacing (backstop, plan 142-04)
expected: A rejected Background/Audio Firestore write (e.g. simulated permission-denied) leaves the chip on its prior last-saved value — never the attempted-but-failed one.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
