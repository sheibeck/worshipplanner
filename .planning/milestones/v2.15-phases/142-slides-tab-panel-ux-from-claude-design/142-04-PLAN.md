---
phase: 142-slides-tab-panel-ux-from-claude-design
plan: 04
type: execute
wave: 3
depends_on: [142-03]
files_modified:
  - src/components/slides/SlideGrid.vue
  - src/components/slides/__tests__/SlideGrid.test.ts
  - src/components/slides/SlidesTab.vue
  - src/components/slides/__tests__/SlidesTab.test.ts
  - .planning/codebase/ARCHITECTURE.md
  - .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md
autonomous: true
requirements: [R032, R055, R425, R437]
tags: [vue, integration, firestore, testid-migration, regression-gate]

must_haves:
  truths:
    - "Selecting any group in the Slides tab shows the group-media panel with the 7a chip row (Display · Background · Audio) — the panel gate is Boolean(selectedSlot); it renders for editor and viewer, locked and unlocked, group materialized or not; it is absent only when no group is selected (UI-SPEC §7; CONTEXT.md › Layout)"
    - "Loop (MISC/ANNOUNCEMENTS), the Congregational-reading button (Scripture) and Remove-imported-entries render AFTER the three chips on the same row, with their existing gates and testids preserved (CONTEXT.md › Layout: 'the chips always lead')"
    - "SlideGrid.vue remains the sole Firestore write owner: the strip's six emits are wired to the unchanged onAttachGroupMusic / onRemoveGroupMusic / onAttachGroupVamp / onAttachGroupBackground / onRemoveGroupBackground handlers and the inline video-output-change relay"
    - "onAttachGroupVamp no longer early-returns on a missing downloadUrl: a no-MP3 vamp reaches setGroupBedMedia as { serviceId, bedVampId, bedVampLabel } with no bedAudioUrl key (CONTEXT.md › Audio slot: 'Today's onAttachGroupVamp early-return … is removed for this path')"
    - "Background popover recents are the distinct background URLs already set on groups (sorted by updatedAt desc) and song-inherited backgrounds in this service, deduped, capped to 4, derived in SlidesTab from already-subscribed data with no new Firestore read (CONTEXT.md › Background & Display chips; Claude's discretion resolved per RESEARCH Pitfall 3 fallback)"
    - "Locked service / non-editor: music and background are no longer hidden — the chips state the current setup as inert spans (the old hide-when-locked behavior for music/background is replaced)"
    - "The row-end caption 'applies to all N slide(s) in this group, unless a slide sets its own' renders for every selected group (no longer suppressed while a song background is inherited — the popover carries the inherited explainer instead)"
    - "Retired testids slide-grid-group-background and slide-grid-group-background-caption no longer exist; every SlideGrid.test.ts assertion on retired/relocated testids is migrated to the new contract (RESEARCH Finding 8: ~42 references across five describe blocks + the per-item Video output block)"
    - "Full app suite (bare npx vitest run) matches the 2-file baseline — only src/storage.rules.test.ts fails — and npm run type-check (vue-tsc --build) is clean"
    - "empty — panel wrapper: never empty now that Display always renders a chip; gate simplified to Boolean(selectedSlot)"
    - statement: "Background/Audio Firestore write failure: SlideGrid handlers only console.error (pre-existing pattern, unchanged) — verify at UAT that a failed group-media write does not leave the popover showing a value that never saved (the chip keeps reflecting the stored group prop, so it should revert visually)"
      verification: backstop
  artifacts:
    - path: "src/components/slides/SlideGrid.vue"
      provides: "panel gate Boolean(selectedSlot); SlideGroupSetupStrip mount with trailing slot; recentBackgrounds prop; onAttachGroupVamp without the no-URL guard; retired gates/caption removed"
    - path: "src/components/slides/SlidesTab.vue"
      provides: "recentBackgrounds computed → :recent-backgrounds on SlideGrid"
    - path: "src/components/slides/__tests__/SlideGrid.test.ts"
      provides: "migrated group-media / vamp-wiring / background / panel / video-output blocks"
    - path: "src/components/slides/__tests__/SlidesTab.test.ts"
      provides: "recentBackgrounds derivation test"
  key_links:
    - from: "SlideGroupSetupStrip emits"
      to: "slideGroups store writes"
      via: "SlideGrid handlers (canWriteGroupMedia re-check inside each) — unchanged bodies except onAttachGroupVamp's guard removal"
    - from: "SlidesTab.recentBackgrounds"
      to: "BackgroundControl.recents"
      via: "SlidesTab → SlideGrid :recent-backgrounds → SlideGroupSetupStrip :recent-backgrounds → BackgroundControl :recents"
    - from: "SlideGrid panel v-if"
      to: "selectedSlot prop"
      via: "Boolean(selectedSlot) — retired six-condition OR"
---

<objective>
Swap the finished `SlideGroupSetupStrip` into `SlideGrid.vue`'s group-media panel, simplify the panel gate
to `Boolean(selectedSlot)`, remove `onAttachGroupVamp`'s no-URL early return so a no-MP3 vamp reaches the
(now fixed) store, thread `recentBackgrounds` from `SlidesTab.vue`, migrate `SlideGrid.test.ts`'s ~42
retired/relocated testid references, and close the phase with the full-suite + type-check gate.

Decision areas implemented (142-CONTEXT.md): "Layout variant & panel structure" (chips lead; Loop /
Congregational / Remove-imported trail on the same row; locked = visible-but-inert replaces hide-when-locked),
"Audio slot" (no-MP3 vamp write reaches the store), "Background & Display chips" (recents source — Claude's
discretion resolved: group-owned sorted by `updatedAt` desc, then song-inherited in plan order, deduped,
capped to 4; no new Firestore read).

Purpose: this is the integration that makes 7a real in the app. Everything below it is already tested in
isolation, so the risk here is wiring and the test-migration bill — budgeted as its own work, not a
byproduct.
Output: integrated Slides-tab panel; migrated grid suite; SlidesTab recents; green full suite + type-check;
VALIDATION.md rows marked.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Project docs (do NOT read `.planning/STATE.md` whole — ~450KB; frontmatter + first ~120 lines only.
ROADMAP.md: only the `### Phase 142` section, ~line 1099):
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-CONTEXT.md
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-UI-SPEC.md
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-PATTERNS.md
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md
@.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-03-SUMMARY.md

RESEARCH.md "Finding 6" (recents source, the `updatedAt` gap) and "Finding 8" (the exact describe blocks
and line ranges in SlideGrid.test.ts) — .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-RESEARCH.md.

Project rules (CLAUDE.md): `npm run type-check` (vue-tsc --build, includes tests) is the gate — never
`-p tsconfig.app.json`; bare `npx vitest run` is the app suite (2-file baseline: only
`src/storage.rules.test.ts` fails; the full run takes ~9 min → run it detached); short comments, rationale
in ADRs/map docs; run `worktree-env-setup` in a fresh worktree.
</context>

## Artifacts this phase produces (phase-wide inventory; items marked ★ are created by THIS plan)

| Artifact | Kind | Where |
|---|---|---|
| `setGroupBedMedia` third patch branch | store logic | Plan 01 |
| `BackgroundControl` `variant` / `recents`, swatch testids, drop target | component | Plan 01 |
| `SlotVideoOutputControl` tiles + `sizeHint` | component | Plan 01 |
| `VampPicker allowUnattached`; `AudioPlayer loadedmetadata`; `SlideGroupMusicControl` tabs + `close` emit | component | Plan 02 |
| `SlideGroupSetupStrip.vue` + test file + ARCHITECTURE.md entry | component | Plan 03 |
| ★ `SlideGrid.vue`: panel `v-if="Boolean(selectedSlot)"`; `<SlideGroupSetupStrip>` mount with `#trailing` slot (Loop / Congregational / Remove-imported); new prop `recentBackgrounds?: { url: string; label: string }[]` (default `[]`); `onAttachGroupVamp` writes `{ serviceId, bedVampId, bedVampLabel, ...(downloadUrl ? { bedAudioUrl } : {}) }`; REMOVED: `showGroupMusicControl`, `showGroupBackgroundControl`, `showVideoOutputControl`, `groupBackgroundCaption`, the `slide-grid-group-background` wrapper div, the `slide-grid-group-background-caption` paragraph, the three direct control imports | component | Plan 04 |
| ★ `SlidesTab.vue`: `recentBackgrounds` computed (`{ url, label }[]`, ≤ 4) and `:recent-backgrounds` binding on `SlideGrid` | component | Plan 04 |
| ★ `SlideGrid.test.ts` migrated blocks; `SlidesTab.test.ts` recents test | tests | Plan 04 |
| ★ `.planning/codebase/ARCHITECTURE.md` SlideGrid entry: gate-change note | map doc | Plan 04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SlideGrid.vue — mount the strip, simplify the panel gate, drop the no-URL vamp guard, add recentBackgrounds; migrate every SlideGrid.test.ts assertion on retired/relocated testids</name>
  <files>src/components/slides/SlideGrid.vue, src/components/slides/__tests__/SlideGrid.test.ts, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - src/components/slides/SlideGrid.vue lines 60-265 (the panel: its ~70-line HTML comment history block, the six-condition `v-if`, the four child controls, the `slide-grid-group-background` wrapper, the trailing buttons, the caption paragraph), lines 395-402 (imports), 413-441 (props), 540-545 and 580-592 (`canLoopSlot`, `showVideoOutputControl`, `canWriteGroupMedia`, the two `show*` gates), 655-800 (`openMenuEntryId` watcher, the five group-media handlers, `groupBackgroundCaption`, `songBackgroundForInheritedDisplay`)
    - src/components/slides/SlideGroupSetupStrip.vue (Plan 03 — its props/emits/`trailing` slot are the binding target)
    - .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-UI-SPEC.md "### 7. `SlideGrid.vue` integration — the gate change" and "## Testid Contract" (preserved / new / retired tables)
    - src/components/slides/__tests__/SlideGrid.test.ts: lines 1-135 (mocks — note `findComponent(SlideGroupMusicControl)` / `findComponent(BackgroundControl)` are imported at lines 7-8 and are still valid: those components now mount INSIDE an open popover, so tests must click the chip first), 219-310 (`makeSlot`/`makeAssembled`/`makeGroup`/`mountGrid`), and the describe blocks at 596 ('group music bar'), 695 ('group-level vamp bed wiring'), 840 ('group background control'), 986 ('group media panel'), 1232 ('group media panel — no-behaviour-change regression'), 1781-1812 ('36-01 phase invariant' — one `slide-grid-group-background` reference at ~1809), 2738 ('per-item Video output'); re-grep the file for every retired/relocated testid before starting (RESEARCH A1: do not trust the count alone)
    - .planning/codebase/ARCHITECTURE.md lines 1865-1875 (the SlideGrid `canMutateGroup/canWriteGroupMedia` note to extend)
  </read_first>
  <behavior>
    - panel gate: `slide-grid-group-media-panel` exists for `selectedSlot` set with each of {editor+unlocked, viewer, editor+locked} × {group null, group with media}; absent when `selectedSlot: null`
    - the panel contains `slide-group-setup-strip`; chips are BUTTONs for editor+unlocked and SPANs for viewer / locked; DOM order inside the strip: chip-display, chip-background, chip-audio, then (when applicable) `slot-loop-row` / `slide-grid-congregational-btn` / `slide-grid-remove-imported-btn`, then `slide-group-setup-caption`
    - a song group on a draft service (canWriteGroupMedia carve-out): chips are BUTTONs; opening background shows `background-control-swatch-upload`; opening audio shows `group-music-audio-tab-track`; slide mutation (`slide-grid-add-slide`, drop tile) stays absent (R054, unchanged assertions)
    - music wiring: click `slide-group-setup-chip-audio` → `findComponent(SlideGroupMusicControl).props('audioUrl')` = group.bedAudioUrl; `vm.$emit('attach', url)` → `mockSetGroupBedMedia('org-1','slot-1', { serviceId, bedAudioUrl: url })`; `vm.$emit('remove')` → `{ serviceId, clearAudio: true }`; every pre-existing assertion in the 'group music bar' and 'vamp bed wiring' blocks holds after opening the chip (props `bedVampId`/`bedVampLabel`/`vamps`/`vampsLoading`, idempotent re-select, MP3 vamp write shape, viewer never writes)
    - no-MP3 vamp: `vm.$emit('attach-vamp', vamp3NoFile)` → `mockSetGroupBedMedia` called once with a third argument that has `bedVampId === vamp3NoFile.id`, `bedVampLabel === \`${vamp3NoFile.name} · ${vamp3NoFile.key}\``, `serviceId`, and `'bedAudioUrl' in patch === false` (replaces the old 'writes nothing' test); re-emitting the same no-MP3 vamp when the group already holds `bedVampId`/`bedVampLabel` and no `bedAudioUrl` writes nothing (idempotent)
    - background wiring: click `slide-group-setup-chip-background` → `findComponent(BackgroundControl).props()` include `variant: 'chip-popover'`, `imageUrl` = group.backgroundImageUrl, `inheritedFrom` = `{ url, label }` for a SONG group whose slides carry `backgroundSource: 'song'` and `undefined` for a PRAYER group / a SONG group with its own background; `vm.$emit('attach', url)` → `mockSetGroupBackground('org-1','slot-1', { serviceId, backgroundImageUrl: url })`; `vm.$emit('remove')` → `{ serviceId, clearBackground: true }`; clicking `background-control-swatch-none` while a background is set also produces the `clearBackground` write (replaces the old `background-control-remove` aria-label test)
    - inherited display: chip-background text contains "(song)" and the popover shows `background-control-inherited-explainer` and NO `background-control-swatch-upload`; a group with its own background shows no explainer
    - caption: `slide-group-setup-caption` text is "applies to all 1 slide in this group, unless a slide sets its own" for one card and "applies to all 2 slides …" for two; it renders even when the background is song-inherited; it appears after the chips in `panel.html()` order
    - panel chrome: the panel wrapper still carries `rounded-md border border-gray-800 bg-gray-900`; the strip root carries none of `border-gray-800` / `bg-gray-900`
    - video output: editor → `slide-group-setup-chip-display` is a BUTTON with text "Full-screen"; click → `slot-video-output-row` inside `slide-group-setup-popover-display`; viewer and locked → chip is a SPAN with text "Full-screen", clicking renders no popover (replaces the three old 'hidden' assertions with visible-but-inert ones); `slot.videoOutput.mode: 'banner'` → chip text "Banner"; open display + click `slot-video-output-banner-btn` → `emitted('video-output-change')[0]` equals `[3, { mode: 'banner' }]`
    - `36-01 phase invariant` block: the `slide-grid-group-background` existence check becomes `slide-group-setup-chip-background` existence
    - zero occurrences of `slide-grid-group-background`, `slide-grid-group-background-caption`, `group-music-add`, and the three `142 interim` markers remain in SlideGrid.test.ts
  </behavior>
  <action>
    <!-- planner-discipline-allow: showGroupMusicControl -->
    <!-- planner-discipline-allow: showGroupBackgroundControl -->
    <!-- planner-discipline-allow: showVideoOutputControl -->
    <!-- planner-discipline-allow: groupBackgroundCaption -->
    <!-- planner-discipline-allow: if (!downloadUrl) return -->
    <!-- planner-discipline-allow: slide-grid-group-background -->
    <!-- planner-discipline-allow: 142 interim -->
    (The retired testids, gates and the interim markers named below are to be DELETED outright — never re-mentioned in a comment in the .vue or the test file; the acceptance gates grep both files for them.)
    `SlideGrid.vue` — imports: delete the `SlideGroupMusicControl`, `BackgroundControl`, `SlotVideoOutputControl` imports; add `import SlideGroupSetupStrip from './SlideGroupSetupStrip.vue'`; keep `SlotLoopControl`, `useVampStore`, `backgroundImageLabel`. Props: add `recentBackgrounds?: { url: string; label: string }[]` with a two-line doc comment ("142 — recents for the Background popover; derived in SlidesTab from already-loaded groups, no read here") and `recentBackgrounds: () => []` in `withDefaults`.

    Template: replace the ENTIRE panel — from the ~70-line HTML comment history block that precedes it (starting near "34-11" / "owner follow-up") through the closing `</div>` after the `slide-grid-group-background-caption` paragraph — with: a 3-line HTML comment (`142 — 7a chip row (Display · Background · Audio); per-kind actions trail in #trailing. Gate is Boolean(selectedSlot): Display always renders a chip, so the panel is never empty. See ARCHITECTURE.md § SlideGrid.vue`), then `<div v-if="Boolean(selectedSlot)" class="mx-6 mt-3 rounded-md border border-gray-800 bg-gray-900 px-3 py-2" data-testid="slide-grid-group-media-panel">` (drop the old `flex flex-wrap items-start gap-x-3 gap-y-2` — the strip is now the flex row) containing `<SlideGroupSetupStrip :selected-slot="selectedSlot!" :group="group" :editable="canWriteGroupMedia" :slide-count="cards.length" :org-id="orgId" :inherited-background="songBackgroundForInheritedDisplay" :recent-backgrounds="recentBackgrounds" :vamps="vampStore.vamps" :vamps-loading="vampStore.isLoading" @attach-music="onAttachGroupMusic" @remove-music="onRemoveGroupMusic" @attach-vamp="onAttachGroupVamp" @attach-background="onAttachGroupBackground" @remove-background="onRemoveGroupBackground" @video-output-change="(videoOutput) => emit('video-output-change', slotArrayIndex, videoOutput)">` with `<template #trailing>` holding, unchanged (same `v-if`s, testids, classes, handlers, each with its existing short comment trimmed to one line): `<SlotLoopControl v-if="canLoopSlot" ...>`, the `slide-grid-congregational-btn` button, the `slide-grid-remove-imported-btn` button. Nothing else in the template changes.

    Script: delete the computeds `showGroupMusicControl`, `showGroupBackgroundControl`, `showVideoOutputControl` (its `Boolean(selectedSlot) && isEditor && !serviceLocked` collapses into the panel gate + `canWriteGroupMedia` — the strip's `editable` is the editability check UI-SPEC §7 describes) and `groupBackgroundCaption` (the strip owns the caption, with the singular/plural fix) together with their doc comments; delete the ARCHITECTURE pointer comment paragraph that only described the two `show*` gates (34-11 note) if it becomes orphaned. In `onAttachGroupVamp`: remove the line `if (!downloadUrl) return`; keep `const downloadUrl = vamp.attachment?.downloadUrl` (now `string | undefined`), keep the label literal and the idempotency comparison exactly as is (it already compares `props.group.bedAudioUrl === downloadUrl`, which is `undefined === undefined` for a no-MP3 re-select), and build the patch as `{ serviceId: props.serviceId, bedVampId: vamp.id, bedVampLabel: label, ...(downloadUrl ? { bedAudioUrl: downloadUrl } : {}) }` so the no-URL case sends NO `bedAudioUrl` key (the store's third branch, Plan 01). Update the handler's one-line comment to say a no-MP3 vamp is allowed at the group level (142). Handler bodies for music/background otherwise unchanged; `canWriteGroupMedia` re-checks stay.

    `SlideGrid.test.ts` — migrate, block by block, to the `<behavior>` contract. Add a helper near `mountGrid`: `async function openChip(wrapper, id: 'display' | 'background' | 'audio') { await wrapper.get(\`[data-testid="slide-group-setup-chip-${id}"]\`).trigger('click') }` and use it before every `findComponent(SlideGroupMusicControl)` / `findComponent(BackgroundControl)` / `slot-video-output-*` lookup. Block-specific rules: 'group music bar' — add `await openChip(wrapper, 'audio')` after each mount; the source-order test compares `slide-grid-title` < `slide-group-setup-strip` < `slide-grid-cards`. 'vamp bed wiring' — same open step; rewrite the 'attachment is null writes nothing' test to the no-MP3 write-shape assertion and add the no-MP3 idempotency test. 'group background control' — open the background chip; the two wrapper-existence tests (viewer with/without background) become chip-is-SPAN / chip-is-BUTTON + value-text assertions; the `background-control-remove` aria-label test becomes the swatch-none → `clearBackground` write test. 'group media panel (34-11 Task 1)' — rewrite the four gate tests to the new gate (panel present in every selectedSlot case, absent only for null); rewrite the chrome test to assert the panel wrapper classes and that the strip root carries none; rewrite the "not containerized" test to assert the strip root has `flex-wrap`; rewrite the caption tests to `slide-group-setup-caption` (singular/plural, order after chips). 'no-behaviour-change regression (34-11 Task 2)' — caption N=1 tests now expect the singular string; inherited tests use chip text "(song)" + `background-control-inherited-explainer`; the carve-out test opens background/audio and checks `background-control-swatch-upload` / `group-music-audio-tab-track`; remove the three `142 interim` comments while rewriting. '36-01 phase invariant' — swap the one retired testid. 'per-item Video output' — rewrite the four tests per `<behavior>` (BUTTON vs SPAN, open → row, Banner text, emit `[3, { mode: 'banner' }]`). Do not touch blocks that never referenced these testids (drag, drop, import, menu ownership, locked-service add/import checks, congregational, remove-imported, loop).

    `ARCHITECTURE.md`: in the `### src/components/slides/SlideGrid.vue` entry, append one short paragraph: "**Phase 142 — group-media panel gate:** the panel renders whenever a group is selected (`Boolean(selectedSlot)`); `SlideGroupSetupStrip` hosts Display/Background/Audio chips and receives `canWriteGroupMedia` as `editable` (button vs inert span). The old per-control existence gates are gone; a locked service shows the chips inert rather than hiding music/background. `onAttachGroupVamp` accepts a vamp with no MP3 (writes id/label only). Every handler still re-checks `canWriteGroupMedia`."
  </action>
  <verify>
    <automated>cd /c/projects/worshipplanner && npx vitest run src/components/slides/__tests__/SlideGrid.test.ts src/components/slides/__tests__/SlideGroupSetupStrip.test.ts src/components/slides/__tests__/SlidesTab.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v-if="Boolean(selectedSlot)"' src/components/slides/SlideGrid.vue` outputs `1`
    - `grep -c "<SlideGroupSetupStrip" src/components/slides/SlideGrid.vue` outputs `1` and `grep -c "#trailing"` outputs `1`
    - `grep -c "import SlideGroupSetupStrip from './SlideGroupSetupStrip.vue'" src/components/slides/SlideGrid.vue` outputs `1`
    - `grep -c "import SlideGroupMusicControl from" src/components/slides/SlideGrid.vue` outputs `0`; same for `import BackgroundControl from` and `import SlotVideoOutputControl from` (each `0`)
    - `grep -c "showGroupMusicControl" src/components/slides/SlideGrid.vue` outputs `0`; same for `showGroupBackgroundControl`, `showVideoOutputControl`, `groupBackgroundCaption` (each `0`)
    - `grep -c "if (!downloadUrl) return" src/components/slides/SlideGrid.vue` outputs `0`
    - `grep -c 'data-testid="slide-grid-group-background"' src/components/slides/SlideGrid.vue` outputs `0` and `grep -c 'data-testid="slide-grid-group-background-caption"'` outputs `0`
    - `grep -c 'data-testid="slide-grid-group-media-panel"' src/components/slides/SlideGrid.vue` outputs `1`; `grep -c 'data-testid="slide-grid-congregational-btn"'` outputs `1`; `grep -c 'data-testid="slide-grid-remove-imported-btn"'` outputs `1`; `grep -c "<SlotLoopControl"` outputs `1`
    - `grep -c "recentBackgrounds" src/components/slides/SlideGrid.vue` outputs at least `2` (prop + binding)
    - `grep -c "canWriteGroupMedia.value) return" src/components/slides/SlideGrid.vue` outputs at least `8` (every mutation handler still re-checks — count must not drop below today's)
    - `grep -c "slide-grid-group-background" src/components/slides/__tests__/SlideGrid.test.ts` outputs `0`; `grep -c "group-music-add"` outputs `0`; `grep -c "142 interim"` outputs `0`
    - `grep -c "async function openChip" src/components/slides/__tests__/SlideGrid.test.ts` outputs `1`
    - `grep -c "Phase 142 — group-media panel gate" .planning/codebase/ARCHITECTURE.md` outputs `1`
    - `npx vitest run src/components/slides/__tests__/SlideGrid.test.ts src/components/slides/__tests__/SlideGroupSetupStrip.test.ts src/components/slides/__tests__/SlidesTab.test.ts` exits 0
  </acceptance_criteria>
  <done>SlideGrid mounts the 7a strip behind a `Boolean(selectedSlot)` gate, stays the sole write owner, forwards a no-MP3 vamp to the store, and its full suite passes against the new testid contract with zero retired references left.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SlidesTab.vue — derive `recentBackgrounds` from already-loaded groups + song-inherited slides and pass it to SlideGrid</name>
  <files>src/components/slides/SlidesTab.vue, src/components/slides/__tests__/SlidesTab.test.ts</files>
  <read_first>
    - src/components/slides/SlidesTab.vue lines 1-40 (the `<SlideGrid>` mount and its bindings), 90-140 (props: `assembledSlideshow`, `groupsBySlotId: Map<string, SlideGroup>`; imports), 255-290 (`selectedGroup` / `selectedGroupAssembledSlides` computeds — the style to match)
    - src/components/slides/SlideGrid.vue `songBackgroundForInheritedDisplay` (~line 756) — the existing `backgroundSource === 'song'` read on `assembledSlide.slide` to mirror
    - src/types/slideGroup.ts (`SlideGroup.updatedAt: Timestamp` — `.seconds` is the public field; fixtures use `{} as never`), src/types/slide.ts lines 28-42 (`backgroundImageUrl`, `backgroundSource`)
    - src/components/slides/slideDisplay.ts (`backgroundImageLabel`)
    - src/components/slides/__tests__/SlidesTab.test.ts lines 1-80 (`mountTab` helper — `groupsBySlotId`, `assembledSlideshow` props) and 178-260 (the 'grid wiring' describe to extend with `grid.props('recentBackgrounds')` assertions)
    - .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-RESEARCH.md "Pitfall 3" / "Finding 6" (why song-inherited entries have no timestamp and the accepted fallback ordering)
  </read_first>
  <behavior>
    - groups A (`backgroundImageUrl: 'https://s/a.jpg'`, `updatedAt: { seconds: 100 } as never`), B (`'https://s/b.jpg'`, seconds 300), C (no background), D (`'https://s/a.jpg'` again, seconds 200) + assembled slides where one carries `backgroundSource: 'song'`, `backgroundImageUrl: 'https://s/song.jpg'` and another carries `backgroundSource: 'group'`, `backgroundImageUrl: 'https://s/b.jpg'` → `grid.props('recentBackgrounds')` equals `[{ url: 'https://s/b.jpg', label: 'b.jpg' }, { url: 'https://s/a.jpg', label: 'a.jpg' }, { url: 'https://s/song.jpg', label: 'song.jpg' }]` (group-owned first by updatedAt desc, deduped, song-sourced appended, `backgroundSource: 'group'` slides ignored because their URL is already covered by the group scan)
    - six distinct group backgrounds → `recentBackgrounds` has length 4
    - no backgrounds anywhere → `[]`
    - the prop is reactive: after `setProps({ groupsBySlotId: newMap })` the list updates
  </behavior>
  <action>
    In `SlidesTab.vue` add, near `selectedGroup`, `const recentBackgrounds = computed<{ url: string; label: string }[]>(() => { ... })`: collect `[...props.groupsBySlotId.values()]` with a truthy `backgroundImageUrl`, sort by `(b.updatedAt?.seconds ?? 0) - (a.updatedAt?.seconds ?? 0)` (cast `updatedAt` through `{ seconds?: number }` if the `Timestamp` type makes `?.seconds` awkward — no `toMillis()` call, so plain fixture objects work), push each unseen URL as `{ url, label: backgroundImageLabel(url) }` into a `Set`-deduped list; then iterate `props.assembledSlideshow` and push unseen URLs where `assembled.slide.backgroundSource === 'song' && assembled.slide.backgroundImageUrl` (plan order — no timestamp exists for song-sourced entries; RESEARCH Pitfall 3's accepted fallback, recorded here as the resolved discretion); return `.slice(0, 4)`. Import `backgroundImageLabel` from `./slideDisplay` if not already imported. Bind `:recent-backgrounds="recentBackgrounds"` on the `<SlideGrid>` in the template. Add a 2-line comment on the computed: "142 — Background popover recents, derived from already-subscribed data (no read); song-sourced entries have no updatedAt, so they trail in plan order".

    In `SlidesTab.test.ts` 'grid wiring' describe, add the four `<behavior>` tests using `mountTab({ slots, groupsBySlotId, assembledSlideshow })` and `wrapper.findComponent(SlideGrid).props('recentBackgrounds')`. Build `SlideGroup` fixtures inline (id/slotId/serviceId/slides/createdAt/updatedAt) matching the file's existing conventions; reuse `makeAssembled` and override `slide.backgroundSource` / `slide.backgroundImageUrl` on the returned object.
  </action>
  <verify>
    <automated>cd /c/projects/worshipplanner && npx vitest run src/components/slides/__tests__/SlidesTab.test.ts src/components/slides/__tests__/SlideGrid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const recentBackgrounds = computed" src/components/slides/SlidesTab.vue` outputs `1`
    - `grep -c ':recent-backgrounds="recentBackgrounds"' src/components/slides/SlidesTab.vue` outputs `1`
    - `grep -c "backgroundSource === 'song'" src/components/slides/SlidesTab.vue` outputs `1`
    - `grep -c "slice(0, 4)" src/components/slides/SlidesTab.vue` outputs `1`
    - `grep -c "getDocs\|onSnapshot\|useSongs" src/components/slides/SlidesTab.vue` outputs `0` (no new read / subscription)
    - `grep -c "recentBackgrounds" src/components/slides/__tests__/SlidesTab.test.ts` outputs at least `4`
    - `npx vitest run src/components/slides/__tests__/SlidesTab.test.ts src/components/slides/__tests__/SlideGrid.test.ts` exits 0
  </acceptance_criteria>
  <done>The Background popover's recents come from data SlidesTab already holds — ordered, deduped, capped — with no new Firestore read.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — type-check, full app suite against the 2-file baseline, VALIDATION.md rows marked</name>
  <files>.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md</files>
  <read_first>
    - .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md (Per-Task Verification Map rows carry task IDs `142-01-T1` … `142-04-T2`; Sampling Rate; Wave 0 checklist)
    - CLAUDE.md "Testing: two suites" (bare `npx vitest run` is correct; do NOT use `--dir src`; `render-service/**` and `src/rules.test.ts` are excluded by `vite.config.ts`; only `src/storage.rules.test.ts` is expected to fail)
  </read_first>
  <action>
    Run `npm run type-check` (must exit 0). Run the full app suite detached — `npx vitest run > "$SCRATCH/142-full-suite.log" 2>&1` in the background (or via the Bash tool's `run_in_background`), where `$SCRATCH` is the session scratchpad directory — and wait for it to finish (~9 min). Inspect the tail of the log: the summary line must read `Test Files  1 failed` with the failing file being `src/storage.rules.test.ts` (the documented Storage-emulator limitation — never "fix" its rule), and every Phase 142 file — `slideGroups.test.ts`, `slideshowAssembler.test.ts`, `VampPicker.test.ts`, `AudioPlayer.test.ts`, `SlideGroupMusicControl.test.ts`, `BackgroundControl.test.ts`, `SlotVideoOutputControl.test.ts`, `SlideGroupSetupStrip.test.ts`, `SlideGrid.test.ts`, `SlidesTab.test.ts`, `EditSlideDrawer.test.ts`, `RunControlView.audio.test.ts` — must be listed as passing. If any other file fails, fix the regression in the owning file (it is in scope if it is one of this phase's `files_modified`; otherwise stop and report it as a blocker with the failing assertion) and re-run the affected file plus the full suite. Record the summary line, total test count and the elapsed time in the SUMMARY.

    Then edit `142-VALIDATION.md`: set every Per-Task Verification Map row's Status to `✅ green`, tick the three Wave 0 checkboxes, and set frontmatter `wave_0_complete: true`. Leave `status: draft` and `nyquist_compliant: false` — `/gsd-validate-phase` owns those.
  </action>
  <verify>
    <automated>cd /c/projects/worshipplanner && npm run type-check && npx vitest run src/components/slides/__tests__/SlideGrid.test.ts src/components/slides/__tests__/SlideGroupSetupStrip.test.ts src/components/slides/__tests__/SlideGroupMusicControl.test.ts src/components/slides/__tests__/BackgroundControl.test.ts src/components/slides/__tests__/SlotVideoOutputControl.test.ts src/components/__tests__/VampPicker.test.ts src/stores/__tests__/slideGroups.test.ts src/components/slides/__tests__/SlidesTab.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0
    - the detached full-suite log's summary contains `Test Files  1 failed` and the only failing file is `src/storage.rules.test.ts`
    - `grep -c "✅ green" .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md` outputs at least `9`
    - `grep -c "wave_0_complete: true" .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md` outputs `1`
    - `grep -c "⬜ pending" .planning/phases/142-slides-tab-panel-ux-from-claude-design/142-VALIDATION.md` outputs `0`
  </acceptance_criteria>
  <done>The whole app suite is at the documented 2-file baseline, vue-tsc --build is clean, and the validation map reflects it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Firestore `slideGroups/{slotId}` | all six strip emits terminate in `SlideGrid` handlers that re-check `canWriteGroupMedia` before calling the store; `firestore.rules` `isOrgEditor(orgId) && parentDraft(...)` unchanged |
| non-editor / locked viewer → panel | the panel now RENDERS for viewers (it used to hide music/background); the strip's inert-span rule is the UI gate, the handlers + rules are the security boundary |
| `SlidesTab.recentBackgrounds` | derived from already-authorized subscriptions; no new read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-142-15 | Elevation of Privilege | panel rendering for viewers / locked services | high | mitigate | `editable = canWriteGroupMedia` (editor && !locked) → inert spans, no popover, no control mounts; every handler still starts with `if (!canWriteGroupMedia.value) return` (Task 1 acceptance counts ≥ 8 re-checks); rules unchanged |
| T-142-16 | Tampering (data integrity) | `onAttachGroupVamp` no-URL path | high | mitigate | patch omits `bedAudioUrl` so the store's third branch `deleteField()`s any stale URL (Plan 01 T-142-01); Task 1 test asserts `'bedAudioUrl' in patch === false` |
| T-142-17 | Information Disclosure | `recentBackgrounds` | low | accept | URLs already rendered on the same service's slideshow for the same org viewer; no cross-service/cross-org aggregation (`groupsBySlotId` is per-service) |
| T-142-18 | Repudiation / regression | testid migration hiding a broken gate | medium | mitigate | migrated tests assert BUTTON vs SPAN per editability and absent popovers for viewers/locked; full suite + type-check gate (Task 3) |
| T-142-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `npx vitest run` (bare, detached) → `Test Files  1 failed` (`src/storage.rules.test.ts` only)
- `npm run type-check` exits 0
- `git diff --stat` touches only the six files in `files_modified`
- Manual (deferred to batched UAT per 142-VALIDATION.md "Manual-Only Verifications"): popover never clipped by the scrolling grid and flips at the right edge; drop an image on ＋ uploads; Track ▶ plays, no-MP3 vamp is silent live with the amber label
</verification>

<success_criteria>
- The Slides tab shows the 7a chip row for every selected group; chips lead, per-kind actions trail; viewers/locked see inert chips
- A no-MP3 vamp can be assigned from the Audio popover and survives the round trip (chip amber, store has id/label, no URL)
- Recents populate the Background popover from already-loaded data
- Grid suite migrated with zero retired testid references; full suite at baseline; type-check clean
</success_criteria>

<output>
Create `.planning/phases/142-slides-tab-panel-ux-from-claude-design/142-04-SUMMARY.md` when done
</output>
