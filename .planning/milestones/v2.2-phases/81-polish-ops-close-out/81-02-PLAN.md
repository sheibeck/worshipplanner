---
phase: 81-polish-ops-close-out
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/ConfigurationTab.vue
  - src/components/admin/__tests__/ConfigurationTab.test.ts
  - src/components/admin/OrganizationsTab.vue
  - src/components/admin/__tests__/OrganizationsTab.test.ts
  - src/components/admin/ConfigTextField.vue
  - src/components/admin/__tests__/ConfigTextField.test.ts
autonomous: true
requirements: [R239]
must_haves:
  truths:
    - "The super-admin grant email input in ConfigurationTab has a programmatic accessible name (associated <label>)"
    - "The Organizations onboard 'Church name' and 'First admin email' inputs each have an associated <label>"
    - "The per-row 'Assign admin' email input, rendered inside v-for over orgs, has an accessible name via aria-label (NOT a static id/for pair, so no duplicate ids across rows)"
    - "ConfigTextField renders a <label> whose for attribute matches its input's id, and multiple ConfigTextField instances on one page produce unique ids (no id collision between the Sender and AI-Proxy cards)"
    - "No auth-gating v-if / route guard is altered; only presentational label/id/aria attributes are added"
  artifacts:
    - src/components/admin/__tests__/ConfigurationTab.test.ts
  key_links:
    - "each <label for> targets exactly one input id in the same non-repeated block; repeated (v-for) inputs use aria-label instead"
    - "ConfigTextField id is generated with Vue useId() so it is unique per instance and stable across renders"
---

<objective>
R239 part 1 of 2 — give every Owner Console text input a programmatic accessible name. Fix the 4 placeholder-only inputs (ConfigurationTab grant form; OrganizationsTab onboard x2; OrganizationsTab per-row assign) and the unassociated `<label>` in the shared ConfigTextField (used by the Sender and AI-Proxy cards). This is a template-and-attribute change only — no logic, no mount-strategy, no auth-gating change.

Purpose: Screen-reader users currently get no accessible name for these inputs (a placeholder is not a label). Per RESEARCH.md this is a fully-enumerated defect list, so we fix the named inputs directly. NOTE: per the orchestrator's instruction we deliberately do NOT add `eslint-plugin-vuejs-accessibility` — it is flagged `[SUS]`/too-new by the package-legitimacy gate (which would force a blocking human checkpoint), and the defect surface is already fully enumerated, so the linter is unnecessary here. A backlog note to add the a11y lint-guard once the plugin version clears the legitimacy re-check is filed separately by the orchestrator — do NOT add the dependency in this plan.
Output: labeled inputs across `ConfigurationTab.vue`, `OrganizationsTab.vue`, and `ConfigTextField.vue`, with component tests asserting each accessible name.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-polish-ops-close-out/81-RESEARCH.md
@src/components/admin/ConfigurationTab.vue
@src/components/admin/OrganizationsTab.vue
@src/components/admin/ConfigTextField.vue
@src/components/admin/SenderConfigCard.vue
@src/components/admin/__tests__/OrganizationsTab.test.ts
@src/components/admin/__tests__/ConfigTextField.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (R239): Label the ConfigurationTab super-admin grant email input</name>
  <files>src/components/admin/ConfigurationTab.vue, src/components/admin/__tests__/ConfigurationTab.test.ts</files>
  <behavior>
    - Mounting ConfigurationTab renders a &lt;label&gt; associated (for -> id) with the grant email input, so the input has an accessible name.
    - The grant form still submits on Enter and via the Grant button (existing behavior unchanged).
  </behavior>
  <action>
In the grant form (the single, non-repeated `type="email"` input bound to `grantEmail`), add an associated `<label>` with a `for` attribute and give the input a matching `id` (e.g. `grant-email`). This input renders once — a static id/for pair is safe here. Keep the existing placeholder, `v-model`, `@keydown.enter="onGrant"`, and classes; the label may be visually styled to match the console (small, gray) or visually-hidden, but it MUST be a real associated `<label>`, not an `aria-label` on the input alone (RESEARCH prefers real labels for the single-instance inputs). Do NOT touch the `onSnapshot` roster subscription, `appConfigStore` calls, or any auth logic.

Create `src/components/admin/__tests__/ConfigurationTab.test.ts` (new — Wave 0 gap): mount the component and assert the grant input resolves to an accessible name via its associated label (e.g. query the label's `for`, assert it equals the input's `id`, and the label text is non-empty). Mock Firestore `onSnapshot`/`httpsCallable`/`appConfig` as the sibling admin tests do so the mount does not hit real Firebase.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/__tests__/ConfigurationTab.test.ts</automated>
  </verify>
  <done>New `ConfigurationTab.test.ts` passes, asserting the grant email input has an associated `<label>` (for === input id, non-empty text). Roster/config subscriptions untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (R239): Label the Organizations onboard inputs and aria-label the per-row assign input</name>
  <files>src/components/admin/OrganizationsTab.vue, src/components/admin/__tests__/OrganizationsTab.test.ts</files>
  <behavior>
    - The onboard 'Church name' and 'First admin email' inputs each have an associated &lt;label&gt; (single-instance, safe id/for).
    - The per-row 'Assign admin' input (inside v-for over orgs) has aria-label "Admin email" and NO static id — mounting two org rows yields zero duplicate ids on that input.
    - Onboard and assign flows still work exactly as before.
  </behavior>
  <action>
Onboard form (two single-instance inputs): add associated `<label for>` + matching `id` to the `churchName` input (e.g. `onboard-church-name`) and the `adminEmail` input (e.g. `onboard-admin-email`). These render once — static id/for is safe.

Per-row assign input (bound to `assignEmail`, rendered inside `v-for="org in orgs"`): do NOT add a static `id`/`for` here — that id would repeat once per row, producing duplicate ids and making every row's label target the first row's input (RESEARCH Pitfall 5). Instead add `aria-label="Admin email"` directly on this input. Keep all existing bindings (`v-model`, `@keydown.enter`, classes, the `assigningOrgId` gate). Do NOT alter any `httpsCallable` logic, in-flight guards, or the delete dialog.

Extend `OrganizationsTab.test.ts`: assert both onboard inputs have associated labels (for === id, non-empty text), and assert the per-row assign input exposes `aria-label="Admin email"`. Mount with at least two org rows (open one row's assign form) and assert there is no duplicate `id` on the assign input across rows.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/__tests__/OrganizationsTab.test.ts</automated>
  </verify>
  <done>Onboard inputs have associated labels; the per-row assign input uses `aria-label="Admin email"` with no duplicate ids across rows; onboard/assign behavior unchanged; extended `OrganizationsTab.test.ts` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (R239): Associate ConfigTextField's label with its input via a unique id</name>
  <files>src/components/admin/ConfigTextField.vue, src/components/admin/__tests__/ConfigTextField.test.ts</files>
  <behavior>
    - ConfigTextField's &lt;label&gt; carries a for that matches its &lt;input&gt; id, so the input has an accessible name equal to the label prop.
    - Two ConfigTextField instances (e.g. Sender fromName + fromAddress) produce two distinct ids — no collision.
  </behavior>
  <action>
`ConfigTextField.vue` currently renders `<label>{{ label }}</label>` as a sibling of `<input>` with no `for`/`id` association. Generate a unique, render-stable id with Vue's `useId()` (available in Vue 3.5.x, already installed) and bind it as the input's `:id` and the label's `:for`. Do NOT plumb an id prop through every caller and do NOT slugify the label (labels like "Display name" could repeat or collide across cards) — `useId()` guarantees per-instance uniqueness with zero call-site changes. Keep every existing prop, the `(default)` badge span, the warning/error paragraphs, and the Save button untouched.

Extend `ConfigTextField.test.ts`: assert the rendered `<label>`'s `for` equals the `<input>`'s `id` and both are non-empty; mount two instances and assert their input ids differ.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/__tests__/ConfigTextField.test.ts && npm run type-check</automated>
  </verify>
  <done>ConfigTextField label `for` matches input `id`, ids are unique across instances, `vue-tsc --build` clean, extended `ConfigTextField.test.ts` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Super-admin session -> Owner Console DOM | Only an `isSuperAdmin`-gated session reaches these components; this change adds presentational markup within that boundary, not new access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-02-01 | Tampering (assistive-tech association integrity) | OrganizationsTab per-row assign input inside v-for | medium | mitigate | Use `aria-label` (no id/for) on the repeated input so no duplicate ids form; test asserts zero duplicate ids across two rows |
| T-81-02-02 | Elevation of privilege (accidental auth-gate edit) | Owner Console admin components | low | mitigate | Change is limited to label/id/aria attributes; no `v-if`/route-guard/`httpsCallable` logic is edited (V4 access control unchanged) |
| T-81-02-03 | Tampering | ConfigTextField id generation | low | accept | `useId()` yields stable, unique ids; no user input flows into the id, so no injection surface |
</threat_model>

<verification>
- `npx vitest run src/components/admin/__tests__/ConfigurationTab.test.ts src/components/admin/__tests__/OrganizationsTab.test.ts src/components/admin/__tests__/ConfigTextField.test.ts` — all green.
- `npm run type-check` (`vue-tsc --build`) clean.
- No diff to auth-gating `v-if`, route guards, or `httpsCallable` calls in the touched components.
</verification>

<success_criteria>
All 4 previously placeholder-only inputs plus the shared ConfigTextField have programmatic accessible names; the repeated assign input uses `aria-label` with no duplicate ids; component tests assert each accessible name; no dependency added; no auth or mount-strategy change.
</success_criteria>

<output>
Create `.planning/phases/81-polish-ops-close-out/81-02-SUMMARY.md` when done.
</output>
