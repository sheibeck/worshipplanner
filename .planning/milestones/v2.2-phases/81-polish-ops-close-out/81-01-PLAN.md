---
phase: 81-polish-ops-close-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/DEPLOY-EMAIL-DOMAIN.md
  - .planning/PENDING-VERIFICATION.md
autonomous: true
requirements: [R237, R238]
user_setup:
  - service: resend
    why: "R238 — verified sending domain (SPF/DKIM/DMARC) is owner-run DNS, not app-deployable or app-verifiable"
    dashboard_config:
      - task: "Add a real sending domain (a dedicated subdomain, e.g. send.<yourdomain>) and publish the generated SPF/DKIM/MX/DMARC records at the DNS provider, wait for ALL records to show Verified, then set the Owner Console Sender 'From address' to the verified address"
        location: "Resend Dashboard -> Domains -> Add Domain; then Owner Console -> Configuration tab -> Sender card"
must_haves:
  truths:
    - "The 3 existing PC-export tests proving PRAYER/MESSAGE/ANNOUNCEMENTS/MISC slots are exported in all three modes (never IMPORTED) pass unchanged"
    - "vue-tsc --build stays clean, keeping the compiler-enforced exhaustive SlotKind dispatch in addSlotAsItem load-bearing (a future 9th kind without a branch is a type error, not a silent drop)"
    - "A step-by-step owner runbook exists covering Resend domain add, SPF/DKIM/DMARC records, the verify-before-flip sequencing, setting fromAddress in the Owner Console, and a real external-inbox test send"
    - "The two live send paths' existing regression tests confirm the From address is built from config.sender.fromAddress (Firestore-backed), not a hard-coded onboarding sender"
    - "PENDING-VERIFICATION.md records the owner-run R238 steps as an outstanding manual verification"
  artifacts:
    - functions/DEPLOY-EMAIL-DOMAIN.md
    - .planning/PENDING-VERIFICATION.md
  key_links:
    - "SenderConfigCard fromAddress -> Firestore appConfig/global.sender.fromAddress -> getAppConfig() read in sendQueuedMessageHandler + sendAdminOnboardingEmail"
    - "runbook sequencing: verify domain in Resend BEFORE flipping the live Owner Console fromAddress"
---

<objective>
Close R237 and R238 as verification + documentation, NOT as new build work. Per RESEARCH.md both are already implemented and unit-tested by prior quick tasks (R237 by `260809-vvq` commit `8c602bc0`; R238's send-path wiring across Phases 69-70). This plan (1) re-confirms the shipped R237 PC-export coverage and the exhaustive `SlotKind` dispatch, documenting the intentional `IMPORTED` exclusion; (2) writes the R238 owner runbook and a PENDING-VERIFICATION entry, and re-confirms both live send paths read the configured sender.

Purpose: R237 and R238 must be traceably verified so the phase can close, without re-implementing already-shipped, already-tested code (RESEARCH Pitfalls 1 and 2 — a rebuild risks regressing the exhaustiveness-guarded dispatch or the From-construction logic).
Output: `functions/DEPLOY-EMAIL-DOMAIN.md` (new owner runbook), an appended `.planning/PENDING-VERIFICATION.md` R238 owner entry, and green re-runs of the existing R237/R238 test coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-polish-ops-close-out/81-RESEARCH.md
@.planning/PENDING-VERIFICATION.md

# R237 already-correct dispatch + tests (verify only, do NOT edit source)
@src/utils/planningCenterApi.ts
@src/views/__tests__/ServiceEditorView.test.ts

# R238 send paths + sender config (verify only, do NOT edit source)
@functions/src/appConfig.ts
@src/components/admin/SenderConfigCard.vue

# Runbook placement precedent — new doc mirrors these siblings
@functions/DEPLOY-RUNTIME-CONFIG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (R237): Re-confirm PC-export slot coverage and exhaustive SlotKind dispatch — verification only</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts, src/utils/planningCenterApi.ts</files>
  <action>
Verification task — make NO source change to `planningCenterApi.ts` or `ServiceEditorView.vue`. Per RESEARCH.md, all three PC-export code paths (new-plan no-template, new-plan with-template, existing-plan) already append PRAYER/MESSAGE/ANNOUNCEMENTS/MISC slots via `addSlotAsItem`, whose per-`SlotKind` branch set ends in a `never`-typed exhaustiveness backstop that turns any future unhandled kind into a `vue-tsc` compile error rather than a silent drop.

Steps: (1) Run the 3 existing R237 tests (the `260809-vvq` block near the end of `ServiceEditorView.test.ts`) and confirm all pass. (2) Run `npm run type-check` and confirm the exhaustive dispatch still compiles (this is R237's compile-time guarantee). (3) Record in `.planning/phases/81-polish-ops-close-out/81-01-SUMMARY.md` — creating/writing the SUMMARY as part of THIS task, not deferring it to plan-end — the explicit, intended decision that `IMPORTED` slides stay excluded from PC export (use the literal token `IMPORTED` in that note), because they have no analogous Planning Center item type, the exclusion is already asserted by the existing tests, and it is consistent with the requirement's own examples (prayer/offering/welcome/message/announcements), so R237's "no dropped items" is satisfied. The secondary automated grep below verifies this note is present. Add a coverage assertion ONLY if a real gap surfaces when reading the dispatch against the current `SlotKind` union — RESEARCH found none, so none is expected.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts -t "IMPORTED|previously dropped" && npm run type-check && grep -qi 'IMPORTED' .planning/phases/81-polish-ops-close-out/81-01-SUMMARY.md</automated>
  </verify>
  <done>The 3 R237 export-mode tests pass, `vue-tsc --build` is clean, and `81-01-SUMMARY.md` records the intentional `IMPORTED`-excluded decision (grep-verified). No change to `planningCenterApi.ts`/`ServiceEditorView.vue`.</done>
</task>

<task type="auto">
  <name>Task 2 (R238): Write the owner email-domain runbook and PENDING-VERIFICATION entry</name>
  <files>functions/DEPLOY-EMAIL-DOMAIN.md, .planning/PENDING-VERIFICATION.md</files>
  <action>
Create `functions/DEPLOY-EMAIL-DOMAIN.md`, matching the tone and placement of the sibling `functions/DEPLOY-*.md` runbooks. Document, in owner-executable order per RESEARCH.md's Code Examples and Pitfall 3: (1) choose a real owner-controlled domain — call out that `*.web.app`/`*.firebaseapp.com` CANNOT be verified (Google-managed, no DNS access), which the Sender card's unverifiable-host warning already flags; (2) Resend Dashboard -> Domains -> Add Domain, recommending a dedicated sending subdomain; (3) publish the generated DNS records — SPF (TXT), DKIM (TXT at the `resend._domainkey` subdomain, a literal value not a CNAME), MX for bounce/complaint feedback, and DMARC (TXT at `_dmarc`, starting in monitoring mode `p=none` and progressing to quarantine/reject only after legitimate mail passes); (4) click Verify and wait for ALL records to show Verified (not Pending) — propagation can take up to 48h; (5) set the Owner Console Sender "From address" to the verified address — a LIVE Firestore write, no redeploy, and note that `SERVICE_SHARE_BASE_URL` stays on the Firebase default per the locked R238 decision; (6) send a real test message to a real EXTERNAL inbox (not the Resend account owner's own address) and check the message's delivery-history rollup for partial/failed status. Use placeholder domains only — embed no real secrets or real domains.

Then append an R238 entry to `.planning/PENDING-VERIFICATION.md` recording these owner-run steps as an outstanding manual verification (DNS verification is owner-run and not app-verifiable), pointing to `functions/DEPLOY-EMAIL-DOMAIN.md`.
  </action>
  <verify>
    <automated>test -f functions/DEPLOY-EMAIL-DOMAIN.md && grep -qi 'DKIM' functions/DEPLOY-EMAIL-DOMAIN.md && grep -qi 'DMARC' functions/DEPLOY-EMAIL-DOMAIN.md && grep -qi 'from address' functions/DEPLOY-EMAIL-DOMAIN.md && grep -qi 'R238' .planning/PENDING-VERIFICATION.md</automated>
  </verify>
  <done>`functions/DEPLOY-EMAIL-DOMAIN.md` exists and covers Resend domain add + SPF/DKIM/DMARC + verify-before-flip sequencing + setting fromAddress in the Console + an external test send; `.planning/PENDING-VERIFICATION.md` carries an R238 owner entry.</done>
</task>

<task type="auto">
  <name>Task 3 (R238): Re-confirm both live send paths read config.sender.fromAddress — verification only</name>
  <files>functions/src/index.test.ts, functions/src/adminEmail.test.ts</files>
  <action>
Verification task — make NO change to `functions/src/index.ts` or `functions/src/adminEmail.ts`. Per RESEARCH.md both send paths (`sendQueuedMessageHandler`, `sendAdminOnboardingEmail`) already read `config.sender.fromAddress` live via `getAppConfig()`; `onboarding@resend.dev` exists only as `DEFAULT_APP_CONFIG.sender.fromAddress` (the correct fallback until an owner sets a real value), not as a hard-coded override on any send path. Run the existing regression tests that assert the From address is built from the injected `config.sender.fromAddress` and confirm they pass. If — contrary to RESEARCH — a hard-coded sender were found on a live send path, make the configured value authoritative; RESEARCH found none, so no source change is expected.
  </action>
  <verify>
    <automated>cd functions && npx vitest run src/index.test.ts -t "config.sender.fromAddress" && npx vitest run src/adminEmail.test.ts</automated>
  </verify>
  <done>Both send-path test groups pass, confirming the From address derives from `config.sender.fromAddress`. No change to `index.ts`/`adminEmail.ts`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner -> Firestore appConfig/global | Owner sets `sender.fromAddress` live via the Console; consumed unvalidated-for-domain-verification by the send path |
| App -> Resend API | The send path submits the configured From address; Resend rejects unverified domains at send time only |
| DNS provider (owner-run) | SPF/DKIM/DMARC records live outside the app; the app cannot read or verify them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-01-01 | Denial (of email delivery) | Resend send path after a premature `fromAddress` change | high | mitigate | Runbook enforces verify-ALL-records-in-Resend BEFORE flipping the live Console value, then a mandatory real external-inbox test send to catch `403 domain not verified` (which otherwise degrades silently to a `partial`/`failed` message status) |
| T-81-01-02 | Information disclosure | `functions/DEPLOY-EMAIL-DOMAIN.md` committed to the repo | low | mitigate | Runbook uses placeholder domains only; embeds no real secrets, real domains, or DNS record values |
| T-81-01-03 | Tampering | R237 exhaustive `SlotKind` dispatch | low | accept | Compile-time `never` backstop already guards silent drops; verification-only task makes no source edit that could weaken it, and `npm run type-check` re-confirms it |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts -t "IMPORTED|previously dropped"` — 3 R237 tests green.
- `npm run type-check` (`vue-tsc --build`) clean — exhaustive dispatch intact.
- `cd functions && npx vitest run src/index.test.ts -t "config.sender.fromAddress"` and `npx vitest run src/adminEmail.test.ts` green.
- `functions/DEPLOY-EMAIL-DOMAIN.md` present with DKIM/DMARC/From-address content; `.planning/PENDING-VERIFICATION.md` has an R238 owner entry.
</verification>

<success_criteria>
R237: existing 3 export-mode tests pass, type-check clean, `IMPORTED`-excluded decision documented — no source rebuild. R238: owner runbook written, PENDING-VERIFICATION entry recorded, both send-path tests confirm the configured sender — no send-path code change.
</success_criteria>

<output>
Create `.planning/phases/81-polish-ops-close-out/81-01-SUMMARY.md` when done.
</output>
