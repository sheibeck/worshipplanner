---
phase: 86-recurring-team-scheduling
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - src/components/TeamRecurrenceSlideOver.vue
  - src/components/TeamsConfigPanel.vue
  - src/components/__tests__/TeamsConfigPanel.test.ts
autonomous: true
requirements: [R254]
must_haves:
  truths:
    - "Each team row on the Volunteer -> Teams tab shows a trailing > chevron that opens a right-side slide-over."
    - "The slide-over lets a planner multi-select 1st through 5th Sunday and clear back to none, bound to the team's recurrence ordinals."
    - "Saving the slide-over persists the selected ordinals to the team via teamsStore.updateTeam(id, { recurrence })."
    - "Reopening a team's slide-over shows that team's previously saved ordinals."
  artifacts:
    - "src/components/TeamRecurrenceSlideOver.vue — Teleported right drawer mirroring SongSlideOver's shell"
    - "src/components/TeamsConfigPanel.vue — per-row > chevron opening the slide-over"
    - "src/components/__tests__/TeamsConfigPanel.test.ts — slideout save round-trip coverage"
  key_links:
    - "TeamsConfigPanel row chevron -> TeamRecurrenceSlideOver(open, team)"
    - "TeamRecurrenceSlideOver Save -> teamsStore.updateTeam(id, { recurrence: { ordinals } })"
    - "team.recurrence (from 86-01) seeds the slide-over on open (round-trip display)"
---

<objective>
Deliver R254: the configuration UI for team recurrence.

Add a per-row `>` chevron to `TeamsConfigPanel.vue` (Volunteer -> Teams) that opens a
Teleported right-side slide-over mirroring `SongSlideOver.vue`'s shell. The slide-over body
is a multi-select of the five Sunday ordinals (1st-5th) plus a clear/none state, bound to the
team's `recurrence.ordinals`, and Save persists via the existing `teamsStore.updateTeam(id, { recurrence })`.

Purpose: Lets a planner give a team its Nth-Sunday-of-month pattern from the exact slideout
pattern the Song table already uses (the owner's referenced UX), and shows the saved pattern
on reopen. Consumes the `recurrence` field and matching semantics from 86-01.
Output: new `TeamRecurrenceSlideOver.vue`, chevron wiring in `TeamsConfigPanel.vue`, and a
save round-trip test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-recurring-team-scheduling/86-CONTEXT.md

# The Teleported right-drawer shell to MIRROR (Teleport to body, translate-x-full, scrim, header + close/save)
@src/components/SongSlideOver.vue
# The Teams tab rows to add the chevron + slide-over to; updateTeam already persists partial patches
@src/components/TeamsConfigPanel.vue
@src/components/__tests__/TeamsConfigPanel.test.ts
# recurrence type shape (from 86-01)
@src/types/team.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeamRecurrenceSlideOver component (mirrors SongSlideOver shell)</name>
  <files>src/components/TeamRecurrenceSlideOver.vue</files>
  <read_first>src/components/SongSlideOver.vue (Teleport to body, backdrop Transition, translate-x-full panel Transition, fixed inset-y-0 right-0 panel, header with Cancel/Save/close), src/types/team.ts (Team + recurrence), src/stores/teams.ts (updateTeam(id, patch) partial updateDoc)</read_first>
  <action>
    Create src/components/TeamRecurrenceSlideOver.vue mirroring SongSlideOver.vue's drawer shell
    per the CONTEXT "Slideout UX" decision: `Teleport to="body"`, an opacity-transitioned scrim
    (`fixed inset-0`) that closes on click, and a transform-transitioned panel
    (`fixed inset-y-0 right-0`, enter/leave `translate-x-full` <-> `translate-x-0`) with a header
    row containing the team name, a Save button, and a close (X) button. Props: `open: boolean`,
    `team: Team | null`. Emits: `close`, `saved`. Body: five toggle controls labelled 1st / 2nd /
    3rd / 4th / 5th Sunday, each toggling membership of a value 1-5 in a LOCAL editable ordinals
    array (a copy, not the store object), plus a Clear/None action that empties the selection. On
    open (watch props.open), seed the local ordinals from props.team?.recurrence?.ordinals (default
    to empty when absent) and sort ascending — this is what makes a reopened slide-over show the
    saved pattern (R254 round-trip). Save handler: call the injected teams store
    updateTeam(team.id, { recurrence: { ordinals: <sorted local copy> } }) — an empty array persists
    as { ordinals: [] } (CONTEXT: empty means no auto-select; no deleteField needed), then emit
    `saved`. Cancel/close/scrim emit `close` without persisting. Import useTeamsStore from
    @/stores/teams and the Team type from @/types/team. Do NOT add a second recurrence pattern kind
    ("every N weeks" is dropped this phase). Keep the visual language consistent with SongSlideOver
    (gray-900 panel, indigo Save, gray borders); the exact toggle styling is Claude's discretion.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>TeamRecurrenceSlideOver.vue exists, mirrors the SongSlideOver Teleport/transition/header shell, exposes open+team props and close+saved emits, seeds ordinals from team.recurrence on open, and Save calls updateTeam with { recurrence: { ordinals } }.</done>
</task>

<task type="auto">
  <name>Task 2: Per-row > chevron opening the slide-over in TeamsConfigPanel</name>
  <files>src/components/TeamsConfigPanel.vue</files>
  <read_first>src/components/TeamsConfigPanel.vue (per-row flex with Save Team / Delete buttons; teamsStore already imported), src/components/SongSlideOver.vue (chevron/trailing-affordance reference)</read_first>
  <action>
    In TeamsConfigPanel.vue add a trailing `>` chevron button to each team row (in the same flex
    row as Save Team / Delete), with an accessible per-row aria-label naming the team (e.g.
    "Edit recurring schedule for <name>"), matching the CONTEXT reference to "the slideout with >
    at the end like the Song table has". Clicking it sets a reactive `slideoverTeam` (or
    `slideoverTeamId`) to that row's team. Import and mount TeamRecurrenceSlideOver once (outside
    the row loop), bound to `:open="!!slideoverTeam"` and `:team="slideoverTeam"`, with
    `@close` clearing the selection and `@saved` clearing it (the store's onSnapshot refreshes the
    list; recurrence is not part of the inline name draft so no draft reconciliation is needed).
    Do NOT alter the existing name-edit draft, rename soft-warn (WR-02), duplicate-name guard
    (WR-01), delete confirm, or Add-Team behaviour — this is additive. The chevron is a sibling
    affordance, not a replacement for any existing button.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <done>Each team row renders a > chevron with a per-team aria-label; clicking it opens TeamRecurrenceSlideOver for that team; existing row behaviours are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Slideout save round-trip test</name>
  <files>src/components/__tests__/TeamsConfigPanel.test.ts</files>
  <read_first>src/components/__tests__/TeamsConfigPanel.test.ts (mockTeams + mockUpdateTeam vi.mock of @/stores/teams; mountPanel; existing button-text query helpers)</read_first>
  <behavior>
    - Each team row exposes a > chevron control (found by its per-team aria-label)
    - Clicking a team's chevron opens the slide-over showing that team's currently saved ordinals as selected (a team seeded with recurrence.ordinals [1,3] shows 1st and 3rd Sunday active on open)
    - Toggling ordinals and clicking Save calls teamsStore.updateTeam with the team id and { recurrence: { ordinals: <sorted selection> } }
    - Clearing to none then Save persists { recurrence: { ordinals: [] } }
    - A team opened with no recurrence starts with nothing selected
  </behavior>
  <action>
    Extend src/components/__tests__/TeamsConfigPanel.test.ts (RED first). The file already mocks
    @/stores/teams with mockUpdateTeam and a mockTeams array — the same mock is shared by the
    slide-over, so add a recurrence to at least one seeded team (e.g. Choir ordinals [1,3]) and,
    if the Teleport confuses queries, stub Teleport in mountPanel's global.stubs the same way
    NewServiceDialog.test.ts does. Add tests that: (1) find a row's chevron by aria-label and click
    it; (2) assert the slide-over renders with that team's saved ordinals pre-selected; (3) toggle
    the selection, click Save, and assert mockUpdateTeam was called once with the team id and
    { recurrence: { ordinals: [...] } } (sorted); (4) assert clearing to none then saving persists
    an empty ordinals array. Keep all existing TeamsConfigPanel tests passing unchanged. Use the
    existing button-text / aria-label query style already in the file.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/TeamsConfigPanel.test.ts</automated>
  </verify>
  <done>The chevron opens the slide-over, saved ordinals round-trip on open, Save calls updateTeam with { recurrence: { ordinals } } (including the empty-clear case), and all pre-existing TeamsConfigPanel tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client -> Firestore | Save writes team.recurrence via the existing org-scoped teamsStore.updateTeam path (organizations/{org}/teams/{id}); no new collection, endpoint, or rules change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-03 | Tampering | recurrence write payload | low | mitigate | The slide-over only emits integers 1-5 from fixed toggle controls; ordinals are sorted and de-duplicated before persist, so no free-form value reaches the doc. |
| T-86-04 | Elevation of Privilege | teams subcollection write | low | accept | updateTeam reuses the existing org-membership Firestore rule that already gates every team write; this plan adds no new write path and no rules change. |
| T-86-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan (existing Vue/Pinia/Tailwind only). No supply-chain surface introduced. |
</threat_model>

<verification>
- `npm run type-check` (vue-tsc --build) passes.
- `npx vitest run src/components/__tests__/TeamsConfigPanel.test.ts` passes.
- Manual sanity (optional): a saved pattern reappears when the slide-over is reopened.
</verification>

<success_criteria>
- A planner can open a `>` slide-over per team on the Volunteer -> Teams tab and multi-select
  1st-5th Sunday (or clear to none), matching the Song-table slideout pattern (R254).
- Save persists the ordinals via teamsStore.updateTeam(id, { recurrence }); reopening shows the
  saved pattern (R254 round-trip).
- All existing TeamsConfigPanel behaviours (rename/delete/add/guards) remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/86-recurring-team-scheduling/86-02-SUMMARY.md` when done
</output>
