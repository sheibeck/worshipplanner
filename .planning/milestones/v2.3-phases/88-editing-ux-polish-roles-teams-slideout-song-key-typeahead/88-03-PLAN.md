---
phase: 88-editing-ux-polish-roles-teams-slideout-song-key-typeahead
plan: 03
type: execute
wave: 2
depends_on: ["88-02"]
files_modified:
  - src/components/RolesConfigPanel.vue
  - src/components/__tests__/RolesConfigPanel.test.ts
  - src/components/TeamsConfigPanel.vue
  - src/components/__tests__/TeamsConfigPanel.test.ts
  - src/views/RosterView.vue
  - src/views/__tests__/RosterView.test.ts
  - src/components/TeamRecurrenceSlideOver.vue
autonomous: true
requirements: [R257]
must_haves:
  truths:
    - "The Roles tab renders read-only rows (name · group badge · default count · vocal marker) still grouped under Band/Tech/Other headers, each with a trailing `>` chevron; clicking a row opens RoleSlideOver on that role, and a header '+ Add role' opens it in create mode (R257)."
    - "The Teams tab renders read-only rows (name · recurrence summary) each with a trailing `>` chevron; clicking a row opens TeamSlideOver on that team, and a header '+ Add team' opens create mode; recurrence is edited inside that one slideout (R257)."
    - "No inline editing affordance remains in either panel — the always-live inputs, inline Save buttons, and inline delete/rename-confirm blocks are gone; add/edit/delete happen only through the slideout (R257)."
    - "RosterView owns selectedRole/selectedTeam + open state and mounts both slideouts, mirroring how SongsView owns selectedSong + mounts SongSlideOver (R257)."
    - "TeamRecurrenceSlideOver.vue is deleted — recurrence editing lives only in TeamSlideOver."
  artifacts:
    - "Refactored src/components/RolesConfigPanel.vue — read-only grouped rows + `@edit`/`@add` emits"
    - "Refactored src/components/TeamsConfigPanel.vue — read-only flat rows + `@edit`/`@add` emits, no recurrence drawer import"
    - "src/views/RosterView.vue — selectedRole/selectedTeam ownership + mounted RoleSlideOver/TeamSlideOver"
    - "(removed) src/components/TeamRecurrenceSlideOver.vue"
  key_links:
    - "RolesConfigPanel @edit(role) / @add ↔ RosterView onEditRole/onAddRole ↔ RoleSlideOver :open/:role"
    - "TeamsConfigPanel @edit(team) / @add ↔ RosterView onEditTeam/onAddTeam ↔ TeamSlideOver :open/:team"
    - "RosterView.test.ts stale 'Roles config' assertion updated to match the read-only reality"
---

<objective>
Make the Volunteer → **Roles** and **Teams** tabs match the Songs editing pattern: read-only rows that
open a right-side slideout on click. Refactor `RolesConfigPanel.vue` and `TeamsConfigPanel.vue` into
pure read-only lists that emit `@edit(item)` / `@add`, move slideout ownership into `RosterView.vue`
(mirroring `SongsView` + `SongSlideOver`), mount the Plan-02 `RoleSlideOver`/`TeamSlideOver`, and delete
the now-absorbed `TeamRecurrenceSlideOver.vue` (R257).

Purpose: Close the v2.3 UAT finding that Roles/Teams deviate from every other table's edit UX.
Output: two read-only panels, RosterView as the slideout owner, the recurrence-only drawer removed, and
updated tests — including fixing the pre-existing stale `RosterView.test.ts` assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-editing-ux-polish-roles-teams-slideout-song-key-typeahead/88-CONTEXT.md

# The reference pattern: parent owns selectedSong + open-on-row-click + New = create mode + mounts SongSlideOver (Teleported)
@src/views/SongsView.vue
# The panels being refactored (their current inline editors are removed; keep header copy + group headers)
@src/components/RolesConfigPanel.vue
@src/components/TeamsConfigPanel.vue
# The view that mounts both panels and becomes the slideout owner (its Volunteer drawer + seed/subscribe lifecycle stays)
@src/views/RosterView.vue
# The slideouts to mount (built in Plan 02) — props/emits contract
@src/components/RoleSlideOver.vue
@src/components/TeamSlideOver.vue
# The existing tests to update (inline-edit assertions move out; keep the R246/R256 copy tests in RolesConfigPanel.test.ts)
@src/components/__tests__/RolesConfigPanel.test.ts
@src/components/__tests__/TeamsConfigPanel.test.ts
@src/views/__tests__/RosterView.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RolesConfigPanel → read-only grouped rows + @edit/@add</name>
  <files>src/components/RolesConfigPanel.vue, src/components/__tests__/RolesConfigPanel.test.ts</files>
  <behavior>
    - Renders one read-only row per role, still grouped under the Band/Tech/Other headers, showing name, a group badge, the default count, and a vocal marker only when role.vocal is true — no <input>/<select>/<textarea> anywhere in the panel.
    - Clicking a role row emits 'edit' with that role object.
    - A header '+ Add role' button emits 'add' (no payload).
    - The R246/R256 header description copy is preserved (the accurate 'auto-fills … each service' wording), and the deprecated soft-target framing is still absent.
  </behavior>
  <action>
    Refactor `RolesConfigPanel.vue` to a read-only presenter. Keep the outer card, the "Roles" header, and
    its existing description paragraph EXACTLY as-is (that copy is asserted by the R246/R256 tests — do not
    reword it, and do not reintroduce the deprecated default-count framing those tests negative-assert).
    Keep the Band/Tech/Other group sections and their group badges (owner decision: grouping stays).

    Within each group, replace the current editable row (name input, group select, count input, vocal
    checkbox, Save/Delete buttons, and inline delete-confirm) with a single clickable read-only row:
    role name, the group badge (reuse `groupBadgeClasses`), the default count as muted text (e.g. `Default {n}`),
    a small "Vocal" marker pill rendered `v-if="role.vocal"`, and a trailing `>` chevron (reuse the SVG chevron
    markup from RosterView's volunteer rows / TeamsConfigPanel). The row is a `<button>` or a
    `role`/`tabindex`+keydown element with `@click="emit('edit', role)"` and an `aria-label` naming the role.
    Add a "+ Add role" button in the panel header wired to `@click="emit('add')"`.

    Delete the entire `<script setup>` machinery that no longer applies: roleDrafts + its watcher,
    onSaveRole, savingRoleId/savedRoleId timers, confirmDeleteId/onConfirmDelete, and the whole Add-Role
    form state (newRoleName/newRoleGroup/newRoleCount/newRoleVocal/onAddRole). The panel no longer imports
    or calls `rosterStore.addRole/updateRole/deleteRole` (that moved to RoleSlideOver in Plan 02). Declare
    `defineEmits<{ edit: [role: Role]; add: [] }>()`. It still reads `rosterStore.roles` for display via the
    existing `groupedRoles` computed.

    Rewrite `src/components/__tests__/RolesConfigPanel.test.ts`: KEEP the two header-copy tests (R246 and
    R256) unchanged — they still pass against the preserved header. REMOVE the inline edit/save/delete/add
    tests (that behavior now lives in RoleSlideOver.test.ts). ADD: (a) no form controls render
    (`wrapper.findAll('input, select, textarea').length === 0`); (b) clicking a role row emits `edit` with
    the matching role; (c) a "+ Add role" affordance emits `add`; (d) a role with `vocal:true` shows the
    Vocal marker and one without does not.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/RolesConfigPanel.test.ts</automated>
  </verify>
  <done>RolesConfigPanel is read-only (zero form controls), keeps the group headers + R246/R256 copy, and emits edit/add; its suite passes; `npm run type-check` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TeamsConfigPanel → read-only rows + recurrence summary + @edit/@add, drop recurrence drawer</name>
  <files>src/components/TeamsConfigPanel.vue, src/components/__tests__/TeamsConfigPanel.test.ts</files>
  <behavior>
    - Renders one read-only row per team (flat list, ordered), showing the team name and a recurrence summary (e.g. '1st & 3rd Sun' for ordinals [1,3], or '—' when none), each with a trailing `>` chevron — no <input> anywhere in the panel.
    - Clicking a team row emits 'edit' with that team object.
    - A header '+ Add team' button emits 'add'.
    - The empty state ('No teams yet.') still renders when there are zero teams.
    - The panel no longer imports or mounts TeamRecurrenceSlideOver.
  </behavior>
  <action>
    Refactor `TeamsConfigPanel.vue` to a read-only presenter. Keep the outer card + "Teams" header +
    description. Replace each editable row (name input, Save/Delete buttons, rename/delete confirm blocks,
    and the recurrence `>` chevron that opened TeamRecurrenceSlideOver) with a single clickable read-only
    row: team name, a recurrence summary, and a trailing `>` chevron; the row wires `@click="emit('edit',
    team)"` with an `aria-label` naming the team. Add a "+ Add team" header button wired to
    `@click="emit('add')"`. Preserve the zero-teams empty state copy.

    Add a `formatRecurrence(ordinals?: number[]): string` helper: map 1→'1st',2→'2nd',3→'3rd',4→'4th',
    5→'5th', join sorted+deduped with ', ' and ' & ' before the last, suffix ' Sun'; return '—' for
    empty/absent (e.g. [1,3] → '1st & 3rd Sun', [1,2,3] → '1st, 2nd & 3rd Sun').

    Remove the now-dead `<script setup>`: the `import TeamRecurrenceSlideOver` and its `<TeamRecurrenceSlideOver>`
    mount, teamDrafts + watcher, onSaveTeam, savingTeamId/savedTeamId, confirmRenameId, confirmDeleteId/
    onConfirmDelete, slideoverTeam, the Add-Team form state (newTeamName/adding/onAddTeam), and
    `isDuplicateName` (the dup guard now lives in TeamSlideOver, Plan 02). The panel no longer imports/calls
    `teamsStore.addTeam/updateTeam/deleteTeam` or `useToasts`. Declare `defineEmits<{ edit: [team: Team];
    add: [] }>()`; keep reading `teamsStore.teams` for display.

    Rewrite `src/components/__tests__/TeamsConfigPanel.test.ts`: REMOVE the inline name-edit, Save, rename
    soft-warn, delete-confirm, add-team, dup-name, in-flight, and R254 recurrence-editing tests (recurrence
    round-trip now lives in TeamSlideOver.test.ts). ADD: (a) one read-only row per team, ordered, with no
    `<input>`; (b) a row shows the recurrence summary '1st & 3rd Sun' for a team with ordinals [1,3] and '—'
    for one with none; (c) clicking a row emits `edit` with the team; (d) '+ Add team' emits `add`; (e) the
    empty state renders for zero teams. Drop the Teleport stub if no longer needed.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/TeamsConfigPanel.test.ts</automated>
  </verify>
  <done>TeamsConfigPanel is read-only with a recurrence summary + chevron, emits edit/add, no longer references TeamRecurrenceSlideOver; suite passes; `npm run type-check` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RosterView owns the slideouts + mount them + delete TeamRecurrenceSlideOver + fix stale test</name>
  <files>src/views/RosterView.vue, src/views/__tests__/RosterView.test.ts, src/components/TeamRecurrenceSlideOver.vue</files>
  <behavior>
    - RosterView holds selectedRole/roleSlideOpen and selectedTeam/teamSlideOpen refs; '+ Add' from a panel opens the matching slideout in create mode (selected=null), a row @edit opens it on that item.
    - RoleSlideOver and TeamSlideOver are mounted from RosterView (Teleported) and start closed; their close/saved/deleted events close the slideout.
    - The Roles and Teams tab wrappers still render RolesConfigPanel / TeamsConfigPanel and are width-constrained (existing R244 behavior preserved).
    - src/components/TeamRecurrenceSlideOver.vue no longer exists.
  </behavior>
  <action>
    In `RosterView.vue`, add slideout ownership mirroring `SongsView.vue`'s selectedSong/slideOverOpen: refs
    `selectedRole = ref<Role | null>(null)`, `roleSlideOpen = ref(false)`, `selectedTeam = ref<Team | null>(null)`,
    `teamSlideOpen = ref(false)`. Handlers: `onAddRole()` sets selectedRole=null + roleSlideOpen=true;
    `onEditRole(role)` sets selectedRole=role + roleSlideOpen=true; `onAddTeam()`/`onEditTeam(team)` the same
    for teams; a shared close sets the open flag false. Import `Role` from `@/types/roster` (RoleGroup is
    already imported) and `Team` from `@/types/team`, plus `RoleSlideOver` and `TeamSlideOver`.

    In the template, wire the panels: `<RolesConfigPanel @edit="onEditRole" @add="onAddRole" />` and
    `<TeamsConfigPanel @edit="onEditTeam" @add="onAddTeam" />` (keep the surrounding `max-w-4xl` tab
    wrappers). Mount both slideouts near the existing Volunteer drawer / Teleport region:
    `<RoleSlideOver :open="roleSlideOpen" :role="selectedRole" @close="roleSlideOpen = false"
    @saved="roleSlideOpen = false" @deleted="roleSlideOpen = false" />` and the TeamSlideOver equivalent
    with `:team="selectedTeam"`. The existing subscribe/seed lifecycle for roster + teams stores stays
    unchanged (the slideouts read those same stores).

    Delete `src/components/TeamRecurrenceSlideOver.vue` (its body was absorbed into TeamSlideOver in Plan 02;
    grep the repo to confirm no other importer remains before deleting).

    Update `src/views/__tests__/RosterView.test.ts`: (1) FIX the stale assertion in the
    'collapsible dense sections (R-11)' describe — the current expectation that the rendered text contains
    'Roles config' is false (RolesConfigPanel is stubbed and the tab label is just 'Roles'); replace that
    test with an accurate one (e.g. assert the Roles tab button renders, or fold it into the existing R244
    width-constraint assertion) so the file no longer carries a knowingly-failing case. (2) Add
    `RoleSlideOver` and `TeamSlideOver` to the component stubs in `mountRosterView` (RosterView now imports
    them). (3) Add a wiring test: stub RolesConfigPanel/TeamsConfigPanel so they can `$emit('add')` and
    `$emit('edit', item)`, and assert the corresponding slideout stub receives `open=true` (and the expected
    `role`/`team` prop) after the emit — proving RosterView owns and opens the slideouts. Keep all other
    existing RosterView tests passing.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/RosterView.test.ts</automated>
  </verify>
  <done>RosterView owns + mounts both slideouts and opens them from panel edit/add; TeamRecurrenceSlideOver.vue is deleted with no dangling import; the stale 'Roles config' assertion is fixed; RosterView suite + `npm run type-check` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (roles/teams subcollections) | This plan only relocates WHERE the edit UI lives (read-only row → slideout); the actual writes still go through the same store ops governed by the same org-scoped rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-04 | Tampering | Team edit path after removing the panel's dup guard | medium | mitigate | The WR-01 duplicate-name guard moved into TeamSlideOver (Plan 02, verified there); removing it from the panel does not drop the protection because the panel no longer writes. Confirm TeamSlideOver is the sole team write path before deleting the panel logic. |
| T-88-05 | Denial of Service | Deleting TeamRecurrenceSlideOver.vue | low | mitigate | Grep for remaining importers before delete so no view fails to resolve the module at build; `npm run type-check` + the RosterView suite are the gate. |
| T-88-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependencies installed — pure component/view refactor. No package legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/RolesConfigPanel.test.ts src/components/__tests__/TeamsConfigPanel.test.ts src/views/__tests__/RosterView.test.ts` — all green.
- `npm run type-check` (vue-tsc --build) — clean, including test files (per CLAUDE.md).
- Full app suite `npx vitest run` — only the documented `src/storage.rules.test.ts` baseline remains failing (RosterView.test.ts's stale assertion is fixed by this plan; do NOT use `--dir src`).
- Grep confirms no remaining import of `TeamRecurrenceSlideOver` and no `<input>/<select>` inside RolesConfigPanel.vue / TeamsConfigPanel.vue templates.
</verification>

<success_criteria>
The Roles and Teams tabs are read-only rows that open RoleSlideOver / TeamSlideOver on click (add via a
header '+ Add'), Roles keeps its Band/Tech/Other group headers, no inline editing remains, RosterView owns
the slideout state exactly like SongsView, TeamRecurrenceSlideOver is gone, and the previously-stale
RosterView test is corrected.
</success_criteria>

<output>
Create `.planning/phases/88-editing-ux-polish-roles-teams-slideout-song-key-typeahead/88-03-SUMMARY.md` when done.
</output>
