---
milestone: v2.9
milestone_name: Live Presentation Field Fixes
audited: 2026-09-04
status: passed
closure_state: code_complete_pending_batched_uat
scores:
  requirements: 15/15
  phases: 3/3
  integration: 11/11 seams WIRED
  flows: no broken flows
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
deferred_verification:
  policy: "Owner instruction 2026-09-03 — run v2.9 autonomously, defer UAT to milestone end (.planning/v2.9-DEFERRED-VERIFICATION.md)"
  batched_items:
    - phase: 114
      items: ["N-monitor listing + roles stick (no false reprompt)", "per-display fullscreen placement without dragging", "nicknames persist"]
    - phase: 115
      items: ["projector WYSIWYG at projection distance", "macOS overlay-scrollbar stays visible", "at-a-glance filmstrip/preview legibility"]
    - phase: 116
      items: ["edit-link opens a new tab on the Lyrics sub-tab", "SongSelect link resolves to the correct CCLI page", "credits editor round-trip vs real Firestore, lyric sections unaffected"]
---

# v2.9 Milestone Audit — Live Presentation Field Fixes

**Audited:** 2026-09-04
**Status:** ✅ passed (code-complete) — **batched human/hardware UAT still pending** (not yet owner-closed)
**Milestone:** v2.9 (Phases 114–116), requirements R324–R338.

## Definition of Done

Fix the readability, multi-monitor, and lyric-editing rough edges surfaced by the first real
church-projector run, so a projectionist can set up and read the live output without fighting the tool —
no new feature areas, all internal fixes/polish to the existing v2.4 "Run the Service" + v2.7
presentation/lyric-editor features.

## Result Summary

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | **15/15** | Every R324–R338 mapped to exactly one phase, marked Complete in traceability, and code-verified in its phase VERIFICATION.md |
| Phases | **3/3** | 114, 115, 116 all executed + code-reviewed + auto-verified green |
| Integration | **11/11 WIRED** | Cross-phase seams verified; the 114↔115 output-surface seam traced end-to-end |
| Broken flows | **0** | No BLOCKERs, no WARNINGs at the integration level |
| Gaps | **0** | No unsatisfied/orphaned requirements |
| Tech debt | **0 new** | (pre-existing backlog 999.13 audio/media cluster remains explicitly out of scope) |

## Requirements Coverage (3-source cross-reference)

All 15 requirements are satisfied at code level — each has a phase VERIFICATION.md truth verified against
the actual codebase, a SUMMARY `requirements-completed` entry, and a `Complete` traceability row.

| Phase | Requirements | Verification | Integration |
|-------|--------------|--------------|-------------|
| 114 Multi-Monitor Assignment Rework | R324, R325, R326, R327, R328, R338 | human_needed — 100% code must-haves verified | WIRED |
| 115 Live-Output Readability & Layout | R329, R330, R331, R332 | human_needed — 17/17 code must-haves verified | WIRED |
| 116 Lyric Editor & Song UX | R333, R334, R335, R336, R337 | human_needed — 6/6 code truths verified | WIRED (independent) |

## Cross-Phase Integration

- **114 ↔ 115 (primary seam) — WIRED.** Phase 114's Run-control → output-window plumbing (monitor
  fingerprint hand-off via `SCREEN_QUERY_PARAM`, `useOutputWindow` fullscreen/blackout/re-enter) and
  Phase 115's auto-fit output views (`useContainScale` stage + `SlideCanvas` `--slide-fit-scale`) coexist
  on the same output DOM node via a merged function-ref (`setRootRefs`). Phase 115 did not touch or shadow
  Phase 114's placement logic; the Confidence-view runtime-blackout invariant (R305 guard) is intact. The
  `--slide-font-scale` removal chain (115-03 → 115-04 → 115-05) is complete with no reader stranded
  mid-sequence.
- **116 — independent silo, CONFIRMED.** No file or symbol overlap with 114/115; no cross-phase
  touchpoint expected or needed. No regression risk.

## Build Coherence

- `npm run type-check` (`vue-tsc --build`): clean (verified directly during audit).
- Full `npx vitest run`: documented baseline throughout — 185/186 files, only `src/storage.rules.test.ts`
  failing (Storage-emulator env limitation per CLAUDE.md). Reconfirmed across every plan's SUMMARY.
- Code review + fix ran on 115 (5 findings → 4 fixed, 1 documented skip) and 116 (3 findings → 3 fixed,
  incl. the WR-02 in-place-save that prevents invisible lyric-version bloat).

## Closure State — CODE-COMPLETE, NOT YET OWNER-CLOSED

The milestone is **code-complete and audit-clean**, but under the owner's explicit 2026-09-03 instruction
("run v2.9 autonomously, defer UAT to the end") all three phases' human/hardware verification is **batched**
into `.planning/v2.9-DEFERRED-VERIFICATION.md` for a single owner pass and has **not yet been run**. In
addition, v2.9 is a client-side (hosting) change set that is **not yet deployed** to production.

Therefore this audit does **not** auto-advance to `complete-milestone`/archive. Full closure requires the
owner to:
1. Run the batched UAT (`.planning/v2.9-DEFERRED-VERIFICATION.md`) — 9 checks across 114/115/116.
2. Decide on the production **hosting deploy** (per the confirm-before-deploy policy — Claude will not
   deploy without explicit per-deploy confirmation).
3. Then close/archive the milestone (`/gsd-complete-milestone v2.9`).

No gaps block that closure — the remaining work is verification and release, not implementation.
