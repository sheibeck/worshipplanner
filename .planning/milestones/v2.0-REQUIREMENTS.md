# Requirements — v2.0 Multi-Church Onboarding & Owner Console Tabs (Phases 72–74)

**Archived 2026-08-23** on milestone close. Full phase detail and success criteria live in
[v2.0-ROADMAP.md](v2.0-ROADMAP.md); this file is the requirement-level record.

**Coverage:** R193–R211 (19 requirements, 100% mapped) — all satisfied, code-complete + auto-verified
+ SECURED (Phases 73, 74), deployed to production 2026-08-23.

- **R193–R195 (Phase 72 — Owner Console Tabs):** tabbed Configuration/Organizations shell with the
  pre-v2.0 console body relocated byte-for-byte, super-admin-gated, tab reflected in the route query.
- **R196–R206 (Phase 74 — Organizations: List, Onboard & Admin Assignment):** super-admin-gated
  `listOrganizations` (server `count()` summaries), `onboardOrganization` (atomic org + default
  `OrgSettings` + seeded service template + first admin at editor tier), and `assignOrgAdmin`
  (additive `arrayUnion` membership or invite) — all pure server callables; the client never writes
  `organizations/*`, `orgNames/*`, or another org's `members/*`; duplicate-name and unknown-email
  failures never strand a half-created org.
- **R207–R211 (Phase 73 — Multi-Org Storage Auth Claim, backlog 999.5):** the org-membership claim
  widened to an additive `orgs: {[orgId]: role}` map (recomputed from `collectionGroup('members')`,
  superAdmin-preserving), `storage.rules`' `isOrgMemberByClaim` widened with a null-guarded `orgs` arm
  ORed with the legacy primary-only arm (no access gap during rollout), and an idempotent
  dry-run/`--apply` backfill.

**Deploy (owner-run 2026-08-23):** widened `syncOrgMembershipClaim` + the three callables via
`firebase deploy --only functions`; `storage.rules` via `firebase deploy --only storage`; multi-org
claim backfill via `node lib/backfillOrgClaims.js --apply` (3 accounts processed, idempotent-verified).

**Deferred (owner-accepted, preserved in PENDING-VERIFICATION.md):** human UAT `/gsd-verify-work 72–74`.
