---
phase: 28-song-lyrics-editor-rework-risk-low
plan: 06
type: execute
wave: 6
depends_on: ["28-03", "28-05"]
files_modified:
  - src/components/__tests__/SongLyricsTab.r035.test.ts
  - src/components/__tests__/SongSlideOver.test.ts
autonomous: true
requirements: [R035, R018]

must_haves:
  truths:
    - "R035's two conditions are asserted, not eyeballed: the Lyrics tab has no nested scrollbar, and it presents exactly one list."
    - "That single list IS the slide order — its rendered row sequence equals the order the document stores and the order the slideshow assembles from."
    - "Phase 26's Edit-in-song link still lands: a link built for a song's lyrics parses back to that song and that tab, and the slide-over opens on the Lyrics tab when handed it."
    - "D006 survives: lyrics still enter the app by pasting CCLI SongSelect text, section markers are still parsed, and a repeated marker enters as a repeat rather than a copy."
    - "A lyrics edit still reaches every service referencing that song — slide entries hold references, and the text they resolve to is the text just edited (D002/D007, Phase 24 D-02)."
    - "The phase leaves the tree green: type-check at 0, a successful production build, and a full unit run failing in the same ten baseline files and no more."
  artifacts:
    - "src/components/__tests__/SongLyricsTab.r035.test.ts — the requirement's acceptance block, named for what it proves"
    - "28-06-SUMMARY.md recording the goal-backward check for every must-have across the phase and the outstanding human-verify items"
  key_links:
    - "The acceptance test must count scroll containers over the MOUNTED tab subtree, not over the editor component alone — the defect R035 names was a panel scrolling inside a panel, which only appears once both are mounted together."
    - "Counting lists means counting the ordered row containers the tab renders. Phase 18 shipped two; 2a ships one. A count is the assertion; a snapshot is not."
    - "The Edit-in-song chain has three links — the pure link builder, the songs view that parses an arriving query, and the slide-over's opening-tab prop. This plan asserts the ends; the middle is Phase 26's and is unchanged."
    - "The baseline is a FILE SET, not a count. Deleting the Phase 18 builder's test file in 28-02 legitimately lowered the passing test count; the gate is that the failing file set has not grown past the ten known files."
---

<objective>
Close the phase by proving R035 rather than asserting it, and by confirming the four things
28-CONTEXT.md said must not break are still working after the rework.

Purpose: R035 is a quality-attribute requirement whose success condition — one scroll surface, one
list — is unusually concrete. 28-CONTEXT.md calls it out as worth asserting rather than eyeballing.
This plan does that, then runs the full gate for the last phase of the milestone.
Output: an acceptance test named for the requirement, a green tree, and a SUMMARY carrying the
phase's goal-backward check and the human-verify items that join the milestone's batch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-song-lyrics-editor-rework-risk-low/28-CONTEXT.md
@.planning/phases/28-song-lyrics-editor-rework-risk-low/28-04-SUMMARY.md
@.planning/phases/28-song-lyrics-editor-rework-risk-low/28-05-SUMMARY.md

@src/components/SongSlideOver.vue
@src/components/SongLyricEditor.vue
@src/utils/songEditLink.ts
@src/utils/ccliParser.ts
</context>

<interfaces>
**R035, verbatim:** the song lyrics editor presents one scroll surface and one list that IS the slide
order — no nested scrollbar, no duplicated Available-Sections / Performance-Order lists.

**The four hard constraints from `28-CONTEXT.md` this plan re-checks:**

1. Phase 26's Edit-in-song link — `src/utils/songEditLink.ts` builds a query, `SongsView.vue` parses
   it, `SongSlideOver.vue` opens on the requested tab via `initialTab`.
2. D006 — manual CCLI copy/paste with section-marker parsing. CCLI has no API, so this is how lyrics
   enter the app at all.
3. D002/D007 — one canonical lyric version; services reference it live and never copy it.
4. The Slides tab's live rendering — a lyrics edit must still flow to every service referencing the
   song, and Phase 26-09's reconciliation fix must still hold. 28-03 covered the reconciler; this
   plan covers the end-to-end statement.

**Baseline for the gate, from the phase brief:** a full `npx vitest run src/` currently fails in
exactly ten FILES — eight stale duplicates under the quarantined worktrees directory, the storage
rules suite which needs an emulator that is deliberately not running, and one roster view suite with a
stale assertion. Judge by file set. Never run the rules suite, never restart the emulator.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The R035 acceptance block</name>
  <files>src/components/__tests__/SongLyricsTab.r035.test.ts</files>
  <behavior>
    - Mounting the slide-over on the Lyrics tab with a lyrics document present yields exactly one
      element in that subtree declaring vertical overflow scrolling — no nested scrollbar.
    - That same subtree renders exactly one ordered row container — one list, where Phase 18 rendered
      two.
    - The rendered row sequence, read top to bottom, equals the document's stored order element for
      element, including a repeated section appearing at each of its positions.
    - Reordering the list changes what is saved as the order, so the list is the order rather than a
      view onto it.
    - A link built for a song's lyrics parses back to the same song id and the lyrics tab; handing
      that tab to the slide-over opens it on the Lyrics tab showing the editor.
    - Parsing a CCLI paste containing section markers still yields sections, and a paste naming the
      same section twice normalises to one pooled section referenced twice.
    - Editing a section's words and then resolving the slide entries that reference it produces the
      edited words — the entries carry references, and the text is read live.
  </behavior>
  <action>
Create `src/components/__tests__/SongLyricsTab.r035.test.ts` as the requirement's acceptance block.
Name the describe block for R035 and its two conditions so a future reader can find what proves the
requirement without tracing it through component-named files.

Assert the two structural conditions over the MOUNTED tab subtree, with the slide-over open on the
Lyrics tab and a lyrics document present in the store — the defect this requirement names only exists
when the panel and the editor are mounted together. Count scroll containers by collecting elements
whose class attribute carries a vertical-overflow utility, and count lists by collecting the ordered
row containers the tab renders. Both counts are one. Write the assertions as counts with a message
naming the requirement, so a regression reads as a requirement failure rather than an anonymous
number mismatch.

Assert the list-is-the-order property twice, in both directions: the rendered sequence equals the
stored order, and a reorder changes the saved order. Include a document whose order references one
section twice so the repeat case is inside the acceptance block rather than only in unit tests.

Cover the constraint checks with focused cases rather than a second full integration: round-trip the
link builder through its parser and hand the result to the slide-over's opening-tab prop; run a
CCLI fixture through the parser and the paste normaliser together; and resolve slide entries for an
edited section to confirm the words that come back are the edited ones.

Follow the codebase's teleport testing pattern — a `DOMWrapper` over `document.body` with
`enableAutoUnmount(afterEach)`, teleport left unstubbed under `shallowMount`. Reuse fixture shapes
from the suites written in 28-04 and 28-05 rather than inventing new ones.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongLyricsTab.r035.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <done>
  The acceptance block passes: one scroll container, one list, the list equals the order in both
  directions including a repeat, the Edit-in-song round trip lands, a paste still parses and
  normalises, and an edit resolves live through the slide entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase gate and goal-backward check</name>
  <files>src/components/__tests__/SongSlideOver.test.ts</files>
  <action>
Run the full gate for the last phase of the milestone.

Run `npm run type-check` and confirm 0 errors. Run `npm run build` and confirm it succeeds — the
build guard aborts when Firebase environment variables are missing, so if it fails on configuration
rather than on code, confirm `.env.local` is present in this tree before treating it as a code
failure.

Run `npx vitest run src/` in full and compare the failing FILE set against the ten baseline files.
The set must not have grown. Record the observed set in the SUMMARY by name. Note there that the
Phase 18 builder's test file was deleted in 28-02, which lowers the passing test count legitimately
and is not a regression.

Sweep `src/` for leftovers from the rework and fix or record each: no importer of the deleted builder
component, no reference to the removed Song-record order field, and no remaining caller of the
removed lyrics-store action. If any surfaces, fix it in this task and say so in the SUMMARY.

Add any assertion `src/components/__tests__/SongSlideOver.test.ts` still needs to stay consistent
with the final shape of the tab — this file is listed here so a small consistency fix has a home; if
none is needed, leave it untouched and say so.

Write `28-06-SUMMARY.md` with a goal-backward check covering every must-have listed across
28-01 through 28-06, each marked met or not met with the evidence that settles it. Include the
outstanding human-verify items below verbatim, since this milestone settles verification in one batch
rather than per phase.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npm run build</automated>
    <automated>npx vitest run src/ 2>&1 | tail -40</automated>
    <human-check>
    Deferred to the milestone's batch human-verify, alongside the outstanding Phase 20-23 and 25-27
    items: open a song's Lyrics tab and confirm one scrollbar and one list; drag a section by its
    handle and confirm it moves and stays moved; expand a section, edit a word, and confirm a
    repeated occurrence of that section shows the change too; duplicate a section and confirm the new
    row reads as a linked repeat; remove one occurrence of a repeated section and confirm the other
    keeps its words; add a section from the quick-add chips and type into it; paste a CCLI song whose
    text names the chorus more than once and confirm it arrives as one chorus referenced several
    times; open version history from the editor header and restore an earlier version; follow the
    Edit-in-song link from the Slides tab's Edit Slide drawer and confirm it lands on this editor; and
    finally open a service that uses that song and confirm the Slides tab shows the edited words in
    the order the list now has.
    </human-check>
  </verify>
  <done>
  Type-check is 0, the production build succeeds, the full run fails in exactly the ten baseline files
  and no more, the leftover sweep is clean or its fixes are recorded, and the SUMMARY carries the
  goal-backward check plus the human-verify list.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → shipped behaviour | This plan's assertions are the last automated gate before the milestone's human-verify batch; a weak assertion here is a defect that ships. |
| production build → deployed bundle | The build guard is the barrier against shipping a bundle with missing Firebase configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-20 | Repudiation | acceptance evidence | medium | mitigate | The requirement's conditions are asserted as counts in a file named for the requirement, so what proves R035 is discoverable rather than inferred from component tests. |
| T-28-21 | Tampering | baseline judged by count | medium | mitigate | The gate compares the failing FILE SET by name, and the SUMMARY records that set, so a newly failing file cannot hide behind a deletion that lowered the total. |
| T-28-22 | Information Disclosure | build with absent configuration | high | mitigate | The build guard aborts when Firebase variables are missing; the task instructs confirming the environment file before attributing a build failure to code, so the guard is never worked around. |
| T-28-23 | Denial of Service | test run touching the emulator | medium | mitigate | The rules suite is never run and the emulator is never restarted; the storage rules file stays in the accepted baseline set. |
| T-28-SC | Tampering | package installs | high | accept | This plan installs nothing; no package-manager command runs. |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/SongLyricsTab.r035.test.ts` — green.
- `npm run type-check` — 0 errors.
- `npm run build` — succeeds.
- `npx vitest run src/` — failing file set equals the ten baseline files, recorded by name in the
  SUMMARY.
</verification>

<success_criteria>
R035 is proven by assertion rather than inspection; all four hard constraints are re-confirmed; the
tree is green; and the phase's human-verify items are queued for the milestone batch.
</success_criteria>

<output>
Create `.planning/phases/28-song-lyrics-editor-rework-risk-low/28-06-SUMMARY.md` when done.
</output>
