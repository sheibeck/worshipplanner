---
phase: 24-slide-group-model-and-migration
plan: 04
type: execute
wave: 3
depends_on: ["24-01", "24-03"]
files_modified:
  - src/utils/slideshowAssembler.ts
  - src/utils/__tests__/slideshowAssembler.test.ts
  - src/types/slide.ts
  - src/composables/useSlideshowAssembly.ts
  - src/components/AudioPlayer.vue
  - src/components/__tests__/AudioPlayer.test.ts
  - src/components/PresentationViewer.vue
  - src/components/__tests__/PresentationViewer.test.ts
autonomous: true
requirements: [R028, R030, R018]

must_haves:
  truths:
    - "Assembled slides appear strictly in ServiceSlot.position order across groups, and in GroupSlideEntry.order within a group; changing a slot's position changes group order in the output while leaving every GroupSlideEntry.order value untouched (R028 ordering edge)."
    - "An assembled slide's id equals the stored GroupSlideEntry.id — never a value recomputed from slot index or emission order — so Phase 23's WR-02 keying on slide.id keeps forcing a fresh media child per slide."
    - "For two consecutive slides in the same group where the first has per-slide audio and the second has none, the first resolves to its own audioUrl and the second resolves to the group bed — the bed resumes rather than staying suppressed (R030 adjacency edge)."
    - "A group with no bedAudioUrl and no per-slide audioUrl on any entry produces assembled slides that carry no audioUrl key at all, and no audio element is rendered (R030 empty edge)."
    - "Per-slide audio applies to its own slide only regardless of that slide's position in the group, and the bed applies to every entry that has no audioUrl of its own (R030 ordering edge)."
    - "audioLoop is emitted only when the per-slide entry set it; a slide whose audio came from the group bed never carries audioLoop, because a bed does not loop (D-04)."
    - "A slot that has no group document yet assembles through a fallback derivation whose slide ids are stable across recomputes (derived from the stable slot id), so a pre-materialization render cannot churn Vue keys."
    - "PresentationViewer.test.ts stays green with no assertion weakened — the Phase 23 regression guard for this refactor."
  artifacts:
    - "src/utils/slideshowAssembler.ts refactored in place (not rewritten)"
    - "AssembledSlide carrying groupId, groupSlideId, audioFromBed, videoFromBed"
    - "SlideBase carrying audioLoop"
    - "AudioPlayer loop prop"
  key_links:
    - "AssemblyInputs.groupsBySlotId is the join between service.slots and the slideGroups collection"
    - "AssembledSlide.groupId + audioFromBed are what let PresentationViewer key a group bed to the GROUP rather than the slide, so the bed plays across the group"
  prohibitions: []
---

<objective>
Refactor `assembleSlideshow` in place so it reads stored group structure instead of deriving
everything from scratch, resolves live text through `sourceRef`, and resolves effective audio/video by
D-04's precedence rule. Then pass the one new field the players need (`loop`) and make a group bed
survive slide transitions.

Purpose: this is the change that makes R030's two audio levels real, and it is the change most likely
to break Phase 23. `PresentationViewer.test.ts` is the named regression guard.
Output: a refactored pure assembler, an additive `AssembledSlide`/`SlideBase` shape, and two small
player-side changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-slide-group-model-and-migration/24-CONTEXT.md
@.planning/phases/24-slide-group-model-and-migration/24-RESEARCH.md
@.planning/phases/24-slide-group-model-and-migration/24-PATTERNS.md
@.planning/phases/24-slide-group-model-and-migration/24-01-SUMMARY.md
@.planning/phases/24-slide-group-model-and-migration/24-03-SUMMARY.md
</context>

<hard_constraints>
From STATE.md — non-negotiable for every task in this plan:
- Do NOT run `npm run test:rules`. Do NOT start, stop, or restart the Firebase emulator.
- Do NOT run bare `npm run test:unit`. Use `npx vitest run <path>` or `npx vitest run src/`.
- Sequential main-tree on `milestone/M001`. Do not create or switch branches.
- Phase 23 is code-complete but its human-verify checkpoint is still OUTSTANDING. Every player-side
  change in this plan must be inert for data that has no slide groups, so the pending checkpoint is
  not invalidated.
</hard_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assembler reads stored groups and resolves live text via sourceRef</name>
  <files>src/utils/slideshowAssembler.ts, src/types/slide.ts, src/composables/useSlideshowAssembly.ts, src/utils/__tests__/slideshowAssembler.test.ts</files>
  <read_first>
    - src/utils/slideshowAssembler.ts lines 1-178 — the WHOLE current implementation; this is a refactor in place, not a rewrite. Preserve the file-head purity doc comment, `resolveSongOrder`, `buildCopyrightSlideContent`, the sort-by-position pairing that keeps `slotIndex` honest, and the `AssembledSlide[]` output shape
    - src/types/slide.ts — `SlideBase`, the five `Slide` variants, `AssembledSlide`, `AssembledSection`
    - src/types/slideGroup.ts and src/utils/slideGroupMaterializer.ts — `SourceRef`'s five kinds and the derivation whose per-kind ORDER this must mirror exactly
    - src/utils/__tests__/slideshowAssembler.test.ts — the builder-function convention; add `makeSlideGroup`/`makeGroupSlideEntry` builders alongside the existing ones in the same style
    - src/composables/useSlideshowAssembly.ts lines 162-171 — the single `assembleSlideshow` call site that must pass the new input
    - .planning/phases/24-slide-group-model-and-migration/24-RESEARCH.md § Pattern 5 and § Anti-Patterns
  </read_first>
  <behavior>
    - With a group present for a slot, output slide ids equal that group's `GroupSlideEntry.id` values in `order` order.
    - A `lyric` sourceRef resolves its text from `songLyricsById` at assembly time, so editing the canonical lyrics changes the assembled text with no group write (D-02).
    - A `copyright` sourceRef resolves to the same copyright content the current implementation builds.
    - A `scripture` / `imported` sourceRef resolves its inner slide by `innerSlideId` from the reading/deck in the input maps.
    - An entry whose source no longer resolves is OMITTED from the assembled output while remaining stored in the group.
    - A slot with no group in `groupsBySlotId` still assembles, via fallback derivation, to output identical to today's behaviour except for slide ids.
    - Fallback slide ids are stable across two successive calls with identical inputs.
    - `AssembledSlide` carries `groupId` and `groupSlideId` when the slide came from a stored group, and omits both on the fallback path.
    - Slides emit strictly in slot `position` order across slots and `entry.order` order within a slot.
  </behavior>
  <action>
Add `groupsBySlotId: Map<string, SlideGroup>` to `AssemblyInputs` as a REQUIRED field, then update the
single call site in `src/composables/useSlideshowAssembly.ts` to import `useSlideGroups` and pass
`slideGroupsStore.groupsBySlotId`. The store's subscription is wired in plan 24-05; until then the map
is legitimately empty and the fallback path below produces today's output, so the app is coherent at
every commit.

Refactor `assembleSlideshow` in place. Keep the existing indexed-then-sorted-by-position walk. Inside
the loop, replace the from-scratch per-kind derivation with:
1. Look up `inputs.groupsBySlotId.get(slot.id)`.
2. When a group exists, walk `group.slides` sorted ascending by `order`. For each entry, resolve its
   content from `entry.sourceRef`: `lyric` finds the section in `songLyricsById.get(songId).sections`
   and builds the same `LyricSlide` content the current code builds; `copyright` calls the existing
   `buildCopyrightSlideContent`; `scripture` and `imported` find the inner slide by `innerSlideId` in
   the reading/deck and strip its stored `id`/`position` exactly as the current code does; `text`
   builds the same `TextSlide` content the PRAYER/MESSAGE/HYMN branches build today. Emit with
   `id: entry.id` — the stored UUID. Never recompute an id from slot index or emission order; Phase 23
   keys `PresentationViewer`'s `AudioPlayer`/`VideoPlayer` child instances on `slide.id` specifically
   so a reorder cannot leak a muted/blocked state across slides, and recomputing the id reintroduces
   that bug. Set `groupId` to the group's id and `groupSlideId` to the entry id on the emitted
   `AssembledSlide`.
3. When an entry's source does not resolve, SKIP it. RESEARCH.md § Pattern 3 suggested rendering a
   removed-section placeholder; that is deliberately rejected here because the assembled slideshow
   feeds a live projector and a placeholder slide would be shown to a congregation. The entry remains
   stored, so Phase 25/26 can surface it as an unresolved card in the Slides grid. Record this as a
   planner decision in the summary.
4. When NO group exists for the slot, fall back to today's derivation for that slot, but emit ids of
   the form `slot.id` joined with the local emission sequence rather than the current slot-array-index
   form. The slot id is stable across reorders and across recomputes, so a pre-materialization render
   does not churn Vue keys; the old index-derived form did. Leave `groupId`/`groupSlideId` unset on
   this path.

In `src/types/slide.ts`, extend `AssembledSlide` with optional `groupId?: string` and
`groupSlideId?: string`. These are additive; every existing consumer keeps compiling.

Keep the function PURE — no Pinia, no Firestore, no Vue imports may appear in
`src/utils/slideshowAssembler.ts`.

Extend `src/utils/__tests__/slideshowAssembler.test.ts` rather than replacing it: keep every existing
test (they now exercise the fallback path) and add group-driven cases using new `makeSlideGroup` /
`makeGroupSlideEntry` builders written in the file's existing style.
  </action>
  <acceptance_criteria>
    - `grep -c "@/stores\|@/firebase\|from 'vue'" src/utils/slideshowAssembler.ts` is 0.
    - A test asserts assembled slide ids are exactly the `GroupSlideEntry.id` values supplied in the group fixture.
    - A test mutates the canonical lyrics between two assembly calls with an unchanged group and asserts the emitted text changed.
    - A test asserts an entry whose `sourceRef` does not resolve produces no assembled slide while the input group object is unmodified.
    - A test calls `assembleSlideshow` twice with an empty `groupsBySlotId` and asserts the two id sequences are identical.
    - A test asserts cross-slot output order follows `position` and within-slot output order follows `entry.order`.
    - `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts` exits 0 and `npm run type-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideshowAssembler.test.ts</automated>
  </verify>
  <done>The assembler is a join over stored structure and live content, with stable persisted slide ids.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-level audio precedence, video bed, and the per-slide loop flag</name>
  <files>src/utils/slideshowAssembler.ts, src/types/slide.ts, src/utils/__tests__/slideshowAssembler.test.ts</files>
  <read_first>
    - src/utils/slideshowAssembler.ts lines 71-98 — the `slotsWithMediaAttached` Set and the first-slide-only media rule that this task REPLACES (D-04 supersedes it, it is not extended)
    - src/types/slide.ts lines 11-24 — `SlideBase`'s existing `audioUrl`/`videoUrl` render carriers and their doc comment, which needs updating
    - .planning/phases/24-slide-group-model-and-migration/24-CONTEXT.md § D-04 and .planning/milestones/v1.2-REQUIREMENTS.md R030
    - .planning/phases/24-slide-group-model-and-migration/24-RESEARCH.md § Pattern 4 — audio is two-level, video is bed-only
  </read_first>
  <behavior>
    - An entry with its own `audioUrl` resolves to that url even when the group has a bed.
    - An entry with no `audioUrl` in a group with a bed resolves to the bed url.
    - Given three consecutive entries where only the middle one has its own audio, the emitted urls are bed, own, bed.
    - An entry with its own audio and `audioLoop: true` emits `audioLoop: true`; a bed-resolved slide never emits `audioLoop` regardless of any other entry's flag.
    - A group with neither a bed nor any per-slide audio emits slides with no `audioUrl` key.
    - `bedVideoUrl` resolves onto every entry in the group (video is a bed-only layer with no per-slide override).
    - `audioFromBed` / `videoFromBed` are true exactly when the resolved url came from the group rather than the entry.
    - The old first-slide-only attachment behaviour is gone from the group path; on the no-group fallback path a slot's deprecated `audioUrl`/`videoUrl` still land on its first emitted slide, so pre-migration services keep behaving as they did.
  </behavior>
  <action>
Add `audioLoop?: boolean` to `SlideBase` in `src/types/slide.ts`, and update `SlideBase`'s existing
doc comment: these render carriers are now filled from the resolved GROUP bed and per-slide entry
audio, not from the slot's deprecated media fields. Add `audioFromBed?: boolean` and
`videoFromBed?: boolean` to `AssembledSlide` (not to `SlideBase` — they are assembly provenance, not
slide content).

In `assembleSlideshow`'s group path, resolve effective media per entry with a small pure helper:
- effective audio url is `entry.audioUrl` when present, else `group.bedAudioUrl` when present, else
  nothing; `audioFromBed` is true only in the second case.
- `audioLoop` is copied ONLY from `entry.audioLoop`, and only when the audio came from the entry. A
  group bed never loops (D-04), so a bed-resolved slide must not carry the flag even if some other
  entry in the group set it.
- effective video url is `group.bedVideoUrl` when present; `videoFromBed` is true whenever it is set.
  There is no per-slide video field — R030 and D-04 define two AUDIO layers only.
- Omit each key entirely when there is no value; do not write `undefined`.

Delete the `slotsWithMediaAttached` Set from the group path — D-04's precedence REPLACES the
first-emitted-slide-only rule rather than extending it. Keep that Set alive on the no-group fallback
path only, so a service that has not yet materialized behaves exactly as it does today.

The assembler must NOT read `entry.audioScope`. Scope is a UI round-trip field only: writing
`'group'` is persisted by the Phase 26 drawer as a write to the parent group's `bedAudioUrl` with the
entry's own `audioUrl` cleared (the decision recorded in 24-02). Precedence therefore reads exactly
two inputs, `entry.audioUrl` and `group.bedAudioUrl`, and has no third state to disambiguate.

Extend the assembler test file with the behaviors above, including the three-entry bed/own/bed
adjacency case and the loop-never-on-bed case.
  </action>
  <acceptance_criteria>
    - A test with entries [no audio, own audio, no audio] in a group with a bed asserts the emitted urls are [bed, own, bed].
    - A test asserts a bed-resolved slide has no `audioLoop` key while the entry-resolved slide in the same group has `audioLoop: true`.
    - A test asserts a group with no bed and no entry audio emits slides where `'audioUrl' in slide` is false.
    - A test asserts `bedVideoUrl` lands on every slide of the group and `videoFromBed` is true on each.
    - A test asserts a slot with no group still attaches its deprecated media to only its first emitted slide.
    - `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slideshowAssembler.test.ts</automated>
  </verify>
  <done>R030's two audio levels and D-04's precedence are a pure data transform with full test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AudioPlayer loop pass-through and group-scoped bed keying in PresentationViewer</name>
  <files>src/components/AudioPlayer.vue, src/components/__tests__/AudioPlayer.test.ts, src/components/PresentationViewer.vue, src/components/__tests__/PresentationViewer.test.ts</files>
  <read_first>
    - src/components/AudioPlayer.vue lines 1-60 — the `<audio>` element bindings and the `defineProps<{ src, label?, chromeless? }>()` block, plus the doc comment that currently states the component never loops
    - src/components/PresentationViewer.vue lines 160-190 and 350-380 — the AudioPlayer/VideoPlayer mounts, `currentAudioUrl`/`currentVideoUrl`, and the WR-02 doc comment explaining why the media children are keyed on the slide id
    - src/components/__tests__/PresentationViewer.test.ts — the Phase 23 regression guard; read it before touching the component so no existing assertion is disturbed
    - src/components/__tests__/AudioPlayer.test.ts — the existing prop/behaviour test conventions
  </read_first>
  <behavior>
    - `AudioPlayer` with `loop` true renders an `<audio>` element whose `loop` attribute is set; without the prop the attribute is absent, matching today.
    - `PresentationViewer` passes the current slide's `audioLoop` through to `AudioPlayer`.
    - When the current slide's audio came from the group bed, the AudioPlayer key is derived from the group id, so advancing to the next bed-carrying slide of the SAME group does not remount the player and the bed keeps playing across the group.
    - When the current slide's audio is per-slide, the key is derived from the slide id exactly as today, preserving the WR-02 anti-state-leak guarantee.
    - For an `AssembledSlide` with no `groupId` (a pre-migration service), both keys are byte-identical to today's values.
    - The whole existing `PresentationViewer.test.ts` suite passes unchanged.
  </behavior>
  <action>
In `src/components/AudioPlayer.vue`, add an optional `loop?: boolean` prop and bind it to the native
`loop` attribute on the `<audio>` element. Update the component's doc comment, which currently asserts
the component never loops, to say looping is opt-in per slide and that a group bed never sets it. This
is a native attribute pass-through — no new timer, no `ended` re-trigger logic.

In `src/components/PresentationViewer.vue`:
- Pass `:loop="currentSlide?.slide.audioLoop"` to the `AudioPlayer` mount.
- Change `currentAudioKey` so that when the current `AssembledSlide` has `audioFromBed` true and a
  `groupId`, the key is built from the group id and the url instead of the slide id and the url;
  otherwise keep the existing slide-id-and-url form. Do the same for `currentVideoKey` using
  `videoFromBed`. Extend the existing WR-02 doc comment above those computeds to explain the split:
  per-slide media still forces a fresh child on every slide so muted/blocked state cannot leak, while
  a GROUP BED is deliberately one continuous instance across the group — that continuity is what R030
  means by a bed that "plays across the group".
- Change nothing else. `currentAudioUrl`/`currentVideoUrl` still read `slide.audioUrl`/`slide.videoUrl`,
  so a service with no groups produces `audioFromBed` undefined and therefore byte-identical keys to
  today. Phase 23's outstanding human-verify checkpoint is not affected by data that does not exist yet.

Extend `AudioPlayer.test.ts` with the loop-attribute cases and `PresentationViewer.test.ts` with the
bed-continuity and per-slide-isolation key cases. Do not weaken, rewrite, or delete any existing
assertion in `PresentationViewer.test.ts` — it is this phase's named regression guard.
  </action>
  <acceptance_criteria>
    - A test asserts the rendered `<audio>` element has `loop` set when the prop is true and unset when it is absent.
    - A test asserts advancing between two bed-carrying slides of the same group leaves the AudioPlayer key unchanged.
    - A test asserts advancing between two slides with per-slide audio changes the AudioPlayer key.
    - A test asserts a slide with no `groupId` produces the same key string as the pre-refactor formula.
    - `npx vitest run src/components/__tests__/PresentationViewer.test.ts` exits 0 with no fewer passing assertions than before this task.
    - `npx vitest run src/components/__tests__/AudioPlayer.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/PresentationViewer.test.ts src/components/__tests__/AudioPlayer.test.ts</automated>
  </verify>
  <done>A per-slide loop is honoured and a group bed genuinely plays across its group, with Phase 23 intact.</done>
</task>

</tasks>

## Artifacts this phase produces (contributed by 24-04)

| Symbol / path | Kind | Notes |
|---|---|---|
| `AssemblyInputs.groupsBySlotId` | new required field | `Map<string, SlideGroup>` |
| `AssembledSlide.groupId` | new optional field | absent on the no-group fallback path |
| `AssembledSlide.groupSlideId` | new optional field | equals the stored `GroupSlideEntry.id` |
| `AssembledSlide.audioFromBed` | new optional field | assembly provenance for bed continuity |
| `AssembledSlide.videoFromBed` | new optional field | assembly provenance for bed continuity |
| `SlideBase.audioLoop` | new optional field | per-slide only; a bed never sets it |
| `AudioPlayer` `loop` prop | new component prop | native `<audio loop>` pass-through |
| `currentAudioKey` / `currentVideoKey` | changed computeds | group-scoped when the media came from the bed |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored group + canonical content → projected slideshow | Everything a congregation sees crosses here; a wrong join shows the wrong text or the wrong media to a live room. |
| assembled slide → media element | Urls resolved here are handed to `<audio>`/`<video>` src bindings. |

## STRIDE Threat Register (OWASP ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04-01 | Tampering | assembled `slide.id` provenance | high | mitigate | Ids come from the stored `GroupSlideEntry.id`, never recomputed. `PresentationViewer` keys its media children on that id (WR-02); a recomputed id reintroduces the muted-state-leak bug. Covered by an explicit assembler test and by the `PresentationViewer.test.ts` regression guard. |
| T-24-04-02 | Information Disclosure | wrong slide text shown to a congregation | high | mitigate | Content is resolved through the typed `sourceRef` union rather than by position, and an unresolvable ref is omitted rather than substituted. Adjacency and ordering are covered by explicit tests. |
| T-24-04-03 | Spoofing | media url injected into an `<audio>`/`<video>` src | medium | accept | Urls originate only from `useMediaUpload`'s existing org-scoped Storage upload path and Storage rules' size/type caps; this phase changes which document field holds them, not who can supply them. |
| T-24-04-04 | Denial of Service | pure assembler recompute cost on every reactive tick | low | accept | The join is a map lookup per slot plus a linear walk of entries — the same order of work as today's derivation. |
| T-24-04-05 | Tampering | player-side change invalidating Phase 23's pending human-verify | medium | mitigate | Both key computeds fall back to the exact pre-refactor formula when `audioFromBed`/`videoFromBed` is unset, which is always true for data with no slide groups. Asserted by a dedicated test. |
| T-24-04-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed. RESEARCH.md § Package Legitimacy Audit records "N/A". |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/slideshowAssembler.test.ts` exits 0.
- `npx vitest run src/components/__tests__/PresentationViewer.test.ts src/components/__tests__/AudioPlayer.test.ts` exits 0.
- `npm run type-check` exits 0.
- `npx vitest run src/` shows no NEW failures beyond the ~44 pre-existing ones catalogued in STATE.md.
</verification>

<success_criteria>
- The assembler joins stored group structure against live canonical content and stays pure.
- Audio precedence, bed resume, and the per-slide loop flag are proven at the pure-function layer.
- Phase 23's presentation behaviour is unchanged for data that has no slide groups.
</success_criteria>

<output>
Create `.planning/phases/24-slide-group-model-and-migration/24-04-SUMMARY.md` when done.
</output>
