---
phase: 27-service-order-tab-rename-and-strip-slide-editing-risk-medium
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R034, R018]

must_haves:
  truths:
    - "The first tab button reads exactly 'Service Order' (two words, title case) and selecting it still shows the order of service (D-03, D009)."
    - "The tab's internal identity is renamed too — the tab-selection union reads service-order / roles / slides and the page still opens on the first tab by default (D-03)."
    - "Phase 26's 'Edit in scripture' relay still lands on the first tab with the requested passage editor open, because its tab assignment was renamed with everything else."
    - "The four unrelated views that happen to declare their own tab-selection ref are byte-for-byte unchanged."
    - "The first tab's panel carries a stable test seam so panel-visibility assertions no longer have to probe a slide component that later plans remove."
  artifacts:
    - "src/views/ServiceEditorView.vue with the renamed label, renamed union, renamed default and renamed call sites"
    - "src/views/__tests__/ServiceEditorView.test.ts asserting the new label and the new panel seam"
  key_links:
    - "The tab-selection ref name is shared by five unrelated files. Only the service editor view's union changes; a project-wide find-and-replace would corrupt the quarter, roster, services-list and song-slide-over views, which each own their own independent ref of the same name."
    - "Phase 26's scripture-navigation handler assigns the first tab's value directly. If that assignment is missed, the handler silently switches to a tab value that matches nothing and the whole page body disappears — no error, no test failure unless the relay tests run."
    - "The tab panels are v-show, not v-if: a value that matches no branch hides every panel rather than throwing."
  prohibitions:
    - "MUST NOT touch src/views/QuarterView.vue, src/views/RosterView.vue, src/views/ServicesView.vue or src/components/SongSlideOver.vue. Their identically-named tab refs are unrelated."
    - "MUST NOT remove, add or reorder any tab. There are three tabs before and three after; only the first one's label and value change."
    - "MUST NOT drop the editor-only guard on the Roles tab button or the viewer-visible behaviour of the Slides tab button while editing that markup."
    - "MUST NOT remove or alter any slide surface in this plan. Stripping is 27-03 through 27-05; this plan is vocabulary only."
    - "MUST NOT rename the third tab. Its label stays 'Slides'."
  <!-- planner-discipline-allow: 'music' -->
---

<objective>
Rename the service editor's first tab from Music to Service Order — the visible label AND the internal
tab value — per D-03 and D009, leaving no stale vocabulary behind.

Purpose: R034's first half. The rail note shipped in Phase 25 already reads "order locked to Service
Order" and the empty state already points at "the Service Order tab"; the tab button itself is the last
place still saying Music, so the app currently contradicts itself.
Output: A renamed first tab, a renamed tab-selection union, and a stable panel test seam that later
plans use instead of probing a slide component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-CONTEXT.md
@.planning/phases/26-edit-slide-drawer-risk-medium/26-03-SUMMARY.md
</context>

<interfaces>
Sites in `src/views/ServiceEditorView.vue` that carry the old vocabulary (line numbers approximate —
re-locate before editing, the file is 2847 lines):

- ~397 — a prose comment introducing the tab bar by its old first-tab name.
- ~402 / ~405 — the first tab button's active-class test and its click assignment.
- ~407 — the first tab button's visible label text.
- ~420-424 — the Slides tab button's comment, which currently says the rename is Phase 27's job. That
  sentence becomes false in this plan.
- ~437 — the first tab panel's `v-show` test. This div currently carries no attributes.
- ~1222 — the tab-selection ref declaration: a three-member string-literal union plus its initial value.
- ~1377 — Phase 26's scripture-navigation handler assigning the first tab. Named
  `handleNavigateToScriptureEditor`; its JSDoc already describes the target generically as "the tab that
  owns the scripture editor", so the prose needs no change, only the assigned value.

Sites in `src/views/__tests__/ServiceEditorView.test.ts` (approximate):

- ~1279-1280 — a three-element expected tab-label array.
- ~1292 / ~1307 — test titles naming the old first tab, inside `describe('ServiceEditorView - Slides tab (Phase 25-03)')`.
- ~1355 — a button lookup by the old label text.
- ~1373-1376 — a test titled after the old label that asserts the first tab button's text.
- ~1409 — a test title inside `describe('ServiceEditorView - Edit in scripture plumbing (Phase 26-03)')`.

The helper `isVShowHidden(wrapper)` at ~line 119 accepts anything exposing `.element`, so it works with
a `wrapper.find('[data-testid=...]')` DOMWrapper as well as with `findComponent`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Turn the tab-label and panel-seam assertions red</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
  Update every assertion and test title in this file that names the first tab by its old label so it
  names the new label "Service Order" instead (per D-03): the three-element expected label array and its
  filter list, the button-lookup by text, the dedicated first-tab-label test and its title, and the two
  test titles inside the Slides-tab and Edit-in-scripture describe blocks that mention the old tab by
  name. Leave the third tab's label assertions reading "Slides" and the second's reading "Roles".

  Then add one new test to the Slides-tab describe block asserting the first tab's panel is reachable by
  a stable seam: it finds `[data-testid="service-order-panel"]`, asserts it exists, and asserts
  `isVShowHidden` on it is false on initial render. Do not yet change the existing panel-visibility
  assertions that probe the slideshow-preview component — those are swapped in 27-05 when that component
  actually leaves.

  Do not touch any other file. Do not modify the view yet — this task is expected to leave the suite red,
  and its commit is the test commit of a RED then GREEN pair.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>The targeted suite now FAILS, and every failure is an expected-label or missing-seam mismatch — no mount errors, no unrelated regressions.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Rename the first tab, its value and its vocabulary</name>
  <files>src/views/ServiceEditorView.vue</files>
  <action>
  In `src/views/ServiceEditorView.vue` only:

  1. Change the first tab button's visible text to exactly "Service Order" — two words, title case, no
     ampersand, no abbreviation (D009, and matching the rail note Phase 25 already shipped).
  2. Change the tab-selection ref's union so its first member and its initial value are `'service-order'`
     instead of the old first-tab value, keeping `'roles'` and `'slides'` unchanged and keeping the union
     in the same declaration form.
  3. Update all five in-file comparisons and assignments of the old value: the first tab button's
     active-class test, its click handler assignment, the first tab panel's `v-show` test, and Phase 26's
     scripture-navigation handler assignment near line 1377. After this step the old string literal must
     not survive anywhere in this file outside of prose.
  4. Add `data-testid="service-order-panel"` to the first tab panel div (the one carrying the `v-show`).
     This is a test seam only — it renders nothing and changes no behaviour.
  5. Update the two stale prose comments: the tab-bar introduction comment, and the Slides-tab button
     comment whose last sentence claims the rename is still Phase 27's future work — replace that clause
     so it states the current fact rather than a pending intention.

  Do not edit any other file. In particular the four unrelated views that declare their own identically
  named tab ref are out of scope and must remain untouched.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
    <automated>npm run type-check</automated>
    <automated>test -z "$(git diff --name-only -- src/views/QuarterView.vue src/views/RosterView.vue src/views/ServicesView.vue src/components/SongSlideOver.vue)" && echo UNRELATED-VIEWS-CLEAN</automated>
    <automated>test "$(grep -v "^[[:space:]]*//\|^[[:space:]]*\*\|<!--" src/views/ServiceEditorView.vue | grep -c "'music'")" -eq 0 && echo NO-STALE-TAB-VALUE</automated>
  </verify>
  <done>
  The targeted suite passes, type-check reports 0 errors, the four unrelated views show no diff, and the
  old tab value survives nowhere in the renamed file outside comments. The tab bar still renders three
  buttons; Roles is still editor-only and Slides is still viewer-visible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer → service editor | A non-editor loads the same view; per-tab and per-control guards decide what renders. |
| Slides tab subtree → page state | Phase 26's relay reaches page-level tab state through a declared event only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-02-01 | Elevation of Privilege | tab bar markup | high | mitigate | The Roles tab button's editor-only guard is inside the markup being edited; Task 2 forbids touching it and the existing viewer test (Slides present, Roles absent) is left in place to catch a drop. |
| T-27-02-02 | Denial of Service | tab panels | medium | mitigate | Panels are v-show, so a half-renamed value hides the whole page body silently. Task 2's gate greps the file for any surviving occurrence of the old value outside comments. |
| T-27-02-03 | Tampering | four unrelated views | high | mitigate | A project-wide replace of the shared ref name would corrupt them. Task 2's gate asserts a zero-length diff for all four files. |
| T-27-02-04 | Denial of Service | Phase 26 scripture relay | medium | mitigate | The relay assigns the first tab by value; the Edit-in-scripture describe block is run in this plan's gate, so a missed assignment fails immediately. |

No package-manager installs occur in this plan, so the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` passes.
- `npm run type-check` reports 0 errors.
- `npm run build` succeeds.
- `git diff --name-only` lists exactly two files: the service editor view and its test.
</verification>

<success_criteria>
The first tab reads "Service Order", its internal value matches, Phase 26's scripture relay still opens
the passage editor on that tab, the four unrelated views are untouched, and a stable panel test seam
exists for later plans to probe.
</success_criteria>

<output>
Create `.planning/phases/27-service-order-tab-rename-and-strip-slide-editing-risk-medium/27-02-SUMMARY.md` when done
</output>
