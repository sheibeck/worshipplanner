---
phase: 25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/slides/slideDisplay.ts
  - src/components/slides/__tests__/slideDisplay.test.ts
  - src/components/slides/SlidePlanRail.vue
  - src/components/slides/__tests__/SlidePlanRail.test.ts
  - src/components/slides/SlidesTab.vue
  - src/components/slides/__tests__/SlidesTab.test.ts
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R031, R018]

must_haves:
  truths:
    - "The service editor has a third tab labelled Slides, alongside Music and Roles, and it is reachable by viewers as well as editors (R031)."
    - "The plan rail lists every plan item in service-plan order with its kind badge, its slide count and its title, and shows a group-music line only for groups that actually have a bed (R031)."
    - "The rail mirrors the plan and is not reorderable: it renders no drag handle, no grab cursor and no draggable-looking affordance of any kind, and the only explanation offered is the 'order locked ⇄ Service Order' note (D-06, D-04)."
    - "Opening the Slides tab auto-selects the first plan item in plan order, so the tab is never a blank right-hand column (D-05)."
    - "A plan item whose group holds zero slides still appears in the rail, showing a count of zero rather than being hidden (D-08)."
    - "A service with no plan items shows a rail empty state pointing the user at the Service Order tab, and the Slides tab stays reachable (D-07)."
    - "The rail carries no orphan or unanchored block, no page-level import button and no generate-missing-slides button — all three were cut and must not reappear from the mockup (D-01, D-02, D-03)."
    - "Kind badge colours come from a static, fully-spelled-out class map keyed by slot kind, so no badge colour is lost to Tailwind v4's production purge (a bug this codebase has shipped twice)."
  artifacts:
    - "src/components/slides/slideDisplay.ts — shared static badge class map and display-text helpers"
    - "src/components/slides/SlidePlanRail.vue and its test"
    - "src/components/slides/SlidesTab.vue holding the selection contract, and its test"
    - "src/views/ServiceEditorView.vue with the third tab button, the third panel and the widened activeTab union"
  key_links:
    - "The rail's slide count is derived from the assembled slideshow filtered by slot array index, NOT from `group.slides.length` — a slot whose group has not materialized yet still produces real fallback-path slides, and counting the group document would show zero while the grid shows six"
    - "`SlidesTab` and every component under it receive assembled data as props from `ServiceEditorView`, which is the SOLE owner of `useSlideshowAssembly()`; a second call in a child would run a second set of materialize and reconcile watchers"
    - "Rail rows are ordered by `slot.position`, but the array index of each slot is what the grid filters the assembled slideshow by — both must be carried, and they are not the same number for a service whose positions have drifted"
  prohibitions:
    - "MUST NOT import or call `useSlideshowAssembly()` from any component under `src/components/slides/`."
    - "MUST NOT give the rail a drag handle, a grab cursor, a draggable attribute or a drop target. D-06 forbids a rail that merely looks draggable and then rejects the drop."
    - "MUST NOT rename the first tab to 'Service Order'. Only the rail's note anticipates that rename; the tab button itself is renamed in Phase 27 (D-04, UI-SPEC Mockup Correction 5)."
    - "MUST NOT build the mockup's Grid/List toggle, its UNANCHORED block, its page-level import button or its generate-missing-slides button (D-09, D-01, D-02, D-03)."
---

<objective>
Ship the Slides tab itself: the third tab button and panel in `ServiceEditorView`, the service-plan
rail that mirrors plan order without being reorderable, and the selection state the slide grid and
the future Edit Slide drawer both hang off.

Purpose: this is R031's left half and the seam every later plan in this phase plugs into. It also
fixes the four mockup affordances Phase 24's decisions cut, by never building them.
Output: a shared display module, two new components, one view extension, four test suites.

The right-hand grid column is filled by 25-04 in this same phase — this plan owns the tab, the rail
and the selection contract, not the grid.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-PATTERNS.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-VALIDATION.md
@.planning/phases/24-slide-group-model-and-migration/24-06-SUMMARY.md
</context>

<hard_constraints>
Non-negotiable for every task in this plan:
- Do NOT run `npm run test:rules`. Do NOT start, stop, or restart the Firebase emulator.
- Use `npx vitest run <path>`. Never bare `vitest`, never `npm run test:unit`.
- Sequential main-tree execution on branch `milestone/M001`. Do not create or switch branches.
- Known-failing baseline for `npx vitest run src/` is exactly **10 FILES**: 8 under
  `.gsd/quarantine/worktrees/**`, plus `src/storage.rules.test.ts` and
  `src/views/__tests__/RosterView.test.ts`. Judge against the FILE SET, not the flapping test count.
- `npm run type-check` must stay at 0 errors and `npm run build` must succeed.
- **25-UI-SPEC.md carries two DEVELOPER-APPROVED EXCEPTION blocks** (owner sign-off, 2026-07-26): the
  three font weights plus `text-[10px]`, and the 6px `gap-1.5` spacing value. Both were BLOCKED by the
  generic UI checker and both were approved on measured codebase-grounding evidence. Do NOT "correct"
  them during implementation — matching the existing app is the intent.
- **Tailwind v4 has no config file here and purges dynamically-built class names.** Every kind-to-colour
  mapping must be a static, fully-spelled-out object keyed by the discriminant, following
  `src/components/SongBadge.vue` and `src/components/TeamTagPill.vue`. A template string that
  interpolates a colour name compiles, passes tests, and then ships colourless.
- `src/views/__tests__/ServiceEditorView.test.ts` already carries Pinia stubs for `scriptureSlides`,
  `importedSlides` AND `slideGroups`. Reuse them; adding a fourth store or removing one crashes the
  mount at setup rather than at an assertion.
</hard_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared slide-display module and the plan rail</name>
  <files>src/components/slides/slideDisplay.ts, src/components/slides/__tests__/slideDisplay.test.ts, src/components/slides/SlidePlanRail.vue, src/components/slides/__tests__/SlidePlanRail.test.ts</files>
  <read_first>
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md § Color (the kind badge colour map table), § Typography, § Spacing Scale, § Copywriting Contract (rail empty-state rows), § Grid & Interaction Rules (rail row overflow, rail hover, zero-slide group)
    - src/components/SongBadge.vue and src/components/TeamTagPill.vue in full — the static class-map pattern to copy, and the exact class strings the UI-SPEC's colour map reuses
    - src/views/ServiceEditorView.vue lines 424-427 — the existing eyebrow heading classes the SERVICE PLAN label must reuse verbatim
    - src/views/ServiceEditorView.vue lines 513-536 — the existing slot-item row markup, which the rail row copies MINUS the drag handle
    - src/types/service.ts lines 1-100 — `SlotKind`, every `ServiceSlot` variant and which title field each carries
    - src/utils/slotTypes.ts lines 21-40 — `slotLabel`, the existing per-kind human label
    - src/types/slide.ts — `AssembledSlide` and its `slotIndex` field
    - src/types/slideGroup.ts — `SlideGroup.bedAudioUrl` / `bedVideoUrl`
  </read_first>
  <behavior>
    - The badge class map returns a distinct, fully-spelled-out class string for each of the six slot kinds and never builds one by interpolation.
    - The display-title helper returns the song title for a song slot, a readable passage reference for a scripture slot, the hymn name for a hymn slot, and the existing per-kind label for the rest, falling back to the per-kind label whenever the specific field is empty.
    - The rail renders one row per plan item, ordered by plan position, each showing its kind badge, its slide count and its title.
    - A row's slide count equals the number of assembled slides whose slot array index is that row's, so it agrees with the grid even before the group document exists.
    - A plan item with zero slides renders a row showing zero.
    - A group with a bed shows an extra music line naming the bed; a group without one shows no placeholder.
    - Clicking a row emits a selection event carrying that row's slot id.
    - The selected row is the only one carrying the accent border and tint.
    - With no plan items the rail renders the empty-state heading and body from the copywriting contract instead of any rows.
    - While the group subscription is still loading and there are no rows yet, the rail renders skeleton rows.
    - No row renders a drag handle, a grab cursor, a draggable attribute or a drop handler.
  </behavior>
  <action>
Create `src/components/slides/slideDisplay.ts` as the single shared display module for this phase's
components. It exports, at minimum: a static class map keyed by `SlotKind` holding exactly the class
strings 25-UI-SPEC.md's kind-badge colour table specifies (song and hymn share the indigo family,
scripture reuses `TeamTagPill`'s theme variant, prayer its team variant, message its user variant, and
imported reuses `SongBadge`'s amber); a helper returning a plan item's display title per the behaviour
above; and a helper returning a short uppercase content label for one assembled slide (25-04 consumes
this one for card labels, so define it here rather than duplicating it later). Write the class map as a
literal object with fully-written class strings — never a template string with an interpolated colour
name, which Tailwind v4 silently purges from the production bundle. Unit-test the module directly; it
is pure and this is the cheapest place to prove the map is complete for every slot kind.

Create `src/components/slides/SlidePlanRail.vue` as a presentational, prop-driven component. It takes
the service's slots, the assembled slideshow, the groups map keyed by slot id, the currently selected
slot id, and a groups-loading flag. It emits a single selection event carrying a slot id. It reads no
store and calls no composable.

Order rows by `slot.position` ascending, matching how the assembler orders the show. Carry each slot's
ARRAY index alongside it — that array index, not the position value and not the group id, is what the
assembled slides' `slotIndex` field matches, and 25-04's grid filters on the same number. Derive each
row's count by filtering the assembled slideshow on that array index. Do not count `group.slides.length`
instead: a slot whose group has not yet materialized still produces real fallback-path slides, and a
group-document count would read zero while the grid showed a full group.

Build the rail chrome per 25-UI-SPEC.md's layout reference: a fixed 260px column with a right border
and vertical scrolling only, an eyebrow heading reading SERVICE PLAN using the exact existing eyebrow
classes, and beneath it the note reading "order locked ⇄ Service Order". Write that note with the new
tab name now, per D-04 — Phase 27 renames the tab button itself, and shipping the old name here just to
rename it next phase is churn.

Row anatomy per the UI-SPEC and the mockup: kind badge, slide count, title clamped to two lines with
ellipsis, and a music line rendered only when the row's group has a bed. Hover is a background tint
only. The selected row gets the accent border and tint; no other row does.

Render the empty state when there are no plan items, using the copywriting contract's heading and body
verbatim. Render skeleton rows while the groups-loading flag is true and there are no rows yet — take
that flag from the `slideGroups` store's own loading state, which the parent passes down; the assembly
composable's `isLoading` reflects only the per-song lyrics loader and will not tell you the group
subscription is still cold.

Deliberately do not build: any orphaned or unanchored block, any reassignment affordance, any
page-level import button, any generate-missing-slides button, and any drag affordance. All five were
cut by 25-CONTEXT.md D-01 through D-03 and D-06 and are present in the mockup only because it predates
those decisions.

Test both files. The rail's tests must include explicit negative assertions for the drag affordance and
for the cut mockup blocks — those are the assertions that stop the mockup creeping back in later.
  </action>
  <acceptance_criteria>
    - A test asserts the badge class map has an entry for every slot kind and that each value is a non-empty literal string.
    - A test asserts the display-title helper returns the song title, the passage reference, the hymn name, and the per-kind fallback for an empty field.
    - A test renders three plan items out of position order and asserts the rendered rows appear in position order.
    - A test asserts a row's count equals the number of assembled slides carrying that row's slot array index, using a fixture where a group document is absent but assembled slides exist.
    - A test asserts a plan item with zero assembled slides still renders a row showing zero.
    - A test asserts a group with a bed renders the music line and a group without one renders no music line.
    - A test clicks a row and asserts the selection event carries that row's slot id.
    - A test asserts only the selected row carries the accent classes.
    - A test with no plan items asserts the empty-state heading and body render and no rows do.
    - A test asserts no rendered rail element carries a drag-handle class, a grab cursor class, a draggable attribute or a drop handler.
    - A test asserts the rail renders no orphan block, no page-level import control and no generate-missing-slides control.
    - `npx vitest run src/components/slides/__tests__/slideDisplay.test.ts src/components/slides/__tests__/SlidePlanRail.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/slideDisplay.test.ts src/components/slides/__tests__/SlidePlanRail.test.ts</automated>
  </verify>
  <done>The rail mirrors the plan, explains that it does, and offers no way to reorder it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SlidesTab shell and the selection contract Phase 26 consumes</name>
  <files>src/components/slides/SlidesTab.vue, src/components/slides/__tests__/SlidesTab.test.ts</files>
  <read_first>
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md § D-05 and § D-12 — auto-selection and the seam Phase 26 fills
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md § Pattern 1 (sole composable owner), § Pattern 2 (slot-index filtering), § Pitfall 4 (slide id churn at materialization)
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md § Layout Reference — the two-column split and the 260px rail
    - src/composables/useSlideshowAssembly.ts lines 74-88 — the `PendingReconciliation` shape and the return interface, so the prop types match the source exactly
    - src/components/slides/SlidePlanRail.vue (from Task 1) — its props and its selection event
  </read_first>
  <behavior>
    - The tab renders a fixed-width rail column and a flexible content column beside it.
    - When the tab becomes active and nothing valid is selected, the first plan item in plan order is selected.
    - When plan items arrive after the tab is already active, the first one is selected then.
    - Selecting a different plan item clears any selected slide, because a slide selection belongs to its group.
    - A selected slide id that no longer resolves against the current group's assembled slides is cleared rather than left dangling.
    - Removing the currently selected plan item from the service moves the selection to a plan item that still exists.
    - With no plan items, nothing is selected and the rail's empty state is what shows.
    - The component reads no store and calls no composable — every input arrives as a prop.
  </behavior>
  <action>
Create `src/components/slides/SlidesTab.vue` as the panel component mounted inside the new tab. Its
props are exactly the data `ServiceEditorView` already has: the service's slots, the service id, the
org id, the assembled slideshow, the groups map, the pending reconciliations, an editor-capability
flag, a groups-loading flag, and an active flag that is true while the Slides tab is the visible one.

Do not import `useSlideshowAssembly` here or anywhere below here. `ServiceEditorView` is its sole
owner; a second invocation instantiates a second copy of the composable's local state and runs a second
set of materialize and reconcile watchers against the same documents. Reading the `slideGroups` Pinia
store directly is a different matter and is allowed for writes in later plans — it is a plain singleton
store, and `ServiceEditorView` already calls its actions directly.

Hold the selection contract here, as two pieces of local state: the selected slot id and the selected
slide id. Define and document that contract explicitly in this file — Phase 26 opens its Edit Slide
drawer against the selected slide id, and this is the seam D-12 asks this phase to leave clean. The
selected slide id is the assembled slide's own id, which equals the stored group entry id once the
group has materialized.

Implement D-05's auto-selection as a watcher on the active flag and on the ordered slot list: when the
tab is active and the current selection is either unset or no longer present among the slots, select the
first slot in plan order. Ordering here must match the rail's — by `slot.position`.

Clear the selected slide id whenever the selected slot id changes. Also clear it when it no longer
appears among the selected group's assembled slides: 25-RESEARCH.md Pitfall 4 documents that slide ids
change shape the moment a group materializes (a slot-derived fallback id gives way to the stored entry
id), so a selection made in that brief window would otherwise dangle forever. Do not attempt to fix the
id churn itself — the id-minting scheme is Phase 23's WR-02 contract.

Lay the panel out as the UI-SPEC's two columns: the rail at its fixed width with a right border, and a
flexible column beside it that 25-04 fills with the slide grid. Mount `SlidePlanRail` with its props and
handle its selection event.

Test the component with `shallowMount` following the codebase's existing conventions.
  </action>
  <acceptance_criteria>
    - A test asserts that activating the tab with plan items present selects the first item in plan order, not the first in array order, using a fixture whose array order and position order differ.
    - A test asserts that plan items arriving after activation trigger the same auto-selection.
    - A test changes the selected plan item and asserts the selected slide id is cleared.
    - A test asserts a selected slide id that is absent from the selected group's assembled slides is cleared.
    - A test removes the selected plan item and asserts the selection moves to a surviving item.
    - A test with no plan items asserts nothing is selected and the component does not throw.
    - A test asserts the component's rendered rail receives the position-ordered slots.
    - `npx vitest run src/components/slides/__tests__/SlidesTab.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlidesTab.test.ts</automated>
  </verify>
  <done>The tab always has something selected, and the selection Phase 26 needs is defined and stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Third tab button and panel in ServiceEditorView</name>
  <files>src/views/ServiceEditorView.vue, src/views/__tests__/ServiceEditorView.test.ts</files>
  <read_first>
    - src/views/ServiceEditorView.vue lines 397-422 — the two-button tab bar, its exact class strings and its active-state conditional
    - src/views/ServiceEditorView.vue lines 1005-1010 — where the roles panel's `v-show` block sits, so the third panel becomes a third sibling
    - src/views/ServiceEditorView.vue line 1182 — the `activeTab` ref and its union type
    - src/views/ServiceEditorView.vue lines 1176-1180 — the existing `slideGroups` store handle and the comment explaining why the composable re-exposes the groups map
    - src/views/ServiceEditorView.vue lines 1459-1466 — the single `useSlideshowAssembly` call site and its current destructure
    - src/views/__tests__/ServiceEditorView.test.ts lines 55-115 — the three-store Pinia stub block, including the stateful `slideGroups` stub
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md § Mockup Corrections rows 2 and 5
  </read_first>
  <behavior>
    - The tab bar renders three buttons, the third labelled Slides, styled identically to the existing two.
    - The Slides button is visible to a viewer as well as to an editor, unlike the editor-only Roles button.
    - Clicking Slides shows the slides panel and hides the music panel; clicking Music restores it.
    - The default active tab is unchanged — the editor still opens on the music tab.
    - The slides panel receives the assembled slideshow, the groups map, the pending reconciliations, the slots, the org id, the service id, the editor flag and the group-subscription loading flag.
    - The slides panel is told when it is the active tab.
    - The header keeps exactly the buttons it has today — no import button and no generate-missing-slides button are added anywhere on the page.
    - The first tab still reads Music.
  </behavior>
  <action>
Widen the `activeTab` ref's union with a third member and leave its default value alone — the editor
opens on the music tab as it does today, and D-05's auto-selection is about which GROUP is selected once
the Slides tab is opened, not about which tab opens first.

Add a third tab button copying the existing two verbatim in structure and classes, labelled Slides.
Do not gate it behind the editor check the Roles button uses: nothing in 25-CONTEXT.md restricts the
Slides tab to editors, and a viewer being able to see the slides is consistent with the rest of the
page. Write-capable controls inside the panel are gated separately, by the editor flag this task passes
down.

Do NOT rename the first tab. UI-SPEC Mockup Correction 5 is explicit: the tab button becomes "Service
Order" in Phase 27, not here. Only the rail's note (Task 1) anticipates that rename.

Add a third panel as a sibling of the existing two `v-show` blocks, mounting `SlidesTab` with its props.
Add `pendingReconciliations` to the existing `useSlideshowAssembly` destructure — it is already computed
and returned by the composable but is not currently destructured at this call site. Pass the
`slideGroups` store's own loading state as the groups-loading prop; the composable's `isLoading` is the
lyrics loader's flag only and will not tell the rail that the group subscription is still cold.

Do not add any header button. 25-CONTEXT.md D-02 and D-03 cut the mockup's page-level import and
generate-missing-slides buttons; "page-level header actions" in the roadmap prose means the existing
Present and Save controls, which are unchanged.

Extend `src/views/__tests__/ServiceEditorView.test.ts` with a new describe block for the third tab,
reusing the three-store Pinia stub block already in the file — no new mock infrastructure is needed.
Include the negative assertions for the cut header buttons.
  </action>
  <acceptance_criteria>
    - A test asserts three tab buttons render and that the third reads Slides.
    - A test mounted as a viewer asserts the Slides button is present while the Roles button is not.
    - A test clicks the Slides button and asserts the slides panel is shown and the music panel hidden.
    - A test asserts the default active tab is unchanged.
    - A test asserts the slides panel receives the assembled slideshow, the groups map and the pending reconciliations as props.
    - A test asserts the slides panel is told it is active only while the Slides tab is selected.
    - A test asserts no page-level import control and no generate-missing-slides control render on the page.
    - A test asserts the first tab button still reads Music.
    - Every pre-existing test in `src/views/__tests__/ServiceEditorView.test.ts` passes with its assertions unmodified.
    - `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` exits 0.
    - `npm run type-check` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>The Slides tab exists, opens, and is fed everything it needs from the page's single assembly owner.</done>
</task>

</tasks>

## Artifacts this phase produces (contributed by 25-03)

| Symbol / path | Kind | Notes |
|---|---|---|
| `src/components/slides/slideDisplay.ts` | new module | static badge class map, display-title and content-label helpers, shared by rail and card |
| `src/components/slides/SlidePlanRail.vue` | new component | prop-driven, selection-emitting, never draggable |
| `src/components/slides/SlidesTab.vue` | new component | holds `selectedSlotId` / `selectedSlideId` — the D-12 seam Phase 26 opens against |
| `activeTab` union | widened | third member for the slides panel |
| `pendingReconciliations` | newly destructured | already returned by the composable; now reaches the UI |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer session → editor-only controls | The Slides tab is reachable by viewers; every write control inside it must be gated. |
| assembled data → new prop-drilled component tree | A second subscription or a second composable instance here would double-write to Firestore. |

## STRIDE Threat Register (OWASP ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03-01 | Elevation of Privilege | a viewer reaching write controls through the newly-reachable Slides tab | high | mitigate | This plan ships no write control at all; the editor flag is passed down now so 25-04 through 25-06 gate on it from the start. Firestore's org-editor rule on `slideGroups` remains the authoritative boundary — UI hiding is never the only gate. |
| T-25-03-02 | Tampering | a second `useSlideshowAssembly()` instance issuing duplicate materialize and reconcile writes | high | mitigate | Prohibited in `must_haves.prohibitions`; every component under `src/components/slides/` is prop-driven, asserted by tests that mount them with plain props and no Pinia-backed composable. |
| T-25-03-03 | Repudiation | the rail showing a slide count that disagrees with the grid | medium | mitigate | Both derive from the same assembled slideshow filtered by the same slot array index, never from the group document's own array length. Asserted by a test using a group-absent fixture. |
| T-25-03-04 | Tampering | badge colours silently purged from the production bundle | medium | mitigate | Static fully-spelled-out class map, unit-tested for completeness across all six slot kinds, plus a `npm run build` gate in this plan's verification. |
| T-25-03-05 | Denial of Service | selecting a plan item that no longer exists after a concurrent edit | low | mitigate | The auto-selection watcher re-selects a surviving item whenever the current selection is absent from the slot list. Asserted by a test. |
| T-25-03-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed. 25-RESEARCH.md § Package Legitimacy Audit records "Not applicable". |
</threat_model>

<verification>
- `npx vitest run src/components/slides/ src/views/__tests__/ServiceEditorView.test.ts` exits 0.
- `npx vitest run src/` fails in no more than the 10 baseline files listed in `<hard_constraints>`.
- `npm run type-check` exits 0 and `npm run build` exits 0.
- `git grep -n "useSlideshowAssembly" src/components/slides/` returns nothing.

<human-check>
Deferred to the project's batch human-verify (STATE.md Deferred Verification), NOT a blocking
checkpoint. Carried from `25-VALIDATION.md` § Manual-Only Verifications:
1. Visual density and spacing of the rail against `docs/design/slides-tab.dc.html` Turn 1 State 1 —
   pixel judgment, not automatable.
2. Confirm the rail reads as navigation, not as a reorderable list, to someone who has not read D-06.
</human-check>
</verification>

<success_criteria>
- A third tab exists, opens on a populated rail, and never on a blank column.
- The rail mirrors the plan and cannot be mistaken for a place to reorder it.
- None of the four cut mockup affordances exists in the shipped code.
</success_criteria>

<output>
Create `.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-03-SUMMARY.md` when done.
</output>
