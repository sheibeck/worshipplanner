---
phase: 25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium
plan: 07
type: execute
wave: 5
depends_on: ["25-01", "25-06"]
files_modified:
  - src/components/PptxImportModal.vue
  - src/components/__tests__/PptxImportModal.test.ts
  - src/components/slides/dropRouting.ts
  - src/components/slides/__tests__/dropRouting.test.ts
  - src/components/slides/SlideDropTarget.vue
  - src/components/slides/__tests__/SlideDropTarget.test.ts
  - src/components/slides/SlideGrid.vue
  - src/components/slides/__tests__/SlideGrid.test.ts
autonomous: true
requirements: [R032, R018]

must_haves:
  truths:
    - "A slide deck, an image, a video or an audio file can be added to any plan item in the service, not only to Announcements or Sermon (R032)."
    - "A dropped PPTX or image appends slides to the SELECTED group and creates no new plan item — the plan is untouched by an import (D-16, R032)."
    - "A dropped video appends a video slide to the selected group (D-17), while dropped audio becomes the group's music bed and appends nothing (D-14). These two are deliberately NOT symmetrical."
    - "Everything appended lands at the END of the group's existing slides (D-16)."
    - "A drop tile renders as the last item in the grid at every slide count, including zero, so the empty state and the drop target are always shown together (D-13, D-08)."
    - "Dragging files anywhere over the grid highlights the whole grid, not only the tile, so the drop is not a pixel hunt (D-13)."
    - "A file matching none of the four accepted kinds is rejected with a message naming what is accepted, and nothing is uploaded (R018)."
    - "PPTX and image import reuse the existing import modal — its upload, server parse, preview and confirm logic gains a second entry point, not a second implementation (D-15)."
  artifacts:
    - "src/components/PptxImportModal.vue with an additive exposed entry point for externally-supplied files"
    - "src/components/slides/dropRouting.ts — pure partitioning of a dropped file list into the four accepted kinds plus rejects"
    - "src/components/slides/SlideDropTarget.vue and its test"
    - "src/components/slides/SlideGrid.vue with the grid-wide drag highlight, the import action, and the four persistence paths"
  key_links:
    - "The grid mounts its OWN import-modal instance and handles its confirmed event itself — the existing handler in ServiceEditorView creates a brand-new IMPORTED plan item, which is the correct behaviour for the Service Order tab and exactly the wrong behaviour for D-16"
    - "The drop tile must not carry the card class SortableJS is scoped to, or the tile counts as a draggable sibling and every reorder index shifts by one"
    - "A native file drop delivers raw File objects with no filtering — the file input's accept attribute is advisory and is bypassed entirely, so the drop path must re-run the same validation itself"
  prohibitions:
    - "MUST NOT create a new plan item, call the slot factory, or reindex the service's slots on any import path. An import grows the selected group's slides and nothing else."
    - "MUST NOT build a second PPTX upload, parse, preview or confirm implementation, and MUST NOT synthesize a DataTransfer to fire a fake change event at the modal's hidden input (D-15)."
    - "MUST NOT route a dropped video to the group bed. 25-RESEARCH.md recommended that before D-17 was decided; 25-VALIDATION.md records the correction and the owner chose the video slide."
    - "MUST NOT route a dropped audio file to a slide. Audio is the group bed only (D-14)."
    - "MUST NOT modify the existing import handler in ServiceEditorView or the Service Order tab's import buttons — those are Phase 27's to retire."
---

<objective>
Complete R032: make the slide grid accept a PPTX, an image, a video or an audio file dropped anywhere on
it, plus an explicit import action in the group header, and route each of the four kinds to the right
destination — slides for the first three, the group's music bed for audio.

Purpose: this is the phase's headline capability and the last piece of the Slides tab shell. It is also
the first native drag-and-drop surface in this codebase, so it carries the phase's only backstop-status
UI consideration.
Output: one additive modal entry point, one pure routing module, one new component, and the grid's four
persistence paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-PATTERNS.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-VALIDATION.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-01-SUMMARY.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-05-SUMMARY.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-06-SUMMARY.md
</context>

<hard_constraints>
Non-negotiable for every task in this plan:
- Do NOT run `npm run test:rules`. Do NOT start, stop, or restart the Firebase emulator.
- Use `npx vitest run <path>`. Never bare `vitest`, never `npm run test:unit`.
- Sequential main-tree execution on branch `milestone/M001`. Do not create or switch branches.
- Known-failing baseline for `npx vitest run src/` is exactly **10 FILES**: 8 under
  `.gsd/quarantine/worktrees/**`, plus `src/storage.rules.test.ts` and
  `src/views/__tests__/RosterView.test.ts`. Judge against the FILE SET, not the flapping test count.
- `npm run type-check` must stay at 0 errors and `npm run build` must succeed.
- `PptxImportModal.vue` teleports to the document body. Testing it needs a DOM wrapper over
  `document.body` plus `enableAutoUnmount(afterEach)`, and under `shallowMount` the teleport must be
  explicitly un-stubbed or its content never renders (found in 21-05 and again in 24-06).
- **25-RESEARCH.md's video recommendation is stale.** Its Pattern 5, Assumption A2 and Pitfall 7 predate
  owner decision D-17 and route a dropped video to the group bed. 25-VALIDATION.md's correction is
  authoritative: video appends a slide, audio sets the bed.
- 25-UI-SPEC.md's two DEVELOPER-APPROVED EXCEPTION blocks stand. Do not "correct" the font weights,
  `text-[10px]`, or the 6px `gap-1.5`.
</hard_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: An additive entry point on the import modal for externally-supplied files</name>
  <files>src/components/PptxImportModal.vue, src/components/__tests__/PptxImportModal.test.ts</files>
  <read_first>
    - src/components/PptxImportModal.vue in full — the two file inputs and their change handlers, `importPptx` and `importImages` (the functions the new entry point calls straight into), the step state machine, the reset-on-open watcher, and `onConfirm`'s deck creation and confirmed emit
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md § Pitfall 5 — why an exposed method is preferred over synthesizing a DataTransfer and dispatching a fake change event
    - src/components/__tests__/PptxImportModal.test.ts — the teleport and body-wrapper testing conventions already established in that file
  </read_first>
  <behavior>
    - A caller holding a component reference can hand the modal a PPTX file and the modal runs its existing PPTX import path.
    - A caller can hand it one or more image files and the modal runs its existing image import path.
    - The modal's own file inputs keep working exactly as they do today.
    - The reset-on-open behaviour, the step state machine, the retry wiring and the confirmed emit are unchanged.
    - Handing it a file after the modal is already mid-upload does not start a second import.
  </behavior>
  <action>
Add an exposed entry point to `PptxImportModal.vue` that lets an external drop zone hand it files. Expose
either two functions (one per kind) or one dispatching function — either satisfies D-15; pick one and
document it. Each must call straight into the EXISTING `importPptx` or `importImages` functions. Add no
new upload, parse, preview or confirm logic: the whole point of D-15 is that this component remains the
single import implementation and merely gains a second caller.

Guard against re-entry: if the modal is already in its uploading, parsing or confirming step, the entry
point returns without starting anything. The modal is a single-batch machine and a second concurrent
import would corrupt its preview state.

Do not synthesize a `DataTransfer` and dispatch a fake change event at the hidden inputs. It works and is
browser-compatibility fragile, and it makes the drop path depend on the modal's private DOM.

Change nothing else. Every existing test in `src/components/__tests__/PptxImportModal.test.ts` must pass
with its assertions unmodified — this component is Phase 21's and is still driven by the Service Order
tab.

Extend that test file with the new entry point's behaviours, following its existing teleport and
body-wrapper conventions.
  </action>
  <acceptance_criteria>
    - A test obtains the component reference, hands it a PPTX file, and asserts the modal advances into its upload step.
    - A test hands it image files and asserts the same.
    - A test asserts calling the entry point while the modal is mid-upload starts nothing and leaves the step unchanged.
    - A test asserts the modal's own file inputs still work.
    - Every pre-existing test in `src/components/__tests__/PptxImportModal.test.ts` passes with its assertions unmodified.
    - `npx vitest run src/components/__tests__/PptxImportModal.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/PptxImportModal.test.ts</automated>
  </verify>
  <done>A dropped deck can reach the one import implementation this app has, without a second one being written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop tile, grid-wide drag highlight, and deterministic file routing</name>
  <files>src/components/slides/dropRouting.ts, src/components/slides/__tests__/dropRouting.test.ts, src/components/slides/SlideDropTarget.vue, src/components/slides/__tests__/SlideDropTarget.test.ts, src/components/slides/SlideGrid.vue, src/components/slides/__tests__/SlideGrid.test.ts</files>
  <read_first>
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md § Copywriting Contract (the drop tile heading, its subtext, and the rejected-file line), § Grid & Interaction Rules (the tile is always last, the whole-grid highlight and its treatment), § UI Considerations (the backstop row on unsupported file types)
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md § D-13 and § D-14, and the Claude's Discretion grant covering how the drop target distinguishes the four kinds
    - src/composables/useMediaUpload.ts lines 62-73 — the exact MIME prefixes and size cap the drop path must respect
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-PATTERNS.md § No Analog Found — there is no native drag-and-drop precedent anywhere in this codebase; this is genuinely new interaction code
    - src/components/slides/SlideGrid.vue (from 25-04 through 25-06) — the card container, the empty state and the header action area
  </read_first>
  <behavior>
    - The routing module partitions a file list into deck, image, video, audio and rejected buckets, classifying a PPTX by its file extension and the other three by MIME prefix.
    - A file that matches none of the four is classified as rejected rather than guessed at.
    - The drop tile renders as the last item in the grid at every slide count, including zero.
    - The tile does not participate in drag reordering.
    - Dragging files over the grid highlights the whole grid; leaving or dropping clears the highlight.
    - A drag that carries no files does not highlight the grid.
    - Dropping on the tile and dropping anywhere else on the grid produce the same routing.
    - A drop containing an unsupported file shows the rejection message and uploads nothing for that file.
    - The tile and the drop handling are present for editors only.
  </behavior>
  <action>
Create `src/components/slides/dropRouting.ts` as a pure module partitioning a list of files into the four
accepted kinds plus a rejected bucket. Classify a deck by its file-name extension (a PPTX often arrives
with an empty or unreliable MIME type from the OS) and images, video and audio by MIME prefix, using the
same prefixes the media-upload composable validates against so the two cannot disagree. A native drop
delivers raw File objects with no filtering whatsoever — the file input's accept attribute never runs on
this path — so this module IS the filter, not a convenience. Unit-test it directly; it is the cheapest
place to prove every classification.

Also decide and export the resolution order for a multi-kind drop, since D-14 grants discretion here.
Use this rule and document it in the summary: the first audio file becomes the group's music; every
video file appends a slide, in drop order; for the two modal-backed kinds, a PPTX takes precedence and
the first one is imported, otherwise every image is imported as one deck; anything left over — extra
audio files, images skipped because a PPTX won, and anything unsupported — is reported in the inline
notice rather than silently dropped.

Create `src/components/slides/SlideDropTarget.vue` as the tile itself, using the copywriting contract's
heading and subtext verbatim — including the subtext's statement that PPTX, image and video append a
slide while audio sets the group's music, which is the one behavioural line that differs from the
mockup. Its own drop handler emits the dropped file list upward; it performs no upload and no routing
decision of its own.

Mount the tile in `SlideGrid.vue` as the last grid item, unconditionally — at zero slides it sits beneath
the empty state, at six it sits after the sixth card. Do NOT give it the card class SortableJS is scoped
to; that selector governs both drag eligibility and the old-index/new-index arithmetic, so a tile inside
the draggable set would shift every reorder index by one.

Add the grid-wide drag affordance in `SlideGrid.vue`: on drag enter and drag over, when the drag actually
carries files, prevent the default so a drop can fire and apply the highlight treatment to the grid
container; on drop and on a genuine drag leave, clear it. Guard the leave handler against the child-element
leave events that fire constantly while moving across cards inside the container — a naive leave handler
flickers the highlight off and on.

Route both drop entry points — the tile's and the container's — through the same routing module and the
same handler, so they cannot diverge.

Render the inline rejection notice near the tile using the copywriting contract's line verbatim, clearing
it after a short interval. This is the phase's one backstop-status UI consideration: there is no existing
reject-and-recover pattern in this codebase to point at, so it needs an explicit test rather than an
assumption.

Gate the tile and every drop handler on the editor flag.

This task wires the routing and the affordances; the four persistence paths land in Task 3. Keep the
handler's per-kind branches as named seams for that task rather than leaving them unreferenced.
  </action>
  <acceptance_criteria>
    - A test asserts the routing module classifies a .pptx file, an image file, a video file and an audio file into their four buckets and an unknown file into the rejected bucket.
    - A test asserts a .pptx with an empty MIME type is still classified as a deck.
    - A test asserts the documented resolution order for a drop containing several kinds at once.
    - A test asserts the drop tile renders last in the grid at zero slides and at several slides.
    - A test asserts the tile does not carry the class the drag library is scoped to.
    - A test fires a file drag over the grid container and asserts the highlight classes are applied, then cleared on leave and on drop.
    - A test fires a drag carrying no files and asserts no highlight is applied.
    - A test drops the same file set on the tile and on the container and asserts identical routing.
    - A test drops an unsupported file and asserts the rejection message renders and no upload is attempted.
    - A test mounted as a viewer asserts the tile is absent and a drop on the container does nothing.
    - `npx vitest run src/components/slides/__tests__/dropRouting.test.ts src/components/slides/__tests__/SlideDropTarget.test.ts src/components/slides/__tests__/SlideGrid.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/dropRouting.test.ts src/components/slides/__tests__/SlideDropTarget.test.ts</automated>
  </verify>
  <done>The grid is a drop target you cannot miss, and it knows exactly what it was handed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The four persistence paths and the group import action</name>
  <files>src/components/slides/SlideGrid.vue, src/components/slides/__tests__/SlideGrid.test.ts</files>
  <read_first>
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md § Pattern 4 — the worked shape for turning a created deck into group entries appended to the selected group, and why the existing view handler is the wrong one to reuse
    - src/views/ServiceEditorView.vue lines 1339-1349 — that existing handler, which creates a new IMPORTED plan item; read it to know exactly what NOT to do here, and leave it untouched
    - src/stores/importedSlides.ts lines 55-95 — `createDeck` and `getDeck`, and the deck document shape the confirmed event points at
    - src/types/slideGroup.ts — `GroupSlideEntry` and the `imported` and `video` source refs (the latter added in 25-01)
    - src/composables/useMediaUpload.ts — `uploadMedia` and its reactive progress and error refs
    - src/components/slides/SlideGrid.vue (from 25-05 and 25-06) — the on-demand group materializer prop, the append arithmetic already used by the add-slide action, and the bed-media write already used by the music control
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md § Copywriting Contract (the secondary action row, the media-upload rows)
  </read_first>
  <behavior>
    - The group header renders an import action for editors, which opens the grid's own import modal.
    - Confirming an import appends one group entry per slide in the created deck, in deck order, at the end of the selected group.
    - No new plan item is created by any import path, and the service's slots are neither pushed to nor reindexed.
    - A dropped video uploads and appends one video slide entry carrying its own source and its file name.
    - Several dropped videos append in drop order.
    - A dropped audio file sets the selected group's music bed and appends nothing.
    - Every append lands at the end, with orders continuing from the group's current highest.
    - Appending to a plan item with no group document yet works, because the group is resolved on demand first.
    - Upload progress and failure use the media-upload composable's own reactive values and the existing wording.
    - A failed upload appends nothing and leaves the group unchanged.
    - The modal receives the selected plan item's section, and the section it carries has no bearing on which group the entries land in.
  </behavior>
  <action>
Mount a `PptxImportModal` instance inside `SlideGrid.vue`, with its own open state and its own confirmed
handler. Do NOT reuse or modify the instance and handler in `ServiceEditorView`: that handler creates a
brand-new IMPORTED plan item, which is correct for the Service Order tab and is exactly the behaviour
D-16 forbids here. Mounting a second instance of the same component still satisfies D-15 — the
prohibition is against a second import IMPLEMENTATION, and there is still only one.

Add the import action to the group header's action area using the copywriting contract's label verbatim,
styled as a neutral bordered secondary button like the add-slide action, editor-only. It opens the modal
in its own idle state so the user can pick a file; the drop path instead calls the modal's exposed entry
point from Task 1 with the already-dropped file.

Pass the modal the selected plan item's section, falling back to the first service section when the plan
item has none. The section is a required prop and only sets the created deck's own section field — it has
no bearing on which group the entries land in.

Implement the confirmed handler per 25-RESEARCH.md Pattern 4: read the created deck by its import id from
the imported-slides store, resolve the selected group through the on-demand materializer, mint one group
entry per inner slide referencing that import id and that inner slide's id, append them after the group's
existing entries with orders continuing from the current highest, and write through the slide-group
store's slide-replacing action with the group's stored source signature passed through unchanged. Never
call the slot factory or the slot reindexer on this path.

Implement the video path: upload through the media-upload composable, then append one entry per video
carrying the video source ref added in 25-01 — its own source URL and its original file name — using the
same resolve-then-append-at-the-end arithmetic. This is D-17's payoff and the one place 25-RESEARCH.md's
stale recommendation must not be followed: a dropped video appends a slide, it does not set the bed.

Implement the audio path: upload through the same composable, then write the selected group's bed audio
through the bed-media write 25-06 already established. Audio appends nothing.

Reuse the composable's reactive progress and error values and the existing progress and error wording for
all upload feedback; do not author new copy. A failed upload must append nothing and leave the group
untouched.

Reading the imported-slides store and the slide-groups store directly from this component is correct —
both are plain singleton Pinia stores that `ServiceEditorView` already calls actions on. The prohibition
is on calling `useSlideshowAssembly()`, the composable, a second time.
  </action>
  <acceptance_criteria>
    - A test asserts the import action renders for an editor, not for a viewer, and opens the modal.
    - A test emits the modal's confirmed event and asserts the slide-replacing action received the group's existing entries followed by one entry per deck slide, in deck order.
    - A test asserts the appended entries reference the import id and each inner slide's id.
    - A test asserts no import path calls the slot factory or the slot reindexer, and that the service store's update action is not called.
    - A test drops a video and asserts one video entry was appended carrying the uploaded URL and the file name.
    - A test drops two videos and asserts both were appended in drop order.
    - A test drops an audio file and asserts the bed-media write was called and the slide-replacing action was not.
    - A test asserts appended orders continue from the group's current highest.
    - A test on a plan item with no group asserts the on-demand materializer was awaited before the append.
    - A test forces an upload rejection and asserts nothing was appended and the error text renders.
    - A test asserts the modal receives the selected plan item's section.
    - `npx vitest run src/components/slides/ src/components/__tests__/PptxImportModal.test.ts` exits 0.
    - `npm run type-check` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideGrid.test.ts</automated>
  </verify>
  <done>Any of the four accepted file kinds can be added to any plan item, and each lands where its decision says it should.</done>
</task>

</tasks>

## Artifacts this phase produces (contributed by 25-07)

| Symbol / path | Kind | Notes |
|---|---|---|
| import-modal exposed entry point | additive API | second caller for the existing import path, not a second implementation |
| `src/components/slides/dropRouting.ts` | new pure module | four-kind partition plus rejects; the actual filter on the native drop path |
| `src/components/slides/SlideDropTarget.vue` | new component | always the last grid item, never inside the draggable set |
| grid-wide drag highlight | new behaviour | first native drag-and-drop surface in this codebase |
| four persistence paths | new handlers | deck and image and video append slides; audio sets the bed |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS file drop → Firebase Storage | Raw File objects with attacker-influenced names, sizes and declared MIME types, with no accept-attribute filtering at all. |
| dropped deck → persisted group structure | An import writes many entries at once into a document that drives a live projector. |
| viewer session → drop and import paths | The Slides tab is reachable by viewers. |

## STRIDE Threat Register (OWASP ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07-01 | Denial of Service | oversized or wrong-type file uploaded through the drop path, which no accept attribute filters | high | mitigate | The pure routing module classifies before anything is uploaded, and every upload still goes through the media-upload composable, whose MIME and size validation runs before any bytes leave the browser and mirrors the server-side `storage.rules` ceiling. Asserted by routing tests and by an unsupported-file test that no upload is attempted. |
| T-25-07-02 | Tampering | an import silently creating a new plan item and desynchronising the rail from the plan | high | mitigate | The grid uses its own confirmed handler, never the view's; explicitly asserted by a test that the slot factory, the slot reindexer and the service store's update action are all uncalled. |
| T-25-07-03 | Tampering | a dropped video routed to the group bed per the stale research recommendation, silently discarding D-17 | high | mitigate | Called out in `<hard_constraints>` and in `must_haves.prohibitions`; asserted by a test that a video drop appends an entry and by a test that an audio drop does not. |
| T-25-07-04 | Elevation of Privilege | a viewer importing or dropping files | high | mitigate | The tile, the import action and every drop handler are gated on the editor flag; the Firestore org-editor rule on `slideGroups` and the Storage rule on the org media path are the authoritative boundaries. |
| T-25-07-05 | Tampering | an append overwriting a group's existing slides because the caller read a stale groups map | high | mitigate | Every append resolves the group through the on-demand materializer from 25-05, which returns the entries it wrote rather than relying on the lagging store map. Asserted by the append-order tests. |
| T-25-07-06 | Denial of Service | a second concurrent import corrupting the modal's preview state | medium | mitigate | The exposed entry point returns without acting while the modal is mid-upload, mid-parse or mid-confirm. Asserted by a test. |
| T-25-07-07 | Repudiation | files silently ignored in a multi-kind drop | medium | mitigate | The documented resolution order reports every skipped or unsupported file in the inline notice rather than dropping it silently. Asserted by a routing test and a notice test. |
| T-25-07-08 | Spoofing | a file whose extension and MIME type disagree being classified wrongly | low | accept | A deck is classified by extension because OS drops often supply no MIME type for PPTX; the server-side parse function is the authority on whether the bytes are really a deck, and it already fails safe with the modal's friendly error. No client-side classification can be authoritative here. |
| T-25-07-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed. 25-RESEARCH.md § Package Legitimacy Audit records "Not applicable". |
</threat_model>

<verification>
- `npx vitest run src/components/slides/ src/components/__tests__/PptxImportModal.test.ts` exits 0.
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` exits 0 — the Service Order tab's own import flow is untouched.
- `npx vitest run src/` fails in no more than the 10 baseline files listed in `<hard_constraints>`.
- `npm run type-check` exits 0 and `npm run build` exits 0.
- `git diff --stat src/views/ServiceEditorView.vue` is empty for this plan — no view change was needed.
- `git diff --stat storage.rules firestore.rules` is empty.

<human-check>
Deferred to the project's batch human-verify (STATE.md Deferred Verification), NOT a blocking
checkpoint. Carried from `25-VALIDATION.md` § Manual-Only Verifications — jsdom cannot produce a genuine
OS DataTransfer carrying real File payloads, so the whole drop gesture is manual by nature:
1. Drag `docs/example.pptx` from the desktop onto the grid and confirm its slides append to the selected
   group and that no new plan item appears in the rail.
2. Drag `docs/example.mp3` onto the grid and confirm it becomes the group's music rather than a slide.
3. Drag a real video file onto the grid and confirm it appends a video slide, then Present and confirm it
   plays.
4. Drag an unsupported file and confirm the rejection message appears and nothing uploads.
5. Confirm the whole-grid highlight appears while dragging anywhere over the grid, not only over the tile.
</human-check>
</verification>

<success_criteria>
- Any of the four accepted file kinds can be added to any plan item in the service.
- An import grows the selected group and never the plan.
- Video appends a slide and audio sets the bed, exactly as the owner decided.
- There is still only one PPTX import implementation in this codebase.
</success_criteria>

<output>
Create `.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-07-SUMMARY.md` when done.
</output>
