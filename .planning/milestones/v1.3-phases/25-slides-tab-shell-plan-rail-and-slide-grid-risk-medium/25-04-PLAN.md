---
phase: 25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium
plan: 04
type: execute
wave: 2
depends_on: ["25-03"]
files_modified:
  - src/components/slides/SlideCard.vue
  - src/components/slides/__tests__/SlideCard.test.ts
  - src/components/slides/SlideGrid.vue
  - src/components/slides/__tests__/SlideGrid.test.ts
  - src/components/slides/SlidesTab.vue
  - src/components/slides/__tests__/SlidesTab.test.ts
autonomous: true
requirements: [R031, R018]

must_haves:
  truths:
    - "Selecting a plan item in the rail shows that group's slides as cards in the grid, in the order they play (R031)."
    - "The grid derives its slides by filtering the assembled slideshow on the selected plan item's array index, so a group that has not materialized yet still shows its real slides instead of an empty grid."
    - "Each card shows the slide's text body, its kind badge, its slide number, its content label and an audio chip when that slide carries audio (D-10)."
    - "Clicking a card selects it and the selection is visible; the click is never a dead click, and the id it sets is the seam Phase 26's drawer opens against (D-12)."
    - "A group with zero slides shows the grid empty state rather than a blank column (D-08)."
    - "The grid header names the group, states its position in the plan with the 'follows plan' phrasing, and states the reading order of the cards."
    - "A pending reconciliation on the selected group surfaces as a passive, non-blocking notice — nothing is silently stale, and nothing about selecting or viewing slides is blocked by it (R018)."
    - "The grid ships as a grid only: there is no Grid/List toggle, not even a disabled one (D-09)."
  artifacts:
    - "src/components/slides/SlideCard.vue and its test"
    - "src/components/slides/SlideGrid.vue and its test"
    - "src/components/slides/SlidesTab.vue mounting the grid and completing the selection round trip"
  key_links:
    - "The grid filters on the slot's ARRAY index in the service's slots, which is what `AssembledSlide.slotIndex` carries — filtering on `groupId` instead drops every slide during the window before the group document's snapshot lands"
    - "A lyric slide and a copyright slide both carry the same content kind and are told apart by shape, so the card's content label must use the shared display helper rather than reading the raw content kind"
    - "Card selection is emitted upward to SlidesTab, which owns the selection state — the card holds no selection state of its own, so Phase 26 has exactly one place to read it from"
  prohibitions:
    - "MUST NOT build the mockup's Grid/List toggle in any form, including a disabled or single-option control (D-09)."
    - "MUST NOT render real slide formatting or slide visuals on the cards. Card content is text body plus metadata; true formatted-slide rendering is explicitly deferred (D-10, 25-CONTEXT.md Deferred Ideas)."
    - "MUST NOT build any apply, reject, review or confirm affordance for a pending reconciliation. Phase 25 renders a passive notice; the confirm flow is Phase 26 (R033)."
    - "MUST NOT block card selection or grid interaction while a reconciliation is pending — the slides being shown are the stored, currently-correct ones."
---

<objective>
Ship R031's right half: the slide grid for the selected group, its cards, and the card-selection
contract Phase 26's Edit Slide drawer will open against.

Purpose: the rail navigates; this is what it navigates to. It is also where D-12's seam becomes real —
a click that changes visible state now, and opens a drawer next phase.
Output: two new components, one wiring change in the tab shell, three test suites.

The group header's action buttons land with their behaviour in 25-05 and 25-06 rather than as dead
controls here, and the drop tile that completes the empty state lands in 25-06. Both are inside this
same phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-PATTERNS.md
@.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-03-SUMMARY.md
</context>

<hard_constraints>
Non-negotiable for every task in this plan:
- Do NOT run `npm run test:rules`. Do NOT start, stop, or restart the Firebase emulator.
- Use `npx vitest run <path>`. Never bare `vitest`, never `npm run test:unit`.
- Sequential main-tree execution on branch `milestone/M001`. Do not create or switch branches.
- Known-failing baseline for `npx vitest run src/` is exactly **10 FILES**: 8 under
  `.gsd/quarantine/worktrees/**`, plus `src/storage.rules.test.ts` and
  `src/views/__tests__/RosterView.test.ts`. Judge against the FILE SET, not the flapping test count.
- `npm run type-check` must stay at 0 errors and `npm run build` must succeed.
- **25-UI-SPEC.md carries two DEVELOPER-APPROVED EXCEPTION blocks** (owner sign-off, 2026-07-26): the
  three font weights plus `text-[10px]`, and the 6px `gap-1.5` spacing value. The card's content label
  uses `text-[10px]` and the card footer uses `gap-1.5` — both are covered by that sign-off. Do NOT
  "correct" either during implementation.
- **Tailwind v4 purges dynamically-built class names.** Reuse the static badge class map from
  `src/components/slides/slideDisplay.ts` (25-03) rather than building a second one, and never
  interpolate a colour name into a class string.
- No component under `src/components/slides/` may import or call `useSlideshowAssembly()`.
  `ServiceEditorView` is its sole owner and everything below receives props.
</hard_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SlideCard — content, metadata and selection state</name>
  <files>src/components/slides/SlideCard.vue, src/components/slides/__tests__/SlideCard.test.ts</files>
  <read_first>
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md § Color (kind badge map, the plain-text content label treatment, the accent's four permitted uses), § Typography (the meta, body and label roles), § Grid & Interaction Rules (card preview height, line clamping, card selection), § Accessible names for icon-only controls
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md § D-10 and § D-12
    - src/components/slides/slideDisplay.ts (from 25-03) — the badge class map and the content-label helper to reuse
    - src/types/slide.ts — every `Slide` variant's fields, including the `VideoSlide` added in 25-01, and `AssembledSlide`
    - src/components/SlideshowPreview.vue lines 42-95 — how each slide kind's text is currently pulled for a compact card, the closest existing precedent
    - src/views/ServiceEditorView.vue lines 513-536 — the `.slot-item` card surface classes (rounded, gray-900, gray-800 border, 12px padding) this card matches
  </read_first>
  <behavior>
    - A lyric card shows its section label and its lines; a copyright card shows the song title; a scripture card shows its reference and text; a text card shows its title and body; an image card shows the image; a video card identifies itself as video and names its file when it has one.
    - Every card shows its slot kind badge and its one-based slide number within the group.
    - A card whose slide carries audio shows an audio chip; a card without audio shows no chip and no placeholder.
    - Long body text is clamped inside a fixed-height preview area rather than growing the card or shrinking the type.
    - Clicking a card emits a selection event carrying that slide's id.
    - The selected card, and only the selected card, carries the accent border treatment.
    - The audio chip carries an accessible name when it shows no visible text.
    - The card holds no selection state of its own.
  </behavior>
  <action>
Create `src/components/slides/SlideCard.vue` as a presentational component taking one assembled slide,
its one-based number within the group, and a selected flag. It emits a selection event carrying the
slide's id and holds no state.

Compose the card from three regions, per 25-UI-SPEC.md's card anatomy and the mockup: a fixed-height
preview area carrying the content label and the body text; a slide-number chip and the kind badge; and
a footer line carrying the slide's label and its audio chip.

Pull each kind's body text through the shared content-label and body helpers rather than reading the raw
content kind inline. A lyric slide and a copyright slide both report the same content kind and are told
apart by shape, which is exactly the trap the shared helper exists to avoid — `SlideshowPreview.vue` and
`PresentationViewer.vue` both carry a local copy of that narrowing today; do not add a third. If the
shared module from 25-03 does not yet expose a body-text helper, add one there rather than inlining the
narrowing here.

Use the shared static badge class map for the kind badge. Render the content label as plain accent text
with no chip background, per the UI-SPEC's explicit note that this one element stays faithful to the
mockup because it sits over a preview surface.

The preview area is a fixed height with hidden overflow, and long body text is clamped by line count —
never scrolled inside the card and never shrunk to fit. This is consistent with D-10's deferred
rendering fidelity: cards show text, not slide visuals.

Clicking anywhere on the card emits selection. Do not build a separate hit target for it, and do not
add a drag handle in this task — the handle and its exclusion zone land in 25-05 alongside the drag
behaviour, so this phase never ships a grip that does not drag.

Give the audio chip an accessible name per the UI-SPEC's icon-only-controls table.

Do not render real slide formatting or a rendered slide visual. That is a long-standing user request
with its own deferral (25-CONTEXT.md Deferred Ideas) and is not this phase's scope.
  </action>
  <acceptance_criteria>
    - A test per slide kind (lyric, copyright, scripture, text, image, video) asserts the card renders that kind's expected text or media.
    - A test asserts the slide number rendered equals the number passed in.
    - A test asserts the kind badge classes come from the shared map for the slide's slot kind.
    - A test asserts a slide with audio renders the audio chip and a slide without audio renders none.
    - A test asserts the audio chip has an accessible name.
    - A test clicks the card and asserts the selection event carries the slide id.
    - A test asserts only a card mounted as selected carries the accent border class.
    - A test with a long body asserts the preview region carries the clamping class and a fixed height.
    - `npx vitest run src/components/slides/__tests__/SlideCard.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideCard.test.ts</automated>
  </verify>
  <done>Every kind of slide has a readable card, and clicking one visibly selects it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SlideGrid — header, slot-index filtering, cards, empty state, reconciliation notice</name>
  <files>src/components/slides/SlideGrid.vue, src/components/slides/__tests__/SlideGrid.test.ts</files>
  <read_first>
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md § Pattern 2 (deriving the selected group's slides), § Pattern 6 (pendingReconciliations — the passive-banner middle ground and what NOT to build), § Pitfall 2 (filtering by groupId)
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-UI-SPEC.md § Layout Reference, § Grid & Interaction Rules (card grid template, drop tile always last, zero-slide group), § Copywriting Contract (grid empty-state rows), § Visual hierarchy
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-CONTEXT.md § D-08, § D-09, § Specific Ideas (the grid header's three lines, verbatim from the mockup)
    - src/composables/useSlideshowAssembly.ts lines 74-88 — the `PendingReconciliation` shape including its optional loss counts
    - src/components/slides/slideDisplay.ts (from 25-03) — the display-title helper
    - src/components/slides/SlideCard.vue (from Task 1) — its props and its selection event
  </read_first>
  <behavior>
    - The grid renders one card per assembled slide belonging to the selected plan item, in play order.
    - Slides are matched by the selected plan item's array index in the service's slots, so a group with no document yet still renders its slides.
    - Cards are numbered from one within the group, independent of their position in the whole service.
    - The header names the group, states its one-based position among the plan items with the 'follows plan' phrasing, and states the reading order.
    - The reading-order line reflects the actual card count, and is omitted when there are no cards.
    - A group with no slides renders the empty-state heading and body instead of cards.
    - A pending reconciliation for the selected plan item renders a passive notice naming how many slides may need review; one for a different plan item renders nothing.
    - Cards remain selectable while a reconciliation is pending.
    - Card selection is emitted upward, and the card matching the selected slide id is the one shown as selected.
    - No view-mode toggle control renders.
  </behavior>
  <action>
Create `src/components/slides/SlideGrid.vue` taking the selected plan item, its array index within the
service's slots, its one-based position among the plan items, the total plan-item count, the assembled
slideshow, the selected slide id, the group for the selected plan item, the pending reconciliations, and
the editor-capability flag. It emits card selection upward.

Derive the group's slides by filtering the assembled slideshow on `slotIndex` equal to the selected plan
item's ARRAY index. Do not filter on `groupId`: that field is set only on the group-resolved emission
path, so filtering by it renders an empty grid for the entire window before the group document's
snapshot lands — even though the fallback path is already producing real, correct slides. 25-RESEARCH.md
Pitfall 2 documents this as the failure that makes a newly-assigned song look empty for a beat.

Build the header's three lines from the mockup and 25-CONTEXT.md § Specific Ideas: the group title
(the plan item's kind label and its display title, collapsed to one when a plan item has no title of its
own beyond its kind), the position line stating which plan item of how many this is with the
'follows plan' phrasing kept verbatim — that phrasing is the user's mirror-not-duplicate principle
stated in the UI itself — and the reading-order line stating the play order of the cards, reflecting the
real card count.

Lay the cards out with CSS Grid using the UI-SPEC's literal template and gutter, which reflows from
three columns to one without hand-authored breakpoints. This is the first CSS Grid surface in the app;
there is no in-repo precedent to reconcile against, so follow the spec's values.

Render the empty state when the filtered list is empty, using the copywriting contract's heading and
body verbatim. The body's reference to dropping a file is correct as written: 25-06 adds the drop tile
directly beneath this empty state, completing the phase's delivery of D-08.

Render the passive reconciliation notice when the pending list contains an entry whose slot id matches
the selected plan item, using the entry's own loss count for the number. Keep it non-blocking and keep
it free of any affordance: no apply, no reject, no review, no dismiss that implies resolution. The
stored slides being rendered are the currently-correct ones — the reconciler deliberately leaves them
untouched while confirmation is pending — so nothing about the grid needs to be disabled. Phase 26 owns
the confirm dialog (R033). Not surfacing this state at all was rejected because it makes the grid
silently stale, which R018 and the project's failure-visibility norm both forbid.

Do not build a Grid/List toggle in any form. D-09 cut it as unrequired scope under R031, and a
one-option control is worse than none.
  </action>
  <acceptance_criteria>
    - A test asserts the grid renders one card per assembled slide whose slot index matches the selected plan item, and none belonging to other plan items.
    - A test with assembled slides present but no group document asserts cards still render.
    - A test asserts card numbering starts at one within the group even when the group is not first in the service.
    - A test asserts the header renders the group title, the position line with the 'follows plan' phrasing, and the reading-order line.
    - A test with zero cards asserts the reading-order line is absent and the empty-state heading and body render.
    - A test asserts a pending reconciliation for the selected plan item renders the notice with the entry's loss count, and one for a different plan item renders nothing.
    - A test asserts a card is still selectable while a reconciliation is pending.
    - A test asserts no apply, reject or confirm control renders alongside the notice.
    - A test asserts the card matching the selected slide id is the one rendered as selected, and that clicking another card emits its id.
    - A test asserts no view-mode toggle control renders.
    - `npx vitest run src/components/slides/__tests__/SlideGrid.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideGrid.test.ts</automated>
  </verify>
  <done>The selected group's slides are on screen, correctly numbered, and honest about their state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount the grid in SlidesTab and close the selection round trip</name>
  <files>src/components/slides/SlidesTab.vue, src/components/slides/__tests__/SlidesTab.test.ts</files>
  <read_first>
    - src/components/slides/SlidesTab.vue (from 25-03) — the two-column layout, the selection state it holds and the documented D-12 contract
    - src/components/slides/SlideGrid.vue (from Task 2) — its props and its selection event
    - .planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-RESEARCH.md § Pitfall 4 — why a selected slide id can stop resolving
  </read_first>
  <behavior>
    - The tab's content column renders the grid for the currently selected plan item.
    - The grid receives the selected plan item's array index and its one-based position among the plan items, and those two numbers are computed independently rather than assumed equal.
    - Selecting a card in the grid updates the tab's selected slide id, and the grid then shows that card as selected.
    - Selecting a different plan item in the rail changes which group the grid shows and clears the card selection.
    - With no plan items, the content column renders nothing that throws.
  </behavior>
  <action>
Mount `SlideGrid` in the content column of `SlidesTab.vue` and pass it the selected plan item together
with both numbers it needs: the array index of that slot in the service's slots (which the grid filters
the assembled slideshow by) and its one-based position among the plan items ordered by `slot.position`
(which the header displays). Compute these separately. They coincide for a well-formed service and
diverge for one whose stored positions have drifted, and conflating them shows the wrong group's slides
— exactly the class of bug D-01's stable slot id exists to prevent elsewhere.

Handle the grid's selection event by writing the tab's selected slide id, and pass that id back down so
the grid can mark the matching card. That round trip is the whole of D-12 for this phase: the click
changes visible state now, and Phase 26 mounts its drawer against the same piece of state.

Extend the tab's test with the behaviours above, including the differing-index fixture.
  </action>
  <acceptance_criteria>
    - A test asserts the grid is mounted with the selected plan item and receives an array index matching that slot's index in the slots array.
    - A test with a service whose array order and position order differ asserts the grid receives the correct array index and the correct one-based position, and that the two differ in that fixture.
    - A test emits a card selection from the grid and asserts the tab passes that id back down as the selected slide.
    - A test changes the selected plan item and asserts the grid's plan item changed and the selected slide id was cleared.
    - A test with no plan items asserts the component renders without throwing.
    - `npx vitest run src/components/slides/` exits 0.
    - `npm run type-check` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlidesTab.test.ts</automated>
  </verify>
  <done>Rail selection drives the grid, card selection drives the seam, and neither number is guessed.</done>
</task>

</tasks>

## Artifacts this phase produces (contributed by 25-04)

| Symbol / path | Kind | Notes |
|---|---|---|
| `src/components/slides/SlideCard.vue` | new component | per-kind card content, kind badge, number, label, audio chip, selection accent |
| `src/components/slides/SlideGrid.vue` | new component | slot-index filtering, three-line header, CSS grid, empty state, passive reconciliation notice |
| `SlidesTab` grid wiring | changed | array index and plan position computed separately; selection round trip closed |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored group state → what the operator believes will project | A grid that renders stale or wrong-group slides misleads someone preparing a live service. |
| pending reconciliation state → user awareness | The one place this phase can silently hide a known divergence. |

## STRIDE Threat Register (OWASP ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-04-01 | Repudiation | the grid showing another plan item's slides because array index and plan position were conflated | high | mitigate | The two numbers are computed independently in `SlidesTab` and asserted by a test whose fixture makes them differ; the grid filters strictly on the array index that `AssembledSlide.slotIndex` carries. |
| T-25-04-02 | Repudiation | a group silently rendering stale content while a reconciliation is pending | high | mitigate | A passive notice renders for the selected group, carrying the reconciler's own loss count. Asserted by tests, including one that no resolution affordance is offered (that is Phase 26's). |
| T-25-04-03 | Denial of Service | an empty grid for a group whose document has not landed yet, read as data loss by the user | medium | mitigate | Filtering on slot array index covers both the group-resolved and fallback emission paths. Asserted by a group-absent fixture test. |
| T-25-04-04 | Elevation of Privilege | a viewer reaching a write control through the grid | high | mitigate | This plan ships no write control; the editor flag is threaded through for 25-05 and 25-06. Firestore's org-editor rule on `slideGroups` remains the authoritative boundary. |
| T-25-04-05 | Information Disclosure | a slide's operator notes leaking onto a card visible in a shared screen | low | accept | Cards render body text, label and an audio indicator only. Operator notes belong to the Phase 26 drawer and are not read here. |
| T-25-04-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed. 25-RESEARCH.md § Package Legitimacy Audit records "Not applicable". |
</threat_model>

<verification>
- `npx vitest run src/components/slides/` exits 0.
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` exits 0 — the tab wiring from 25-03 still holds.
- `npx vitest run src/` fails in no more than the 10 baseline files listed in `<hard_constraints>`.
- `npm run type-check` exits 0 and `npm run build` exits 0.

<human-check>
Deferred to the project's batch human-verify (STATE.md Deferred Verification), NOT a blocking
checkpoint. Carried from `25-VALIDATION.md` § Manual-Only Verifications:
1. Visual density, card proportions and grid reflow against `docs/design/slides-tab.dc.html`
   Turn 1 State 1 — pixel judgment.
2. Confirm the grid, not the rail, reads as the focal point of the screen (25-UI-SPEC.md § Visual
   hierarchy).
</human-check>
</verification>

<success_criteria>
- Selecting a plan item shows its slides, correctly numbered, with no wrong-group or empty-grid window.
- A click on a card visibly selects it and sets the state Phase 26 will open a drawer against.
- Nothing is silently stale, and nothing that belongs to Phase 26 was built early.
</success_criteria>

<output>
Create `.planning/phases/25-slides-tab-shell-plan-rail-and-slide-grid-risk-medium/25-04-SUMMARY.md` when done.
</output>
