---
phase: 52-default-service-template
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - src/components/settings/ServiceTemplateEditor.vue
  - src/components/settings/__tests__/ServiceTemplateEditor.test.ts
autonomous: true
requirements: [R114, R116]

must_haves:
  truths:
    - "The template editor's seed-order button reads 'Suggested Template' and is shown regardless of Vertical Worship mode; no label/availability tie to the 1-2-3 progression (R114)"
    - "Clicking 'Suggested Template' seeds the draft with the 1-2-2-3-derived entries via the shared buildSuggestedTemplateEntries() — a single source, no forked copy (R114)"
    - "The confirm-on-non-empty-draft guard and the template-reset / template-reset-confirm* testids are unchanged (R114 — copy rename only, no testid churn)"
    - "A MISC (and ANNOUNCEMENTS) template row renders a template-item-body textarea bound to entry.body; editing it updates the draft entry (R116)"
  artifacts:
    - "src/components/settings/ServiceTemplateEditor.vue: seed button label 'Suggested Template'; template-reset testid retained"
    - "template-item-body textarea in ServiceTemplateEditor.vue for MISC/ANNOUNCEMENTS entries; onBodyChange handler"
  key_links:
    - "applyReset → buildSuggestedTemplateEntries() (imported from slotTypes; replaces the inline buildSlots('1-2-2-3').map copy)"
    - "onBodyChange → draft entry.body → onSave stripUndefined(draft) payload → Firestore (schemaless, free)"
---

<objective>
Reshape the template editor's UI per R114 (rename the seed control to "Suggested Template", decouple its copy from Vertical Worship / the 1-2-3 framing) and the UI half of R116 (expose the live editor's body textarea for Miscellaneous — and Announcements — template rows, bound to the new ServiceTemplateEntry.body field).

Purpose: Make the editor present the org's suggested-template preset without Vertical-Worship language, and let an org pre-fill recurring body content (canned music, standing announcement slides) into the default. The button now seeds from the ONE shared preset helper introduced in plan 52-01, so the editor draft and every new service share identical content.

Output: Renamed/reworded seed control, a `template-item-body` textarea for MISC/ANNOUNCEMENTS, an `onBodyChange` handler, and updated component tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-default-service-template/52-RESEARCH.md

# Depends on 52-01: buildSuggestedTemplateEntries() export and ServiceTemplateEntry.body?: string
# must already exist (this plan is Wave 2). If 52-01-SUMMARY.md is not present, stop.
@.planning/phases/52-default-service-template/52-01-SUMMARY.md

# Gate discipline (CLAUDE.md): type gate is `npm run type-check` (vue-tsc --build, checks test files).
# App suite: `npx vitest run --dir src --exclude '**/rules.test.ts'`. Baseline exactly 2 known-failing files.
# The editor is teleported to <body>: tests query through a DOMWrapper over document.body (existing body() helper).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R114 — rename the seed control to "Suggested Template", drop the 1-2-3 / VW framing</name>
  <files>src/components/settings/ServiceTemplateEditor.vue, src/components/settings/__tests__/ServiceTemplateEditor.test.ts</files>
  <read_first>
    - src/components/settings/ServiceTemplateEditor.vue (empty-state copy @44-48; footer reset/confirm block @158-196; the seed button @179-185; applyReset + its comment @425-446; imports @217)
    - src/components/settings/__tests__/ServiceTemplateEditor.test.ts (the "Reset to 1-2-3 default" describe @254; confirm-copy assertion @295-296; the applyReset/Replace-Template flow @287-318)
  </read_first>
  <behavior>
    - The seed button's visible label reads "Suggested Template"; its data-testid stays "template-reset".
    - The button renders regardless of vwModeEnabled (there is no v-if on VW — confirm none is added).
    - Non-empty draft still shows the confirm before replacing; empty draft applies immediately.
    - applyReset seeds from buildSuggestedTemplateEntries(); two seeds produce fresh ids.
  </behavior>
  <action>
    Per R114. In src/components/settings/ServiceTemplateEditor.vue:
    - Seed button (~:185): change the visible label text "Reset to 1-2-3 default" → "Suggested Template". KEEP data-testid="template-reset" and the @click="onResetClick" wiring and the :disabled="!authStore.isEditor" gate. Do NOT add any v-if tied to vwModeEnabled (none exists today — confirm the button carries no VW gate).
    - Confirm copy (~:161): reword "Replace your custom template with the standard 1-2-3 flow? This clears every item you've added." → replace the "standard 1-2-3 flow" phrasing with "the Suggested Template" (e.g. "Replace your custom template with the Suggested Template? This clears every item you've added."). Keep the template-reset-confirm, template-reset-cancel, template-reset-confirm-button testids and the "Replace Template" button label unchanged.
    - Empty-state copy (~:45-48): reword the "click Reset to 1-2-3 default to start from the standard Vertical Worship flow" sentence off the 1-2-3 / Vertical Worship framing → e.g. "or click Suggested Template to start from the suggested service order." Keep the template-empty-state testid.
    - applyReset (~:438-444): replace the inline `buildSlots('1-2-2-3').map(...)` body with `draft.value = buildSuggestedTemplateEntries()`. Reword the section comment at ~:425-427 ("never an automatic fallback for an unset template") — the same preset IS now the createService fallback source (R115), reframed as the suggested starting template, not a VW artifact.
    - Imports (~:217): replace `buildSlots` with `buildSuggestedTemplateEntries` in the import from '@/utils/slotTypes' (buildSlots is no longer referenced in this file — confirm no other use before removing; createSlot, slotLabel, groupBySection, flattenBySection stay).
    - Update ServiceTemplateEditor.test.ts: rename the describe/any assertion that hard-codes the "Reset to 1-2-3 default" label to assert the button text is "Suggested Template" (query by data-testid="template-reset"); update the confirm-copy assertion (~:295-296) to the new sentence. Keep the Replace-Template flow (empty applies immediately; non-empty shows confirm; confirm-button seeds the entries) — assert the seeded draft has the 9 suggested entries (kinds SONG/SCRIPTURE/SONG/PRAYER/SCRIPTURE/SONG/SONG/MESSAGE/SONG) rather than asserting VW-flow language.
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/ServiceTemplateEditor.test.ts && npm run type-check</automated>
  </verify>
  <done>Button reads "Suggested Template" (testid template-reset retained); confirm/empty-state copy carry no 1-2-3 / Vertical Worship framing; applyReset seeds via buildSuggestedTemplateEntries(); buildSlots import removed; component tests green; type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R116 — expose the body textarea for MISC/ANNOUNCEMENTS template rows</name>
  <files>src/components/settings/ServiceTemplateEditor.vue, src/components/settings/__tests__/ServiceTemplateEditor.test.ts</files>
  <read_first>
    - src/components/settings/ServiceTemplateEditor.vue (the per-entry template-item block @89-138, esp. the flex-1 content column @108-110; onSectionChange @284-288 as the handler pattern to mirror)
    - src/views/ServiceEditorView.vue (the live MESSAGE/ANNOUNCEMENTS/MISC body textarea @1092-1100 — the UI to mirror)
  </read_first>
  <behavior>
    - A MISC template row renders a textarea with data-testid="template-item-body" bound to entry.body.
    - An ANNOUNCEMENTS template row renders the same textarea (owner example: "more announcement slides").
    - Non-body kinds (SONG/SCRIPTURE/PRAYER/MESSAGE) render no template-item-body textarea.
    - Typing sets the draft entry's body; clearing it to empty leaves the entry bodyless (undefined).
  </behavior>
  <action>
    Per R116. Claude's Discretion decision (recorded): scope the template body input to MISC and ANNOUNCEMENTS — MISC is the named requirement, ANNOUNCEMENTS mirrors the owner's literal example ("canned music, more announcement slides"); MESSAGE is left without a template body input (a templated sermon body is not an owner ask). The underlying createSlot/buildSlotsFromTemplate threading (plan 52-01) already supports all three body-bearing kinds, so this is purely which rows show the control.

    In src/components/settings/ServiceTemplateEditor.vue:
    - Inside the per-entry `template-item` row, within the `flex-1 min-w-0` content column (after the `<p>{{ kindLabel(entry.kind) }}</p>`), add a `<textarea>` shown via `v-if="entry.kind === 'MISC' || entry.kind === 'ANNOUNCEMENTS'"`, bound `:value="entry.body ?? ''"` and `@input="onBodyChange(entry.id, ($event.target as HTMLTextAreaElement).value)"`, rows="2", a static placeholder such as "Recurring content for this item…", data-testid="template-item-body", styled to match the row's dark inputs (mirror the live editor's textarea classes at ServiceEditorView.vue:1099, e.g. rounded-md bg-gray-800 border border-gray-700 text-gray-200 text-sm px-3 py-2 focus ring, w-full, resize-y, mt-2). Do NOT use v-html anywhere (ASVS V5 — Vue text/attr binding auto-escapes).
    - Add an onBodyChange(id: string, value: string): void handler mirroring onSectionChange: find the draft entry by id; set entry.body = value === '' ? undefined : value (keeps a cleared body truly absent, consistent with onSectionChange's empty→undefined rule, so a bodyless entry stays bodyless through onSave's stripUndefined).
    - No change needed to onSave — stripUndefined(draft.value) already carries a present body and drops an undefined one; Firestore is schemaless.
    - Add the assertion to ServiceTemplateEditor.test.ts: seed/add a MISC entry (palette-add-misc) and assert exactly one [data-testid="template-item-body"] textarea renders in that row; assert an ANNOUNCEMENTS entry also renders it; assert a SONG/PRAYER entry renders none; assert @input updates the draft (e.g. after setValue, the textarea's value reflects the typed text). Query through the body() DOMWrapper (teleported panel).
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/ServiceTemplateEditor.test.ts && npm run type-check</automated>
  </verify>
  <done>MISC and ANNOUNCEMENTS template rows render a template-item-body textarea bound to entry.body; non-body kinds render none; onBodyChange updates the draft (empty → undefined); component tests green; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor draft (client) → Firestore | onSave mirror-writes settings.defaultServiceTemplate (now body-bearing) to organizations/{orgId} |
| stored body text → slide render | A template body flows into a created service's slot body, later rendered on slides |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-03 | Tampering (stored XSS) | template-item-body free-text field | medium | mitigate | Render only through Vue text/attribute binding (`:value` / interpolation) — auto-escaped. Do NOT introduce v-html for the body, matching the live editor's existing escaped body rendering (ServiceEditorView.vue:1102 whitespace-pre-wrap text). |
| T-52-04 | Elevation of Privilege | Save gated by authStore.isEditor | low | mitigate | onSave already returns early unless authStore.isEditor; the template write path is the unchanged server-gated updateDoc(organizations/{orgId}). Client gate is defence-in-depth, not the boundary. |
| T-52-05 | Tampering (undefined→Firestore) | onBodyChange / onSave | low | mitigate | onBodyChange normalizes empty → undefined; onSave's existing stripUndefined removes undefined keys so no raw undefined reaches Firestore. |
</threat_model>

<verification>
- `npx vitest run src/components/settings/__tests__/ServiceTemplateEditor.test.ts` green.
- `npm run type-check` (vue-tsc --build) clean.
- Full app suite `npx vitest run --dir src --exclude '**/rules.test.ts'` at the exactly-2-file baseline.
- Manual (deferred per v1.6 grant): open the editor cog, click "Suggested Template", confirm it seeds the suggested order; add a MISC item, type a body, Save, create a service, confirm the MISC slot carries the body.
</verification>

<success_criteria>
R114: seed button labelled "Suggested Template", shown regardless of VW mode, no 1-2-3/VW dependence in label or copy, seeding via the single shared helper. R116 (UI half): MISC (and ANNOUNCEMENTS) template rows expose a body textarea bound to entry.body, edits persisting through the existing save path.
</success_criteria>

<output>
Create `.planning/phases/52-default-service-template/52-02-SUMMARY.md` when done.
</output>
