---
phase: 52-default-service-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/organization.ts
  - src/utils/slotTypes.ts
  - src/stores/services.ts
  - src/utils/__tests__/slotTypes.test.ts
  - src/stores/__tests__/services.test.ts
autonomous: true
requirements: [R115, R116]

must_haves:
  truths:
    - "Creating a new service with an empty/unset defaultServiceTemplate produces a service seeded from the Suggested Template — the 9-slot 1-2-2-3-derived order SONG, SCRIPTURE, SONG, PRAYER, SCRIPTURE, SONG, SONG, MESSAGE, SONG — not 0 slots (R115, supersedes v1.5 Phase 44 criterion #2)"
    - "With Vertical Worship mode on, the suggested template's 5 SONG slots receive requiredVwType following the ordinal sequence [1,2,2,3,3] at creation (R115 criterion 3)"
    - "A non-empty stored template is still used verbatim (kind/section/order), unchanged from v1.5 (R115)"
    - "A template MISC entry carrying body text produces a service MISC slot with that body; a bodyless entry produces a slot with the 'body' key absent (R116)"
    - "buildSlotsFromTemplate([], true) still returns [] — the util stays pure, the fallback is the caller's decision (R115)"
    - "createSlot('MISC') / createSlot('ANNOUNCEMENTS') with no body argument still omit the body key entirely (R116, existing shape preserved)"
  artifacts:
    - "src/utils/slotTypes.ts exports buildSuggestedTemplateEntries(): ServiceTemplateEntry[]"
    - "src/types/organization.ts ServiceTemplateEntry gains optional body?: string"
    - "src/stores/services.ts createService resolves the effective template (stored vs suggested) at the call site"
  key_links:
    - "createService → buildSuggestedTemplateEntries() (empty→suggested) → buildSlotsFromTemplate (stays pure) → createSlot"
    - "ServiceTemplateEntry.body → buildSlotsFromTemplate(entry.body) → createSlot 4th param → NonAssignableSlot.body"
    - "buildSuggestedTemplateEntries derived from buildSlots('1-2-2-3') — ONE definition shared with the R114 button (plan 52-02)"
---

<objective>
Reverse the v1.5 empty-by-default creation path and thread a template body field through the pure slot-building path. Delivers R115 (every new service starts from the Suggested Template — fallback resolved at the createService call site, buildSlotsFromTemplate kept pure) and the util/type/store half of R116 (optional body on ServiceTemplateEntry, threaded through buildSlotsFromTemplate → createSlot into the created slot).

Purpose: This is the foundational data-path plan. It introduces the single shared preset helper `buildSuggestedTemplateEntries()` that the template-editor button (plan 52-02) also consumes, so the suggested-template content can never fork into two copies. It also lands the `ServiceTemplateEntry.body` type field that plan 52-02's textarea binds against.

Output: `buildSuggestedTemplateEntries` export, `ServiceTemplateEntry.body?` field, a createService effective-template resolution, and the reversed/extended unit tests that pin all of it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-default-service-template/52-RESEARCH.md

# Gate discipline (CLAUDE.md):
#  - Type gate is `npm run type-check` (= vue-tsc --build; checks test files). NOT `-p tsconfig.app.json`.
#  - App suite: `npx vitest run --dir src --exclude '**/rules.test.ts'` (or bare `npx vitest run`).
#  - Known-failing baseline is EXACTLY 2 files: src/storage.rules.test.ts and
#    src/views/__tests__/RosterView.test.ts. This plan touches neither — baseline stays 2.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — reverse and extend the unit tests (RED-first)</name>
  <files>src/stores/__tests__/services.test.ts, src/utils/__tests__/slotTypes.test.ts</files>
  <read_first>
    - src/utils/__tests__/slotTypes.test.ts (existing purity test @798; the "omits body entirely" tests @643/@656; the buildSlotsFromTemplate + progressionVwTypeSequence blocks @783-)
    - src/stores/__tests__/services.test.ts (the empty→0 test @489; the verbatim/VW tests @508/@536; the describe header @465)
    - src/utils/slotTypes.ts (buildSlots @273-319 for the exact 9-kind suggested order)
  </read_first>
  <action>
    Write the reversing/extending assertions FIRST so they fail (RED) until Tasks 2-3 land. Per R115 and R116.

    In src/stores/__tests__/services.test.ts:
    - REVERSE the test at ~:489 ("an empty/unset defaultServiceTemplate produces ... 0 slots"). Its NEW truth: an empty/unset defaultServiceTemplate produces a new service seeded from the Suggested Template. Assert slots has length 9 and slots.map(s => s.kind) equals ['SONG','SCRIPTURE','SONG','PRAYER','SCRIPTURE','SONG','SONG','MESSAGE','SONG'] (the buildSlots('1-2-2-3') kind order). Rename the test title to describe the suggested-template start, and update the describe header at ~:465 off "empty-by-default" to "template-driven, suggested-by-default".
    - Leave the "non-empty template ... verbatim" (~:508) and "VW mode ON ... ordinal 1-2-2-3 sequence" (~:536) tests unchanged — they must still pass.
    - Add ONE test: with an empty template AND vwModeEnabled true (set mockAuthState.settings.vwModeEnabled = true), the 5 SONG slots' requiredVwType equals [1,2,2,3,3] in order (proves criterion 3 holds for the suggested fallback, not only a customized template).

    In src/utils/__tests__/slotTypes.test.ts:
    - Add a describe for buildSuggestedTemplateEntries: it returns 9 entries whose kinds equal the same 9-kind order above; every entry has a non-empty string id; two successive calls return different ids for the same ordinal position (fresh crypto.randomUUID per call); sections match buildSlots('1-2-2-3') defaults (worship for positions 0-6, message for position 7, sending for position 8). Also assert buildSlotsFromTemplate(buildSuggestedTemplateEntries(), true) yields 5 SONG slots with requiredVwType [1,2,2,3,3].
    - Extend the "ANNOUNCEMENTS and MISC" block WITHOUT touching @643/@656: add that createSlot('MISC', undefined, undefined, 'canned music') sets slot.body === 'canned music', and the same for createSlot('ANNOUNCEMENTS', ...). Keep @643/@656 (bodyless call → 'body' in slot === false) exactly as-is.
    - Extend the buildSlotsFromTemplate block WITHOUT touching @798: add that an entry { kind: 'MISC', body: 'x' } threads body 'x' into the built slot, and an entry { kind: 'MISC' } (no body) yields a slot where 'body' in slot === false.

    Do NOT import from a not-yet-existing symbol in a way that breaks unrelated tests — buildSuggestedTemplateEntries will be added in Task 2; importing it now is the intended RED (module-resolution/compile failure limited to this file until Task 2).
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slotTypes.test.ts src/stores/__tests__/services.test.ts</automated>
  </verify>
  <done>New/reversed assertions are present and RED (failing on the missing buildSuggestedTemplateEntries export, the missing createSlot body param, and the not-yet-reversed createService), while the untouched purity (@798) and omits-body (@643/@656) tests still pass. RED for the correct reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add body to the type; add buildSuggestedTemplateEntries + thread body through slotTypes</name>
  <files>src/types/organization.ts, src/utils/slotTypes.ts</files>
  <read_first>
    - src/types/organization.ts (ServiceTemplateEntry @12-16; defaultServiceTemplate JSDoc @60-68 — the stale empty-default sentence)
    - src/utils/slotTypes.ts (createSlot @78-122; buildSlots @273-319; buildSlotsFromTemplate @362-380 incl. docstring @339-361)
    - src/types/service.ts (NonAssignableSlot.body? @73-88 — the target field, already exists)
  </read_first>
  <behavior>
    - buildSuggestedTemplateEntries() returns the 1-2-2-3 entry list (9 entries, kinds SONG/SCRIPTURE/SONG/PRAYER/SCRIPTURE/SONG/SONG/MESSAGE/SONG), fresh ids each call, sections from buildSlots defaults.
    - createSlot('MISC', vw?, section?, 'text') → slot.body === 'text'; createSlot('MISC') → 'body' in slot === false.
    - buildSlotsFromTemplate threads entry.body; empty entries → [] unchanged (purity).
  </behavior>
  <action>
    Per R116 and R115.

    src/types/organization.ts: add optional `body?: string` to the ServiceTemplateEntry interface (after `section?`). Update the interface JSDoc note "never chosen content (no ... body text)" — body IS now a carried field for body-bearing kinds (R116). Reverse the defaultServiceTemplate JSDoc sentence at ~:64-67 that says an empty/unset template produces an EMPTY new service: it now produces the Suggested Template (R115 supersedes the 2026-08-07 override). Keep the "entries carry only creation shape, VW type derived fresh" spirit; do not add a position field.

    src/utils/slotTypes.ts:
    - Add exported function buildSuggestedTemplateEntries(): ServiceTemplateEntry[] that returns buildSlots('1-2-2-3').map(slot => ({ id: crypto.randomUUID(), kind: slot.kind, ...(slot.section ? { section: slot.section } : {}) })). This is the ONE source of the suggested-template content (plan 52-02's applyReset will import it — do not duplicate the buildSlots('1-2-2-3').map body anywhere else). ServiceTemplateEntry is already imported at the top of this file.
    - createSlot: add a 4th optional parameter `body?: string` after `section`. Add `const bodyFields = body ? { body } : {}` alongside the existing sectionFields. Spread `...bodyFields` into the MESSAGE, ANNOUNCEMENTS, and MISC return objects only (the three body-bearing NonAssignable kinds the live editor treats the same way; PRAYER has no body input). Do NOT set body: '' or body: undefined unconditionally — the `...(body ? {body} : {})` spread keeps the key absent when unset (pins @643/@656). Keep the switch exhaustive with NO default arm (vue-tsc --build must stay clean).
    - buildSlotsFromTemplate: pass entry.body as the 4th argument — createSlot(entry.kind, vwType, entry.section, entry.body). Keep the empty-entries → [] behavior and the pure `[]`→`[]` contract intact (do NOT reinstate any buildSlots() fallback inside this function). Fix the docstring parenthetical at ~:357-360 that says "services.ts::createService does not make it, per the owner's 2026-08-07 override" — createService NOW resolves a suggested fallback (R115); keep the "stays pure / the caller decides" sentence.
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/slotTypes.test.ts && npm run type-check</automated>
  </verify>
  <done>slotTypes.test.ts is green (buildSuggestedTemplateEntries, createSlot body, buildSlotsFromTemplate body threading all pass; @643/@656/@798 still green). `npm run type-check` (vue-tsc --build) is clean. ServiceTemplateEntry.body?: string exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve the effective template at createService (empty → suggested)</name>
  <files>src/stores/services.ts</files>
  <read_first>
    - src/stores/services.ts (createService @233-257; the slotTypes import @28)
    - src/utils/slotTypes.ts (buildSuggestedTemplateEntries added in Task 2)
  </read_first>
  <behavior>
    - Empty/unset defaultServiceTemplate → new service seeded from buildSuggestedTemplateEntries() (9 slots).
    - Non-empty stored template → used verbatim (unchanged).
    - vwModeEnabled still passed through so SONG slots receive VW types.
  </behavior>
  <action>
    Per R115. In src/stores/services.ts::createService, add buildSuggestedTemplateEntries to the existing import from '@/utils/slotTypes' (line 28, already imports buildSlotsFromTemplate and orderSlotsBySection). Replace the direct buildSlotsFromTemplate call: read `const stored = authStore.settings.defaultServiceTemplate`, compute `const effectiveTemplate = stored.length > 0 ? stored : buildSuggestedTemplateEntries()`, then `const slots = buildSlotsFromTemplate(effectiveTemplate, authStore.settings.vwModeEnabled)`. Do NOT push the fallback into buildSlotsFromTemplate — the resolution is the caller's decision here (keeps the util pure). Reverse the stale comment block at ~:235-239 that says "or an EMPTY service when the template is unset (owner override 2026-08-07) ... buildSlots() is NEVER reinstated": rewrite it to describe the Suggested Template fallback via buildSuggestedTemplateEntries() (R115). vwModeEnabled continues to be passed so the ordinal VW walk applies to the fallback too.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts && npm run type-check</automated>
  </verify>
  <done>services.test.ts green: empty template → 9 suggested slots in the SONG/SCRIPTURE/SONG/PRAYER/SCRIPTURE/SONG/SONG/MESSAGE/SONG order; VW-on empty template → SONG requiredVwType [1,2,2,3,3]; non-empty template still verbatim. `npm run type-check` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| createService → Firestore | The created service's slots (now possibly body-bearing) are written to `organizations/{orgId}/services/{id}` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Tampering / data corruption | createSlot / buildSlotsFromTemplate body threading | medium | mitigate | Set body via `...(body ? { body } : {})` so an unset/empty body leaves the key absent — never writes raw `undefined` (Firestore rejects it); existing `stripUndefined` on the editor save payload is the backstop. Verified by the @643/@656 omits-body tests staying green. |
| T-52-02 | Tampering | createService slot payload | low | accept | No new auth surface; write path is the same already-gated `addDoc(organizations/{orgId}/services)`. No package installs this phase (no `T-52-SC` row). |
</threat_model>

<verification>
- `npx vitest run src/utils/__tests__/slotTypes.test.ts src/stores/__tests__/services.test.ts` green.
- `npm run type-check` (vue-tsc --build) clean — createSlot's exhaustive switch stays default-free.
- Full app suite `npx vitest run --dir src --exclude '**/rules.test.ts'` at the exactly-2-file known-failing baseline.
</verification>

<success_criteria>
R115: every new service starts from the Suggested Template (empty → 9 suggested slots) with VW types applied when vwModeEnabled; buildSlotsFromTemplate stays pure. R116 (util/type half): ServiceTemplateEntry.body threads through buildSlotsFromTemplate → createSlot into the created slot; bodyless entries keep the absent-body shape.
</success_criteria>

<output>
Create `.planning/phases/52-default-service-template/52-01-SUMMARY.md` when done.
</output>
