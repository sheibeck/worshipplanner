---
phase: 51-service-order-editing-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/ServiceTemplateEditor.vue
  - src/components/settings/__tests__/ServiceTemplateEditor.test.ts
autonomous: true
requirements: [R110]
must_haves:
  truths:
    - "Dragging a template item across sections in the DEFAULT-TEMPLATE editor leaves exactly one rendered row for that item — no orphaned, handler-less 'No Section' duplicate remains in the source list."
    - "The phantom is gone without a page refresh; the fix mirrors the live-editor fix (same DOM-ownership root cause, same mechanism)."
    - "The v1.4 per-section SortableJS architecture (stable entry.id keys, evt.*DraggableIndex reads) and the existing save-time stripUndefined are unchanged."
  artifacts:
    - "src/components/settings/__tests__/ServiceTemplateEditor.test.ts — a DOM-mutating cross-section drag repro that physically moves the node between template containers before onEnd."
    - "src/components/settings/ServiceTemplateEditor.vue — a container render-nonce bump in onTemplateSortEnd that rebuilds the Sortable-mutated section-list container <div> from reactive state."
  key_links:
    - "onTemplateSortEnd bumps templateRenderNonce AFTER draft.value is reassigned; the ref-bearing template section-list container <div> :key includes that nonce, so Vue discards and rebuilds the orphaned node from state."
---

<objective>
Fix R110 in the default-service-template editor (`ServiceTemplateEditor.vue`): the same cross-section drag phantom the live editor has. R110 explicitly covers the default-template editor. Repro-test-first — commit a FAILING DOM-level test, then apply the same nonce-rebuild fix.

Purpose: The template editor ports `ServiceEditorView.vue`'s per-section SortableJS reorder byte-for-byte (51-RESEARCH.md), so it carries the identical DOM-ownership defect. Phase 52 makes this editor the universal starting point for new services, so it must inherit the reliability fix now.
Output: One RED repro test + one source fix; the template-editor phantom is gone, proven by automated DOM assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-service-order-editing-reliability/51-RESEARCH.md
@.planning/phases/51-service-order-editing-reliability/51-CONTEXT.md
@CLAUDE.md
@src/components/settings/ServiceTemplateEditor.vue
@src/components/settings/__tests__/ServiceTemplateEditor.test.ts
@src/components/slides/SlideGrid.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — DOM-mutating cross-section drag repro in ServiceTemplateEditor.test.ts</name>
  <read_first>
    - src/components/settings/__tests__/ServiceTemplateEditor.test.ts (its `sortablejs` capture mock + any existing `onTemplateSortEnd` synthetic-event helper — mirrors the ServiceEditorView harness).
    - src/components/settings/ServiceTemplateEditor.vue:55-117 (section container markup: the ref-bearing section-list container `<div>` at 70 with `:data-testid="template-section-list-${group.key}"`, rows keyed `:key="entry.id"` at 86) and 338-361 (`onTemplateSortEnd`).
    - 51-RESEARCH.md § "CRITICAL caveat for the R110 RED test" and § Pitfall 1 (the mock does NOT move nodes).
  </read_first>
  <files>src/components/settings/__tests__/ServiceTemplateEditor.test.ts</files>
  <action>
    Add ONE new test reproducing the cross-section phantom. As in the live editor, the mocked `Sortable.create` never relocates a node, so the test MUST physically simulate SortableJS's real move: resolve the source template container (`[data-testid="template-section-list-ungrouped"]`) and the target section container (`[data-testid="template-section-list-worship"]`), read the row selector the editor uses (the element keyed by `entry.id` inside those containers — confirm exact class/tag from the markup at 55-117), detach the dragged row DOM node from the source and append it into the target BEFORE invoking the captured `onEnd`, then await a re-render.

    Scenario: mount the template editor with a draft that has an item at "No Section" (ungrouped), drag it into the worship section. After the simulated physical move + `onEnd` + re-render, assert on RENDERED DOM node counts (never on the reactive `draft` array — it is already correct): (a) the ungrouped container has ZERO rows matching the moved entry's id, and (b) the whole subtree contains EXACTLY ONE row for that id. Keep the physical-move logic additive so existing tests are untouched. Per CONTEXT locked decision "Write a failing reproduction test FIRST".
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/ServiceTemplateEditor.test.ts</automated>
    Run the scoped command; the NEW test must FAIL (RED) — the buggy code leaves a duplicate row in the ungrouped container. Every pre-existing test in the file must still pass.
  </verify>
  <done>The new DOM-mutating cross-section repro test exists and FAILS for the documented reason (orphaned duplicate row remains in the source list after a cross-section drag). No other test in the file regresses. Committed as the RED baseline before any source change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — nonce-rebuild the Sortable-mutated section container in onTemplateSortEnd</name>
  <behavior>
    - After a cross-section drag: source container has 0 duplicate rows for the moved id; exactly 1 exists in the target section.
    - Same-section reorder still lands the item at the dropped position with no duplicate.
    - No library swap, no change to the reactive move logic, the `evt.*DraggableIndex` reads, or the save-time `stripUndefined`.
  </behavior>
  <read_first>
    - src/components/settings/ServiceTemplateEditor.vue:313-399 (`sectionSortables` map, `onTemplateSortEnd` at 338, the ref-callback Sortable teardown/recreate at ~373-394) and 55-117 (section container markup).
    - src/components/slides/SlideGrid.vue:262, 1065, 1141 — the in-repo nonce precedent (`gridRenderNonce`).
    - 51-RESEARCH.md § "R110 fix pattern" option 2.
  </read_first>
  <files>src/components/settings/ServiceTemplateEditor.vue</files>
  <action>
    Apply the identical nonce-keyed container re-render used for the live editor (51-01), so both editors share one proven mechanism. Add a component-local `const templateRenderNonce = ref(0)`. On the ref-bearing section-list container `<div>` at ServiceTemplateEditor.vue:70 (the element carrying `:data-testid="template-section-list-${group.key}"`), add a `:key` combining the section key with the nonce so a bump forces Vue to discard and rebuild that container from reactive state. At the END of `onTemplateSortEnd`, immediately after the reactive reassignment `draft.value = flattenBySection(grouped)` (~line 360), increment `templateRenderNonce.value` by 1.

    Do NOT touch the reactive move logic (`moved.section = toKey`/`undefined`, `groupBySection`/`flattenBySection`), the `evt.*DraggableIndex` reads, the per-section Sortable architecture, or the existing save-path `stripUndefined(draft.value)` at line 442 — this adds ONLY the container rebuild. Confirm the ref-callback teardown/recreate at ~373-394 re-binds the section Sortable onto the rebuilt container `<div>`. Keep stable `entry.id` row keys as-is.
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/ServiceTemplateEditor.test.ts && npm run type-check</automated>
    The Task 1 RED test now PASSES (GREEN); all other tests in the file still pass; `vue-tsc --build` is clean.
  </verify>
  <done>The template-editor cross-section drag repro passes: no duplicate row in the source list, exactly one in the target section, no refresh. Same-section reorder tests still pass. `npm run type-check` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SortableJS DOM ↔ Vue reactive ownership | Cross-container drags let SortableJS mutate DOM Vue owns; the desync is client-only and never crosses into the persisted `OrgSettings.defaultServiceTemplate`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02-01 | Tampering | `onTemplateSortEnd` reactive move | low | accept | Reactive move logic unchanged; the fix only re-renders from already-correct state. The existing save-time `stripUndefined` (line 442) is untouched. RED DOM test gates correctness. |
| T-51-02-SC | Tampering | npm installs | low | accept | No package installs this phase. No registry verification required. |
</threat_model>

<verification>
- `npx vitest run src/components/settings/__tests__/ServiceTemplateEditor.test.ts` — full file green (RED repro now passing).
- `npm run type-check` — clean (`vue-tsc --build`).
- Both editors now use the same nonce-rebuild mechanism; no shared helper was extracted (CONTEXT: extract only if it reduces risk — the nonce is component-local state, unsuited to extraction).
</verification>

<success_criteria>
- A cross-section drag in the default-template editor places exactly one item in the target section with no phantom "No Section" duplicate, proven by automated DOM assertions and holding without a refresh (R110, ROADMAP success criteria 1 & 4 — template-editor half).
- The RED repro was committed before the fix.
</success_criteria>

<output>
Create `.planning/phases/51-service-order-editing-reliability/51-02-SUMMARY.md` when done.
</output>
