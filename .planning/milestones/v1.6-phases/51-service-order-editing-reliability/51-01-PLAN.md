---
phase: 51-service-order-editing-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R110]
must_haves:
  truths:
    - "Dragging a service item across sections in a LIVE service plan leaves exactly one rendered .slot-item for that item — no orphaned, handler-less 'No Section' duplicate remains in the source list."
    - "The phantom is gone without a page refresh (fix corrects the SortableJS<->Vue DOM-ownership desync at its source)."
    - "Same-section reorder and the v1.4 per-section SortableJS architecture (stable slot.id keys, evt.*DraggableIndex reads) are unchanged."
  artifacts:
    - "src/views/__tests__/ServiceEditorView.test.ts — a DOM-mutating cross-section drag repro that physically moves the node between containers before onEnd."
    - "src/views/ServiceEditorView.vue — a container render-nonce bump in onSlotSortEnd that rebuilds the Sortable-mutated section-list container <div> from reactive state."
  key_links:
    - "onSlotSortEnd bumps slotRenderNonce AFTER the reactive slots reassignment; the ref-bearing section-list container <div> :key includes that nonce, so Vue discards and rebuilds the orphaned node from state."
---

<objective>
Fix R110 in the LIVE service plan editor (`ServiceEditorView.vue`): a cross-section drag spawns a second, undeletable "No Section" copy of the dragged item. Repro-test-first — commit a FAILING DOM-level test that reproduces the phantom, then apply the minimal fix that makes it pass without a page refresh.

Purpose: Cross-section drags are the single most disruptive editing defect; this restores trust in the live Service Order tab. Confirmed root cause (51-RESEARCH.md): the reactive logic in `onSlotSortEnd` is already correct, but neither editor reverts SortableJS's physical cross-container DOM move, leaving an orphaned handler-less clone in the source ("No Section") list.
Output: One RED repro test + one source fix; the phantom is gone, proven by automated DOM assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-service-order-editing-reliability/51-RESEARCH.md
@.planning/phases/51-service-order-editing-reliability/51-CONTEXT.md
@CLAUDE.md
@src/views/ServiceEditorView.vue
@src/views/__tests__/ServiceEditorView.test.ts
@src/components/slides/SlideGrid.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — DOM-mutating cross-section drag repro in ServiceEditorView.test.ts</name>
  <read_first>
    - src/views/__tests__/ServiceEditorView.test.ts:66-107 (the capturing `sortablejs` mock, `captureForSection`, `flatCapture`) and 4095-4161 (`simulateSlotDrag` and its DOM-index helpers — the pattern to extend).
    - src/views/ServiceEditorView.vue:786-871 (section container markup: the ref-bearing section-list container `<div>` at 837 with `:data-testid="section-list-${group.key}"`, rows `<div class="slot-item" :key="slot.id">` at 866) and 1881-1969 (`onSlotSortEnd`).
    - 51-RESEARCH.md § "CRITICAL caveat for the R110 RED test" and § "Common Pitfalls / Pitfall 1".
  </read_first>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <action>
    Add ONE new test to the cross-section drag describe block. The existing `simulateSlotDrag` derives SortableJS indices from the live DOM but does NOT relocate the node — and the mocked `Sortable.create` never moves a node either — so calling `onEnd` alone passes green on buggy code and proves nothing (Pitfall 1). The new test MUST physically simulate SortableJS's real move: resolve the source container (`[data-testid="section-list-ungrouped"]`) and the target section container (`[data-testid="section-list-worship"]`), detach the dragged `.slot-item` DOM node from the source and append it into the target BEFORE invoking the captured `onEnd`, then await a re-render.

    Scenario: mount a live service, add a Song item at "No Section" (ungrouped), drag it into the worship section. After the simulated physical move + `onEnd` + re-render, assert on RENDERED DOM node counts (never on the reactive `slots` array — it is already correct): (a) the ungrouped container has ZERO `.slot-item` matching the moved slot's id, and (b) the whole mounted subtree contains EXACTLY ONE `.slot-item` for that id. Keep the physical-move logic additive (an inline helper in the test or a new `simulateCrossSectionDragWithDomMove`) so existing tests that call `simulateSlotDrag` are untouched. Per CONTEXT locked decision "Write a failing reproduction test FIRST (RED -> GREEN)".
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
    Run the scoped command; the NEW test must FAIL (RED) — the buggy code leaves a duplicate `.slot-item` in the ungrouped container. Every pre-existing test in the file must still pass.
  </verify>
  <done>The new DOM-mutating cross-section repro test exists and FAILS for the documented reason (an orphaned duplicate `.slot-item` remains in the source list after a cross-section drag). No other test in the file regresses. Committed as the RED baseline before any source change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — nonce-rebuild the Sortable-mutated section container in onSlotSortEnd</name>
  <behavior>
    - After a cross-section drag: source container has 0 duplicate `.slot-item` for the moved id; exactly 1 exists in the target section.
    - Same-section reorder still lands the item at the dropped position with no duplicate.
    - No library swap, no change to the reactive move logic or the `evt.oldDraggableIndex`/`evt.newDraggableIndex` reads.
  </behavior>
  <read_first>
    - src/views/ServiceEditorView.vue:1864-1998 (`sectionSortables` map, `onSlotSortEnd` at 1881, the ref-callback Sortable teardown/recreate at ~1977-1998) and 786-871 (section container markup).
    - src/components/slides/SlideGrid.vue:262 (`:key="gridRenderNonce"`), 1065 (`const gridRenderNonce = ref(0)`), 1141 (`gridRenderNonce.value += 1`) — the in-repo nonce precedent to mirror.
    - 51-RESEARCH.md § "R110 fix pattern" option 2 (nonce-keyed container re-render).
  </read_first>
  <files>src/views/ServiceEditorView.vue</files>
  <action>
    Apply the nonce-keyed container re-render (51-RESEARCH.md fix option 2 — proven in this repo at SlideGrid.vue, lower risk than manual DOM-revert index math, satisfies the CONTEXT "fix at source, no refresh" decision). Add a component-local `const slotRenderNonce = ref(0)`. On the ref-bearing section-list container `<div>` at ServiceEditorView.vue:837 (the element carrying `:data-testid="section-list-${group.key}"`), add a `:key` that combines the section key with the nonce (stable per section, changes only on bump) so a bump forces Vue to discard and rebuild that container from reactive state, reclaiming any node SortableJS orphaned. At the END of `onSlotSortEnd`, immediately after the reactive reassignment `localService.value.slots = reindexSlots(flattenBySection(grouped))` (~line 1921), increment `slotRenderNonce.value` by 1.

    Do NOT touch the reactive move logic, the `bucketForKey`/`groupBySection`/`flattenBySection` calls, the `evt.*DraggableIndex` reads, or the per-section Sortable architecture — this adds ONLY the container rebuild. Confirm the existing ref-callback teardown/recreate at ~1977-1998 re-binds the section Sortable onto the rebuilt container `<div>` (it fires with null on unmount -> `destroy()`+`delete`, then with the fresh el -> `Sortable.create`), so no manual Sortable re-registration is needed. Keep the v1.4 architecture and stable `slot.id` row keys exactly as-is.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts && npm run type-check</automated>
    The Task 1 RED test now PASSES (GREEN); all other tests in the file still pass; `vue-tsc --build` is clean.
  </verify>
  <done>The cross-section drag repro passes: no duplicate `.slot-item` in the source list, exactly one in the target section, with no page refresh. Same-section reorder tests still pass. `npm run type-check` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SortableJS DOM ↔ Vue reactive ownership | Cross-container drags let SortableJS mutate DOM Vue believes it owns; the desync is client-only and never crosses into persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01-01 | Tampering | `onSlotSortEnd` reactive move | low | accept | Reactive move logic is unchanged; the fix only forces a re-render from already-correct state. RED DOM test gates correctness. |
| T-51-01-SC | Tampering | npm installs | low | accept | No package installs this phase — all work uses existing modules (`sortablejs`, in-repo nonce pattern). No registry verification required. |
</threat_model>

<verification>
- `npx vitest run src/views/__tests__/ServiceEditorView.test.ts` — full file green (RED repro now passing).
- `npm run type-check` — clean (`vue-tsc --build`, per CLAUDE.md; do NOT substitute `-p tsconfig.app.json`).
- SlideGrid.vue is confirmed OUT OF SCOPE for an R110-class fix: 51-RESEARCH.md verified it creates a SINGLE Sortable instance with no `group` option, so no cross-container move and no DOM-ownership phantom exist there. No change required; it is the source of the nonce technique, not a victim.
</verification>

<success_criteria>
- A cross-section drag in the live service plan places exactly one item in the target section with no phantom "No Section" duplicate, proven by automated DOM node-count assertions and holding without a refresh (R110, ROADMAP success criteria 1 & 4 — live-plan half).
- The RED repro was committed before the fix; the fix is the minimal nonce-rebuild, keeping the v1.4 per-section architecture intact.
</success_criteria>

<output>
Create `.planning/phases/51-service-order-editing-reliability/51-01-SUMMARY.md` when done.
</output>
