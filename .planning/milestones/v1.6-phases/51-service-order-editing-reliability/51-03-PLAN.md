---
phase: 51-service-order-editing-reliability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/services.ts
  - src/stores/__tests__/services.test.ts
autonomous: true
requirements: [R111]
must_haves:
  truths:
    - "Moving a sectioned item back to 'No Section' via the dropdown and saving completes with no Firestore 'Unsupported field value: undefined' error — without a page refresh."
    - "Every live-plan write path (autosave onSave, direct drag write, song assign/clear) is undefined-safe via the single updateService funnel — not just the dropdown path."
    - "The draft-lock (assertWritable) and share-refresh (maybeRefreshShareLink) behavior in updateService are unchanged; null/''/0/false and FieldValue sentinels (serverTimestamp) survive stripping."
  artifacts:
    - "src/stores/__tests__/services.test.ts — a test asserting updateService hands updateDoc a payload with no raw undefined (the moved slot's section key is absent)."
    - "src/stores/services.ts — updateService funnels the payload through stripUndefined before updateDoc."
  key_links:
    - "updateService applies stripUndefined(data) INSIDE the write body, AFTER assertWritable and BEFORE adding the serverTimestamp() sentinel; omission of the section key reads back as 'No Section'."
---

<objective>
Fix R111: moving an item back to "No Section" via the section dropdown throws a save error. Repro-test-first — commit a FAILING test proving the live-plan save funnel forwards raw `undefined` to Firestore, then apply `stripUndefined` in the one funnel that covers every write path.

Purpose: Confirmed root cause (51-RESEARCH.md): `onSectionChange` sets `slot.section = undefined` (an explicit own key), which rides through `reindexSlots`/`onSave` into the payload; `services.ts::updateService` (line 299) spreads `...data` into `updateDoc` with no strip, so raw `undefined` reaches Firestore -> "Unsupported field value: undefined". The template editor is immune only because it already strips at save (line 442). Fixing the store funnel makes all live-plan writes safe at once.
Output: One RED store test + one funnel fix; the "No Section" save succeeds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-service-order-editing-reliability/51-RESEARCH.md
@.planning/phases/51-service-order-editing-reliability/51-CONTEXT.md
@CLAUDE.md
@src/stores/services.ts
@src/stores/__tests__/services.test.ts
@src/utils/stripUndefined.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — assert updateService forwards no raw undefined to updateDoc</name>
  <read_first>
    - src/stores/services.ts:291-310 (`assertWritable` + `updateService` funnel; the `{ ...data, updatedAt: serverTimestamp() }` write at 302-305).
    - src/stores/__tests__/services.test.ts (the existing `firebase/firestore` mock; how it captures `updateDoc` call args — reuse the same spy pattern).
    - src/utils/stripUndefined.ts (semantics: drops `undefined` at any depth, preserves null/''/0/false and FieldValue sentinels).
    - 51-RESEARCH.md § "R111 RED test".
  </read_first>
  <files>src/stores/__tests__/services.test.ts</files>
  <action>
    Add a test to the `services` store suite that reproduces R111 at the store funnel. Arrange a draft service the store can write (so `assertWritable` passes), then call `updateService(id, payload)` where `payload.slots` contains a slot object carrying an explicit `section: undefined` own property (mirroring what `onSectionChange('')` produces for a "No Section" move). Assert against the captured `updateDoc` mock call: the payload written for that slot has NO `section` key (equivalently, a deep scan of the written payload finds NO value strictly equal to `undefined`). This is RED against current code — `updateService` spreads `...data` unmodified, so the recorded `updateDoc` args still contain `section: undefined`.

    Do NOT rely on the mock throwing; assert on the recorded argument shape (the real Firestore throws on `undefined`, but the RED signal here is the presence of the `undefined` key in the captured payload). Keep other tests untouched. Per CONTEXT locked decision "Write a failing reproduction test FIRST".
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts</automated>
    Run the scoped command; the NEW test must FAIL (RED) — the captured `updateDoc` payload still carries `section: undefined`. Every pre-existing test in the file must still pass.
  </verify>
  <done>The new store test exists and FAILS for the documented reason (raw `undefined` reaches the `updateDoc` payload). No other test in the file regresses. Committed as the RED baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — apply stripUndefined in the updateService funnel</name>
  <behavior>
    - updateService(id, { slots: [ ...slot with section: undefined... ] }) writes a payload whose affected slot has NO section key.
    - assertWritable still runs first and still throws ServiceLockedError for a locked (planned, non-export) write.
    - serverTimestamp(), null values, empty strings, and 0/false all survive; maybeRefreshShareLink still runs.
  </behavior>
  <read_first>
    - src/stores/services.ts:1-32 (imports — no `stripUndefined` import yet), 291-310 (`updateService`).
    - src/utils/stripUndefined.ts (its docstring: add FieldValue sentinels AFTER stripping the plain payload).
    - 51-RESEARCH.md § "R111 one-line funnel fix" and § "Common Pitfalls / Pitfall 2 & Pitfall 5".
  </read_first>
  <files>src/stores/services.ts</files>
  <action>
    Add `import { stripUndefined } from '@/utils/stripUndefined'` to the imports at the top of services.ts (it is not yet imported). In `updateService` (line 299), change the `updateDoc` payload from `{ ...data, updatedAt: serverTimestamp() }` to spread `...stripUndefined(data)` first and then add `updatedAt: serverTimestamp()` AFTER the stripped spread (the helper's docstring mandates adding FieldValue sentinels after stripping; `serverTimestamp()` is a sentinel, not a plain value). Leave `assertWritable(id, data)` at line 301 exactly where it is — it must run against the ORIGINAL `data` before stripping, so the lock contract is unchanged. Leave the `maybeRefreshShareLink(id, data as Partial<Service>)` call at line 309 unchanged.

    Do NOT use `deleteField()` — it cannot target an element inside an array (Pitfall 5); the whole `slots` array is replaced on each write, so omitting the key is correct and reads back as "No Section". This one change covers autosave `onSave`, the direct drag-reorder write, and song assign/clear — all funnel through `updateService` (Pitfall 2). The role-override paths use `updateDoc` directly and do NOT flow through `updateService`, so they are unaffected.
  </action>
  <verify>
    <automated>npx vitest run src/stores/__tests__/services.test.ts && npm run type-check</automated>
    The Task 1 RED test now PASSES (GREEN); the draft-lock and share-refresh tests still pass; `vue-tsc --build` is clean.
  </verify>
  <done>Moving an item to "No Section" saves with no raw `undefined` in the payload; assertWritable and maybeRefreshShareLink behavior is preserved; `serverTimestamp()`/null/''/0/false survive. `npm run type-check` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (Pinia services store) → Firestore `updateDoc` | The slots array payload crosses into persisted service documents; the write contract rejects `undefined`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-03-01 | Elevation of Privilege | `updateService` draft-lock | medium | mitigate | `assertWritable(id, data)` runs on the ORIGINAL data BEFORE `stripUndefined`; the strip is inside the write body only. A retained test asserts a locked (planned, non-export) write still throws ServiceLockedError. |
| T-51-03-02 | Tampering | Firestore write contract (data integrity) | medium | mitigate | `stripUndefined` recurses arrays + nested objects and preserves null/FieldValue, so no legitimate field (sermonPassage:null, serverTimestamp) is lost while raw `undefined` is dropped. |
| T-51-03-SC | Tampering | npm installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `npx vitest run src/stores/__tests__/services.test.ts` — full file green (RED repro now passing, draft-lock tests intact).
- `npm run type-check` — clean.
- Manual confirmation deferred to /gsd-verify-work: in the running app, move an item to "No Section" via the dropdown in a live plan and confirm autosave completes with no error toast and no refresh.
</verification>

<success_criteria>
- Moving a sectioned item back to "No Section" via the dropdown saves successfully with no error, without a page refresh (R111, ROADMAP success criteria 2 & 4).
- The fix lives in the single write funnel so every live-plan write path is undefined-safe; the draft-lock and Phase 41 share-refresh are preserved.
- The RED repro was committed before the fix.
</success_criteria>

<output>
Create `.planning/phases/51-service-order-editing-reliability/51-03-SUMMARY.md` when done.
</output>
