---
phase: 55-preview-export-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ServiceEditorView.vue
  - src/views/__tests__/ServiceEditorView.test.ts
autonomous: true
requirements: [R125]
must_haves:
  truths:
    - "While a Planning Center export is running, the Confirm Export button shows a visible spinner (animate-spin ring) so the user can see it is working"
    - "The spinner is absent when no export is running"
    - "The Confirm Export button stays disabled while exporting (existing guard preserved), preventing double-invocation"
  artifacts:
    - "src/views/ServiceEditorView.vue — an animate-spin glyph with data-testid=\"export-spinner\" rendered inside the Confirm Export button when isExporting is true"
    - "src/views/__tests__/ServiceEditorView.test.ts — a test asserting the export-spinner renders when isExporting and the Confirm button is disabled"
  key_links:
    - "Reuses the EXISTING isExporting ref (:1667, set :3269, cleared in finally :3617) — no new flag is introduced (a second flag risks the two disagreeing)"
    - "Spinner sits on the Confirm Export button (:495-500), the long PC round-trip in onConfirmExport — NOT the fast dialog-open which already shows 'Loading options...'"
---

<objective>
R125: Add a visible in-progress spinner to the Planning Center export so the user can see it is working. Reuse the app's existing `animate-spin` ring glyph and the already-wired `isExporting` reactive flag; add no new flag or guard.

Purpose: The owner asked for "a spinner to the services planning center export so users can see it's doing something." The export flow is near-scaffolded — flag, `:disabled` guards, store-status re-check, and an "Exporting..." text label already exist; only the visual spinner glyph is missing.
Output: One spinner span (with a test id) inside the Confirm Export button, plus a component test asserting presence-when-exporting / disabled-button. `npm run type-check` clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-preview-export-polish/55-CONTEXT.md
@.planning/phases/55-preview-export-polish/55-RESEARCH.md
@.planning/phases/55-preview-export-polish/55-VALIDATION.md

# The export dialog + flag (this plan's surface)
@src/views/ServiceEditorView.vue
@src/views/__tests__/ServiceEditorView.test.ts
# The established spinner affordance to reuse (prior art — read, do not modify)
@src/components/VolunteerCsvImportModal.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the export-spinner test (RED first)</name>
  <files>src/views/__tests__/ServiceEditorView.test.ts</files>
  <read_first>
    - src/views/__tests__/ServiceEditorView.test.ts (existing mount/export patterns — reuse them; there is currently NO isExporting/spinner test)
    - 55-RESEARCH.md § "R125 — export spinner seam" (isExporting at :1667, Confirm button :495-500, disabled guard :498)
    - 55-VALIDATION.md § Wave 0 Requirements (the export-spinner test row)
  </read_first>
  <action>
    Add one component test to ServiceEditorView.test.ts, reusing the file's existing mounting/setup pattern for the export dialog. Drive the component into the exporting state (open the export dialog so the Confirm button renders, then set the existing `isExporting` reactive flag true — via the mounted instance / the file's established helper for reaching export state) and assert that an element matching `[data-testid="export-spinner"]` IS present and that the Confirm Export button is `disabled`. Add the complementary case: when `isExporting` is false, `[data-testid="export-spinner"]` is absent. Land this test FIRST so it is RED (the spinner glyph does not exist yet). Per R125.
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>The new test exists and FAILS on the missing `[data-testid="export-spinner"]` element (expected RED); the not-exporting/absent case passes. No other ServiceEditorView tests regress.</done>
</task>

<task type="auto">
  <name>Task 2: Render the animate-spin glyph inside the Confirm Export button (GREEN)</name>
  <files>src/views/ServiceEditorView.vue</files>
  <read_first>
    - src/views/ServiceEditorView.vue (Confirm Export button :495-500; existing isExporting-driven label at :500)
    - src/components/VolunteerCsvImportModal.vue (:99 the established `animate-spin` ring markup to mirror)
    - 55-RESEARCH.md § "R125 — export spinner seam" (the exact-change guidance)
  </read_first>
  <action>
    Inside the Confirm Export button (:495-500), render an inline spinner span, shown only when `isExporting`, immediately before the existing `{{ isExporting ? 'Exporting...' : ... }}` label. Mirror the app's established affordance from VolunteerCsvImportModal.vue:99 — a `rounded-full animate-spin` ring — sized down for an inline button glyph (h-4 w-4, border-2, a light border with a transparent top edge so it reads on the indigo button). Give the span `data-testid="export-spinner"` and `aria-hidden="true"`. Add `inline-flex items-center justify-center gap-2` to the button's class list so the glyph sits beside the label.

    Reuse the EXISTING `isExporting` ref — do NOT add a new flag or guard. Leave the existing `:disabled="isExporting || !exportSelectedServiceTypeId"` guard (:498) and the store-status re-check in onConfirmExport untouched. Spinner goes on the Confirm Export button only, not the fast dialog-open trigger (which already shows "Loading options...").
  </action>
  <verify>
    <automated>npx vitest run src/views/__tests__/ServiceEditorView.test.ts</automated>
  </verify>
  <done>The Task 1 test now PASSES: `[data-testid="export-spinner"]` renders while isExporting is true, is absent when false, and the Confirm button is disabled during export. `npm run type-check` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → Planning Center export (async API round-trip) | The Confirm Export button triggers a long-running PC API export; the concern is double-invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-02 | Tampering (duplicate export / double-fire) | ServiceEditorView.vue Confirm Export button | low | accept | The existing `:disabled="isExporting || !exportSelectedServiceTypeId"` guard plus the store-status re-check in onConfirmExport already prevent a second export; this plan is presentation-only (adds a glyph) and preserves both guards — no new guard needed and none removed. |
</threat_model>

<verification>
Per-task quick command above. Wave/phase gate:
- `npm run type-check` (vue-tsc --build) clean.
- `npx vitest run` (bare) green except the known 2-file baseline.

Manual (deferred to `.planning/PENDING-VERIFICATION.md` § Phase 55 under the v1.6 autonomy grant — owner away): trigger a real Planning Center export and confirm the Confirm Export button shows a spinner and is disabled until the export completes or fails.
</verification>

<success_criteria>
The Planning Center Confirm Export button shows a visible animate-spin glyph while exporting and none otherwise; the button stays disabled during export; the existing isExporting flag/guards are reused unchanged; type-check clean; app suite at the 2-file baseline.
</success_criteria>

<output>
Create `.planning/phases/55-preview-export-polish/55-02-SUMMARY.md` when done.
</output>
