---
phase: 55-preview-export-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/slides/slideDisplay.ts
  - src/components/PresentationViewer.vue
  - src/components/slides/__tests__/slideDisplay.test.ts
  - src/components/__tests__/PresentationViewer.test.ts
autonomous: true
requirements: [R124]
must_haves:
  truths:
    - "A scripture slide in the slideshow preview (PresentationViewer) shows only its own text — no auto-appended (ESV)/(NLT) version suffix"
    - "The grid card body and edit-drawer preview (both fed by slideBodyText) likewise show scripture text with no auto-appended version suffix"
    - "The version can still be added manually by typing it into the slide's own editable text — nothing blocks that (no code enforces its absence in stored text)"
    - "The provenance machinery is untouched: scriptureAttribution + resolveTranslationSource still exist and still return the version suffix; scripture.test.ts stays green (render-only proof, R092 preserved)"
  artifacts:
    - "src/components/slides/slideDisplay.ts — suffix expression removed from slideBodyText, dead scriptureAttribution/resolveTranslationSource imports removed"
    - "src/components/PresentationViewer.vue — scriptureAttributionSuffix function deleted, both template call expressions dropped, dead import removed"
  key_links:
    - "slideBodyText is consumed by SlideCard.vue and EditSlideDrawer.vue preview surfaces — removing the suffix there propagates to both, no separate edit needed"
    - "src/utils/scripture.ts (scriptureAttribution, resolveTranslationSource, translationSource) is NOT modified → scripture.test.ts:523-534 must stay green"
---

<objective>
R124: Stop the slideshow preview from auto-appending the Bible version (ESV/NLT) to scripture slides. Remove the automatic render-time suffix at the TWO confirmed render sites, leaving the underlying provenance data (per-slide `translationSource`) stamped and the `scriptureAttribution`/`resolveTranslationSource` helpers intact for manual/future use.

Purpose: The owner wants clean scripture slides when presenting — "don't auto add it" — while keeping the ability to type the version into a slide manually and keeping R092 capture-once immutability data.
Output: Two render sites edited, their now-dead imports removed, and every existing suffix-asserting test re-pointed to assert ABSENCE. `npm run type-check` (vue-tsc --build) stays clean; `scripture.test.ts` stays green (render-only proof).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-preview-export-polish/55-CONTEXT.md
@.planning/phases/55-preview-export-polish/55-RESEARCH.md
@.planning/phases/55-preview-export-polish/55-VALIDATION.md

# The two render sites and their tests (the whole surface of this plan)
@src/components/slides/slideDisplay.ts
@src/components/PresentationViewer.vue
@src/components/slides/__tests__/slideDisplay.test.ts
@src/components/__tests__/PresentationViewer.test.ts
# Proof the change is render-only — must stay green, DO NOT modify
@src/utils/scripture.ts
@src/utils/__tests__/scripture.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point every suffix-asserting test to assert ABSENCE (RED first)</name>
  <files>src/components/slides/__tests__/slideDisplay.test.ts, src/components/__tests__/PresentationViewer.test.ts</files>
  <read_first>
    - src/components/slides/__tests__/slideDisplay.test.ts (the suffix group :358-432)
    - src/components/__tests__/PresentationViewer.test.ts (:653-655, :721-722, the describe('scripture attribution suffix …') block :738-809, :831, :855, :1865)
    - 55-RESEARCH.md § Validation Architecture (the enumerated re-point list) and 55-VALIDATION.md § "R124 — existing suffix tests to re-point"
  </read_first>
  <action>
    Flip every assertion that currently expects the auto-appended version suffix so it expects the no-suffix text instead — this task lands FIRST so the tests go RED against the still-present suffix, and Task 2 turns them GREEN. Per R124.

    In slideDisplay.test.ts, edit the slideBodyText suffix group: change the combined reference+text expectation at :358-366 from the "...shepherd" + version-suffix form to the plain "Psalms 23:1-6\nThe LORD is my shepherd" form; drop the trailing version suffix from the NLT case (:369-378), the congregational-section case (:395-405), the later-section case (:409-419), and the reference-state case (:424-432). Leave the reference-only-no-text case (:381-389) exactly as-is — it already has no suffix and is a regression anchor. Add explicit `not.toContain('(ESV)')` and `not.toContain('(NLT)')` guards to at least the section and reference-state cases to lock the new behavior.

    In PresentationViewer.test.ts, drop the version suffix from the expectations at :653-655 and :721-722; re-point the whole `describe('scripture attribution suffix (45-04, R091/R092)')` block (:738-809, assertions around :740/:751/:761/:771/:782/:802) so each asserts that NEITHER version string appears (extend the existing single-version `not.toContain` cases to assert both `(ESV)` and `(NLT)` are absent); and drop the suffix from :831, :855, and :1865. Do NOT touch scripture.test.ts — the helper is preserved and must keep returning the suffix.
  </action>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/slideDisplay.test.ts src/components/__tests__/PresentationViewer.test.ts</automated>
  </verify>
  <done>The two test files assert ABSENCE of the version suffix (with not.toContain guards). Running them now FAILS on the not-yet-removed suffix (expected RED); the reference-only anchor and all non-suffix assertions still pass. scripture.test.ts is unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the auto-append at both render sites + delete dead imports (GREEN)</name>
  <files>src/components/slides/slideDisplay.ts, src/components/PresentationViewer.vue</files>
  <read_first>
    - src/components/slides/slideDisplay.ts (imports :14-19; slideBodyText scripture branch :216-219)
    - src/components/PresentationViewer.vue (template call sites :212-213 and :221-222; import :401; scriptureAttributionSuffix definition :707-709)
    - 55-RESEARCH.md § "R124 — the two render sites (definitive, with file:line)"
  </read_first>
  <action>
    Site 1 — src/components/slides/slideDisplay.ts, slideBodyText() scripture branch (~:216-219): delete the `suffix` local (the ` ${scriptureAttribution(resolveTranslationSource(slide))}` expression) and its two usages, so the branch returns `${slide.reference}\n${slide.text}` when `showReference` is true and `slide.text` otherwise. Then remove the now-unused named imports `scriptureAttribution` and `resolveTranslationSource` from the `@/utils/scripture` import (:14-19), KEEPING `formatScriptureReference` and `scriptureRefFromSlot`. Do not change the `!slide.text` early return.

    Site 2 — src/components/PresentationViewer.vue: delete the `scriptureAttributionSuffix` function (:707-709); in the template, drop the trailing `{{ scriptureAttributionSuffix(...) }}` interpolation from the congregational-section paragraph (:212-213) and from the normal-mode passage paragraph (:221-222) so each paragraph renders only the section/slide text; and remove the now-dead import `import { scriptureAttribution, resolveTranslationSource } from '@/utils/scripture'` (:401).

    Do NOT modify src/utils/scripture.ts — the helpers and the `translationSource` field stay (R124 is render-only; provenance and R092 immutability preserved). Manual addition remains possible because the slide text stays user-editable; add no code for it.
  </action>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/slideDisplay.test.ts src/components/__tests__/PresentationViewer.test.ts src/utils/__tests__/scripture.test.ts</automated>
  </verify>
  <done>The suffix group tests from Task 1 now PASS (suffix gone at both sites); scripture.test.ts:523-534 still passes (helpers unchanged, still return the version suffix). No dead imports remain. `npm run type-check` reports no TS6133 "declared but never read" for scriptureAttribution/resolveTranslationSource in either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed slide text → slide render | A user can now type the version (e.g. the letters ESV) into a slide's own editable text; that text is rendered in preview/presenter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Tampering (XSS) | PresentationViewer.vue / slideDisplay.ts scripture text render | low | accept | Slide text is rendered via Vue `{{ }}` mustache interpolation (auto-escaped), never `v-html` — the existing PresentationViewer test at :802 proves no markup executes. R124 removes render output, it adds no new sink. No new control needed. |
</threat_model>

<verification>
Per-task quick commands above. Wave/phase gate:
- `npm run type-check` (vue-tsc --build) clean — the whole point of removing the dead imports in the same edit.
- `npx vitest run` (bare) green except the known 2-file baseline (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`).

Manual (deferred to `.planning/PENDING-VERIFICATION.md` § Phase 55 under the v1.6 autonomy grant — owner away): present a service with a scripture slide and confirm no auto version suffix appears when presenting; then type "(ESV)" into the slide text and confirm it shows.
</verification>

<success_criteria>
The slideshow preview, grid card, and edit-drawer preview show scripture text with no auto-appended (ESV)/(NLT) suffix; manual addition via the slide's editable text still works; scriptureAttribution/resolveTranslationSource/translationSource are unchanged and scripture.test.ts stays green; type-check clean; app suite at the 2-file baseline.
</success_criteria>

<output>
Create `.planning/phases/55-preview-export-polish/55-01-SUMMARY.md` when done.
</output>
