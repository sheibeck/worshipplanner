---
phase: 55-preview-export-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/config/slideFonts.ts
  - src/utils/slideTypography.ts
  - src/config/__tests__/slideFonts.test.ts
  - src/utils/__tests__/slideTypography.test.ts
autonomous: false
requirements: [R126]
user_setup: []
must_haves:
  truths:
    - "Roboto is available as a curated, self-hosted slide font in the typography picker (a sixth SLIDE_FONTS family)"
    - "Inter (and the other four families) remain available and unchanged; Inter stays first and stays DEFAULT_FAMILY"
    - "Roboto CSS loads on demand through the data-driven loader (no runtime Google Fonts API — self-hosted @fontsource woff2 only)"
    - "@fontsource/roboto is installed, pinned ^5.3.0, license OFL-1.1 — consistent with the existing five @fontsource deps"
  artifacts:
    - "package.json / package-lock.json — @fontsource/roboto@^5.3.0 dependency"
    - "src/config/slideFonts.ts — a Roboto SLIDE_FONTS entry with weights [300,400,500,600,700], license OFL-1.1"
    - "src/utils/slideTypography.ts — a FONT_CSS_LOADERS Roboto line using the static @fontsource/roboto/${weight}.css prefix"
  key_links:
    - "SLIDE_FONT_FAMILY_NAMES is derived from Object.keys(SLIDE_FONTS) → the Settings <select> picks up Roboto automatically; no picker edit needed"
    - "snapWeight/cssVarsFor/waitForSlideFont/loadFontCss are all data-driven off the registry — one registry entry + one loader line is the whole wiring"
    - "The install (Wave-0) MUST precede the slideTypography loader test resolving and the build"
---

<objective>
R126: Add Roboto to the curated, self-hosted slide-font set alongside the five families shipped in v1.5 Phase 46 (Inter, Open Sans, Poppins, Lora, Source Serif 4). Install `@fontsource/roboto@^5.3.0` (self-hosted woff2 — never the runtime Google Fonts API), add one SLIDE_FONTS registry entry and one loader-map line, and update the registry tests from five families to six.

Purpose: The owner asked to "add the Inter and Roboto fonts" — Inter already ships; this phase adds Roboto. The font registry is data-driven, so the entire feature is one dependency + one registry entry + one loader line + the tests those change.
Output: `@fontsource/roboto` pinned in package.json, a Roboto registry entry + loader line, six-family tests. `npm run type-check` clean; the package legitimacy SUS/too-new flag dispositioned as the documented fontsource-lockstep false positive (Phase 46 precedent).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-preview-export-polish/55-CONTEXT.md
@.planning/phases/55-preview-export-polish/55-RESEARCH.md
@.planning/phases/55-preview-export-polish/55-VALIDATION.md

# The registry + loader + their tests (this plan's surface)
@src/config/slideFonts.ts
@src/utils/slideTypography.ts
@src/config/__tests__/slideFonts.test.ts
@src/utils/__tests__/slideTypography.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — @fontsource/roboto (defer under v1.6 grant)</name>
  <action>
    Record the package legitimacy disposition for `@fontsource/roboto@^5.3.0` before install. Do NOT self-approve as passed. Under the v1.6 STANDING AUTONOMY GRANT (owner away — "do not block on human-verify checkpoints; defer and keep going") the executor does not wait: write a DEFERRED sign-off entry to `.planning/PENDING-VERIFICATION.md` § Phase 55 noting the SUS/too-new verdict is the documented fontsource-lockstep structural false positive (Phase 46 precedent) with direct in-tarball LICENSE + weight-file + null-postinstall verification already performed by research, then proceed to Task 2.
  </action>
  <what-built>
    Package legitimacy verification for `@fontsource/roboto@^5.3.0` before it is installed (R126). Research (55-RESEARCH.md § Package Legitimacy Audit) ran the Phase 46 protocol and verified directly this session: OFL-1.1 license (verbatim SIL Open Font License 1.1 string in the in-tarball LICENSE; `npm view` reports OFL-1.1 for 5.3.0 — early 5.x reported Apache-2.0, relicensed at 5.2.0), full 100–900 static weight ramp incl. 600 in the tarball, `postinstall` null, ~1.26M weekly downloads, canonical repo github.com/fontsource/font-files. The legitimacy scanner's `SUS`/`too-new` verdict is the identical structural false positive documented for Phase 46's five fontsource packages — the entire @fontsource catalog re-publishes in lockstep on every upstream Google Fonts refresh, so publishedAt always looks new.
  </what-built>
  <how-to-verify>
    Optional owner spot-check: `npm view @fontsource/roboto version` → 5.3.0; `npm view @fontsource/roboto license` → OFL-1.1; confirm github.com/fontsource/font-files is the source repo. This mirrors exactly how Phase 46 dispositioned its five fontsource packages.
  </how-to-verify>
  <resume-signal>
    Under the v1.6 grant the executor records the DEFERRED entry (NOT self-approved as passed) and proceeds to Task 2 — the Phase 46 precedent applied faithfully. Type "approved" to instead sign it off now.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wave-0 install + Roboto registry entry + loader line</name>
  <files>package.json, package-lock.json, src/config/slideFonts.ts, src/utils/slideTypography.ts</files>
  <read_first>
    - src/config/slideFonts.ts (the SLIDE_FONTS record :65-107; header comment on the weight-ramp + legitimacy convention :1-40)
    - src/utils/slideTypography.ts (FONT_CSS_LOADERS :155-161 and the static-prefix warning :148-154)
    - 55-RESEARCH.md § "R126 — Roboto registry + loader wiring"
  </read_first>
  <action>
    Wave-0 step FIRST — run `npm install @fontsource/roboto@^5.3.0` (a reversible build-dependency add; NOT a deploy, NOT an .env.local change). This pins ^5.3.0, consistent with the existing five `@fontsource/*@^5.3.0` deps, and lands on OFL-1.1.

    Then add a `Roboto` entry to the `SLIDE_FONTS` record in src/config/slideFonts.ts: family `Roboto`, package `@fontsource/roboto`, category `sans`, weights `[300, 400, 500, 600, 700]` (Roboto ships the full 100–900 ramp incl. 600 — no omissions, unlike Lora), license `OFL-1.1`, licenseUrl `https://fontsource.org/fonts/roboto/license`. Keep `Inter` FIRST (the "lists Inter first" test and DEFAULT_FAMILY depend on it) — place Roboto after Inter or at the end. Mirror the header comment's legitimacy note: record that the SUS/too-new flag is the documented fontsource-lockstep false positive, per Phase 46.

    Then add one line to `FONT_CSS_LOADERS` in src/utils/slideTypography.ts: a `Roboto` key whose loader calls `import` with the STATIC `@fontsource/roboto/${weight}.css` prefix (mirror the existing five lines exactly). Do NOT template the package name dynamically — the static prefix is required for Vite import-analysis to discover the per-weight chunks.

    No other edits: SLIDE_FONT_FAMILY_NAMES, snapWeight, cssVarsFor, waitForSlideFont, loadFontCss are all derived from the registry and pick up Roboto automatically; Inter stays DEFAULT_FAMILY.
  </action>
  <verify>
    <automated>npx vitest run src/config/__tests__/slideFonts.test.ts src/utils/__tests__/slideTypography.test.ts</automated>
  </verify>
  <done>@fontsource/roboto@^5.3.0 is in package.json; SLIDE_FONTS has a Roboto entry ([300,400,500,600,700], OFL-1.1) with Inter still first; FONT_CSS_LOADERS has a Roboto static-prefix line. (The slideFonts "exactly five" test still fails here — it is fixed in Task 3.)</done>
</task>

<task type="auto">
  <name>Task 3: Update the registry + loader tests to six families</name>
  <files>src/config/__tests__/slideFonts.test.ts, src/utils/__tests__/slideTypography.test.ts</files>
  <read_first>
    - src/config/__tests__/slideFonts.test.ts (the "exactly five expected family keys" test :5-10; the license loop :17-23 and weight-ramp loops auto-cover the new entry)
    - src/utils/__tests__/slideTypography.test.ts (no existing Roboto/loader coverage — add one)
    - 55-VALIDATION.md § Wave 0 Requirements (the slideFonts + slideTypography test rows)
  </read_first>
  <action>
    In slideFonts.test.ts, update the "has exactly the five expected family keys" test (:5-10): change the expected key set to the six keys including `'Roboto'` and change `toHaveLength(5)` to `toHaveLength(6)`. The existing per-family license loop (:17-23), the locked-ramp bounds loop (:34-41), and the package/category loop (:47-53) are generic and will auto-cover Roboto — leave them. Optionally add a focused assertion that `SLIDE_FONTS['Roboto']` has weights `[300,400,500,600,700]` and license `OFL-1.1`.

    In slideTypography.test.ts, add a test proving Roboto is wired into the loader map. Assert that `loadFontCss('Roboto', 400)` returns a resolving Promise (does not reject) — i.e. Roboto is a known loader key, distinct from an unknown family which no-ops. (FONT_CSS_LOADERS is currently module-private; if a direct membership assertion reads cleaner, export it for the test or spy on the import — executor's discretion, but keep the static-prefix loader form intact.)
  </action>
  <verify>
    <automated>npx vitest run src/config/__tests__/slideFonts.test.ts src/utils/__tests__/slideTypography.test.ts</automated>
  </verify>
  <done>slideFonts.test.ts asserts six families incl. Roboto and passes; the generic license/weight loops pass for Roboto; slideTypography.test.ts's new Roboto loader assertion passes. `npm run type-check` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build (dependency install) | A new third-party package is added to the build |
| stored font-family setting → document.fonts.load() | A family name from settings drives on-demand font-CSS loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-SC | Tampering (supply chain) | `npm install @fontsource/roboto` | high | mitigate | Package legitimacy checkpoint (Task 1) + direct in-tarball LICENSE / weight-file / null-postinstall verification performed in research; SUS/too-new dispositioned as the documented fontsource-lockstep false positive (Phase 46 precedent); pinned ^5.3.0 matching the existing five deps. Deferred sign-off recorded (not self-approved) per the v1.6 grant. |
| T-55-03 | Tampering | slideTypography `cssVarsFor` → `document.fonts.load()` | low | accept | Roboto is a curated registry KEY, not free-text input; `cssVarsFor` already falls back to Inter for any family not in `SLIDE_FONTS` (T-46-03) — no new input path is opened. |
</threat_model>

<verification>
Per-task quick commands above. Wave/phase gate:
- `npm run type-check` (vue-tsc --build) clean.
- `npx vitest run` (bare) green except the known 2-file baseline (`src/storage.rules.test.ts`, `src/views/__tests__/RosterView.test.ts`).
- Package legitimacy DEFERRED entry written to `.planning/PENDING-VERIFICATION.md` § Phase 55 (structural false positive, direct verification done).

Manual (deferred to PENDING-VERIFICATION § Phase 55 under the v1.6 grant — owner away): in Slide Typography settings, pick Roboto; confirm slides render in Roboto and Inter is still available.
</verification>

<success_criteria>
Roboto is a sixth curated, self-hosted SLIDE_FONTS family with weights [300,400,500,600,700] and OFL-1.1, wired into the loader with the static @fontsource/roboto/${weight}.css prefix; Inter and the other four remain unchanged and Inter stays first/default; @fontsource/roboto@^5.3.0 is installed; the registry tests assert six families; type-check clean; app suite at the 2-file baseline; legitimacy deferral recorded.
</success_criteria>

<output>
Create `.planning/phases/55-preview-export-polish/55-03-SUMMARY.md` when done.
</output>
