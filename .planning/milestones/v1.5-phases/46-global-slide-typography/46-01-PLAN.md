---
phase: 46-global-slide-typography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/config/slideFonts.ts
  - src/config/__tests__/slideFonts.test.ts
autonomous: false
requirements: [R093]
user_setup: []

must_haves:
  truths:
    - "The five curated families (Inter, Open Sans, Poppins, Lora, Source Serif 4) are installed as self-hosted @fontsource woff2 packages — never the runtime Google Fonts API."
    - "Every family added ships with a license value that was read from that package's own shipped LICENSE file, not assumed by analogy to Inter (R093 success criterion 4)."
    - "The font-family names in the picker's <select> fit a native option — the longest curated name (Source Serif 4) needs no custom dropdown/overflow handling."
  artifacts:
    - "src/config/slideFonts.ts exporting SLIDE_FONTS (Record<family, { family, package, category, weights, license, licenseUrl }>) and the derived SLIDE_FONT_FAMILY_NAMES list"
    - "src/config/__tests__/slideFonts.test.ts asserting registry membership, per-family weight ramps, and OFL-1.1 license on every entry"
    - "@fontsource/inter, @fontsource/open-sans, @fontsource/poppins, @fontsource/lora, @fontsource/source-serif-4 at 5.3.0 pinned in package.json + package-lock.json"
  key_links:
    - "SLIDE_FONTS[family].weights is the single source of truth the Settings card (46-03) and the snapWeight helper (46-02) read to derive the per-family weight ramp"
    - "SLIDE_FONTS[family].package + category feed the on-demand font-CSS loader (46-02 loadFontCss) and the CSS font-family stack (46-02 cssVarsFor)"
---

<objective>
Bring the five curated fonts into the app as self-hosted @fontsource packages, gated behind a package-legitimacy checkpoint, and record each family's verified license + shipped-weight ramp in a typed registry (`src/config/slideFonts.ts`). This is the license-evidence and supply-chain foundation for R093 success criterion 4.

Purpose: Every later plan (Settings card, render-site application, presenter gate) reads `SLIDE_FONTS` for the family list, per-family weight ramp, package name, and font category. Getting the registry right — and verified against the packages' actual LICENSE/metadata files — is what makes the phase's license claim honest rather than assumed.

Output: Five pinned @fontsource dependencies + `src/config/slideFonts.ts` + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-global-slide-typography/46-CONTEXT.md
@.planning/phases/46-global-slide-typography/46-RESEARCH.md

# The CORRECTED, package-verified weight/license table this plan builds SLIDE_FONTS from
# lives in 46-RESEARCH.md § "Curated Font List (... CORRECTED from 46-UI-SPEC.md)".
# Build from the RESEARCH table, NOT the UI-SPEC table (Open Sans ships 500; Source Serif 4 ships 300 and 500).
</context>

## Artifacts this phase produces

The phase-wide inventory of every new symbol/file, recorded here in the entry plan so executors of later plans can see the whole surface at a glance:

| Artifact | Plan | Kind |
|----------|------|------|
| `@fontsource/{inter,open-sans,poppins,lora,source-serif-4}` deps (5.3.0) | 46-01 | npm packages |
| `src/config/slideFonts.ts` — `SLIDE_FONTS`, `SLIDE_FONT_FAMILY_NAMES` | 46-01 | registry |
| `OrgSettings.slideTypography: { fontFamily: string; fontWeight: number; fontScale: 'sm'\|'md'\|'lg' }` | 46-02 | type field |
| `DEFAULT_ORG_SETTINGS.slideTypography = { fontFamily: 'Inter', fontWeight: 400, fontScale: 'md' }` | 46-02 | default |
| `src/utils/slideTypography.ts` — `SCALE_MAP`, `FONT_LOAD_TIMEOUT_MS`, `cssVarsFor()`, `snapWeight()`, `waitForSlideFont()`, `loadFontCss()` | 46-02 | helpers |
| `:root { --slide-font-family; --slide-font-weight; --slide-font-scale }` in `src/assets/main.css` | 46-02 | CSS variables |
| eager `@fontsource/inter/400.css` import in `src/main.ts` | 46-02 | app-init wiring |
| "Slide Typography" card markup + state + save + live Preview in `src/views/SettingsView.vue` | 46-03 | Settings UI |
| CSS-variable wrappers on `PresentationViewer.vue`, `SlideGrid.vue`, `EditSlideDrawer.vue` preview | 46-04 | render-site application |
| presenter font-load gate (`fontReady` + `waitForSlideFont`) in `PresentationViewer.vue` | 46-04 | R094 gate |
| test files: `slideFonts.test.ts`, `slideTypography.test.ts`, added cases in `SettingsView.test.ts` + `PresentationViewer.test.ts` (+ render-site tests) | 46-01/02/03/04 | tests |

`ServicePrintLayout.vue` is explicitly NOT in this inventory and MUST NOT be modified — owner-locked exclusion (REQUIREMENTS.md Out of Scope; 46-CONTEXT.md).

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy checkpoint for the five @fontsource packages</name>
  <read_first>
    - .planning/phases/46-global-slide-typography/46-RESEARCH.md § "Package Legitimacy Audit" (all five verdicts + the "too-new / catalog-lockstep" reason)
    - .planning/STATE.md § "★★ STANDING AUTONOMY GRANT — v1.5" (human-verify checkpoints are DEFERRED, not blocking, during autonomous runs)
  </read_first>
  <what-built>
    The five packages `@fontsource/inter`, `@fontsource/open-sans`, `@fontsource/poppins`, `@fontsource/lora`, `@fontsource/source-serif-4` (all `5.3.0`, OFL-1.1) are proposed for install. `gsd-tools query package-legitimacy check` flagged all five `SUS` with reason `too-new` — a known false positive from @fontsource's catalog-wide lockstep release cadence (canonical `github.com/fontsource/font-files` repo, weekly downloads 104K–2.37M, `postinstall` is null on every one). RESEARCH already downloaded each tarball and read its `metadata.json`/`LICENSE`/`*.css` directly.
  </what-built>
  <action>
    Pause before the Task 2 `npm install`. Confirm each of the five `@fontsource/*` packages on npmjs.com (fontsource-published, `github.com/fontsource/font-files`, `5.3.0`, OFL-1.1, no install scripts). Under the STATE.md v1.5 standing autonomy grant, if running autonomously do NOT block: record this checkpoint as DEFERRED in `.planning/PENDING-VERIFICATION.md` § Phase 46 (never self-approved) and proceed to Task 2, since RESEARCH already performed direct tarball + registry verification. This is a legitimacy confirmation, not a slopsquat suspicion.
  </action>
  <how-to-verify>
    1. Open `npmjs.com/package/@fontsource/inter`, `.../open-sans`, `.../poppins`, `.../lora`, `.../source-serif-4`.
    2. Confirm each is published by the fontsource org, links to `github.com/fontsource/font-files`, shows version `5.3.0` with license `OFL-1.1`, and declares no install scripts.
    3. Confirm the `5.3.0` pin lands cleanly in `package-lock.json` after install (integrity hashes present).
  </how-to-verify>
  <acceptance_criteria>
    - Each of the five packages is confirmed on npmjs.com as fontsource-published, OFL-1.1, no postinstall script.
    - OR, under the STATE.md standing autonomy grant: the checkpoint is recorded DEFERRED in `.planning/PENDING-VERIFICATION.md` § Phase 46 (never self-approved) and execution proceeds to Task 2, because RESEARCH already performed direct tarball + registry verification.
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm the five packages, or (autonomous) record DEFERRED in PENDING-VERIFICATION.md and continue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the five packages and build the SLIDE_FONTS registry</name>
  <files>package.json, package-lock.json, src/config/slideFonts.ts, src/config/__tests__/slideFonts.test.ts</files>
  <read_first>
    - .planning/phases/46-global-slide-typography/46-RESEARCH.md § "Curated Font List (... CORRECTED from 46-UI-SPEC.md)" — the authoritative weight/license table
    - .planning/phases/46-global-slide-typography/46-RESEARCH.md § "Standard Stack" (exact package names + version 5.3.0)
    - src/types/organization.ts (for the in-repo JSDoc/registry style to mirror)
  </read_first>
  <action>
    Run `npm install @fontsource/inter@5.3.0 @fontsource/open-sans@5.3.0 @fontsource/poppins@5.3.0 @fontsource/lora@5.3.0 @fontsource/source-serif-4@5.3.0` so all five pin at `5.3.0` in package.json + package-lock.json.
    Create `src/config/slideFonts.ts` exporting a typed `SLIDE_FONTS` as `Record<string, { family: string; package: string; category: 'sans' | 'serif'; weights: number[]; license: string; licenseUrl: string }>`, keyed by display family name. Populate exactly five entries from the CORRECTED RESEARCH table: `Inter` (`@fontsource/inter`, sans, weights [300,400,500,600,700]); `Open Sans` (`@fontsource/open-sans`, sans, [300,400,500,600,700] — 500 IS shipped, corrected in); `Poppins` (`@fontsource/poppins`, sans, [300,400,500,600,700]); `Lora` (`@fontsource/lora`, serif, [400,500,600,700] — no 300); `Source Serif 4` (`@fontsource/source-serif-4`, serif, [300,400,500,600,700] — 300 and 500 ARE shipped, corrected in). Set `license: 'OFL-1.1'` and a `licenseUrl` for every entry, each VERIFIED by opening the installed `node_modules/@fontsource/<package>/LICENSE` file (do not copy the value by analogy to Inter). Do NOT list weights outside the locked 300–700 ramp (no 100/200/800/900). Also export `SLIDE_FONT_FAMILY_NAMES` derived as `Object.keys(SLIDE_FONTS)` (Inter first) for the Settings `<select>`. Record in the eventual SUMMARY the per-package legitimacy verdict (all SUS/too-new, catalog-lockstep false positive) per 46-CONTEXT.md's "record the verdict in the phase SUMMARY" instruction.
    Create `src/config/__tests__/slideFonts.test.ts` asserting: exactly the five expected family keys are present; `Inter` is the first key; every entry's `license` is `'OFL-1.1'` and `licenseUrl` is a non-empty string; `Lora.weights` does NOT include 300; `Open Sans.weights` and `Source Serif 4.weights` DO include 500; no entry lists a weight below 300 or above 700; `SLIDE_FONT_FAMILY_NAMES` equals the registry keys.
  </action>
  <acceptance_criteria>
    - `package.json` lists all five `@fontsource/*` deps at `^5.3.0` (or `5.3.0`) and `package-lock.json` resolves them with integrity hashes.
    - `SLIDE_FONTS` has exactly five keys; each carries a non-empty `licenseUrl` and `license === 'OFL-1.1'` matching the installed package's LICENSE file.
    - `SLIDE_FONTS['Lora'].weights` excludes 300; `SLIDE_FONTS['Open Sans'].weights` and `SLIDE_FONTS['Source Serif 4'].weights` both include 500.
    - No weight outside [300,700] appears in any entry.
    - `npx vitest run src/config/__tests__/slideFonts.test.ts` is green; `npm run type-check` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/config/__tests__/slideFonts.test.ts</automated>
  </verify>
  <done>Five packages installed and pinned; `slideFonts.ts` registry built from the verified table with a per-family LICENSE-checked license; registry test green; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build/bundle | Third-party font packages enter the shipped bundle at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-SC | Tampering | `@fontsource/*` npm installs | high | mitigate | `checkpoint:human-verify` (Task 1) before install verifying fontsource-published/canonical-repo/no-postinstall; pin every package at `5.3.0` in package-lock; record the SUS/too-new verdict + catalog-lockstep rationale in SUMMARY. No package carries a postinstall script (verified in RESEARCH). |
| T-46-SC2 | Spoofing | slopsquat of a font package name | low | accept | Names verified against `github.com/fontsource/font-files` and download counts (104K–2.37M/wk) in RESEARCH; residual risk accepted given canonical-repo match. |
</threat_model>

<verification>
- `npm run type-check` clean (vue-tsc --build, per CLAUDE.md — not `-p tsconfig.app.json`).
- `npx vitest run src/config/__tests__/slideFonts.test.ts` green.
- Full app suite unchanged from the documented 2-file baseline (`src/storage.rules.test.ts`, `RosterView.test.ts`): `npx vitest run`.
</verification>

<success_criteria>
- Five self-hosted @fontsource packages installed and pinned; no runtime Google Fonts dependency added.
- `SLIDE_FONTS` records a package-LICENSE-verified license and the corrected per-family weight ramp for all five families.
- Registry test + type-check green.
</success_criteria>

<output>
Create `.planning/phases/46-global-slide-typography/46-01-SUMMARY.md` when done, recording the per-package legitimacy verdict.
</output>
