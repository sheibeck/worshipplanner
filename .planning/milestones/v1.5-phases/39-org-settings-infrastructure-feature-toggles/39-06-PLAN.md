---
phase: 39-org-settings-infrastructure-feature-toggles
plan: 06
type: execute
wave: 3
depends_on: ["39-03", "39-04", "39-05"]
files_modified:
  - .planning/PENDING-VERIFICATION.md
autonomous: false
requirements: [R073, R088, R089]

must_haves:
  truths:
    - "`npm run type-check` — the `vue-tsc --build` form, which typechecks test files — exits 0 over the completed phase. This is the only type gate that counts; the narrower project-scoped form silently skips tests and let five errors survive two full phases"
    - "The full app suite run with the exact documented command fails in EXACTLY the two known baseline files and no others, so no regression is hiding behind a tooling artifact"
    - "Every one of R073, R088 and R089 has at least one passing automated assertion, and each can be named with the command that runs it"
    - "All five manual backstops from the UI contract are written into `.planning/PENDING-VERIFICATION.md` as the owner's to-do, marked DEFERRED — never marked passed"
    - "The Firestore rules question raised in 39-03's threat model is resolved in writing: either the org-document update rule was confirmed to gate on editor role, or the client-side-only gap is recorded as an open finding"
  artifacts:
    - ".planning/PENDING-VERIFICATION.md — five new Phase 39 items, each marked DEFERRED"
  key_links:
    - "The suite command is load-bearing: `npx vitest run src/` dies on a version mismatch by picking up the render-service tests, and a bare `--dir src` bypasses the config's exclude and runs the rules test without an emulator. A run reporting `src/rules.test.ts` failing is an artifact of the wrong command, not a regression"
    - "Deferring a check is not passing it — the standing autonomy grant permits the first and forbids the second"
---

<objective>
Close Phase 39 with the gates that only make sense over the finished phase: the type check that
actually covers test files, the full suite measured against its documented baseline, per-requirement
traceability, and the disclosure of every manual check this run could not perform.

Purpose: the individual plans each verify their own slice. Nothing before this point runs
`npm run type-check` over the assembled phase or compares the whole suite against the known-failing
baseline — and CLAUDE.md records both as places this project has been burned. It also records the
more important lesson: a test explained away as an environment quirk is an untested assertion, and
that mislabel let a deny-everyone security rule reach production for a full milestone.

Output: a clean type gate, a suite result at baseline, a requirement-to-command map in the SUMMARY,
and five DEFERRED items in `.planning/PENDING-VERIFICATION.md`.

**Binding rule from `.planning/STATE.md` § standing autonomy grant (v1.5, 2026-08-06)** — quoted
here because STATE.md is ~65k tokens and is deliberately not `@`-referenced by any plan in this
phase:

> Do not block on human-verify checkpoints. Record each as DEFERRED in
> `.planning/PENDING-VERIFICATION.md`, then continue. **Never record a deferred check as passed.**
> "Skip the checkpoint" means defer and disclose, not self-approve.

The grant also prohibits deploys. This phase deploys nothing and changes no rules file; it needs no
deploy hand-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-VALIDATION.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-UI-SPEC.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-02-SUMMARY.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-03-SUMMARY.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-04-SUMMARY.md
@.planning/phases/39-org-settings-infrastructure-feature-toggles/39-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Full symbol inventory for Phase 39 (repeated in every plan so any executor sees the whole surface):

| Symbol | Kind | Location | Plan |
|---|---|---|---|
| `Organization` | interface | `src/types/organization.ts` (new) | 39-02 |
| `OrgSettings` | interface | `src/types/organization.ts` (new) | 39-02 |
| `DEFAULT_ORG_SETTINGS` | const | `src/types/organization.ts` (new) | 39-02 |
| `settings` | store ref + export | `src/stores/auth.ts` | 39-02 |
| `isAiEnabled()` | private guard fn | `src/utils/claudeApi.ts` | 39-04 |
| `pcEnabled` | field on `ActionBarContext` | `src/views/serviceEditorActionBar.ts` | 39-05 |
| `aiEnabledInput` / `aiSavedFeedback` / `aiSaveError` | refs | `src/views/SettingsView.vue` | 39-03 |
| `pcEnabledInput` / `pcEnabledSavedFeedback` / `pcEnabledSaveError` | refs | `src/views/SettingsView.vue` | 39-03 |
| `onToggleAiEnabled()` | handler | `src/views/SettingsView.vue` | 39-03 |
| `onTogglePcEnabled()` | handler | `src/views/SettingsView.vue` | 39-03 |
| `src/views/__tests__/SettingsView.test.ts` | test file (new) | — | 39-01 |
| `src/views/__tests__/SongsView.test.ts` | test file (new) | — | 39-01 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — type check, suite baseline, requirement traceability</name>

  <files>(no source files — this task produces the evidence recorded in 39-06-SUMMARY.md)</files>

  <read_first>
    - `CLAUDE.md` §§ "Type-checking", "Testing: two suites" and the known-failing baseline note.
      Both the type command and the suite command are constrained there for reasons this project
      learned the hard way; neither constraint is optional.
    - `39-VALIDATION.md` § Per-Requirement Verification Map — the twelve rows this task maps to
      actual commands and results.
    - The four preceding SUMMARY files, for the per-plan claims this task independently re-runs
      rather than trusts.
  </read_first>

  <action>
Run the type gate with the build form that includes test files. Do not substitute the narrower
project-scoped invocation: it silently skips test files, and five type errors introduced in an
earlier phase survived two full phases behind exactly that substitution. If it reports errors, fix
them in the owning plan's files and re-run; do not narrow the command to make it pass.

Run the full app suite with the exact command from `39-VALIDATION.md`. Do not run the bare-path
variant, which picks up the render-service test by substring match and dies on a Vitest version
mismatch, and do not run the directory form without the exclude, which bypasses the config's
relative exclude and runs the Firestore-rules test without an emulator. A result naming
`src/rules.test.ts` means the wrong command was used — re-run correctly rather than reporting it.

Compare the failing-FILE set against the documented baseline of exactly two files:
`src/storage.rules.test.ts` (two allow-cases, an environment limitation of one cross-service rule,
fixed in production and scheduled for a durable fix in a later phase) and
`src/views/__tests__/RosterView.test.ts` (a stale assertion). Any third failing file is this
phase's regression and must be fixed, not explained. If a failure in one of the two baseline files
looks DIFFERENT from its documented signature — a different test name, a different count — treat it
as a real failure until proven otherwise. That is the one lesson the standing autonomy grant
singles out as not to be repeated.

Build the requirement-to-command map for the SUMMARY: for each of R073, R088 and R089, name the
specific test command that proves it and the result. Every requirement must have at least one
passing automated assertion. If any row cannot be filled with a real command, say so plainly rather
than asserting coverage.

Resolve the Firestore-rules question 39-03's threat model raised: state in the SUMMARY whether the
`organizations/{orgId}` update rule was read and confirmed to gate on editor role, or whether the
gate is client-side only. If the latter, record it as an open finding and add it to
`.planning/PENDING-VERIFICATION.md`. Do not modify `firestore.rules` — rules changes are
deploy-gated and belong to Phases 40 and 41.
  </action>

  <verify>
    <automated>npm run type-check && npx vitest run --dir src --exclude '**/rules.test.ts'</automated>
  </verify>

  <acceptance_criteria>
    - `npm run type-check` exits 0.
    - `npx vitest run --dir src --exclude '**/rules.test.ts'` reports failures in exactly two files:
      `src/storage.rules.test.ts` and `src/views/__tests__/RosterView.test.ts`. Zero other failing
      files.
    - `npx vitest run src/stores/__tests__/auth.test.ts -t "OrgSettings"` exits 0 (R073).
    - `npx vitest run src/utils/__tests__/claudeApi.test.ts -t "aiEnabled"` exits 0 (R088).
    - `npx vitest run src/views/__tests__/serviceEditorActionBar.test.ts -t "pcEnabled"` exits 0
      (R089).
    - `39-06-SUMMARY.md` contains a requirement-to-command table with a real command and a real
      result for each of R073, R088, R089 — no row reading "covered" without a command.
    - `39-06-SUMMARY.md` states the Firestore-rules finding explicitly, either way.
    - `git status --porcelain firestore.rules storage.rules` is empty.
  </acceptance_criteria>

  <done>
Type gate clean under the build form, suite at the documented two-file baseline with no third
failure, all three requirements mapped to passing commands, and the rules question answered in
writing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner verification of the five manual backstops</name>

  <files>.planning/PENDING-VERIFICATION.md</files>

  <action>
Present the five backstop checks below to the owner. If the owner is present, walk them through in
order and record each result. If running under the standing autonomy grant, do not block: write all
five into `.planning/PENDING-VERIFICATION.md` as Phase 39 items marked DEFERRED, phrased as the
owner's to-do with the exact steps given, star the credential-retention item as the one that can
destroy data, and record in `39-06-SUMMARY.md` that verification was deferred rather than passed.
  </action>

  <what-built>
Phase 39 in full: a typed `OrgSettings` shape with one defaults-merge point, the `vwModeEnabled`
storage migration behind a dual-read, an AI Features section and a Planning Center enable toggle on
the Settings screen, the AI guard at `claudeApi.ts`'s module entry point, and hiding at all three
AI affordances and all five Planning Center entry points.

Everything automatable has been automated and is passing. What remains are the five checks
`39-UI-SPEC.md` § UI Considerations classified as backstop — each requires a real browser, a real
Firestore round-trip, or a real legacy organization document, and none can be proven in jsdom.
  </what-built>

  <how-to-verify>
Five checks. The first is the one that can silently destroy data; do it first.

**1. Credential retention across a real off → reload → on cycle (R089).**
   a. Open Settings for an org with Planning Center credentials saved. Confirm the masked
      credential display is visible.
   b. Turn the Planning Center toggle OFF. The credentials block should disappear; the section
      heading and the toggle itself must remain.
   c. **Reload the page.** This step is the whole point — a unit test proves the handler does not
      clear, but only a reload proves the value survived the round-trip.
   d. Turn the toggle back ON.
   e. Expected: the identical masked credential display returns, unchanged. If the credentials are
      gone, STOP — this is data loss, not a cosmetic issue.

**2. Defaults on a genuinely pre-v1.5 organization document (R073).**
   Open Settings as an organization whose document predates v1.5 — a real one, not a fixture.
   Expected: both new toggles render CHECKED, with no blank and no indeterminate checkbox, and
   every other screen behaves normally. This is ROADMAP success criterion 1.

**3. Vertical Worship is not silently flipped (R073).**
   Against an organization with a flat `vwModeEnabled: false` and no nested settings, open Settings.
   Expected: the Vertical Worship toggle still renders UNCHECKED. If it renders checked, the
   dual-read is wrong and a church that deliberately turned Vertical Worship off has had it turned
   back on. There is no error, no log and no failing test for this — the eye is the only detector.

**4. AI feature list does not wrap past two lines (R088).**
   At a standard desktop viewport, Settings → AI Features. Expected: no item in the three-item
   feature list wraps beyond two lines.

**5. Congregational editor button-row reflow (R088).**
   With AI turned off, open a congregational reading editor. Expected: the button row shows two
   buttons instead of three and is not visibly unbalanced, AND hand-dividing a reading works
   identically with the AI button absent. The functional half is the real guarantee.

**If running under the standing autonomy grant:** do not block. Write all five into
`.planning/PENDING-VERIFICATION.md` as Phase 39 items, each marked DEFERRED, phrased as the owner's
to-do with the exact steps above. Item 1 gets a star as the one that can destroy data. Then record
in `39-06-SUMMARY.md` that verification was deferred, not passed — the grant permits deferring and
forbids self-approving.
  </how-to-verify>

  <verify>
    <human-check>All five backstop checks pass, or all five are recorded as DEFERRED in
    `.planning/PENDING-VERIFICATION.md` with the steps above.</human-check>
    <automated>grep -c "Phase 39" .planning/PENDING-VERIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/PENDING-VERIFICATION.md` contains five Phase 39 items, one per backstop check.
    - Each item is marked DEFERRED. Source assertion: no Phase 39 item in that file is marked
      passed, approved, or verified.
    - The credential-retention item is present and flagged as the data-loss risk.
    - `39-06-SUMMARY.md` states that Phase 39's human verification was DEFERRED, not passed.
  </acceptance_criteria>

  <done>
All five backstop checks are either confirmed passing by the owner, or recorded in
`.planning/PENDING-VERIFICATION.md` as Phase 39 items marked DEFERRED with their full steps. No
Phase 39 item in that file reads as passed, approved or verified unless the owner actually ran it.
  </done>

  <resume-signal>Type "approved", describe issues, or — under the standing autonomy grant — record all five as DEFERRED and continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification evidence → project record | The boundary where a claim becomes accepted truth. The failure mode here is not an attacker; it is a deferred or unrunnable check recorded as a passing one. |

## STRIDE Threat Register

Stable IDs shared across all Phase 39 plans. Rows below are in scope for this plan.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-14 | Repudiation | phase verification record (`39-06-SUMMARY.md`, `.planning/PENDING-VERIFICATION.md`) | high | mitigate | Recording a deferred or environment-blocked check as passed is how this project shipped a deny-everyone storage rule and carried it for a full milestone behind a test labelled "not a defect." Mitigated by the standing autonomy grant's explicit prohibition, by requiring each of the five backstops to appear in `PENDING-VERIFICATION.md` marked DEFERRED, and by the acceptance criterion that no Phase 39 item there may read as passed. |
| T-39-15 | Spoofing | the test-suite command itself | medium | mitigate | Two natural ways of scoping the suite produce misleading results: one dies on a version mismatch by pulling in the render-service tests, the other bypasses the config exclude and runs the Firestore-rules test with no emulator. Either can be mistaken for a regression, or — worse — can mask the real two-file baseline. Mitigated by pinning the exact command in Task 1 and by the acceptance criterion enumerating the permitted failing files by name. |
| T-39-01 | Elevation of Privilege | `organizations/{orgId}` settings write path | medium | mitigate | Carried forward from 39-03 for closure: Task 1 requires the Firestore rules question to be answered in writing — confirmed editor-gated, or recorded as a client-side-only finding. It may not be left implicit. |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed by this phase — `39-RESEARCH.md` § Package Legitimacy Audit records "Not applicable." No install task exists in any Phase 39 plan. |
</threat_model>

<verification>
- `npm run type-check` exits 0.
- `npx vitest run --dir src --exclude '**/rules.test.ts'` fails in exactly two files, both named in
  the documented baseline.
- Each of R073, R088, R089 maps to a named, passing command in `39-06-SUMMARY.md`.
- `.planning/PENDING-VERIFICATION.md` carries five Phase 39 items, all DEFERRED.
- `firestore.rules` and `storage.rules` are unmodified.
</verification>

<success_criteria>
Phase 39 is type-clean under the gate that covers test files, the suite sits at its documented
two-file baseline with no new failure, every requirement is traceable to a passing command, the
Firestore-rules question is answered in writing, and all five manual backstops are disclosed as
DEFERRED rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/39-org-settings-infrastructure-feature-toggles/39-06-SUMMARY.md` when done.
</output>
