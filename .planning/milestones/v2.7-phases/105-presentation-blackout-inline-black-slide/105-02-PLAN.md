---
phase: 105-presentation-blackout-inline-black-slide
plan: 02
type: execute
wave: 2
depends_on: [105-01]
files_modified:
  - src/components/slides/SlideCanvas.vue
  - src/components/slides/__tests__/SlideCanvas.test.ts
  - src/components/SongLyricEditor.vue
  - src/components/__tests__/SongLyricEditor.test.ts
  - src/components/slides/SlideCard.vue
  - src/components/slides/__tests__/SlideCard.test.ts
autonomous: true
requirements: [R302, R303, R304]
must_haves:
  truths:
    - "In the Song Lyrics editor a user inserts a black slide via a 'Black Slide' add-chip; it slots into the single-list slide order like any other row (drag/duplicate/remove) and creates NO new service section (R302)."
    - "A blackout row shows distinct chrome (black swatch + 'Solid black — no text or image' caption + 'no text') so it never reads as broken/empty (PITFALLS UX pitfall)."
    - "SlideCanvas renders a blackout slide as pure solid black — no lyric text, no background image, no organizational labels — used by Audience output, Confidence monitor, and the in-app preview (R303); attached audio still plays."
    - "The read-only Slides-tab card shows a BLACKOUT content label, a black preview pane, a 'Solid black' caption, and a 'Black Slide' footer (R303 preview)."
  artifacts:
    - src/components/slides/SlideCanvas.vue
    - src/components/SongLyricEditor.vue
    - src/components/slides/SlideCard.vue
  key_links:
    - "SongLyricEditor 'Black Slide' add-chip → onAddSection('BLACKOUT') → songSectionOrder.addSection blackout kind (105-01)"
    - "SlideCanvas per-kind chain gains a blackout branch that renders nothing (parent's bg-black shows through); currentBackgroundUrl is forced null for a blackout slide so no background image ever paints"
---

<objective>
Build the authoring affordance and the read-only rendering fallout of the blackout content kind:
the "Insert black slide" chip + blackout row in `SongLyricEditor.vue`, the pure-black render branch in
`SlideCanvas.vue` (which serves Audience/Confidence/in-app preview by construction), and the
`SlideCard.vue` grid preview. Depends on 105-01 for the `'blackout'` content kind, the
`addSection('BLACKOUT')` rule, and the display-label helpers.

Purpose: deliver R302 authoring and R303 rendering on the app's existing generic slide pipeline with the
smallest possible per-surface work — one render branch, one editor row branch, one card branch — exactly
as ARCHITECTURE.md ("free by construction") and the UI-SPEC prescribe.
Output: a presenter can insert a black interlude slide in the lyric editor and it projects solid black on
every surface, while remaining a first-class, reorderable/duplicable/removable row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-presentation-blackout-inline-black-slide/105-CONTEXT.md
@.planning/phases/105-presentation-blackout-inline-black-slide/105-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/phases/105-presentation-blackout-inline-black-slide/105-01-SUMMARY.md

# The exact files this plan edits (read before editing):
@src/components/slides/SlideCanvas.vue
@src/components/SongLyricEditor.vue
@src/components/slides/SlideCard.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render a blackout slide as pure black in SlideCanvas</name>
  <files>src/components/slides/SlideCanvas.vue, src/components/slides/__tests__/SlideCanvas.test.ts</files>
  <behavior>
    - For a slide whose contentKind is 'blackout', SlideCanvas renders NO lyric/scripture/text/image/copyright body element (no presentation-body etc.) — the surface is solid black (the parent root's bg-black shows through) with a stable marker element for tests.
    - No background image is painted for a blackout slide even if the assembled slide carries a resolved backgroundImageUrl (R303 "no background image"): currentBackgroundUrl resolves null for a blackout slide, so neither presentation-background nor presentation-background-scrim renders.
    - Attached audio still plays: a blackout slide carrying audioUrl still mounts the audio player (the audio block is not gated on slide kind) — assert presentation-audio renders when audioUrl is present on a blackout slide.
    - Render-pending/render-failed branches are unaffected (blackout carries no renderState).
  </behavior>
  <action>
    In src/components/slides/SlideCanvas.vue: extend the local `CardKind` type to include `'blackout'` (the existing `cardKind()` already returns `slide.contentKind as CardKind` for non-lyric kinds, so blackout flows through automatically). Add a per-kind template branch in the v-else-if chain for `slideKind === 'blackout'` that renders an empty, text-free marker (e.g. a `data-testid="presentation-blackout"` element with `aria-hidden="true"` and no visible text/content) so the parent's existing black background is the entire visible result — matching ARCHITECTURE.md's "render NOTHING inside SlideCanvas's per-kind chain for contentKind 'blackout'". In the `currentBackgroundUrl` computed, add a first-check that returns null when the current slide's contentKind is 'blackout' (ahead of the existing suppressBackground/video checks) so a blackout slide never paints a background image or scrim. Leave the audio/video blocks untouched (they are siblings of the per-kind chain and stay unconditional on kind — a bed track under a black interlude is intended, per UI-SPEC). Add a short comment referencing R303 (solid black, no text/background/label) and the CONTEXT.md decision.
    Extend src/components/slides/__tests__/SlideCanvas.test.ts: a blackout AssembledSlide renders presentation-blackout and NOT presentation-body/presentation-background/presentation-background-scrim; a blackout slide with audioUrl still renders presentation-audio.
  </action>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideCanvas.test.ts && npm run type-check</automated>
  </verify>
  <done>A blackout slide renders solid black (marker present, no body, no background/scrim) and still mounts audio when present; SlideCanvas tests pass; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Insert black slide + blackout row in the Song Lyrics editor</name>
  <files>src/components/SongLyricEditor.vue, src/components/__tests__/SongLyricEditor.test.ts</files>
  <behavior>
    - The "+ Add section" row shows a 7th chip 'Black Slide' (data-testid add-section-chip-blackout) styled identically to the six ADD_SECTION_KINDS chips; clicking it calls onAddSection('BLACKOUT'), appends a blackout row, and auto-expands it (existing add-section behavior).
    - A blackout row (section.kind 'blackout') renders the SAME row card/header structure as a lyric row: position number, drag handle, label chip reading its displayLabel uppercased ('BLACK SLIDE' / 'BLACK SLIDE 2'), Duplicate/Remove (when expanded), expand/collapse chevron — all unchanged.
    - In place of the lyric preview span, a collapsed blackout row shows a small solid-black swatch + the muted caption 'Solid black — no text or image'; in place of the 'N lines' span it shows 'no text'.
    - An expanded blackout row shows a calm placeholder panel (bg-black, border, centered) reading 'This slide renders solid black — no text, background, or label.' — NO textarea and NO slide-split UI (a blackout has no lines to split).
    - Drag-reorder, Duplicate, and Remove work on a blackout row via the existing pure helpers (moveRow/duplicateRow/removeRow) with the existing Remove confirm dialog — no new UI. Inserting a blackout creates NO new service section (it is a LyricSection row in the single-list order).
    - The blackout section's kind field round-trips through the editor's autosave (doAutoSave writes { sections, performanceOrder }) — confirm the persisted section retains kind 'blackout' (a store round-trip test or an autosave-payload assertion).
  </behavior>
  <action>
    In src/components/SongLyricEditor.vue: add a dedicated 'Black Slide' chip button immediately after the `v-for="kind in ADD_SECTION_KINDS"` chip loop inside the add-section-row, styled with the exact same classes as the loop chips (border-gray-700 bg-gray-800 text-gray-300, hover:border-indigo-600 hover:text-indigo-300, rounded-full px-2.5 py-1 text-[11px]), `data-testid="add-section-chip-blackout"`, calling `onAddSection('BLACKOUT')` (onAddSection already forwards its argument to songSectionOrder.addSection — no change to onAddSection needed). Add a computed/helper `isBlackout(row)` returning `row.section.kind === 'blackout'`. In the non-repeat row branch, when isBlackout(row): replace the `row-preview` span with a black swatch (`inline-block h-3 w-3 shrink-0 rounded-sm bg-black border border-gray-700`) followed by the caption `Solid black — no text or image` (reuse the `text-[11.5px] text-gray-400` classes of the span it replaces), and replace the `row-line-count` text with `no text` (keep the same `text-[10.5px] text-gray-500` span). In the expanded-body block, add a branch for `isExpanded(row) && !row.isRepeat && isBlackout(row)` rendering the calm placeholder panel (`rounded-md border border-gray-800 bg-black px-3 py-6 flex items-center justify-center` + `text-[12px] text-gray-500` caption) instead of the textarea+split block; keep the existing textarea branch gated to non-blackout rows. For an expanded REPEATED blackout row, render the same calm placeholder instead of the empty `row-shared-text` box so it never reads as broken. Do NOT change moveRow/duplicateRow/removeRow wiring, the confirm dialog, or Sortable config — a blackout row is a first-class row for all of them. Add short comments referencing R302 (no new service section) and the UI-SPEC Visual Contract.
    Extend src/components/__tests__/SongLyricEditor.test.ts: clicking add-section-chip-blackout inserts a blackout row (kind 'blackout', lines []) and auto-expands it; the collapsed row shows the swatch caption + 'no text' and NO textarea; duplicating and removing a blackout row work via the existing controls; the autosave payload / persisted section retains kind 'blackout'.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/SongLyricEditor.test.ts && npm run type-check</automated>
  </verify>
  <done>The add-chip inserts a first-class blackout row with distinct non-empty chrome and no textarea/split UI; drag/duplicate/remove work unchanged; kind:'blackout' persists through autosave; editor tests pass; type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Blackout preview in the read-only Slides-tab card</name>
  <files>src/components/slides/SlideCard.vue, src/components/slides/__tests__/SlideCard.test.ts</files>
  <behavior>
    - A blackout AssembledSlide's card shows content label 'BLACKOUT' (slide-card-content-label), body caption 'Solid black' (slide-card-body), and footer label 'Black Slide' (slide-card-label) — all from the 105-01 display helpers.
    - The preview pane background is bg-black for a blackout slide only (replacing the default bg-gray-950/40), and the 'Solid black' caption is centered, so the card visually previews what will project.
    - The existing 3-dot menu behavior is unchanged (a blackout slide is lyric-sourced: 'Edit details' / 'Edit in song', no in-card Duplicate/Delete per P-03) — no menu change in this task.
  </behavior>
  <action>
    In src/components/slides/SlideCard.vue: add a computed `isBlackout` (`props.assembledSlide.slide.contentKind === 'blackout'`). Bind the preview pane div's background class conditionally — `bg-black` when isBlackout, otherwise the existing `bg-gray-950/40`. When isBlackout and not render-pending/failed/image, render the existing `slide-card-body` paragraph (it already shows `bodyText` = 'Solid black' via the 105-01 helper) but center it for the blackout case (add `flex items-center justify-center text-center` or equivalent centering, replacing the top-padded left-aligned lyric layout). contentLabel/footerLabel already resolve to BLACKOUT / Black Slide via the helpers — no extra binding needed. Add a short comment referencing the UI-SPEC card contract.
    Extend src/components/slides/__tests__/SlideCard.test.ts: a blackout AssembledSlide card renders slide-card-content-label 'BLACKOUT', slide-card-body 'Solid black', slide-card-label 'Black Slide', and a bg-black preview pane.
  </action>
  <verify>
    <automated>npx vitest run src/components/slides/__tests__/SlideCard.test.ts && npm run type-check</automated>
  </verify>
  <done>The Slides-tab card previews a blackout slide as a black pane with BLACKOUT / Solid black / Black Slide labels; SlideCard tests pass; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org editor → SongLyricEditor autosave → Firestore lyrics doc | An org editor inserts/edits a blackout row; autosave persists the section array. Same boundary as every existing lyric edit — no new network/untrusted input. |
| assembled slide → SlideCanvas render | Read-only render of an already-resolved AssembledSlide; the blackout branch renders no dynamic text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-105-03 | Tampering | SlideCanvas blackout branch | low | mitigate | The blackout branch renders NO text and NO background — currentBackgroundUrl is forced null for a blackout slide so a stale/crafted backgroundImageUrl can never paint behind it; asserted by test (no presentation-background/scrim). |
| T-105-04 | Denial of Service | editor add/duplicate | low | accept | Insert/duplicate reuse the existing bounded pure helpers (addSection/duplicateRow) and the existing autosave path — no new unbounded growth surface beyond ordinary section adds. |
| T-105-NP | Tampering | dependencies | n/a | accept | No npm/pip/cargo installs in this phase — no package-legitimacy surface. |
</threat_model>

<verification>
- `npm run type-check` authoritative (typechecks the extended component tests too).
- Scoped suite: `npx vitest run src/components/slides/__tests__/SlideCanvas.test.ts src/components/__tests__/SongLyricEditor.test.ts src/components/slides/__tests__/SlideCard.test.ts` (never `--dir`).
- In-app preview (PresentationViewer) and both output windows consume SlideCanvas, so the Task 1 branch covers Audience/Confidence/preview by construction — no separate change needed there.
- Print/export (ServicePrintLayout.vue) renders section/song/scripture references only, never slide bodies (ARCHITECTURE.md + verified) — a blackout slide therefore never prints as stray text or a label, satisfying R303's print/export clause with no code change. Confirm this in the SUMMARY (no ServicePrintLayout edit).
</verification>

<success_criteria>
- A user can insert a black slide from the Song Lyrics editor between lyric slides, with no new service section (R302).
- The blackout row has distinct, non-empty chrome and no textarea/split UI, and drag/duplicate/remove work like any other row (R302/R304 editor surface).
- A blackout slide renders solid black — no text, no background, no labels — in SlideCanvas (Audience/Confidence/in-app preview) and previews correctly on the Slides-tab card (R303).
- type-check clean; all three scoped test files pass.
</success_criteria>

<output>
Create `.planning/phases/105-presentation-blackout-inline-black-slide/105-02-SUMMARY.md` when done.
</output>
