---
phase: 21-powerpoint-import-announcements-and-sermon
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/vitest.config.ts
  - functions/package.json
  - functions/src/__fixtures__/README.md
  - functions/src/__fixtures__/corrupted.pptx
  - functions/src/__fixtures__/not-a-pptx.txt
autonomous: false
requirements: [R010]
user_setup:
  - service: powerpoint-fixtures
    why: "Real, diverse .pptx decks are the only faithful input for the phase's central parsing-fidelity risk; Claude cannot author authentic PowerPoint binaries."
    dashboard_config:
      - task: "Export and drop three sample .pptx decks into functions/src/__fixtures__/: text-only.pptx (bullets/headings, no images), image-only.pptx (full-bleed image slides), mixed.pptx (slides mixing a background/logo image with overlaid text)."
        location: "PowerPoint or Google Slides → File → Export/Download → .pptx"
must_haves:
  truths:
    - "The functions/ package can run a Vitest suite in a Node environment (previously it had no test tooling at all)."
    - "officeparser is installed in functions/ ONLY after a human confirms its legitimacy evidence at the blocking checkpoint."
    - "Fixture .pptx inputs (text-only, image-only, mixed, plus a corrupted/non-pptx file) exist under functions/src/__fixtures__/ for the parser tests in 21-04."
  artifacts:
    - "functions/vitest.config.ts — Node-environment Vitest config"
    - "functions/package.json — vitest devDependency, test script, officeparser dependency"
    - "functions/src/__fixtures__/ — fixture decks and error-path files"
  key_links:
    - "The human-verify checkpoint MUST gate the npm install officeparser step; installing before approval violates the package-legitimacy gate (officeparser carries a [SUS] verdict)."
---

<objective>
Stand up the Cloud Functions test harness (functions/ has no Vitest today) and install the chosen PPTX parsing library officeparser — but only behind a blocking human-verify checkpoint, because the package-legitimacy audit assigned officeparser a [SUS] verdict (a false-positive "too-new" signal keyed to its latest version bump, not its 2019 creation date). Also assemble the fixture .pptx decks the parser tests in 21-04 require, including the corrupted/non-pptx error-path inputs.

Purpose: 21-04 (the parsePptx function) cannot compile, install-free, or test without officeparser present, a Vitest config in functions/, and real fixture decks exercising the central fidelity risk (text-only, image-only, mixed, corrupted).
Output: functions/ Vitest config + test script, officeparser installed post-approval, and a populated functions/src/__fixtures__/ directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-powerpoint-import-announcements-and-sermon/21-RESEARCH.md
</context>

<artifacts_this_phase>
Phase 21 produces (this plan marked ✅): functions Vitest infra ✅, officeparser install (gated) ✅, fixture .pptx decks ✅; types + store + assembly (21-01), Storage/Functions bootstrap + storage.rules (21-02), parsePptx + pptxParser.ts / mapAstToSlides (21-04), pptxUpload util + PptxImportModal (21-05), ImportedSlideEditor + ServiceEditorView wiring (21-06).
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: Add the functions/ Vitest harness and fixture scaffold</name>
  <files>functions/vitest.config.ts, functions/package.json, functions/src/__fixtures__/README.md, functions/src/__fixtures__/corrupted.pptx, functions/src/__fixtures__/not-a-pptx.txt</files>
  <read_first>
    - functions/package.json (scripts build/serve/deploy, engines node 22, dependencies firebase-admin/firebase-functions, devDependencies typescript)
    - functions/src/index.ts (existing Cloud Function module compiled by tsc)
    - vitest.config.ts (root — the Node vs jsdom environment convention; functions tests must use node)
    - .planning/phases/21-powerpoint-import-announcements-and-sermon/21-RESEARCH.md (Validation Architecture — Wave 0 Gaps: functions vitest config, test script, fixtures list)
  </read_first>
  <action>
    Create functions/vitest.config.ts defining a Vitest config with a node test environment and a generous testTimeout (parsing real decks is slower than a unit test), including functions/src test files.
    In functions/package.json add a devDependency on vitest (a version compatible with the root ^4 line) and a "test" script running vitest run; leave the existing build/serve/deploy scripts and the node 22 engine intact. Install the vitest devDependency (this is a dev tool, not a runtime package under the officeparser legitimacy gate).
    Create functions/src/__fixtures__/README.md documenting the four expected fixtures and their roles: text-only.pptx, image-only.pptx, mixed.pptx (human-provided per user_setup), and the two error-path files created here.
    Create functions/src/__fixtures__/corrupted.pptx containing bytes that are NOT a valid zip (so its leading bytes are not the PK zip signature) to drive the friendly-error / never-delete path. Create functions/src/__fixtures__/not-a-pptx.txt containing plain text, representing a mis-declared upload.
  </action>
  <acceptance_criteria>
    - functions/vitest.config.ts exists and selects the node environment.
    - functions/package.json declares a vitest devDependency and a test script running vitest run; build/serve/deploy and engines node 22 are unchanged.
    - functions/src/__fixtures__/ contains README.md, corrupted.pptx (not a valid zip — no PK signature at offset 0), and not-a-pptx.txt.
    - cd functions && npx vitest run exits cleanly (zero test files is a pass; it proves the harness boots).
  </acceptance_criteria>
  <verify>
    <automated>cd functions && npx vitest run --passWithNoTests</automated>
  </verify>
  <done>The functions/ Vitest harness runs and the error-path fixtures plus the fixture README exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human-verify officeparser legitimacy before install</name>
  <action>Human reviews the officeparser package-legitimacy evidence (npm creation date 2019, ~585K weekly downloads, MIT license, real GitHub repo at github.com/harshankur/officeParser, node >=18 engines) and confirms the [SUS] "too-new" verdict is a false positive before authorizing npm install officeparser; also confirms the three human-provided fixture decks are in functions/src/__fixtures__/. This checkpoint is blocking and not auto-approvable.</action>
  <what-built>The functions/ Vitest harness and fixture scaffold are in place. The next task installs officeparser, which the package-legitimacy audit flagged [SUS]. No install has run yet.</what-built>
  <how-to-verify>
    Review the officeparser legitimacy evidence before approving the install:
    1. Registry age: `npm view officeparser time.created` should show 2019 (the package predates the "too-new" heuristic that triggered the [SUS] verdict — that signal is keyed to the latest version publish date, ~2026-07-19, not the package's creation date).
    2. Adoption: `npm view officeparser` — weekly downloads ~585K.
    3. Source + license: repository github.com/harshankur/officeParser, license MIT.
    4. Compatibility: `npm view officeparser engines` should allow node >=18 (matches functions node 22) and expose both require and import entrypoints.
    Confirm these match RESEARCH.md's Package Legitimacy Audit. This checkpoint is not auto-approvable.
    Also confirm the three human-provided fixture decks (text-only.pptx, image-only.pptx, mixed.pptx) have been dropped into functions/src/__fixtures__/ per the plan's user_setup, since 21-04's parser tests consume them.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install officeparser`, or describe concerns (a rejection blocks the install and the phase).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install officeparser in functions/ after approval</name>
  <files>functions/package.json</files>
  <read_first>
    - functions/package.json (dependencies block — officeparser is added here, not devDependencies, since the deployed function needs it at runtime)
    - .planning/phases/21-powerpoint-import-announcements-and-sermon/21-RESEARCH.md (Standard Stack officeparser ^7.4.0; Pitfall 3 on its heavy dependency tree)
  </read_first>
  <action>
    Only after the preceding checkpoint is approved, run npm install officeparser from within the functions/ directory so it is recorded as a runtime dependency in functions/package.json (target the ^7.4.0 line). Then verify the package resolves by importing it in a throwaway Node evaluation (require or dynamic import of officeparser) and confirming the module loads without error. Do not set any OCR option anywhere — the deployed function only parses .pptx.
  </action>
  <acceptance_criteria>
    - functions/package.json dependencies includes officeparser at a ^7.x version.
    - officeparser resolves and imports without error from within functions/.
    - The install ran only after the checkpoint approval (no pre-approval install).
  </acceptance_criteria>
  <verify>
    <automated>cd functions && node -e "const o=require('officeparser'); if(!o){process.exit(1)} console.log('officeparser loaded')"</automated>
  </verify>
  <done>officeparser is a recorded runtime dependency of functions/ and loads cleanly, post-approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → functions runtime | A third-party package is pulled into the deployed Cloud Function's dependency tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-SC | Tampering | npm install officeparser | high | mitigate | Blocking, non-auto-approvable human-verify checkpoint before install, presenting the audited evidence (2019 creation, 585K weekly downloads, MIT, real repo) that the [SUS] "too-new" verdict is a false positive. |
| T-21-03-01 | Denial of Service | officeparser transitive deps (tesseract.js, pdfjs-dist) inflating cold start | low | accept | Bundle-weight tradeoff accepted for this phase; mitigated operationally by the memory/timeout caps set on parsePptx in 21-04. OCR is never enabled. |
</threat_model>

<verification>
- cd functions && npx vitest run --passWithNoTests boots the harness.
- Post-approval, officeparser imports cleanly from functions/.
- functions/src/__fixtures__/ holds the error-path files and the human-provided decks.
</verification>

<success_criteria>
functions/ has a working Vitest config and test script; officeparser is installed as a runtime dependency only after the blocking legitimacy checkpoint; the fixture directory is populated for 21-04's parser tests.
</success_criteria>

<output>
Create `.planning/phases/21-powerpoint-import-announcements-and-sermon/21-03-SUMMARY.md` when done.
</output>
