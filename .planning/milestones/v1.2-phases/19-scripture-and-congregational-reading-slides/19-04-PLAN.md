---
phase: 19-scripture-and-congregational-reading-slides
plan: 04
type: tdd
wave: 3
depends_on: [19-01, 19-02]
requirements: [R009, R017, R018]
files_modified:
  - src/components/CongregationalEditor.vue
  - src/components/__tests__/CongregationalEditor.test.ts
autonomous: true
---

<objective>
Deliver congregational reading mode (R009): the `CongregationalEditor` component — same reference/fetch flow, split into alternating Leader/Congregation sections with per-section speaker assignment, a distinct-styled preview, and auto-save.

Purpose: Responsive/liturgical readings need Leader and Congregation parts clearly labeled so the congregation knows when to read aloud. This is the R009 surface.
Output: `src/components/CongregationalEditor.vue` + component tests.
</objective>

<context>
@.planning/phases/19-scripture-and-congregational-reading-slides/19-RESEARCH.md
@src/utils/scriptureSplitter.ts
@src/stores/scriptureSlides.ts
@src/utils/esvApi.ts
@src/composables/useAutoSave.ts
</context>

## Must-Haves

- `CongregationalEditor` component: same reference flow with Leader/Congregation section assignment — component tests pass.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: CongregationalEditor with Leader / Congregation section assignment</name>
  <files>src/components/CongregationalEditor.vue, src/components/__tests__/CongregationalEditor.test.ts</files>
  <behavior>
    - Props: `orgId`, optional `readingId`.
    - Reference input + fetch (same as ScriptureSlideEditor); on fetch, `splitPassage` output is mapped to alternating sections (`idx % 2 === 0 ? LEADER : CONGREGATION`).
    - Each section (`sections-container`) shows a `speaker-toggle-{idx}` button that flips LEADER ↔ CONGREGATION; Leader styled indigo, Congregation amber (distinct per role, R018).
    - A `preview-panel` renders `preview-label-{idx}` (Leader: / Congregation:) + text with distinct styling.
    - Creates a reading with `readingMode: 'congregational'` and `congregationalSections`; auto-save via `useAutoSave` watching sections.
    - Loads an existing reading in `onMounted` (falls back to deriving sections from `slides` when no `congregationalSections`); cleans up in `onUnmounted`.
  </behavior>
  <action>
    RED first: author `src/components/__tests__/CongregationalEditor.test.ts` covering render with speaker toggles, toggle reflects in preview, auto-save on section change, and status transitions. Confirm failure, then implement `CongregationalEditor.vue` to green.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/CongregationalEditor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/CongregationalEditor.test.ts` passes.
    - Component exposes `reference-input`, `fetch-btn`, `sections-container`, `speaker-toggle-{idx}`, and `preview-panel` testids.
    - `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>The congregational editor fetches, assigns Leader/Congregation sections with distinct styling, and auto-saves; its suite is green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/__tests__/CongregationalEditor.test.ts` green.
- `npx vue-tsc --build` green.
</verification>

<output>
Create `.planning/phases/19-scripture-and-congregational-reading-slides/19-04-SUMMARY.md` when done.
</output>
