---
phase: 19-scripture-and-congregational-reading-slides
plan: 03
type: tdd
wave: 3
depends_on: [19-01, 19-02]
requirements: [R008, R017, R018]
files_modified:
  - src/components/ScriptureSlideEditor.vue
  - src/components/__tests__/ScriptureSlideEditor.test.ts
autonomous: true
---

<objective>
Build the main user-facing deliverable for normal scripture slides (R008): the `ScriptureSlideEditor` component — enter a reference, fetch the ESV text, see it auto-split into slide-sized chunks, manually override any slide, and auto-save.

Purpose: A volunteer enters a scripture reference, sees the ESV text auto-fetched and split into slide-sized chunks, can manually override any slide, and edits auto-save. This is the primary R008 surface.
Output: `src/components/ScriptureSlideEditor.vue` + component tests.
</objective>

<context>
@.planning/phases/19-scripture-and-congregational-reading-slides/19-RESEARCH.md
@src/utils/scriptureSplitter.ts
@src/stores/scriptureSlides.ts
@src/utils/esvApi.ts
@src/utils/scripture.ts
@src/composables/useAutoSave.ts
</context>

## Must-Haves

- `ScriptureSlideEditor` component: enter reference → fetch ESV text → auto-split preview → manual override → auto-save — component tests pass.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: ScriptureSlideEditor with ESV fetch, auto-split preview, and auto-save</name>
  <files>src/components/ScriptureSlideEditor.vue, src/components/__tests__/ScriptureSlideEditor.test.ts</files>
  <behavior>
    - Props: `orgId`, optional `readingId`.
    - Reference input (`reference-input`) with a `fetch-btn` gated by `canFetch` (`parseScriptureInput` non-null); on fetch, calls `fetchPassageText`, runs `splitPassage`, and renders slide cards (`slides-container`, `slide-textarea-{idx}`) with verse-range labels.
    - Fetch errors surface via `fetch-error`. Creates a reading on first fetch (or updates an existing one).
    - Manual edits to a slide textarea update local state and track the index in `overriddenSlides` (`Set<number>`).
    - Auto-save via `useAutoSave` watching local slides; status indicator shows pending / Saving... / Saved.
    - Loads an existing reading in `onMounted` when `readingId` is provided; cleans up auto-save + subscription in `onUnmounted`.
  </behavior>
  <action>
    RED first: author `src/components/__tests__/ScriptureSlideEditor.test.ts` covering render (input + fetch button), fetch → slides render, slide edit → override tracked + auto-save fires, status transitions, and the empty/error states. Mock `esvApi`, the store, and `useAutoSave` as needed. Confirm failure, then implement `ScriptureSlideEditor.vue` to green. Dark-first Tailwind styling (R018).
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ScriptureSlideEditor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/ScriptureSlideEditor.test.ts` passes.
    - Component exposes `reference-input`, `fetch-btn`, `fetch-error`, `slides-container`, and per-slide textarea testids.
    - `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>The editor fetches, auto-splits, allows manual override, and auto-saves; its suite is green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/__tests__/ScriptureSlideEditor.test.ts` green.
- `npx vue-tsc --build` green.

Known follow-up (carried from UAT): the overridden-slide tracking exists (`overriddenSlides`) but no conditional CSS class distinguishes an overridden card visually — the visual-distinction sub-requirement of manual override was left unmet at UAT time.
</verification>

<output>
Create `.planning/phases/19-scripture-and-congregational-reading-slides/19-03-SUMMARY.md` when done.
</output>
