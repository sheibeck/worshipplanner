---
phase: 19-scripture-and-congregational-reading-slides
plan: 05
type: tdd
wave: 4
depends_on: [19-03, 19-04]
requirements: [R008, R009, R018]
files_modified:
  - src/views/ServiceEditorView.vue
  - src/types/service.ts
  - src/components/__tests__/ServiceScriptureIntegration.test.ts
autonomous: true
---

<objective>
Wire the two new editors into the existing service editor so users can actually reach scripture slide creation, and add a Normal/Congregational reading-mode toggle on the ScriptureSlot UI.

Purpose: Without wiring the new editors into the existing service editor UI, users have no way to access scripture slide creation. This connects the Phase 19 editors to the existing ScriptureSlot UI in `ServiceEditorView`, completing the user-facing flow and running a full-suite regression check.
Output: `ServiceEditorView.vue` integration, a `readingMode` field on the ScriptureSlot type, and integration tests.
</objective>

<context>
@.planning/phases/19-scripture-and-congregational-reading-slides/19-RESEARCH.md
@src/components/ScriptureSlideEditor.vue
@src/components/CongregationalEditor.vue
@src/views/ServiceEditorView.vue
@src/types/service.ts
</context>

## Must-Haves

- Service editor exposes an "Edit Scripture Slides" affordance on a ScriptureSlot, guarded so it only shows when the reference is complete and the user is an editor.
- Normal/Congregational reading-mode toggle renders the corresponding editor; the chosen mode persists on the slot.
- Full test suite passes with no regressions (`npx vitest run`).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Service editor integration and reading-mode toggle</name>
  <files>src/views/ServiceEditorView.vue, src/types/service.ts, src/components/__tests__/ServiceScriptureIntegration.test.ts</files>
  <behavior>
    - `ScriptureSlot` gains a `readingMode?: 'normal' | 'congregational'` field on the service type.
    - `ServiceEditorView` renders a `reading-mode-toggle` (Normal / Congregational) on a ScriptureSlot; `setReadingMode` updates the slot; `getSlotReadingMode` reads the persisted mode and drives which editor (`ScriptureSlideEditor` vs `CongregationalEditor`) renders.
    - The "Edit Scripture Slides" section is `v-if`-guarded on a complete reference (`slotToScriptureRef(slot)` non-null) AND `authStore.isEditor`.
    - The deep watcher on the local service auto-saves (existing 800ms debounce), so mode + assignments persist across collapse/re-expand.
  </behavior>
  <action>
    RED first: author `src/components/__tests__/ServiceScriptureIntegration.test.ts` covering the toggle switching editors, mode persistence, the empty-reference guard, the viewer-role restriction, and rapid-toggle stability. Confirm failure, then wire `ServiceEditorView.vue` + add the `readingMode` field to green. Finish with a full-suite regression run.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/ServiceScriptureIntegration.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/ServiceScriptureIntegration.test.ts` passes.
    - `npx vitest run` — full suite green (no regressions).
    - Edit button hidden when the reference is incomplete and for non-editor users.
    - `npx vue-tsc --build` green.
  </acceptance_criteria>
  <done>Both editors are reachable from the service editor behind a reading-mode toggle; mode persists; full suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/__tests__/ServiceScriptureIntegration.test.ts` green.
- `npx vitest run` — full suite green.
- `npx vue-tsc --build` green.
</verification>

<output>
Create `.planning/phases/19-scripture-and-congregational-reading-slides/19-05-SUMMARY.md` when done.
</output>
